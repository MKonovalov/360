---
phase: 22-verification-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.ts
  - e2e/auth.setup.ts
  - .gitignore
  - .env.local
autonomous: true
requirements: [VER-02, VER-05]
user_setup:
  - service: clerk
    why: "Playwright e2e authenticates through the real Clerk hosted login flow with a dedicated test staff account (D-22-05 — no cookie-injection stubs)"
    env_vars:
      - name: E2E_CLERK_USER_EMAIL
        source: "Provisioned by the executor via Clerk Backend API (createClerkClient().users.createUser) using the CLERK_SECRET_KEY already in .env.local; written to .env.local (gitignored)"
      - name: E2E_CLERK_USER_PASSWORD
        source: "Generated by the executor at provisioning time; written to .env.local (gitignored). Not needed by clerk.signIn (testing token), kept for deterministic password-auth fallback"
    dashboard_config:
      - task: "Only if Backend API user creation fails: create the test staff account in Clerk Dashboard → Users → Create user with E2E_CLERK_USER_EMAIL"
        location: "Clerk Dashboard → Users"
must_haves:
  truths:
    - "Playwright is a devDependency (D-22-04) with the chromium browser downloaded and a working `npm run e2e` script"
    - "The harness boots the Next dev server itself (webServer config) and reuses an already-running one locally (reuseExistingServer: !process.env.CI)"
    - "The e2e authenticates through the REAL Clerk login flow (clerkSetup + clerk.signIn — D-22-05), never a cookie-injection stub, and persists storageState"
    - "clerkSetup() lives in the project-based setup (e2e/auth.setup.ts), NOT a function globalSetup — the documented Clerk env-propagation pitfall (CLERK_FAPI/CLERK_TESTING_TOKEN) is avoided"
    - "A dedicated Clerk test staff account exists and E2E_CLERK_USER_EMAIL is set in .env.local (operator prerequisite, D-22-05)"
    - "Playwright storageState (real session cookies) is gitignored — e2e/.clerk/ never commits session tokens"
  artifacts:
    - path: "playwright.config.ts"
      provides: "E2E harness: webServer (npm run dev), workers: 1, two projects (auth-setup + chromium with storageState dependency)"
      contains: "webServer"
    - path: "e2e/auth.setup.ts"
      provides: "Real Clerk login setup: clerkSetup() + clerk.signIn + storageState write"
      contains: "clerk.signIn"
    - path: "package.json"
      provides: "e2e script + @playwright/test + @clerk/testing devDependencies"
      contains: "\"e2e\""
    - path: ".gitignore"
      provides: "Excludes Playwright storageState (e2e/.clerk/) from the repo"
      contains: "e2e/.clerk"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/auth.setup.ts"
      via: "project dependency (chromium project dependencies: ['auth-setup'])"
      pattern: "dependencies"
    - from: "e2e/auth.setup.ts"
      to: "e2e/.clerk/user.json"
      via: "storageState write"
      pattern: "storageState"
    - from: "playwright.config.ts"
      to: "npm run dev"
      via: "webServer.command"
      pattern: "npm run dev"
---

<objective>
Stand up the Playwright e2e harness (D-22-04/05): install `@playwright/test@^1.62.1` + `@clerk/testing@^2.2.16` as devDependencies, download chromium, add the `e2e` npm script, create `playwright.config.ts` (webServer auto-start, workers: 1, project-based Clerk auth setup) + `e2e/auth.setup.ts` (REAL Clerk login flow), gitignore the storageState, and provision the dedicated Clerk test staff account (Backend API first; dashboard fallback).

Purpose: VER-02 (live-key Analyze e2e) and VER-05 (browser UAT) both need a Playwright harness that authenticates through the real Clerk hosted login — this plan builds that shared foundation once. The account provisioning is D-22-05's operational prerequisite, resolved here so the spec plans (22-05/22-06) can assume it.
Output: Working `npm run e2e` harness with real Clerk auth; `e2e/.clerk/user.json` storageState produced by a passing `auth-setup` project run; Clerk test staff account existing; `.gitignore` covering session state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-verification-gate/22-CONTEXT.md (D-22-04 Playwright devDep; D-22-05 real Clerk login; Claude's discretion on account mechanics + spec placement)
@.planning/phases/22-verification-gate/22-RESEARCH.md (§Pattern 1 l.178-232 — webServer + project-based setup; §Pitfall 3 l.396-401 — clerkSetup MUST be project-based, never globalSetup; §Pitfall 7 l.420-424 — dotenv load required; §Open Questions 1 l.505-508 — account provisioning; §Package Legitimacy Audit l.101-113 — both packages [OK])
@.planning/phases/22-verification-gate/22-PATTERNS.md (§playwright.config.ts l.184-191; §auth.setup.ts l.194-213; §package.json l.246-257 — --save-dev is REQUIRED, the slopcheck research install was reverted; §.gitignore l.261-267)
vitest.config.ts (harness config analog — defineConfig + dotenv-load precedent at src/scripts/seed.ts:3-12)
package.json (current scripts/devDeps — @playwright/test and @clerk/testing NOT present, verified 2026-08-03)
.gitignore (l.37-45 "Agent tooling / runtime state" section — the e2e/.clerk/ entry lands here)
.env.local (gitignored; contains CLERK_SECRET_KEY — used for Backend API account provisioning)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright + Clerk testing devDeps, download chromium, add e2e script, gitignore storageState</name>
  <files>package.json, package-lock.json, .gitignore</files>
  <read_first>
    package.json (current scripts block + devDependencies — verify @playwright/test/@clerk/testing absent before install)
    .planning/phases/22-verification-gate/22-RESEARCH.md l.91-99 (§Installation — exact commands) + l.99 (slopcheck accidentally wrote to dependencies and was reverted — executor MUST use --save-dev)
    .planning/phases/22-verification-gate/22-PATTERNS.md l.246-257 (package.json modification contract) + l.261-267 (.gitignore contract)
    .gitignore (l.37-45 agent-tooling section — append the storageState entry there, one line per entry)
  </read_first>
  <action>
    From the repo root run: `npm install --save-dev @playwright/test@^1.62.1 @clerk/testing@^2.2.16` — the `--save-dev` flag is MANDATORY (research's slopcheck install wrote them to `dependencies` and was reverted; a repeat would ship test tooling in production deps). Then `npx playwright install chromium` (the browser download D-22-04 explicitly includes).

    Edit `package.json`: add `"e2e": "playwright test"` to `scripts` immediately after the `"test": "vitest run"` line (l.14 area). Verify the two packages landed in `devDependencies` (NOT `dependencies`) — grep the file after install.

    Edit `.gitignore`: append to the "Agent tooling / runtime state" section (l.37-45), matching its one-line-per-entry style:
    `# Playwright e2e auth state (real Clerk session cookies — never commit)`
    `e2e/.clerk/`
    Why-comment not needed in .gitignore (house style: no comments beyond the section headers already present).

    Verify with `git status --porcelain` — package.json + package-lock.json + .gitignore modified; NO yarn.lock introduced (the repo is npm-managed, package-lock.json is the lockfile).
  </action>
  <acceptance_criteria>
    - `grep -v '^#' package.json | grep -c '"@playwright/test"'` → 1 and it appears under `devDependencies` (check the section placement in the file, not just the string)
    - `grep -v '^#' package.json | grep -c '"@clerk/testing"'` → 1 under `devDependencies`
    - `grep -v '^#' package.json | grep -c '"e2e": "playwright test"'` → 1
    - `grep -c "e2e/.clerk" .gitignore` → ≥ 1
    - `test ! -f yarn.lock` (no yarn.lock introduced)
    - `ls node_modules/playwright-core 2>/dev/null || npx playwright --version` succeeds (install intact); chromium installed (`ls ~/Library/Caches/ms-playwright/ 2>/dev/null | grep -i chromium` non-empty on darwin)
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' package.json | grep -c '"@playwright/test"' | grep -qx 1 && grep -v '^#' package.json | grep -c '"@clerk/testing"' | grep -qx 1 && grep -v '^#' package.json | grep -c '"e2e": "playwright test"' | grep -qx 1 && grep -c "e2e/.clerk" .gitignore | grep -qx 1 && test ! -f yarn.lock && npx playwright --version</automated>
  </verify>
  <done>@playwright/test@^1.62.1 + @clerk/testing@^2.2.16 installed as devDependencies (npm, --save-dev), chromium downloaded, `npm run e2e` script present, e2e/.clerk/ gitignored, no yarn.lock.</done>
</task>

<task type="auto">
  <name>Task 2: Create playwright.config.ts + e2e/auth.setup.ts (real Clerk login, project-based setup)</name>
  <files>playwright.config.ts, e2e/auth.setup.ts</files>
  <read_first>
    .planning/phases/22-verification-gate/22-RESEARCH.md §Pattern 1 l.178-232 (the verified config + setup skeleton — copy verbatim) + §Pitfall 3 l.396-401 (project-based setup MANDATORY)
    .planning/phases/22-verification-gate/22-PATTERNS.md §playwright.config.ts l.184-191 + §auth.setup.ts l.194-213 (config contract; storageState path e2e/.clerk/user.json)
    src/scripts/seed.ts l.3-12 (dotenv .env.local load precedent — Playwright does NOT auto-load it)
    vitest.config.ts (defineConfig style analog)
  </read_first>
  <action>
    Create `playwright.config.ts` at the repo root per RESEARCH Pattern 1 (l.184-212), verbatim:
    - `import { defineConfig } from '@playwright/test';` + `import { config } from 'dotenv'; config({ path: '.env.local' });` at the TOP (Pitfall 7 — Playwright does not auto-load .env.local; needed for CLERK keys + DATABASE_URL + E2E_CLERK_USER_EMAIL). Add a why-comment citing seed.ts:12 precedent.
    - `testDir: './e2e'`, `timeout: 60_000` (per-test bumped to 120s in the VER-02 spec for the real 43-50s analyze — Pitfall 2), `workers: 1`, `fullyParallel: false` (Pitfall 5 — live-key runs must never overlap).
    - `webServer: { command: 'npm run dev', url: 'http://localhost:3000', timeout: 120_000, reuseExistingServer: !process.env.CI }` — answers the Claude's-discretion question "gates on dev server or starts it": Playwright starts it, local runs reuse an already-running one.
    - `use: { baseURL: 'http://localhost:3000' }`.
    - Two projects: `{ name: 'auth-setup', testMatch: /auth\.setup\.ts/, testDir: './e2e' }` and `{ name: 'chromium', testIgnore: /auth\.setup\.ts/, use: { storageState: 'e2e/.clerk/user.json' }, dependencies: ['auth-setup'] }` — the dependency edge forces setup-first, serial ordering (Pitfall 3).
    - Why-comment above the projects array: clerkSetup() MUST be project-based — a function globalSetup runs in a separate process and CLERK_FAPI/CLERK_TESTING_TOKEN never propagate, producing "Clerk Frontend API URL is required" (docs-verified pitfall).

    Create `e2e/auth.setup.ts` per RESEARCH Pattern 1 (l.214-230), verbatim:
    - `import { clerkSetup, clerk } from '@clerk/testing/playwright';` + `import { test as setup } from '@playwright/test';` + `import path from 'path';`
    - `setup.describe.configure({ mode: 'serial' });`
    - `setup('global setup', async () => { await clerkSetup(); });` — mints the testing token.
    - `setup('authenticate and save state', async ({ page }) => { await page.goto('/'); await clerk.signIn({ page, emailAddress: process.env.E2E_CLERK_USER_EMAIL! }); await page.waitForURL('**/companies/**'); await page.context().storageState({ path: path.join(__dirname, '.clerk/user.json') }); });` — the waitForURL dashboard redirect proves the REAL auth gate (D-22-05: cookie-injection stubs rejected).
    - The `E2E_CLERK_USER_EMAIL!` non-null assertion is intentional — Task 3 guarantees the var; if it's missing the setup fails loudly rather than silently proceeding.

    Do NOT add a function-based globalSetup anywhere — that is the documented Pitfall 3.
  </action>
  <acceptance_criteria>
    - `playwright.config.ts` contains `webServer`, `reuseExistingServer`, `workers: 1`, `fullyParallel: false`, `storageState: 'e2e/.clerk/user.json'`, and the two-project structure with `dependencies: ['auth-setup']`
    - `playwright.config.ts` contains `config({ path: '.env.local' })` at the top (dotenv load — Pitfall 7)
    - `e2e/auth.setup.ts` contains `clerkSetup()`, `clerk.signIn`, `E2E_CLERK_USER_EMAIL`, and a storageState write to `.clerk/user.json`
    - `npx playwright test --list` exits 0 and lists the auth-setup + chromium projects (config parses)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "webServer" playwright.config.ts && grep -q "storageState" playwright.config.ts && grep -q "clerk.signIn" e2e/auth.setup.ts && grep -q "clerkSetup()" e2e/auth.setup.ts && npx playwright test --list</automated>
  </verify>
  <done>The Playwright harness is configured: webServer auto-start, workers: 1, project-based Clerk auth setup (auth-setup → chromium dependency), storageState at e2e/.clerk/user.json, dotenv .env.local loaded, config parses.</done>
</task>

<task type="auto">
  <name>Task 3: Provision the Clerk test staff account (Backend API first) + smoke-run the auth-setup project</name>
  <files>.env.local</files>
  <read_first>
    .planning/phases/22-verification-gate/22-RESEARCH.md §Open Questions 1 l.505-508 (account provisioning mechanics: Backend API `createClerkClient().users.createUser` OR dashboard) + §Environment Availability l.548 (blocks VER-02 + VER-05 until provisioned)
    .planning/phases/22-verification-gate/22-CONTEXT.md (D-22-05 — dedicated test staff account, real Clerk login; Claude's discretion on account mechanics)
    node_modules/@clerk/backend (createClerkClient export verified at dist/index.d.ts:26 — importable in a tsx script)
  </read_first>
  <action>
    Provision the dedicated Clerk test staff account (D-22-05) — automation-first, Backend API:

    1. Check `.env.local` for an existing `E2E_CLERK_USER_EMAIL`; if present, verify the account exists via Backend API (getUserList filter) and skip creation.
    2. If absent: write a small tsx script (e.g. `scripts/provision-clerk-test-user.ts`, or a `node -e`/tsx one-liner) that dotenv-loads `.env.local`, then uses `createClerkClient({ secretKey: process.env.CLERK_SECRET_KEY })` from `@clerk/backend` to call `users.createUser({ emailAddress: <E2E_CLERK_USER_EMAIL>, password: <generated>, firstName: 'E2E', lastName: 'TestStaff' })`. Use a deterministic email like `e2e-staff@arclumenpartners.com` and generate the password with `node -e "console.log(require('crypto').randomBytes(16).toString('hex') + 'Aa1!')"` (Clerk password policy-compliant). Write `E2E_CLERK_USER_EMAIL` and `E2E_CLERK_USER_PASSWORD` into `.env.local` (gitignored — Security Domain: test credentials never committed).
    3. If `users.createUser` fails (e.g., instance policy blocks API creation): escalate with a blocking checkpoint:human-verify — the operator creates the account in Clerk Dashboard → Users → Create user with the same email, sets E2E_CLERK_USER_EMAIL (+ password) in .env.local, and confirms. Do NOT proceed to the smoke run until the account exists.
    4. Smoke-run the harness: `npx playwright test --project=auth-setup` (webServer auto-starts `npm run dev`). It must complete green and produce `e2e/.clerk/user.json` — proving the real Clerk login flow works end-to-end (D-22-05). If the smoke fails on "Clerk Frontend API URL is required", the setup is in a function globalSetup — it isn't (Task 2 used project-based setup); debug the env propagation per the verified pitfall.

    Cleanup: delete the provisioning script after the account exists (one-off, not committed tooling) — or keep it only if the repo already has a scripts/ provisioning convention (it does not; refresh-model-catalog.ts and the probe are the only scripts/ residents).
  </action>
  <acceptance_criteria>
    - `.env.local` contains `E2E_CLERK_USER_EMAIL=<email>` (gitignored — never committed)
    - Backend API `users.getUserList({ emailAddress: [E2E_CLERK_USER_EMAIL] })` returns the account (or the operator-created account verifies)
    - `npx playwright test --project=auth-setup` exits 0 and `e2e/.clerk/user.json` exists and is non-empty (contains real session cookies)
    - `git status --porcelain e2e/.clerk/` is empty (storageState gitignored — no session tokens on disk-visible state)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "^E2E_CLERK_USER_EMAIL=" .env.local && npx playwright test --project=auth-setup && test -s e2e/.clerk/user.json && test -z "$(git status --porcelain e2e/.clerk/)"</automated>
  </verify>
  <done>The dedicated Clerk test staff account exists (Backend API provisioned or operator-created), E2E_CLERK_USER_EMAIL is in .env.local, and the auth-setup project signs in through the real Clerk hosted flow, producing the gitignored storageState the spec plans consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Two new devDependencies (@playwright/test, @clerk/testing) cross into the repo — both slopcheck-[OK], both devDependencies only (D-22-04) |
| Playwright harness → Clerk auth | The e2e authenticates through Clerk's REAL sign-in infrastructure with a testing token (D-22-05) — a real auth path, not a stub |
| e2e/.clerk/ storageState | Real Clerk session cookies on disk — must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-SC | Tampering | npm install of @playwright/test + @clerk/testing | mitigate | Package Legitimacy Audit passed (RESEARCH l.101-113): both [OK] via slopcheck, official repos (microsoft/playwright, clerk/clerk), high downloads. `[OK]` disposition requires NO blocking human checkpoint. Both installed with `--save-dev` (never dependencies — the slopcheck research run was reverted). `npx playwright install chromium` is the official browser download |
| T-22-04 | Information Disclosure | e2e/.clerk/user.json (real Clerk session cookies) | mitigate | .gitignore entry `e2e/.clerk/` (Task 1) + Task 3's `git status --porcelain e2e/.clerk/` empty gate — session state can never be committed |
| T-22-05 | Information Disclosure | test credentials (E2E_CLERK_USER_EMAIL/E2E_CLERK_USER_PASSWORD) | mitigate | Written to .env.local only (gitignored by the existing `.env.*` rule); never in any committed file; the provisioning script is deleted after use |
| T-22-06 | Spoofing | e2e auth via cookie-injection stub | mitigate | D-22-05 mandates the real Clerk login — clerkSetup + clerk.signIn through Clerk's actual sign-in infrastructure; the auth-setup smoke (Task 3) proves the real gate (waitForURL '**/companies/**' after sign-in) |

## Package Legitimacy

| Package | Registry | Disposition |
|---------|----------|-------------|
| @playwright/test@^1.62.1 | npm | [OK] — Approved devDependency (RESEARCH audit 2026-08-03) |
| @clerk/testing@^2.2.16 | npm | [OK] — Approved devDependency (RESEARCH audit 2026-08-03) |

No [ASSUMED]/[SUS]/[SLOP] items — no legitimacy checkpoint required.
</threat_model>

<verification>
1. `npx playwright test --project=auth-setup` → green, produces e2e/.clerk/user.json (the real Clerk login gate works)
2. `npx playwright test --list` → config parses, both projects listed
3. `git status --porcelain e2e/.clerk/` → empty (storageState gitignored)
4. `npm run e2e` with no specs yet → exits 0 (harness runs; specs arrive in plans 22-05/22-06)
</verification>

<success_criteria>
- @playwright/test + @clerk/testing installed as devDependencies with chromium downloaded and the `e2e` npm script wired (D-22-04)
- Real Clerk login e2e works end-to-end (D-22-05) — the auth-setup project signs in through Clerk's actual infrastructure and persists a gitignored storageState
- The dedicated Clerk test staff account exists (operator prerequisite resolved) and E2E_CLERK_USER_EMAIL is in .env.local
- The harness answers the Claude's-discretion items: Playwright starts the dev server itself (reuseExistingServer locally), project-based setup (never globalSetup), storageState at e2e/.clerk/user.json
</success_criteria>

<output>
Create `.planning/phases/22-verification-gate/22-03-SUMMARY.md` when done
</output>
