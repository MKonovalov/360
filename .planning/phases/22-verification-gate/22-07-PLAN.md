---
phase: 22-verification-gate
plan: 07
type: execute
wave: 4
depends_on: [22-01, 22-02, 22-03, 22-04, 22-05, 22-06]
files_modified:
  - .planning/phases/22-verification-gate/22-VERIFICATION.md
  - .planning/phases/22-verification-gate/22-HUMAN-UAT.md
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05]
must_haves:
  truths:
    - "22-VERIFICATION.md records all five success criteria with evidence from the executed plans — per the 19/20/21 VERIFICATION.md conventions (frontmatter + goal-achievement truth table)"
    - "Every live-key proof records status/JSON shapes only — never key values (Security Domain)"
    - "22-HUMAN-UAT.md carries the items that remain genuinely human (IN-02 stale-primary badge observation follow-up, IN-03 billing ERROR_COPY row observation, any live-key evidence the operator may want to re-run)"
    - "The named verification-matrix section (D-22-06) is consolidated — the VER-01 audit maps each locked cell to its test file/line"
  artifacts:
    - path: ".planning/phases/22-verification-gate/22-VERIFICATION.md"
      provides: "Phase 22 proof record: 5/5 success criteria with evidence"
      contains: "VER-02"
    - path: ".planning/phases/22-verification-gate/22-HUMAN-UAT.md"
      provides: "Human-UAT entries for the phase's manual-only items"
      contains: "IN-02"
  key_links:
    - from: "22-VERIFICATION.md"
      to: "src/lib/agents/runAgent.test.ts"
      via: "VER-01 evidence (isOpenRouterPlatformRateLimit direct tests)"
      pattern: "runAgent.test.ts"
    - from: "22-VERIFICATION.md"
      to: "e2e/ver-02-analyze.spec.ts"
      via: "VER-02 live-run evidence (201 + modelUsed verbatim)"
      pattern: "ver-02-analyze"
---

<objective>
Record the phase's proofs (per the 19/20/21 conventions): write `22-VERIFICATION.md` mapping all five success criteria to the executed evidence (test commands, live-run shapes, DB read-backs, browser observations) and `22-HUMAN-UAT.md` for the genuinely-human items (IN-02 stale-primary badge follow-up, IN-03 billing ERROR_COPY observation, live-key re-run consent). This is the milestone's final verification-gate deliverable — it closes v1.4.

Purpose: The phase's five proofs mean nothing unless they're recorded durably with evidence anyone can re-run. This plan consolidates the audit (VER-01 matrix cell map), the live-run evidence shapes (VER-02/03, never key values), the security-gate result (VER-04), and the browser observations (VER-05 + IN-02/IN-03 carries) into the standard VERIFICATION/HUMAN-UAT artifacts.
Output: `22-VERIFICATION.md` (5/5 success criteria with evidence) + `22-HUMAN-UAT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-provider-registry-servable-model-source/19-VERIFICATION.md + 19-HUMAN-UAT.md (proof-recording format precedent)
@.planning/phases/20-cross-provider-run-path/20-VERIFICATION.md (evidence-convention precedent)
@.planning/phases/21-settings-ui/21-VERIFICATION.md (22/22 must-have truth-table format precedent)
@.planning/phases/21-settings-ui/21-REVIEW.md (IN-02 stale-primary badge guess — Phase 22 observation carry; IN-03 billing ERROR_COPY row)
.planning/phases/22-verification-gate/22-01-SUMMARY.md (VER-01 audit: cell → test → line map)
.planning/phases/22-verification-gate/22-02-SUMMARY.md (VER-04 gate green)
.planning/phases/22-verification-gate/22-03-SUMMARY.md (harness + account provisioning)
.planning/phases/22-verification-gate/22-04-SUMMARY.md (VER-03 child-env evidence shapes)
.planning/phases/22-verification-gate/22-05-SUMMARY.md (VER-02 live-run evidence shapes)
.planning/phases/22-verification-gate/22-06-SUMMARY.md (VER-05 browser observations + IN-02)
.planning/STATE.md (v1.4 milestone framing; Deferred Items section)
.planning/ROADMAP.md (Phase 22 success criteria — the five must-be-TRUE claims)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 22-VERIFICATION.md — 5/5 success criteria with evidence shapes</name>
  <files>.planning/phases/22-verification-gate/22-VERIFICATION.md</files>
  <read_first>
    .planning/phases/21-settings-ui/21-VERIFICATION.md (the format precedent: frontmatter with status/score, Observable Truths table with evidence column, Deferred Items)
    .planning/phases/22-verification-gate/22-01-SUMMARY.md through 22-06-SUMMARY.md (the evidence to record — read each once, extract the shapes)
    .planning/ROADMAP.md §Phase 22 (the five success criteria — the truths being proven)
    .planning/STATE.md (Deferred Items — human_needed verification carries from v1.3)
  </read_first>
  <action>
    Create `.planning/phases/22-verification-gate/22-VERIFICATION.md` following the 21-VERIFICATION.md format (frontmatter: phase/verified/status/score; then a Goal Achievement table — one row per truth, with Evidence column; then Deferred Items):

    - Frontmatter: `status: passed` (all five automated gates green), `score: 5/5 success criteria verified`, `verified: <today ISO>`.
    - **VER-01 truth row**: the three matrices locked — evidence = the audit map from 22-01-SUMMARY (collision canaries at catalog.test.ts:186-192 audited untouched; 4-cell hop at modelConfig.test.ts:135-161; error classes at modelConfig.test.ts:56-77; PLUS the two closed gaps: `isOpenRouterPlatformRateLimit` direct tests in runAgent.test.ts (VER-01 gap describe) and the statusCode-200 → 'input' pin in modelConfig.test.ts). Include the WR-01 comment-site verification result (modelConfig.ts/runAgent.ts already say 'input'). Command: `npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelConfig.test.ts src/lib/agents/runAgent.test.ts` green.
    - **VER-02 truth row**: evidence = 22-05-SUMMARY live-run shapes — 201 status, `body.modelUsed === 'anthropic/claude-sonnet-4.6'`, `getRunById(id).modelUsed === 'anthropic/claude-sonnet-4.6'`, run id, save path used (UI vs direct action). NEVER the key value, never full response bodies.
    - **VER-03 truth row**: evidence = 22-04-SUMMARY — child-env spawn with ANTHROPIC_API_KEY stripped, `out.ok === true`, `out.modelUsed === 'anthropic/claude-sonnet-4.6'`, skip-guard behavior noted (skipped vs run — record which).
    - **VER-04 truth row**: evidence = 22-02-SUMMARY — the security-grep Vitest gate green under `npm test`; baseline restated (OPENROUTER in exactly 3 non-test server files: env.ts, modelFactory.ts, analyzeCompany.ts); canary verified non-vacuous.
    - **VER-05 truth row**: evidence = 22-06-SUMMARY — the four browser behaviors observed green + the IN-02 observation note (what rendered for a stale primary).
    - **Deferred Items**: IN-03 billing ERROR_COPY row (no 'billing' entry in analyze-run-status.tsx ERROR_COPY — Phase 20 carry; recorded as a HUMAN-UAT observation, NOT this phase's scope per CONTEXT Open Question 4), plus the v1.3 human_needed VERIFICATION carries (unchanged).
    - Security-Domain rule: every live-key evidence entry records shapes only — a `sk-or-` grep gate on this file is the final check.

    Do NOT invent evidence — every claim must trace to a 22-0X-SUMMARY.md value. If a summary is missing a value, re-run the targeted command from the plan that produced it.
  </action>
  <acceptance_criteria>
    - `22-VERIFICATION.md` exists with frontmatter (`status: passed`) and a Goal Achievement table with ≥ 5 truth rows (one per VER requirement)
    - The file references `runAgent.test.ts`, `modelConfig.test.ts`, `ver-02-analyze.spec.ts`, `ver-05-settings.spec.ts`, `security-grep.test.ts`, and `openrouter-only-chain.test.ts` (the evidence surfaces)
    - Contains the verbatim modelUsed evidence string `'anthropic/claude-sonnet-4.6'` for the VER-02/03 rows
    - `grep -c "sk-or-" 22-VERIFICATION.md` → 0 (no key values in the record)
    - The VER-01 row names the D-22-06 named verification-matrix consolidation (the audit map)
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/22-verification-gate/22-VERIFICATION.md && grep -q "status: passed" .planning/phases/22-verification-gate/22-VERIFICATION.md && grep -q "ver-02-analyze.spec.ts" .planning/phases/22-verification-gate/22-VERIFICATION.md && grep -q "anthropic/claude-sonnet-4.6" .planning/phases/22-verification-gate/22-VERIFICATION.md && ! grep -q "sk-or-" .planning/phases/22-verification-gate/22-VERIFICATION.md</automated>
  </verify>
  <done>22-VERIFICATION.md records all five success criteria with evidence traced to the plan summaries — matrices locked, live proofs with shape-only evidence, security gate green, browser observations captured — zero key values in the record.</done>
</task>

<task type="auto">
  <name>Task 2: Write 22-HUMAN-UAT.md — the genuinely-human items and observations</name>
  <files>.planning/phases/22-verification-gate/22-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/19-provider-registry-servable-model-source/19-HUMAN-UAT.md (the format: frontmatter status/source/started, Current Test, Tests table with expected/result, Summary)
    .planning/phases/21-settings-ui/21-REVIEW.md (IN-02 stale-primary badge guess; IN-03 billing ERROR_COPY row)
    .planning/phases/22-verification-gate/22-05-SUMMARY.md (any IN-03 observation from the live run)
    .planning/phases/22-verification-gate/22-06-SUMMARY.md (the IN-02 observation result)
    .planning/STATE.md (Deferred Items — human_needed carries)
  </read_first>
  <action>
    Create `.planning/phases/22-verification-gate/22-HUMAN-UAT.md` per the 19-HUMAN-UAT.md format (frontmatter `status: pending` — these items await human action/confirmation; Tests table with expected + result fields):

    - **Item 1 — IN-02 stale-primary badge observation (21-REVIEW carry)**: expected — record what the Settings UI renders when a saved primary is stale (dropped from the union servable set): does the trigger badge guess the provider correctly or fall back to the raw id? result: the 22-06-SUMMARY observation (what the spec observed); if the observation is inconclusive, mark the item for a human browser check with steps (open /settings, save a stale chain via DB, observe the trigger).
    - **Item 2 — IN-03 billing ERROR_COPY row (Phase 20 carry)**: expected — the analyze-run-status.tsx ERROR_COPY map has no 'billing' row, so a 402 renders the generic "The analysis failed"; record whether the VER-02 live run hit 402 (it should not on a healthy credited key — expected result: not observed, carried as a gap-closure candidate, NOT this phase's scope per CONTEXT Open Question 4).
    - **Item 3 — live-key re-run consent (VER-02/03)**: expected — the operator confirms the ~cents credit spend is acceptable for any re-runs of the live proofs; result: [pending] (this phase's approved spend is D-22-01; re-runs need consent).
    - **Item 4 — v1.3 human_needed VERIFICATION carries** (from STATE.md Deferred Items: 01/02/03/04-VERIFICATION.md still human_needed): expected — unchanged by this phase; result: [pending] (out of scope, carried).
    - **Summary block**: total / passed / issues / pending / skipped / blocked counts.

    Each item's `result` field starts as `[pending]` except where a 22-0X-SUMMARY provides a recorded observation (fill those in with the observation text + source summary reference). Do NOT mark any human item passed — only the operator's later action closes these.
  </action>
  <acceptance_criteria>
    - `22-HUMAN-UAT.md` exists with frontmatter (`status: pending`) and a Tests table containing at least the IN-02 and IN-03 items with `expected` + `result: [pending]` (or recorded observation + source) fields
    - The Summary block has the total/passed/pending counts
    - `grep -c "sk-or-" 22-HUMAN-UAT.md` → 0 (no key values)
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/22-verification-gate/22-HUMAN-UAT.md && grep -q "IN-02" .planning/phases/22-verification-gate/22-HUMAN-UAT.md && grep -q "IN-03" .planning/phases/22-verification-gate/22-HUMAN-UAT.md && ! grep -q "sk-or-" .planning/phases/22-verification-gate/22-HUMAN-UAT.md</automated>
  </verify>
  <done>22-HUMAN-UAT.md captures the genuinely-human items: the IN-02 stale-primary badge observation, the IN-03 billing ERROR_COPY observation, live-key re-run consent, and the v1.3 human_needed carries — each with expected/result fields and no key values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence documents → repo | VERIFICATION/HUMAN-UAT files are committed — any leaked key value would persist in git history forever |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information Disclosure | live-key evidence in committed VERIFICATION/HUMAN-UAT files | mitigate | Security-Domain rule enforced by acceptance greps: `! grep "sk-or-"` on both files — evidence records status/JSON shapes only, never key values (RESEARCH Security Domain l.619) |
| T-22-16 | Repudiation | evidence not traceable to reproducible commands | mitigate | Every truth row cites the producing plan's SUMMARY + the re-runnable command; no invented evidence — if a summary lacks a value, the executor re-runs the targeted command from the originating plan |
| T-22-SC | Tampering | npm installs | accept | No installs in this plan |

## Package Legitimacy

No packages installed by this plan.
</threat_model>

<verification>
1. `test -s 22-VERIFICATION.md` + frontmatter `status: passed` + all five VER rows present
2. `grep -c "sk-or-" 22-VERIFICATION.md 22-HUMAN-UAT.md` → 0 (zero key leakage into committed evidence)
3. Every evidence claim traces to a 22-0X-SUMMARY.md value (cross-check Task 1 acceptance)
4. HUMAN-UAT items are `[pending]` or carry a recorded observation — never falsely marked passed
</verification>

<success_criteria>
- The milestone's five correctness claims are recorded durably: matrices locked (VER-01), live OpenRouter-primary E2E proof (VER-02), key-isolation proof (VER-03), security-gate green (VER-04), browser behaviors proven (VER-05)
- The D-22-06 named verification-matrix consolidation is captured in the VER-01 row
- All genuinely-human items (IN-02/IN-03 observations, live-key re-run consent, v1.3 carries) live in 22-HUMAN-UAT.md
- No key values anywhere in the committed evidence — v1.4's verification gate closes clean
</success_criteria>

<output>
Create `.planning/phases/22-verification-gate/22-07-SUMMARY.md` when done
</output>
