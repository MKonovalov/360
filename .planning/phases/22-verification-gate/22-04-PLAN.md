---
phase: 22-verification-gate
plan: 04
type: execute
wave: 2
depends_on: [22-03]
files_modified:
  - scripts/probe-openrouter-only.ts
  - src/lib/agents/openrouter-only-chain.test.ts
autonomous: true
requirements: [VER-03]
user_setup:
  - service: openrouter
    why: "VER-03 proves an OpenRouter-only chain runs with ONLY the OpenRouter key — the child-env test spawns a real analyzeCompany against a live OpenRouter model (~cents credits)"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "Already in .env.local (verified present 2026-08-03). The test skip-guards when absent; the child env strips ANTHROPIC_API_KEY"
    dashboard_config: []
must_haves:
  truths:
    - "A child process runs analyzeCompany with OPENROUTER_API_KEY set and ANTHROPIC_API_KEY unset (empty string) in the CHILD env only — the parent env is never mutated (D-22-03)"
    - "The child probe succeeds and reports ok: true with modelUsed === the saved OpenRouter slug 'anthropic/claude-sonnet-4.6' (verbatim, FAL-05)"
    - "The test skips gracefully when live keys are absent (describe.skipIf guard — CI-safe, Claude's discretion)"
    - "The probe dotenv-loads .env.local, resolves the seeded test company BY NAME (ids are unstable across seed runs — Pitfall 4), sets a *.test domain, upserts OpenRouter-only settings, and runs analyzeCompany via the real query layer"
    - "The probe never prints env/key values — evidence records shapes ({ ok, modelUsed, modelChain }) only (Security Domain)"
    - "The test timeout is 120s — vitest's 5s default would kill the real 43-50s run (Pitfall 1)"
  artifacts:
    - path: "scripts/probe-openrouter-only.ts"
      provides: "VER-03 child probe: dotenv load, company-by-name lookup, test-domain set, OR-only settings upsert, analyzeCompany, JSON out"
      contains: "analyzeCompany"
    - path: "src/lib/agents/openrouter-only-chain.test.ts"
      provides: "Child-env integration test (spawnSync + stripped ANTHROPIC_API_KEY + skip guard)"
      contains: "spawnSync"
  key_links:
    - from: "src/lib/agents/openrouter-only-chain.test.ts"
      to: "scripts/probe-openrouter-only.ts"
      via: "spawnSync(process.execPath, [require.resolve('tsx/cli'), 'scripts/probe-openrouter-only.ts'])"
      pattern: "probe-openrouter-only"
    - from: "scripts/probe-openrouter-only.ts"
      to: "src/lib/agents/analyzeCompany.ts"
      via: "dynamic import after dotenv load"
      pattern: "analyzeCompany"
    - from: "scripts/probe-openrouter-only.ts"
      to: "src/lib/db/queries/companies.ts"
      via: "getCompanyByName"
      pattern: "getCompanyByName"
---

<objective>
Prove VER-03 (D-22-03): an OpenRouter-only chain runs successfully with ONLY `OPENROUTER_API_KEY` set — no Anthropic key required. A node-env Vitest test spawns a tsx child process (the probe script) with `ANTHROPIC_API_KEY` stripped to an empty string in the child env, and the probe runs a REAL analyzeCompany against the seeded test company with OpenRouter-only settings, printing `{ ok, modelUsed, modelChain }` as JSON for the parent to assert.

Purpose: Phase 20's unit coverage proves the gate logic with mocked env (`analyzeCompany.test.ts:331-344`); VER-03 is the STRUCTURAL proof — real key isolation in a child process, without mutating the developer's real env (D-22-03 rejects the "temporarily unset ANTHROPIC_API_KEY in the shell" approach).
Output: `scripts/probe-openrouter-only.ts` + `src/lib/agents/openrouter-only-chain.test.ts` (auto-discovered by vitest's include glob), both skip-guarded for CI, green on the live local environment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-verification-gate/22-CONTEXT.md (D-22-03 child-env proof; Claude's discretion on the exact skip guard; seeded test company identity)
@.planning/phases/22-verification-gate/22-RESEARCH.md (§Pattern 2 l.234-270 — the verified test skeleton; §VER-03 row l.47; §Pitfall 1 l.384-388 — 120s timeout; §Pitfall 4 l.402-407 — company lookup BY NAME; §Anti-Patterns l.364-368 — never mutate parent env)
@.planning/phases/22-verification-gate/22-PATTERNS.md (§openrouter-only-chain.test.ts l.110-143 — verbatim test shape; §probe-openrouter-only.ts l.146-180 — header/placement/main shape + query-layer signatures)
scripts/refresh-model-catalog.ts (repo-root scripts/ precedent — why-comment header, main().then/catch exit shape)
src/scripts/seed.ts (l.1-12 dotenv + dynamic-import pattern — ESM hoisting why-comment)
src/lib/db/queries/companies.ts (getCompanyByName l.54-57; domain column)
src/lib/db/queries/userModelSettings.ts (upsertModelSettings l.18-33)
src/lib/agents/analyzeCompany.ts (analyzeCompany(companyId, userId) l.65 — the run entry)
src/lib/db/schema.ts (company table — domain column, unique)
data/seed/companies.csv ('Acme Test Co' — the committed seeded test company)
@.planning/phases/22-verification-gate/22-03-PLAN.md (Task 3 provisioned E2E_CLERK_USER_EMAIL — the probe resolves the Clerk userId from it)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the VER-03 child probe script (dotenv, company-by-name + test-domain, OR-only settings, analyzeCompany, JSON out)</name>
  <files>scripts/probe-openrouter-only.ts</files>
  <read_first>
    scripts/refresh-model-catalog.ts (l.1-15 header + placement why-comment precedent; l.157-200 main()/exit shape)
    src/scripts/seed.ts (l.1-12 dotenv + dynamic-import why-comment — copy the structure EXACTLY)
    src/lib/db/queries/companies.ts (getCompanyByName l.54-57 — name lookup because seed ids drift)
    src/lib/db/queries/userModelSettings.ts (upsertModelSettings l.18-33 — atomic full-value upsert keyed on userId)
    src/lib/agents/analyzeCompany.ts (l.65 analyzeCompany(companyId, userId) — the run entry; env gate at entry)
    src/lib/db/schema.ts (company table — domain: text('domain').unique() — the .test domain set)
    .planning/phases/22-verification-gate/22-PATTERNS.md §probe-openrouter-only.ts l.146-180 (header contract + query-layer signatures)
    node_modules/@clerk/backend/dist/index.d.ts (createClerkClient l.26 — userId lookup from E2E_CLERK_USER_EMAIL)
  </read_first>
  <action>
    Create `scripts/probe-openrouter-only.ts` (repo-root scripts/ — NOT src/scripts/: the Phase 18 gate greps zero `child_process` in src/, and this script is only ever spawned by the vitest child-env test, never by hand — why-comment this placement). Shape per PATTERNS l.146-180:

    - Header why-comment block: purpose (VER-03 child-env probe), the run command it is spawned under (tsx, by openrouter-only-chain.test.ts), and the placement rationale.
    - `import { config } from 'dotenv'; config({ path: '.env.local' });` FIRST (seed.ts:12 precedent — tsx does not auto-load .env.local; must load before anything transitively importing src/lib/env.ts).
    - Inside `main()` (ESM import hoisting — seed.ts:5-11 why-comment): dynamic-import `analyzeCompany` from '../src/lib/agents/analyzeCompany', `getCompanyByName` from '../src/lib/db/queries/companies', `upsertModelSettings` from '../src/lib/db/queries/userModelSettings', `db` + `company` schema pieces + `eq` from drizzle for the domain set, and `createClerkClient` from '@clerk/backend'.
    - Resolve the Clerk userId: `createClerkClient({ secretKey: process.env.CLERK_SECRET_KEY }).users.getUserList({ emailAddress: [process.env.E2E_CLERK_USER_EMAIL!] })` → `users[0].id`. If E2E_CLERK_USER_EMAIL is missing or the lookup returns zero users, exit(1) with a clear message (provision it per plan 22-03 Task 3).
    - Look up the seeded test company BY NAME: `getCompanyByName('Acme Test Co')` (companies.ts:54-57 — name, NEVER id; seed re-inserts with growing serial PKs, Pitfall 4). If undefined → exit(1) with "run `npm run seed` first".
    - Set the test-domain identity on the row (Claude's discretion per CONTEXT + research recommendation (b)): `db.update(company).set({ domain: 'acmetest.arclumen.test' }).where(eq(company.id, row.id))` — a synthetic *.test domain that can never collide with real ICP data; analyzeCompany tolerates the domain being absent but the test-domain requirement wants it explicit.
    - Upsert OpenRouter-only settings: `upsertModelSettings({ userId, primaryModel: 'anthropic/claude-sonnet-4.6', fallbackModels: [] })` — the D-07 pinned concrete OpenRouter default slug, openrouter-only chain (no Anthropic id anywhere in the chain).
    - Run: `const result = await analyzeCompany(companyId, userId)` (the real run entry — the child env gate (D-20-03/FAL-04) must pass with only OPENROUTER set).
    - Output: `console.log(JSON.stringify({ ok: result.ok, modelUsed: result.modelUsed, modelChain: result.modelChain }))` — SHAPES ONLY, never env/key values (Security Domain).
    - Exit shape (refresh-model-catalog.ts precedent): `main().then(() => process.exit(0)).catch((error) => { console.error(error instanceof Error ? error.message : error); process.exit(1); });`
  </action>
  <acceptance_criteria>
    - `scripts/probe-openrouter-only.ts` exists with `config({ path: '.env.local' })` at the top (before any src/lib import)
    - Contains `getCompanyByName('Acme Test Co')` (name lookup, never id)
    - Contains `acmetest.arclumen.test` (the synthetic test-domain value)
    - Contains `upsertModelSettings` with `primaryModel: 'anthropic/claude-sonnet-4.6'` and `fallbackModels: []`
    - Contains `console.log(JSON.stringify({ ok: result.ok, modelUsed: result.modelUsed, modelChain: result.modelChain }))` (shapes only)
    - Does NOT contain `process.env.OPENROUTER_API_KEY` in any console.log/print path (no key leakage into evidence)
    - Script is under `scripts/`, NOT `src/` (Phase 18 child_process grep constraint)
  </acceptance_criteria>
  <verify>
    <automated>test -f scripts/probe-openrouter-only.ts && grep -q "config({ path: '.env.local' })" scripts/probe-openrouter-only.ts && grep -q "getCompanyByName('Acme Test Co')" scripts/probe-openrouter-only.ts && grep -q "acmetest.arclumen.test" scripts/probe-openrouter-only.ts && grep -q "anthropic/claude-sonnet-4.6" scripts/probe-openrouter-only.ts && ! grep -q "console.log(.*OPENROUTER_API_KEY" scripts/probe-openrouter-only.ts && test -z "$(echo scripts/probe-openrouter-only.ts | grep '^src/')"</automated>
  </verify>
  <done>The probe script exists: dotenv-loaded, company-by-name with a *.test domain, OpenRouter-only settings upsert, real analyzeCompany run, JSON shape output with zero key leakage, spawnable under tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the child-env Vitest test (spawnSync, ANTHROPIC stripped in child, skip guard, 120s) and run it</name>
  <files>src/lib/agents/openrouter-only-chain.test.ts</files>
  <behavior>
    - Test 1: with live keys present (OPENROUTER_API_KEY + FIRECRAWL_API_KEY + DATABASE_URL in .env.local), spawn `tsx scripts/probe-openrouter-only.ts` with `env: { ...process.env, ANTHROPIC_API_KEY: '' }` (stripped ONLY in the child env object — the parent process env is never deleted), timeout 110s; assert status 0, `out.ok === true`, and `out.modelUsed === 'anthropic/claude-sonnet-4.6'` (the as-saved slug verbatim — FAL-05)
    - Test 2 (implicit): when live keys are absent (CI), `describe.skipIf(!hasLiveKeys)` skips the whole suite gracefully — no failure, no key requirement
  </behavior>
  <action>
    Create `src/lib/agents/openrouter-only-chain.test.ts` per RESEARCH Pattern 2 (l.240-268) verbatim — the file is auto-discovered by vitest's `include: ['src/**/*.test.ts']` glob with zero config change:

    - Imports: `import { describe, expect, it } from 'vitest';` + `import { spawnSync } from 'node:child_process';` + `import { config } from 'dotenv';` — then `config({ path: '.env.local' });` at module top (vitest does NOT auto-load .env.local — seed.ts precedent; needed for the skip guard to read real keys).
    - `const hasLiveKeys = !!process.env.OPENROUTER_API_KEY && !!process.env.FIRECRAWL_API_KEY && !!process.env.DATABASE_URL;` — the Claude's-discretion skip guard (graceful CI skip).
    - `describe.skipIf(!hasLiveKeys)('VER-03 openrouter-only chain (child-env, real keys)', () => { ... })` — the describe is skipped without live keys, never fails.
    - Inside, ONE `it('runs analyzeCompany with ANTHROPIC_API_KEY unset in the child env', { timeout: 120_000 }, () => { ... })` — the SECOND argument `{ timeout: 120_000 }` is mandatory (vitest 5s default kills the real 43-50s run — Pitfall 1; verified supported in installed vitest 4.1.10).
    - The child env: `const childEnv = { ...process.env, ANTHROPIC_API_KEY: '' };` — STRIP IN THE CHILD ENV OBJECT ONLY, never `delete process.env.ANTHROPIC_API_KEY` in the test process (other tests / the dev shell depend on it — Anti-Pattern l.366-368). Why-comment this.
    - `spawnSync(process.execPath, [require.resolve('tsx/cli'), 'scripts/probe-openrouter-only.ts'], { env: childEnv, encoding: 'utf-8', timeout: 110_000 })` — spawn via the installed tsx CLI; the probe path is repo-root-relative (spawnSync cwd is the repo root under vitest).
    - Assertions: `expect(result.status, result.stderr).toBe(0);` then `const out = JSON.parse(result.stdout); expect(out.ok).toBe(true); expect(out.modelUsed).toBe('anthropic/claude-sonnet-4.6');` — the exact saved slug, verbatim (FAL-05).
    - Why-comment above the describe: D-22-03 structural proof — the child env proves key isolation without mutating the developer's real env; the parent env stays untouched.
    - Run: `npx vitest run src/lib/agents/openrouter-only-chain.test.ts` — with live keys present it runs the real ~43-50s analyze and passes; without keys it skips (exit 0 with a skip note).
  </action>
  <acceptance_criteria>
    - `src/lib/agents/openrouter-only-chain.test.ts` exists with `describe.skipIf(!hasLiveKeys)` guard and `{ timeout: 120_000 }` on the it()
    - Contains `ANTHROPIC_API_KEY: ''` in a `childEnv` object (never `delete process.env` — grep the file for the mutation anti-pattern, must be absent)
    - Contains `expect(out.modelUsed).toBe('anthropic/claude-sonnet-4.6')` (verbatim slug)
    - `npx vitest run src/lib/agents/openrouter-only-chain.test.ts` exits 0 (runs green with live keys, or skips gracefully without)
    - `npm test` exits 0 (the new file is part of the full suite)
  </acceptance_criteria>
  <verify>
    <automated>grep -q "describe.skipIf" src/lib/agents/openrouter-only-chain.test.ts && grep -q "timeout: 120_000" src/lib/agents/openrouter-only-chain.test.ts && grep -q "ANTHROPIC_API_KEY: ''" src/lib/agents/openrouter-only-chain.test.ts && ! grep -q "delete process.env" src/lib/agents/openrouter-only-chain.test.ts && npx vitest run src/lib/agents/openrouter-only-chain.test.ts</automated>
  </verify>
  <done>The child-env integration test is in place and green: it spawns the real probe with ANTHROPIC_API_KEY stripped in the child env, asserts ok: true and the verbatim modelUsed slug, skip-guards without live keys, and runs within the 120s budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → child process | The vitest parent spawns a tsx child with a MODIFIED env (ANTHROPIC_API_KEY: '') — the child runs real analyzeCompany with real OpenRouter calls against the live Neon DB |
| child process → OpenRouter/Neon/Firecrawl | Live provider + DB calls from the child — real keys, real credits, real writes (settings upsert + company domain update) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information Disclosure | real-key values leaking into test evidence | mitigate | The probe prints ONLY shapes ({ ok, modelUsed, modelChain }) — never env contents, never key values; the Security Domain rule is enforced in Task 1's acceptance grep (`! grep console.log.*OPENROUTER_API_KEY`) |
| T-22-07 | Tampering | parent env mutation in the test | mitigate | The child env object `{ ...process.env, ANTHROPIC_API_KEY: '' }` is the only mutation site — `delete process.env` is prohibited and grep-gated (Task 2 acceptance); other tests and the dev shell keep their keys |
| T-22-08 | Tampering | live-key runs overlapping (rate limits, doubled credit spend) | mitigate | The test lives in vitest (separate `npm test` run), and plan 22-05 (VER-02 live e2e) depends_on this plan — the wave ordering serializes the two live-key surfaces (Pitfall 5); Playwright's workers: 1 covers the e2e side |
| T-22-09 | Spoofing | seeded-company identity drift (seed re-inserts with growing ids) | mitigate | Company looked up BY NAME ('Acme Test Co', committed CSV row) — never a hard-coded id (Pitfall 4); the *.test domain (acmetest.arclumen.test) never collides with real ICP data |
| T-22-SC | Tampering | npm installs | accept | No installs in this plan (tsx + dotenv + vitest all already installed and verified) |

## Package Legitimacy

No packages installed by this plan (uses existing tsx@^4.23.1, dotenv@^17.4.2, vitest@4.1.10).
</threat_model>

<verification>
1. `npx vitest run src/lib/agents/openrouter-only-chain.test.ts` → green with live keys (real ~43-50s analyze) or graceful skip without
2. `npm test` → full suite green (the child-env test is part of every run)
3. Grep gates: `grep -c "ANTHROPIC_API_KEY: ''" src/lib/agents/openrouter-only-chain.test.ts` → ≥ 1 (strip in child only); `grep -c "delete process.env" src/lib/agents/openrouter-only-chain.test.ts` → 0 (parent env untouched)
4. Probe evidence recorded in 22-VERIFICATION.md (plan 22-07): { ok, modelUsed, modelChain } shapes only — never key values
</verification>

<success_criteria>
- VER-03 proven structurally: an OpenRouter-only chain runs end-to-end with ANTHROPIC_API_KEY stripped in a child env — only OPENROUTER_API_KEY needed (D-22-03)
- The proof is automated + repeatable (vitest suite member) and CI-safe (describe.skipIf guard — Claude's discretion honored)
- No real-key values appear in any evidence; the parent dev env is never mutated
- `agent_run.model_used` records the as-saved OpenRouter slug verbatim ('anthropic/claude-sonnet-4.6' — FAL-05)
</success_criteria>

<output>
Create `.planning/phases/22-verification-gate/22-04-SUMMARY.md` when done
</output>
