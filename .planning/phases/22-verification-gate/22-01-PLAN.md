---
phase: 22-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agents/runAgent.test.ts
  - src/lib/agents/modelConfig.test.ts
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "The collision matrix is verified locked in catalog.test.ts — claude-sonnet-5 → anthropic and anthropic/claude-sonnet-5 → openrouter (existing canaries at :186-192, audited not rewritten, per D-22-06)"
    - "The 4-cell 429 hop table is verified locked in modelConfig.test.ts — all 4 cells + null fail-closed (existing at :135-161, audited not rewritten)"
    - "The error matrix is verified locked for 402→billing (never advances) and 502/503→server_error (advances) in modelConfig.test.ts (:56-77, audited not rewritten)"
    - "The platform-vs-upstream 429 diagnostics split (isOpenRouterPlatformRateLimit) has direct unit tests — the VER-01 gap is closed"
    - "The WR-01 carry is pinned: a statusCode-200 APICallError classifies 'input' (never failover-eligible) — the second VER-01 gap is closed"
    - "The WR-01 comment sites in modelConfig.ts and runAgent.ts are verified to already say 'input' (mid-stream 429 classification) — no comment rewrite needed"
  artifacts:
    - path: "src/lib/agents/runAgent.test.ts"
      provides: "Direct unit tests for isOpenRouterPlatformRateLimit (platform vs upstream split, D-20-08)"
      contains: "isOpenRouterPlatformRateLimit"
    - path: "src/lib/agents/modelConfig.test.ts"
      provides: "statusCode-200 → 'input' pin (WR-01 carry)"
      contains: "statusCode: 200"
  key_links:
    - from: "src/lib/agents/runAgent.test.ts"
      to: "src/lib/agents/runAgent.ts"
      via: "import of isOpenRouterPlatformRateLimit"
      pattern: "isOpenRouterPlatformRateLimit"
    - from: "src/lib/agents/modelConfig.test.ts"
      to: "src/lib/agents/modelConfig.ts"
      via: "classifyModelError on a statusCode-200 APICallError"
      pattern: "toBe\\('input'\\)"
---

<objective>
VER-01 audit + gap-fill per D-22-06: verify every locked matrix cell in the existing test suite (collision canaries, 4-cell 429 hop table, error classes), add ONLY the two genuinely-missing test groups — direct `isOpenRouterPlatformRateLimit` unit tests (platform-vs-upstream 429 diagnostics split) and the statusCode-200 → `'input'` pin (WR-01 carry from Phase 20). No blind rewrites, no redundant assertions; the existing matrices stay untouched.

Purpose: This is the verification phase's first requirement — the milestone's correctness claims must be locked by automated tests. RESEARCH.md's in-repo audit found exactly two gaps; this plan closes them at their home files (D-16 convention), leaving the already-green matrices verbatim.
Output: `runAgent.test.ts` gains a new `isOpenRouterPlatformRateLimit` describe (6 cases); `modelConfig.test.ts` gains one WR-01 pin case; the WR-01 comment sites are verified (already corrected to 'input'); full unit suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md (Phase 20 decision log: D-20-08 diagnostics-only helper, 4-cell matrix, WR-01 'input' carry)
@.planning/phases/22-verification-gate/22-CONTEXT.md (D-22-06 audit + fill gaps, no rewrite-from-scratch)
@.planning/phases/22-verification-gate/22-RESEARCH.md (§Phase Requirements VER-01 row, §Wave 0 Gaps, §State of the Art — WR-01 comment sites)
@.planning/phases/22-verification-gate/22-PATTERNS.md (§runAgent.test.ts MODIFY l.25-53 — the 6 required cases + inline error shapes; §modelConfig.test.ts MODIFY l.57-88 — the 200-case shape)
src/lib/agents/runAgent.test.ts (existing suite — apiErr helper :151-152, hoisted mocks, D-16 conventions)
src/lib/agents/modelConfig.test.ts (existing suite — apiErr helper :19-25, classifyModelError describe :27-133)
src/lib/agents/runAgent.ts (:126-135 isOpenRouterPlatformRateLimit — the helper under test)
src/lib/agents/modelConfig.ts (:42-82 classifyModelError — the classifier under test; :65-72 WR-01 comment verified already 'input')
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the three existing matrices cell-by-cell and record the audit (no test-file changes)</name>
  <files>src/lib/agents/runAgent.test.ts, src/lib/agents/modelConfig.test.ts</files>
  <read_first>
    src/lib/agents/modelConfig.test.ts (lines 135-161 the 4-cell matrix, 56-77 the error classes — audit targets)
    src/lib/models/catalog.test.ts (lines 186-192 the collision canaries — audit target; READ ONLY, do not modify)
    src/lib/agents/runAgent.test.ts (lines 301-336 the hop behavior tests — audit target)
    .planning/phases/22-verification-gate/22-RESEARCH.md l.9 (the two documented gaps) + §Phase Requirements VER-01 row (l.45)
    .planning/phases/22-verification-gate/22-PATTERNS.md l.29-31 (do-not-disturb note: existing 4-cell tests, catalog.json mock at :43, D-16 doctrine)
  </read_first>
  <action>
    Run the targeted regression command to prove the existing matrices are green BEFORE touching anything: `npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelConfig.test.ts src/lib/agents/runAgent.test.ts` — all must pass. Then, READ-ONLY audit each locked cell against the source (no test edits in this task):

    1. Collision matrix: confirm `catalog.test.ts` contains all three snapshot-canary assertions — `anthropic/claude-sonnet-4.6` → openrouter, `claude-sonnet-5` → anthropic, `anthropic/claude-sonnet-5` → openrouter (expected ~lines 186-192). These lock the two collision claims VER-01 names.
    2. 4-cell 429 hop table: confirm `modelConfig.test.ts` `shouldAdvance` describe asserts all four cells (`rate_limited` anthropic→anthropic false, openrouter→openrouter false, anthropic→openrouter true, openrouter→anthropic true) plus the null-identity fail-closed pair (expected ~lines 135-161).
    3. Error classes: confirm `modelConfig.test.ts` `classifyModelError` describe asserts 402 → 'billing' with `isFailoverEligible('billing') === false` and 502/503 → 'server_error' with eligibility true (expected ~lines 56-77).
    4. WR-01 comment sites: grep the four sites RESEARCH §State of the Art names — `src/lib/agents/modelConfig.ts` (mid-stream-429 comment above the InvalidResponseDataError branch) and `src/lib/agents/runAgent.ts` (the D-20-05/06 comments). Verify each now says 'input' (they were corrected in Phase 20/21 — confirmed 2026-08-03). If any site still says 'output' for mid-stream 429s, fix ONLY the comment text to say 'input' with a `// WR-01 (Phase 22 verified)` marker — comment-only, zero behavior change. If all already say 'input' (expected), record that in the summary and change nothing.

    Record the audit result in the plan SUMMARY (`22-01-SUMMARY.md` at the end): a cell → test → file:line mapping for every locked cell, the gap list (exactly the two RESEARCH names), and the comment-site verification result. Do NOT modify any assertion in the existing matrices — D-22-06 forbids rewrites and the suite is green as-is.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelConfig.test.ts src/lib/agents/runAgent.test.ts` exits 0 (the targeted 3-file regression command)
    - `grep -c "claude-sonnet-5" src/lib/models/catalog.test.ts` returns ≥ 2 (both collision canaries present) — no modification made
    - `grep -c "shouldAdvance" src/lib/agents/modelConfig.test.ts` returns ≥ 5 (4-cell matrix + null fail-closed assertions present)
    - `grep -n "statusCode: 200" src/lib/agents/modelConfig.test.ts` does NOT exist yet (proves the gap is genuine — the pin is added in Task 2)
    - `grep -n "isOpenRouterPlatformRateLimit" src/lib/agents/runAgent.test.ts` does NOT exist yet (proves the gap is genuine — direct tests added in Task 2)
    - `git diff --stat src/lib/models/catalog.test.ts` is empty (collision matrix untouched — D-22-06)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelConfig.test.ts src/lib/agents/runAgent.test.ts && test -z "$(git diff --stat src/lib/models/catalog.test.ts)" && ! grep -q "statusCode: 200" src/lib/agents/modelConfig.test.ts && ! grep -q "isOpenRouterPlatformRateLimit" src/lib/agents/runAgent.test.ts</automated>
  </verify>
  <done>The three matrices are audited green cell-by-cell with the audit recorded (cell → test → line mapping in SUMMARY); the two gaps confirmed genuinely absent; WR-01 comment sites verified to already say 'input' (or comment-only corrected); no existing assertion modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the two VER-01 gaps — isOpenRouterPlatformRateLimit direct tests + statusCode-200 → 'input' pin</name>
  <files>src/lib/agents/runAgent.test.ts, src/lib/agents/modelConfig.test.ts</files>
  <behavior>
    - Test 1 (runAgent.test.ts): isOpenRouterPlatformRateLimit(APICallError statusCode 429 with responseHeaders { 'x-ratelimit-limit': '20' }) === true (platform-level)
    - Test 2 (runAgent.test.ts): isOpenRouterPlatformRateLimit(APICallError 429 with data.error.metadata = { error_type: 'rate_limit_exceeded', provider_code: 'anthropic' }) === false (upstream pass-through)
    - Test 3 (runAgent.test.ts): isOpenRouterPlatformRateLimit(APICallError 429 with data.error.metadata = { error_type: 'rate_limit_exceeded' } and no provider_code) === true (platform-level)
    - Test 4 (runAgent.test.ts): isOpenRouterPlatformRateLimit(new Error('x')) === false (non-APICallError)
    - Test 5 (runAgent.test.ts): isOpenRouterPlatformRateLimit(APICallError 429 with no headers and no data) === false (header-dependent, empty body)
    - Test 6 (runAgent.test.ts): isOpenRouterPlatformRateLimit(APICallError statusCode 200 with data + x-ratelimit headers) === true; same with no headers === false (mid-stream 429 shape, header-dependent)
    - Test 7 (modelConfig.test.ts): classifyModelError(APICallError statusCode 200 with data.error.message) === 'input' AND isFailoverEligible('input') === false (WR-01 pin)
  </behavior>
  <action>
    ADD ONLY (never edit existing assertions — D-22-06):

    In `src/lib/agents/runAgent.test.ts` — add a NEW top-level describe at the end of the file (do NOT nest inside the failover-loop describe; do NOT touch the hoisted mocks — the helper is a pure export of runAgent.ts with no module deps beyond `APICallError.isInstance`):
    `describe('isOpenRouterPlatformRateLimit (D-20-08, VER-01 gap)', () => { ... })` with the 6 behavior cases above, per PATTERNS l.40-53. Construct real APICallError instances inline with the extra fields the helper reads (runAgent.ts:126-135): `responseHeaders` for the header cases, `data: { error: { metadata: { error_type, provider_code } } }` for the metadata cases. Import `{ isOpenRouterPlatformRateLimit }` from './runAgent' alongside the existing `runAgent` import (line 45). Use the existing apiErr helper where applicable, extended inline for the extra fields. Add a why-comment above the describe explaining: diagnostics-only helper (D-20-08), the advance decision already happened in the loop — these tests lock the REASON string split used by analyzeCompany (platform vs upstream).

    In `src/lib/agents/modelConfig.test.ts` — add ONE `it(...)` INSIDE the existing `describe('classifyModelError', ...)` block (after the never-advances 400/401/403 case, ~line 100), constructing the mid-stream shape exactly per PATTERNS l.74-87: `new APICallError({ message: 'finish_reason: error', url: 'u', requestBodyValues: {}, statusCode: 200, data: { error: { message: 'rate limit exceeded mid-stream' } } })`, asserting `classifyModelError(midStream) === 'input'` and `isFailoverEligible('input') === false`. Why-comment: `// D-20-05/06 (WR-01): mid-stream 429s surface as APICallError with statusCode 200 + data — the classifier falls through to 'input' (never failover-eligible); Phase 22 error matrix records 'input', NOT 'output'.` Do NOT reclassify — the 200 case legitimately falls through the statusCode switch to `return 'input'` (modelConfig.ts:62).

    Do not modify modelConfig.ts or runAgent.ts source (comment sites verified in Task 1).
  </action>
  <acceptance_criteria>
    - `src/lib/agents/runAgent.test.ts` contains a top-level describe named `isOpenRouterPlatformRateLimit (D-20-08, VER-01 gap)` with ≥ 5 it() cases (expected 6)
    - `src/lib/agents/runAgent.test.ts` contains `import { isOpenRouterPlatformRateLimit } from './runAgent'`
    - `src/lib/agents/modelConfig.test.ts` contains an it() asserting `classifyModelError(midStream)).toBe('input')` with a statusCode-200 APICallError construction
    - `npx vitest run src/lib/agents/runAgent.test.ts src/lib/agents/modelConfig.test.ts` exits 0
    - `npm test` (full unit suite, 32+ files) exits 0 — no regression
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/agents/runAgent.test.ts src/lib/agents/modelConfig.test.ts && grep -q "VER-01 gap" src/lib/agents/runAgent.test.ts && grep -q "toBe('input')" src/lib/agents/modelConfig.test.ts && npm test</automated>
  </verify>
  <done>Both VER-01 gaps closed at their home files: isOpenRouterPlatformRateLimit has direct unit tests locking platform-vs-upstream 429 diagnostics; the statusCode-200 → 'input' pin locks the WR-01 carry; the full unit suite is green with zero changes to existing matrix assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → source under test | Unit tests exercise pure exports (classifyModelError, isOpenRouterPlatformRateLimit) — no new trust surface; D-16 zero-live-call doctrine keeps the suite offline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | runAgent.test.ts / modelConfig.test.ts assertions | mitigate | Task 1's audit gates: existing matrix assertions are proven green and untouched (`git diff --stat catalog.test.ts` empty); Task 2 adds ONLY the two documented gaps — a blind rewrite could silently change locked cells (D-22-06) |
| T-22-02 | Tampering | WR-01 comment sites (modelConfig.ts:65-72, runAgent.ts:48-51,104-105) | mitigate | Task 1 verifies the comments already say 'input' (corrected in Phase 20/21, verified 2026-08-03); any correction is comment-only with a `// WR-01 (Phase 22 verified)` marker — behavior never touched |
| T-22-SC | Tampering | npm installs | accept | No new package installs in this plan (pure unit-test additions to existing files); the phase's only installs live in plan 22-03 where both packages are slopcheck-[OK] (RESEARCH Package Legitimacy Audit) |

## Package Legitimacy

No packages installed by this plan. The phase's two new devDependencies (@playwright/test@^1.62.1, @clerk/testing@^2.2.16) are [OK] per the RESEARCH Package Legitimacy Audit and installed in plan 22-03 — no [ASSUMED]/[SUS] items exist, so no legitimacy checkpoint is required anywhere in the phase.
</threat_model>

<verification>
1. `npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelConfig.test.ts src/lib/agents/runAgent.test.ts` → green (targeted 3-file regression — the Phase 22 matrix lock)
2. `npm test` → full suite green (366+ existing tests + the new VER-01 groups)
3. Grep gates: `grep -c "VER-01 gap" src/lib/agents/runAgent.test.ts` → ≥ 1; `grep -c "toBe('input')" src/lib/agents/modelConfig.test.ts` → ≥ 1 (the WR-01 pin)
4. `git diff --stat src/lib/models/catalog.test.ts` → empty (collision matrix untouched per D-22-06)
</verification>

<success_criteria>
- VER-01's three matrices (collision, 4-cell hop, error classes) are audited green cell-by-cell and recorded in the SUMMARY — with the two RESEARCH-documented gaps now closed by direct tests (isOpenRouterPlatformRateLimit) and the WR-01 pin (statusCode-200 → 'input')
- The error-matrix claim "platform vs upstream 429" is locked at unit level (was only indirect via analyzeCompany.test.ts:374-406)
- The WR-01 comment sites are verified accurate ('input', not 'output') — no stale documentation remains
- Full unit suite green; zero modifications to existing matrix assertions (D-22-06 audit + fill, not rewrite)
</success_criteria>

<output>
Create `.planning/phases/22-verification-gate/22-01-SUMMARY.md` when done
</output>
