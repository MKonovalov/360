---
phase: 22-verification-gate
plan: 05
type: execute
wave: 3
depends_on: [22-03, 22-04]
files_modified:
  - e2e/ver-02-analyze.spec.ts
autonomous: false
requirements: [VER-02]
user_setup:
  - service: openrouter
    why: "VER-02 is a live real-key E2E run (D-22-01) — a real Analyze call against a seeded test company with a saved OpenRouter primary, ~cents of credits"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "Already in .env.local (verified present 2026-08-03). Task 1 verifies live validity + credits via the OpenRouter API; the blocking checkpoint in Task 2 confirms it is credited before the ~cents run"
    dashboard_config: []
must_haves:
  truths:
    - "An OpenRouter primary (anthropic/claude-sonnet-4.6) saves through the real Settings UI or Server Action and persists to user_model_settings (D-22-02 seeded test company flow)"
    - "Analyze on the seeded test company returns 201 with modelUsed === the saved OpenRouter slug, verbatim (FAL-05)"
    - "agent_run.model_used read back via getRunById matches the saved slug — durable DB truth (D-14)"
    - "The spec targets the seeded test company BY NAME ('Acme Test Co') with a *.test domain — never a hard-coded id (Pitfall 4), never real ICP data"
    - "The spec runs with 120s timeouts (test.setTimeout + request timeout) — the real analyze runs 43-50s (Pitfalls 1-2)"
    - "The e2e authenticates through the real Clerk login (storageState from the auth-setup project, plan 22-03) — real auth gate (D-22-05)"
  artifacts:
    - path: "e2e/ver-02-analyze.spec.ts"
      provides: "Live-key E2E: save OR primary → POST analyze → assert 201 modelUsed → getRunById read-back"
      contains: "modelUsed"
  key_links:
    - from: "e2e/ver-02-analyze.spec.ts"
      to: "/api/companies/[id]/analyze"
      via: "authenticated page.request POST (shares the browser session)"
      pattern: "analyze"
    - from: "e2e/ver-02-analyze.spec.ts"
      to: "src/lib/db/queries/runs.ts"
      via: "getRunById import (real DB read-back)"
      pattern: "getRunById"
    - from: "e2e/ver-02-analyze.spec.ts"
      to: "src/lib/db/queries/companies.ts"
      via: "getCompanyByName import (seeded test company by NAME)"
      pattern: "getCompanyByName"
---

<objective>
Deliver VER-02 (D-22-01/02): a live real-key E2E spec — sign in through the real Clerk flow (plan 22-03 storageState), save an OpenRouter primary, run Analyze against the seeded test company, and assert `agent_run.model_used` matches the saved OpenRouter slug verbatim, both in the 201 response and via the `getRunById` DB read-back.

Purpose: This is the milestone's strongest literal proof — the full stack (Settings save → auth gate → analyze route → real OpenRouter provider contract → DB audit columns) exercised with real keys against a deterministic seeded test company. The live run costs ~cents of credits (D-22-01 accepts this). This plan depends on 22-04 (VER-03) because both make live OpenRouter calls — Pitfall 5 serializes the two live-key surfaces so they never overlap (rate limits, doubled credit spend).
Output: `e2e/ver-02-analyze.spec.ts`, green under `npm run e2e`, with evidence (status + JSON shapes + DB read-back, never key values) recorded for 22-VERIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-verification-gate/22-CONTEXT.md (D-22-01 live real-key run; D-22-02 seeded test company; D-22-05 real Clerk login)
@.planning/phases/22-verification-gate/22-RESEARCH.md (§Pattern 4 l.329-361 — the verified spec skeleton; §VER-02 row l.46; §Pitfall 2 l.390-394 — 120s timeouts; §Pitfall 4 l.402-407 — name lookup; §Open Questions 3 l.515-518 — UI save vs direct action)
@.planning/phases/22-verification-gate/22-PATTERNS.md (§ver-02 spec l.217-242 — relative import convention, DB-layer signatures)
src/lib/db/queries/runs.ts (getRunById l.41-43 — returns row with modelUsed)
src/lib/db/queries/companies.ts (getCompanyByName l.54-57; domain column)
src/app/api/companies/[id]/analyze/route.ts (POST handler: requireStaffAccess FIRST → companyIdSchema → analyzeCompany → 201 { ...run, proposalCount, modelUsedName } — run carries modelUsed)
src/app/actions/settings.ts (saveSettingsAction: requireStaffAccess FIRST → zod → union servable check → dedupe → upsert)
src/app/(dashboard)/settings/page.tsx (the Settings surface the UI save drives)
src/components/settings/model-settings-form.tsx (Save changes button l.356; 'Saved.' text l.360)
data/seed/companies.csv ('Acme Test Co' — committed seeded test company, domain NULL)
src/scripts/seed.ts (npm run seed — the deterministic reseed command, Pitfall 4 note)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify operator prerequisites (credited key + seeded company) and author the VER-02 spec</name>
  <files>e2e/ver-02-analyze.spec.ts</files>
  <read_first>
    .planning/phases/22-verification-gate/22-RESEARCH.md §Pattern 4 l.335-361 (spec skeleton — copy structure) + §Open Questions 3 l.515-518 (UI save recommended; direct action fallback if flaky)
    .planning/phases/22-verification-gate/22-PATTERNS.md §ver-02 key excerpts l.221-238 (relative imports, 120s timeouts, getRunById/getCompanyByName signatures)
    src/lib/db/queries/runs.ts (getRunById l.41-43)
    src/lib/db/queries/companies.ts (getCompanyByName l.54-57)
    src/app/actions/settings.ts (saveSettingsAction contract — requireStaffAccess FIRST, union servable gate)
    src/app/api/companies/[id]/analyze/route.ts (POST success shape: 201 { ...run, proposalCount, usedFallback, modelUsedName } — run carries modelUsed)
    data/seed/companies.csv ('Acme Test Co' row)
    .env.local (OPENROUTER_API_KEY presence — verified 2026-08-03)
  </read_first>
  <action>
    FIRST, verify the two operator prerequisites with automation (do not write the spec until both pass):

    1. **Credited OpenRouter key**: `curl -s https://openrouter.ai/api/v1/auth/key -H "Authorization: Bearer $OPENROUTER_API_KEY"` (after dotenv-loading .env.local) — the response's `data.limit`/`data.usage` must show a positive remaining balance. If the API returns an error or zero balance, STOP and surface the Task 2 blocking checkpoint (the key needs credits — a human must top up; do NOT weaken the spec to skip the live run).
    2. **Seeded test company**: query `getCompanyByName('Acme Test Co')` via a quick tsx one-liner. If missing, run `npm run seed` (the deterministic reseed — deletes and re-inserts seeded rows, serial PKs grow; the spec must NOT rely on ids — Pitfall 4). Confirm the row exists after.

    THEN create `e2e/ver-02-analyze.spec.ts` per RESEARCH Pattern 4 (l.335-361) + PATTERNS (l.221-238), verbatim structure:

    - Header why-comment: VER-02 live-key E2E (D-22-01/02); real Clerk login via storageState; the seeded test company is targeted BY NAME with a *.test domain — real ICP data is never touched.
    - Imports: `import { test, expect } from '@playwright/test';` + RELATIVE imports for the DB read-back per PATTERNS (l.224-226): `import { getRunById } from '../src/lib/db/queries/runs';` and `import { getCompanyByName } from '../src/lib/db/queries/companies';` — relative (not @/) because the Playwright runner has no tsconfig-path alias (PATTERNS note).
    - `test('VER-02: OpenRouter primary → Analyze → model_used matches', async ({ page, request }) => { test.setTimeout(120_000); ... })` — the 120s timeout is MANDATORY (Pitfall 2: Playwright defaults are 30s test/30s request; the real analyze runs 43-50s).
    - **Save step** (Open Question 3 recommendation — drive the real UI; it doubles as a badge-observation pass): `await page.goto('/settings');` then via the form: select provider 'OpenRouter' (SelectTrigger aria-label="AI provider"), pick `anthropic/claude-sonnet-4.6` in the primary ModelPicker (ariaLabel "Primary model", search 'claude-sonnet-4.6'), click "Save changes", `await expect(page.getByText('Saved.')).toBeVisible();` (form l.360). IF the UI save proves flaky in execution, fall back to invoking `saveSettingsAction({ primaryModel: 'anthropic/claude-sonnet-4.6', fallbacks: [] })` directly via `page.request` against the Server Action endpoint — document which path was used in the SUMMARY.
    - **Company lookup by NAME**: `const company = await getCompanyByName('Acme Test Co');` — if undefined, fail the test with the message "run npm run seed" (never a hard-coded id). Optionally assert/set its domain to a *.test value via the query layer (companies.ts domain column) — the research recommendation (b) sets `acmetest.arclumen.test` so no real-domain data is involved; this is a nice-to-have, not a hard assertion (analyzeCompany tolerates null domain).
    - **Analyze POST through the authenticated context**: `const res = await request.post(\`/api/companies/${company.id}/analyze\`, { timeout: 120_000 });` — page.request shares the browser's real session cookies (the real auth gate, D-22-05). `expect(res.status()).toBe(201);`
    - **Response assertion**: `const body = await res.json(); expect(body.modelUsed).toBe('anthropic/claude-sonnet-4.6');` — as-saved slug VERBATIM (FAL-05; the route returns modelUsed via ...run).
    - **Durable read-back (D-14)**: `const run = await getRunById(body.id); expect(run.modelUsed).toBe('anthropic/claude-sonnet-4.6');` — the DB row is the durable truth.
    - No try/catch around the query-layer calls (house convention: caller owns error handling — PATTERNS l.240).

    Do NOT record the response body or any env values beyond the modelUsed assertion in committed evidence — Security Domain: shapes only.
  </action>
  <acceptance_criteria>
    - The OpenRouter key check (`curl .../auth/key`) returned a valid key with positive remaining balance (recorded in the SUMMARY as evidence — status/limit/usage shapes, never the key)
    - `getCompanyByName('Acme Test Co')` resolved to a row (seeded if needed via `npm run seed`)
    - `e2e/ver-02-analyze.spec.ts` exists with `test.setTimeout(120_000)`, `request.post(... , { timeout: 120_000 })`, `expect(body.modelUsed).toBe('anthropic/claude-sonnet-4.6')`, and `expect(run.modelUsed).toBe('anthropic/claude-sonnet-4.6')`
    - The spec imports `getRunById` and `getCompanyByName` via RELATIVE paths (../src/lib/...)
  </acceptance_criteria>
  <verify>
    <automated>test -f e2e/ver-02-analyze.spec.ts && grep -q "setTimeout(120_000" e2e/ver-02-analyze.spec.ts && grep -q "getRunById" e2e/ver-02-analyze.spec.ts && grep -q "getCompanyByName" e2e/ver-02-analyze.spec.ts && grep -q "toBe('anthropic/claude-sonnet-4.6')" e2e/ver-02-analyze.spec.ts && grep -q "getCompanyByName('Acme Test Co')" e2e/ver-02-analyze.spec.ts</automated>
  </verify>
  <done>The VER-02 spec is authored against verified prerequisites: a credited OpenRouter key, the seeded Acme Test Co company present, 120s timeouts, relative DB imports, verbatim modelUsed assertions on both the 201 response and the getRunById read-back.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Operator confirms the credited OpenRouter key + seeded test company before the live ~cents run</name>
  <files>e2e/ver-02-analyze.spec.ts</files>
  <action>Pause for operator approval of the VER-02 live real-key run. The automation (Task 1) has already: (1) verified the OpenRouter key is valid and credited via the auth/key API, (2) confirmed (or seeded) the 'Acme Test Co' row via getCompanyByName, (3) authored the spec with 120s timeouts and verbatim modelUsed assertions. This checkpoint exists because the live run spends real credits (D-22-01, ~cents) — an operator confirm is required before the spend. On approval, proceed to Task 3 (run the spec green). On rejection, fix the flagged prerequisite (top up key / seed / provision account per plan 22-03 Task 3) and re-run Task 1's checks before resuming.</action>
  <what-built>The VER-02 live-key e2e spec (`e2e/ver-02-analyze.spec.ts`) plus the automated prerequisite checks: OpenRouter key validity/credits (via the auth/key API) and the seeded 'Acme Test Co' company presence. The blocking gate exists because the live run consumes real API credits (D-22-01, ~cents) — an operator confirm before spend.</what-built>
  <how-to-verify>
    1. Confirm the executor's automated key check result: the `curl https://openrouter.ai/api/v1/auth/key` response (shown in the session output, key value redacted) shows a positive remaining balance — the key is live and credited.
    2. Confirm the seeded test company exists (executor's `getCompanyByName('Acme Test Co')` result) — the e2e targets ONLY this deterministic test row (D-22-02), never real ICP data.
    3. Confirm the Clerk test staff account (E2E_CLERK_USER_EMAIL, provisioned in plan 22-03) can sign in — the auth-setup storageState was produced.
    4. Approve the live run: ~cents of OpenRouter credits will be spent on one real Analyze call against Acme Test Co.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize the live ~cents run, or describe any prerequisite issue (key balance, seed, account) to fix first.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run the VER-02 spec green and record evidence shapes</name>
  <files>e2e/ver-02-analyze.spec.ts</files>
  <read_first>
    e2e/ver-02-analyze.spec.ts (the spec just authored in Task 1 — run it as-is, fix ONLY genuine spec bugs, never weaken assertions)
    .planning/phases/22-verification-gate/22-RESEARCH.md §Pitfall 2 l.390-394 (timeout failure signatures: "Timeout 30000ms exceeded" on the analyze POST)
  </read_first>
  <action>
    Run the spec: `npx playwright test e2e/ver-02-analyze.spec.ts` (the auth-setup project runs first via the config dependency, signing in through the real Clerk flow; webServer auto-starts/reuses the dev server).

    - The full run takes 2-4 min (browser boot + real ~43-50s analyze). Do NOT interrupt it.
    - If it fails: read the failure against the known pitfall signatures — "Timeout 30000ms exceeded" means a Playwright default timeout leaked through (fix: ensure the 120s overrides are on the POST and the test), a 400 not_configured means the saved chain or keys are off (check the save step), a 404 means the company lookup failed (re-seed). Fix genuine spec bugs, NEVER weaken the modelUsed assertions or lower the status expectation below 201.
    - Record evidence for 22-VERIFICATION.md (plan 22-07): the 201 status, `body.modelUsed` value, the `getRunById(...).modelUsed` read-back value, and the run id — SHAPES ONLY, never the key values, never the full response bodies.
    - Note in the SUMMARY which save path was used (real UI vs direct action invocation) per Open Question 3, and any IN-03 billing observation if the run accidentally hits 402 (record as HUMAN-UAT observation — do NOT add scope).
  </action>
  <acceptance_criteria>
    - `npx playwright test e2e/ver-02-analyze.spec.ts` exits 0 (spec green)
    - The SUMMARY records: 201 status, `body.modelUsed === 'anthropic/claude-sonnet-4.6'`, `getRunById(id).modelUsed === 'anthropic/claude-sonnet-4.6'`, run id — no key values anywhere
    - No committed file contains the OPENROUTER_API_KEY value or any other secret (grep the spec + summary for `sk-or-` → must be absent)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/ver-02-analyze.spec.ts && ! grep -rq "sk-or-" e2e/ .planning/phases/22-verification-gate/</automated>
  </verify>
  <done>VER-02 is proven: an OpenRouter primary saved → Analyze on the seeded test company → 201 with modelUsed matching the saved slug, durable in agent_run.model_used via getRunById — with evidence shapes recorded and zero key leakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Clerk auth | Real Clerk session (storageState from plan 22-03) — the authenticated page.request shares the browser's real cookies |
| page.request → analyze route → OpenRouter | The 201 analyze POST drives a REAL provider call with REAL credits — the strongest live proof (D-22-01) |
| spec → DB queries (getRunById/getCompanyByName) | The spec reads the live Neon DB directly for the seeded-company lookup and the durable run read-back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information Disclosure | live-key values in test evidence | mitigate | Evidence records status/JSON SHAPES only (modelUsed, run id) — never the OPENROUTER_API_KEY value; Task 3's `! grep -rq "sk-or-"` gate on the spec + phase dir enforces it |
| T-22-10 | Spoofing | uncredited/fake key producing a false pass | mitigate | Task 1's automated `curl /auth/key` check verifies live validity + positive balance BEFORE the run; Task 2's blocking checkpoint is the operator confirm (quality gate: credited key gates live-key tasks) |
| T-22-11 | Tampering | hitting the wrong company (real ICP data, unstable ids) | mitigate | Company targeted BY NAME ('Acme Test Co') with a *.test domain — never a hard-coded serial id (Pitfall 4); the spec fails loudly if the row is absent rather than targeting arbitrary rows |
| T-22-12 | Tampering | weakened assertions to force a pass | mitigate | Task 3 forbids lowering the status below 201 or weakening the verbatim modelUsed assertions; genuine spec bugs are fixed, claims are not relaxed |
| T-22-SC | Tampering | npm installs | accept | No installs in this plan |

## Package Legitimacy

No packages installed by this plan (the harness devDeps landed in plan 22-03, both slopcheck-[OK]).
</threat_model>

<verification>
1. `npx playwright test e2e/ver-02-analyze.spec.ts` → green (auth-setup project runs first via config dependency)
2. Evidence recorded in the SUMMARY: 201 status + modelUsed verbatim + getRunById read-back + run id — shapes only
3. `grep -r "sk-or-" e2e/ .planning/phases/22-verification-gate/` → zero (no key leakage into committed files)
4. The blocking checkpoint (Task 2) was approved before the live ~cents run
</verification>

<success_criteria>
- VER-02 proven end-to-end with real keys (D-22-01): save OpenRouter primary → Analyze on the seeded test company → `agent_run.model_used` matches the saved slug in both the 201 response and the durable DB read-back (D-14)
- The proof targets the deterministic seeded test company by name with a *.test domain (D-22-02) — reproducible regardless of which other rows exist in the DB
- The real Clerk auth gate is exercised (D-22-05), the credited-key prerequisite is operator-gated, and no key values ever reach committed evidence
</success_criteria>

<output>
Create `.planning/phases/22-verification-gate/22-05-SUMMARY.md` when done
</output>
