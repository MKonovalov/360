---
phase: 16-failover-orchestration
plan: 04
type: execute
wave: 2
depends_on: [16-01]
files_modified: [src/components/agents/analyze-run-status.tsx]
autonomous: true
requirements: [FAL-05]
user_setup: []
must_haves:
  truths:
    - "The AnalyzeRunStatus client strip shows 'Rate limited — try again in a moment' when the API returns error: 'rate_limited' (D-04 — the ONLY new ERROR_COPY row)"
    - "A success-after-fallback run appends ' — ran on {display name} (fallback)' to the existing 'Analysis complete' line (D-06)"
    - "A normal success stays exactly 'Analysis complete' — no fallback note when usedFallback is falsy"
  artifacts:
    - path: "src/components/agents/analyze-run-status.tsx"
      provides: "ERROR_COPY rate_limited row + success-line fallback note + modelUsed/modelUsedName/usedFallback in RunState"
      contains: "rate_limited"
  key_links:
    - from: "src/components/agents/analyze-run-status.tsx"
      to: "POST /api/companies/[id]/analyze response"
      via: "fetch json: { proposalCount, modelUsed, modelUsedName, usedFallback }"
      pattern: "modelUsedName"
    - from: "src/components/agents/analyze-run-status.tsx"
      to: "error body error: 'rate_limited'"
      via: "ERROR_COPY[reason] lookup"
      pattern: "Rate limited"
---

<objective>
Surface the failover result in the Analyze status strip: the D-04 `rate_limited` staff copy and the D-06 success-after-fallback note, driven by the flat `{ modelUsed, modelUsedName, usedFallback }` API response fields.

Purpose: FAL-05's staff-facing half — staff see when a fallback ran and get a specific message on rate limits. The display name arrives server-computed (`modelUsedName`, from catalog.ts's getModelDisplayName via the route) because catalog.json is server-only (D-07); the component never imports the catalog.
Output: `src/components/agents/analyze-run-status.tsx` changes only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-failover-orchestration/16-RESEARCH.md
@.planning/phases/16-failover-orchestration/16-PATTERNS.md
@src/components/agents/analyze-run-status.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: analyze-run-status.tsx — rate_limited copy row + success-line fallback note</name>
  <files>src/components/agents/analyze-run-status.tsx</files>
  <read_first>
    - src/components/agents/analyze-run-status.tsx (ERROR_COPY L29-38, RunState L20-25, run() L69-103, success render L123-132)
    - 16-RESEARCH.md Common Operation 3 (lines 450-471: ERROR_COPY delta + success-state typing + fallback note)
    - 16-PATTERNS.md lines 364-399 (status-strip delta with exact strings)
    - CONTEXT.md D-04/D-06 (exact copy strings are locked)
  </read_first>
  <action>
    Modify `src/components/agents/analyze-run-status.tsx`:
    - ERROR_COPY table (L29-38): ADD exactly one new row — `rate_limited: 'Rate limited — try again in a moment',` (D-04 — the ONLY new reason row; other non-failover classes keep the existing generic `analysis_failed` row, which already has copy at L34). Keep every existing row unchanged.
    - RunState type (L20-25): extend the success variant — `| { status: 'success'; proposalCount: number; modelUsed?: string; modelUsedName?: string; usedFallback?: boolean }` (D-05/D-06 flat shape). The other variants (idle/running/successNoNew/failure) unchanged.
    - run() success branch (L76-89): change the fetch json cast at L77 to include the new fields: `const data = (await res.json().catch(() => ({}))) as { proposalCount?: number; modelUsed?: string; modelUsedName?: string; usedFallback?: boolean };`. In the proposalCount > 0 branch (L79-85), pass the fields through: `setState({ status: 'success', proposalCount, modelUsed: data.modelUsed, modelUsedName: data.modelUsedName, usedFallback: data.usedFallback })`. In the successNoNew branch (L81-84) keep as-is (no fallback note there — D-06 applies to the success line only). Keep router.refresh() (L88).
    - Success render (L123-132): replace the hardcoded `<p ...>Analysis complete</p>` text with a template that appends the fallback note ONLY when usedFallback is truthy (D-06): render `{`Analysis complete${state.usedFallback ? ` — ran on ${state.modelUsedName ?? state.modelUsed} (fallback)` : ''}`}` — the display name comes from modelUsedName (server-computed catalog name); fall back to the raw modelUsed id when the name is absent. Normal success stays exactly `Analysis complete`. Keep the `Review N proposals` Link and the surrounding div/classes unchanged.
    - Failure render (L142-158) needs NO change — the new `rate_limited` reason flows through the existing `errorMessage()` lookup (L40-42) automatically.
    - No imports added/removed; no new dependencies.
  </action>
  <acceptance_criteria>
    - `ERROR_COPY` contains exactly one new key `rate_limited` with the exact string `'Rate limited — try again in a moment'`
    - RunState success variant includes `modelUsed?: string; modelUsedName?: string; usedFallback?: boolean`
    - Success render uses `Analysis complete${state.usedFallback ? ` — ran on ${state.modelUsedName ?? state.modelUsed} (fallback)` : ''}` — normal success unchanged, fallback note only when usedFallback is truthy
    - The run() json cast includes modelUsed/modelUsedName/usedFallback and setState passes them through
    - `npx tsc --noEmit` exits 0
    - Grep gate: `grep -c "rate_limited" src/components/agents/analyze-run-status.tsx` ≥ 1 (the ERROR_COPY key row — the error-body reference is a dynamic lookup, not a literal), and the only new ERROR_COPY row is rate_limited
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -c "Rate limited — try again in a moment" src/components/agents/analyze-run-status.tsx</automated>
  </verify>
  <done>Status strip shows the rate_limited copy and the success-after-fallback note; normal success unchanged; compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → client component | Response fields (modelUsed/modelUsedName/usedFallback) are rendered into JSX — must be treated as untrusted strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | fallback-note interpolation | mitigate | Display name is server-computed (`modelUsedName` from the catalog snapshot via the route) and rendered as a React text child — JSX auto-escapes, no dangerouslySetInnerHTML; raw provider IDs are allowlist-gated upstream (T-16-01). Even a hostile model id renders inert text (ASVS V5 output encoding) |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | Zero install surface — no packages installed by this plan; slopcheck gate not applicable |
</threat_model>

<verification>
- `npx tsc --noEmit` — clean
- Grep gate: `grep -c "Rate limited — try again in a moment" src/components/agents/analyze-run-status.tsx` — exactly 1 (the ERROR_COPY row)
- Grep gate: `grep -c "ran on" src/components/agents/analyze-run-status.tsx` — exactly 1 (the D-06 fallback note template)
- Manual/browser verification is deferred to Phase 18 VER-03 live-browser UAT (repo has no component tests — QLTY-01 constraint, 16-VALIDATION.md)
</verification>

<success_criteria>
- Rate-limited runs surface "Rate limited — try again in a moment" (D-04)
- Success-after-fallback surfaces "Analysis complete — ran on {display name} (fallback)" (D-06)
- Normal success and the failure strip are unchanged in behavior
- No catalog import in the client bundle (display name stays server-side, D-07)
</success_criteria>

<output>
Create `.planning/phases/16-failover-orchestration/16-04-SUMMARY.md` when done
</output>
