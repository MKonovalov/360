---
phase: 16-failover-orchestration
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [src/lib/agents/runAgent.ts, src/lib/agents/runAgent.test.ts]
autonomous: true
requirements: [FAL-03, FAL-04]
user_setup: []
must_haves:
  truths:
    - "runAgent iterates the resolved chain; a failover-eligible error (404/5xx/connection) on the primary advances to the fallback"
    - "429 and 4xx/output/config errors never advance the chain — the run fails loud after a single attempt (D-01, Pitfall 2)"
    - "Each attempt passes timeout: { totalMs } — 35s primary / 20s fallback — so 35+20=55s < 60s maxDuration holds by construction (FAL-04)"
    - "The loop returns { ...result, modelUsed, usedFallback } — the audit identity survives to persistence (Pitfall 5)"
    - "Chain exhaustion rethrows the last error — the existing 502 analysis_failed contract, never a silent switch (D-06)"
  artifacts:
    - path: "src/lib/agents/runAgent.ts"
      provides: "Failover chain loop over LanguageModel[] with per-attempt totalMs budgets"
      exports: ["runAgent", "RunAgentInput"]
    - path: "src/lib/agents/runAgent.test.ts"
      provides: "Loop cases: 404 advances, 429 stops, 400 stops, exhaustion rethrows, timeout shape (FAL-03/04)"
      contains: "usedFallback"
  key_links:
    - from: "src/lib/agents/runAgent.ts"
      to: "./modelConfig"
      via: "import { classifyModelError, isFailoverEligible }"
      pattern: "isFailoverEligible\\(classifyModelError"
    - from: "src/lib/agents/runAgent.ts"
      to: "@/lib/models/catalog"
      via: "import { FAST_MODEL_ID } (relocated constant)"
      pattern: "FAST_MODEL_ID"
    - from: "src/lib/agents/runAgent.ts"
      to: "generateText"
      via: "timeout: { totalMs: i === 0 ? timeouts.primaryMs : timeouts.fallbackMs }"
      pattern: "totalMs"
---

<objective>
Convert `runAgent` from a single-model call into the bounded failover chain loop: iterate `LanguageModel[]`, classify each attempt's error via `modelConfig`, advance only on failover-eligible classes, cap each attempt's total time with `{ totalMs }`, and return the audit identity (`modelUsed`, `usedFallback`) that Phase 15's `createRun` seam persists.

Purpose: FAL-03 (failover loop + fail-loud) and FAL-04 (60s budget) — the loop is the app's only safety net for model-availability drift (no SDK fallback helper exists, verified). The budget math is locked by a why-comment so nobody "fixes" the timeout later.
Output: `runAgent.ts` chain loop + extended `runAgent.test.ts` (loop cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-failover-orchestration/16-RESEARCH.md
@.planning/phases/16-failover-orchestration/16-PATTERNS.md
@src/lib/agents/modelConfig.ts
@src/lib/agents/runAgent.ts
@src/lib/agents/runAgent.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the single-model seam with the failover chain loop</name>
  <files>src/lib/agents/runAgent.ts</files>
  <read_first>
    - src/lib/agents/runAgent.ts (current 39-line single-call body; FAST_MODEL_ID const at line 13)
    - src/lib/agents/modelConfig.ts (classifyModelError / isFailoverEligible — produced by plan 16-01, MUST exist)
    - 16-RESEARCH.md Pattern 2 (lines 287-321, the loop) + Pitfall 4 (lines 371-375, budget math)
    - 16-PATTERNS.md lines 143-204 (import block delta, interface delta, core loop)
    - src/lib/models/catalog.ts (FAST_MODEL_ID now exported from here)
  </read_first>
  <action>
    Modify `src/lib/agents/runAgent.ts` per 16-PATTERNS.md lines 143-204:
    Import block (lines 1-5): keep `anthropic` from '@ai-sdk/anthropic'; extend the `ai` import to `import { generateText, isStepCount, Output, type LanguageModel } from 'ai'`; ADD `import { classifyModelError, isFailoverEligible } from './modelConfig';` and `import { FAST_MODEL_ID } from '@/lib/models/catalog';`. DELETE the local `const FAST_MODEL_ID = 'claude-sonnet-4-6';` (line 13) — it now lives in catalog.ts (relocated in 16-01; its roster-verification comment moved with it).
    `RunAgentInput` interface (lines 15-19): replace `model?: ReturnType<typeof anthropic>` with:
    - `models?: LanguageModel[]` (the ordered chain; replaces the single-model seam — PATTERNS line 162)
    - `timeouts?: { primaryMs: number; fallbackMs: number }` (FAL-04; defaults below)
    Body (lines 27-39) becomes the chain loop from RESEARCH Pattern 2 (lines 298-320), verbatim in behavior:
    - Signature destructure gains `models = [anthropic(FAST_MODEL_ID)]` and `timeouts = { primaryMs: 35_000, fallbackMs: 20_000 }` defaults (D-09: a single-model chain runs through the same loop — no special-casing; the default keeps the old no-args call working so existing call sites/tests stay green).
    - `let lastError: unknown;` before the loop; `for (let i = 0; i < models.length; i++)` with an inner try/catch.
    - `generateText` call keeps `model: models[i]`, `tools: { webSearch: webSearchTool }`, `prompt: buildAnalyzePrompt(company, liveSignals)`, `stopWhen: isStepCount(12)`, `output: Output.object({ schema: outputSchema })` — UNCHANGED from current.
    - ADD `timeout: { totalMs: i === 0 ? timeouts.primaryMs : timeouts.fallbackMs }` with the mandated FAL-04 why-comment (house convention, RESEARCH Pitfall 4 / PATTERNS lines 184-190): `{ totalMs }` is the TOTAL budget for the whole call INCLUDING the SDK's own retries + backoff (verified: mergeAbortSignals feeds the retry loop's abort signal) — 55s worst case (35+20) holds under Vercel's 60s maxDuration (route.ts:16) with ~5s margin for DB reads/writes + trace URL lookup. Keep SDK default maxRetries: 2; never hand-roll AbortController + setTimeout.
    - Success: `return { ...result, modelUsed: models[i].modelId, usedFallback: i > 0 };` (FAL-05 audit identity; `modelId` verified present on the model-object shape by runAgent.test.ts:64 mock — Pitfall 11 pre-flight note: confirm via `npx tsc --noEmit`).
    - Catch: `lastError = err; if (!isFailoverEligible(classifyModelError(err))) throw err;` (Pitfall 2/3 gate — 429/4xx/output/config never burn a fallback; D-01).
    - After the loop: `throw lastError;` (chain exhausted — fail loud, never a silent switch; propagates to the route's 502 analysis_failed).
  </action>
  <acceptance_criteria>
    - `runAgent.ts` imports `classifyModelError, isFailoverEligible` from './modelConfig' and `FAST_MODEL_ID` from '@/lib/models/catalog'; no local FAST_MODEL_ID const remains
    - `RunAgentInput` has `models?: LanguageModel[]` and `timeouts?: { primaryMs: number; fallbackMs: number }`; no `model?:` field remains
    - The loop body passes `timeout: { totalMs: ... }` on every generateText call with i===0 → primaryMs (35000 default) and i>0 → fallbackMs (20000 default)
    - The loop contains exactly one `isFailoverEligible(classifyModelError(err))` gate and one `throw lastError` after the loop
    - Success returns `{ ...result, modelUsed: models[i].modelId, usedFallback: i > 0 }`
    - `npx tsc --noEmit` exits 0 (Pitfall 11 pre-flight: modelId property + totalMs option verified against installed ai@7.0.45 types)
    - `grep -c "totalMs" src/lib/agents/runAgent.ts` ≥ 1 and the why-comment text `55s worst case` is present
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>runAgent is a failover loop over LanguageModel[] with per-attempt totalMs budgets, eligibility gate, audit-identity return, and exhaustion rethrow; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend runAgent.test.ts — loop cases + deliberate L87 return-shape update</name>
  <files>src/lib/agents/runAgent.test.ts</files>
  <read_first>
    - src/lib/agents/runAgent.test.ts (existing mock seam lines 7-28, describe block 61-99, L87 assertion)
    - 16-PATTERNS.md lines 208-228 (deliberate L87 update + new loop cases)
    - 16-RESEARCH.md Phase Requirements → Test Map FAL-03/04 row + Common Operation 1 (constructed APICallError/RetryError)
  </read_first>
  <action>
    Update `src/lib/agents/runAgent.test.ts`:
    - DELIBERATELY update line 87: `expect(result).toEqual(resolvedRun)` → `expect(result).toEqual({ ...resolvedRun, modelUsed: 'claude-sonnet-4-6', usedFallback: false })` — the return grows to `{ ...result, modelUsed, usedFallback }`; update the expectation, do NOT delete the assertion (Pitfall 10 checklist / 16-VALIDATION.md Wave 0).
    - Import `{ APICallError, RetryError } from 'ai'` at the top (the existing vi.mock('ai') uses `importOriginal` spread, so real classes stay available for constructing error instances — RESEARCH Common Operation 1 pattern: `new APICallError({ message, url: 'https://api.anthropic.com/v1/messages', requestBodyValues: {}, statusCode })`). Keep the existing hoisted mock block and beforeEach EXACTLY as-is (mocks.generateText.mockResolvedValue(resolvedRun), mocks.anthropic.mockReturnValue({ provider: 'anthropic', modelId: 'claude-sonnet-4-6' })).
    - New `describe('runAgent failover loop (FAL-03/04)')` block with these cases (assert via `mocks.generateText.mock.calls[i][0].timeout`):
      1. primary 404 → generateText called twice; second call receives `{ totalMs: 20000 }`; result carries `usedFallback: true` and `modelUsed: 'claude-sonnet-4-6'`. Setup: `mocks.generateText.mockRejectedValueOnce(new APICallError({ message: 'model not found', url: 'u', requestBodyValues: {}, statusCode: 404 })).mockResolvedValueOnce(resolvedRun)`.
       2. 429 → called once, throws the 429 error (D-01 never advances). Setup: mockRejectedValueOnce(apiErr(429)); assert `await expect(runAgent(...)).rejects.toThrow()` and `mocks.generateText.toHaveBeenCalledTimes(1)`.
      3. 400 → called once, throws (Pitfall 2 non-failover).
      4. all models fail (e.g. 2 models, both 5xx) → rethrows the LAST error (exhaustion, never a silent undefined). Setup: `mockRejectedValueOnce(apiErr(500)).mockRejectedValueOnce(apiErr(502))` with `models: [anthropic(...), anthropic(...)]`; assert rejects with the 502 error instance.
      5. per-attempt timeout shape (FAL-04): with two models, assert `mocks.generateText.mock.calls[0][0].timeout` equals `{ totalMs: 35000 }` and `mock.calls[1][0].timeout` equals `{ totalMs: 20000 }` (defaults), or the passed `timeouts` override.
      - Pass `models: [mocks.anthropic(), mocks.anthropic()]` explicitly for the multi-attempt cases (or rely on the default chain); mocks.anthropic returns the { provider, modelId } stub per beforeEach.
    - Keep the existing 3 tests (including the initLangfuse negative test) intact and green.
  </action>
  <acceptance_criteria>
    - Line 87 assertion updated to `expect(result).toEqual({ ...resolvedRun, modelUsed: 'claude-sonnet-4-6', usedFallback: false })` — assertion not deleted
    - New loop cases cover: 404-advances-with-20s-second-attempt, 429-single-attempt-throws, 400-single-attempt-throws, exhaustion-rethrows-last-error, per-attempt totalMs shape
    - All cases assert per-attempt timeout via `mock.calls[i][0].timeout`
    - `npx vitest run src/lib/agents/runAgent.test.ts` exits 0 (all loop cases + 3 original tests green)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/agents/runAgent.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Loop behavior locked by tests: 404 advances, 429/400 never advance, exhaustion rethrows, per-attempt budgets asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider-API → generateText | Untrusted provider responses/errors cross here; the SDK's retry machinery wraps them (RetryError) |
| runAgent → caller | The returned result now carries model identity (modelUsed/usedFallback) that flows to persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02 | DoS (cost) | eligibility gate in the loop | mitigate | `isFailoverEligible(classifyModelError(err))` restricts advancement to model/connection/server errors only — a broken primary cannot burn the fallback on 429/4xx/output/config (Pitfall 2; RESEARCH Security Domain row 2) |
| T-16-03 | DoS (cost/latency) | 429 handling in the loop | mitigate | D-01: 429 classifies rate_limited → isFailoverEligible false → the run throws after the SDK's own retries; the fallback never re-hits the same account-level limit (RESEARCH Security Domain row 3) |
| T-16-05 | DoS (resource exhaustion) | per-attempt timeouts | mitigate | `timeout: { totalMs }` per attempt (35s/20s) is a HARD total INCLUDING SDK retries+backoff (verified mergeAbortSignals); 55s worst case < 60s maxDuration holds by construction; budget math documented in the loop why-comment (Pitfall 4/6) |
| T-16-06 | (non-STRIDE) observability | audit identity on return | mitigate | Loop returns modelUsed/usedFallback so agent_run.model_used/model_chain stay the durable truth; Langfuse per-attempt spans are the best-effort mirror (RESEARCH Security Domain row 6) |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | Zero install surface — no packages installed by this plan; slopcheck gate not applicable |
</threat_model>

<verification>
- `npx vitest run src/lib/agents/runAgent.test.ts src/lib/agents/modelConfig.test.ts` — full green (loop + classifier together)
- `npx tsc --noEmit` — clean (modelId + totalMs verified against installed ai@7.0.45)
- Grep hygiene: `grep -rn "AIConnectionError" src/lib/agents/runAgent.ts` — 0 hits
- Grep hygiene: `grep -n "FAST_MODEL_ID" src/lib/agents/runAgent.ts` — only the catalog import + default-chain usage, no local const
</verification>

<success_criteria>
- runAgent iterates the chain with per-attempt { totalMs } budgets (35s/20s defaults)
- Only model_not_found/server_error/connection advance; 429/4xx/output/config fail loud after one attempt
- Exhaustion rethrows the last error (502 contract preserved, never a 504, never a silent switch)
- Return shape { ...result, modelUsed, usedFallback } locked by the deliberately-updated L87 assertion
- FAST_MODEL_ID relocated to catalog.ts — no circular import, no local duplicate
</success_criteria>

<output>
Create `.planning/phases/16-failover-orchestration/16-02-SUMMARY.md` when done
</output>
