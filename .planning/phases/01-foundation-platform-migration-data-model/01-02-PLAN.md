---
phase: 01-foundation-platform-migration-data-model
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/index.ts
  - drizzle.config.ts
  - .env.local
  - .env.example
autonomous: false
requirements: [FOUND-02, DATA-02, DATA-03]

user_setup:
  - service: "Neon Postgres (via Vercel Marketplace integration)"
    why: "Phase 1's schema (company/persona/signal/companyPersonaRole) must be pushed to a live Postgres instance"
    env_vars:
      - name: DATABASE_URL
        source: "Vercel Dashboard -> project 360-arclumen -> Storage tab -> Neon integration (auto-injected), or `npx vercel env pull .env.local`"
      - name: DATABASE_URL_UNPOOLED
        source: "Same as above, auto-injected by the Neon Marketplace integration"
    dashboard_config:
      - task: "Connect the Neon Marketplace integration to project 360-arclumen"
        location: "Vercel Dashboard -> Storage tab -> Browse Marketplace -> Neon -> Connect"

must_haves:
  truths:
    - "Company, Persona, Signal, and CompanyPersonaRole tables exist in Neon Postgres"
    - "Signal type and strength are enforced as Postgres enums, not free text"
    - "Company and Persona relate only through the CompanyPersonaRole join table with date-range metadata"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "Drizzle schema: company/persona/signal/companyPersonaRole tables + signalType/signalStrength enums"
      contains: "companyPersonaRole"
    - path: "src/lib/db/index.ts"
      provides: "Shared Drizzle client (neon-http driver)"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config for push/generate"
  key_links:
    - from: "src/lib/db/schema.ts signal table"
      to: "company table"
      via: "companyId foreign key"
      pattern: "references\\(\\(\\) => company\\.id\\)"
    - from: "src/lib/db/schema.ts companyPersonaRole table"
      to: "company + persona tables"
      via: "many-to-many FKs plus startDate/endDate/isCurrent"
      pattern: "isCurrent"
---

<objective>
Provision (or confirm) the Neon Postgres instance, define the Drizzle schema (Company, Persona, Signal, CompanyPersonaRole + two enums per DATA-02/DATA-03/D-05/D-06/D-07), and push it live with `drizzle-kit push`.

Purpose: This is the data-platform half of the walking skeleton (FOUND-02) — nothing in 01-03 (seed script) or 01-04 (dashboard read) can run until real tables exist in Neon.
Output: A live Neon Postgres database with the four Phase 1 tables and two enums applied, queryable via the shared Drizzle client.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-platform-migration-data-model/01-CONTEXT.md
@.planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md
@.planning/phases/01-foundation-platform-migration-data-model/01-PATTERNS.md
@src/lib/env.ts
@.vercel/project.json
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm/provision Neon Postgres via Vercel Marketplace, sync DATABASE_URL locally</name>
  <files>.env.local</files>
  <read_first>
    .vercel/project.json, .env.local (post-01-01 state), .planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md Open Question #1 and Environment Availability table
  </read_first>
  <what-built>
    Verified via `npx vercel link` (already linked to `prj_DbEzimzON9nzF7Nmk7Nueta7k00V` per `.vercel/project.json`) and `npx vercel env ls` that no CLI-only path exists to provision a NEW Marketplace database integration non-interactively — `vercel integration add <name>` requires an interactive terminal/browser to accept marketplace legal terms (confirmed via `vercel integration --help`).
  </what-built>
  <action>
    Human action required (no CLI/API path exists for the provisioning step itself):
    1. Run `npx vercel env ls` yourself. If `DATABASE_URL` is already listed, skip to step 4.
    2. If missing, provision Neon via the Vercel Dashboard: open https://vercel.com/dashboard -> project `360-arclumen` -> Storage tab -> Browse Marketplace -> Neon -> Connect to this project. This auto-injects `DATABASE_URL` and `DATABASE_URL_UNPOOLED` into all environments.
    3. Alternatively, run `npx vercel integration add neon` yourself in an interactive terminal and connect it to `360-arclumen`.
    4. Back up the current `.env.local` (it has live Clerk keys from 01-01), then run `npx vercel env pull .env.local` to sync `DATABASE_URL`/`DATABASE_URL_UNPOOLED` locally. Diff the result against your backup — if `vercel env pull` did not carry over `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`/`CLERK_SECRET_KEY` (they may only exist locally, not yet in Vercel's env store), manually restore those two lines from the backup.
    Resume signal: reply "provisioned" once `.env.local` has a working `DATABASE_URL` and the Clerk keys are still intact.
  </action>
  <acceptance_criteria>
    - `.env.local` contains a `DATABASE_URL=` line whose value starts with `postgres://` or `postgresql://` and is non-empty.
    - `.env.local` still contains `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY` with their prior (01-01) values intact — not lost by an env-pull overwrite.
    - `npx vercel env ls` (or the Vercel dashboard) lists `DATABASE_URL` for at least the Development environment.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this is a human-action checkpoint (Vercel Marketplace provisioning has no non-interactive CLI path); Task 3's `npx drizzle-kit push` provides the first automated confirmation that DATABASE_URL actually works</automated>
  </verify>
  <done>`.env.local` has a working `DATABASE_URL` (and `DATABASE_URL_UNPOOLED`), Clerk keys from 01-01 remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Define Drizzle schema (company, persona, signal, companyPersonaRole) and the shared db client</name>
  <files>src/lib/db/schema.ts, src/lib/db/index.ts, drizzle.config.ts</files>
  <read_first>
    .planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md (Pattern 2 code block, Drizzle connection setup, drizzle.config.ts examples), .planning/phases/01-foundation-platform-migration-data-model/01-CONTEXT.md (D-05, D-06, D-07, DATA-02, DATA-03), src/lib/env.ts
  </read_first>
  <action>
    Create `src/lib/db/schema.ts` using the exact structure from 01-RESEARCH.md's Pattern 2: `signalTypeEnum = pgEnum('signal_type', ['cost_pressure','immature_gbs_org','new_cfo_or_gbs_head','transformation_announcement'])` (D-07, 4 seeded values — fixed-but-extensible, a 5th value later is a `drizzle-kit generate` migration, not a redesign); `signalStrengthEnum = pgEnum('signal_strength', ['low','medium','high'])` (D-05, 3-tier, not numeric); `company` table (`id` serial PK, `name` text not null, `industry` text nullable, `createdAt` timestamp default now — remaining firmographic columns land in Phase 2 per COMP-01, do not add them now); `persona` table (`id` serial PK, `name` text not null, `title` text nullable, `createdAt` timestamp default now — remaining fields land in Phase 3 per PERS-01..04); `signal` table (`id` serial PK, `companyId` integer not null `.references(() => company.id)`, `signalType` using `signalTypeEnum` not null, `strength` using `signalStrengthEnum` not null, `source` text nullable, `detectedAt` date not null, `note` text nullable (D-06 — keep alongside the typed columns, never replace them), `createdAt` timestamp default now); `companyPersonaRole` table (`id` serial PK, `companyId` integer not null `.references(() => company.id)`, `personaId` integer not null `.references(() => persona.id)`, `title` text nullable, `isCurrent` boolean not null default false, `startDate` date nullable, `endDate` date nullable — DATA-02: this join table is the ONLY way Company and Persona relate; never add a scalar `companyId` FK directly on the `persona` table). Create `src/lib/db/index.ts`: import `neon` from `@neondatabase/serverless`, `drizzle` from `drizzle-orm/neon-http`, `* as schema` from `./schema`, and `env` from `../env` (the zod-validated export from 01-01, NOT raw `process.env.DATABASE_URL!`); construct `const sql = neon(env.DATABASE_URL)`; export `export const db = drizzle({ client: sql, schema })`. Create `drizzle.config.ts` at the repo root: `import 'dotenv/config'; import { defineConfig } from 'drizzle-kit'; export default defineConfig({ out: './drizzle', schema: './src/lib/db/schema.ts', dialect: 'postgresql', dbCredentials: { url: process.env.DATABASE_URL! } })` — use raw `process.env.DATABASE_URL!` here, not `src/lib/env.ts`'s `env`, since this file runs standalone via `dotenv` outside the Next.js app's own env loading.
  </action>
  <acceptance_criteria>
    - `src/lib/db/schema.ts` exports `signalTypeEnum`, `signalStrengthEnum`, `company`, `persona`, `signal`, `companyPersonaRole`.
    - `grep -c "note" src/lib/db/schema.ts` returns at least `1` (free-text `note` column present, D-06).
    - The text of `src/lib/db/schema.ts` between `export const persona = pgTable` and the next `export const` block contains no `companyId` (proves DATA-02: no scalar FK on `persona`).
    - `src/lib/db/index.ts` exports `db` and imports `env` from `../env` (not raw `process.env`).
    - `drizzle.config.ts` exists at the repo root with `dialect: 'postgresql'` and `schema: './src/lib/db/schema.ts'`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`src/lib/db/schema.ts`, `src/lib/db/index.ts`, and `drizzle.config.ts` exist matching the structure above; `npm run build` type-checks cleanly.</done>
</task>

<task type="auto" blocking="true">
  <name>Task 3: [BLOCKING] Run drizzle-kit push, verify tables and enum constraints are live in Neon</name>
  <files>none (applies schema.ts to the live database, no repo files change)</files>
  <read_first>
    drizzle.config.ts, .env.local (DATABASE_URL from Task 1), src/lib/db/schema.ts (Task 2), .planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md Schema iteration commands section
  </read_first>
  <action>
    [BLOCKING] Run `npx drizzle-kit push` from the repo root (dotenv loads `DATABASE_URL` from `.env.local` via `drizzle.config.ts`'s `import 'dotenv/config'`). This applies the four tables and two enums directly to Neon — `push` generates no `drizzle/*.sql` files (that only happens with `drizzle-kit generate`, out of scope this phase per 01-RESEARCH.md). If the command prompts for a destructive-change confirmation, treat that as a signal to inspect the existing table shape before answering — do not blindly accept a data-loss prompt; at this point in Phase 1 there should be no real seed data at risk (D-03's real handoff happens in 01-03/execution, not before). After push succeeds, verify all four tables exist and are queryable: run a throwaway `npx tsx -e` script that imports `db` and each table from `src/lib/db`/`src/lib/db/schema`, runs `await db.select().from(company).limit(1)`, `.from(persona)`, `.from(signal)`, `.from(companyPersonaRole)`, and logs each result (expect `[]` with no thrown error — proves the table exists and is queryable, not just that `push` exited 0). Then run a second throwaway script that attempts `db.insert(signal).values({ companyId: 999999, signalType: 'not_a_real_type' as any, strength: 'low', detectedAt: '2026-01-01' })` and confirms it throws a Postgres error containing `invalid input value for enum` (proves DATA-03's enum constraint is enforced at the database level, not just in TypeScript types) — this insert must fail, so no cleanup is needed.
  </action>
  <acceptance_criteria>
    - `npx drizzle-kit push` exits `0`.
    - A direct Drizzle `select().from(...)` against each of `company`, `persona`, `signal`, `companyPersonaRole` returns `[]` with no thrown error.
    - Attempting to insert a `signal` row with an invalid `signalType` enum value throws a Postgres error containing `invalid input value for enum signal_type` — proving the enum constraint is enforced by the database, not just at the TypeScript layer.
  </acceptance_criteria>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <done>Neon Postgres has `company`, `persona`, `signal`, `company_persona_role` tables plus `signal_type`/`signal_strength` enums live and queryable; an invalid enum insert is rejected by Postgres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Next.js server -> Neon Postgres | All queries flow through the shared `db` client (`src/lib/db/index.ts`); no direct client access to Postgres ever exists |
| `drizzle.config.ts` CLI process -> Neon Postgres | `drizzle-kit push`/future `generate`/`migrate` runs outside the request path, using the same `DATABASE_URL` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-1-03 | Tampering | `src/lib/db/schema.ts`, all future query functions | mitigate | Drizzle's query builder parameterizes all values by default — no raw SQL string concatenation is used anywhere in this plan; if raw SQL is ever needed later, Drizzle's `sql` tagged-template helper must be used, which still parameterizes interpolated values |
| T-1-04 | Information Disclosure | `DATABASE_URL` in `.env.local` / Vercel Project Settings | mitigate | `DATABASE_URL` is never prefixed `NEXT_PUBLIC_`; `src/lib/db/index.ts` is server-only code (never imported into a Client Component); `drizzle.config.ts` reads `process.env.DATABASE_URL!` directly and never logs it |
| T-1-SC | Tampering | No new package installs this plan | accept | Continuity of T-1-SC from 01-01 — no new dependencies introduced here |

</threat_model>

<verification>
`npx drizzle-kit push` exits 0; the throwaway select/insert checks in Task 3 both pass as described. Manual smoke check (deferred to end-of-phase): open `npx drizzle-kit studio` and visually confirm the four tables/two enums.
</verification>

<success_criteria>
- Neon Postgres is provisioned and reachable via `DATABASE_URL`.
- `company`, `persona`, `signal`, `companyPersonaRole` tables exist with the schema shape locked by DATA-02/DATA-03/D-05/D-06/D-07.
- Postgres enforces the `signal_type`/`signal_strength` enum constraints at the database level.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-platform-migration-data-model/01-02-SUMMARY.md` when done
</output>
