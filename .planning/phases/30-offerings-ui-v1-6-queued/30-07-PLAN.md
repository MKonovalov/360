---
phase: 30-offerings-ui-v1-6-queued
plan: 07
type: execute
wave: 5
depends_on: ["30-02", "30-06"]
files_modified:
  - src/components/offerings/buyer-role-panel.tsx
autonomous: true
requirements: [OFR-06, OFR-08]

must_haves:
  truths:
    - "A staff member can open a 'Manage Buyer Roles' Sheet showing every buyer role (name + description), create a new one inline, edit an existing one, and delete one (guarded) — this is the single shared place buyer roles are managed"
    - "Deleting a Buyer Role that is still referenced by an Offering's ranked buyer roles or a Persona Signal is refused, never silently cascaded"
  artifacts:
    - path: "src/components/offerings/buyer-role-panel.tsx"
      provides: "BuyerRolePanel — Sheet lookup CRUD, list + inline create + edit + guarded delete"
      min_lines: 100
  key_links:
    - from: "src/components/offerings/buyer-role-panel.tsx"
      to: "src/app/actions/buyerRoles.ts"
      via: "createBuyerRoleAction / updateBuyerRoleAction"
      pattern: "createBuyerRoleAction|updateBuyerRoleAction"
    - from: "src/components/offerings/buyer-role-panel.tsx"
      to: "src/components/offerings/delete-guard-dialog.tsx"
      via: "DeleteGuardDialog with onDelete={() => deleteBuyerRoleAction(role.id)} — buyer_role has no status column so its ONLY removal path is this guarded delete"
      pattern: "DeleteGuardDialog"
---

<objective>
Build the shared Buyer Role lookup CRUD panel (OFR-06) — the single place both Offerings and Signals reference for managing the firm-wide buyer-role list. Opened as a Sheet from a "Manage Buyer Roles" button that will be placed on both Offerings tabs (30-10).

Purpose: Close out the last entity kind's UI surface before the two composite hierarchy/matrix components (Wave 5's siblings) and the final page shell (Wave 6) can wire everything together.
Output: `buyer-role-panel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-CONTEXT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: BuyerRolePanel (Sheet, list + inline create + edit + guarded delete)</name>
  <files>src/components/offerings/buyer-role-panel.tsx</files>
  <read_first>
    src/components/signals/signal-form.tsx lines 1-97, 148-160 (Sheet controlled-open state pattern — copy the open-state wiring, not the multi-field form body)
    src/components/signals/signal-table.tsx lines 140-225 (TableRow-per-item + trailing action-button cluster shape — adapt to a plain div-per-row list, not a Table, per UI-SPEC's row anatomy)
    src/components/offerings/delete-guard-dialog.tsx (built in 30-06 — its exact prop contract: `{ entityLabel, onDelete, trigger? }`)
    src/app/actions/buyerRoles.ts (as it exists after 30-02 — createBuyerRoleAction/updateBuyerRoleAction/deleteBuyerRoleAction signatures)
    src/lib/db/queries/buyerRoles.ts (listBuyerRoles's return shape: `{ id, name, description, createdBy, updatedBy, createdAt, updatedAt }[]`)
    .planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md lines 179-183 (row anatomy: name Body/14/600 semibold — the one place font-semibold is used at body size — description Label/12/400 slate-500, trailing Edit pencil-icon + Delete trash-icon ghost buttons; "New Buyer Role" opens an INLINE expansion at the top of the list, not a nested Sheet/Dialog; empty-state copy "No buyer roles yet" / "Click New Buyer Role to create the first one.")
  </read_first>
  <action>
    Per D-05, create `BuyerRolePanel` in `src/components/offerings/buyer-role-panel.tsx`, a `'use client'` component — the single shared lookup CRUD panel for both Offerings and Signals. Props: `{ buyerRoles: Array<{ id: number; name: string; description: string | null }>; trigger: React.ReactNode }`. Controlled Sheet `open`/`setOpen` state (default `sm:max-w-sm` width, no override needed — this is a simple list, not the widened Offering form).

    Internal state: `creating: boolean` (toggles the inline create-form expansion), `newName`/`newDescription` (create-form fields), `editingId: number | null` (which row, if any, is in inline edit mode), `editName`/`editDescription` (edit-form fields for that row), `pending`/`startTransition`, `error`.

    Sheet body: `SheetHeader` with `SheetTitle` "Manage Buyer Roles". Inside the scrollable content: a `Button variant="default"` "New Buyer Role" that toggles `creating`; when `creating` is true, render an inline `name` `Input` + `description` `Textarea` pair with Save/Cancel buttons directly above the role list (NOT a second Sheet or Dialog — this is the explicit "inline expansion" decision, avoiding Sheet-over-Sheet stacking). Save calls `createBuyerRoleAction({ name: newName.trim(), description: newDescription.trim() || undefined })`; on success, reset the create fields, set `creating` to false, `router.refresh()`.

    If `buyerRoles.length === 0` (and not currently creating), render the empty state: heading "No buyer roles yet", body `"Click **New Buyer Role** to create the first one."` (matching the Copywriting Contract's bold-keyword convention — render the literal "New Buyer Role" text in a `<strong>` tag, same pattern as signal-table.tsx's empty-state strong usage).

    Otherwise, map `buyerRoles` to a row list: each row `<div className="flex items-center justify-between gap-2 rounded-md border border-slate-200 p-2">`. When `editingId !== role.id`: left side shows `<p className="text-sm font-semibold text-foreground">{role.name}</p>` and `<p className="text-[12px] text-slate-500">{role.description}</p>`; right side shows two `Button variant="ghost" size="icon"` — an Edit button (`Pencil` icon, `aria-label={'Edit ' + role.name}`) that sets `editingId` to this role's id and seeds `editName`/`editDescription`, and a `DeleteGuardDialog` (imported from `./delete-guard-dialog`) with `entityLabel="Buyer Role"`, `onDelete={() => deleteBuyerRoleAction(role.id)}`, and `trigger={<Button variant="ghost" size="icon" aria-label={'Delete ' + role.name}><Trash2 /></Button>}`.

    When `editingId === role.id`: render the row as an inline `name`/`description` edit form (same shape as the create form) with Save (`updateBuyerRoleAction(role.id, { name: editName.trim(), description: editDescription.trim() || undefined })`) and Cancel (sets `editingId` back to `null`) buttons.

    Import `Pencil`, `Trash2` from `lucide-react`. Import `createBuyerRoleAction`, `updateBuyerRoleAction`, `deleteBuyerRoleAction` from `@/app/actions/buyerRoles`.
  </action>
  <acceptance_criteria>
    - Source: `buyer-role-panel.tsx` exports `function BuyerRolePanel` (or const)
    - Source: imports `DeleteGuardDialog` from `./delete-guard-dialog` and passes it `entityLabel="Buyer Role"` and an `onDelete` callback wrapping `deleteBuyerRoleAction`
    - Source: does NOT import or render `Dialog`/`Sheet` a second time for the create form — the create form is an inline expansion inside the SAME Sheet's content area, not a nested modal (`grep -c "SheetContent\|DialogContent" src/components/offerings/buyer-role-panel.tsx` returns exactly 1, for the outer panel Sheet only)
    - Source: uses `Table`/`TableRow` nowhere — this is a div-based row list per UI-SPEC's row anatomy, not a Table component
    - Test command: `npx tsc --noEmit` passes with no errors attributable to this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>BuyerRolePanel renders the list, supports inline create/edit, and every delete routes through the guarded DeleteGuardDialog — no second modal layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| BuyerRolePanel create/edit form submit → buyerRoles.ts Server Actions | Untrusted client input for name/description |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07-01 | Repudiation | Buyer Role delete guard | mitigate | Routed exclusively through `DeleteGuardDialog`'s `onDelete` callback wrapping `deleteBuyerRoleAction`, which itself passes the query layer's `has_dependents` result straight through unmodified (30-02) — this panel introduces no second, unguarded removal path for buyer roles |
| T-30-07-02 | Tampering | Inline create/edit form validation | accept | Client-side only convenience (non-empty name check before enabling Save); the authoritative validation is `buyerRoleInputSchema`'s zod `safeParse` in buyerRoles.ts, which runs regardless of what this UI sends |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- Manual: opening the panel shows all 5 seeded GBS buyer roles; attempting to delete "CFO" (referenced by seeded offering_buyer_role rows) shows the blocked DeleteGuardDialog message
</verification>

<success_criteria>
- BuyerRolePanel is the single, complete lookup CRUD surface for buyer roles, with the guarded delete correctly wired
</success_criteria>

<output>
Create `.planning/phases/30-offerings-ui-v1-6-queued/30-07-SUMMARY.md` when done
</output>
