---
phase: 30-offerings-ui-v1-6-queued
plan: 09
type: execute
wave: 5
depends_on: ["30-05", "30-06"]
files_modified:
  - src/lib/params/offeringsFilters.ts
  - src/components/offerings/offerings-filters.tsx
  - src/components/offerings/offerings-matrix.tsx
autonomous: true
requirements: [OFR-05]

must_haves:
  truths:
    - "A staff member can filter the Matrix tab by Practice Area (URL-synced, so a shared link preserves the filter)"
    - "The Matrix table groups offerings under Design/Build/Run-style Domain section headers within the filtered Practice Area"
    - "A staff member can add or remove a Trigger, and edit ranked Primary Buyers, directly from a Matrix row without opening the Offering edit Sheet"
  artifacts:
    - path: "src/lib/params/offeringsFilters.ts"
      provides: "parseOfferingsFilters — practiceAreaId-only param parser"
    - path: "src/components/offerings/offerings-filters.tsx"
      provides: "OfferingsFilters — single Practice Area EnumFilterSelect"
    - path: "src/components/offerings/offerings-matrix.tsx"
      provides: "OfferingsMatrix — grouped Table with inline Trigger/Buyer editors"
      min_lines: 150
  key_links:
    - from: "src/components/offerings/offerings-matrix.tsx"
      to: "src/components/offerings/ranked-buyer-roles-picker.tsx"
      via: "Primary Buyer(s) cell opens the SAME picker in a Popover for inline editing, persisting via updateOfferingBuyerRolesAction"
      pattern: "RankedBuyerRolesPicker"
    - from: "src/components/offerings/offerings-matrix.tsx"
      to: "src/components/offerings/trigger-editor.tsx"
      via: "Trigger(s) cell's '+ Add trigger' ghost button"
      pattern: "TriggerEditor"
---

<objective>
Build the Offering × Trigger × Buyer Matrix tab's filter bar and table (OFR-05) — a Practice-Area-filtered, Domain-grouped table where Triggers and Primary Buyers are editable inline, without leaving the tab.

Purpose: This is the second of the two tab surfaces (Service Portfolio being the first, 30-08) that the Wave 6 OfferingsTabs shell composes.
Output: `offeringsFilters.ts`, `offerings-filters.tsx`, `offerings-matrix.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-CONTEXT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: offeringsFilters.ts param parser + OfferingsFilters (single Practice Area Select)</name>
  <files>src/lib/params/offeringsFilters.ts, src/components/offerings/offerings-filters.tsx</files>
  <read_first>
    src/lib/params/signalFilters.ts (full, 40 lines — firstValue + the practiceAreaId NaN-guard pattern, THE exact template scoped down to one field)
    src/components/signals/signal-filters.tsx (full, 115 lines — EnumFilterSelect at lines 30-65 is the exact template; this phase needs only ONE instance of it, drop the search Input and the category/status EnumFilterSelect calls)
    .planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md line 172 (filter bar: single Select only, reuses EnumFilterSelect's nuqs useQueryState + shallow:false verbatim)
  </read_first>
  <action>
    Create `src/lib/params/offeringsFilters.ts` mirroring signalFilters.ts's `firstValue` helper (copy verbatim or import it — since it's a generic pure function, re-declaring it locally is acceptable and matches the existing house pattern of NOT sharing it as a cross-module utility) and export `interface OfferingsFiltersShape { practiceAreaId?: number }` plus `parseOfferingsFilters(params: { [key: string]: string | string[] | undefined }): OfferingsFiltersShape` — parses only the `practiceArea` query param to a NaN-guarded `number | undefined`, identical logic to signalFilters.ts's practiceAreaId branch.

    Create `OfferingsFilters` in `src/components/offerings/offerings-filters.tsx`, a `'use client'` component with props `{ practiceAreas: Array<{ id: number; name: string }> }`. Copy signal-filters.tsx's `EnumFilterSelect` helper function VERBATIM (nuqs `useQueryState` + `parseAsStringEnum` + `{ shallow: false }`). Render a `<div className="flex flex-wrap gap-3">` containing exactly ONE `EnumFilterSelect` instance: `paramKey="practiceArea"`, `placeholder="Practice area"`, `options={practiceAreas.map(pa => String(pa.id))}`, `humanize={false}`, `labelMap={Object.fromEntries(practiceAreas.map(pa => [String(pa.id), pa.name]))}` — do NOT include a search Input or category/status filters (the Matrix tab has only the one Practice Area dimension per UI-SPEC line 172).
  </action>
  <acceptance_criteria>
    - Source: `offeringsFilters.ts` exports `parseOfferingsFilters` and `interface OfferingsFiltersShape` (or equivalent type) containing only `practiceAreaId?: number`
    - Source: `offerings-filters.tsx` contains exactly one `EnumFilterSelect` JSX usage — `grep -c "<EnumFilterSelect" src/components/offerings/offerings-filters.tsx` returns 1
    - Source: `offerings-filters.tsx` does NOT import `parseAsString` or render an `Input` for search — this tab has no search field
    - Test command: `npx tsc --noEmit` passes with no errors attributable to either file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The Matrix tab's filter bar is a single URL-synced Practice Area dropdown, and the param parser correctly NaN-guards a malformed value.</done>
</task>

<task type="auto">
  <name>Task 2: OfferingsMatrix — grouped Table with inline Trigger/Buyer editors</name>
  <files>src/components/offerings/offerings-matrix.tsx</files>
  <read_first>
    src/components/signals/signal-table.tsx (full, 226 lines — empty-state pattern 103-138, table container/header 140-154, Popover-triggered cell pattern (LinkedOfferingsCell) 57-88, row status styling `opacity-70` at line 163)
    src/components/offerings/ranked-buyer-roles-picker.tsx (built in 30-05 — its exact prop contract)
    src/components/offerings/trigger-editor.tsx (built in 30-06 — its exact prop contract)
    src/app/actions/offerings.ts (as it exists after 30-03 — `updateOfferingBuyerRolesAction(offeringId, buyerRoles)` and `deleteTriggerAction(id)` signatures)
    .planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md lines 172-177 (columns: Offering → Trigger(s) → Primary Buyer(s) → Commercial Model; domain-group header row as a full-width colSpan TableCell reading the domain name; trigger chip cell with inline × remove + trailing "+ Add trigger"; Primary Buyers cell as ranked "1. CFO, 2. Head of GBS" text opening the SAME RankedBuyerRolesPicker in a Popover when clicked; Commercial Model is plain truncated text, no inline edit — editing stays in the Offering Sheet)
  </read_first>
  <action>
    Create `OfferingsMatrix` in `src/components/offerings/offerings-matrix.tsx`. Props: `{ domains: Array<{ id: number; name: string }>; offeringsByDomainId: Record<number, Array<OfferingRow>>; offeringsWithoutDomainForPracticeArea: Array<OfferingRow>; buyerRoles: Array<{ id: number; name: string }>; rankedBuyerRolesByOfferingId: Record<number, Array<{ buyerRoleId: number; name: string; rank: number }>>; triggersByOfferingId: Record<number, Array<{ id: number; triggerText: string }>>; hasActiveFilters: boolean; hasAnyOfferingsForPracticeArea: boolean }` where `OfferingRow = { id: number; name: string; commercialModelText: string | null; status: 'active'|'draft'|'retired' }`.

    Empty states (copy exact per Copywriting Contract): if `!hasAnyOfferingsForPracticeArea`, render the card "No offerings for this Practice Area yet" / "Switch to the Service Portfolio tab to add offerings, or choose a different Practice Area filter." If `hasAnyOfferingsForPracticeArea && domains.length === 0 && offeringsWithoutDomainForPracticeArea.length === 0` (defensive, filtered-to-zero edge case) render "No offerings match your filters" / "Try removing a filter or clearing your search."

    Otherwise render `<div className="rounded-lg border border-slate-200 bg-white"><Table>` with a `TableHeader` row of 4 `TableHead`s: "Offering", "Trigger(s)", "Primary Buyer(s)", "Commercial Model". `TableBody` content: for each domain in `domains`, a domain-group header `TableRow className="bg-muted"` with one `TableCell colSpan={4} className="font-semibold text-[14px]"` showing `domain.name`, followed by that domain's offering rows from `offeringsByDomainId[domain.id]`; after all domain groups, if `offeringsWithoutDomainForPracticeArea.length > 0` render one more group header cell reading "Other" (or omit the header and just append the rows directly — Claude's Discretion, but be consistent) followed by those offering rows.

    Each offering row (`TableRow className={offering.status === 'retired' ? 'opacity-70' : undefined}`): Offering name cell (plain text); Trigger(s) cell — `<div className="flex flex-wrap items-center gap-1">` mapping `triggersByOfferingId[offering.id] ?? []` to `<Badge variant="outline" className="gap-1">{trigger.triggerText}<button aria-label="Remove trigger" onClick={() => deleteTriggerAction(trigger.id)}>×</button></Badge>`, followed by a `TriggerEditor offeringId={offering.id} trigger={<Button variant="ghost" size="sm">+ Add trigger</Button>}`, and if the triggers array is empty render the inline caption "No triggers yet." next to the add button (not a card); Primary Buyer(s) cell — a `Popover` whose trigger is a `<span className="cursor-pointer text-sm">` showing `rankedBuyerRolesByOfferingId[offering.id]?.map((b,i) => \`${i+1}. ${b.name}\`).join(', ') || '—'`, and whose `PopoverContent` renders `RankedBuyerRolesPicker` with `selectedRanked` seeded from that same ranked array and `onChange` calling `updateOfferingBuyerRolesAction(offering.id, next)` immediately (wrap in `useTransition`, `router.refresh()` on success — this is the "persist immediately" path, distinct from the Offering form's local-state-then-submit); Commercial Model cell — plain truncated text `<span className="truncate">{offering.commercialModelText ?? '—'}</span>`, no edit affordance on this tab.

    Import `deleteTriggerAction`, `updateOfferingBuyerRolesAction` from `@/app/actions/offerings`.
  </action>
  <acceptance_criteria>
    - Source: `offerings-matrix.tsx` exports `function OfferingsMatrix` (or const)
    - Source: contains a `TableRow className="bg-muted"` with a `colSpan={4}` `TableCell` for the domain-group header
    - Source: imports and uses `RankedBuyerRolesPicker` inside a `Popover` for the Primary Buyer(s) cell, and `TriggerEditor` for the "+ Add trigger" affordance
    - Source: the Commercial Model cell contains NO click handler, `Popover`, `Sheet`, or edit affordance — `grep -A3 "commercialModelText" src/components/offerings/offerings-matrix.tsx` shows plain text rendering only
    - Source: contains both exact empty-state heading strings "No offerings for this Practice Area yet" and (a variant of) "No offerings match your filters"
    - Test command: `npx tsc --noEmit` passes with no errors attributable to this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>OfferingsMatrix renders the grouped table with fully inline Trigger add/remove and Primary Buyer re-ranking, while Commercial Model stays read-only on this tab per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Matrix inline Popover edits (Triggers, Buyers) → Server Actions | Immediate-persist writes triggered from table-row interactions, not a Sheet submit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-09-01 | Tampering | Immediate-persist `updateOfferingBuyerRolesAction` call from the Popover | mitigate | Routes through the SAME `syncOfferingBuyerRoles` diff-and-sync helper as the Offering form's submit path (30-03) — no separate, divergent write logic for the "quick edit" surface |
| T-30-09-02 | Tampering | Trigger remove button (`×`) calling `deleteTriggerAction` directly, no confirmation dialog | accept | Triggers are NOT one of the four DATA-10-guarded entity kinds and are low-stakes, frequently-edited content (a single sentence describing when to raise this offering) — matching the UI-SPEC's explicit choice of a bare inline remove rather than a confirmation dialog for this specific, easily-recreated entity |
| T-30-09-03 | Information Disclosure | Practice Area filter URL param | accept | `practiceAreaId` is a non-sensitive numeric id; the underlying data fetch is still staff-gated at the page level (30-10) regardless of what filter value is present in the URL |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- Manual: switching the Practice Area filter changes the URL and the rendered domain groups; clicking a Primary Buyer(s) cell opens the ranked picker and a rank change persists on refresh; adding a trigger via the Popover appears as a new chip without a full page reload
</verification>

<success_criteria>
- OfferingsMatrix delivers OFR-05's full spec: Practice-Area-filtered, Domain-grouped, with fully inline Trigger and ranked-Buyer editing
</success_criteria>

<output>
Create `.planning/phases/30-offerings-ui-v1-6-queued/30-09-SUMMARY.md` when done
</output>
