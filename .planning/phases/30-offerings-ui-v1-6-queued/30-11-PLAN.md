---
phase: 30-offerings-ui-v1-6-queued
plan: 11
type: execute
wave: 7
depends_on: ["30-10"]
files_modified: []
autonomous: false
requirements: [OFR-01, OFR-02, OFR-03, OFR-04, OFR-05, OFR-06, OFR-07, OFR-08]

must_haves:
  truths:
    - "A human has visually confirmed, against the live GBS seed data, that every OFR-01..08 behavior works exactly as designed in the deployed/dev environment"
  artifacts: []
  key_links: []
---

<objective>
Close the phase with a live-browser manual QA checkpoint, mirroring Phase 29's 29-08 precedent — the human confirms every automated build actually produces the intended, working UI end-to-end against real GBS seed data. No code changes in this plan.

Purpose: Catch anything automated `tsc`/`vitest` checks structurally cannot — visual correctness, interaction feel, and the full 8-requirement acceptance sweep — before this phase is marked complete.
Output: A recorded "approved" confirmation (or a documented list of issues if not approved, routed back to the orchestrator for a gap-closure plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-CONTEXT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Live UAT sweep of OFR-01..OFR-08 against GBS seed data</name>
  <files>none (visual/interaction verification only, no code changes)</files>
  <read_first>
    .planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md (full file — the exact visual/copy/spacing contract being verified)
    .planning/phases/30-offerings-ui-v1-6-queued/30-CONTEXT.md (D-01..D-11 — the locked decisions this UAT confirms were honored)
  </read_first>
  <action>
    Claude confirms the dev server is running against a staff-authenticated session (or starts `npm run dev` and signs in), navigates to `/offerings`, and walks through the 8-step verification checklist below, reporting the result of each step to the human for final sign-off. This is a verification task, not an implementation task — no files are modified.
  </action>
  <what-built>
    The full Offerings UI: a new "Offerings" sidebar item, a two-tab `/offerings` screen (Service Portfolio hierarchy manager + Offering × Trigger × Buyer Matrix), a shared Buyer Role lookup CRUD panel, and delete-guard/archive UI wired to the Phase 28 dependents check.
  </what-built>
  <how-to-verify>
    Run `npm run dev`, sign in as a staff user, and walk through:
    1. **Nav (OFR-01):** Confirm "Offerings" appears in the sidebar's Manage group between Signals and Settings, and highlights active only while on `/offerings*`.
    2. **Two tabs (OFR-02):** Confirm `/offerings` shows "Service Portfolio" and "Matrix" tabs.
    3. **Service Portfolio (OFR-03):** Expand GBS, expand a Domain, confirm Offerings list beneath it. Create a new Domain and a new Offering via the dashed "+ New..." rows. Edit an existing Offering (all 8 fields render and save). Reorder two Domains with the up/down arrows and confirm the order persists after a refresh. Archive an Offering (confirm dialog is NOT red) and confirm it shows muted/retired styling but is still visible.
    4. **Offering form fields (OFR-04):** In the Offering edit Sheet, confirm Name, Practice Area, Domain (with a working "No domain" option), Offer Type, Description, Commercial Model Text, ranked Buyer Roles (checkbox + up/down + "1. CFO" numbering), and Status all render and save correctly.
    5. **Matrix tab (OFR-05):** Confirm it defaults to the GBS Practice Area with no URL param. Confirm rows are grouped under Domain section headers. Add a Trigger via the "+ Add trigger" popover and remove one via its "×". Click a Primary Buyer(s) cell, change the ranking in the popover, and confirm it persists on refresh WITHOUT opening the Offering Sheet.
    6. **Buyer Role panel (OFR-06):** Click "Manage Buyer Roles" from BOTH tabs. Create a new role, edit an existing one, and attempt to delete "CFO" (should be blocked — it has dependents) then delete a fresh, unreferenced role (should succeed).
    7. **Reverse lookup (OFR-07):** Open the edit Sheet for a seeded Offering known to have linked signals (per the Phase 28 seed's 10 representative links) and confirm the "Linked Signals" section lists real Company/Persona Signal names, not raw ids.
    8. **Delete guard (OFR-08):** Attempt to delete a Practice Area/Domain/Offering/Buyer Role that has dependents — confirm the blocking dialog with NO confirm button appears. Attempt to delete something with no dependents — confirm the red destructive confirm appears and the delete succeeds.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass, or describe the specific issue(s) found (which numbered check, what was observed vs. expected)</resume-signal>
  <verify>
    <automated>npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>Human has typed "approved" confirming all 8 OFR-01..OFR-08 checks pass against live GBS seed data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None — this plan makes no code changes | N/A |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-11-01 | N/A | This is a verification-only plan | accept | No new attack surface is introduced; this plan's only output is human confirmation that the security-relevant behaviors built in Waves 1-6 (staff gate, delete-guard, archive-vs-delete color coding) actually render and behave as specified in a live environment |
</threat_model>

<verification>
- Human types "approved" after completing all 8 numbered checks above
</verification>

<success_criteria>
- All 8 OFR requirements are visually and interactively confirmed working against real GBS seed data in a live dev environment
- Any issues found are routed back to the orchestrator as gap-closure candidates rather than silently accepted
</success_criteria>

<output>
Create `.planning/phases/30-offerings-ui-v1-6-queued/30-11-SUMMARY.md` when done
</output>
