---
phase: 30-offerings-ui-v1-6-queued
plan: 03
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - src/app/actions/offerings.ts
autonomous: true
requirements: [OFR-03, OFR-04, OFR-05, OFR-08]

must_haves:
  truths:
    - "A staff member can create/edit/archive/delete/reorder an Offering, with its ranked Buyer Roles synced atomically-in-sequence alongside the offering itself"
    - "The Matrix tab's inline Popover editor can persist a ranked Buyer Role change to one offering immediately, without opening the full Offering edit Sheet"
    - "A staff member can add a Trigger to an offering and remove one, both staff-gated and validated"
    - "Deleting an Offering with dependent records (buyer-role links, triggers, or signal-offering links) is refused, never silently cascaded"
  artifacts:
    - path: "src/app/actions/offerings.ts"
      provides: "createOfferingAction/updateOfferingAction/archiveOfferingAction/deleteOfferingAction/reorderOfferingsAction/updateOfferingBuyerRolesAction/createTriggerAction/deleteTriggerAction appended to the file 30-02 created"
      exports: ["createOfferingAction", "updateOfferingAction", "archiveOfferingAction", "deleteOfferingAction", "reorderOfferingsAction", "updateOfferingBuyerRolesAction", "createTriggerAction", "deleteTriggerAction"]
  key_links:
    - from: "createOfferingAction/updateOfferingAction/updateOfferingBuyerRolesAction"
      to: "src/lib/db/queries/offerings.ts's listBuyerRolesForOffering/insertOfferingBuyerRole/deleteOfferingBuyerRole/updateOfferingBuyerRoleRank"
      via: "a shared internal syncOfferingBuyerRoles(offeringId, nextRanked, userId) helper, diff-and-sync pattern"
      pattern: "async function syncOfferingBuyerRoles"
---

<objective>
Complete the Server Actions layer for the Offering entity (the most complex entity in this phase — 8 form fields plus a ranked many-to-many Buyer Role relationship) and its child Trigger entity. Appends to the SAME `src/app/actions/offerings.ts` file 30-02 created, hence the sequential wave-3 dependency (same-file conflict rule).

Purpose: Give the Offering Sheet form (Wave 4) and the Matrix tab's inline buyer/trigger editors (Wave 5) a complete, tested write path, including the diff-and-sync pattern for the ranked join table that both the create flow and the Matrix's immediate-persist Popover reuse.
Output: `offerings.ts` grows to include the full Offering + Trigger action surface.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-CONTEXT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Offering CRUD + ranked Buyer Role diff-and-sync + immediate-persist action</name>
  <files>src/app/actions/offerings.ts, src/app/actions/offerings.test.ts</files>
  <read_first>
    src/app/actions/offerings.ts (the file created by 30-02 — read it in full to append correctly, matching its existing import block and OfferingsActionResult type; do not redeclare the type)
    src/app/actions/signals.ts lines 45-140 (the syncSignalOfferingLinks diff-and-sync helper — THE exact template for syncOfferingBuyerRoles, and createCompanySignalAction/updateCompanySignalAction — the exact template for how a create/update action calls its sync helper after the primary insert/update)
    src/lib/db/queries/offerings.ts (full — insertOffering, updateOffering, listAllOfferingsForPracticeArea, insertOfferingBuyerRole, listBuyerRolesForOffering, hasOfferingDependents/deleteOffering, plus the three helpers 30-01 added: updateOfferingSortOrder, deleteOfferingBuyerRole, updateOfferingBuyerRoleRank)
    src/lib/db/schema.ts lines 298-317, 349-363 (catalogStatusEnum = ['active','draft','retired'] — Offering uses this 3-value enum, so 'retired' IS valid here unlike Practice Area; offerTypeEnum's 7 values)
    .planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md lines 507-556 (syncOfferingBuyerRoles template — note the plan's own naming; adapt function names to match this plan's final list)
  </read_first>
  <behavior>
    - createOfferingAction(input) validates offeringInputSchema (no sortOrder field — server-computed), inserts the offering, then calls syncOfferingBuyerRoles(insertedId, buyerRoles, userId) which inserts every entry (no existing rows to diff against on create)
    - updateOfferingAction(id, input) updates the offering fields, then diffs the next ranked buyerRoles array against listBuyerRolesForOffering(id): inserts additions via insertOfferingBuyerRole, deletes removals via deleteOfferingBuyerRole, updates rank-only changes via updateOfferingBuyerRoleRank
    - archiveOfferingAction(id) sets status:'retired' (valid for Offering's 3-value catalogStatusEnum, unlike Practice Area)
    - deleteOfferingAction(id) passes deleteOffering's has_dependents result straight through
    - reorderOfferingsAction(orderedIds) sequentially calls updateOfferingSortOrder per id, positional index becomes the new sortOrder
    - updateOfferingBuyerRolesAction(offeringId, buyerRoles) is a standalone EXPORTED action (not the internal sync helper) that staff-gates, then calls the SAME syncOfferingBuyerRoles helper directly and revalidates — this is the action the Matrix tab's inline Popover editor calls to persist a buyer-role change immediately without opening the Offering Sheet
    - if insertOfferingBuyerRole/deleteOfferingBuyerRole ever throws mid-sync, the outer try/catch still returns action_failed (no rollback — documented accepted risk, matches signals.ts's syncSignalOfferingLinks precedent, no db.transaction available)
  </behavior>
  <action>
    Append to the existing offerings.ts (do not create a new file, do not redeclare OfferingsActionResult). Import `catalogStatusEnum, offerTypeEnum` from `@/lib/db/schema` (add to the existing schema import if one exists, or add a new import line). Import from `@/lib/db/queries/offerings`: `insertOffering, updateOffering, listAllOfferingsForPracticeArea, insertOfferingBuyerRole, listBuyerRolesForOffering, deleteOffering, updateOfferingSortOrder, deleteOfferingBuyerRole, updateOfferingBuyerRoleRank, insertTrigger, deleteTrigger`.

    Define `offeringInputSchema = z.object({ practiceAreaId: z.number().int().positive(), domainId: z.number().int().positive().nullable().optional(), name: z.string().trim().min(1).max(200), offerType: z.enum(offerTypeEnum.enumValues), description: z.string().trim().min(1), commercialModelText: z.string().trim().optional(), status: z.enum(catalogStatusEnum.enumValues).optional(), buyerRoles: z.array(z.object({ buyerRoleId: z.number().int().positive(), rank: z.number().int().positive() })).default([]) })`. Define `triggerInputSchema = z.object({ offeringId: z.number().int().positive(), triggerText: z.string().trim().min(1).max(500) })`.

    Write an internal (non-exported) `async function syncOfferingBuyerRoles(offeringId: number, nextRanked: Array<{buyerRoleId:number;rank:number}>, userId: string): Promise<OfferingsActionResult>` — diff `existing = await listBuyerRolesForOffering(offeringId)` against `nextRanked` by `buyerRoleId`: `toAdd` = ids in next not in existing (insertOfferingBuyerRole per entry), `toRemove` = existing ids not in next (deleteOfferingBuyerRole per entry), `toUpdateRank` = ids present in both where `rank` differs (updateOfferingBuyerRoleRank per entry). Sequential awaited loops, no Promise.all needed here (small lists, order doesn't matter for correctness but keep it simple and sequential matching the house no-transaction convention).

    Write `createOfferingAction(input: unknown)` — parse, destructure `{ buyerRoles, ...offeringFields }`, compute `sortOrder` server-side (count of existing offerings scoped to the SAME `domainId ?? null` within the practiceAreaId — a simple filter over `listAllOfferingsForPracticeArea(practiceAreaId)`), insert, then `syncOfferingBuyerRoles(inserted.id, buyerRoles, userId)`, revalidate on success. `updateOfferingAction(id, input)` mirrors this without touching sortOrder. `archiveOfferingAction(id)` sets `status:'retired'` directly. `deleteOfferingAction(id)` pass-through. `reorderOfferingsAction(orderedIds: number[])` sequential loop over `updateOfferingSortOrder`. `updateOfferingBuyerRolesAction(offeringId: number, buyerRoles: Array<{buyerRoleId:number;rank:number}>): Promise<OfferingsActionResult>` — staff-gate, call `syncOfferingBuyerRoles(offeringId, buyerRoles, userId)` directly, revalidate on success.

    Write `createTriggerAction(input: unknown): Promise<OfferingsActionResult>` — parse `triggerInputSchema`, compute sortOrder as `(await import of listTriggersForOffering — or just always append with a large safe sortOrder via count)`: call `listTriggersForOffering(offeringId)` (already exported from offerings.ts query module) to get the count, `insertTrigger({ offeringId, triggerText, sortOrder: count, createdBy: userId })`, revalidate. `deleteTriggerAction(id: number): Promise<OfferingsActionResult>` — staff-gate, `await deleteTrigger(id)` (unconditional, no guard per 30-01's design note), revalidate, return `{ ok: true }` — wrap in try/catch for action_failed since deleteTrigger can still throw on a genuine DB error.

    Extend `offerings.test.ts` (created in 30-02) with new describe blocks covering: createOfferingAction syncs buyerRoles via insertOfferingBuyerRole for a fresh offering; updateOfferingAction diffs correctly (add/remove/rank-change cases, mirroring signals.test.ts's syncSignalOfferingLinks test shape); archiveOfferingAction sets status:'retired'; deleteOfferingAction has_dependents pass-through; updateOfferingBuyerRolesAction calls the sync helper directly without touching updateOffering; createTriggerAction/deleteTriggerAction staff-gate-first and revalidate.
  </action>
  <acceptance_criteria>
    - Source: `offerings.ts` contains `async function syncOfferingBuyerRoles(`, `export async function createOfferingAction(`, `export async function updateOfferingAction(`, `export async function archiveOfferingAction(`, `export async function deleteOfferingAction(`, `export async function reorderOfferingsAction(`, `export async function updateOfferingBuyerRolesAction(`, `export async function createTriggerAction(`, `export async function deleteTriggerAction(`
    - Source: `archiveOfferingAction`'s body contains `status: 'retired'` (valid for Offering, unlike Practice Area's 'draft')
    - Test command: `npx vitest run src/app/actions/offerings.test.ts` — all tests pass, including the new Offering/Trigger describe blocks
    - Behavior: a mocked `listBuyerRolesForOffering` returning `[{buyerRoleId:1,rank:1},{buyerRoleId:2,rank:2}]` against a next-ranked input of `[{buyerRoleId:1,rank:2},{buyerRoleId:3,rank:1}]` results in exactly one `insertOfferingBuyerRole` call (buyerRoleId:3), one `deleteOfferingBuyerRole` call (buyerRoleId:2), and one `updateOfferingBuyerRoleRank` call (buyerRoleId:1, rank:2)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/actions/offerings.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>offerings.ts's Offering + Trigger action surface is complete and unit-tested, including the diff-and-sync helper reused by both the create/update flow and the Matrix's immediate-persist action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client (Offering Sheet submit, Matrix Popover submit) → Server Action | Untrusted input for the offering's full field set AND the ranked buyer-role array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03-01 | Elevation of Privilege | `updateOfferingBuyerRolesAction` — a second, independent write path to the same join table beyond the main Offering Sheet | mitigate | Staff-gated identically to every other action in this file; both paths route through the SAME `syncOfferingBuyerRoles` helper, so there is exactly one place the diff logic can regress, not two divergent implementations |
| T-30-03-02 | Tampering | `buyerRoleId`/`rank` values inside the offeringInputSchema array | mitigate | zod validates each array entry is a positive integer pair before any DB write; a malformed rank or a non-existent buyerRoleId still fails at the DB's FK constraint on `offering_buyer_role.buyer_role_id`, providing a second backstop |
| T-30-03-03 | Tampering | `archiveOfferingAction` status value | mitigate | Hardcoded literal `'retired'` server-side, never accepted from client input — the client cannot request an arbitrary status transition through the archive path |
| T-30-03-04 | Repudiation | `deleteOfferingAction` guard | mitigate | Pass-through of `deleteOffering`'s pre-checked `has_dependents` result, never re-derived — same invariant as 30-02's Practice Area/Domain delete actions |
</threat_model>

<verification>
- `npx vitest run src/app/actions/offerings.test.ts` — all pass (both 30-02's and this plan's test cases)
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
- Full Offering + Trigger action surface exists, tested, with the diff-and-sync buyer-role helper correctly shared between the create/update flow and the standalone immediate-persist action
</success_criteria>

<output>
Create `.planning/phases/30-offerings-ui-v1-6-queued/30-03-SUMMARY.md` when done
</output>
