---
phase: 30-offerings-ui-v1-6-queued
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/queries/practiceAreas.ts
  - src/lib/db/queries/domains.ts
  - src/lib/db/queries/offerings.ts
  - src/lib/nav.ts
  - src/components/layout/app-sidebar.tsx
  - src/lib/sidebar-collapse.ts
autonomous: true
requirements: [OFR-01, OFR-03]

must_haves:
  truths:
    - "A signed-in staff member sees an 'Offerings' item in the sidebar's Manage group, between Signals and Settings"
    - "Clicking the Offerings sidebar item highlights it as active (even though /offerings 404s until Wave 6 ships the page)"
    - "The query layer exposes reorder helpers for Practice Area, Domain, and Offering sort_order, and a delete helper for Trigger, ready for the Server Actions layer to call"
  artifacts:
    - path: "src/lib/db/queries/practiceAreas.ts"
      provides: "updatePracticeAreaSortOrder(id, sortOrder, updatedBy) thin wrapper"
    - path: "src/lib/db/queries/domains.ts"
      provides: "updateDomainSortOrder(id, sortOrder, updatedBy) thin wrapper"
    - path: "src/lib/db/queries/offerings.ts"
      provides: "updateOfferingSortOrder, deleteOfferingBuyerRole, updateOfferingBuyerRoleRank, deleteTrigger"
    - path: "src/lib/nav.ts"
      provides: "'offerings' NavKey + getActiveNavKey /offerings branch"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Offerings SidebarMenuItem between Signals and Settings"
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/lib/nav.ts"
      via: "getActiveNavKey(pathname) === 'offerings'"
      pattern: "activeKey === 'offerings'"
    - from: "src/lib/sidebar-collapse.ts"
      to: "src/lib/nav.ts"
      via: "getNavTooltipLabel accepts the widened NavKey union"
      pattern: "offerings: 'Offerings'"
---

<objective>
Lay the foundation this entire phase builds on: (1) small reorder/delete query-layer additions to the existing Phase 28 modules that every later Server Action plan calls, and (2) the sidebar nav wiring that makes `/offerings` a real, highlightable destination (OFR-01). No new files for the query layer — pure in-place additions per 30-CONTEXT.md's Integration Points. This plan has zero UI dependencies and unblocks Wave 2's Server Actions plan.

Purpose: Establish the exact function signatures every downstream Server Action calls (Interface-First Ordering) and ship the nav entry point users click to reach this feature.
Output: Reorder/delete query helpers ready to import; a clickable, correctly-highlighting "Offerings" sidebar item (route itself still 404s until 30-10 ships the page — that is expected and acceptable at this wave).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-CONTEXT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reorder/delete query-layer helpers to the existing Phase 28 modules</name>
  <files>src/lib/db/queries/practiceAreas.ts, src/lib/db/queries/domains.ts, src/lib/db/queries/offerings.ts</files>
  <read_first>
    src/lib/db/queries/practiceAreas.ts (full — updatePracticeArea shape at lines 35-48, the exact wrapper target)
    src/lib/db/queries/domains.ts (full — updateDomain shape at lines 31-44)
    src/lib/db/queries/offerings.ts (full — updateOffering at 40-53, insertOfferingBuyerRole at 76-91, offeringBuyerRole/trigger schema imports at line 3)
    src/lib/db/schema.ts lines 380-409 (offeringBuyerRole unique index on offeringId+buyerRoleId; trigger table shape — no status column on either)
    .planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md lines 595-637 ("Query-Layer Additions" section — exact target shapes, but see corrections below)
  </read_first>
  <action>
    In practiceAreas.ts, add `export async function updatePracticeAreaSortOrder(id: number, sortOrder: number, updatedBy: string)` — a one-line thin wrapper calling `updatePracticeArea(id, { sortOrder }, updatedBy)`. Same shape in domains.ts for `updateDomainSortOrder(id, sortOrder, updatedBy)` wrapping `updateDomain`.

    In offerings.ts add four functions: (1) `updateOfferingSortOrder(id, sortOrder, updatedBy)` thin-wrapping `updateOffering`; (2) `deleteOfferingBuyerRole(offeringId: number, buyerRoleId: number)` — unconditional `db.delete(offeringBuyerRole).where(and(eq(offeringBuyerRole.offeringId, offeringId), eq(offeringBuyerRole.buyerRoleId, buyerRoleId)))`, no dependents guard (join-table rows are never guarded themselves, mirroring deleteSignalOfferingLink's unconditional shape); (3) `updateOfferingBuyerRoleRank(offeringId: number, buyerRoleId: number, rank: number, updatedBy: string)` — `db.update(offeringBuyerRole).set({ rank, updatedAt: new Date(), updatedBy }).where(and(eq(offeringBuyerRole.offeringId, offeringId), eq(offeringBuyerRole.buyerRoleId, buyerRoleId))).returning()`, return the first row; (4) `deleteTrigger(id: number)` — unconditional `db.delete(trigger).where(eq(trigger.id, id))`, no guard (trigger is never one of the 4 DATA-10-guarded entities).

    Do NOT add `updateTrigger`, `updateTriggerSortOrder`, or any Domain archive/status helper — 30-UI-SPEC.md/30-CONTEXT.md's "editable Trigger(s) (add/remove)" language (OFR-05) never describes an edit-in-place or reorder for individual triggers, only add/remove; building unused query surface is scope creep. Similarly, `domain` has NO `status` column (schema.ts:336-345 confirmed — only `id, practiceAreaId, name, sortOrder, createdBy, updatedBy, createdAt, updatedAt`) so do not add any Domain archive/status wrapper here — Domain's only removal path is the guarded `deleteDomain` (already exists from Phase 28). Flag this for the Server Actions plan: Domain gets NO archive action, only create/update/delete/reorder.

    All four additions follow the house no-try/catch fail-loud convention already used in every function in these three files (comment at offerings.ts:8-9) — do not wrap in try/catch.
  </action>
  <acceptance_criteria>
    - Source: `practiceAreas.ts` contains `export async function updatePracticeAreaSortOrder(`
    - Source: `domains.ts` contains `export async function updateDomainSortOrder(`
    - Source: `offerings.ts` contains `export async function updateOfferingSortOrder(`, `export async function deleteOfferingBuyerRole(`, `export async function updateOfferingBuyerRoleRank(`, `export async function deleteTrigger(`
    - Source: none of the three files contain `updateTrigger(` (singular, non-sortOrder) or `updateDomainStatus(` or a Domain `archive` helper — grep `grep -c "export async function updateTrigger\b" src/lib/db/queries/offerings.ts` returns 0
    - Test command: `npx tsc --noEmit` passes with no new errors attributable to these files
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>All four new query-layer functions exist, correctly typed against the schema, and are importable by the Wave 2 Server Actions plan.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Offerings sidebar nav item (OFR-01, D-01)</name>
  <files>src/lib/nav.ts, src/components/layout/app-sidebar.tsx, src/lib/sidebar-collapse.ts</files>
  <read_first>
    src/lib/nav.ts (full, 21 lines — the exact `/signals` branch at line 16 is the template)
    src/components/layout/app-sidebar.tsx (full — the `signals` SidebarMenuItem block at lines 200-212 is the exact template; icon import line 6; Manage SidebarGroup starts line 169)
    src/lib/sidebar-collapse.ts (full, 17 lines — `getNavTooltipLabel`'s object-literal return at line 16 is a Record keyed by NavKey and currently has NO 'offerings' key; TypeScript will error once NavKey widens unless this is fixed in the SAME commit)
    .planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md lines 145-151 (Layers icon decision, sidebar placement decision: Reviews → Signals → Offerings → Settings)
  </read_first>
  <action>
    In nav.ts: widen `NavKey` to `'start' | 'companies' | 'personas' | 'reviews' | 'signals' | 'offerings' | 'settings'`. Add a new branch immediately after the `/signals` branch (before the `/settings` exact-match branch): `if (pathname === '/offerings' || pathname.startsWith('/offerings/')) return 'offerings';` — prefix-match style like `/signals`, so a future `/offerings/[id]` detail route (if ever added) still highlights correctly, while `/offerings-archive` (sibling prefix) is still rejected by the exact `===`/`startsWith('/offerings/')` pair.

    In app-sidebar.tsx: add `Layers` to the existing `lucide-react` import on line 6 (alongside `Building2, Inbox, LayoutDashboard, Mail, PanelLeftClose, PanelLeftOpen, Radar, Settings, Users`). Insert a new `SidebarMenuItem` block, copied structurally from the `signals` block (lines 200-212), placed AFTER the `signals` SidebarMenuItem and BEFORE the `settings` SidebarMenuItem inside the same `Manage` `SidebarGroup`'s `SidebarMenu`. Set `isActive={activeKey === 'offerings'}`, `tooltip={getNavTooltipLabel('offerings', pendingCount)}`, `href="/offerings"`, icon `<Layers />`, label text "Offerings". Keep the exact same `className="h-[30px] p-0 px-2 gap-2.5 rounded-[4px] text-[15px] font-normal"` on the `SidebarMenuButton`.

    In sidebar-collapse.ts: this is the required companion fix PATTERNS.md flagged as unverified — `getNavTooltipLabel`'s fallback object literal (line 16) is a `Record`-style lookup keyed by every non-'reviews' NavKey value; it currently omits `offerings` entirely, which will fail `tsc` once NavKey widens (the literal must satisfy `Record<Exclude<NavKey,'reviews'>, string>` or equivalent — check the function's actual return type annotation before editing). Add `offerings: 'Offerings'` to that object literal, in the same position as `signals: 'Signals'`.
  </action>
  <acceptance_criteria>
    - Source: `nav.ts`'s `NavKey` type string literal union contains `'offerings'`
    - Source: `nav.ts` contains `if (pathname === '/offerings' || pathname.startsWith('/offerings/')) return 'offerings';`
    - Source: `app-sidebar.tsx` contains `isActive={activeKey === 'offerings'}` and `<Link href="/offerings">` and `<Layers />`
    - Source: `sidebar-collapse.ts`'s tooltip-label object literal contains `offerings: 'Offerings'`
    - Test command: `npx tsc --noEmit` passes — this is the concrete proof the sidebar-collapse.ts fix was necessary and applied (a missing key here fails the build the moment NavKey widens)
    - Behavior: navigating to any URL and manually setting `pathname` to `/offerings-archive` in a `getActiveNavKey` unit test returns `null`, not `'offerings'` (boundary guard, mirrors the existing `/signals`-vs-`/signals-archive` test in `nav.test.ts` if one exists — check via Glob before assuming)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/nav.test.ts</automated>
  </verify>
  <done>The Offerings sidebar item renders between Signals and Settings, highlights correctly on any /offerings* path, and the whole app still type-checks cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Sidebar nav → route | Purely client-side route highlighting; no data crosses this boundary in this plan |
| Query-layer additions → Postgres | Server-only, parameterized Drizzle calls (no raw SQL string interpolation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01-01 | Tampering | New query-layer functions (updatePracticeAreaSortOrder etc.) | accept | These are unreachable without a Server Action calling them (Wave 2+), and every Server Action in this phase gates on `requireStaffAccess()` first — no direct client path to this file exists. The functions themselves accept only typed numeric/string params, no raw user HTML/SQL. |
| T-30-01-02 | Tampering | `deleteOfferingBuyerRole`/`deleteTrigger` are unconditional (no dependents guard) | accept | Documented intentional per DATA-10's scope: join-table rows (offering_buyer_role) and leaf rows (trigger) are never referenced by anything else, so guarding them would be a no-op. The FOUR actually-guarded entities (Practice Area, Domain, Offering, Buyer Role) keep their existing Phase 28 guards untouched by this plan. |
| T-30-01-03 | Spoofing | Sidebar active-state highlight | accept | Purely cosmetic client-side routing state; no authorization decision is made by `getActiveNavKey` — `requireStaffAccess()` on the eventual `/offerings` page (30-10) is the actual gate. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- `getActiveNavKey('/offerings')` returns `'offerings'`; `getActiveNavKey('/offerings-archive')` returns `null`
- Sidebar visually shows Offerings between Signals and Settings (spot-check via `npm run dev`, not required to pass automated but recommended before moving to Wave 2)
</verification>

<success_criteria>
- All four new query-layer functions exist with correct signatures and no try/catch (house convention)
- Domain gets no archive/status helper (confirmed absent from schema)
- NavKey widens cleanly, getActiveNavKey handles /offerings with the boundary guard, sidebar-collapse.ts's tooltip map is updated in the same commit, and the whole project still compiles
</success_criteria>

<output>
Create `.planning/phases/30-offerings-ui-v1-6-queued/30-01-SUMMARY.md` when done
</output>
