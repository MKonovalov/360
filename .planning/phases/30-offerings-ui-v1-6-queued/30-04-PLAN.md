---
phase: 30-offerings-ui-v1-6-queued
plan: 04
type: execute
wave: 3
depends_on: ["30-02"]
files_modified:
  - src/components/offerings/practice-area-form.tsx
  - src/components/offerings/domain-form.tsx
autonomous: true
requirements: [OFR-03]

must_haves:
  truths:
    - "A staff member can open a Sheet to create a new Practice Area with Name, Short Code, Description, and Status fields, or edit an existing one"
    - "A staff member can open a Sheet to create a new Domain (Name only, scoped to a parent Practice Area) or edit an existing one — no Status field, since Domain has no status column"
  artifacts:
    - path: "src/components/offerings/practice-area-form.tsx"
      provides: "PracticeAreaForm Sheet CRUD component"
      min_lines: 120
    - path: "src/components/offerings/domain-form.tsx"
      provides: "DomainForm Sheet CRUD component"
      min_lines: 80
  key_links:
    - from: "src/components/offerings/practice-area-form.tsx"
      to: "src/app/actions/offerings.ts"
      via: "createPracticeAreaAction / updatePracticeAreaAction"
      pattern: "createPracticeAreaAction|updatePracticeAreaAction"
    - from: "src/components/offerings/domain-form.tsx"
      to: "src/app/actions/offerings.ts"
      via: "createDomainAction / updateDomainAction"
      pattern: "createDomainAction|updateDomainAction"
---

<objective>
Build the two smallest Sheet-based CRUD forms in this phase's Service Portfolio hierarchy: Practice Area (4 fields) and Domain (1 field + implicit parent scope). Both follow signal-form.tsx's controlled-open, reset-on-open, canSave-gated pattern verbatim, scaled down.

Purpose: Give the Wave 5 ServicePortfolio hierarchy component (30-08) two ready-to-drop-in "Edit"/"New" trigger components at the top two hierarchy levels.
Output: `practice-area-form.tsx`, `domain-form.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-CONTEXT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PracticeAreaForm (Sheet CRUD, 4 fields)</name>
  <files>src/components/offerings/practice-area-form.tsx</files>
  <read_first>
    src/components/signals/signal-form.tsx (full, 282 lines — THE controlled-open/reset-on-open/canSave/submit/Sheet-JSX-skeleton template; lines 1-97 state pattern, 104-146 submit pattern, 148-281 Sheet JSX)
    src/app/actions/offerings.ts (as it exists after 30-02 — exact signatures of createPracticeAreaAction/updatePracticeAreaAction and the practiceAreaInputSchema field shape: name, shortCode, description?, status?)
    src/lib/db/schema.ts lines 303-305, 321-332 (practiceAreaStatusEnum = ['active','draft'] — only 2 status options, no 'retired')
    .planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md lines 91, 131, 137-140 (default CTA button variant, Sheet default width sm:max-w-sm for this small form, Select/Input/Textarea field inventory)
  </read_first>
  <action>
    Create `PracticeAreaForm` in `src/components/offerings/practice-area-form.tsx`, a `'use client'` component copying signal-form.tsx's exact controlled-open (`open`, `pending`/`startTransition`, `error` state), `resetFields`/`handleOpenChange`, and Sheet JSX skeleton (SheetTrigger asChild wrapping the passed `trigger` prop, SheetHeader/SheetTitle/SheetDescription, `space-y-4 overflow-y-auto p-4` field container, SheetFooter Cancel/Save button pair).

    Props: `{ mode: 'create'|'edit'; practiceArea?: { id: number; name: string; shortCode: string; description: string | null; status: 'active'|'draft' }; trigger: React.ReactNode }`. Field state: `name` (Input), `shortCode` (Input), `description` (Textarea, optional), `status` (Select, options from `practiceAreaStatusEnum.enumValues` imported from `@/lib/db/schema` — exactly 2 SelectItems: active/draft — default to signal-form.tsx's STATUS_OPTIONS pattern but scoped to this 2-value enum).

    `canSave = name.trim().length > 0 && shortCode.trim().length > 0`. `handleSubmit` calls `createPracticeAreaAction({ name: name.trim(), shortCode: shortCode.trim(), description: description.trim() || undefined, status })` in create mode, or `updatePracticeAreaAction(practiceArea!.id, { ... })` in edit mode, imported from `@/app/actions/offerings`. On `!result.ok`, `setError('Could not save this Practice Area. Please try again.')`. On success, `setOpen(false); router.refresh();` matching signal-form.tsx exactly.

    Title: `mode === 'create' ? 'New Practice Area' : 'Edit Practice Area'` (per Copywriting Contract). No `className` override on `SheetContent` — this form stays at the default `sm:max-w-sm` width (only the Offering form in 30-05 widens to `sm:max-w-lg`).
  </action>
  <acceptance_criteria>
    - Source: `practice-area-form.tsx` exports `function PracticeAreaForm` (or `const PracticeAreaForm =`)
    - Source: contains `createPracticeAreaAction` and `updatePracticeAreaAction` imports from `@/app/actions/offerings`
    - Source: contains `practiceAreaStatusEnum.enumValues` (imported from `@/lib/db/schema`) driving the Status Select options — never a hardcoded `['active','draft']` array
    - Source: contains exactly 4 field groups (Name, Short Code, Description, Status) — `grep -c "space-y-1.5" src/components/offerings/practice-area-form.tsx` returns at least 4
    - Test command: `npx tsc --noEmit` passes with no errors attributable to this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PracticeAreaForm renders a working create/edit Sheet with all 4 fields, correctly gated on the 2-value status enum.</done>
</task>

<task type="auto">
  <name>Task 2: DomainForm (Sheet CRUD, 1 field, no status)</name>
  <files>src/components/offerings/domain-form.tsx</files>
  <read_first>
    src/components/signals/signal-form.tsx (same template as Task 1, scaled down further — no Status field this time)
    src/app/actions/offerings.ts (as it exists after 30-02 — createDomainAction/updateDomainAction signatures; domainInputSchema has ONLY practiceAreaId + name, no status, no shortCode)
  </read_first>
  <action>
    Create `DomainForm` in `src/components/offerings/domain-form.tsx`, the same controlled-open/reset-on-open/canSave/submit/Sheet-JSX pattern as PracticeAreaForm (Task 1), reduced to ONE field.

    Props: `{ mode: 'create'|'edit'; practiceAreaId: number; domain?: { id: number; name: string }; trigger: React.ReactNode }`. Field state: `name` (Input) only — `canSave = name.trim().length > 0`. No Status field (Domain has no status column — do not render one, do not import `catalogStatusEnum` or any status enum here).

    `handleSubmit` calls `createDomainAction({ practiceAreaId, name: name.trim() })` in create mode (practiceAreaId comes from the prop, not a form field — the parent hierarchy row already knows which Practice Area this Domain belongs to) or `updateDomainAction(domain!.id, { practiceAreaId, name: name.trim() })` in edit mode. Same error/success handling as PracticeAreaForm.

    Title: `mode === 'create' ? 'New Domain' : 'Edit Domain'`.
  </action>
  <acceptance_criteria>
    - Source: `domain-form.tsx` exports `function DomainForm` (or `const DomainForm =`)
    - Source: contains `createDomainAction` and `updateDomainAction` imports from `@/app/actions/offerings`
    - Source: does NOT import `catalogStatusEnum` or `practiceAreaStatusEnum` and does NOT render a Status `Select` — `grep -c "Status" src/components/offerings/domain-form.tsx` returns 0 (case-sensitive, matching neither a label nor a status field)
    - Test command: `npx tsc --noEmit` passes with no errors attributable to this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>DomainForm renders a minimal create/edit Sheet with only a Name field, correctly omitting any status control.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser form state → Server Action call | Client-trimmed strings are re-validated server-side by offerings.ts's zod schemas regardless of what this UI sends |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04-01 | Tampering | Client-side `canSave` gating | accept | This is a UX convenience only — the authoritative validation is offerings.ts's zod `safeParse` (30-02), which runs regardless of what this form does or does not disable client-side |
| T-30-04-02 | Information Disclosure | Error state rendering | accept | Errors render the generic "Could not save this X" string, never the Server Action's raw `reason` value, matching signal-form.tsx's precedent |
</threat_model>

<verification>
- `npx tsc --noEmit` passes for both new files
- Manual: opening PracticeAreaForm shows 4 fields incl. a 2-option Status select; opening DomainForm shows exactly 1 field and no Status control
</verification>

<success_criteria>
- Both forms compile cleanly, call the correct Wave 2 Server Actions, and correctly reflect their entities' actual schema shape (Domain has no status)
</success_criteria>

<output>
Create `.planning/phases/30-offerings-ui-v1-6-queued/30-04-SUMMARY.md` when done
</output>
