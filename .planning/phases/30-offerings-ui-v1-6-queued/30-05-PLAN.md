---
phase: 30-offerings-ui-v1-6-queued
plan: 05
type: execute
wave: 4
depends_on: ["30-03"]
files_modified:
  - src/components/offerings/ranked-buyer-roles-picker.tsx
  - src/components/offerings/offering-form.tsx
autonomous: true
requirements: [OFR-04, OFR-07]

must_haves:
  truths:
    - "A staff member can select and rank Buyer Roles for an Offering via checkboxes plus up/down reorder — the ranked order is what gets saved"
    - "A staff member can open a Sheet to create or edit an Offering with all 8 fields: Name, Practice Area, Domain (optional), Offer Type, Description, Commercial Model Text, ranked Buyer Roles, Status"
    - "The Offering edit Sheet shows a read-only list of Company/Persona Signals currently linked to it, at the bottom of the form"
  artifacts:
    - path: "src/components/offerings/ranked-buyer-roles-picker.tsx"
      provides: "RankedBuyerRolesPicker — checkbox-in-ScrollArea + numbered rank list with up/down/remove"
      min_lines: 60
    - path: "src/components/offerings/offering-form.tsx"
      provides: "OfferingForm — the widest Sheet in this phase (sm:max-w-lg override)"
      min_lines: 200
  key_links:
    - from: "src/components/offerings/offering-form.tsx"
      to: "src/components/offerings/ranked-buyer-roles-picker.tsx"
      via: "RankedBuyerRolesPicker slotted in place of a plain Select for the Buyer Roles field"
      pattern: "RankedBuyerRolesPicker"
    - from: "src/components/offerings/offering-form.tsx"
      to: "src/app/actions/offerings.ts"
      via: "createOfferingAction / updateOfferingAction, submitting the ranked buyerRoles array as one field of the payload"
      pattern: "createOfferingAction|updateOfferingAction"
---

<objective>
Build the two components at the heart of OFR-04: a reusable ranked multi-select picker extending Phase 29's checkbox-in-ScrollArea pattern with up/down reordering, and the Offering Sheet form itself — the largest, most field-dense form in this phase, plus its read-only OFR-07 reverse-lookup section.

Purpose: RankedBuyerRolesPicker is reused by BOTH the Offering form (this plan, local-state-then-submit) and the Matrix tab's inline Popover editor (30-09, immediate-persist) — it must stay a pure controlled component with zero Server Action calls inside it.
Output: `ranked-buyer-roles-picker.tsx`, `offering-form.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-CONTEXT.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md
@.planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RankedBuyerRolesPicker (pure controlled component)</name>
  <files>src/components/offerings/ranked-buyer-roles-picker.tsx</files>
  <read_first>
    src/components/signals/linked-offerings-picker.tsx (full, 55 lines — the exact base checkbox-in-ScrollArea structure to extend)
    .planning/phases/30-offerings-ui-v1-6-queued/30-PATTERNS.md lines 241-286 (the extended code skeleton with the rank-list extension comment)
    .planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md lines 109, 119, 148, 168 (rank prefix format "1. CFO", ArrowUp/ArrowDown icon choice distinct from Chevrons, empty-selection caption copy, no search input since only 5 roles exist)
  </read_first>
  <action>
    Per D-04, create `RankedBuyerRolesPicker` in `src/components/offerings/ranked-buyer-roles-picker.tsx`, a `'use client'` PURE CONTROLLED component — it accepts `{ buyerRoles: Array<{ id: number; name: string }>; selectedRanked: Array<{ buyerRoleId: number; rank: number }>; onChange: (next: Array<{ buyerRoleId: number; rank: number }>) => void }` and calls `onChange` on every mutation. It must NEVER import from `@/app/actions/offerings` or `@/app/actions/buyerRoles` — no Server Action calls live inside this component; both consumers (offering-form.tsx in this plan, and 30-09's Matrix Popover cell) own the decision of when/whether to persist.

    Structure per linked-offerings-picker.tsx: if `buyerRoles.length === 0`, render `<p className="text-sm text-slate-500">No buyer roles yet — use Manage Buyer Roles to create one.</p>`. Otherwise render a `ScrollArea className="h-40"` containing (1) the checkbox list — each role a `label` with `Checkbox` (`checked = selectedRanked.some(r => r.buyerRoleId === role.id)`), `onCheckedChange` appends `{ buyerRoleId: role.id, rank: selectedRanked.length + 1 }` to `selectedRanked` when checked, or filters it out when unchecked; (2) below the checkbox list, the ranked numbered list: sort `selectedRanked` by `rank` ascending, render each as `"{rank}. {role name resolved from buyerRoles by id}"` with three trailing icon buttons — `ArrowUp` (`variant="ghost" size="icon" aria-label="Move up"`, disabled at rank 1, swaps this entry's rank with the entry immediately above), `ArrowDown` (same, disabled at the last rank, swaps with the entry below), and a remove `"×"` button that both removes the entry from `selectedRanked` AND visually unchecks the source checkbox (since it reads from the same array, removing from `selectedRanked` handles both automatically).

    Above the whole picker (not inside the ScrollArea), if `selectedRanked.length === 0`, render the caption `"No buyer roles assigned yet — select from the list below."` (per Copywriting Contract) — this is separate from the `buyerRoles.length === 0` guard above (zero AVAILABLE vs zero SELECTED are different states).

    Import `ArrowUp`, `ArrowDown` from `lucide-react` (per UI-SPEC's deliberate choice to keep reorder icons visually distinct from `ChevronRight`/`ChevronDown` used for hierarchy disclosure elsewhere in this phase).
  </action>
  <acceptance_criteria>
    - Source: `ranked-buyer-roles-picker.tsx` exports `function RankedBuyerRolesPicker` (or const)
    - Source: does NOT contain the string `@/app/actions/` anywhere — `grep -c "@/app/actions/" src/components/offerings/ranked-buyer-roles-picker.tsx` returns 0
    - Source: imports `ArrowUp` and `ArrowDown` from `'lucide-react'`, never `ChevronUp`/`ChevronDown`
    - Behavior: given `selectedRanked=[{buyerRoleId:1,rank:1},{buyerRoleId:2,rank:2}]`, clicking rank-2's "Move up" button calls `onChange` with the two entries' ranks swapped (buyerRoleId:2 now rank:1, buyerRoleId:1 now rank:2)
    - Test command: `npx tsc --noEmit` passes with no errors attributable to this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>RankedBuyerRolesPicker is a pure, reusable, Server-Action-free controlled component ready to be dropped into both the Offering form and the Matrix's inline editor.</done>
</task>

<task type="auto">
  <name>Task 2: OfferingForm — 8 fields + ranked picker + read-only Linked Signals section (OFR-04, OFR-07)</name>
  <files>src/components/offerings/offering-form.tsx</files>
  <read_first>
    src/components/signals/signal-form.tsx (full, 282 lines — same controlled-open/canSave/submit/Sheet-JSX template as 30-04's forms, but this is the widest form in the phase)
    src/components/offerings/ranked-buyer-roles-picker.tsx (the component built in Task 1 of this plan — its exact prop names)
    src/app/actions/offerings.ts (as it exists after 30-03 — createOfferingAction/updateOfferingAction signatures; offeringInputSchema's exact field list: practiceAreaId, domainId?, name, offerType, description, commercialModelText?, status?, buyerRoles[])
    src/lib/db/schema.ts lines 298-317, 349-363 (offerTypeEnum's 7 values, catalogStatusEnum's 3 values — Offering uses the 3-value enum unlike Practice Area)
    .planning/phases/30-offerings-ui-v1-6-queued/30-UI-SPEC.md lines 131, 165, 167, 120-121 (Sheet sm:max-w-lg override — the ONLY form in this phase that widens; field order: Name → Practice Area → Domain (optional) → Offer Type → Description → Commercial Model Text → ranked Buyer Roles → Status → read-only Linked Signals section, separated by a Separator; "No domain (goes straight to Practice Area)" null option; Commercial Model Text placeholder copy)
    src/components/ui/separator.tsx (confirms the `Separator` export exists and is already vendored — use it, do not fall back to a plain `hr`)
  </read_first>
  <action>
    Create `OfferingForm` in `src/components/offerings/offering-form.tsx`, following signal-form.tsx's exact controlled-open/reset-on-open/canSave/submit/Sheet-JSX skeleton, scaled to 8 fields plus the read-only section.

    Props: `{ mode: 'create'|'edit'; offering?: { id: number; practiceAreaId: number; domainId: number | null; name: string; offerType: string; description: string; commercialModelText: string | null; status: 'active'|'draft'|'retired' }; existingRankedBuyerRoles?: Array<{ buyerRoleId: number; name: string; rank: number }>; linkedSignals?: Array<{ signalType: 'company'|'persona'; signalId: number; name: string }>; practiceAreas: Array<{ id: number; name: string }>; domainsByPracticeAreaId: Record<number, Array<{ id: number; name: string }>>; buyerRoles: Array<{ id: number; name: string }>; trigger: React.ReactNode }`.

    Field state, in the exact UI-SPEC-mandated order: `name` (Input) → `practiceAreaId` (Select, options from `practiceAreas` prop; changing it must reset `domainId` to `null` since the Domain list is filtered by the chosen Practice Area) → `domainId: number | null` (Select sourced from `domainsByPracticeAreaId[practiceAreaId] ?? []`, with a top option `"No domain (goes straight to Practice Area)"` mapping to `null` — value serialization: use the string `'none'` for the null option since shadcn `Select` cannot hold an empty-string SelectItem value, then map back to `null` on change) → `offerType` (Select, options from `offerTypeEnum.enumValues` imported from `@/lib/db/schema`) → `description` (Textarea) → `commercialModelText` (Textarea, placeholder `"e.g. Fixed fee, short, ≈3–5 weeks"`) → ranked Buyer Roles (`RankedBuyerRolesPicker` with `buyerRoles` prop passed through, `selectedRanked` state seeded from `existingRankedBuyerRoles?.map(r => ({buyerRoleId:r.buyerRoleId, rank:r.rank})) ?? []`) → `status` (Select, options from `catalogStatusEnum.enumValues`).

    Per D-09, below the Status field and SheetFooter-adjacent content (still inside the scrollable field container, per UI-SPEC — the read-only section lives inside `SheetContent`, visually separated from the editable fields by `<Separator className="my-2" />`), render a read-only "Linked Signals" section (OFR-07's reverse lookup, surfaced in the Offering edit Sheet per D-09's placement decision): heading `<p className="text-sm font-semibold text-foreground">Linked Signals</p>`, then if `!linkedSignals || linkedSignals.length === 0` render `"No signals are currently linked to this offering."`, else render two grouped lists (Company Signals, Persona Signals) each as a bullet/plain list of `signal.name` — group by `signalType`. This section renders in edit mode only when `linkedSignals` is provided (create mode has no offering id yet, so omit the section entirely when `mode === 'create'`).

    `canSave = name.trim().length > 0 && practiceAreaId > 0 && description.trim().length > 0`. `handleSubmit` builds the payload `{ practiceAreaId, domainId, name: name.trim(), offerType, description: description.trim(), commercialModelText: commercialModelText.trim() || undefined, status, buyerRoles: selectedRanked }` and calls `createOfferingAction(payload)` or `updateOfferingAction(offering!.id, payload)` imported from `@/app/actions/offerings`.

    On `SheetContent`, add `className="sm:max-w-lg"` — the ONLY Sheet in this entire phase that overrides the default width (per D-03/UI-SPEC line 131). Title: `mode === 'create' ? 'New Offering' : 'Edit Offering'`.
  </action>
  <acceptance_criteria>
    - Source: `offering-form.tsx` exports `function OfferingForm` (or const)
    - Source: `<SheetContent className="sm:max-w-lg">` appears exactly once — this is the only Offering-phase form with a width override; `grep -c "sm:max-w-lg" src/components/offerings/practice-area-form.tsx src/components/offerings/domain-form.tsx` returns 0 for both other files
    - Source: contains `RankedBuyerRolesPicker` import and usage
    - Source: contains `offerTypeEnum.enumValues` and `catalogStatusEnum.enumValues` imports from `@/lib/db/schema` (never hardcoded arrays)
    - Source: contains `"No domain (goes straight to Practice Area)"` literal string (Copywriting Contract exact copy)
    - Source: contains `Separator` import from `@/components/ui/separator` and its usage before the Linked Signals section
    - Behavior: changing the Practice Area Select resets `domainId` to `null` in the same state update (verifiable by reading the onValueChange handler body — it must set both practiceAreaId and clear domainId, not leave a stale domainId from the previous practice area pointing at a domain that no longer appears in the filtered list)
    - Test command: `npx tsc --noEmit` passes with no errors attributable to this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>OfferingForm renders all 8 fields in the mandated order plus the OFR-07 read-only reverse-lookup section, uniquely widened per D-03, and correctly resets Domain when Practice Area changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| OfferingForm submit → createOfferingAction/updateOfferingAction | Untrusted client payload incl. the ranked buyerRoles array |
| linkedSignals prop (OFR-07 read-only display) | Server-fetched, pre-resolved names — this component never fetches or writes signal data itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-05-01 | Information Disclosure | Linked Signals read-only section | accept | Purely a display of names already resolved server-side and passed as props — no new data-fetching surface, no write capability from this section |
| T-30-05-02 | Tampering | Client-computed `domainId`/`offerType`/`status` values | mitigate | All are constrained to Select dropdowns sourced from server-provided option lists (practiceAreas, domainsByPracticeAreaId, the schema enums) — a tampered raw fetch/postMessage call bypassing this UI still hits offerings.ts's zod `.enum()`/`.nullable().optional()` validation server-side (30-03) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- Manual: opening OfferingForm in edit mode with a seeded offering shows all 8 fields pre-populated plus a non-empty Linked Signals section for an offering that has signal links
</verification>

<success_criteria>
- RankedBuyerRolesPicker is a pure, dependency-free controlled component
- OfferingForm covers every OFR-04 field in the mandated order, widens correctly, and surfaces OFR-07's reverse lookup
</success_criteria>

<output>
Create `.planning/phases/30-offerings-ui-v1-6-queued/30-05-SUMMARY.md` when done
</output>
