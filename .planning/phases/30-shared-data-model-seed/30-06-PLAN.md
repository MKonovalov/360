---
phase: 30-shared-data-model-seed
plan: 06
type: execute
wave: 3
depends_on: ["30-02", "30-03", "30-04", "30-05"]
files_modified:
  - src/scripts/seedGbs.ts
  - src/scripts/seedGbs.integration.test.ts
  - package.json
autonomous: true
requirements: [DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, DATA-09]

must_haves:
  truths:
    - "Querying practice_area/domain/buyer_role/offering/offering_buyer_role/trigger after running the seed script returns exactly: 1 practice area, 3 domains, 5 buyer roles, 11 offerings, 22 offering_buyer_role rows, 11 triggers"
    - "Querying company_signal returns 27 rows across all 8 GBS categories; querying persona_signal returns 12 rows, every one referencing a real buyer_role"
    - "Querying signal_offering_link returns exactly the 10 representative link rows derived from spec Section 7.6, each passing the practice-area-match guard"
    - "Re-running the seed script is idempotent — it clears its own previously-seeded rows first, so re-running never duplicates data"
  artifacts:
    - path: "src/scripts/seedGbs.ts"
      provides: "Idempotent GBS seed script — practice area, domains, buyer roles, offerings, triggers, offering-buyer-role links, company signals, persona signals, signal-offering links"
      min_lines: 150
    - path: "package.json"
      provides: "seed:gbs npm script"
      contains: "\"seed:gbs\": \"tsx src/scripts/seedGbs.ts\""
  key_links:
    - from: "seedGbs.ts"
      to: "src/lib/db/queries/offerings.ts, companySignals.ts, personaSignals.ts, signalOfferingLinks.ts, practiceAreas.ts, domains.ts, buyerRoles.ts"
      via: "calls each module's insert*() function rather than raw db.insert(), so the seed data is proven against the same guards (e.g. insertSignalOfferingLink's practice-area check) the app itself will use"
      pattern: "await insert"
---

<objective>
Write the idempotent GBS seed script that loads the full spec Section 7 dataset — the GBS practice area, its 3 domains, 5 buyer roles, 11 offerings (with triggers and ranked buyer-role links), 27 company signals, 12 persona signals, and 10 representative signal-offering links — using the query-module insert functions built in Plans 02-05 (not raw `db.insert()` calls), so the seed data is validated by the exact same guards (delete-guard tables exist, practice-area-match check) the future app will rely on.

Purpose: This is the final, user-visible deliverable of Phase 30 — DATA-03 through DATA-08 are only satisfied once this data is live in Neon. Phase 31 and Phase 32 cannot be usefully demoed without it (empty pickers, no buyer roles to select).
Output: `src/scripts/seedGbs.ts`, `npm run seed:gbs` script entry, `seedGbs.integration.test.ts` row-count assertions, and the actual seeded rows present in the live Neon database.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-shared-data-model-seed/30-CONTEXT.md
@.planning/phases/30-shared-data-model-seed/30-RESEARCH.md
@.planning/specs/v1.4-signals-offerings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: seedGbs.ts skeleton + Offerings-side data (practice area, domains, buyer roles, offerings, triggers, buyer-role links)</name>
  <files>src/scripts/seedGbs.ts, package.json</files>
  <read_first>
    src/scripts/seed.ts (full file — structural analog: dotenv-before-dynamic-import boilerplate, idempotent delete-then-insert, nameToId Map pattern, resolve-or-throw on unresolved reference, main().then/catch exit-code convention)
    src/lib/db/queries/practiceAreas.ts, domains.ts, buyerRoles.ts, offerings.ts (Plans 02/03 output — exact function signatures to call: `insertPracticeArea`, `insertDomain`, `insertBuyerRole`, `insertOffering`, `insertOfferingBuyerRole`, `insertTrigger`)
    package.json (existing `"seed": "tsx src/scripts/seed.ts"` script entry — add a new, separate `"seed:gbs"` entry alongside it, do not overload the existing `seed` script)
    .planning/specs/v1.4-signals-offerings.md (Section 7.1-7.3, lines 210-250 — cross-check the literal data below against the source spec)
  </read_first>
  <action>
    Create `src/scripts/seedGbs.ts` with the exact dotenv-before-dynamic-import boilerplate from `seed.ts` (`import { config } from 'dotenv'; config({ path: '.env.local' });` at the very top, before any other import that transitively touches `src/lib/env.ts`). Define `const SEEDED_BY = 'seed-script';` (sentinel `createdBy`/`updatedBy` value for every row this script inserts — no Clerk session exists in a CLI context, per 30-RESEARCH.md Assumption A2).

    Inside `async function main()`, dynamically import `{ db }` from `'../lib/db'` and the query modules (`practiceAreas`, `domains`, `buyerRoles`, `offerings`) needed for this task, plus the raw schema table objects needed for the idempotent delete step.

    **Idempotent delete (children → parents, respecting FK order — this task's slice only; Task 2 adds the remaining deletes above these in the same sequence):** delete `signalOfferingLink`, then `trigger`, then `offeringBuyerRole`, then `offering`, then `domain`, then `personaSignal`, then `companySignal`, then `buyerRole`, then `practiceArea` — this exact 9-table order must be in place even though this task only inserts a subset, because Task 2 will insert the rest into the same idempotent run; write the full delete sequence now so Task 2 only needs to add insert code.

    **Insert practice area** (spec Section 7.1): call `practiceAreas.insertPracticeArea({ name: 'GBS — Design, Build & Run', shortCode: 'GBS', sortOrder: 1, status: 'active', createdBy: SEEDED_BY })`. Store its `id`.

    **Insert domains** (spec Section 7.1), each via `domains.insertDomain({ practiceAreaId: <gbs id>, name, sortOrder, createdBy: SEEDED_BY })`, building a `domainNameToId` Map:
    | name | sortOrder |
    |---|---|
    | Design | 1 |
    | Build | 2 |
    | Run | 3 |

    **Insert buyer roles** (spec Section 7.2), each via `buyerRoles.insertBuyerRole({ name, createdBy: SEEDED_BY })` (no `description` — spec gives none), building a `buyerRoleNameToId` Map:
    CFO, COO, Head of GBS, Transformation Sponsor, CIO (5 total, insert in this order).

    **Insert 11 offerings** (spec Section 7.3), each via `offerings.insertOffering({ practiceAreaId: <gbs id>, domainId: <domainNameToId lookup>, name, offerType, description, commercialModelText, sortOrder, status: 'active', createdBy: SEEDED_BY })`, building an `offeringNameToId` Map keyed by the exact offering `name` string (needed later for links). The `description` field is NOT supplied by spec Section 7.3 (which only tabulates Name/Offer type/Entry trigger/Buyers) — use the exact one-line descriptions below, written to be a faithful 1-2 sentence summary of each offering's stated entry trigger and buyer intent; **flag in the SUMMARY.md that both `description` and `commercial_model_text` are Claude-authored content consistent with the catalogue's own language, not sourced from the (unavailable) source `.docx` catalogue, mirroring the same accepted-assumption treatment CONTEXT.md already gives `commercial_model_text`.**

    | # | Domain | Name | offerType | sortOrder | description | commercialModelText | Buyers (rank 1, rank 2) |
    |---|---|---|---|---|---|---|---|
    | 1 | Design | GBS Maturity & Readiness Assessment | entry | 1 | An honest, structured baseline of the client's current shared-services/GBS maturity, benchmarked against comparable transformations, used to decide whether and how to proceed. | Fixed fee, short, ≈3–5 weeks | CFO, Head of GBS |
    | 2 | Design | Target Operating Model & Business Case | core | 2 | Designs the future-state GBS operating model and builds the quantified business case for the investment decision to build or reshape it. | Fixed fee, scoped by deliverable, ≈6–10 weeks | CFO, COO |
    | 3 | Design | Footprint & Location Strategy | core | 3 | Evaluates delivery footprint options — captive, BPO, or hybrid — and recommends locations and geographies aligned to cost, talent, and risk objectives. | Fixed fee, scoped by deliverable, ≈6–10 weeks | CFO, Head of GBS |
    | 4 | Build | Transition & Migration Management | programme | 1 | Plans and executes the move of in-scope processes and people into the GBS without disrupting live service delivery. | Programme-based, milestone-billed | Head of GBS, Transformation Sponsor |
    | 5 | Build | Service Management & SLA Framework Set-up | core | 2 | Stands up the service-management machinery — SLAs, KPIs, governance cadence — a GBS needs to run as a service rather than a back office. | Fixed fee, scoped by deliverable, ≈6–10 weeks | Head of GBS, COO |
    | 6 | Build | Change, Adoption & Capability Build | core | 3 | Builds the organisational capability and change adoption needed so people and process actually run the new GBS model, not just the design. | Fixed fee, scoped by deliverable, ≈6–10 weeks | Head of GBS, Transformation Sponsor |
    | 7 | Build | Carve-out / Integration Support | operator_differentiator | 4 | Provides hands-on, operator-grade execution support for shared-services aspects of an M&A carve-out or integration. | Day-rate, deployed as needed, scope varies with the deal | CFO, Transformation Sponsor |
    | 8 | Run | Governance Advisory | retainer | 1 | An independent design authority and governance advisor for a GBS programme or operation already underway. | Retainer, ongoing | CFO, Head of GBS |
    | 9 | Run | Service Performance Assurance | retainer | 2 | Independent oversight of live SLA/KPI performance with a continuous-improvement cadence for an operating GBS. | Retainer, ongoing | Head of GBS, COO |
    | 10 | Run | Interim GBS Leadership | on_request | 3 | Places hands-on interim leadership into the GBS function or programme when the client needs execution, not advice. | Day-rate, on request, duration set by engagement | CFO, COO |
    | 11 | Run | Automation & AI Portfolio Governance & Benefit Realisation | retainer | 4 | Governs the GBS's automation/AI portfolio end-to-end and protects the realisation of its intended benefits. | Retainer, ongoing | CFO, Head of GBS |

    **Insert triggers** — for each offering above, one `trigger` row via `offerings.insertTrigger({ offeringId: <offeringNameToId lookup>, triggerText: <exact Entry Trigger sentence from spec Section 7.3>, sortOrder: 1, createdBy: SEEDED_BY })`. Copy the exact Entry Trigger sentence verbatim from `.planning/specs/v1.4-signals-offerings.md` lines 230-248 for each of the 11 offerings (e.g. offering 1's trigger is "Organisation runs (or is considering) GBS / shared services and wants an honest baseline before committing.").

    **Insert offering_buyer_role links** — for each offering, two `offeringBuyerRole` rows via `offerings.insertOfferingBuyerRole({ offeringId, buyerRoleId: <buyerRoleNameToId lookup>, rank, createdBy: SEEDED_BY })` — `rank: 1` for the first-listed buyer, `rank: 2` for the second, per the "Buyers (rank 1, rank 2)" column in the table above. Total: 22 rows (11 offerings × 2 buyers each).

    For every `<...NameToId>` Map lookup, if the name is not found, throw a descriptive error exactly like `seed.ts`'s `signals.csv references unknown company_name "${row.company_name}"` pattern — e.g. `` `Offering "${name}" references unknown domain "${domainName}" — must match a domain seeded above.` ``.

    Add `"seed:gbs": "tsx src/scripts/seedGbs.ts"` to `package.json`'s `"scripts"` block, alongside the existing `"seed"` entry — a new, separately-runnable script, not an extension of the existing one.

    Do NOT call `main()`/`process.exit` yet in this task — Task 2 completes `main()`'s body (adding the Signals-side inserts) and adds the final `main().then().catch()` invocation. Leave `main()` un-invoked at the bottom of the file for now, or add a `// TODO Task 2: signals-side inserts + main() invocation` comment marker if that is clearer — either is fine as long as the file does not attempt to run yet.
  </action>
  <acceptance_criteria>
    - `grep -c "const SEEDED_BY = 'seed-script'" src/scripts/seedGbs.ts` equals 1
    - `grep -c "insertPracticeArea" src/scripts/seedGbs.ts` is at least 1
    - `grep -c "insertOfferingBuyerRole" src/scripts/seedGbs.ts` is at least 1
    - `grep -c "db.transaction" src/scripts/seedGbs.ts` equals 0 (neon-http has no transaction support — never used)
    - `grep -c "\"seed:gbs\": \"tsx src/scripts/seedGbs.ts\"" package.json` equals 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>seedGbs.ts contains the full idempotent delete sequence and inserts the practice area, 3 domains, 5 buyer roles, 11 offerings, 11 triggers, and 22 offering_buyer_role rows; package.json has the new seed:gbs script; file compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Signals-side data (company signals, persona signals, signal-offering links) + main() wiring</name>
  <files>src/scripts/seedGbs.ts</files>
  <read_first>
    src/scripts/seedGbs.ts (Task 1 output — extend, do not replace)
    src/lib/db/queries/companySignals.ts, personaSignals.ts, signalOfferingLinks.ts (Plans 04/05 output — exact function signatures: `insertCompanySignal`, `insertPersonaSignal`, `insertSignalOfferingLink`)
    .planning/specs/v1.4-signals-offerings.md (Section 7.4-7.6, lines 252-281 — cross-check the literal data below against the source spec)
  </read_first>
  <action>
    Extend `src/scripts/seedGbs.ts` (same file as Task 1) to dynamically import `companySignals`, `personaSignals`, `signalOfferingLinks` query modules inside `main()`.

    **Content-shape decision (apply uniformly, flag in SUMMARY.md):** spec Section 7.4/7.5 gives exactly ONE string per signal (no separate name/description pair), while the schema requires both `name` (short label) and `description` (text, NOT NULL) as distinct columns. Set `description` equal to the same literal string used for `name`, verbatim, for every company_signal and persona_signal row seeded below — this avoids inventing new business-content sentences the firm hasn't reviewed, while still satisfying the NOT NULL constraint faithfully to the one string the source actually provides. This is a deliberate, documented choice, not an oversight — call it out explicitly as an assumption in the plan's completion SUMMARY.

    **Insert 27 company signals** (spec Section 7.4) via `companySignals.insertCompanySignal({ practiceAreaId: <gbs id>, name, category, description: name, status: 'active', createdBy: SEEDED_BY })`, building a `companySignalNameToId` Map keyed by exact `name`:

    | category | names (one row per signal) |
    |---|---|
    | GBS-state | No GBS/SSC exists · GBS exists but has plateaued · GBS recently stood up · Fragmented delivery / captive-BPO renewal window |
    | Financial & commercial | Cost-efficiency narrative in investor communications · Margin compression · Activist investor / cost-cutting mandate · PE ownership change with value-creation plan · Credit outlook citing cost structure |
    | Organizational & restructuring | Announced restructuring or simplification program · New GBS/Shared Services leadership layer visible · Back-office-concentrated layoffs · Named transformation program |
    | M&A & structural | Announced deal with shared-services scope · TSA referenced in deal coverage · Day-1 / 100-day-plan language · Carve-out entity in motion |
    | Technology & ERP | S/4HANA migration announced with no process-redesign narrative · ERP SI tender activity · Legacy system end-of-life pressure |
    | Automation & AI maturity | Ungoverned automation/AI pilots · Automation CoE / AI governance hiring with no existing governance retainer |
    | Public content & intent | GBS/shared-services hiring pattern (Build vs. Run role types) · Shared-services language in careers/press content · Industry conference presence · Public case studies featuring own GBS leadership |
    | Geographic | Expansion into DACH / France / China / APAC / Middle East / CIS |

    Count check: 4 + 5 + 4 + 4 + 3 + 2 + 4 + 1 = 27 rows total (spec's own Section 1/ROADMAP.md approximation of "~24" undercounts the literal Section 7.4 list — seed the full literal 27, never truncate to hit the approximate figure).

    **Insert 12 persona signals** (spec Section 7.5) via `personaSignals.insertPersonaSignal({ practiceAreaId: <gbs id>, buyerRoleId: <buyerRoleNameToId lookup>, name, category, description: name, status: 'active', createdBy: SEEDED_BY })`, building a `personaSignalNameToId` Map keyed by exact `name`. Spec Section 7.5 groups "Head of GBS / COO" as one joint bucket, but `persona_signal.buyer_role_id` is a single required FK — **assign every signal in that bucket to the "Head of GBS" buyer role** (the first-listed, more GBS-specific role), never "COO" and never duplicated across both:

    | buyerRole | category | name |
    |---|---|---|
    | CFO | Tenure/mandate | Newly appointed within 12 months |
    | CFO | Public conviction | Cost-efficiency / operating-model language in public statements |
    | CFO | Career pattern | Previously sponsored a GBS/SSC build |
    | CFO | Org/hiring signal | FP&A/controllership hiring tied to efficiency agenda |
    | Head of GBS | Tenure/mandate | Newly appointed (6–12 months) with prior GBS-build experience |
    | Head of GBS | Tenure/mandate | Entrenched 2+ years with no change narrative |
    | Head of GBS | Public conviction | Talks/posts on maturity assessments, SLA/KPI frameworks |
    | Head of GBS | Org/hiring signal | Hiring transition managers, service delivery managers, or CI leads |
    | Head of GBS | Career pattern | Visible industry engagement (shared-services conferences) |
    | Transformation Sponsor | Tenure/mandate | Role appeared concurrently with an announced deal or named program (tenure irrelevant) |
    | Transformation Sponsor | Public conviction | LinkedIn 'started new position' tied to the named program |
    | Transformation Sponsor | Career pattern | Has run PMO/integration roles across multiple prior deals |

    Note the 4th CFO row's category: spec Section 7.5 labels it ambiguously as "Content engagement/org" — use category `Org/hiring signal` (the signal content describes a hiring pattern, not content engagement) as the resolved, concrete value; do not seed a category literally named "Content engagement/org" (not one of the 5 defined categories).

    Count check: 4 (CFO) + 5 (Head of GBS) + 3 (Transformation Sponsor) = 12 rows total (spec's own approximation of "~9" undercounts the literal Section 7.5 list — seed the full literal 12).

    **Insert 10 signal_offering_link rows** (spec Section 7.6 — 8 table entries, 2 of which link to 2 offerings each, producing 10 total rows) via `signalOfferingLinks.insertSignalOfferingLink({ signalType, signalId: <lookup in companySignalNameToId or personaSignalNameToId depending on signalType>, offeringId: <offeringNameToId lookup>, createdBy: SEEDED_BY })` — no `relevanceNote` (spec gives none):

    | signalType | signal name (must match a name seeded above) | offering name |
    |---|---|---|
    | company | No GBS/SSC exists | GBS Maturity & Readiness Assessment |
    | company | No GBS/SSC exists | Target Operating Model & Business Case |
    | company | GBS exists but has plateaued | Service Performance Assurance |
    | company | GBS exists but has plateaued | Governance Advisory |
    | company | Announced deal with shared-services scope | Carve-out / Integration Support |
    | company | S/4HANA migration announced with no process-redesign narrative | Target Operating Model & Business Case |
    | company | Ungoverned automation/AI pilots | Automation & AI Portfolio Governance & Benefit Realisation |
    | persona | Newly appointed (6–12 months) with prior GBS-build experience | GBS Maturity & Readiness Assessment |
    | persona | Entrenched 2+ years with no change narrative | Service Performance Assurance |
    | persona | Role appeared concurrently with an announced deal or named program (tenure irrelevant) | Carve-out / Integration Support |

    Every `insertSignalOfferingLink` call MUST check its return value: if `{ ok: false, reason: 'practice_area_mismatch' }` is ever returned for one of these 10 rows, throw a descriptive error (`Signal-offering link seed data inconsistency: "${name}" and "${offeringName}" are in different practice areas`) rather than silently skipping it — since every offering/signal seeded in this script belongs to the single GBS practice area, none of these 10 calls should ever hit the mismatch branch; a mismatch here indicates a bug in the seed data itself, not an expected outcome.

    Finish `main()` with a `console.log` summarizing counts inserted (mirrors `seed.ts`'s closing `console.log`, e.g. `` `Inserted: 1 practice area, 3 domains, 5 buyer roles, 11 offerings, 11 triggers, 22 offering-buyer-role links, 27 company signals, 12 persona signals, 10 signal-offering links` ``). Add the closing `main().then(() => process.exit(0)).catch((error) => { console.error(error instanceof Error ? error.message : error); process.exit(1); });` block, copied verbatim in structure from `seed.ts`'s closing block.
  </action>
  <acceptance_criteria>
    - `grep -c "insertSignalOfferingLink" src/scripts/seedGbs.ts` is at least 1
    - `grep -c "practice_area_mismatch" src/scripts/seedGbs.ts` is at least 1 (the mismatch-guard error-throw is present)
    - `grep -c "main().then" src/scripts/seedGbs.ts` equals 1
    - `npx tsc --noEmit` exits 0
    - Manual line-count sanity: the file contains exactly 27 distinct company-signal name strings and exactly 12 distinct persona-signal name strings (spot-check against the tables above)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>seedGbs.ts fully implements the idempotent GBS seed (all 9 entity types), throws loudly on any unresolved name reference or unexpected practice-area mismatch, and compiles clean.</done>
</task>

<task type="auto">
  <name>Task 3: Run the seed against the live DB + row-count integration test</name>
  <files>src/scripts/seedGbs.integration.test.ts</files>
  <read_first>
    src/lib/db/queries/userModelSettings.integration.test.ts (gating/setup pattern — adapt for a read-only count-check, no per-test fixture cleanup needed since this test reads the seed script's own output)
    src/scripts/seedGbs.ts (Task 1+2 output)
  </read_first>
  <action>
    Run `npm run seed:gbs` against the real `DATABASE_URL` in `.env.local` (the live Neon dev database Plan 01 already pushed the schema to). Confirm the script exits 0 and its closing `console.log` reports the expected counts.

    Create `src/scripts/seedGbs.integration.test.ts`, gated on `process.env.TEST_DATABASE_URL` exactly like `userModelSettings.integration.test.ts` (`describe.skip` fallback when unset). Inside the gated `describe` block, assert via direct `db.select({ count: sql<number>`count(*)::int` })` queries (or `db.select().from(table)` + `.length`) against each of the 9 new tables, run AFTER invoking the same insert logic this script uses (either by importing and calling a small exported `seedGbs()` function refactored out of `main()`, or by re-running `npm run seed:gbs` as a `beforeAll` `execSync` call against `TEST_DATABASE_URL` — pick whichever keeps the test self-contained without duplicating the seed data a third time; if refactoring `main()`'s body into an exported `seedGbs(databaseUrl?: string)` function is cleaner, do that and call it directly from the test's `beforeAll`). Assert exact counts: `practiceArea` = 1, `domain` = 3, `buyerRole` = 5, `offering` = 11, `offeringBuyerRole` = 22, `trigger` = 11, `companySignal` = 27, `personaSignal` = 12, `signalOfferingLink` = 10.
  </action>
  <acceptance_criteria>
    - `npm run seed:gbs` (run manually against `.env.local`) exits 0
    - A direct row-count query against the live `DATABASE_URL` confirms: 1 practice_area, 3 domain, 5 buyer_role, 11 offering, 22 offering_buyer_role, 11 trigger, 27 company_signal, 12 persona_signal, 10 signal_offering_link
    - Re-running `npm run seed:gbs` a second time produces the SAME counts (idempotency — confirms the delete-then-insert sequence fully cleans up before re-inserting)
    - `npx vitest run src/scripts/seedGbs.integration.test.ts` passes when `TEST_DATABASE_URL` is set (or reports skipped, not failed, when unset)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/scripts/seedGbs.integration.test.ts</automated>
  </verify>
  <done>GBS seed data is live in the real Neon dev database with exact expected row counts across all 9 tables; a second seed run is idempotent; the gated integration test encodes these counts as a repeatable check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `tsx` CLI script → live Neon Postgres | The seed script runs with full DB write access outside any request/auth boundary — it is a trusted, developer-run CLI tool, not a web-facing surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering / Information Disclosure | `insertSignalOfferingLink` calls inside `seedGbs.ts` | mitigate | The seed script routes all 10 links through the same `insertSignalOfferingLink()` guard built in Plan 05 (cross-practice-area link integrity) rather than a raw `db.insert()` — Task 2 explicitly throws if any of the 10 calls ever returns `practice_area_mismatch`, so a future spec-data error surfaces loudly instead of silently seeding a broken link |
| T-30-09 | Repudiation | `SEEDED_BY = 'seed-script'` sentinel on every row | accept | Documented, low-risk assumption (30-RESEARCH.md A2) — no Clerk session exists in a CLI context; the sentinel is a plain string with no FK, easily correctable via a bulk `UPDATE` later if the firm wants real-user attribution for seed data |
| T-30-09b | Tampering | Running an idempotent delete-then-insert script directly against production Neon data | accept | This is the intended, documented workflow for this dev-tool-only script (matches `seed.ts` precedent exactly) — the delete step only ever targets the 9 Phase 30 tables, never `company`/`persona`/`signal` or any other existing entity |
</threat_model>

<verification>
- `npx tsc --noEmit` passes after both Task 1 and Task 2
- `npm run seed:gbs` succeeds against the real dev DB with exact expected counts, and is idempotent on a second run
- `npx vitest run src/scripts/seedGbs.integration.test.ts` passes when `TEST_DATABASE_URL` is set
</verification>

<success_criteria>
- All GBS seed data from spec Section 7 is live in the database with the exact counts: 1/3/5/11/22/11/27/12/10 across the 9 tables
- Every `commercial_model_text` and offering `description` is flagged in the SUMMARY as Claude-authored (not sourced from the unavailable catalogue `.docx`), matching CONTEXT.md's pre-approved treatment of this exact gap
- Every company/persona signal's `description` is documented as intentionally identical to its `name` (spec provides only one string per signal, not a separate description)
- Re-running `npm run seed:gbs` is idempotent
</success_criteria>

<output>
Create `.planning/phases/30-shared-data-model-seed/30-06-SUMMARY.md` when done — explicitly list the content-authorship assumptions (offering description + commercial_model_text; company/persona signal description = name) and the two disambiguation decisions made (Head of GBS/COO bucket → Head of GBS; CFO's "Content engagement/org" → "Org/hiring signal") for human review.
</output>
