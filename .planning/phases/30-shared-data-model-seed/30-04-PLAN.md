---
phase: 30-shared-data-model-seed
plan: 04
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/lib/db/queries/companySignals.ts
  - src/lib/db/queries/companySignals.test.ts
  - src/lib/db/queries/companySignals.integration.test.ts
  - src/lib/db/queries/personaSignals.ts
  - src/lib/db/queries/personaSignals.test.ts
  - src/lib/db/queries/personaSignals.integration.test.ts
autonomous: true
requirements: [DATA-02, DATA-09]

must_haves:
  truths:
    - "company_signal and persona_signal each have CRUD query functions, distinct from the unrelated existing signals.ts module for the v1.0 buying-signal entity"
    - "Every persona_signal always references a real buyer_role_id — never null, never a placeholder"
    - "Category values are free text (no enum) with a distinct-values query to support future autocomplete"
  artifacts:
    - path: "src/lib/db/queries/companySignals.ts"
      provides: "insertCompanySignal, updateCompanySignal, listAllCompanySignalsForPracticeArea, listActiveCompanySignalsForPracticeArea, listDistinctCompanySignalCategories"
    - path: "src/lib/db/queries/personaSignals.ts"
      provides: "insertPersonaSignal, updatePersonaSignal, listAllPersonaSignalsForPracticeArea, listActivePersonaSignalsForPracticeArea, listDistinctPersonaSignalCategories"
  key_links:
    - from: "personaSignal.buyerRoleId"
      to: "buyerRole.id"
      via: "insertPersonaSignal requires buyerRoleId: number (not optional)"
      pattern: "buyerRoleId: number"
---

<objective>
Build the two Signals-feature query modules — `companySignals.ts` and `personaSignals.ts` — covering CRUD and the active/all picker split, explicitly named to avoid colliding with the pre-existing, unrelated `signals.ts` module (the v1.0 typed buying-signal entity).

Purpose: Company and Persona Signals are the entities the seed script populates (DATA-06/DATA-07) and that Phase 31's UI will list/filter/edit — this module establishes their query-layer contract now.
Output: `companySignals.ts`, `personaSignals.ts` + matching unit and integration test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-shared-data-model-seed/30-CONTEXT.md
@.planning/phases/30-shared-data-model-seed/30-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: companySignals.ts query module + tests</name>
  <files>src/lib/db/queries/companySignals.ts, src/lib/db/queries/companySignals.test.ts, src/lib/db/queries/companySignals.integration.test.ts</files>
  <read_first>
    src/lib/db/queries/signals.ts (structural analog — insert + list-by-parent shape, full file; DO NOT name the new file this — it already exists for the unrelated v1.0 `signal` buying-signal entity, per RESEARCH.md Pitfall 4)
    src/lib/db/queries/proposals.ts (filtered-list idiom for the active/all split)
    src/lib/db/queries/userModelSettings.integration.test.ts (integration test scaffold pattern)
    src/lib/db/schema.ts (`companySignal` table shape after Plan 01)
  </read_first>
  <behavior>
    - `insertCompanySignal` inserts a row with `createdBy`/`updatedBy` from input, `category` stored as free text exactly as given (no validation against a fixed list)
    - `updateCompanySignal` explicitly sets `updatedAt`/`updatedBy`
    - `listAllCompanySignalsForPracticeArea(practiceAreaId)` returns every company signal for that practice area regardless of status
    - `listActiveCompanySignalsForPracticeArea(practiceAreaId)` returns only `status = 'active'` rows
    - `listDistinctCompanySignalCategories()` returns the distinct set of `category` values currently in use, for autocomplete
  </behavior>
  <action>
    Create `src/lib/db/queries/companySignals.ts` (relative imports, never `@/`). Export:
    - `insertCompanySignal(input: { practiceAreaId: number; name: string; category: string; description: string; status?: 'active'|'draft'|'retired'; createdBy: string })` — `.insert(companySignal).values({ ...input, updatedBy: input.createdBy }).returning()`.
    - `updateCompanySignal(id: number, patch: Partial<typeof companySignal.$inferInsert>, updatedBy: string)` — explicit `updatedAt`/`updatedBy` set.
    - `listAllCompanySignalsForPracticeArea(practiceAreaId: number)` — `.where(eq(companySignal.practiceAreaId, practiceAreaId))`.
    - `listActiveCompanySignalsForPracticeArea(practiceAreaId: number)` — adds `eq(companySignal.status, 'active')`.
    - `listDistinctCompanySignalCategories()` — `.selectDistinct({ category: companySignal.category }).from(companySignal).orderBy(companySignal.category)`, mapped to `string[]` before returning (supports the spec's "autocomplete suggestions from existing values" requirement without hardcoding categories as an enum).
    No try/catch.

    Create `companySignals.test.ts` (mocked db, `vi.hoisted`/`vi.mock('../index', ...)` pattern from `proposals.test.ts`): cover insert sets `updatedBy = createdBy`, `category` is passed through unmodified as free text (assert the exact string round-trips, not coerced to any enum), active/all split filters correctly.

    Create `companySignals.integration.test.ts` mirroring `userModelSettings.integration.test.ts` — insert a company signal under a real practice area, confirm round-trip, confirm `listDistinctCompanySignalCategories` includes the inserted category.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/db/queries/companySignals.test.ts` passes
    - The file is NOT named `signals.ts` (confirm `ls src/lib/db/queries/companySignals.ts` succeeds and the pre-existing `src/lib/db/queries/signals.ts` is untouched — `git diff --stat src/lib/db/queries/signals.ts` shows no changes)
    - `grep -c "requireStaffAccess" src/lib/db/queries/companySignals.ts` equals 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/db/queries/companySignals.test.ts</automated>
  </verify>
  <done>companySignals.ts implemented and unit-tested; pre-existing signals.ts is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: personaSignals.ts query module + tests</name>
  <files>src/lib/db/queries/personaSignals.ts, src/lib/db/queries/personaSignals.test.ts, src/lib/db/queries/personaSignals.integration.test.ts</files>
  <read_first>
    src/lib/db/queries/companySignals.ts (Task 1 output — match its exact shape, extended with the required `buyerRoleId`)
    src/lib/db/schema.ts (`personaSignal` table shape — `buyerRoleId` is `notNull`, references `buyerRole.id`)
  </read_first>
  <behavior>
    - `insertPersonaSignal` requires `buyerRoleId: number` (never optional, never null) alongside the same fields as `companySignal`
    - `updatePersonaSignal` explicitly sets `updatedAt`/`updatedBy`
    - `listAllPersonaSignalsForPracticeArea(practiceAreaId)` / `listActivePersonaSignalsForPracticeArea(practiceAreaId)` mirror the company-signal split
    - `listDistinctPersonaSignalCategories()` returns distinct category values
  </behavior>
  <action>
    Create `src/lib/db/queries/personaSignals.ts` with the exact same shape as `companySignals.ts`, plus the required `buyerRoleId` field: `insertPersonaSignal(input: { practiceAreaId: number; buyerRoleId: number; name: string; category: string; description: string; status?: 'active'|'draft'|'retired'; createdBy: string })`, `updatePersonaSignal(id, patch, updatedBy)`, `listAllPersonaSignalsForPracticeArea(practiceAreaId)`, `listActivePersonaSignalsForPracticeArea(practiceAreaId)`, `listDistinctPersonaSignalCategories()`. No try/catch. `buyerRoleId` is a TypeScript required field (not `?`) — this is the mechanism that makes "never null, never a placeholder" (DATA-07) enforceable at the call-site type level, in addition to the DB's `NOT NULL` constraint.

    Create `personaSignals.test.ts` and `personaSignals.integration.test.ts` mirroring `companySignals.ts`'s test structure, additionally asserting `buyerRoleId` is present in every insert payload and that the integration test's inserted row references a real `buyer_role` row (not a fabricated id).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/db/queries/personaSignals.test.ts` passes
    - `grep -c "buyerRoleId: number" src/lib/db/queries/personaSignals.ts` is at least 1 (non-optional, no `?` suffix on the type)
    - `grep -c "buyerRoleId?" src/lib/db/queries/personaSignals.ts` equals 0 (confirms it is never made optional)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/db/queries/personaSignals.test.ts</automated>
  </verify>
  <done>personaSignals.ts implemented, buyerRoleId enforced as required at both the TS and DB layers, unit-tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Future Signals UI (Phase 31) form submission → these query modules | Category free-text values are user-authored strings; stored as-is with no server-side allowlist (spec explicitly rejects a fixed enum) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-07 | Tampering | `category` free-text field (`company_signal`, `persona_signal`) | accept | Low risk — internal 3-partner tool, no public write surface exists yet (no Server Actions in this phase); Drizzle parameterizes all values, so this is a data-hygiene concern (inconsistent category spellings), not an injection risk |
| T-30-03 | Repudiation | `insertCompanySignal`/`insertPersonaSignal` | accept | `created_by`/`updated_by` `NOT NULL` at schema level; functions require them as parameters |
</threat_model>

<verification>
- `npx vitest run src/lib/db/queries/companySignals.test.ts src/lib/db/queries/personaSignals.test.ts` pass
- `npm test` full suite green
</verification>

<success_criteria>
- `companySignals.ts` and `personaSignals.ts` exist with the exact function sets specified, correctly named to avoid the `signals.ts` collision
- `personaSignal.buyerRoleId` is enforced as required (never optional) in both the TypeScript input type and the underlying DB `NOT NULL` constraint
- Both modules support the active/all picker split and a distinct-categories helper for future autocomplete
</success_criteria>

<output>
Create `.planning/phases/30-shared-data-model-seed/30-04-SUMMARY.md` when done
</output>
