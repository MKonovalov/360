---
phase: 18-verification-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agents/runAgent.test.ts
  - src/lib/models/catalog.test.ts
  - src/lib/agents/modelConfig.test.ts
  - .planning/phases/18-verification-gate/18-VER-01-MATRIX.md
autonomous: true
requirements: [VER-01, VER-02]

must_haves:
  truths:
    - "401, 403, and output/schema errors never advance the chain at loop level (single attempt, fail loud per D-06)"
    - "A RetryError-wrapped 404 unwraps to model_not_found and advances to the fallback at loop level"
    - "The committed 1131-model snapshot yields exactly ['claude-sonnet-4-6'] with zero opencode//dated-ID leakage"
    - "resolveModelChain covers the default/partial/full chain matrix (partial cell now explicit)"
    - "A reviewer can map each of the 13 checklist items to a test / UAT line / grep proof in one artifact"
  artifacts:
    - path: "src/lib/agents/runAgent.test.ts"
      provides: "4 new loop-level failover tests in describe('runAgent failover loop (FAL-03/04)')"
      contains: "401 never advances|403 never advances|output/schema errors never advance|RetryError-wrapped 404"
    - path: "src/lib/models/catalog.test.ts"
      provides: "real-snapshot test pinning the committed catalog.json"
      contains: "committed 1131-model snapshot"
    - path: "src/lib/agents/modelConfig.test.ts"
      provides: "explicit partial-chain pass-through test"
      contains: "partial chain"
    - path: ".planning/phases/18-verification-gate/18-VER-01-MATRIX.md"
      provides: "requirement → test → assertion map + 13-item checklist disposition table (D-18-01/D-18-04)"
      contains: "satisfied-by-extension"
  key_links:
    - from: "src/lib/agents/runAgent.test.ts"
      to: "src/lib/agents/modelConfig.ts"
      via: "real SDK error classes through the vi.mock importOriginal spread"
      pattern: "InvalidResponseDataError"
    - from: "src/lib/models/catalog.test.ts"
      to: "src/lib/models/catalog.json"
      via: "import catalogJson from './catalog.json'"
      pattern: "catalogJson"
    - from: ".planning/phases/18-verification-gate/18-VER-01-MATRIX.md"
      to: "runAgent.test.ts / catalog.test.ts / modelConfig.test.ts test names"
      via: "requirement → test → assertion rows"
      pattern: "covered-by-existing-test|new-work"
---

<objective>
Fill the four loop-level failover test gaps in `runAgent.test.ts` (401, 403, output/schema, RetryError-wrapped 404 — D-18-01), lock VER-02's catalog/chain cells with two additive tests (real-snapshot + explicit partial-chain), and author the `18-VER-01-MATRIX.md` traceability artifact mapping the 13-item PITFALLS checklist onto VER-01..04 (D-18-04).

Purpose: VER-01 and VER-02 are the milestone's automated proof tiers — a reviewer must be able to see the failover taxonomy and catalog/chain logic provable at a glance. This plan produces only tests and a matrix artifact — zero production code changes (D-18-02).
Output: 6 new test cases across 3 suites (≈293 total suite tests), the matrix artifact, and green full-suite + tsc gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 18 section — VER-01/02 success criteria)
@.planning/REQUIREMENTS.md (VER-01/VER-02 definitions)
@.planning/research/PITFALLS.md (13-item "Looks Done But Isn't" Checklist, lines 345-359)
@.planning/phases/18-verification-gate/18-RESEARCH.md (Code Examples 1-6, lines 259-339; Phase Requirements → Test Map, lines 427-440; Pitfalls 1-7)
@.planning/phases/18-verification-gate/18-PATTERNS.md (runAgent test shapes lines 21-110; catalog lines 114-147; modelConfig lines 149-169; matrix format lines 173-196)
@.planning/phases/18-verification-gate/18-VALIDATION.md (per-task verification map rows 18-01-01/02/03; quick-run command)

The 18-VALIDATION.md per-task map is the contract this plan implements: task IDs 18-01-01 (VER-01 loop tests), 18-01-02 (VER-02 additive tests), 18-01-03 (matrix artifact).
</context>

<tasks>

<task type="auto">
  <name>Task 1 (18-01-01): Add 4 loop-level failover tests to runAgent.test.ts — VER-01</name>
  <files>src/lib/agents/runAgent.test.ts</files>
  <read_first>
    - src/lib/agents/runAgent.test.ts (the file being modified — current state: 13 tests, describe block at :124, mock seam :8-29, apiErr helper :136-137, RetryError-5xx pattern :217-236)
    - @.planning/phases/18-verification-gate/18-PATTERNS.md lines 21-110 (verbatim shapes for all 4 tests + import change)
    - @.planning/phases/18-verification-gate/18-RESEARCH.md lines 259-318 (Code Examples 1-4 with assertion contracts)
  </read_first>
  <action>
    Modify `src/lib/agents/runAgent.test.ts` ONLY — no other file. Add exactly 4 new `it(...)` blocks INSIDE the existing `describe('runAgent failover loop (FAL-03/04)')` block (line 124) — do NOT create a new describe block or file (fragments the seam's mock setup, 18-PATTERNS anti-pattern).

    1. Import change (line 2): extend the `'ai'` import to `import { APICallError, InvalidResponseDataError, RetryError } from 'ai';` — `InvalidResponseDataError` survives the vi.mock importOriginal spread (same mechanism that keeps RetryError real, per the comment at :132-135) so the unmocked `classifyModelError` classifies it.

    2. Add these 4 tests inside the failover-loop describe, reusing the existing `apiErr` helper (:136-137), `company`/`resolvedRun` fixtures (:35-55), and the 2-model chain call shape `runAgent({ company, liveSignals: [], models: [mocks.anthropic(), mocks.anthropic()] })`:
       - `it('401 never advances — single attempt, throws (Pitfall 2)')` — `mocks.generateText.mockRejectedValueOnce(apiErr(401))`, then `await expect(...runAgent...).rejects.toThrow()` and `expect(mocks.generateText).toHaveBeenCalledTimes(1)`. Mirror :164-171.
       - `it('403 never advances — single attempt, throws (Pitfall 2)')` — identical shape with `apiErr(403)`.
       - `it('output/schema errors never advance — single attempt, throws (D-01)')` — `mocks.generateText.mockRejectedValueOnce(new InvalidResponseDataError({ data: {} }))` (constructor shape verified at modelConfig.test.ts:80), same rejects + calledTimes(1) assertions.
       - `it('RetryError-wrapped 404 unwraps to model_not_found and still advances (Pitfall 3)')` — `mockRejectedValueOnce(new RetryError({ message: 'max retries exceeded', reason: 'maxRetriesExceeded', errors: [apiErr(404)] })).mockResolvedValueOnce(resolvedRun)`, then `expect(mocks.generateText).toHaveBeenCalledTimes(2)` and `expect(result).toEqual({ ...resolvedRun, modelUsed: 'claude-sonnet-4-6', usedFallback: true })`. Mirror :217-236.

    Do NOT touch: the mock seam (:8-29), the default-model test (:113-116 — checklist item 12 pins `anthropic('claude-sonnet-4-6')`), or any existing test. No fenced code blocks in this action — copy the exact shapes from PATTERNS.md/RESEARCH.md.
  </action>
  <acceptance_criteria>
    - `src/lib/agents/runAgent.test.ts` contains exactly 4 new `it(...)` titles matching `401 never advances`, `403 never advances`, `output/schema errors never advance`, `RetryError-wrapped 404` — checkable via `grep -cE "it\('(401 never advances|403 never advances|output/schema errors never advance|RetryError-wrapped 404)" src/lib/agents/runAgent.test.ts` returning 4.
    - The 4 tests are inside `describe('runAgent failover loop (FAL-03/04)')` (no new describe block — `grep -c "describe(" src/lib/agents/runAgent.test.ts` still returns 3).
    - `npx vitest run src/lib/agents/runAgent.test.ts` exits 0 with 17 tests passing (13 existing + 4 new).
    - The default-model test at :113-116 is unchanged (`anthropic('claude-sonnet-4-6')` still present — checklist item 12).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/agents/runAgent.test.ts</automated>
    <automated>grep -cE "it\('(401 never advances|403 never advances|output/schema errors never advance|RetryError-wrapped 404)" src/lib/agents/runAgent.test.ts</automated>
  </verify>
  <done>4 new loop-level tests pass in the existing failover-loop describe; suite green (17 tests); default-model test untouched.</done>
</task>

<task type="auto">
  <name>Task 2 (18-01-02): Add VER-02 additive tests — real-snapshot catalog + explicit partial-chain resolve</name>
  <files>src/lib/models/catalog.test.ts, src/lib/agents/modelConfig.test.ts</files>
  <read_first>
    - src/lib/models/catalog.test.ts (file being modified — 9 tests, fixture :14-58, describe('getAllowlistedServableIds') :78-82, fixture-decoupling comment :11-13)
    - src/lib/agents/modelConfig.test.ts (file being modified — 12 tests, describe('resolveModelChain') :112-140, cap test :123-127)
    - @.planning/phases/18-verification-gate/18-PATTERNS.md lines 114-169 (verbatim shapes for both additive tests)
    - @.planning/phases/18-verification-gate/18-RESEARCH.md lines 320-339 (Code Examples 5-6; Pitfall 4 rationale)
  </read_first>
  <action>
    Add exactly 2 additive tests (recommended by research Open Questions 2-3 — closes Pitfall 4 and the literal partial-chain cell of VER-02):

    1. `src/lib/models/catalog.test.ts`:
       - Add the import `import catalogJson from './catalog.json';` near the top (alongside the existing named imports at :2-8) — this is the ONE deliberate exception to the fixture-decoupling convention documented at :11-13 (comment stays, add the test below it in the same describe).
       - Inside `describe('getAllowlistedServableIds')` (:78-82) add: `it('committed 1131-model snapshot yields exactly the servable allowlist — zero leakage (CAT-03)')` asserting `expect(getAllowlistedServableIds(catalogJson)).toEqual(['claude-sonnet-4-6'])` and `expect(getAllowlistedServableIds(catalogJson).some((id) => id.includes('/'))).toBe(false)`. Do NOT hardcode "1131" in a way that breaks on refresh — the model count is descriptive in the test title; the assertion pins the allowlist result, not the count.

    2. `src/lib/agents/modelConfig.test.ts`:
       - At the end of `describe('resolveModelChain')` (:112-140) add: `it('a partial chain (primary + one fallback) passes through intact when allowlisted')` asserting `expect(resolveModelChain({ primaryModel: 'a', fallbackModels: ['b'] }, ['a', 'b'])).toEqual(['a', 'b'])`. Two-arg call shape mirrors the cap test at :123-127.

    Do NOT touch any existing test in either file. Do NOT modify catalog.ts, modelConfig.ts, or catalog.json (zero production changes, D-18-02).
  </action>
  <acceptance_criteria>
    - `grep -c "committed 1131-model snapshot" src/lib/models/catalog.test.ts` returns 1 and `grep -c "partial chain" src/lib/agents/modelConfig.test.ts` returns 1.
    - `npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelConfig.test.ts` exits 0 — catalog suite now 10 tests (9+1), modelConfig suite now 13 tests (12+1).
    - `src/lib/models/catalog.test.ts` fixture tests (:60-106) unchanged — the real-snapshot test is additive only.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelConfig.test.ts</automated>
    <automated>grep -c "committed 1131-model snapshot" src/lib/models/catalog.test.ts</automated>
    <automated>grep -c "partial chain" src/lib/agents/modelConfig.test.ts</automated>
  </verify>
  <done>Real-snapshot test pins ['claude-sonnet-4-6'] + zero '/' leakage; partial-chain pass-through test added; both suites green.</done>
</task>

<task type="auto">
  <name>Task 3 (18-01-03): Author 18-VER-01-MATRIX.md — requirement → test → assertion + 13-item checklist map</name>
  <files>.planning/phases/18-verification-gate/18-VER-01-MATRIX.md</files>
  <read_first>
    - @.planning/research/PITFALLS.md lines 345-359 (the AUTHORITATIVE 13-item checklist — copy verbatim, do NOT rewrite, do NOT trust the "12" in prior docs; research Pitfall 1)
    - @.planning/phases/18-verification-gate/18-RESEARCH.md lines 427-440 (Phase Requirements → Test Map — the mapping content source) and lines 211-253 (Pitfalls 1-7: count corrections, usedFallback nuance, SC-3 disposition, integration self-skip)
    - @.planning/phases/18-verification-gate/18-PATTERNS.md lines 173-196 (matrix artifact format: YAML frontmatter per 17-UAT.md:1-7, numbered-row format per 17-UAT.md:15-17)
    - @.planning/phases/17-settings-ui-list-source/17-UAT.md (frontmatter + numbered-test row precedent)
    - The test files just modified (runAgent.test.ts, catalog.test.ts, modelConfig.test.ts) — the matrix cites REAL test names
  </read_first>
  <action>
    Author `.planning/phases/18-verification-gate/18-VER-01-MATRIX.md` (new file) with YAML frontmatter `status: complete`, `phase: 18-verification-gate`, `source: [18-CONTEXT.md, 18-RESEARCH.md]`, `started`/`updated` ISO timestamps. Three required sections:

    1. `## Requirement → Test → Assertion Map` — four subsections `### VER-01` .. `### VER-04`, each a row `requirement | test file | describe block | test name | assertion`. Use the REAL test names from the suites:
       - VER-01: the 4 new runAgent loop tests (401, 403, output/schema, RetryError-404) + existing runAgent loop tests (404 :139, 400 :164, 429 :155, exhaustion :173, budgets :183, clamp :196, RetryError-5xx :217) + modelConfig taxonomy tests (7 classify tests).
       - VER-02: catalog.test.ts (9 tests — CORRECTED count, not 11, per research Pitfall 2) incl. the new real-snapshot test; modelConfig resolveModelChain (5 tests + new partial-chain test); settings.test.ts (7 security-matrix tests).
       - VER-03: row pointing to 18-UAT.md test 1 with the Pitfall-10 wording assertion (`agent_run.model_used` == saved primary) and the exact DB query `SELECT model_used, model_chain FROM agent_run ORDER BY id DESC LIMIT 1;` — add a NOTE that `usedFallback` is response-only (route.ts:111), never a DB column (research Pitfall 5).
       - VER-04: rows for the zero-hit grep gate (exact command from 15-VERIFICATION Truth 8) and the preview-URL render check (executed in plan 18-03).
    2. `## Looks Done But Isn't Checklist Map (D-18-04)` — a table with ALL 13 checklist items copied VERBATIM from PITFALLS.md:347-359, one row per item, each with a `disposition` column containing exactly `covered-by-existing-test` (file + real test name) or `new-work` (new test / 18-UAT.md line / grep command). Every item must land in exactly one proof surface. Item 9 (concurrent saves) disposition cites the 15-VERIFICATION executed run (4/4, 2026-08-02) — the integration suite self-skips without TEST_DATABASE_URL (research Pitfall 3); do not claim plain `npm test` proves it.
    3. `## Dispositions` — two mandatory notes: (a) SC-3 forced-fail clause recorded as `satisfied-by-extension via runAgent.test.ts RetryError-404 + exhaustion tests (D-18-02)` (research Pitfall 6); (b) count corrections — checklist is 13 items (not 12), catalog.test.ts is 9 tests (not 11).

    Structure the checklist map so each disposition token appears exactly once per row — the automated grep relies on `| covered-by-existing-test |` / `| new-work |` cell patterns.
  </action>
  <acceptance_criteria>
    - `grep -cE '^### VER-0[1-4]' .planning/phases/18-verification-gate/18-VER-01-MATRIX.md` returns 4 (one subsection per requirement).
    - `grep -cE '\| (covered-by-existing-test|new-work) \|' .planning/phases/18-verification-gate/18-VER-01-MATRIX.md` returns 13 (every checklist item has exactly one disposition cell).
    - `grep -c "satisfied-by-extension" .planning/phases/18-verification-gate/18-VER-01-MATRIX.md` returns ≥ 1 (SC-3 disposition recorded).
    - `grep -c "9 tests" .planning/phases/18-verification-gate/18-VER-01-MATRIX.md` returns ≥ 1 and `grep -c "13 items"` (or the equivalent "13-item") returns ≥ 1 — the count corrections are visible.
    - Every test name cited in the matrix exists in the suites — spot-verified via grep of 3 cited names.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE '^### VER-0[1-4]' .planning/phases/18-verification-gate/18-VER-01-MATRIX.md</automated>
    <automated>grep -cE '\| (covered-by-existing-test|new-work) \|' .planning/phases/18-verification-gate/18-VER-01-MATRIX.md</automated>
    <automated>grep -c "satisfied-by-extension" .planning/phases/18-verification-gate/18-VER-01-MATRIX.md</automated>
  </verify>
  <done>Matrix artifact maps VER-01..04 to real tests/assertions and all 13 checklist items to exactly one proof surface; SC-3 + count corrections recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production source | Tests import real modules (modelConfig, catalog) but must never alter production behavior |
| catalog.json → filter functions | The committed snapshot is the single source of truth; a stale/tampered snapshot would change what models the app offers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | src/lib/models/catalog.json | mitigate | Real-snapshot test (Task 2) pins `getAllowlistedServableIds(catalogJson)` to exactly `['claude-sonnet-4-6']` with a `.some(id.includes('/')) === false` guard — any snapshot drift or opencode/ leakage fails CI (ASVS V5 servable-set integrity, research threat "snapshot tampering") |
| T-18-02 | Tampering | production source files | mitigate | D-18-02: zero production code changes this phase — only `*.test.ts` files and the phase artifact are touched. The planned grep gate (plan 18-03) confirms no subprocess surface in src/ (research threat "test-only code leaking to prod") |
| T-18-03 | Tampering | test fixtures | accept | Inline fixtures are deliberately decoupled from the snapshot (comment :11-13); the single exception is the real-snapshot test, which is additive and drift-guarded by its assertion — no risk |
| T-18-SC | Tampering | npm installs | accept | No packages installed this phase (research Package Legitimacy Audit: vacuous — vitest 4.1.10 already installed); no install task exists, so no slopcheck checkpoint applies |
</threat_model>

<verification>
Per-task: `npx vitest run` on each touched suite (Tasks 1-2) + grep assertions on the matrix (Task 3).
Per-wave: `npm test` (full suite — checklist item 12, ≈293 tests) and `npx tsc --noEmit` (17-03 precedent type gate) must both exit 0 before the wave is complete.
</verification>

<success_criteria>
- The 4 previously-missing loop-level cases (401, 403, output/schema, RetryError-404) exist and pass in the failover-loop describe block.
- VER-02 cells are cell-for-cell true: real-snapshot catalog test + explicit partial-chain test added, both suites green.
- `18-VER-01-MATRIX.md` maps all 13 checklist items (verbatim from PITFALLS.md:347-359) and all 4 requirements to real tests/assertions/UAT-lines/grep commands.
- Full suite (`npm test`) and type gate (`npx tsc --noEmit`) exit 0.
</success_criteria>

<output>
Create `.planning/phases/18-verification-gate/18-01-SUMMARY.md` when done
</output>
