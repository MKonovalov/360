---
phase: 18-verification-gate
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - .planning/phases/18-verification-gate/18-UAT.md
  - .planning/phases/18-verification-gate/18-VERIFICATION.md
autonomous: false
requirements: [VER-03]

must_haves:
  truths:
    - "A live browser run proves Settings → pick primary → save → run Analyze → agent_run.model_used equals the saved primary (Pitfall 10 core acceptance)"
    - "The 16-HUMAN-UAT 2 pending items (status strip rendering, live-run audit trail) and the 17-03 deferred <human-check> are folded into this run and recorded"
    - "The UAT asserts DB columns model_used/model_chain only — usedFallback is response-only, never queried"
    - "SC-3 forced-fail clause is recorded as satisfied-by-extension via Vitest (D-18-02) — no fail hook added"
  artifacts:
    - path: ".planning/phases/18-verification-gate/18-UAT.md"
      provides: "VER-03 live run record (17-UAT.md format: numbered tests, expected/result, Summary)"
      contains: "model_used|model_chain"
    - path: ".planning/phases/18-verification-gate/18-VERIFICATION.md"
      provides: "phase-gate evidence with SC-3 satisfied-by-extension disposition"
      contains: "satisfied-by-extension"
  key_links:
    - from: "18-UAT.md"
      to: "agent_run row (Postgres)"
      via: "SELECT model_used, model_chain FROM agent_run ORDER BY id DESC LIMIT 1"
      pattern: "SELECT model_used"
    - from: "18-UAT.md"
      to: "src/app/api/companies/[id]/analyze/route.ts"
      via: "201 response modelUsed/usedFallback + createRun persist"
      pattern: "usedFallback"
---

<objective>
Run the live-browser VER-03 UAT on local dev (`npm run dev`, staff Clerk account, real keys from `.env.local` — the 17-UAT precedent): Settings → pick primary → save → run Analyze on a company → assert the durable `agent_run.model_used` equals the saved primary. Record the run in `18-UAT.md` (17-UAT.md format), absorb the 16-HUMAN-UAT 2 pending items and the 17-03 deferred `<human-check>`, and author the phase-gate evidence in `18-VERIFICATION.md` including the SC-3 satisfied-by-extension disposition (D-18-02).

Purpose: VER-03 is the milestone's core acceptance test — "settings actually consumed" (Pitfall 10 / checklist item 1). This plan proves the end-to-end loop against live Postgres; it deliberately does NOT build a forced-fail mechanism (D-18-02 — the fallback proof stays in Vitest from plan 01).
Output: `18-UAT.md` (numbered live tests with pass/fail verdicts + Postgres row evidence) and `18-VERIFICATION.md` (phase-gate evidence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 18 — VER-03, SC-3)
@.planning/phases/18-verification-gate/18-CONTEXT.md (D-18-02 — no forced-fail; D-18-04 absorption)
@.planning/phases/18-verification-gate/18-RESEARCH.md (Pitfall 5 — usedFallback response-only; Pitfall 6 — SC-3 disposition; Assumptions A2/A3 — keys + DB write path; Open Question 4 — local dev precedent)
@.planning/phases/18-verification-gate/18-PATTERNS.md (18-UAT.md format lines 199-243; 18-VERIFICATION.md format lines 247-282)
@.planning/phases/17-settings-ui-list-source/17-UAT.md (6-test precedent — exact format to replicate)
@.planning/phases/16-failover-orchestration/16-HUMAN-UAT.md (2 pending items to absorb: status strip :15-18, audit trail :20-26)
@.planning/phases/17-settings-ui-list-source/17-03-PLAN.md (deferred <human-check> at :188)
@.planning/phases/18-verification-gate/18-01-SUMMARY.md (test evidence referenced by VERIFICATION.md)

This plan has a blocking human-verify checkpoint — `autonomous: false`.
</context>

<tasks>

<task type="auto">
  <name>Task 1 (18-02-01): Verify env keys, start dev server, prepare UAT + DB query — preflight automation</name>
  <files>.planning/phases/18-verification-gate/18-UAT.md (skeleton), .planning/phases/18-verification-gate/18-VERIFICATION.md (skeleton)</files>
  <read_first>
    - @.planning/phases/17-settings-ui-list-source/17-UAT.md (exact artifact format to replicate: YAML frontmatter :1-7, ## Current Test, ## Tests with expected/result, ## Summary, ## Gaps)
    - @.planning/phases/16-failover-orchestration/16-HUMAN-UAT.md (the 2 pending items to absorb — status strip :15-18, audit trail :20-26)
    - @.planning/phases/17-settings-ui-list-source/17-03-PLAN.md (deferred <human-check> at :188 — settings form live check)
    - @.planning/phases/18-verification-gate/18-RESEARCH.md lines 381-384 (Open Question 4 — local dev precedent) and Assumptions A2/A3 (lines 359-360)
  </read_first>
  <action>
    Preflight — do NOT touch production code. Verify the live-run prerequisites, then scaffold the two phase artifacts:

    1. Verify `.env.local` contains the keys the live Analyze needs (presence only — never print values): `ANTHROPIC_API_KEY`, `FIRECRAWL_API_KEY`, `DATABASE_URL`. If `ANTHROPIC_API_KEY` or `FIRECRAWL_API_KEY` is missing, STOP — the UAT cannot run (analyzeCompany.ts:44 `not_configured` gate, research A2); record the blocker and resume after the human supplies keys. `TEST_DATABASE_URL` must remain UNSET (research: do not point integration tests at the live DB).
    2. Start the dev server: `npm run dev` in the background; confirm it responds (e.g., `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000` returns 200). Keep it running for the checkpoint.
    3. Pre-determine the assertion query (research/pitfall 5 — assert DB columns only, never `used_fallback`): `SELECT model_used, model_chain FROM agent_run ORDER BY id DESC LIMIT 1;` — run it via psql using the `DATABASE_URL` from `.env.local` (e.g. `psql "$DATABASE_URL" -c "..."`, sourcing the URL from the env file without echoing it). Record the current latest row (pre-UAT baseline) so the UAT can prove a NEW row was written.
    4. Scaffold `18-UAT.md` with YAML frontmatter (`status: in-progress`, `phase: 18-verification-gate`, `source: [18-CONTEXT.md, 18-RESEARCH.md, 17-UAT.md]`, timestamps) and the numbered test rows (expected only, result: pending):
       1. Settings nav + page renders (config/empty state) — absorbed 17-03 `<human-check>`
       2. Primary picker shows only servable models (Claude Sonnet 4.6, cost caption)
       3. Save lifecycle + persistence (Save → "Saved." → reload reflects saved primary) — absorbed 17-03
       4. Run Analyze → status strip renders (16-HUMAN-UAT item 1: normal run shows 'Analysis complete')
       5. `agent_run.model_used` == saved primary (Pitfall 10 core acceptance; 16-HUMAN-UAT item 2 audit trail)
       6. Model list on `/settings` has zero non-servable rows (opencode/, gpt-*, gemini-*) — checklist item 3
    5. Scaffold `18-VERIFICATION.md` with YAML frontmatter per 18-PATTERNS.md:247-282 (phase, verified, status, score, overrides_applied: 0, human_verification items) — Observable Truths table + Requirements Coverage + Behavioral Spot-Checks sections left pending for Task 3.

    The dev server must be running and the pre-UAT baseline row captured BEFORE the checkpoint. Do not skip step 1 if keys are missing.
  </action>
  <acceptance_criteria>
    - `grep -cE '^(ANTHROPIC_API_KEY|FIRECRAWL_API_KEY|DATABASE_URL)=' .env.local` returns 3 (presence only — values never printed).
    - `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000` returns 200.
    - The pre-UAT `SELECT model_used, model_chain FROM agent_run ORDER BY id DESC LIMIT 1` executed successfully and its output is captured in the task notes (baseline row).
    - `18-UAT.md` exists with 6 numbered test rows (expected populated, result: pending) and YAML frontmatter.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE '^(ANTHROPIC_API_KEY|FIRECRAWL_API_KEY|DATABASE_URL)=' .env.local</automated>
    <automated>curl -s -o /dev/null -w '%{http_code}' http://localhost:3000</automated>
    <automated>grep -c "result: pending" .planning/phases/18-verification-gate/18-UAT.md</automated>
  </verify>
  <done>Dev server up with real keys, baseline agent_run row captured, 18-UAT.md + 18-VERIFICATION.md scaffolds ready for the checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (18-02-02): Live-browser UAT — settings → Analyze → model_used equals saved primary</name>
  <read_first>
    - @.planning/phases/18-verification-gate/18-PATTERNS.md lines 199-243 (18-UAT.md format the results will be recorded into)
    - @.planning/phases/18-verification-gate/18-RESEARCH.md lines 237-247 (Pitfall 5 usedFallback nuance; Pitfall 6 SC-3 disposition) and Assumptions A2/A3 (lines 359-360)
  </read_first>
  <files>http://localhost:3000 (live dev server), .planning/phases/18-verification-gate/18-UAT.md (results recorded by executor post-checkpoint)</files>
  <action>
    The executor has completed the preflight (Task 1): dev server running at localhost:3000 with real keys, 18-UAT.md scaffolded with 6 expected rows, Postgres agent_run baseline captured. The human now performs the browser flow described in <how-to-verify> (sign in, /settings render, servable-only picker, save lifecycle, Analyze on a company, status strip). After the human reports, the executor runs the Postgres assertion and records verdicts (Task 3).
  </action>
  <acceptance_criteria>
    - The human reports "approved" (all 6 how-to-verify steps observed as expected).
    - Post-checkpoint, `18-UAT.md` has all 6 rows with non-pending results (executor task 3 completes this).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "result: pass" .planning/phases/18-verification-gate/18-UAT.md</automated>
  </verify>
  <done>Human confirmed the live browser flow end-to-end; executor records the Postgres evidence in the follow-up task.</done>
  <what-built>
    Local dev server at http://localhost:3000 with real keys from .env.local (Task 1 preflight complete); `18-UAT.md` scaffolded with 6 numbered expected rows; the Postgres `agent_run` table ready for the post-run assertion.
  </what-built>
  <how-to-verify>
    1. Open http://localhost:3000 in a browser and sign in with the staff Clerk account (same account used in 17-UAT precedent).
    2. Navigate to Settings (sidebar Manage group, "Settings" item). Confirm: h1 "Settings", "AI Model Configuration" card, and either the saved config or the "No model configuration saved" callout prefilled with the default primary (Claude Sonnet 4.6). (17-03 human-check item 1)
    3. Confirm the primary picker lists only servable models — today exactly "Claude Sonnet 4.6" with a cost caption like "Claude Sonnet 4.6 · $3 / $15 per MTok". No dated, no opencode/ rows. (checklist item 3 / 17-03)
    4. Pick primary = Claude Sonnet 4.6 (the only servable model), click Save. Confirm inline "Saved." appears, then RELOAD the page and confirm the saved primary is still selected. (17-03 human-check save lifecycle)
    5. Open any Company detail page (e.g. a seeded company from the Companies explorer), click Analyze, wait for the run to complete (may take up to ~60s). Confirm the status strip renders — normal run shows exactly 'Analysis complete' (16-HUMAN-UAT item 1; a fallback run would show 'Analysis complete — ran on Claude Sonnet 4.6 (fallback)' but with a single servable model no fallback is expected).
    6. Tell the executor "done" — the executor will run the Postgres assertion (18-UAT.md test 5: `agent_run.model_used` == the saved primary) and the audit-trail checks (16-HUMAN-UAT item 2).
    Expected outcome for every step: the behavior above is observed exactly. Report "approved" if all 6 steps pass, or describe the failing step(s).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe the failing step and observed behavior.</resume-signal>
</task>

<task type="auto">
  <name>Task 3 (18-02-03): Run Postgres assertion, record verdicts into 18-UAT.md, author 18-VERIFICATION.md</name>
  <files>.planning/phases/18-verification-gate/18-UAT.md, .planning/phases/18-verification-gate/18-VERIFICATION.md, .planning/phases/16-failover-orchestration/16-HUMAN-UAT.md (close-out note)</files>
  <read_first>
    - .planning/phases/18-verification-gate/18-UAT.md (the scaffolded artifact being completed)
    - .planning/phases/18-verification-gate/18-RESEARCH.md Pitfall 5 (lines 237-241 — assert model_used/model_chain only), Pitfall 6 (lines 243-247 — SC-3 disposition wording), Validation Layer Map L2/L4 (lines 423-425)
    - @.planning/phases/18-verification-gate/18-01-SUMMARY.md (the Vitest forced-fail evidence the SC-3 disposition cites)
    - .planning/phases/16-failover-orchestration/16-HUMAN-UAT.md (pending items being closed)
  </read_first>
  <action>
    Record the live run — after the human checkpoint approves:

    1. Run the Postgres assertion (immediately after the Analyze completed in the checkpoint): `SELECT model_used, model_chain FROM agent_run ORDER BY id DESC LIMIT 1;` — assert `model_used` equals the saved primary (`claude-sonnet-4-6`) and `model_chain` contains that id. This is a NEW row vs the pre-UAT baseline from Task 1 (a run performed mid-UAT).
    2. Complete `18-UAT.md`: set `status: complete`, fill each of the 6 rows with `result: pass`/`fail` + the observed evidence (exact status-strip text, the model_used/model_chain row output), add `## Summary` (total/passed/issues/pending/skipped), `## Gaps`. For test 5 cite the actual Postgres row. Do NOT reference a `used_fallback` column — if a fallback served naturally, assert `model_used` == the fallback id present in `model_chain`.
    3. Author `18-VERIFICATION.md` (17-VERIFICATION.md format per 18-PATTERNS.md:247-282): YAML frontmatter with `human_verification` items; `## Goal Achievement` → `### Observable Truths` table (4 rows: settings consumed, raw provider IDs, only usable models, audit trail); `### Requirements Coverage` (VER-03 ✓ + cross-refs to VER-01/02/04); `### Behavioral Spot-Checks` (npm test, tsc, grep — cite plan 01 + plan 03); `### Human Verification Required`; `### Deferred Items` — the 16-HUMAN-UAT 2 items + 17-03 `<human-check>` listed as absorbed/closed by this UAT.
    4. **Mandatory SC-3 disposition** (research Pitfall 6 — without it ROADMAP:146 reads unmet): record verbatim in 18-VERIFICATION.md — "SC-3 forced-fail clause satisfied-by-extension via runAgent.test.ts RetryError-404 + exhaustion tests (D-18-02)". No fail hook was added; the Vitest loop tests from plan 01 ARE the forced-fail evidence.
    5. Add a close-out note to 16-HUMAN-UAT.md marking its 2 pending items as absorbed into 18-UAT.md (do not rewrite the file's history — append a dated note).
  </action>
  <acceptance_criteria>
    - `18-UAT.md` has `status: complete`, exactly 6 rows with non-pending results, and `## Summary` with totals.
    - `grep -c "result: pass" .planning/phases/18-verification-gate/18-UAT.md` returns ≥ 5 (at least the core loop passed).
    - `grep -c "satisfied-by-extension" .planning/phases/18-verification-gate/18-VERIFICATION.md` returns ≥ 1.
    - The Postgres assertion output recorded in 18-UAT.md shows `model_used` = `claude-sonnet-4-6` and `model_chain` containing it (from the actual SELECT output, not a claim).
    - 16-HUMAN-UAT.md contains a dated close-out note referencing 18-UAT.md.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "result: pass" .planning/phases/18-verification-gate/18-UAT.md</automated>
    <automated>grep -c "satisfied-by-extension" .planning/phases/18-verification-gate/18-VERIFICATION.md</automated>
    <automated>psql "$DATABASE_URL" -c "SELECT model_used, model_chain FROM agent_run ORDER BY id DESC LIMIT 1;"</automated>
  </verify>
  <done>agent_run row proves model_used == saved primary; 18-UAT.md complete with verdicts; 18-VERIFICATION.md authored with SC-3 satisfied-by-extension disposition; 16-HUMAN-UAT items closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → local dev server | Staff-authenticated session (Clerk) drives the settings/analyze flows; the UAT operates under the real staff identity |
| dev server → Postgres | Live `agent_run` writes + reads via DATABASE_URL — the durable-truth assertion surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Spoofing | agent_run userId capture | mitigate (verify) | The analyze route captures `userId` from the Clerk session only (route.ts:28, T-16-04); settings.ts never accepts a client userId (T-17-02). UAT asserts the new `agent_run` row is keyed by the session user (research security threat "client-supplied userId") |
| T-18-05 | Information Disclosure | `.env.local` / DATABASE_URL handling | mitigate | Keys verified presence-only (grep names, values never printed); psql invoked with the URL sourced from the env file without echoing; no secret value is written to 18-UAT.md or 18-VERIFICATION.md |
| T-18-06 | Tampering | test-only code leaking to prod | accept | D-18-02: this plan adds NO production code — only phase artifacts. No fail hook, no invalid-model trick; the fallback proof stays in Vitest (plan 01) |
| T-18-SC | Tampering | npm installs | accept | No packages installed (research: zero installs this phase) |
</threat_model>

<verification>
Automated (this plan): grep assertions on 18-UAT.md/18-VERIFICATION.md + the live `SELECT model_used, model_chain FROM agent_run ORDER BY id DESC LIMIT 1` via psql.
Human (this plan): the blocking checkpoint (Task 2) verifies the browser flow — settings render, servable-only picker, save lifecycle, status strip, and the Analyze run.
Cross-plan: full-suite `npm test` + `npx tsc --noEmit` remain green (plan 01 already proved; re-confirm if this plan touched any src/ file — it should not).
</verification>

<success_criteria>
- Live run proves Settings → Analyze → `agent_run.model_used` == saved primary (`claude-sonnet-4-6`), recorded with the actual Postgres row in 18-UAT.md.
- 16-HUMAN-UAT 2 pending items (status strip, audit trail) and 17-03 `<human-check>` are absorbed and closed.
- 18-VERIFICATION.md exists with the SC-3 satisfied-by-extension disposition (D-18-02) and human_verification items.
- Zero production code changes (D-18-02).
</success_criteria>

<output>
Create `.planning/phases/18-verification-gate/18-02-SUMMARY.md` when done
</output>
