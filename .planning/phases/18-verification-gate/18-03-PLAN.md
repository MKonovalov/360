---
phase: 18-verification-gate
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .planning/phases/18-verification-gate/18-VERIFICATION.md
autonomous: false
requirements: [VER-04]
user_setup:
  - service: vercel
    why: "CLI fallback for the preview deployment ONLY if the Vercel GitHub integration does not auto-build a preview for the PR (research assumption A1 — dashboard config not repo-verifiable)"
    env_vars:
      - name: VERCEL_TOKEN
        source: "Vercel Dashboard -> Account Settings -> Tokens (only needed if the PR preview URL does not appear within ~2 minutes)"
    dashboard_config:
      - task: "Confirm the GitHub integration is installed on MKonovalov/360 (Vercel Dashboard -> Settings -> Git)"
        location: "Vercel Dashboard -> project 360-arclumen -> Settings -> Git"
        note: "If the integration is missing, run the vercel-cli-with-tokens skill for the CLI fallback (D-18-03 permits this nudge)"

must_haves:
  truths:
    - "A Vercel preview URL exists for the phase branch PR and /settings renders from the committed catalog.json with no 500, no empty list, and no opencode/ leakage"
    - "The preview prompts Clerk sign-in for anonymous visitors and shows no staff data (V4 access control)"
    - "The zero-hit grep gate (exec|spawn|child_process in src/) returns 0 hits and is recorded"
    - "The phase-gate evidence (18-VERIFICATION.md) is complete with the preview URL + grep output + full-suite green"
  artifacts:
    - path: ".planning/phases/18-verification-gate/18-VERIFICATION.md"
      provides: "VER-04 evidence rows: preview URL, /settings render result, grep gate output"
      contains: "grep|preview"
  key_links:
    - from: "Vercel preview URL"
      to: "/settings"
      via: "staff browser session (Clerk sign-in)"
      pattern: "claude-sonnet-4-6"
    - from: "src/"
      to: "grep gate"
      via: "grep -rE \"node:child_process|execFileSync\\(|execSync\\(|spawnSync\\(|spawn\\(\" src/"
      pattern: "0 hits"
---

<objective>
Open the phase-branch PR against main, obtain the Vercel auto-preview URL (CLI fallback via `vercel-cli-with-tokens` skill if the GitHub integration is absent — D-18-03), verify `/settings` on the preview renders the committed catalog.json model list with no 500, no empty state, and zero `opencode/` leakage, verify the preview is auth-gated (Clerk sign-in prompt, no anonymous staff data), and record the zero-hit `exec|spawn|child_process` grep gate into the phase evidence.

Purpose: VER-04 is the deployed-environment proof that the app runs with zero local opencode — the committed snapshot is the sole model-list source (checklist items 4 and 11). This is the final plan of v1.3.
Output: PR URL + verified preview URL + grep-gate output appended to `18-VERIFICATION.md` (authored in plan 02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 18 — VER-04, SC-4)
@.planning/phases/18-verification-gate/18-CONTEXT.md (D-18-03 — PR auto-preview, no local vercel CLI primary, no --prebuilt)
@.planning/phases/18-verification-gate/18-RESEARCH.md (VER-04 requirement row :56; A1 assumption :358; Environment Availability vercel CLI 54.0.0 :399; Pitfall 7 stale-CLAUDE.md note :249-253 — ignore nodejs20.x references)
@.planning/phases/18-verification-gate/18-VERIFICATION.md (being completed — plan 02 authored the UAT/SC-3 rows)
@.vercel/project.json (project 360-arclumen — prj_DbEzimzON9nzF7Nmk7Nueta7k00V, Node 24.x — do NOT pin a Node 20 runtime)

Zero production code changes this plan — the preview verifies the SHIPPED code from phases 15-17 + the test additions from plan 01. The grep gate is the ASVS V7 check (research Security Domain :468).
</context>

<tasks>

<task type="auto">
  <name>Task 1 (18-03-01): Run grep gate, push phase branch, open PR, obtain preview URL</name>
  <files>.planning/phases/18-verification-gate/18-VERIFICATION.md (VER-04 evidence rows)</files>
  <read_first>
    - @.planning/phases/18-verification-gate/18-RESEARCH.md lines 358-360 (A1 — GitHub integration assumption + CLI fallback trigger) and line 66 (VER-04 zero-hit grep command)
    - @.planning/phases/18-verification-gate/18-PATTERNS.md lines 317-323 (the exact grep gate command — do NOT use a sloppy variant; comparability across phases)
    - @.planning/phases/15-model-registry-foundation-persistence/15-VERIFICATION.md (Truth 8 — the authoritative grep command source)
    - .vercel/project.json (project linkage — do not modify)
  </read_first>
  <action>
    Deploy-proof preflight + PR — zero production code changes:

    1. Run the EXACT zero-hit grep gate and capture the output:
       `grep -rE "node:child_process|execFileSync\(|execSync\(|spawnSync\(|spawn\(" src/`
       Expected result: 0 hits (re-verified 0 during research). If ANY hit appears, STOP and report — the gate fails and the PR must not open.
    2. Run the full-suite gate: `npm test` (≈293 tests) and `npx tsc --noEmit` — both must exit 0 (checklist item 12).
    3. Create the phase branch (e.g. `chore/18-verification-gate`) from main if not already branched, commit all phase work (plan 01 test additions + 18-VER-01-MATRIX.md + 18-UAT.md + 18-VERIFICATION.md) with a conventional commit, and push it.
    4. Open the PR against main via `gh pr create --base main --head chore/18-verification-gate` (gh 2.96.0 installed). Record the PR URL.
    5. Poll for the Vercel preview URL: check the PR for the Vercel bot deployment status (`gh pr view <n> --json statusCheckRollup,url` or the Vercel comment) for up to ~2 minutes. If no preview URL appears, use the `vercel-cli-with-tokens` skill to deploy a preview from the branch (CLI 54.0.0 installed; D-18-03 allows this nudge). Record whichever URL is obtained.
    6. Append the VER-04 evidence rows to 18-VERIFICATION.md: PR URL, preview URL, grep-gate command + 0-hit output, npm test + tsc exit codes.

    Do NOT use `vercel deploy --prebuilt` (D-18-03 rejects the --prebuilt flow); the PR auto-preview is primary, CLI is fallback only.
  </action>
  <acceptance_criteria>
    - `grep -rE "node:child_process|execFileSync\(|execSync\(|spawnSync\(|spawn\(" src/` returns 0 hits (output captured).
    - `npm test` exits 0 and `npx tsc --noEmit` exits 0.
    - `gh pr view` (the created PR) returns the PR URL; a Vercel preview URL is recorded (from the PR bot or the CLI fallback).
    - `grep -c "preview" .planning/phases/18-verification-gate/18-VERIFICATION.md` returns ≥ 1 (evidence row exists).
  </acceptance_criteria>
  <verify>
    <automated>grep -rE "node:child_process|execFileSync\(|execSync\(|spawnSync\(|spawn\(" src/</automated>
    <automated>npm test</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -c "preview" .planning/phases/18-verification-gate/18-VERIFICATION.md</automated>
  </verify>
  <done>Grep gate 0 hits; full suite + tsc green; PR open against main; preview URL obtained and recorded in 18-VERIFICATION.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (18-03-02): Verify /settings on the preview URL — no 500, no empty, no opencode/ leakage, auth-gated</name>
  <read_first>
    - @.planning/phases/18-verification-gate/18-RESEARCH.md lines 358-360 (A1 — preview URL source) and lines 471-477 (Known Threat Patterns — unauthenticated preview V4 check)
    - @.planning/phases/18-verification-gate/18-CONTEXT.md (D-18-03 — preview proves the committed-snapshot render)
  </read_first>
  <files>Vercel preview URL (from Task 1), browser session (private window)</files>
  <action>
    The executor has pushed the phase branch and opened the PR; a preview URL is recorded (Task 1). The human now performs the browser verification described in <how-to-verify> (anonymous auth-gate check, sign-in, /settings render from committed catalog.json, no 500/empty/opencode/ rows, one extra route sanity check). After the human reports, the executor finalizes 18-VERIFICATION.md (Task 3).
  </action>
  <acceptance_criteria>
    - The human reports "approved" (all 5 how-to-verify steps observed as expected).
    - Post-checkpoint, `18-VERIFICATION.md` contains the preview URL evidence and final verdict (executor task 3 completes this).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "VER-04" .planning/phases/18-verification-gate/18-VERIFICATION.md</automated>
  </verify>
  <done>Human confirmed the preview renders from the committed snapshot with zero leakage and is auth-gated; executor records the evidence in the follow-up task.</done>
  <what-built>
    A live Vercel preview deployment of the phase branch (PR # against main, or CLI-fallback preview) rendering the app with the committed `src/lib/models/catalog.json` (1131 models) as the sole model-list source.
  </what-built>
  <how-to-verify>
    1. Open the preview URL (from Task 1) in a PRIVATE browser window (no existing Clerk session).
    2. Anonymous check (V4 access control): confirm visiting the preview root redirects to the Clerk sign-in page — NO company data, no model settings, no staff-only content renders anonymously. (Research security threat: "unauthenticated preview URL exposing staff data")
    3. Sign in with the staff Clerk account. Navigate to /settings.
    4. Model-list check (VER-04 core): confirm the primary picker renders from the committed snapshot — exactly "Claude Sonnet 4.6" with a cost caption. No 500 error page, no empty list/empty state, and NO rows containing `opencode/`, `gpt-`, or `gemini-`.
    5. Sanity check one more route (e.g. the Companies explorer) loads without error on the preview.
    Expected outcome: steps 2-5 all behave as described. Report "approved" or describe the failing step.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe the failing step and what was observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3 (18-03-03): Finalize 18-VERIFICATION.md — complete phase-gate evidence</name>
  <files>.planning/phases/18-verification-gate/18-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/18-verification-gate/18-VERIFICATION.md (the artifact being completed — plan 02 authored the UAT/SC-3 rows)
    - @.planning/phases/18-verification-gate/18-RESEARCH.md Validation Layer Map (lines 421-425 — L3 deployed evidence requirements)
    - @.planning/phases/18-verification-gate/18-PATTERNS.md lines 247-282 (18-VERIFICATION.md format)
  </read_first>
  <action>
    Complete the phase-gate evidence after the human checkpoint approves the preview:

    1. Update `18-VERIFICATION.md` with the final verdicts:
       - Set `status: passed` (or `human_needed` with the specific gap listed), `score: N/N` must-haves verified, `verified` timestamp.
       - Fill the Observable Truths rows for VER-04: (a) preview URL renders /settings from committed catalog.json — no 500, no empty, no opencode/ leakage; (b) anonymous visit prompts Clerk sign-in, no staff data; (c) grep gate 0 hits; (d) full suite + tsc green.
       - Update the Requirements Coverage section: VER-01 ✓ (plan 01), VER-02 ✓ (plan 01), VER-03 ✓ (plan 02), VER-04 ✓ (this plan).
       - Add the Behavioral Spot-Checks section with the exact grep command output (0 hits), npm test exit 0, npx tsc --noEmit exit 0, and the preview URL.
    2. Add a `### Dispositions` block recording: (a) SC-3 satisfied-by-extension (already present from plan 02 — keep verbatim); (b) A1 assumption outcome — whether the GitHub integration auto-built the preview or the CLI fallback was used; (c) the grep gate ASVS V7 disposition (0 hits — V7 satisfied).
    3. Do NOT add new content beyond evidence — the 13-item checklist mapping lives in 18-VER-01-MATRIX.md; VERIFICATION.md references it.
  </action>
  <acceptance_criteria>
    - `grep -c "VER-04" .planning/phases/18-verification-gate/18-VERIFICATION.md` returns ≥ 1 (VER-04 coverage row present).
    - `grep -c "0 hits" .planning/phases/18-verification-gate/18-VERIFICATION.md` returns ≥ 1 (grep gate recorded).
    - `grep -c "status: passed" .planning/phases/18-verification-gate/18-VERIFICATION.md` returns 1 (final verdict set).
    - The preview URL appears in the artifact (`grep -c "https://" .planning/phases/18-verification-gate/18-VERIFICATION.md` ≥ 1).
    - `grep -c "satisfied-by-extension" .planning/phases/18-verification-gate/18-VERIFICATION.md` still returns ≥ 1 (plan 02 disposition preserved).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "VER-04" .planning/phases/18-verification-gate/18-VERIFICATION.md</automated>
    <automated>grep -c "0 hits" .planning/phases/18-verification-gate/18-VERIFICATION.md</automated>
    <automated>grep -c "status: passed" .planning/phases/18-verification-gate/18-VERIFICATION.md</automated>
  </verify>
  <done>18-VERIFICATION.md complete: VER-01..04 all covered, preview URL + 0-hit grep evidence, SC-3 disposition preserved, status: passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor → preview URL | Untrusted, unauthenticated traffic to the deployed preview |
| preview app → committed catalog.json | The model list is sourced solely from the committed snapshot — no runtime opencode |
| src/ → Vercel build | The built artifact must contain no subprocess surface (V7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Information Disclosure | preview URL (unauthenticated) | mitigate (verify) | V4 access control: /settings and the analyze route call `requireStaffAccess()` first (page.tsx:15, route.ts:28). The checkpoint verifies an anonymous visit redirects to Clerk sign-in with NO staff data rendered (research threat "unauthenticated preview exposing staff data") |
| T-18-08 | Tampering | Vercel build / runtime opencode | mitigate (verify) | VER-04's core proof: /settings renders from the committed catalog.json (no 500, no empty, no `opencode/` rows); the zero-hit grep gate (V7, exact command) proves no subprocess surface ships in src/ (checklist items 4 and 11) |
| T-18-09 | Tampering | preview URL leakage of secrets | accept | The preview renders public-static content only behind the same Clerk auth as prod; no env secrets are exposed to the client (PUBLIC_ prefix convention already enforced); no new secrets added this phase |
| T-18-SC | Tampering | npm installs | accept | No packages installed this plan (vercel CLI 54.0.0 already installed; gh 2.96.0 already installed) — no install task, no slopcheck checkpoint |
</threat_model>

<verification>
Automated (this plan): zero-hit grep gate (exact command), `npm test` full suite, `npx tsc --noEmit`, and grep assertions on 18-VERIFICATION.md.
Human (this plan): the blocking checkpoint (Task 2) verifies the deployed preview — anonymous auth-gate behavior and the /settings model-list render on the preview URL.
</verification>

<success_criteria>
- PR open against main with a reachable Vercel preview URL (auto-preview primary; CLI fallback only if the GitHub integration is absent — D-18-03).
- /settings on the preview renders exactly the servable allowlist (Claude Sonnet 4.6) from the committed snapshot — no 500, no empty state, no opencode//gpt-/gemini- rows.
- Anonymous visits to the preview prompt Clerk sign-in with no staff data exposed.
- Zero-hit `exec|spawn|child_process` grep in src/ recorded; full suite + tsc green.
- 18-VERIFICATION.md complete with VER-01..04 coverage, preview URL evidence, and the preserved SC-3 satisfied-by-extension disposition.
</success_criteria>

<output>
Create `.planning/phases/18-verification-gate/18-03-SUMMARY.md` when done
</output>
