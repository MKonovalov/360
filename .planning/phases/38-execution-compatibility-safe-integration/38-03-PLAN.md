---
phase: 38-execution-compatibility-safe-integration
plan: 03
type: execute
wave: 2
depends_on: [38-02]
files_modified:
  - src/components/analysis/analysisLauncherClient.ts
  - src/components/analysis/AnalysisLauncher.tsx
  - src/components/analysis/AnalysisLauncher.test.tsx
  - src/components/analysis/analysisLauncherClient.test.ts
autonomous: true
requirements: [VAL-02, VAL-03, RUN-01]
requirements_addressed: [VAL-02, VAL-03, RUN-01]
must_haves:
  truths:
    - "The launch dialog chooses Practice Area first, then shows fixed v1.7 plus matching active custom agents."
    - "The fixed v1.7 option is the default, and multiple custom matches remain explicit staff choices rather than an automatic ranking."
    - "The browser submits only opaque fixed/custom selection and subject/Practice Area identity; it never submits instructions, schema, capabilities, actor, model chain, provider, tool, budget, or policy."
  artifacts:
    - path: "src/components/analysis/analysisLauncherClient.ts"
      provides: "Strict client response parser and opaque fixed/custom payload builder"
    - path: "src/components/analysis/AnalysisLauncher.tsx"
      provides: "Practice Area-first fixed/custom picker and existing poll/status handoff"
    - path: "src/components/analysis/AnalysisLauncher.test.tsx"
      provides: "UI regression coverage for defaulting, explicit choice, and fixed omission"
  key_links:
    - from: "src/components/analysis/AnalysisLauncher.tsx"
      to: "/api/analysis-options and /api/analysis-preview"
      via: "abort/generation-safe fetch flow"
      pattern: "fetchAnalysisOptions|fetchAnalysisPreview"
    - from: "src/components/analysis/analysisLauncherClient.ts"
      to: "/api/analysis-runs"
      via: "createAnalysisRunPayload"
      pattern: "selection"
---

<objective>
Adapt the existing target-scoped launcher to the locked Practice Area-first selection experience without changing its abort, generation, preview, polling, refresh, or scalar run-status behavior.

Purpose: Implement D-38-01 through D-38-06 at the user boundary while keeping all trust decisions on the server and preserving the fixed v1.7 experience when no custom selection is made.
Output: Client contract/parser, launcher UI ordering and selection state, and deterministic component/client regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/38-execution-compatibility-safe-integration/38-CONTEXT.md
@.planning/phases/38-execution-compatibility-safe-integration/38-RESEARCH.md lines 127-153 and 286-290
@.planning/phases/38-execution-compatibility-safe-integration/38-PATTERNS.md lines 139-173 and 286-297
@src/components/analysis/AnalysisLauncher.tsx
@src/components/analysis/analysisLauncherClient.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace fixed-only client payload with strict opaque selection</name>
  <files>src/components/analysis/analysisLauncherClient.ts, src/components/analysis/analysisLauncherClient.test.ts</files>
  <read_first>src/components/analysis/analysisLauncherClient.ts; src/components/analysis/analysisLauncherClient.test.ts; src/lib/analysis/experienceContracts.ts; src/app/api/analysis-runs/route.test.ts</read_first>
  <action>Extend the client parser to model the two-step options contract from 38-02: `fetchAnalysisOptions(subjectType, undefined, signal)` parses the initial `{ practiceAreas }` response, and `fetchAnalysisOptions(subjectType, practiceAreaId, signal)` sends the follow-up query containing both `subjectType` and `practiceAreaId` and parses `{ practiceAreas, agents }`. Build a strict discriminated launch payload from the selected agent. Fixed selection must preserve the existing templateVersionId request path; custom selection must carry only the opaque custom identity and selected immutable version identifier required by the server contract, plus subject and Practice Area IDs. Reject or strip all instruction, research query, output schema, capability, actor, effort override, model chain, budget, policy, provider, tool, credential, and arbitrary data-source fields. Preserve readJson, safe error-copy, positive-ID, and polling response behavior. Add client parser tests for initial/follow-up URLs, response variants, fixed omission compatibility, valid custom identity/version, malformed options, multiple matches, and all forbidden fields.</action>
  <verify><automated>npm test -- --run src/components/analysis/analysisLauncherClient.test.ts</automated></verify>
  <acceptance_criteria>Client payload tests show fixed requests remain compatible and custom requests contain opaque identity/version only; every forbidden field is rejected or absent before fetch.</acceptance_criteria>
  <done>Client contracts submit only opaque fixed/custom selection data and preserve fixed omission behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Make the launcher Practice Area-first with fixed default and explicit custom choice</name>
  <files>src/components/analysis/AnalysisLauncher.tsx, src/components/analysis/AnalysisLauncher.test.tsx</files>
  <read_first>src/components/analysis/AnalysisLauncher.tsx; src/components/analysis/AnalysisLauncher.test.tsx; src/components/analysis/analysisLauncherClient.ts; .planning/phases/35-company-persona-analysis-experiences/35-CONTEXT.md lines 32-44</read_first>
  <action>Keep the existing dialog, generation counters, AbortControllers, preview reload, launch abort handling, router.refresh, and scalar applicationRunId polling. On open/subject change, call the initial `fetchAnalysisOptions(subjectType, undefined, signal)` and render only the Practice Area selector. When `practiceAreaId` changes, abort the prior options/preview requests, call the follow-up `fetchAnalysisOptions(subjectType, practiceAreaId, signal)`, and only then render the agent picker from its server-projected `agents` response. Render the fixed v1.7 option first and default it after the follow-up response; render every active custom match separately with immutable version metadata. Never auto-select a custom match. Add effect/component tests asserting the initial request has only subjectType, the follow-up contains subjectType plus practiceAreaId, no agent picker appears before the follow-up response, fixed-first defaulting, explicit multiple-custom choice, and preview/launch payload wiring. If fixed is selected or custom is absent, preserve existing fixed behavior exactly per D-38-03 and D-38-06.</action>
  <verify><automated>npm test -- --run src/components/analysis/AnalysisLauncher.test.tsx</automated></verify>
  <acceptance_criteria>Static/component tests prove Practice Area appears before agent picker, fixed is first/default, multiple custom options require explicit selection, and the existing status/polling path remains connected through the scalar run ID.</acceptance_criteria>
  <done>Launcher ordering, fixed default, explicit custom choice, and existing polling handoff pass regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Run launcher regression and project build gate</name>
  <files>src/components/analysis/AnalysisLauncher.tsx, src/components/analysis/analysisLauncherClient.ts, src/components/analysis/AnalysisLauncher.test.tsx, src/components/analysis/analysisLauncherClient.test.ts</files>
  <read_first>src/components/analysis/AnalysisLauncher.tsx; src/components/analysis/analysisLauncherClient.ts; src/components/analysis/AnalysisLauncher.test.tsx; src/components/analysis/analysisLauncherClient.test.ts</read_first>
  <action>Run the complete launcher/client focused matrix and inspect the diff for accidental new client authority, template-picker regressions outside the locked custom picker, or direct execution/review/candidate/provider imports. Do not add a browser E2E suite here; authenticated Company/Persona custom launch and settled result/review/candidate proof remain Phase 39 responsibilities.</action>
  <verify><automated>npm test -- --run src/components/analysis/AnalysisLauncher.test.tsx src/components/analysis/analysisLauncherClient.test.ts && npm run build</automated></verify>
  <acceptance_criteria>Focused launcher tests and the existing project build pass; no client-side authored configuration or second execution path is introduced; Phase 39 handoff remains explicit.</acceptance_criteria>
  <done>Launcher/client integration is build-green and contains no client authority or second executor.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| server option projection → browser | Display metadata is untrusted UI state, not authorization. |
| browser → preview/launch request | Only opaque selection and target identifiers cross the boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-38-07 | Tampering | launcher payload | mitigate | strict parser, opaque identity/version only, server re-resolution |
| T-38-08 | Elevation | custom option selection | mitigate | target/Practice Area filtering is server-owned and multiple matches are explicit |
| T-38-09 | Denial of service | async launcher requests | mitigate | preserve AbortController/generation guards and scalar polling |
| T-38-SC | Tampering | package installs | accept | no packages are added |
</threat_model>

<verification>
Use targeted Vitest after each task and `npm run build` at completion. Visual/authenticated Company and Persona proof is not claimed here; Phase 39 owns it and missing Clerk credentials remain blocked/not-run.
</verification>

<success_criteria>
Staff-facing launch order and choice semantics match the locked decisions, fixed behavior remains compatible, and the client cannot author execution configuration.
</success_criteria>

<output>
Create `.planning/phases/38-execution-compatibility-safe-integration/38-03-SUMMARY.md` when done.
</output>
