---
phase: 38-execution-compatibility-safe-integration
plan: 05
type: execute
wave: 3
depends_on: [38-01, 38-02, 38-03]
files_modified:
  - src/workflows/analysisRun.ts
  - src/lib/db/queries/analysisResults.ts
  - src/lib/db/queries/analysisResults.test.ts
  - src/lib/db/queries/analysisResults.integration.test.ts
  - src/lib/db/queries/analysisRuns.test.ts
  - src/lib/db/queries/analysisRuns.integration.test.ts
  - src/lib/verification/phase38Fixtures.ts
  - src/lib/verification/phase38Fixtures.test.ts
autonomous: true
requirements: [VER-03, RUN-01, RUN-02]
requirements_addressed: [VER-03, RUN-01, RUN-02]
user_setup:
  - service: Neon/Postgres test database
    why: "Required for authoritative snapshot persistence, duplicate, packet, and Workflow integration evidence"
    env_vars:
      - name: TEST_DATABASE_URL
        source: "Disposable non-production Neon/Postgres database connection supplied to the test shell"
must_haves:
  truths:
    - "The Workflow receives only scalar applicationRunId, reloads DB-authoritative template_snapshot.custom and execution_snapshot.customOutputSchema, and never rereads mutable custom-agent/configuration rows."
    - "Custom output travels through GroundedExecutionResult.customOutput, normalizeAnalysisPacket, and persistAnalysisPacket to analysis_run_result.raw_audit.customOutput."
    - "Fixed and custom runs use one CTE run ledger, one scalar Workflow, one grounded packet/evidence path, one whole-run review path, and one confirmed-only candidate read path."
    - "Existing duplicate semantics remain `(subject_type, subject_id, template_id)`: same-template active duplicates fail, while distinct fixed/custom templates may coexist; no global uniqueness change is made."
  artifacts:
    - path: "src/workflows/analysisRun.ts"
      provides: "DB-authoritative custom snapshot/output wiring through the scalar-ID Workflow"
      exports: ["analysisRun"]
    - path: "src/lib/db/queries/analysisResults.ts"
      provides: "Existing CTE persistence of validated custom output at raw_audit.customOutput"
      exports: ["persistAnalysisPacket"]
    - path: "src/lib/verification/phase38Fixtures.test.ts"
      provides: "Fixed/custom durable convergence, replay, duplicate, recovery, and packet-order fixtures"
  key_links:
    - from: "src/workflows/analysisRun.ts"
      to: "src/lib/analysis/execution.ts"
      via: "reload scalar run ID and pass executionSnapshot.customOutputSchema to GroundedExecutionAdapter"
      pattern: "applicationRunId|customOutputSchema|GroundedExecutionAdapter"
    - from: "src/workflows/analysisRun.ts"
      to: "src/lib/analysis/results.ts"
      via: "pass GroundedExecutionResult.customOutput into normalizeAnalysisPacket"
      pattern: "customOutput"
    - from: "src/lib/analysis/results.ts"
      to: "src/lib/db/queries/analysisResults.ts"
      via: "pass normalized customOutput into persistence and packet hash"
      pattern: "customOutput"
    - from: "src/lib/db/queries/analysisResults.ts"
      to: "analysis_run_result.raw_audit.customOutput"
      via: "existing raw_audit JSONB field in the Neon-safe CTE"
      pattern: "raw_audit|customOutput"
---

<objective>
Wire the exact custom snapshot/output contract through the existing DB-authoritative Workflow and run-result persistence. The required path is `analysis_run.template_snapshot.custom` → `analysis_run.execution_snapshot.customOutputSchema` → `analysisRun(applicationRunId)` reload → `GroundedExecutionAdapter.execute({ ..., customOutputSchema })` → `GroundedExecutionResult.customOutput` → `normalizeAnalysisPacket({ ..., customOutput })` → `persistAnalysisPacket({ ..., customOutput })` → `analysis_run_result.raw_audit.customOutput`.

Purpose: Close the checker blocker around runtime wiring while preserving D-38-09 through D-38-12, the scalar Workflow boundary, packet-before-completion, review/candidate convergence, and the existing duplicate index.
Output: Workflow/persistence wiring, deterministic fixed/custom fixtures, and prerequisite-gated Neon/Workflow integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-execution-compatibility-safe-integration/38-CONTEXT.md
@.planning/phases/38-execution-compatibility-safe-integration/38-RESEARCH.md lines 257-337 and 412-447
@.planning/phases/38-execution-compatibility-safe-integration/38-VALIDATION.md lines 59-69 and 98-113
@src/workflows/analysisRun.ts
@src/lib/db/queries/analysisResults.ts
@src/lib/db/queries/analysisRuns.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire DB-authoritative custom snapshots through the scalar Workflow</name>
  <files>src/workflows/analysisRun.ts, src/lib/verification/phase38Fixtures.ts, src/lib/verification/phase38Fixtures.test.ts</files>
  <read_first>src/workflows/analysisRun.ts; src/lib/db/queries/analysisRuns.ts; src/lib/analysis/execution.ts; src/lib/analysis/results.ts; src/lib/verification/phase36Fixtures.ts; src/lib/verification/phase36Fixtures.test.ts</read_first>
  <action>Keep `analysisRun(applicationRunId)` as the only Workflow input. After loading the run row by scalar ID, read `templateSnapshot.custom` and `executionSnapshot.customOutputSchema` from the DB-authoritative snapshots; pass the latter into the existing GroundedExecutionAdapter; pass the returned `GroundedExecutionResult.customOutput` into normalizeAnalysisPacket; and pass the normalized value into persistence. Never read mutable custom-agent rows, client data, Workflow metadata, current settings, or provider configuration during execution. Extend deterministic fixtures to assert the exact reload-to-adapter-to-normalizer chain and fixed behavior when custom snapshot fields are absent.</action>
  <verify><automated>npm test -- --run src/lib/verification/phase38Fixtures.test.ts src/lib/verification/phase36Fixtures.test.ts</automated></verify>
  <acceptance_criteria>Workflow tests show only scalar applicationRunId crosses the durable boundary, custom schema comes from the stored run snapshot, customOutput is passed by name, and fixed runs use the unchanged path.</acceptance_criteria>
  <done>DB-authoritative custom snapshot data is explicitly wired through the existing scalar-ID Workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Persist customOutput in the existing result/audit CTE</name>
  <files>src/lib/db/queries/analysisResults.ts, src/lib/db/queries/analysisResults.test.ts, src/lib/db/queries/analysisRuns.test.ts</files>
  <read_first>src/lib/db/queries/analysisResults.ts; src/lib/db/queries/analysisResults.test.ts; src/lib/db/queries/analysisRuns.ts; src/lib/db/queries/analysisRuns.test.ts; src/lib/analysis/results.ts; src/lib/analysis/groundedContracts.ts</read_first>
  <action>Extend persistAnalysisPacket with a required normalized `customOutput` transport value (null for fixed runs) and write it at the exact existing JSONB path `analysis_run_result.raw_audit.customOutput` alongside the fixed audit fields in the same data-modifying CTE. Include the canonical customOutput representation in packet-hash material so replay with changed custom output raises the existing packet-hash conflict. Keep analysis_run_result narrative/model/finding/source/link columns and the fixed GroundedPacket unchanged. Preserve one CTE, SQLSTATE 23505 active_run_exists mapping, packet-before-completion, guarded transitions, and the partial unique index `(subject_type, subject_id, template_id)`; do not add a migration, a result column, a custom run ledger, or global subject uniqueness.</action>
  <verify><automated>npm test -- --run src/lib/db/queries/analysisResults.test.ts src/lib/db/queries/analysisRuns.test.ts</automated></verify>
  <acceptance_criteria>Persistence tests assert `raw_audit.customOutput` for custom runs, null/absent custom output for fixed runs, hash-covered replay conflict, unchanged fixed packet fields, and unchanged duplicate CTE semantics.</acceptance_criteria>
  <done>Validated custom output is persisted only in existing raw_audit.customOutput JSONB with replay identity and no schema migration.</done>
</task>

<task type="auto">
  <name>Task 3: Add prerequisite-gated Neon and Workflow convergence fixtures</name>
  <files>src/lib/db/queries/analysisResults.integration.test.ts, src/lib/db/queries/analysisRuns.integration.test.ts, src/lib/verification/phase38Fixtures.test.ts</files>
  <read_first>src/lib/db/queries/analysisResults.integration.test.ts; src/lib/db/queries/analysisRuns.integration.test.ts; src/lib/verification/phase38Fixtures.test.ts; src/lib/verification/databaseIdentity.ts; src/workflows/analysisRun.ts; .planning/phases/38-execution-compatibility-safe-integration/38-VALIDATION.md lines 59-69 and 98-113</read_first>
  <action>Extend the guarded integration fixtures for custom template/version rows with `template_snapshot.custom` and `execution_snapshot.customOutputSchema`, active checklist resolution, atomic create, duplicate active custom runs, lifecycle retirement/rejection, scalar Workflow claim/reload/replay, adapter customOutput extraction, normalization, `raw_audit.customOutput` persistence, bounded failure/recovery, packet-before-completion, and fixed regression. Require TEST_DATABASE_URL for authoritative Neon/Workflow evidence; when absent, classify the command blocked/not-run and never pass. Do not use db:push as production proof. Keep live provider smoke optional/not-run with policy_or_credentials_unavailable and leave Phase 39 browser/adversarial/review/no-live-write proof out of this plan.</action>
  <verify><automated>npm run test:workflow</automated></verify>
  <acceptance_criteria>With TEST_DATABASE_URL, integration fixtures pass for fixed/custom snapshot reload, customOutput persistence, duplicate, claim/replay/failure/recovery, and packet ordering; without it, evidence is explicitly blocked/not-run.</acceptance_criteria>
  <done>Authoritative DB/Workflow convergence is proven when prerequisites exist and never falsely claimed when absent.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| DB snapshot → Workflow | Mutable custom state must not be reread after run creation. |
| Workflow output → result persistence | Only normalized bounded customOutput crosses into raw_audit JSONB. |
| run ledger → downstream review/candidates | Existing packet/review/candidate boundaries remain authoritative. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-38-12 | Tampering | Workflow replay | mitigate | scalar run ID, DB reload, guarded transitions, immutable snapshots |
| T-38-13 | Tampering | result persistence | mitigate | bounded customOutput, packet hash, one Neon-safe CTE |
| T-38-14 | Denial of service | duplicate/retry | mitigate | preserve partial unique index and existing replay semantics |
| T-38-SC | Tampering | package installs | accept | no new dependencies |
</threat_model>

<verification>
Run targeted Workflow/persistence tests after each task. Run `npm run test:workflow` only with TEST_DATABASE_URL; otherwise record DB/Workflow evidence as blocked/not-run. Run `npm test` at the wave boundary.
</verification>

<success_criteria>
Custom snapshot/output data is DB-authoritatively carried through the existing scalar Workflow, grounded normalization, and result CTE, while fixed behavior and duplicate semantics remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/38-execution-compatibility-safe-integration/38-05-SUMMARY.md` when done.
</output>
