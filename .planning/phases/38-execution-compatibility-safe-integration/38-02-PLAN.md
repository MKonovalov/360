---
phase: 38-execution-compatibility-safe-integration
plan: 02
type: execute
wave: 1
depends_on: [38-01]
files_modified:
  - src/lib/analysis/compatibility.ts
  - src/lib/analysis/subjects.ts
  - src/lib/db/queries/customAgents.ts
  - src/lib/db/queries/customAgents.test.ts
  - src/app/api/analysis-options/route.ts
  - src/app/api/analysis-preview/route.ts
  - src/app/api/analysis-runs/route.ts
  - src/lib/analysis/compatibility.test.ts
  - src/app/api/analysis-options/route.test.ts
  - src/app/api/analysis-preview/route.test.ts
  - src/app/api/analysis-runs/route.test.ts
autonomous: true
requirements: [VAL-02, VAL-03, VAL-04, VAL-05, RUN-02]
requirements_addressed: [VAL-02, VAL-03, VAL-04, VAL-05, RUN-02]
must_haves:
  truths:
    - "Options, preview, and launch re-resolve custom identity/version, lifecycle, target, Practice Area, checklist, effort, capabilities, output policy, model chain, and execution policy on the server."
    - "Every incompatible custom request is rejected before createAnalysisRun and no queued row exists; fixed requests retain their existing allowlisted branch."
    - "The existing partial unique active-run semantics remain unchanged: duplicate fixed/custom starts are rejected per selected template identity without broadening to global subject uniqueness."
  artifacts:
    - path: "src/lib/analysis/compatibility.ts"
      provides: "Shared server compatibility resolver used by preview and authoritative launch"
      exports: ["resolveAnalysisLaunch"]
    - path: "src/app/api/analysis-runs/route.ts"
      provides: "Authoritative pre-create compatibility gate and existing durable dispatch"
      exports: ["POST"]
    - path: "src/lib/analysis/compatibility.test.ts"
      provides: "Full fixed/custom rejection and success matrix with createAnalysisRun call assertions"
  key_links:
    - from: "src/app/api/analysis-runs/route.ts"
      to: "src/lib/analysis/compatibility.ts"
      via: "server re-resolution before snapshot/create"
      pattern: "resolveAnalysisLaunch"
    - from: "src/lib/analysis/compatibility.ts"
      to: "src/lib/db/queries/customAgents.ts"
      via: "active current immutable custom version lookup"
      pattern: "customAgent"
    - from: "src/app/api/analysis-runs/route.ts"
      to: "createAnalysisRun"
      via: "single post-compatibility convergence call"
      pattern: "createAnalysisRun"
---

<objective>
Build the server-owned compatibility adapter and route gates. Practice Area-first options expose the fixed template plus separately selectable active custom identities; preview is advisory and resolves again; POST is authoritative and resolves twice/snapshots once before the existing CTE.

Purpose: Implement D-38-01 through D-38-07 and D-38-12 without widening fixed queries or trusting browser filtering. This is the only place that decides whether a custom selection is runnable.
Output: Shared resolver, narrow custom query projection, options/preview/launch integration, rejection matrix, and duplicate-boundary regression tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-execution-compatibility-safe-integration/38-CONTEXT.md
@.planning/phases/38-execution-compatibility-safe-integration/38-RESEARCH.md lines 127-153 and 229-256
@.planning/phases/38-execution-compatibility-safe-integration/38-PATTERNS.md lines 43-137 and 175-256
@.planning/phases/36-agent-management-end-to-end-verification/36-CONTEXT.md
@src/lib/analysis/subjects.ts
@src/lib/db/queries/customAgents.ts
@src/app/api/analysis-runs/route.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add server custom selection resolution and option projection</name>
  <files>src/lib/analysis/compatibility.ts, src/lib/analysis/subjects.ts, src/lib/db/queries/customAgents.ts, src/lib/db/queries/customAgents.test.ts</files>
  <read_first>src/lib/analysis/subjects.ts; src/lib/db/queries/customAgents.ts; src/lib/db/queries/customAgents.test.ts; src/lib/db/queries/analysisTemplates.ts; src/lib/analysis/checklist.ts; src/lib/analysis/capabilityPresets.ts; src/lib/analysis/customAgentContracts.ts</read_first>
  <action>Create a typed Resolution result for custom launches and a narrow server query that loads one custom identity plus the requested immutable version, fenced to kind custom, active lifecycle, current version, target type, and selected Practice Area. Expand customAgents.test.ts with query-shape assertions for the kind/status/version/target/Practice Area fences and server-safe option projection. Compose it beside the fixed resolver; never widen listActiveAnalysisTemplates or fixed version lookup. The resolver must load the real Company/Persona, active Practice Area, active target-scoped checklist, supported/default effort, allowlisted capability preset mapping, server model chain, and current execution policy. Reject malformed/stale/retired/not-current/mismatched target or Practice Area, inactive/mismatched signals, invalid effort, unknown/duplicate/incompatible capabilities, client-supplied provider/tool/policy/model fields, and invalid bounded output before returning a snapshot-ready result. Preserve multiple active matches as separate options, fixed first/default, and selected-version semantics per D-38-02 through D-38-07 and D-38-12.</action>
  <verify><automated>npm test -- --run src/lib/analysis/compatibility.test.ts src/lib/analysis/subjects.test.ts src/lib/db/queries/customAgents.test.ts</automated></verify>
  <acceptance_criteria>Resolver and customAgents query tests cover Company/Persona × fixed/custom × matching/mismatching target and Practice Area × active/retired/current/stale, and all rejection results are typed safe errors with no executor or DB run creation side effect.</acceptance_criteria>
  <done>Server compatibility resolution returns only active, current, target/Practice Area-compatible custom selections.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Practice Area-first options and advisory preview routes</name>
  <files>src/app/api/analysis-options/route.ts, src/app/api/analysis-preview/route.ts, src/app/api/analysis-options/route.test.ts, src/app/api/analysis-preview/route.test.ts</files>
  <read_first>src/app/api/analysis-options/route.ts; src/app/api/analysis-preview/route.ts; src/lib/analysis/experienceContracts.ts; src/app/api/analysis-options/route.test.ts; src/app/api/analysis-preview/route.test.ts</read_first>
  <action>Keep requireStaffAccess first and define one explicit two-step GET `/api/analysis-options` contract. The initial request is `GET /api/analysis-options?subjectType={company|persona}` and returns only active Practice Areas in `{ practiceAreas }`; after a user selects `practiceAreaId`, the follow-up is `GET /api/analysis-options?subjectType={...}&practiceAreaId={positiveId}` and returns `{ practiceAreas, agents }`, with `agents[0]` the fixed v1.7 option and remaining entries active custom options filtered server-side by target type plus selected Practice Area. Project only safe display metadata and keep fixed template queries fenced to the two canonical keys. Extend preview to accept the same strict discriminated selection, resolve the selected current immutable version and checklist server-side, and return read-only instruction, bounded output summary, capability display metadata, Practice Area, checklist, and effort; never return actor, provider, tool, budget, credentials, or mutable configuration authority. Add route tests for both GET response shapes, missing/invalid practiceAreaId, target/Practice Area filtering, fixed-first ordering, multiple custom matches, and stale/retired/mismatch preview cases.</action>
  <verify><automated>npm test -- --run src/app/api/analysis-options/route.test.ts src/app/api/analysis-preview/route.test.ts</automated></verify>
  <acceptance_criteria>Options and preview are target/Practice Area scoped, fixed-first, and server projected; forged custom configuration is rejected; preview re-resolution can become stale without creating a run; fixed tests still prove the v1.7 path.</acceptance_criteria>
  <done>Options and preview expose safe fixed/custom projections while preserving the fixed allowlist.</done>
</task>

<task type="auto">
  <name>Task 3: Make POST launch authoritative before the existing run CTE</name>
  <files>src/app/api/analysis-runs/route.ts, src/app/api/analysis-runs/route.test.ts, src/lib/analysis/compatibility.test.ts</files>
  <read_first>src/app/api/analysis-runs/route.ts; src/lib/db/queries/analysisRuns.ts; src/lib/analysis/snapshots.ts; src/workflows/analysisRun.ts; src/app/api/analysis-runs/route.test.ts</read_first>
  <action>Preserve gate-first auth, strict body parsing, and the existing fixed branch. Add the custom branch only after target and active Practice Area resolution: resolve the selected immutable version and all compatibility inputs again, build the Phase 38 snapshot once, then call the existing createAnalysisRun exactly once for both kinds and dispatch only the scalar applicationRunId to the existing Workflow. Return non-active-run 400/404/409 safe responses for every compatibility rejection and assert createAnalysisRun is not called. Preserve the existing one-CТE insert/event boundary, SQLSTATE 23505 active_run_exists mapping, and unique index keyed by subject_type/subject_id/template_id; do not broaden duplicate semantics without product authorization. Do not create a custom executor, queue, review, candidate, or provider path, per D-38-06, D-38-08, D-38-09, D-38-10, and D-38-11.</action>
  <verify><automated>npm test -- --run src/app/api/analysis-runs/route.test.ts src/lib/analysis/compatibility.test.ts src/lib/db/queries/analysisRuns.test.ts</automated></verify>
  <acceptance_criteria>Every listed compatibility rejection returns before createAnalysisRun and leaves no queued-row path; valid fixed and custom requests converge on identical create/dispatch seams; duplicate fixed and custom identity cases preserve existing active_run_exists behavior.</acceptance_criteria>
  <done>Authoritative POST launch rejects before the existing CTE and dispatches only valid fixed/custom snapshots.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Clerk staff request → route | Authentication and actor identity enter the server. |
| browser selection → resolver | Forged identity/version/target/configuration must be rejected. |
| resolver → createAnalysisRun | Only complete server-resolved snapshots may become active rows. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-38-04 | Elevation | options/preview/launch routes | mitigate | requireStaffAccess first, server-side target/status/version/Practice Area checks, actor from Clerk |
| T-38-05 | Tampering | stale preview/custom selection | mitigate | resolve twice, selected immutable version validation, launch-time checklist/policy re-resolution |
| T-38-06 | Denial of service | active-run creation | mitigate | retain partial unique index and one CTE; map only duplicate conflicts to active_run_exists |
| T-38-SC | Tampering | package installs | accept | no new dependency or package-manager task |
</threat_model>

<verification>
Run each targeted route/resolver command after its task, then `npm test`. The absence of TEST_DATABASE_URL must not be used to call route mocks or unit fixtures database proof; DB-authoritative evidence is gated in 38-05.
</verification>

<success_criteria>
Server filtering is advisory only; authoritative POST validation rejects all incompatible custom selections before run insertion, while fixed v1.7 launch behavior and duplicate semantics remain intact.
</success_criteria>

<output>
Create `.planning/phases/38-execution-compatibility-safe-integration/38-02-SUMMARY.md` when done.
</output>
