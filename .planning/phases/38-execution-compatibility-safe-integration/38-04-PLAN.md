---
phase: 38-execution-compatibility-safe-integration
plan: 04
type: execute
wave: 3
depends_on: [38-01, 38-02, 38-03]
files_modified:
  - src/lib/analysis/execution.ts
  - src/lib/analysis/groundedContracts.ts
  - src/lib/analysis/results.ts
  - src/lib/db/queries/analysisResults.ts
  - src/lib/analysis/execution.test.ts
  - src/lib/analysis/groundedContracts.test.ts
  - src/lib/analysis/results.test.ts
  - src/lib/db/queries/analysisResults.test.ts
  - src/lib/db/queries/analysisRuns.test.ts
  - src/lib/verification/phase38Fixtures.test.ts
  - src/lib/verification/phase38Fixtures.ts
autonomous: true
requirements: [VER-03, VAL-04, VAL-05, RUN-01, RUN-02]
requirements_addressed: [VER-03, VAL-04, VAL-05, RUN-01, RUN-02]
user_setup:
  - service: Neon/Postgres test database
    why: "Required for authoritative run-ledger, duplicate, packet persistence, and Workflow integration evidence"
    env_vars:
      - name: TEST_DATABASE_URL
        source: "Disposable non-production Neon/Postgres database connection supplied to the test shell"
must_haves:
  truths:
    - "Custom and fixed runs converge at one createAnalysisRun CTE, one scalar-ID Workflow, one GroundedExecutionAdapter, one immutable packet/evidence path, one whole-run review path, and one confirmed-only candidate read path."
    - "Custom output is bounded and additive; malformed output, unsafe tool results, missing policy, timeout, and provider/model failures produce the existing safe failure reasons."
    - "Duplicate active-run protection, bounded attempts/tool calls/time/spend, claim/reload, replay, packet-before-completion, and recovery apply to custom template identities exactly as fixed identities."
  artifacts:
    - path: "src/lib/analysis/execution.ts"
      provides: "Existing grounded adapter with a narrow server-built additive custom-output adapter"
      exports: ["GroundedExecutionAdapter"]
    - path: "src/lib/db/queries/analysisResults.ts"
      provides: "Existing packet/evidence CTE persistence with bounded custom output in existing JSONB"
      exports: ["persistAnalysisPacket"]
    - path: "src/lib/verification/phase38Fixtures.test.ts"
      provides: "Deterministic fixed/custom convergence, bounded failure, recovery, and replay fixtures"
  key_links:
    - from: "src/lib/analysis/execution.ts"
      to: "src/lib/analysis/groundedContracts.ts"
      via: "fixed grounded envelope validation plus additive custom output validation"
      pattern: "groundedPacketSchema"
    - from: "src/lib/db/queries/analysisResults.ts"
      to: "src/lib/db/queries/analysisRuns.ts"
      via: "packet persistence reads snapshotted checklist and run identity"
      pattern: "analysis_run"
    - from: "src/lib/verification/phase38Fixtures.test.ts"
      to: "src/workflows/analysisRun.ts"
      via: "existing scalar applicationRunId Workflow fixture seam"
      pattern: "applicationRunId"
---

<objective>
Make the existing grounded executor consume the custom snapshot adapter without creating a second executor or downstream data model. Preserve the fixed grounded packet and store only validated additive custom output in the existing result/audit JSONB boundary; keep evidence, findings, review, and candidate semantics untouched.

Purpose: Satisfy RUN-01/RUN-02 and VAL-05 by proving convergence and bounded safe behavior, not by adding a custom execution system. D-38-08 through D-38-11 remain hard regression boundaries.
Output: Narrow executor/output adapter, packet persistence compatibility, deterministic injected fixtures, and conditional DB/Workflow coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-execution-compatibility-safe-integration/38-CONTEXT.md
@.planning/phases/38-execution-compatibility-safe-integration/38-RESEARCH.md lines 243-269 and 317-337
@.planning/phases/38-execution-compatibility-safe-integration/38-VALIDATION.md lines 59-69
@src/lib/analysis/execution.ts
@src/lib/analysis/groundedContracts.ts
@src/lib/db/queries/analysisResults.ts
@src/workflows/analysisRun.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the bounded additive custom-output adapter to grounded execution</name>
  <files>src/lib/analysis/execution.ts, src/lib/analysis/groundedContracts.ts, src/lib/analysis/results.ts, src/lib/analysis/execution.test.ts, src/lib/analysis/groundedContracts.test.ts, src/lib/analysis/results.test.ts</files>
  <read_first>src/lib/analysis/execution.ts; src/lib/analysis/groundedContracts.ts; src/lib/analysis/results.ts; src/lib/analysis/execution.test.ts; src/lib/analysis/groundedContracts.test.ts; src/lib/analysis/customAgentContracts.ts</read_first>
  <action>Extend the existing GroundedExecutionAdapter only through a server-built adapter that asks for the fixed grounded envelope plus an optional bounded custom object described by the snapshotted normalized schema. Validate custom fields with the Phase 37 limits, reject nested/unbounded/reserved/collision fields, and keep model-authored URLs, signal identity, citations, sources, review state, and candidate fields non-authoritative. Persist the validated custom object only in the existing raw_audit/result JSONB shape if that shape accepts an additive field; do not add a SQL column or migration. Preserve modelFactory/instantiateChain, Firecrawl webSearch, writesAllowed false, policy short-circuit, bounded limits, safeToolResults, invalid_packet, unsafe_research_content, missing_key, timeout, and model_failure mappings. Do not change Workflow input or create another adapter boundary, per D-38-09 and D-38-10.</action>
  <verify><automated>npm test -- --run src/lib/analysis/execution.test.ts src/lib/analysis/groundedContracts.test.ts src/lib/analysis/results.test.ts</automated></verify>
  <acceptance_criteria>Tests prove valid additive custom output survives validation, every reserved server-owned collision fails closed, malformed output/tool content maps to safe failure, and fixed output/evidence behavior remains unchanged.</acceptance_criteria>
  <done>Grounded execution accepts only bounded additive custom output and preserves fixed fail-closed behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Prove one run ledger and durable convergence for fixed and custom identities</name>
  <files>src/lib/db/queries/analysisResults.ts, src/lib/db/queries/analysisResults.test.ts, src/lib/db/queries/analysisRuns.test.ts, src/lib/verification/phase38Fixtures.ts, src/lib/verification/phase38Fixtures.test.ts</files>
  <read_first>src/lib/db/queries/analysisResults.ts; src/lib/db/queries/analysisResults.test.ts; src/lib/db/queries/analysisRuns.ts; src/lib/db/queries/analysisRuns.test.ts; src/workflows/analysisRun.ts; src/lib/verification/phase36Fixtures.ts; src/lib/verification/phase36Fixtures.test.ts</read_first>
  <action>Extend deterministic fixture dependencies so fixed and custom template/version IDs both enter the same createAnalysisRun input, scalar applicationRunId Workflow, DB-authoritative snapshot reload, GroundedExecutionAdapter, packet persistence-before-completion, review reconciliation, and confirmed-candidate read seams. Assert no custom query is consulted during Workflow execution. Preserve the existing Neon-safe one data-modifying CTE, packet hash replay conflict, guarded status transitions, and unique active-run index semantics. Add the duplicate matrix for same subject/same fixed, same subject/same custom, and fixed-plus-custom concurrent identities without globalizing uniqueness. Add bounded failure, claim recovery, replay, and safe error assertions.</action>
  <verify><automated>npm test -- --run src/lib/db/queries/analysisResults.test.ts src/lib/db/queries/analysisRuns.test.ts src/lib/verification/phase38Fixtures.test.ts</automated></verify>
  <acceptance_criteria>Deterministic fixtures show fixed/custom convergence at one ledger/executor path, duplicate protection and replay are idempotent, packet persistence precedes completion, and no direct Signal/Offering/link write or second review/candidate path exists.</acceptance_criteria>
  <done>Fixed and custom fixture runs share one scalar Workflow, ledger, packet, review, and candidate-read path.</done>
</task>

<task type="auto">
  <name>Task 3: Add prerequisite-gated Neon and Workflow fixtures</name>
  <files>src/lib/verification/phase38Fixtures.ts, src/lib/verification/phase38Fixtures.test.ts, src/lib/db/queries/analysisResults.integration.test.ts, src/lib/db/queries/analysisRuns.integration.test.ts</files>
  <read_first>src/lib/verification/databaseIdentity.ts; src/lib/verification/phase36Fixtures.test.ts; src/lib/db/queries/analysisResults.integration.test.ts; src/lib/db/queries/analysisRuns.integration.test.ts; src/workflows/analysisRun.ts; .planning/phases/38-execution-compatibility-safe-integration/38-VALIDATION.md lines 59-69 and 98-113</read_first>
  <action>Extend the existing guarded integration fixture path for custom template/version rows, active checklist resolution, atomic create, duplicate active custom run, lifecycle retirement/rejection, scalar Workflow claim/reload/replay, bounded failure/recovery, packet-before-completion, and fixed regression. Keep the TEST_DATABASE_URL guard strict: when absent, the command must classify DB/Workflow evidence blocked/not-run and never pass; do not use db:push as production proof. Keep live provider/Firecrawl smoke optional and record policy_or_credentials_unavailable when credentials or approved policy are absent. Do not include Clerk browser E2E, broad adversarial review-idempotency, no-live-write, confirmed-only aggregation, or canonical /agents proof; those are Phase 39 handoff responsibilities.</action>
  <verify><automated>npm run test:workflow</automated></verify>
  <acceptance_criteria>With TEST_DATABASE_URL, integration fixtures pass for fixed/custom creation, duplicate, claim/replay/failure/recovery, and packet ordering; without it, evidence is explicitly blocked/not-run rather than a false pass. No live-provider or Clerk prerequisite is silently treated as success.</acceptance_criteria>
  <done>DB/Workflow evidence is prerequisite-gated and custom durable behavior is proven when the disposable database is available.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| snapshotted run → Workflow | Durable execution must use DB-authoritative immutable inputs only. |
| model/tool output → packet | Untrusted output and research content cross into validation. |
| packet → review/candidate reads | Downstream projections must remain existing read-only boundaries. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-38-10 | Tampering | Workflow replay | mitigate | scalar run ID, reload snapshots, guarded transitions, no mutable custom lookup |
| T-38-11 | Elevation | output/tool adapter | mitigate | existing modelFactory/Firecrawl only, bounded policy, fixed grounded envelope, writesAllowed false |
| T-38-12 | Denial of service | attempts/tools/time/spend | mitigate | reuse snapshotted server budget and safe terminal failure mapping |
| T-38-13 | Tampering | review/candidate boundary | mitigate | reuse packet/review/candidate queries; no direct live writes or custom tables |
| T-38-SC | Tampering | package installs | accept | no new dependencies |
</threat_model>

<verification>
Run targeted adapter/fixture commands after each task. Run `npm test`; run `npm run test:workflow` only with TEST_DATABASE_URL. Record missing TEST_DATABASE_URL as blocked, live Firecrawl/provider credentials as not-run with policy_or_credentials_unavailable, and Clerk credentials as Phase 39 blocked/not-run.
</verification>

<success_criteria>
Custom and fixed runs share the existing durable ledger, Workflow, grounded evidence, review reconciliation, and confirmed-only projection; bounded failures, retries, recovery, replay, and duplicate protection remain identical by template identity.
</success_criteria>

<output>
Create `.planning/phases/38-execution-compatibility-safe-integration/38-04-SUMMARY.md` when done.
</output>
