---
phase: 38-execution-compatibility-safe-integration
plan: 04
type: execute
wave: 3
depends_on: [38-01, 38-02, 38-03]
files_modified:
  - src/lib/analysis/execution.ts
  - src/lib/analysis/groundedContracts.ts
  - src/lib/analysis/results.ts
  - src/lib/analysis/customAgentContracts.test.ts
  - src/lib/analysis/execution.test.ts
  - src/lib/analysis/groundedContracts.test.ts
  - src/lib/analysis/results.test.ts
autonomous: true
requirements: [VER-03, VAL-04, VAL-05, RUN-01]
requirements_addressed: [VER-03, VAL-04, VAL-05, RUN-01]
must_haves:
  truths:
    - "The existing GroundedExecutionAdapter accepts the DB-snapshotted bounded custom output schema through a named input and returns a named customOutput value."
    - "The fixed grounded packet envelope remains unchanged; custom fields are additive and non-authoritative for findings, evidence, citations, review, or candidates."
    - "The exact output representation is `{ narrative, findings, custom }`, with persistence wiring specified for the next plan at `analysis_run_result.raw_audit.customOutput`."
  artifacts:
    - path: "src/lib/analysis/execution.ts"
      provides: "Server-built bounded custom-output adapter around the existing grounded executor"
      exports: ["GroundedExecutionAdapter"]
    - path: "src/lib/analysis/results.ts"
      provides: "Named customOutput transport through packet normalization without changing GroundedPacket"
      exports: ["normalizeAnalysisPacket"]
    - path: "src/lib/analysis/groundedContracts.ts"
      provides: "Fixed grounded packet plus bounded custom output validation contracts"
  key_links:
    - from: "src/lib/analysis/execution.ts"
      to: "src/lib/analysis/groundedContracts.ts"
      via: "fixed envelope parse plus named customOutput result field"
      pattern: "customOutput"
    - from: "src/lib/analysis/execution.ts"
      to: "src/lib/analysis/results.ts"
      via: "GroundedExecutionResult.customOutput passed into normalization input"
      pattern: "customOutput"
---

<objective>
Implement the bounded additive custom-output adapter around the existing grounded executor. Use the exact model envelope `{ narrative, findings, custom }`; extract the validated `custom` object as `GroundedExecutionResult.customOutput`; and pass that named value through `normalizeAnalysisPacket` while leaving the fixed `GroundedPacket` unchanged. Persistence and DB-authoritative Workflow wiring are isolated in 38-05.

Purpose: Satisfy VAL-05 and create the deterministic execution seam for RUN-01 without creating a second executor, provider/tool boundary, packet shape, review path, or candidate path. D-38-08 through D-38-11 remain hard regression boundaries.
Output: Exact adapter contract, fail-closed output validation, and deterministic adapter/normalization tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/38-execution-compatibility-safe-integration/38-CONTEXT.md
@.planning/phases/38-execution-compatibility-safe-integration/38-RESEARCH.md lines 257-337 and 412-447
@.planning/phases/38-execution-compatibility-safe-integration/38-VALIDATION.md lines 39-69
@src/lib/analysis/execution.ts
@src/lib/analysis/groundedContracts.ts
@src/lib/analysis/results.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement the exact bounded custom-output adapter</name>
  <files>src/lib/analysis/execution.ts, src/lib/analysis/groundedContracts.ts, src/lib/analysis/customAgentContracts.test.ts, src/lib/analysis/execution.test.ts, src/lib/analysis/groundedContracts.test.ts</files>
  <read_first>src/lib/analysis/execution.ts; src/lib/analysis/groundedContracts.ts; src/lib/analysis/customAgentContracts.ts; src/lib/analysis/customAgentContracts.test.ts; src/lib/analysis/execution.test.ts; src/lib/analysis/groundedContracts.test.ts</read_first>
  <action>Extend GroundedExecutionAdapter with a server-built `customOutputSchema` input sourced from `executionSnapshot.customOutputSchema.fields`, and choose the parser by run kind. For fixed runs (`customOutputSchema === null`), preserve the legacy strict `{ narrative, findings }` model envelope and do not require or persist `custom`. For custom runs, require `{ narrative, findings, custom }`, validate `custom` against the snapshotted shallow schema and Phase 37 bounds, return the validated object as `GroundedExecutionResult.customOutput`, and keep `narrative`/`findings` parsed by the existing fixed grounded contract. Reject nested/unbounded/reserved/collision fields and ensure model-authored URLs, signal identity, citations, sources, review state, and candidate fields remain non-authoritative. Preserve modelFactory/instantiateChain, Firecrawl webSearch, writesAllowed false, policy short-circuit, bounded limits, safeToolResults, invalid_packet, unsafe_research_content, missing_key, timeout, and model_failure mappings. No SQL migration or new adapter boundary is permitted.</action>
  <verify><automated>npm test -- --run src/lib/analysis/customAgentContracts.test.ts src/lib/analysis/execution.test.ts src/lib/analysis/groundedContracts.test.ts</automated></verify>
  <acceptance_criteria>Fixed adapter tests accept legacy `{ narrative, findings }` without custom and reject custom-only requirements; custom adapter tests require `{ narrative, findings, custom }`, return named `customOutput`, keep the fixed grounded envelope unchanged, reject every reserved/oversized/nested collision, and preserve existing safe failure behavior.</acceptance_criteria>
  <done>Bounded custom output is validated by the existing grounded executor seam without changing fixed packet semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Carry customOutput through normalization without changing the packet</name>
  <files>src/lib/analysis/results.ts, src/lib/analysis/results.test.ts</files>
  <read_first>src/lib/analysis/results.ts; src/lib/analysis/results.test.ts; src/lib/analysis/groundedContracts.ts; src/lib/analysis/execution.ts</read_first>
  <action>Extend the normalization input/result contract with the named `customOutput` value returned by GroundedExecutionAdapter. Validate it against the snapshotted bounded schema before normalization succeeds, include it in the immutable persistence handoff and packet-hash material, and keep the returned `packet` exactly the existing fixed GroundedPacket shape with no custom finding/evidence/citation/review/candidate fields. Reject malformed custom values with the existing `invalid_packet` safe outcome and preserve source canonicalization, finding identity, citation linkage, duplicate evidence, and Persona policy behavior.</action>
  <verify><automated>npm test -- --run src/lib/analysis/results.test.ts src/lib/analysis/groundedContracts.test.ts</automated></verify>
  <acceptance_criteria>Normalization exposes a named bounded customOutput transport value, fixed packet snapshots are unchanged, custom output participates in replay identity, and malformed/custom-channel collisions cannot alter findings or evidence.</acceptance_criteria>
  <done>Packet normalization has an explicit customOutput handoff while the fixed grounded packet remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Lock fixed/custom adapter regression behavior</name>
  <files>src/lib/analysis/execution.test.ts, src/lib/analysis/results.test.ts, src/lib/analysis/groundedContracts.test.ts</files>
  <read_first>src/lib/analysis/execution.test.ts; src/lib/analysis/results.test.ts; src/lib/analysis/groundedContracts.test.ts; src/lib/analysis/groundedContracts.ts</read_first>
  <action>Add table-driven fixed and custom cases proving fixed runs parse the legacy envelope without a custom object, custom runs require the custom object, bounded custom fields survive as a separate transport value, custom values cannot supply evidence/citations or checklist identity, malformed output/tool content fails safely, and fixed packet hashes/normalized findings remain byte/behavior compatible. Keep the existing injected executor/modelFactory seam and do not add live provider or browser verification.</action>
  <verify><automated>npm test -- --run src/lib/analysis/execution.test.ts src/lib/analysis/results.test.ts src/lib/analysis/groundedContracts.test.ts</automated></verify>
  <acceptance_criteria>Fixed and custom adapter regression tests pass and explicitly prove the fixed grounded envelope remains authoritative.</acceptance_criteria>
  <done>Adapter and normalization behavior is deterministic and ready for DB-authoritative Workflow wiring.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| DB snapshot → executor | Only the snapshotted bounded schema may authorize custom fields. |
| model/tool output → grounded packet | Untrusted output must not become evidence or review authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-38-10 | Tampering | custom output adapter | mitigate | exact schema validation, reserved-channel rejection, fixed packet preservation |
| T-38-11 | Elevation | model/tool boundary | mitigate | existing modelFactory/Firecrawl only, server policy and writesAllowed false |
| T-38-SC | Tampering | package installs | accept | no new dependencies |
</threat_model>

<verification>
Run targeted adapter tests after each task, then `npm test` at the wave boundary. DB persistence and Workflow reload evidence are executed by 38-05 and remain prerequisite-gated by TEST_DATABASE_URL.
</verification>

<success_criteria>
The existing grounded executor accepts the exact bounded custom output extension, returns a named transport value, and preserves fixed packet/evidence semantics.
</success_criteria>

<output>
Create `.planning/phases/38-execution-compatibility-safe-integration/38-04-SUMMARY.md` when done.
</output>
