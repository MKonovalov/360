---
phase: 06-shared-menu-component-start-page
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/components/dashboard/stat-card.tsx
  - src/components/dashboard/recent-signals.tsx
  - src/components/dashboard/recently-viewed.tsx
  - src/components/dashboard/needs-attention.tsx
  - src/components/dashboard/signal-breakdown.tsx
  - src/app/(dashboard)/layout.tsx
  - src/app/(dashboard)/page.tsx
  - src/app/page.tsx
autonomous: true
requirements: [START-01, START-02, START-03, START-04, START-05]

must_haves:
  truths:
    - "Visiting / while signed in shows the Start Page dashboard (3 stat cards, recent signals, recently viewed, needs attention, signal breakdown) — not the old bespoke status page"
    - "Visiting / while signed out redirects to /sign-in, same as every other protected route (D-01 — this removes the app's one prior anonymous-access exception)"
    - "Each of the four dashboard widgets fails independently on its own fetch error — one widget's failure never blanks the other three"
    - "The dashboard is reachable via the sidebar's 'Start' nav item and lives inside the same AppSidebar shell as /companies and /personas"
  artifacts:
    - path: "src/app/(dashboard)/page.tsx"
      provides: "Start Page route (Server Component)"
      contains: "requireStaffAccess"
    - path: "src/app/(dashboard)/layout.tsx"
      provides: "dashboard route group auth + shell wrapper"
    - path: "src/components/dashboard/stat-card.tsx"
      provides: "stat card presentational component"
      exports: ["StatCard"]
    - path: "src/components/dashboard/recent-signals.tsx"
      exports: ["RecentSignals"]
    - path: "src/components/dashboard/recently-viewed.tsx"
      exports: ["RecentlyViewed"]
    - path: "src/components/dashboard/needs-attention.tsx"
      exports: ["NeedsAttention"]
    - path: "src/components/dashboard/signal-breakdown.tsx"
      exports: ["SignalBreakdown"]
  key_links:
    - from: "src/app/(dashboard)/page.tsx"
      to: "src/lib/db/queries/stats.ts"
      via: "calls getDashboardCounts() for the 3 stat cards"
      pattern: "getDashboardCounts\\("
    - from: "src/components/dashboard/recently-viewed.tsx"
      to: "src/lib/db/queries/recentlyViewed.ts"
      via: "calls listRecentlyViewedForUser(userId, 5)"
      pattern: "listRecentlyViewedForUser\\("
    - from: "src/app/(dashboard)/layout.tsx"
      to: "src/components/layout/app-shell-layout.tsx"
      via: "renders <AppShellLayout>{children}</AppShellLayout>"
      pattern: "AppShellLayout"
---

<objective>
Build the Start Page: a new `(dashboard)` route group that fully replaces `src/app/page.tsx` (D-01), rendering 3 stat cards plus 4 independently-failing widgets (Recent Signals, Recently Viewed, Needs Attention, Signal Breakdown), reusing the `getDashboardCounts`/`listRecentSignals`/`listNeedsAttention`/`getSignalTypeBreakdown`/`listRecentlyViewedForUser` queries from Plan 06-01 and the `AppShellLayout` from Plan 06-02.

Purpose: This plan delivers the entire user-observable Start Page — START-01 through START-05's success criteria all become true here.
Output: `src/app/(dashboard)/layout.tsx`, `src/app/(dashboard)/page.tsx`, 5 new dashboard widget components, and deletion of the old `src/app/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-menu-component-start-page/06-CONTEXT.md
@.planning/phases/06-shared-menu-component-start-page/06-RESEARCH.md
@.planning/phases/06-shared-menu-component-start-page/06-PATTERNS.md
@.planning/phases/06-shared-menu-component-start-page/06-UI-SPEC.md

@src/app/page.tsx
@src/app/companies/layout.tsx
@src/app/companies/page.tsx
@src/components/companies/company-list.tsx
@src/components/companies/signal-badge.tsx
@src/lib/auth/requireStaffAccess.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the 5 dashboard widget components (each with its own try/catch, EXPL-06)</name>
  <files>src/components/dashboard/stat-card.tsx, src/components/dashboard/recent-signals.tsx, src/components/dashboard/recently-viewed.tsx, src/components/dashboard/needs-attention.tsx, src/components/dashboard/signal-breakdown.tsx</files>
  <read_first>
    src/components/companies/company-list.tsx (EXPL-06 try/catch → known-good fallback card pattern, and the empty-state card shell to copy)
    src/lib/db/queries/stats.ts (Plan 06-01 output — getDashboardCounts, listRecentSignals, listNeedsAttention, getSignalTypeBreakdown)
    src/lib/db/queries/recentlyViewed.ts (Plan 06-01 output — listRecentlyViewedForUser)
    src/components/companies/signal-badge.tsx (SignalBadge — reuse verbatim for Needs Attention rows and the Signal Breakdown widget, do not create a second badge component)
    src/lib/auth/requireStaffAccess.ts (RecentlyViewed needs userId — call requireStaffAccess() inside the widget itself)
    .planning/phases/06-shared-menu-component-start-page/06-UI-SPEC.md (Copywriting Contract — exact empty/error-state heading and body text per widget; Interaction Contract — Row 1/2/3 card/grid classes)
    .planning/phases/06-shared-menu-component-start-page/06-RESEARCH.md (Pitfall 3 — recordType→route mapping must use an explicit lookup map, never naive `${recordType}s` pluralization, since 'company' pluralizes irregularly to 'companies')
  </read_first>
  <action>
    Create `src/components/dashboard/stat-card.tsx` exporting `StatCard({ label, value }: { label: string; value: number })`: a `div` with `className="rounded-lg border border-slate-200 bg-white p-6"` containing a Label-style caption (`text-[12px] font-normal leading-[1.4] text-slate-500"`) above a Display-style number (`text-[24px] font-semibold leading-[1.2] text-slate-900"`).
    Create `src/components/dashboard/recent-signals.tsx` as an async Server Component `RecentSignals()`. Fetch `await listRecentSignals(5)` inside its own `try { ... } catch { ... }`; on error render the EXPL-06 fallback card with heading `"Couldn't load recent signals"` and the standard body `"Something went wrong fetching this data. Try refreshing the page."`. On empty result render heading `"No signals yet"` / body `"Signals will appear here once they're detected for a company."`. Otherwise render a Heading `"Recent Signals"` above a `space-y-2` list of rows, each linking the company name via `Link href={`/companies?selected=${companyId}`}` (indigo-600 text) alongside the signal type + detected date.
    Create `src/components/dashboard/recently-viewed.tsx` as an async Server Component `RecentlyViewed()`. Call `const { userId } = await requireStaffAccess();` then, inside its own try/catch, `await listRecentlyViewedForUser(userId, 5)`. Resolve each row's display name via `getCompanyById`/`getPersonaById` keyed on `recordType` (N+1 over at most 5 rows is acceptable at this scale, matching `company-list.tsx`'s existing precedent). Build each row's link target via an explicit map, e.g. a `const ROUTE_BY_RECORD_TYPE = { company: '/companies', persona: '/personas' } as const` — NEVER a naive `` `/${recordType}s?selected=${id}` `` template (Pitfall 3 — that incorrectly pluralizes 'company' to 'companys'). Error heading `"Couldn't load recently viewed"`. Empty state heading `"Nothing viewed yet"` / body `"Companies and Personas you open will show up here."`.
    Create `src/components/dashboard/needs-attention.tsx` as an async Server Component `NeedsAttention()`. Fetch `await listNeedsAttention(14)` inside its own try/catch. Error heading `"Couldn't load needs attention"`. Empty state heading `"You're all caught up"` / body `"No high-strength signals are overdue for review."`. Otherwise list each returned company's name (linked via the same `/companies?selected=<id>` convention) alongside its high-strength `SignalBadge`s (fetch signals per company or reuse whatever `listNeedsAttention` already returns — if it returns bare company rows, fetch `listSignalsForCompany` per row and filter to `strength === 'high'` for the badge list).
    Create `src/components/dashboard/signal-breakdown.tsx` as an async Server Component `SignalBreakdown()`. Fetch `await getSignalTypeBreakdown()` inside its own try/catch. Error heading `"Couldn't load signal breakdown"`. No special empty state — always render all 4 rows returned (already zero-filled by the query), each as a `SignalBadge` next to a right-aligned Body-size count.
    Every widget's try/catch is fully independent of the other three — one widget's failure must never blank the page or affect sibling widgets (this is enforced structurally by each widget being its own Server Component invoked separately from the page, not by a shared try/catch).
  </action>
  <acceptance_criteria>
    - All 5 files exist with the named exports StatCard, RecentSignals, RecentlyViewed, NeedsAttention, SignalBreakdown
    - recent-signals.tsx, recently-viewed.tsx, needs-attention.tsx, signal-breakdown.tsx each contain exactly one try/catch block around their own data fetch
    - recently-viewed.tsx builds its link targets via an explicit record-type-to-route lookup, not string-interpolated pluralization (no literal `${recordType}s` substring appears in the file)
    - signal-breakdown.tsx always renders 4 rows with no distinct all-zero empty-state branch
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && for f in recent-signals recently-viewed needs-attention signal-breakdown; do test "$(grep -c 'try {' src/components/dashboard/$f.tsx)" = 1 || exit 1; done && test "$(grep -c '\${recordType}s' src/components/dashboard/recently-viewed.tsx)" = 0</automated>
  </verify>
  <done>All 5 dashboard widget components exist, type-check, and each Server Component widget fails independently with the exact UI-SPEC copy for its error/empty states.</done>
</task>

<task type="auto">
  <name>Task 2: Create (dashboard) route group and replace / with the Start Page (D-01, D-02, START-01..05)</name>
  <files>src/app/(dashboard)/layout.tsx, src/app/(dashboard)/page.tsx, src/app/page.tsx</files>
  <read_first>
    src/app/page.tsx (current — the exact file being fully replaced; confirm its content before deleting so nothing else depends on it)
    src/app/companies/layout.tsx (post-Plan-06-02-refactor shape — the exact thin requireStaffAccess() + AppShellLayout wrapper pattern to mirror)
    src/components/layout/app-shell-layout.tsx (Plan 06-02 output)
    src/lib/db/queries/stats.ts (Plan 06-01 output — getDashboardCounts)
    .planning/phases/06-shared-menu-component-start-page/06-UI-SPEC.md (Start Page layout contract — Row 1/2/3 grid classes, page container classes)
    .planning/phases/06-shared-menu-component-start-page/06-CONTEXT.md (D-01, D-02)
    .planning/phases/06-shared-menu-component-start-page/06-RESEARCH.md (Assumption A1 — "Active Signal count" = plain COUNT(*) FROM signal, no status filter)
  </read_first>
  <action>
    Create `src/app/(dashboard)/layout.tsx` mirroring the post-refactor `companies/layout.tsx` shape exactly: `await requireStaffAccess();` then `return <AppShellLayout>{children}</AppShellLayout>;` (same imports: `requireStaffAccess` from `@/lib/auth/requireStaffAccess`, `AppShellLayout` from `@/components/layout/app-shell-layout`).
    Create `src/app/(dashboard)/page.tsx` as the Start Page default export (async Server Component, `requireStaffAccess()` called first — belt-and-suspenders, matching every other page in the app). Fetch `const counts = await getDashboardCounts();`. Render a `div` with `className="flex flex-col gap-8 p-8"` containing: Row 1 — `div className="grid grid-cols-1 gap-4 sm:grid-cols-3"` with 3 `<StatCard label="Companies" value={counts.companies} />`, `<StatCard label="Personas" value={counts.personas} />`, `<StatCard label="Active Signals" value={counts.signals} />` (plain `COUNT(*) FROM signal`, no status filter — per RESEARCH.md Assumption A1); Row 2 — `div className="grid grid-cols-1 gap-4 lg:grid-cols-2"` with `<RecentSignals />` and `<RecentlyViewed />`; Row 3 — same grid classes with `<NeedsAttention />` and `<SignalBreakdown />`.
    Delete `src/app/page.tsx` entirely (D-01 — the old public "Not signed in" status page and its `RefreshCompanyCount` demo are fully replaced, not kept alongside; a signed-out visitor is now redirected to `/sign-in` by `requireStaffAccess()` before any dashboard content or query runs — no inline "sign in" copy is needed on this route per the UI-SPEC Copywriting Contract).
  </action>
  <acceptance_criteria>
    - src/app/page.tsx no longer exists on disk
    - src/app/(dashboard)/page.tsx calls requireStaffAccess() before calling getDashboardCounts() or rendering any widget
    - The page renders exactly 3 StatCard elements in Row 1, and exactly one instance each of RecentSignals, RecentlyViewed, NeedsAttention, SignalBreakdown in Rows 2-3
    - src/app/(dashboard)/layout.tsx calls requireStaffAccess() then renders AppShellLayout, mirroring companies/layout.tsx's post-refactor shape
  </acceptance_criteria>
  <verify>
    <automated>test ! -f src/app/page.tsx && npx tsc --noEmit && npm run build && test "$(grep -c 'requireStaffAccess' 'src/app/(dashboard)/page.tsx')" -ge 1 && test "$(grep -c 'StatCard' 'src/app/(dashboard)/page.tsx')" -ge 3</automated>
  </verify>
  <done>Visiting / (signed in) renders the Start Page with 3 stat cards and 4 widgets inside the AppSidebar shell; visiting / (signed out) redirects to /sign-in; npm run build succeeds with no duplicate-route error (proving the old src/app/page.tsx is fully gone).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Unauthenticated browser → `/` route | D-01 changes this route from the app's one documented public-access exception to a fully gated route, matching every other page |
| Dashboard Server Components → Neon Postgres | All 5 widgets + the page's own `getDashboardCounts()` call cross into the database layer via the Plan 06-01 query functions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Elevation of Privilege / Information Disclosure | `/` route (formerly the one page without `requireStaffAccess()`) | mitigate | Both `(dashboard)/layout.tsx` and `(dashboard)/page.tsx` call `requireStaffAccess()` (belt-and-suspenders, matching every other route in the app) — a signed-out visitor is redirected to `/sign-in` before any dashboard query executes, fully closing D-01's prior documented exception |
| T-06-11 | Tampering | SQL injection via any of the 5 widgets' underlying queries | mitigate | All queries invoked here (`getDashboardCounts`, `listRecentSignals`, `listNeedsAttention`, `getSignalTypeBreakdown`, `listRecentlyViewedForUser`) were built in Plan 06-01 using Drizzle's parameterized query builder exclusively — this plan only calls them, introduces no new raw SQL |
| T-06-12 | Information Disclosure | `RecentlyViewed` widget displaying another user's viewing history | mitigate | `listRecentlyViewedForUser` is always called with `userId` derived from this page's own `requireStaffAccess()` call, never a route param or query string — a staff member can only ever see their own recently-viewed list, matching START-03's "current user" requirement |
</threat_model>

<verification>
Run `npx tsc --noEmit` after Task 1, then `npx tsc --noEmit && npm run build` after Task 2 (the build check is load-bearing here — it is the only automated proof that removing `src/app/page.tsx` while adding `src/app/(dashboard)/page.tsx` does not produce a duplicate-route build error). Manual UAT: visit `/` signed out — confirm redirect to `/sign-in`. Visit `/` signed in — confirm 3 stat cards match live `SELECT COUNT(*)` values for company/persona/signal tables, Recent Signals/Recently Viewed/Needs Attention/Signal Breakdown all render, and clicking a Company/Persona name in any widget navigates to `/companies?selected=<id>` or `/personas?selected=<id>` and opens that row. Confirm the sidebar's "Start" item is highlighted only on `/`.
</verification>

<success_criteria>
- `/` (signed in) renders the Start Page with 3 accurate stat cards and 4 independently-rendering widgets
- `/` (signed out) redirects to `/sign-in`
- `src/app/page.tsx` is deleted; `npm run build` succeeds with no route conflict
- Every widget's own error state renders in isolation without affecting sibling widgets
</success_criteria>

<output>
Create `.planning/phases/06-shared-menu-component-start-page/06-04-SUMMARY.md` when done
</output>
