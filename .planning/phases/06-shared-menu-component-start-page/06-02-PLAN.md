---
phase: 06-shared-menu-component-start-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/app-shell-layout.tsx
  - src/app/companies/layout.tsx
  - src/app/personas/layout.tsx
  - src/components/ui/dropdown-menu.tsx
  - src/components/explorer/explorer-menu.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements: [MENU-01, MENU-02]

must_haves:
  truths:
    - "companies/layout.tsx and personas/layout.tsx no longer duplicate the sidebar shell body — both delegate to a shared AppShellLayout component"
    - "A shadcn dropdown-menu primitive is installed and wrapped by one shared ExplorerMenu component reused by both the list-page (labeled) and detail-panel (icon) Menu placements"
    - "The sidebar shows a 'Start' nav item above Companies/Key Personas, highlighted only when pathname is exactly '/'"
  artifacts:
    - path: "src/components/layout/app-shell-layout.tsx"
      provides: "shared sidebar shell (SidebarProvider/AppSidebar/SidebarResizeHandle/SidebarInset + cookie-based width restore)"
      exports: ["AppShellLayout"]
    - path: "src/components/ui/dropdown-menu.tsx"
      provides: "vendored Radix dropdown-menu primitive (shadcn radix-nova preset)"
    - path: "src/components/explorer/explorer-menu.tsx"
      provides: "shared Menu dropdown component, parameterized by labeled/icon variant"
      exports: ["ExplorerMenu"]
  key_links:
    - from: "src/app/companies/layout.tsx"
      to: "src/components/layout/app-shell-layout.tsx"
      via: "renders <AppShellLayout>{children}</AppShellLayout>"
      pattern: "AppShellLayout"
    - from: "src/app/personas/layout.tsx"
      to: "src/components/layout/app-shell-layout.tsx"
      via: "renders <AppShellLayout>{children}</AppShellLayout>"
      pattern: "AppShellLayout"
    - from: "src/components/explorer/explorer-menu.tsx"
      to: "src/components/ui/dropdown-menu.tsx"
      via: "imports DropdownMenu/DropdownMenuTrigger/DropdownMenuContent/DropdownMenuItem"
      pattern: "from '@/components/ui/dropdown-menu'"
---

<objective>
Build the two pieces of shared infrastructure Phase 6 needs before any real page can be wired up: (1) extract the byte-identical `companies/layout.tsx`/`personas/layout.tsx` sidebar shell into one `AppShellLayout` component (RESEARCH.md Pitfall 1 — this is the third time the pattern would otherwise be pasted), and (2) install shadcn's `dropdown-menu` primitive and wrap it in one shared `ExplorerMenu` component (D-08/D-09, MENU-01/MENU-02's one-time shared investment). Also add the "Start" sidebar nav item (D-02).

Purpose: Plan 06-03 (wiring Menu buttons into the explorers) and Plan 06-04 (the Start Page route) both consume `AppShellLayout` and `ExplorerMenu` — this plan has zero dependents outside this phase, so it can build fully in parallel with Plan 06-01.
Output: `src/components/layout/app-shell-layout.tsx`, refactored `companies/layout.tsx`/`personas/layout.tsx`, `src/components/ui/dropdown-menu.tsx`, `src/components/explorer/explorer-menu.tsx`, and an updated `app-sidebar.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-shared-menu-component-start-page/06-CONTEXT.md
@.planning/phases/06-shared-menu-component-start-page/06-RESEARCH.md
@.planning/phases/06-shared-menu-component-start-page/06-PATTERNS.md
@.planning/phases/06-shared-menu-component-start-page/06-UI-SPEC.md

@src/app/companies/layout.tsx
@src/app/personas/layout.tsx
@src/components/layout/app-sidebar.tsx
@src/components/ui/select.tsx
@src/components/explorer/explorer-table-behavior.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract AppShellLayout and refactor companies/personas layouts (RESEARCH.md Pitfall 1)</name>
  <files>src/components/layout/app-shell-layout.tsx, src/app/companies/layout.tsx, src/app/personas/layout.tsx</files>
  <read_first>
    src/app/companies/layout.tsx (current — confirmed byte-identical to personas/layout.tsx this session)
    src/app/personas/layout.tsx (current)
    .planning/phases/06-shared-menu-component-start-page/06-RESEARCH.md (Pitfall 1 — mandatory dedupe rationale, "third copy" trigger point)
    .planning/phases/06-shared-menu-component-start-page/06-PATTERNS.md (app-shell-layout.tsx section — exact target refactor shape)
  </read_first>
  <action>
    Create `src/components/layout/app-shell-layout.tsx` exporting an async Server Component `AppShellLayout({ children }: { children: React.ReactNode })`. Move into it everything from the current `companies/layout.tsx` EXCEPT the `requireStaffAccess()` call: the `MIN_SIDEBAR_WIDTH = 200`, `MAX_SIDEBAR_WIDTH = 400`, `DEFAULT_SIDEBAR_WIDTH = 256` constants, the `cookies()` read and `sidebar_width` cookie parsing/clamping logic, and the `SidebarProvider`/`AppSidebar`/`SidebarResizeHandle`/`SidebarInset`/`SidebarTrigger` JSX composition (rendering `{children}` where the route's page content goes). Auth stays out of this component — it is the caller's (route layout's) job, not the shared shell's.
    Refactor `src/app/companies/layout.tsx` to: `await requireStaffAccess();` then `return <AppShellLayout>{children}</AppShellLayout>;` — remove the now-duplicated cookie/Sidebar composition entirely, keeping only the `requireStaffAccess()` call and the import of `AppShellLayout`.
    Apply the identical refactor to `src/app/personas/layout.tsx`.
  </action>
  <acceptance_criteria>
    - app-shell-layout.tsx exports AppShellLayout and contains the SidebarProvider/AppSidebar/SidebarResizeHandle/SidebarInset composition + cookie-width-restore logic
    - app-shell-layout.tsx does NOT call requireStaffAccess() (auth stays in the route layouts)
    - companies/layout.tsx and personas/layout.tsx each call requireStaffAccess() then render <AppShellLayout>{children}</AppShellLayout>
    - Neither companies/layout.tsx nor personas/layout.tsx imports SidebarProvider directly anymore
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test "$(grep -c 'SidebarProvider' src/app/companies/layout.tsx)" = 0 && test "$(grep -c 'SidebarProvider' src/app/personas/layout.tsx)" = 0 && test "$(grep -c 'AppShellLayout' src/app/companies/layout.tsx)" -ge 1 && test "$(grep -c 'AppShellLayout' src/app/personas/layout.tsx)" -ge 1 && test "$(grep -c 'requireStaffAccess' src/components/layout/app-shell-layout.tsx)" = 0</automated>
  </verify>
  <done>The sidebar shell body exists in exactly one file (app-shell-layout.tsx); both route layouts are thin requireStaffAccess() + AppShellLayout wrappers; the app still builds and both /companies and /personas continue to render the sidebar shell identically to before.</done>
</task>

<task type="auto">
  <name>Task 2: Install shadcn dropdown-menu + create shared ExplorerMenu component</name>
  <files>src/components/ui/dropdown-menu.tsx, src/components/explorer/explorer-menu.tsx</files>
  <read_first>
    src/components/ui/select.tsx (Radix-wrapping convention this phase's vendored dropdown-menu.tsx must match — data-slot, cn()-composed className, named function exports)
    src/components/explorer/explorer-table-behavior.tsx (ExplorerCloseButton — Button variant="ghost" size="icon" + aria-label shape to mirror for the icon-variant trigger)
    .planning/phases/06-shared-menu-component-start-page/06-RESEARCH.md (Pattern 4 — exact ExplorerMenu code)
    .planning/phases/06-shared-menu-component-start-page/06-UI-SPEC.md (Menu component contract — labeled vs icon trigger, DropdownMenuContent align="end")
  </read_first>
  <action>
    Run `npx shadcn add dropdown-menu` to vendor `src/components/ui/dropdown-menu.tsx` from the project's already-configured `radix-nova` registry preset (do not hand-author this file — it is generated, built on the already-installed `radix-ui` package).
    Create `src/components/explorer/explorer-menu.tsx` as a Client Component (`'use client'` directive) exporting `ExplorerMenu({ variant, items }: { variant: 'labeled' | 'icon'; items: { label: string; disabled?: boolean }[] })`. Import `ChevronDownIcon, EllipsisVerticalIcon` from `lucide-react`, `Button` from `@/components/ui/button`, and `DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger` from `@/components/ui/dropdown-menu`.
    When `variant === 'labeled'`, the trigger (wrapped in `DropdownMenuTrigger asChild`) is `Button variant="outline"` containing the text "Menu" followed by a trailing `ChevronDownIcon` (`className="size-4"`). When `variant === 'icon'`, the trigger is `Button variant="ghost" size="icon" aria-label="Menu"` containing only `EllipsisVerticalIcon`.
    `DropdownMenuContent align="end"` maps `items` to `DropdownMenuItem key={item.label} disabled={item.disabled}>{item.label}</DropdownMenuItem>`.
  </action>
  <acceptance_criteria>
    - src/components/ui/dropdown-menu.tsx exists and exports DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem
    - explorer-menu.tsx exports a function ExplorerMenu accepting exactly the { variant, items } props described
    - The labeled variant renders button text "Menu" with a ChevronDownIcon; the icon variant renders aria-label="Menu" with an EllipsisVerticalIcon and no visible text
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/ui/dropdown-menu.tsx && npx tsc --noEmit && test "$(grep -c 'export function ExplorerMenu' src/components/explorer/explorer-menu.tsx)" = 1 && test "$(grep -c 'EllipsisVerticalIcon' src/components/explorer/explorer-menu.tsx)" -ge 1</automated>
  </verify>
  <done>dropdown-menu.tsx is vendored and type-checks; ExplorerMenu renders the labeled/icon trigger variants exactly per the UI-SPEC contract, ready to be wired into both list pages and detail panels in Plan 06-03.</done>
</task>

<task type="auto">
  <name>Task 3: Add "Start" nav item to AppSidebar (D-02)</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <read_first>
    src/components/layout/app-sidebar.tsx (current — exact SidebarMenuItem/SidebarMenuButton shape for the existing "Companies" item)
    .planning/phases/06-shared-menu-component-start-page/06-CONTEXT.md (D-02)
    .planning/phases/06-shared-menu-component-start-page/06-PATTERNS.md (app-sidebar.tsx section — the exact-equality isActive gotcha)
  </read_first>
  <action>
    Add a new `SidebarMenuItem` immediately above the existing "Companies" `SidebarMenuItem` (so source order is Start, then Companies, then Key Personas). It must use `SidebarMenuButton asChild isActive={pathname === '/'}` — EXACT equality, not `.startsWith('/')`, since `/` is a string prefix of every route in the app and a naive `.startsWith('/')` check would always match and highlight "Start" on every page. Reuse the identical className already on the other two items: `"data-active:bg-indigo-50 data-active:text-indigo-600 data-active:hover:bg-indigo-50 data-active:hover:text-indigo-600"`. Wrap `<Link href="/">Start</Link>` inside it (link text exactly "Start", per UI-SPEC Copywriting Contract).
  </action>
  <acceptance_criteria>
    - app-sidebar.tsx contains a SidebarMenuItem whose isActive check is exactly pathname === '/' (not .startsWith)
    - The new item's link text is exactly "Start" and its href is "/"
    - The new SidebarMenuItem appears before the "Companies" SidebarMenuItem in source order
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test "$(grep -c "pathname === '/'" src/components/layout/app-sidebar.tsx)" = 1 && test "$(grep -c '>Start</Link>' src/components/layout/app-sidebar.tsx)" = 1</automated>
  </verify>
  <done>The sidebar shows Start above Companies/Key Personas, highlighted only when pathname is exactly "/".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan is pure UI/layout refactoring and a client-side dropdown-menu shell — no new data path, no new server-side write, no new auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | `AppShellLayout` extraction accidentally drops the `requireStaffAccess()` call from a route layout during refactor | mitigate | Task 1's acceptance criteria explicitly require `requireStaffAccess()` to remain in each route layout (companies, personas) — the shared `AppShellLayout` component deliberately does NOT call it, so an accidental omission during the refactor is caught by the grep-based verify step checking for its continued presence in both route layout files |
| T-06-06 | Information Disclosure | Menu dropdown items exposed to any signed-in user with no distinction | accept | This phase's Menu items (Import, Analyze) are inert `disabled` placeholders — no action fires, no data is exposed; the codebase's existing "any authenticated Clerk user = full access" model already applies uniformly to every other page and is explicitly accepted at the project level (STATE.md carried concern) |
</threat_model>

<verification>
Run `npx tsc --noEmit` after each task. After Task 1, manually load `/companies` and `/personas` and confirm the sidebar/resize-handle/inset render identically to before the refactor (no visual regression). After Task 2, confirm `npx shadcn add dropdown-menu` did not modify `components.json`'s registry config beyond adding the component entry. After Task 3, confirm the sidebar highlights "Start" only on `/`, not on `/companies` or `/personas`.
</verification>

<success_criteria>
- Exactly one file contains the sidebar shell composition (`app-shell-layout.tsx`); `companies/layout.tsx` and `personas/layout.tsx` are thin wrappers
- `dropdown-menu.tsx` is vendored and `ExplorerMenu` exists with both `labeled` and `icon` variants working
- Sidebar shows "Start" above "Companies"/"Key Personas" with correct exact-match active-highlighting
- `npx tsc --noEmit` and `npm run build` both succeed
</success_criteria>

<output>
Create `.planning/phases/06-shared-menu-component-start-page/06-02-SUMMARY.md` when done
</output>
