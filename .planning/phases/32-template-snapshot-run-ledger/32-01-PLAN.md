---
phase: 32-template-snapshot-run-ledger
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/analysis/contracts.ts
  - src/lib/analysis/contracts.test.ts
  - scripts/probe-neon-http-transaction.ts
  - .planning/phases/32-template-snapshot-run-ledger/32-TRANSACTION-PROBE.md
autonomous: true
requirements: [CON-02, CON-03, CON-04, CON-05, RUN-02, RUN-05, RUN-06]
must_haves:
  truths:
    - "The Phase 32 status machine, nonterminal set, typed subject union, safe outcome reasons, and snapshot boundaries are executable contracts before schema implementation begins."
    - "The installed Drizzle neon-http driver limitation is recorded, and a Wave 0 probe proves the selected single-statement SQL CTE mechanism before ledger implementation proceeds."
    - "Contract tests cover active/draft/retired filtering semantics, Company/Persona incompatibility, empty checklist validity, bounded attempts, and secret-free snapshot allowlists."
    - "Phase 32 uses the resolved effort value standard and the exact no-op policy/limit snapshot shape rather than leaving effort or policy to implementation discretion."
  artifacts:
    - path: src/lib/analysis/contracts.ts
      provides: "Shared Phase 32 status, subject, outcome, and snapshot contract types/Zod schemas"
      contains: "queued"
    - path: src/lib/analysis/contracts.test.ts
      provides: "Executable contract matrix for lifecycle, compatibility, checklist, and snapshot safety"
      contains: "pending_review"
    - path: scripts/probe-neon-http-transaction.ts
      provides: "Installed-driver transaction/rollback capability probe without dependency changes"
      contains: "neon-http"
  key_links:
    - from: src/lib/analysis/contracts.ts
      to: src/lib/db/schema.ts
      via: "status and snapshot schemas become the typed shape used by JSONB columns"
      pattern: "AnalysisRunStatus"
    - from: scripts/probe-neon-http-transaction.ts
      to: TEST_DATABASE_URL
      via: "probe constructs an isolated neon-http client from the test URL and never imports production db/env"
      pattern: "TEST_DATABASE_URL"
---

<objective>
Lock the Phase 32 contracts and resolve the only research uncertainty before any schema or ledger implementation: how the installed Drizzle `neon-http` client can atomically persist a guarded transition and its event.

Purpose: Downstream plans must implement one exact lifecycle, snapshot, compatibility, and duplicate-guard contract rather than inventing incompatible shapes (D-32-02, D-32-03, D-32-04, D-32-05).
Output: Contract module/tests, a harmless installed-driver probe, and a checked-in probe result used by the ledger plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-template-snapshot-run-ledger/32-CONTEXT.md
@.planning/phases/32-template-snapshot-run-ledger/32-RESEARCH.md
@.planning/phases/31-durable-executor-selection-validation/31-01-SUMMARY.md
@.planning/phases/31-durable-executor-selection-validation/31-02-SUMMARY.md

<interfaces>
From `src/lib/auth/requireStaffAccess.ts`: `requireStaffAccess(): Promise<{ userId: string }>` is the only staff gate and belongs at route/action boundaries, never in query modules.
From `src/lib/db/index.ts`: `db` is the existing Drizzle client created with `drizzle-orm/neon-http`; do not add a driver or package.
From Phase 31: application state is database-authoritative, transitions are guarded, event history is append-only, and executor/system actors may be distinct from Clerk actors.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define the Phase 32 contract and safety schemas</name>
  <files>src/lib/analysis/contracts.ts, src/lib/analysis/contracts.test.ts</files>
  <action>Per D-32-02 through D-32-05, define named exports for `AnalysisRunStatus` with exactly queued/running/completed/failed/cancelled/pending_review/confirmed/dismissed, the nonterminal set exactly queued/running/pending_review, legal transition matrix, bounded attempt/reason unions, discriminated Company/Persona subject input, and allowlisted versioned JSON snapshot schemas. Resolve the research assumptions in the contract: the only seeded effort is `standard`; `supportedEfforts` is a non-empty array containing `standard`; the standard future execution budget is `{ maxAttempts: 2, maxToolCalls: 12, maxExecutionSeconds: 300, maxSpendUsd: 2.5 }`; and the Phase 32 no-op handoff policy is `{ schemaVersion: 1, mode: 'phase32_noop', networkAccess: false, writesAllowed: false, effectiveMaxAttempts: 1, effectiveMaxToolCalls: 0, effectiveMaxExecutionSeconds: 5, effectiveMaxSpendUsd: 0 }`. The policy snapshot is safe configuration metadata, not Phase 33 execution behavior. Snapshot objects must separately validate template, subject, checklist, execution, and policy data; retain scalar relational identity fields in the contract; permit checklist `items: []`; accept only safe model IDs/policy values and reject API keys, database URLs, Clerk/session values, private reasoning, and unrestricted source rows. Make replay/no-transition and safe outcome mappings explicit so later query/API code cannot silently invent states.</action>
  <verify><automated>npm test -- src/lib/analysis/contracts.test.ts</automated></verify>
  <done>All locked statuses, legal edges, typed subject discrimination, empty-checklist behavior, bounded reasons/attempts, and secret-free snapshot allowlists are executable and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Probe atomic neon-http transition/event persistence</name>
  <files>scripts/probe-neon-http-transaction.ts, .planning/phases/32-template-snapshot-run-ledger/32-TRANSACTION-PROBE.md</files>
  <action>Using only installed `drizzle-orm/neon-http` and `@neondatabase/serverless`, read `TEST_DATABASE_URL`, construct a fresh `neon(testDatabaseUrl)` client and `drizzle({ client: testSql })` instance inside the probe, and never import `src/lib/db/index.ts`, `src/lib/env.ts`, or the production-bound `db`/`env.DATABASE_URL`. Record the confirmed runtime limitation that `db.transaction` is unsupported with the exact driver error `No transactions support in neon-http driver`; do not claim or retry that mechanism. Probe the selected fallback as one SQL statement/CTE that conditionally updates a temporary test row and inserts its append-only event in the same statement, then deliberately raises a SQL error in the probe setup and proves the statement leaves no partial update/event. Do not silently substitute independent HTTP requests or a different driver. Do not create permanent tables, alter schema, add dependencies, or print either database URL. Record the CTE SQL shape, update/insert result, rollback evidence, and exact implementation constraint for Plan 32-04 in `32-TRANSACTION-PROBE.md`; fail non-zero when `TEST_DATABASE_URL` is absent or CTE atomicity/rollback is not proven.</action>
  <verify><automated>if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 32 CTE atomicity evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm exec tsx -- scripts/probe-neon-http-transaction.ts</automated></verify>
  <done>The repository records the unsupported `db.transaction` finding and contains reproducible Wave 0 evidence for the single-statement CTE fallback, or execution remains blocked; no schema or dependency side effects occur.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted create input → contract parser | Client can forge subject kind/id, template version, effort, checklist, or reason values. |
| database driver → lifecycle audit | HTTP retries or partial failures could split a status mutation from its event. |
| server configuration → snapshot JSONB | Model/policy resolution may contain secrets that must never cross into persisted snapshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Tampering | `src/lib/analysis/contracts.ts` | mitigate | Zod discriminated unions, bounded enums, and explicit transition matrix reject forged lifecycle/subject/snapshot values before writes. |
| T-32-02 | Repudiation | transaction probe and event contract | mitigate | Require proven atomic transition+event behavior before ledger implementation; record rollback evidence without secrets. |
| T-32-03 | Information Disclosure | snapshot schemas | mitigate | Allowlist model IDs and policy values; reject credentials, URLs, sessions, and private reasoning. |
| T-32-SC | Tampering | package/driver surface | mitigate | Probe only already-installed Drizzle/Neon APIs; no package installation or dependency alteration is permitted. |
</threat_model>

<verification>
Run the focused contract test and the transaction probe with `TEST_DATABASE_URL`; verify the probe result file names the mechanism Plan 32-04 must use and contains no credential material.
</verification>

<success_criteria>
- Downstream plans have one shared lifecycle/subject/snapshot contract and tests.
- The transition/event atomicity question is resolved by evidence, not assumption.
- No database schema, legacy table, dependency, or application runtime behavior is changed beyond the planned contract/probe artifacts.
</success_criteria>

<output>
Create `.planning/phases/32-template-snapshot-run-ledger/32-01-SUMMARY.md` when done.
</output>
