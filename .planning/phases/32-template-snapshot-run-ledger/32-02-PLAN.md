---
phase: 32-template-snapshot-run-ledger
plan: 02
type: execute
wave: 1
depends_on: [32-01]
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0001_phase32_template_snapshot_run_ledger.sql
  - drizzle/meta/0001_snapshot.json
  - drizzle/meta/_journal.json
  - src/scripts/seedAnalysisTemplates.ts
  - src/scripts/seedAnalysisTemplates.integration.test.ts
  - src/lib/db/analysisSchema.integration.test.ts
autonomous: true
requirements: [CON-01, CON-02, RUN-02, RUN-05, RUN-06]
must_haves:
  truths:
    - "The database has additive typed template, immutable version, analysis run, and append-only event structures with the complete locked lifecycle."
    - "The schema enforces the nonterminal duplicate guard for queued, running, and pending_review by subject kind/id plus template identity, including concurrent starts."
    - "Exactly two active GBS templates and one immutable initial version per template are seeded idempotently without mutating existing versions."
    - "Phase 31 workflow proof tables/enums and legacy agent_run, signalProposal, correction, and Reviews relations remain untouched, with executable metadata assertions rather than manual inspection alone."
  artifacts:
    - path: src/lib/db/schema.ts
      provides: "Additive template/version/run/event enums and tables with scalar identity/index columns"
      contains: "analysisRun"
    - path: drizzle/0001_phase32_template_snapshot_run_ledger.sql
      provides: "Reviewable additive migration for the four Phase 32 logical concerns"
      contains: "analysis_run_active_subject_template_idx"
    - path: src/scripts/seedAnalysisTemplates.ts
      provides: "Stable-natural-key, conflict-detecting two-template/two-version seed"
      contains: "company-buying-signal-analysis"
  key_links:
    - from: src/lib/db/schema.ts
      to: drizzle/0001_phase32_template_snapshot_run_ledger.sql
      via: "generated migration matches schema enums, FKs, JSONB columns, and partial unique index"
      pattern: "analysis_template"
    - from: src/scripts/seedAnalysisTemplates.ts
      to: src/lib/db/schema.ts
      via: "seed resolves stable template keys before inserting version 1 and never updates immutable content"
      pattern: "analysisTemplateVersion"
    - from: src/lib/db/analysisSchema.integration.test.ts
      to: pg_indexes
      via: "live metadata query asserts additive tables and exact partial predicate"
      pattern: "analysis_run_active_subject_template_idx"
---

<objective>
Add the v1.7 relational foundation and seed exactly the two locked active GBS templates with immutable initial versions.

Purpose: Establish additive storage for template lifecycle, immutable versions, run identity/snapshots, and append-only history while preserving every legacy and Phase 31 proof surface (D-32-01, D-32-02, D-32-03, D-32-04, D-32-06).
Output: Schema, reviewed migration/push, idempotent seed script, and live integration checks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-template-snapshot-run-ledger/32-CONTEXT.md
@.planning/phases/32-template-snapshot-run-ledger/32-RESEARCH.md
@.planning/phases/32-template-snapshot-run-ledger/32-01-SUMMARY.md
@src/lib/db/schema.ts
@drizzle.config.ts

<interfaces>
Use the existing `catalogStatusEnum` convention for template lifecycle fields and the existing serial integer Company/Persona/Practice Area identities. `companySignal` and `personaSignal` use `status = 'active'|'draft'|'retired'`; `workflowProofRun` and `workflowProofRunEvent` are separate Phase 31 proof structures and must not gain an analysis foreign key.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add additive template/version/run/event schema and migration</name>
  <files>src/lib/db/schema.ts, drizzle/0001_phase32_template_snapshot_run_ledger.sql, drizzle/meta/0001_snapshot.json, drizzle/meta/_journal.json, src/lib/db/analysisSchema.integration.test.ts</files>
  <action>Per D-32-01, D-32-02, D-32-03, D-32-04, and D-32-06, add new enums/tables only: stable template key/name/target type/lifecycle/audit fields; immutable version number/instruction/supported effort/default/created actor-time; analysis run scalar `templateId`, `templateVersionId`, `subjectType`, `subjectId`, required `practiceAreaId`, status, bounded attempt fields, actor/time/terminal fields, and immutable JSONB `templateSnapshot`, `subjectSnapshot`, `checklistSnapshot`, `executionSnapshot`, and `policySnapshot`; append-only event rows with from/to status, actor kind/id, safe reason, attempt, timestamp, and unique replay identity. Persist the exact Wave 0 contracts: `standard` effort, future budget `{ maxAttempts: 2, maxToolCalls: 12, maxExecutionSeconds: 300, maxSpendUsd: 2.5 }`, and Phase 32 no-op effective policy `{ schemaVersion: 1, mode: 'phase32_noop', networkAccess: false, writesAllowed: false, effectiveMaxAttempts: 1, effectiveMaxToolCalls: 0, effectiveMaxExecutionSeconds: 5, effectiveMaxSpendUsd: 0 }`. Add FKs to template/version/practice area only where valid, preserve polymorphic subject discriminator, and define one partial unique index whose predicate is exactly status IN (`queued`, `running`, `pending_review`) over subjectType, subjectId, and templateId. Do not alter `workflow_proof_status`, `workflow_proof_run`, `workflow_proof_run_event`, `agent_run`, `signal_proposal`, `correction`, Reviews, or their relations. The executable schema test must inspect `pg_indexes`/catalog metadata for the exact index predicate and assert the migration text contains only additive Phase 32 creates with no DROP/ALTER of legacy/proof objects. Generate/review the migration, reject destructive/unrelated SQL, and apply the additive migration using the repository's existing DB command only after inspection.</action>
  <verify><automated>if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 32 migration evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run db:push &amp;&amp; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/lib/db/analysisSchema.integration.test.ts</automated></verify>
  <done>The configured database contains only the four additive Phase 32 concerns; executable metadata tests prove the exact partial predicate, expected enum/table/index names, required snapshot columns, and unchanged legacy/proof tables.</done>
</task>

<task type="auto">
  <name>Task 2: Seed exactly two active immutable template versions</name>
  <files>src/scripts/seedAnalysisTemplates.ts, src/scripts/seedAnalysisTemplates.integration.test.ts</files>
  <action>Implement an idempotent seed using stable natural keys `company-buying-signal-analysis` and `persona-buying-signal-analysis`, each targeting its matching Company/Persona subject kind, with one active template row and immutable version 1 containing the locked GBS instruction/default effort contract. Resolve generated IDs by key; insert missing version 1, fail on conflicting existing target/content, and never update or delete an existing version. Use the existing `seed-script` actor convention and a live test that asserts exactly two active templates, two version rows, correct target types, rerun stability, and conflict detection. Do not add admin edit/activation/retirement behavior or modify the existing GBS data seed except where a read-only fixture is needed.</action>
  <verify><automated>if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 32 seed evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/scripts/seedAnalysisTemplates.integration.test.ts</automated></verify>
  <done>Seed reruns preserve the same two active templates and immutable versions, and an attempted content conflict fails rather than mutating history.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → production database | Generated SQL could accidentally alter legacy or proof data. |
| seed input → immutable catalog | A rerun could overwrite a version already used by a run. |
| concurrent run inserts → duplicate index | Two starts can race before either sees the other row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Tampering | Phase 32 migration | mitigate | Review generated SQL for additive-only enums/tables/indexes and fail on legacy/proof drops or alterations before `db:push`. |
| T-32-05 | Repudiation | template version seed | mitigate | Stable keys, insert-only version 1, conflict failure, and integration rerun assertions preserve immutable history. |
| T-32-06 | Denial of Service / Tampering | partial unique index | mitigate | Database partial unique index covers all three locked nonterminal statuses; concurrent integration test proves one winner. |
| T-32-SC | Tampering | migration/seed scripts | mitigate | No dependency installs, no secrets in SQL or seed fixtures, and no legacy table writes. |
</threat_model>

<verification>
Run the fail-fast test-database migration command, then the focused metadata and seed integration suites. `DATABASE_URL` and `TEST_DATABASE_URL` must both be explicitly set from the same non-empty `TEST_DATABASE_URL`; `.env.local` is not trusted for Phase 32 migration evidence. The executable metadata test must prove the partial predicate includes `queued`, `running`, and `pending_review`, and that legacy/proof table definitions remain present and unchanged.
</verification>

<success_criteria>
- Exactly two active typed GBS templates and immutable version 1 rows exist.
- The schema supports later lifecycle management without implementing Phase 36 management behavior.
- No legacy analysis/proposal/review or Phase 31 proof structure is repurposed or modified.
</success_criteria>

<output>
Create `.planning/phases/32-template-snapshot-run-ledger/32-02-SUMMARY.md` when done.
</output>

## Execution Status

- [x] Task 1: Additive template/version/run/event schema and migration — live `db:push` and metadata/concurrency integration tests pass.
- [x] Task 2: Seed exactly two active immutable template versions — live idempotency and conflict-detection integration tests pass.
