---
phase: 32-template-snapshot-run-ledger
plan: 04
type: execute
wave: 3
depends_on: [32-01, 32-02, 32-03]
files_modified:
  - src/lib/db/queries/analysisRuns.ts
  - src/lib/db/queries/analysisRuns.test.ts
  - src/lib/db/queries/analysisRuns.integration.test.ts
  - src/app/api/analysis-runs/route.ts
  - src/app/api/analysis-runs/[id]/route.ts
  - src/app/api/analysis-runs/route.test.ts
  - src/workflows/analysisRun.ts
  - src/workflows/analysisRun.integration.test.ts
autonomous: true
requirements: [RUN-01, RUN-02, RUN-05, RUN-06]
must_haves:
  truths:
    - "A staff create request persists a queued immutable run before dispatch and returns its scalar application run ID without waiting for completion."
    - "Every legal queued→running→completed/failed/cancelled and completed→pending_review→confirmed/dismissed transition is guarded and atomically appends one actor/timestamp event."
    - "Replayed transitions and concurrent duplicate starts produce no duplicate history; SQLSTATE 23505 maps to active_run_exists, with queued/running/pending_review explicit."
    - "Invalid, dispatch-failed, failed, timed-out, cancelled, and successful outcomes remain safe terminal/audited records with bounded attempts."
    - "RUN-05 limits are explicit and enforced: future budget maxAttempts 2/maxToolCalls 12/maxExecutionSeconds 300/maxSpendUsd 2.5, while the Phase 32 no-op handoff uses effective maxAttempts 1/maxToolCalls 0/maxExecutionSeconds 5/maxSpendUsd 0."
    - "The selected Phase 31 durable executor receives only the scalar analysis run ID and performs no modelFactory, Firecrawl, evidence, or Reviews work."
  artifacts:
    - path: src/lib/db/queries/analysisRuns.ts
      provides: "Create, guarded atomic transitions, duplicate mapping, safe outcomes, and ordered history reads"
      exports: [createAnalysisRun, transitionAnalysisRun, listAnalysisRunEvents, getAnalysisRun]
    - path: src/app/api/analysis-runs/route.ts
      provides: "Auth-first staff create/dispatch boundary"
      exports: [POST]
    - path: src/app/api/analysis-runs/[id]/route.ts
      provides: "Auth-first database-authoritative status/history boundary"
      exports: [GET]
    - path: src/workflows/analysisRun.ts
      provides: "Thin scalar durable handoff with no real analysis execution"
      exports: [analysisRun]
  key_links:
    - from: src/lib/db/queries/analysisRuns.ts
      to: src/lib/analysis/contracts.ts
      via: "transition guard and outcome mapping use shared status/reason contracts"
      pattern: "AnalysisRunStatus"
    - from: src/lib/db/queries/analysisRuns.ts
      to: .planning/phases/32-template-snapshot-run-ledger/32-TRANSACTION-PROBE.md
      via: "implementation uses the probed single-statement SQL CTE mechanism; db.transaction is unsupported"
      pattern: "atomic"
    - from: src/app/api/analysis-runs/route.ts
      to: src/workflows/analysisRun.ts
      via: "start receives exactly [applicationRunId] after queued insert"
      pattern: "start(analysisRun"
    - from: src/app/api/analysis-runs/[id]/route.ts
      to: src/lib/db/queries/analysisRuns.ts
      via: "GET reads application status and ordered event history, never Workflow status"
      pattern: "listAnalysisRunEvents"
---

<objective>
Implement the database-authoritative analysis run ledger, guarded event history, duplicate protection, staff-gated API boundaries, and scalar durable handoff.

Purpose: Make on-demand runs reload-safe and auditable while preserving the Phase 31 executor boundary and deferring real execution/review to later phases (D-32-02, D-32-04, D-32-06).
Output: Query/state machine, route tests, concurrency integration tests, and a no-op durable handoff with lifecycle coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-template-snapshot-run-ledger/32-CONTEXT.md
@.planning/phases/32-template-snapshot-run-ledger/32-RESEARCH.md
@.planning/phases/32-template-snapshot-run-ledger/32-01-SUMMARY.md
@.planning/phases/32-template-snapshot-run-ledger/32-02-SUMMARY.md
@.planning/phases/32-template-snapshot-run-ledger/32-03-SUMMARY.md
@.planning/phases/31-durable-executor-selection-validation/31-02-SUMMARY.md

<interfaces>
Use `requireStaffAccess()` as the first operation in both route handlers, as in Phase 31. Query modules remain pure DB access. The Phase 31 Workflow boundary uses `start(workflowProof, [applicationRunId])`, returns immediately, and treats the database as product truth; mirror that shape with the new analysis run ID only.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement the atomic run ledger and guarded state machine</name>
  <files>src/lib/db/queries/analysisRuns.ts, src/lib/db/queries/analysisRuns.test.ts, src/lib/db/queries/analysisRuns.integration.test.ts</files>
  <behavior>
    - Create inserts one immutable snapshot-bearing queued run and its queued event.
    - Legal transitions succeed; illegal expected-status transitions return the current row without an event; terminal rows cannot be reset.
    - Every winning transition has exactly one append-only event with actor, timestamp, from/to status, reason, and bounded attempt metadata.
    - Concurrent same subject/template starts yield one success and one safe `active_run_exists` from SQLSTATE 23505; queued, running, and pending_review all block duplicates, confirmed/dismissed do not.
    - Failed, timed-out, cancelled, invalid, dispatch-failed, completed, confirmed, and dismissed outcomes retain safe audit records and bounded attempts.
  </behavior>
  <action>Implement query-layer create/get/history/transition helpers using the single-statement SQL CTE mechanism proven by Plan 32-01; the unsupported `db.transaction` API must not appear in this path. Each guarded transition statement must conditionally update the run and insert its append-only event atomically, with no independent update-then-insert requests. Persist both the future execution budget and no-op effective policy from the immutable snapshot; enforce no-op `maxAttempts = 1`, `maxToolCalls = 0`, `maxExecutionSeconds = 5`, and `maxSpendUsd = 0` in the synthetic handoff, while retaining the future budget values for Phase 33 consumption. Guard every update with expected current status (and lease/attempt predicates where relevant), make event identity replay-safe, and never expose a mutable snapshot update helper. Catch/classify only PostgreSQL unique violation SQLSTATE `23505` at the boundary that can map it to `active_run_exists`; keep the partial index status list in one shared export used by tests. Use stable server actor labels for workflow/system transitions and Clerk user IDs for staff transitions. No query function may call Clerk or inspect Workflow status. Integration tests must assert each of the four future and four no-op limits, including zero tool calls and zero spend.</action>
  <verify><automated>npm test -- src/lib/db/queries/analysisRuns.test.ts &amp;&amp; if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 32 ledger evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/lib/db/queries/analysisRuns.integration.test.ts</automated></verify>
  <done>State-machine, replay, event atomicity, duplicate race, terminal safety, outcome-reason, and bounded-attempt tests pass against the configured database.</done>
</task>

<task type="auto">
  <name>Task 2: Add auth-first create and status/history API boundaries</name>
  <files>src/app/api/analysis-runs/route.ts, src/app/api/analysis-runs/[id]/route.ts, src/app/api/analysis-runs/route.test.ts</files>
  <action>Build POST `/api/analysis-runs` with `requireStaffAccess()` first, Zod parse of the typed create input, server-side catalog/subject/Practice Area/checklist/model/policy resolution, immutable snapshot construction using exact `standard` effort and the resolved future/no-op limits, queued insert before durable dispatch, and an immediate scalar `{ applicationRunId }` response. Map invalid input, missing/mismatched/nonexistent subjects, missing Practice Area, duplicate SQLSTATE 23505, and dispatch failure to stable safe responses while retaining an audited row where creation has begun; never accept client actor/status/snapshot/event/workflow metadata and never return secrets. Build GET `/api/analysis-runs/[id]` with the same auth-first boundary, positive-ID validation, database-authoritative status plus ordered event/history and safe snapshot summary, without reading Workflow status as product truth. Unit-test auth-first ordering, every error contract, create-before-start, duplicate mapping, response redaction, and exact limits.</action>
  <verify><automated>npm test -- src/app/api/analysis-runs/route.test.ts</automated></verify>
  <done>Unauthorized requests cannot reach parsing or DB work, valid starts return promptly after queued persistence, and reload reads expose safe status/history for every lifecycle state.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the scalar Phase 31 durable handoff without Phase 33 work</name>
  <files>src/workflows/analysisRun.ts, src/workflows/analysisRun.integration.test.ts</files>
  <action>Implement a thin Workflow DevKit orchestrator whose only input is the numeric application run ID. It must enforce the Phase 32 no-op effective policy: at most one attempt, zero tool calls, five seconds maximum execution window, and zero spend; it may claim/transition the ledger and record a deterministic no-op handoff outcome needed to prove queue/reload/lifecycle semantics, but must not import or call `runAgent`, `modelFactory`, Firecrawl, provider SDKs, evidence writers, proposal/review actions, or Exa. Use database transitions and safe terminal outcomes only; keep actual model/Firecrawl execution explicitly deferred to Phase 33. Add isolated integration tests for caller-independent queued→running→completed and deterministic failed/timed-out/cancelled audit paths, replayed transitions, database-authoritative GET state, and all four no-op bounds.</action>
  <verify><automated>if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 32 Workflow evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow</automated></verify>
  <done>The durable handoff is scalar, database-authoritative, replay-safe, and synthetic/no-op only; no Phase 33 execution or Phase 34 review behavior exists in this plan.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated/client request → API | Direct route invocation can bypass page-level UI gates. |
| concurrent requests → database uniqueness/state machine | Starts and transitions can race or replay over HTTP/durable retries. |
| API → Workflow executor | Caller may disconnect; executor may retry or receive forged mutable payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-10 | Spoofing/Elevation | analysis run routes | mitigate | `requireStaffAccess()` is literally first in POST and GET; actor identity comes from Clerk, not input. |
| T-32-11 | Tampering/DoS | partial unique index and transitions | mitigate | DB-backed partial unique guard, SQLSTATE 23505 mapping, expected-status predicates, bounded attempts, and atomic event writes. |
| T-32-12 | Repudiation | analysis_run_event | mitigate | Every winning transition records from/to, actor, reason, attempt, and timestamp; replay losers append nothing. |
| T-32-13 | Information Disclosure/Cost abuse | Workflow handoff | mitigate | Scalar ID only; no provider/Firecrawl imports, secrets, evidence, review, or model execution in Phase 32. |
| T-32-SC | Tampering | durable boundary | mitigate | Integration tests prove no-op/safe outcome behavior and database status remains authoritative over executor metadata. |
</threat_model>

<verification>
Run focused query and route tests only after the revised CTE probe passes against `TEST_DATABASE_URL`, then run live duplicate/transition integration tests, the isolated Workflow suite, `npx tsc --noEmit`, and `npm run build`. Confirm no query path uses `db.transaction` or independent update/event requests, and all nonterminal statuses are explicit in schema, index, mapping, and tests.
</verification>

<success_criteria>
- All RUN-01/RUN-02/RUN-05/RUN-06 lifecycle and audit truths survive API reload and concurrent/replayed calls.
- A durable scalar handoff exists without any real AI/Firecrawl/review execution.
- Every invalid, failed, timeout, cancelled, and success path is safe, bounded, and auditable.
</success_criteria>

<output>
Create `.planning/phases/32-template-snapshot-run-ledger/32-04-SUMMARY.md` when done.
</output>

## Execution Status

- [x] Task 1: Atomic run ledger and guarded state machine — unit 11/11 and live integration 9/9 pass; typecheck passes.
- [x] Task 2: Auth-first create and status/history API boundaries — focused route tests 16/16, typecheck, and build pass.
- [x] Task 3: Scalar Phase 31 durable handoff without Phase 33 work — isolated workflow suite 13/13 and typecheck pass.
