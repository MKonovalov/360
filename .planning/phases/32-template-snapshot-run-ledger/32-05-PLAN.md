---
phase: 32-template-snapshot-run-ledger
plan: 05
type: execute
wave: 4
depends_on: [32-03, 32-04]
files_modified:
  - src/components/analysis/analysis-run-launcher.tsx
  - src/components/analysis/analysis-run-status.tsx
  - src/components/companies/company-detail.tsx
  - src/components/personas/persona-detail.tsx
  - e2e/analysis-runs.spec.ts
  - .planning/phases/32-template-snapshot-run-ledger/32-VERIFICATION.md
autonomous: true
requirements: [RUN-01, RUN-02]
must_haves:
  truths:
    - "A staff member can minimally launch a compatible Company or Persona run from its existing detail surface and see the returned run identity."
    - "After navigation away and reload, the minimum status/history surface reads the application database and displays queued/running/terminal/review states with actor and timestamp events."
    - "The UI exposes safe error copy for mismatch, missing subject/Practice Area, duplicate active run, and dispatch failure without leaking raw SQL, secrets, or private reasoning."
    - "The launcher imports no DB query module; it fetches active compatible templates/versions and Practice Areas from the staff-gated options endpoint."
    - "The Phase 32 gate proves migration/seed, contract, checklist/snapshot, ledger, API, workflow, and authenticated reload behavior without invoking Phase 33/34/35/36 features."
  artifacts:
    - path: src/components/analysis/analysis-run-launcher.tsx
      provides: "Minimum typed launch control for existing Company/Persona detail surfaces"
      contains: "analysis-runs"
    - path: src/components/analysis/analysis-run-status.tsx
      provides: "Reload-safe status and ordered event history presentation"
      contains: "pending_review"
    - path: e2e/analysis-runs.spec.ts
      provides: "Authenticated navigation/reload proof for both target kinds and safe errors"
      contains: "analysis-runs"
    - path: .planning/phases/32-template-snapshot-run-ledger/32-VERIFICATION.md
      provides: "Final Phase 32 automated/migration/seed/reload evidence ledger"
      contains: "CON-01"
  key_links:
    - from: src/components/companies/company-detail.tsx
      to: src/components/analysis/analysis-run-launcher.tsx
      via: "Company detail passes subjectType company and scalar id"
      pattern: "AnalysisRunLauncher"
    - from: src/components/personas/persona-detail.tsx
      to: src/components/analysis/analysis-run-launcher.tsx
      via: "Persona detail passes subjectType persona and scalar id"
      pattern: "AnalysisRunLauncher"
    - from: src/components/analysis/analysis-run-status.tsx
      to: /api/analysis-runs/[id]
      via: "status/history reload reads the database-authoritative endpoint"
      pattern: "api/analysis-runs"
    - from: src/components/analysis/analysis-run-launcher.tsx
      to: /api/analysis-options
      via: "client fetches read-only active template/Practice Area options"
      pattern: "api/analysis-options"
---

<objective>
Deliver only the minimum launch/status/history surface and final automated gate needed to prove reload-safe Phase 32 behavior.

Purpose: Make RUN-01/RUN-02 observable from both existing target types without pulling Phase 35 polish, Phase 34 review, or Phase 33 execution into the foundation (D-32-02, D-32-06).
Output: Small client components, two detail-surface integrations, authenticated E2E coverage, and the final evidence ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-template-snapshot-run-ledger/32-CONTEXT.md
@.planning/phases/32-template-snapshot-run-ledger/32-RESEARCH.md
@.planning/phases/31-durable-executor-selection-validation/31-03-SUMMARY.md
@src/components/companies/company-detail.tsx
@src/components/personas/persona-detail.tsx
@playwright.config.ts

<interfaces>
Existing CompanyDetail and PersonaDetail receive a numeric `id` and are rendered inside the consolidated explorer lists. Existing Playwright uses `e2e/auth.setup.ts`, real Clerk storage state, and deployed-origin `E2E_BASE_URL`; reuse that harness. The API contract from Plan 32-04 returns scalar application ID on create and database status/events on GET.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the minimum typed launch control to Company and Persona details</name>
  <files>src/components/analysis/analysis-run-launcher.tsx, src/components/companies/company-detail.tsx, src/components/personas/persona-detail.tsx</files>
  <action>Build a small client launcher that receives an explicit `subjectType` (`company` or `persona`) and numeric subject ID, fetches `/api/analysis-options?subjectType=...`, selects only the returned active compatible template/version and active Practice Area options, submits one on-demand create request, shows the returned application run ID/status link, and renders generic safe error copy for invalid/mismatch/missing/duplicate/dispatch outcomes. The client component must not import `src/lib/db/*`, `src/lib/analysis/*` query modules, or server-only env/config. Integrate it into both existing detail components without changing their existing data fetches or adding polished preview/history layouts. Do not add bulk/scheduled/rerun controls, per-finding UI, review actions, template editing, or model/provider controls.</action>
  <verify><automated>npm test -- src/app/api/analysis-options/route.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>Both existing detail surfaces can initiate only a typed compatible on-demand run and cannot leak raw backend errors or invoke later-phase behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add reload-safe status and ordered audit history display</name>
  <files>src/components/analysis/analysis-run-status.tsx, src/components/analysis/analysis-run-launcher.tsx</files>
  <action>Implement the minimum status component that fetches `/api/analysis-runs/{id}` after launch and on page reload, displays the database status and safe terminal/review outcome, and renders ordered event action/actor/timestamp/reason history. Preserve the application run ID and never poll or derive product state from Workflow metadata. Make all eight statuses renderable, including pending_review, confirmed, and dismissed, while keeping terminal failure/timed-out/cancelled copy bounded and non-sensitive.</action>
  <verify><automated>npm test -- src/app/api/analysis-runs/route.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>Status/history can be reloaded independently of the initiating component and shows actor/timestamp audit for every lifecycle state.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the Phase 32 surface and record the final gate</name>
  <files>e2e/analysis-runs.spec.ts, .planning/phases/32-template-snapshot-run-ledger/32-VERIFICATION.md</files>
  <action>Reuse the Phase 31 authenticated Playwright setup to test both Company and Persona typed launch paths, active template/Practice Area options loading, immediate navigation away, reload/status polling, ordered audit history, empty checklist acceptance, Company/Persona mismatch rejection, nonexistent subject, duplicate active-run response, exact no-op limits, and safe terminal outcomes. Add a final evidence ledger mapping CON-01..05, RUN-01..02, RUN-05..06 to focused tests, migration/seed metadata checks, the workflow gate, and the authenticated E2E proof. Explicitly record that no modelFactory, Firecrawl, Exa, Review, admin template, legacy migration, bulk/scheduled, or polished Phase 35 behavior was invoked. Run the full phase gate only after the focused Wave 0/1/2/3 gates are green.</action>
  <verify><automated>if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 32 final evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run db:push &amp;&amp; npm test -- src/lib/analysis/contracts.test.ts src/lib/analysis/checklist.test.ts src/lib/analysis/snapshots.test.ts src/lib/db/queries/analysisTemplates.test.ts src/app/api/analysis-options/route.test.ts src/lib/db/queries/analysisRuns.test.ts src/app/api/analysis-runs/route.test.ts &amp;&amp; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/lib/db/analysisSchema.integration.test.ts &amp;&amp; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/scripts/seedAnalysisTemplates.integration.test.ts src/lib/db/queries/analysisRuns.integration.test.ts &amp;&amp; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow &amp;&amp; npm run build &amp;&amp; npm run e2e -- e2e/analysis-runs.spec.ts</automated></verify>
  <done>Authenticated navigation/reload proof and the final evidence ledger cover every in-scope requirement and explicitly exclude all deferred phases.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated browser → launch/status API | UI calls can be replayed or crafted outside the page. |
| API response → client rendering | Backend errors/history may contain SQL, credentials, or private reasoning if not redacted. |
| deployed browser → durable executor | Browser navigation must not own progress or product status. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-14 | Spoofing/Elevation | launcher and E2E | mitigate | API remains auth-first; E2E uses real Clerk setup, never cookie injection or client actor IDs. |
| T-32-15 | Information Disclosure | status/history UI | mitigate | Render only safe API fields/reason codes; never show raw SQL, secrets, workflow internals, or private reasoning. |
| T-32-16 | Tampering/DoS | navigation/reload flow | mitigate | E2E proves application DB status/history continues independently and duplicate starts remain DB-rejected. |
| T-32-SC | Cost abuse | Phase 32 UI/workflow | mitigate | E2E and source-scope checks prove no real model, Firecrawl, Exa, review, bulk, or scheduled execution is reachable. |
</threat_model>

<verification>
Focused Phase 32 suites, the explicit test-database migration/seed checks, `npx tsc --noEmit`, the required database-backed Workflow gate, production build, and authenticated local/deployed-origin Playwright reload smoke must all be green. The ordinary repository-wide `npm test` command is a known unrelated baseline with non-green tests documented by Phase 31; it is informational only and must not replace or weaken any Phase 32 focused test. `TEST_DATABASE_URL` absence must fail the required migration and Workflow commands rather than skip them.
</verification>

<success_criteria>
- Staff can minimally launch and reload-observe Company and Persona runs with safe status/history.
- All CON-01..05, RUN-01..02, RUN-05..06 evidence is recorded and reproducible.
- No out-of-scope Phase 33, 34, 35, 36, legacy migration, bulk/scheduled, or Exa behavior is planned or invoked.
</success_criteria>

<output>
Create `.planning/phases/32-template-snapshot-run-ledger/32-05-SUMMARY.md` when done.
</output>

## Execution Status

- [x] Task 1: Typed launch control on Company and Persona details — automated checks and fresh visual QA reviewers approve.
- [x] Task 2: Reload-safe status and ordered audit history display — focused route tests and TypeScript check pass.
- [x] Task 3: Authenticated Phase 32 gate and final evidence ledger — final gate passed after serializing the shared disposable-database seed/ledger files; no Phase 33 execution was invoked.
