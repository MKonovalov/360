---
phase: 34-whole-run-review-confirmed-candidates
plan: 02
type: execute
wave: 1
depends_on: [34-01]
files_modified:
  - src/lib/db/queries/analysisReviews.ts
  - src/lib/db/queries/analysisReviews.test.ts
  - src/lib/db/queries/analysisReviews.integration.test.ts
  - src/lib/db/queries/confirmedCandidates.ts
  - src/lib/db/queries/confirmedCandidates.test.ts
  - src/lib/db/queries/confirmedCandidates.integration.test.ts
autonomous: true
requirements: [REV-01, REV-02, REV-03, REV-04, REV-05]
must_haves:
  truths:
    - "Every completed run with a visible immutable packet reconciles to one pending-review item, and a missing packet never becomes reviewable."
    - "Confirm/Dismiss is one Neon-http-safe atomic winner-preserving operation with one decision row and one staff lifecycle event; replay and race losers return the original decision."
    - "Candidate reads are read-only positive projections from confirmed runs, persisted source links, and existing polymorphic links, with active offerings by default and preserved provenance identity."
    - "All non-confirmed lifecycle statuses, unsupported finding strengths, source-less findings, and expired Persona packets are excluded."
  artifacts:
    - path: src/lib/db/queries/analysisReviews.ts
      provides: "Packet-required reconciliation, one-row review listing, atomic decision, and replay result queries"
      exports: ["reconcileCompletedRunForReview", "listRunReviewItems", "decideAnalysisRun"]
    - path: src/lib/db/queries/confirmedCandidates.ts
      provides: "Confirmed-only Company/Persona offering projection"
      exports: ["listConfirmedCandidateOfferings"]
    - path: src/lib/db/queries/analysisReviews.integration.test.ts
      provides: "Neon concurrency, replay, lifecycle, and packet immutability evidence"
      contains: "pending_review"
    - path: src/lib/db/queries/confirmedCandidates.integration.test.ts
      provides: "Confirmed-only, provenance, discriminator, offering-status, exclusion, and retention evidence"
      contains: "confirmed"
  key_links:
    - from: src/lib/db/queries/analysisReviews.ts
      to: src/lib/db/queries/analysisResults.ts
      via: "retention-aware packet/result existence and read boundary"
      pattern: "getAnalysisPacket|analysis_run_result"
    - from: src/lib/db/queries/analysisReviews.ts
      to: src/lib/db/queries/analysisRuns.ts
      via: "guarded lifecycle transition/event semantics"
      pattern: "analysis_run_event|pending_review"
    - from: src/lib/db/queries/confirmedCandidates.ts
      to: src/lib/db/schema.ts
      via: "confirmed run → immutable finding/source link → polymorphic signal link → offering join"
      pattern: "signalType.*signalId|confirmed"
---

<objective>
Implement the database-authoritative review boundary and confirmed-only candidate projection.

Purpose: Fulfill D-34-01 through D-34-04 and REV-01..REV-05 without using legacy proposals, mutating Phase 33 packets, or relying on client filtering.
Output: Pure query modules and guarded Neon integration proof for lifecycle, atomicity, provenance, exclusions, retention, and no-live-write behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-CONTEXT.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-PATTERNS.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-RESEARCH.md
@.planning/phases/33-grounded-analysis-execution-evidence/33-06-SUMMARY.md
@src/lib/db/queries/analysisRuns.ts
@src/lib/db/queries/analysisResults.ts
@src/lib/db/queries/signalOfferingLinks.ts
@src/lib/db/queries/offerings.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reconcile packet completion and implement atomic whole-run decisions</name>
  <files>src/lib/db/queries/analysisReviews.ts, src/lib/db/queries/analysisReviews.test.ts, src/lib/db/queries/analysisReviews.integration.test.ts</files>
  <behavior>
    - `completed` plus an existing visible packet transitions once to `pending_review` and appends one lifecycle event; replay returns the existing item without another event; completed-without-packet is rejected/excluded.
    - Review listing returns one item per packet, never one per finding, with packet counts, target-safe run snapshot data, provenance summary, and current decision state.
    - Concurrent Confirm/Confirm, Dismiss/Dismiss, and Confirm/Dismiss attempts produce exactly one terminal status, one immutable decision row, one winning staff event, and the stored winner for every caller that loses or retries.
    - Confirm and Dismiss leave all packet rows and live Signal/Offering/link rows byte-for-byte unchanged.
  </behavior>
  <action>Build `analysisReviews.ts` as pure DB access. Add a packet-required `completed → pending_review` bridge per D-34-01 without changing `ANALYSIS_RUN_TRANSITIONS` or Phase 33 persistence-before-completion ordering; make it a guarded data-modifying CTE that can be safely called by every review read/action boundary and is idempotent. Implement one review-list query joining run/result/review identity and using the retention-aware Persona visibility boundary. Implement Confirm/Dismiss as one Neon-http-safe CTE that conditionally updates only `pending_review`, inserts the unique immutable decision row with the supplied server actor and packet hash, appends one `staff` `analysis_run_event`, and on no-op/replay/race reads and returns the original winner. Never use `db.transaction()`, `acceptProposal`, `signal_proposal`, `agent_run`, or packet update/delete operations. Lock all behaviors with pure mocked-query tests and guarded `TEST_DATABASE_URL` integration fixtures, including duplicate review items, replay, Confirm-vs-Dismiss race, packet immutability, staff attribution, and missing packet.</action>
  <verify><automated>npm test -- src/lib/db/queries/analysisReviews.test.ts &amp;&amp; if [ -n "$TEST_DATABASE_URL" ]; then npm test -- src/lib/db/queries/analysisReviews.integration.test.ts; else echo 'MISSING — TEST_DATABASE_URL required for Neon lifecycle/concurrency evidence'; exit 1; fi</automated></verify>
  <done>Review items and whole-run decisions are DB-authoritative, packet-preserving, idempotent, race-safe, staff-attributed, and proven without any live catalog write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the confirmed-only provenance candidate projection</name>
  <files>src/lib/db/queries/confirmedCandidates.ts, src/lib/db/queries/confirmedCandidates.test.ts, src/lib/db/queries/confirmedCandidates.integration.test.ts</files>
  <behavior>
    - Only `analysis_run.status = confirmed` with a matching confirmed review identity can reach the projection; queued, running, completed, pending_review, failed, cancelled, and dismissed fixtures return no rows.
    - Only strong/weak findings with persisted finding-source links reach candidates; no_evidence, inconclusive, missing links, and missing/expired Persona packets return no rows.
    - Join to `signal_offering_link` uses both the target/signal discriminator and snapshotted signal ID; equal Company/Persona numeric IDs never cross-resolve.
    - Candidate rows retain target type/subject ID, offering ID/status, run/result/packet hash, finding IDs, signal type/ID, source/link IDs and safe source metadata, review actor/time, and deterministic duplicate provenance.
    - Active offerings are returned by default; retired/draft historical identity is not presented as active and is retained only through the explicit historical provenance shape. No query writes any row.
  </behavior>
  <action>Implement a read-only `listConfirmedCandidateOfferings` query using the D-34-04 join direction: confirmed run and confirmed review identity → immutable result/finding/source/link rows → `signal_offering_link` on both `signal_type` and `signal_id` → offering. Apply the positive confirmed predicate in SQL, route Persona visibility through the retention-aware packet rule, and use snapshot signal identity rather than current signal names. Keep duplicates as deterministic evidence rows or an ordered provenance array; never group away run/finding/source identity. Reuse existing active-offering semantics and expose historical link/identity fields without creating or updating catalog rows. Add unit and guarded Neon fixtures for every required status, strength/source, discriminator collision, active/draft/retired semantics, duplicate provenance, Company/Persona targets, and retention case. Add before/after assertions for Signals, Persona/Company Signals, links, and offerings to prove read-only behavior.</action>
  <verify><automated>npm test -- src/lib/db/queries/confirmedCandidates.test.ts &amp;&amp; if [ -n "$TEST_DATABASE_URL" ]; then npm test -- src/lib/db/queries/confirmedCandidates.integration.test.ts; else echo 'MISSING — TEST_DATABASE_URL required for confirmed-only aggregation evidence'; exit 1; fi</automated></verify>
  <done>Phase 35 has a stable, read-only Company/Persona candidate query contract whose SQL itself excludes every non-confirmed or non-source-backed path and preserves provenance.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Review action/query → Neon | Concurrent untrusted attempts race on lifecycle and decision rows. |
| Immutable packet → review/candidate reads | Persona retention and packet identity constrain evidence visibility. |
| Snapshot signal identity → polymorphic catalog link | Company and Persona integer IDs can collide. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-04 | Tampering | reconciliation/decision CTE | mitigate | Packet-required guarded status predicates, unique decision identities, one CTE, and winner-preserving replay. |
| T-34-05 | Repudiation | lifecycle/decision audit | mitigate | Append one event with `actor_kind=staff`, server Clerk actor ID, timestamp, and packet hash. |
| T-34-06 | Information disclosure | Persona packet reads | mitigate | Reuse retention-aware result visibility and exclude expired/tombstoned artifacts in SQL. |
| T-34-07 | Tampering | candidate join | mitigate | Require positive confirmed status and join `signal_type + signal_id`; candidate module has no write imports. |
| T-34-08 | Information disclosure | provenance aggregation | mitigate | Persisted source links only; omit raw/private reasoning and preserve safe source projection. |
| T-34-SC | Tampering | package/runtime surface | mitigate | No dependency/provider install; static scope audit in Plan 34-04. |
</threat_model>

<verification>
Run focused pure tests and both guarded Neon integration suites with `TEST_DATABASE_URL`. Inspect query imports/SQL to confirm this plan has no legacy/live-catalog write path and no interactive transaction.
</verification>

<success_criteria>
The database boundary proves one review winner and a confirmed-only, provenance-backed, discriminator-safe candidate projection under retries, races, retention, and every excluded lifecycle status.
</success_criteria>

<output>
Create `.planning/phases/34-whole-run-review-confirmed-candidates/34-02-SUMMARY.md` when done.
</output>
