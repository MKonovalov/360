---
phase: 34-whole-run-review-confirmed-candidates
plan: 04
type: execute
wave: 3
depends_on: [34-03]
files_modified:
  - scripts/phase34-scope-audit.ts
  - scripts/phase34-scope-audit.test.ts
  - .planning/phases/34-whole-run-review-confirmed-candidates/34-VALIDATION.md
  - .planning/phases/34-whole-run-review-confirmed-candidates/34-VERIFICATION.md
autonomous: false
requirements: [REV-01, REV-02, REV-03, REV-04, REV-05]
must_haves:
  truths:
    - "Automated evidence proves lifecycle bridge/review uniqueness, atomic decision races/replays, packet immutability, confirmed-only aggregation, discriminator-safe provenance, Persona retention, auth, and legacy preservation."
    - "A tracked-source scope audit reports no Phase 34 provider calls, legacy proposal reuse, live Signal/Offering writes, packet mutation, Phase 35/36 scope leakage, or forbidden dependency changes."
    - "Authenticated fixture-only UAT proves the shared Reviews flow and candidate projection without invoking live providers or Firecrawl."
  artifacts:
    - path: scripts/phase34-scope-audit.ts
      provides: "Tracked-source Phase 34 boundary audit"
      contains: "findingCount"
    - path: scripts/phase34-scope-audit.test.ts
      provides: "Zero-finding audit regression test"
      contains: "toEqual"
    - path: .planning/phases/34-whole-run-review-confirmed-candidates/34-VERIFICATION.md
      provides: "Sanitized requirement evidence ledger and deferred live-provider note"
      contains: "policy_or_credentials_unavailable"
  key_links:
    - from: scripts/phase34-scope-audit.ts
      to: src/app/actions/reviews.ts
      via: "tracked source scan for whole-run write/auth boundaries"
      pattern: "reviews|acceptProposal|signal_offering_link"
    - from: .planning/phases/34-whole-run-review-confirmed-candidates/34-VERIFICATION.md
      to: .planning/phases/34-whole-run-review-confirmed-candidates/34-VALIDATION.md
      via: "command evidence mapped to REV-01..REV-05 and UAT matrix"
      pattern: "REV-0[1-5]"
---

<objective>
Close Phase 34 with an adversarial automated gate, tracked-source scope audit, sanitized verification ledger, and authenticated fixture-only UAT.

Purpose: Demonstrate D-34-07 and all roadmap success criteria without treating Phase 33's deferred live smoke as approval or expanding into Phase 35/36.
Output: Scope audit, automated evidence record, and human UAT confirmation over packet fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-CONTEXT.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-PATTERNS.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-VALIDATION.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-RESEARCH.md
@.planning/phases/33-grounded-analysis-execution-evidence/33-VERIFICATION.md
@scripts/phase33-scope-audit.ts
@scripts/phase33-scope-audit.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Run the Phase 34 adversarial gate and scope audit</name>
  <files>scripts/phase34-scope-audit.ts, scripts/phase34-scope-audit.test.ts, .planning/phases/34-whole-run-review-confirmed-candidates/34-VALIDATION.md, .planning/phases/34-whole-run-review-confirmed-candidates/34-VERIFICATION.md</files>
  <action>Adapt the established tracked-source audit pattern into a Phase 34-specific audit that scans only the new review/candidate-owned paths plus schema/query/action/UI/test/scripts changes. Fail on reuse of `agent_run`, `signal_proposal`, legacy proposal Accept, writes to Signal/Company Signal/Persona Signal/Offering/`signal_offering_link`, Phase 33 packet updates/deletes, provider/Firecrawl calls, new dependencies, per-finding/bulk/scheduled/auto-confirmation work, Phase 35 target-record UX, or Phase 36 template/E2E scope. Run the complete focused unit/action/component suite, guarded schema/review/candidate integration suites with `TEST_DATABASE_URL`, scope audit, `npx tsc --noEmit`, and `npm run build`; record sanitized counts/statuses and missing-environment failures in `34-VERIFICATION.md`. Include the exact adversarial matrix from `34-VALIDATION.md`: duplicate review items, Confirm-vs-Dismiss race, replay, packet immutability, signal/persona ID collision, every excluded lifecycle status, active/retired/draft semantics, Persona retention, auth/scope, and no-live-provider evidence. Never print secrets, raw provider/database errors, or claim live smoke success.</action>
  <verify><automated>npm exec tsx -- scripts/phase34-scope-audit.ts &amp;&amp; npm exec vitest run --config vitest.config.ts --include scripts/phase34-scope-audit.test.ts &amp;&amp; npm test -- src/lib/analysis/reviewContracts.test.ts src/lib/db/reviewSchema.integration.test.ts src/lib/db/queries/analysisReviews.test.ts src/lib/db/queries/confirmedCandidates.test.ts src/app/actions/reviews.test.ts src/components/reviews/run-review-card.test.tsx &amp;&amp; if [ -n "$TEST_DATABASE_URL" ]; then npm test -- src/lib/db/queries/analysisReviews.integration.test.ts src/lib/db/queries/confirmedCandidates.integration.test.ts; else echo 'MISSING — TEST_DATABASE_URL required for Phase 34 database evidence'; exit 1; fi &amp;&amp; npx tsc --noEmit &amp;&amp; npm run build</automated></verify>
  <done>Automated contract, database, concurrency, retention, scope, typecheck, and build evidence is recorded per REV-01..REV-05, with live provider execution explicitly absent/deferred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify authenticated Reviews and candidate projection with fixtures</name>
  <files>.planning/phases/34-whole-run-review-confirmed-candidates/34-VERIFICATION.md</files>
  <action>After Task 1 passes, execute the authenticated fixture-only UAT below and append sanitized pass/fail evidence to `34-VERIFICATION.md`. Do not run Analyze, call a provider, or convert the deferred Phase 33 smoke into approval.</action>
  <what-built>Staff-gated shared Reviews page with additive run-level packet review, one whole-run Confirm/Dismiss decision, and read-only confirmed candidate projection backed by seeded Company/Persona packet fixtures.</what-built>
  <how-to-verify>
    1. Use the existing authenticated Playwright/local setup and sign in as staff; do not launch Analyze or invoke a provider.
    2. Open `/reviews`. Confirm the legacy proposal queue is still present and its existing Accept/Reject behavior is unchanged. Confirm one completed packet appears exactly once in the separate v1.7 run-level section.
    3. Expand the packet and verify safe target type/ID, packet counts/hash, strong/weak findings, persisted canonical source links, finding/source provenance IDs, and lifecycle audit state. Verify no private reasoning or expired Persona artifact is shown.
    4. Confirm the fixture from two tabs/replayed requests; verify one terminal decision, original actor/time, no duplicate event/decision, and unchanged packet and live Signal/link counts. Repeat with a Dismiss fixture and verify it produces no candidate rows.
    5. Exercise the confirmed-candidate read surface for Company and Persona fixtures. Verify only confirmed source-backed strong/weak evidence appears, equal Company/Persona signal IDs remain separate, active offerings are displayed by default, and historical link identity/provenance is retained without a catalog write.
    6. Record pass/fail and any screenshots or sanitized counts in `34-VERIFICATION.md`. If auth or `TEST_DATABASE_URL` is unavailable, record the blocked evidence; do not substitute live provider execution or mark it passed.
  </how-to-verify>
  <verify><automated>MISSING — authenticated Clerk/Playwright UAT requires the human checkpoint; use Task 1 automated gate for non-interactive evidence.</automated></verify>
  <done>Authenticated staff UAT records approval that legacy proposals remain intact, one packet can be inspected and decided once, packet/catalog rows remain unchanged, and confirmed-only provenance projection excludes all forbidden fixtures.</done>
  <resume-signal>Type "approved" with the authenticated fixture results, or describe the failing invariant.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked source → scope audit | New Phase 34 paths must not widen into providers, legacy writes, live catalog writes, or later phases. |
| Test database → verification ledger | Database evidence must be real, sanitized, and fail closed when `TEST_DATABASE_URL` is absent. |
| Authenticated browser → staff review surface | UAT must prove direct staff gating and no client-trusted decision actor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-34-13 | Tampering | Phase 34 scope audit | mitigate | Fail-fast tracked-source checks for forbidden imports, writes, provider calls, and later-phase paths. |
| T-34-14 | Information disclosure | verification/UAT ledger | mitigate | Record only sanitized statuses/counts; never include secrets, URLs with credentials, raw errors, PII, or packet private reasoning. |
| T-34-15 | Elevation | authenticated UAT | mitigate | Exercise direct page/action boundary with staff and unauthenticated attempts where fixture setup permits. |
| T-34-16 | Availability/Integrity | missing database/provider prerequisites | accept | Fail closed and record unavailable evidence; no live provider fallback is permitted by D-34-07. |
| T-34-SC | Tampering | package/runtime surface | mitigate | No dependency changes; audit manifest and tracked source before final verdict. |
</threat_model>

<verification>
The phase is complete only after Task 1 automated evidence passes and Task 2 authenticated fixture-only UAT is approved. Phase 33 live smoke remains explicitly deferred as `policy_or_credentials_unavailable`.
</verification>

<success_criteria>
All five REV requirements map to passing automated evidence and authenticated UAT, with no live provider execution, no forbidden writes, no packet mutation, and no Phase 35/36 scope leakage.
</success_criteria>

<output>
Create `.planning/phases/34-whole-run-review-confirmed-candidates/34-04-SUMMARY.md` when done.
</output>
