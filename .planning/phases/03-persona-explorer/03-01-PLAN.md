---
phase: 03-persona-explorer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/queries/personas.ts
  - src/lib/db/queries/companyPersonaRoles.ts
  - src/lib/validation/seed.ts
  - src/scripts/seed.ts
  - data/seed/personas.csv
  - data/seed/company_persona_roles.csv
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "The persona table has seniority (enum), email, and linkedinUrl nullable columns live in Neon Postgres"
    - "listPersonas() supports optional search/seniority/currentCompany/hasSignals filters composed with AND, using Drizzle's parameterized operators including a two-hop EXISTS for hasSignals (no raw SQL string interpolation)"
    - "getPersonaById and listCompanyRolesForPersona(personaId) exist for the detail pane plan to consume, returning current + historical company roles"
    - "The seed dataset's 10 personas have seniority (all 5 tiers represented), and 3 personas (Jordan Sample, Taylor Placeholder, Sydney Placeholdt) have both a current role and at least one historical role with a populated end date"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "seniorityEnum pgEnum; persona table extended with seniority, email, linkedinUrl columns"
      contains: "seniorityEnum"
    - path: "src/lib/db/queries/personas.ts"
      provides: "PersonaFilters, listPersonas(filters), getPersonaById(id), listDistinctCurrentCompanyNames()"
      exports: ["listPersonas", "getPersonaById", "listDistinctCurrentCompanyNames", "PersonaFilters"]
    - path: "src/lib/db/queries/companyPersonaRoles.ts"
      provides: "listCompanyRolesForPersona(personaId) reverse-join query"
      exports: ["listCompanyRolesForPersona"]
    - path: "data/seed/personas.csv"
      provides: "10 personas backfilled with seniority/email/linkedin_url"
      min_lines: 11
  key_links:
    - from: "src/lib/db/queries/personas.ts"
      to: "src/lib/db/schema.ts persona table"
      via: "Drizzle select/where against seniorityEnum column and two-hop EXISTS through companyPersonaRole/company/signal"
      pattern: "exists\\("
    - from: "src/scripts/seed.ts"
      to: "src/lib/db/queries/personas.ts persona insert"
      via: "db.insert(persona).values({...}) with seniority/email/linkedinUrl fields"
      pattern: "linkedinUrl"
---

<objective>
Extend the `persona` schema with Phase 3's seniority/contact fields (D-01, D-02, D-03), push the migration to Neon, extend the Drizzle query layer with the filter/lookup functions Plans 02-03 depend on (including D-06's three-way search and D-07's three filters, one requiring a two-hop EXISTS join), and backfill the existing seed dataset (D-08, D-09) so the Persona Explorer has real, varied data to search/filter/display.

Purpose: This is the data foundation the Persona Explorer UI (Plans 02-03) is built on — no list/detail/filter UI can render seniority, career history, or contact info without these columns, query functions, and seed values existing first.
Output: Migrated `persona` table, extended `src/lib/db/queries/personas.ts` and `companyPersonaRoles.ts`, a fully backfilled 10-persona seed dataset with expanded career history loaded into Neon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-persona-explorer/03-CONTEXT.md
@.planning/phases/03-persona-explorer/03-RESEARCH.md
@.planning/phases/03-persona-explorer/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend persona schema and Drizzle query layer</name>
  <files>src/lib/db/schema.ts, src/lib/db/queries/personas.ts, src/lib/db/queries/companyPersonaRoles.ts</files>
  <read_first>
    src/lib/db/schema.ts (current persona table's placeholder comment at line 60, and the revenueBandEnum/ownershipTypeEnum pgEnum pattern at lines 20-37)
    src/lib/db/queries/personas.ts (current listPersonas()/getPersonaByName() — listPersonas has no callers outside this file, so its signature can change freely; getPersonaByName stays unchanged)
    src/lib/db/queries/companies.ts (listCompanies's and(cond ?? undefined, ...) + single-hop exists() pattern at lines 16-48, the exact shape to extend into search/currentCompany/hasSignals legs)
    src/lib/db/queries/companyPersonaRoles.ts (listPersonasForCompany at lines 19-28 — the exact reverse-join shape to mirror)
    .planning/phases/03-persona-explorer/03-RESEARCH.md "Pattern 1" (full listPersonas(filters) composition, including the two-hop EXISTS) and "Pattern 2" (listCompanyRolesForPersona) and "Pitfall 1" (two-hop EXISTS correctness — verify against seed data mentally before finishing this task)
  </read_first>
  <action>
    In src/lib/db/schema.ts: add `seniorityEnum = pgEnum('seniority', ['ic', 'manager', 'director', 'vp', 'c_level'])` (D-01) near revenueBandEnum/ownershipTypeEnum, with a short comment citing D-01 and matching the existing "fixed-but-extensible enum" comment style. Replace the persona table's `// ... remaining Persona fields land in Phase 3 per PERS-01..04` line with three new nullable columns: `seniority: seniorityEnum('seniority')` (D-01), `email: text('email')` (D-02), `linkedinUrl: text('linkedin_url')` (D-02/D-03: stored and later rendered as a full URL as-is, no parsing). Do not add `.notNull()` to any of these — additive nullable columns keep the schema push non-destructive.
    In src/lib/db/queries/personas.ts: replace the imports with `import { and, eq, ilike, exists, or, sql } from 'drizzle-orm';` and `import { persona, companyPersonaRole, company, signal, seniorityEnum } from '../schema';`. Add an exported `PersonaFilters` interface with optional fields `search`, `seniority`, `currentCompany` (all `string`) and `hasSignals` (`boolean`). Change `listPersonas()` to `listPersonas(filters: PersonaFilters = {})`, composing the WHERE clause with `and(cond ?? undefined, ...)`: (a) the search leg (D-06) is `or(ilike(persona.name, ...), ilike(persona.title, ...), exists(...))` where the third leg is a single-hop EXISTS joining `companyPersonaRole` to `company`, filtered to `eq(companyPersonaRole.personaId, persona.id)`, `eq(companyPersonaRole.isCurrent, true)`, and `ilike(company.name, ...)`; (b) the seniority leg (D-07) is `eq(persona.seniority, filters.seniority as (typeof seniorityEnum.enumValues)[number])`; (c) the currentCompany leg (D-07) is a single-hop EXISTS with the same `companyPersonaRole`⋈`company` shape as (a) but matching `eq(company.name, filters.currentCompany)` instead of `ilike`; (d) the hasSignals leg (D-07) is a two-hop EXISTS chaining `companyPersonaRole.innerJoin(company, ...).innerJoin(signal, eq(signal.companyId, company.id))`, filtered to `eq(companyPersonaRole.personaId, persona.id)` and `eq(companyPersonaRole.isCurrent, true)` — RESEARCH.md Pattern 1 has the exact composition to copy verbatim. Keep `getPersonaByName` unchanged. Add `getPersonaById(id: number)` mirroring `getCompanyById`'s convention exactly (returns `rows[0]`, `undefined` if not found, never throws — the detail page decides whether that means `notFound()`). Add `listDistinctCurrentCompanyNames()`: `db.selectDistinct({ name: company.name }).from(companyPersonaRole).innerJoin(company, eq(companyPersonaRole.companyId, company.id)).where(eq(companyPersonaRole.isCurrent, true))` — only companies with an actual current persona should appear as filter options, not a plain distinct over the company table.
    In src/lib/db/queries/companyPersonaRoles.ts: add `company` to the existing schema import (currently `companyPersonaRole, persona`). Add `listCompanyRolesForPersona(personaId: number)`, the reverse of `listPersonasForCompany`: `db.select({ company, role: companyPersonaRole }).from(companyPersonaRole).innerJoin(company, eq(companyPersonaRole.companyId, company.id)).where(eq(companyPersonaRole.personaId, personaId))`.
  </action>
  <acceptance_criteria>
    - `grep -c "seniorityEnum" src/lib/db/schema.ts` is at least 2 (declaration + column usage)
    - `grep -c "email: text\|linkedinUrl: text" src/lib/db/schema.ts` is 2
    - `grep -c "export function listPersonas\|export async function listPersonas" src/lib/db/queries/personas.ts` is 1, and the function signature includes a `filters` parameter with a default `= {}`
    - `grep -c "getPersonaById\|listDistinctCurrentCompanyNames" src/lib/db/queries/personas.ts` is 2
    - `grep -c "exists(" src/lib/db/queries/personas.ts` is at least 3 (search leg, currentCompany leg, hasSignals two-hop leg)
    - `grep -c "listCompanyRolesForPersona" src/lib/db/queries/companyPersonaRoles.ts` is 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/lib/db/schema.ts has seniorityEnum and the three new persona columns; personas.ts exports PersonaFilters/listPersonas(filters)/getPersonaById/listDistinctCurrentCompanyNames, including the two-hop EXISTS hasSignals filter; companyPersonaRoles.ts exports listCompanyRolesForPersona; project type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Push schema to Neon Postgres</name>
  <files>none (database migration only, no source files modified)</files>
  <read_first>
    drizzle.config.ts (confirms `dialect: 'postgresql'`, dbCredentials from DATABASE_URL, .env.local loaded via dotenv config)
  </read_first>
  <action>
    Run `npx drizzle-kit push` from the repo root. This is a purely additive change (1 new pgEnum type, 3 new nullable persona columns) — no existing column is dropped, renamed, or made non-nullable, so `drizzle-kit push` should apply without an interactive destructive-change prompt. If a prompt does appear, confirm it is genuinely non-destructive (adding a column/enum, not dropping data) before proceeding with `npx drizzle-kit push --force`; do not use `--force` to push through a prompt you have not read. Do not proceed to Task 3 until this push completes successfully — Task 3's seed script inserts into the new columns and will fail if the schema hasn't been pushed.
  </action>
  <acceptance_criteria>
    - `npx drizzle-kit push` exits 0
    - Re-running `npx drizzle-kit push` immediately after reports no pending schema changes (idempotent — confirms Neon now matches src/lib/db/schema.ts)
  </acceptance_criteria>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <done>Neon's `persona` table has the 3 new columns and the `seniority` Postgres enum type exists; a second `drizzle-kit push` run is a no-op.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Backfill persona seed data and extend career history, run seed pipeline</name>
  <files>src/lib/validation/seed.ts, src/scripts/seed.ts, data/seed/personas.csv, data/seed/company_persona_roles.csv</files>
  <read_first>
    src/lib/validation/seed.ts (optionalRevenueBand's blank-to-undefined-then-pipe-to-enum shape at lines 51-56, optionalSafeCsvString's formula-injection guard at lines 27-33, and the current personaRowSchema at lines 75-78)
    src/scripts/seed.ts (persona insert loop at lines 110-117)
    data/seed/personas.csv, data/seed/company_persona_roles.csv (current 10-persona / 11-role content)
    .planning/phases/03-persona-explorer/03-RESEARCH.md "Code Examples" (optionalEmailString's exact Zod v4 `.pipe(z.email().optional())` composition) and "Pitfall 4" (CSV column vs. schema drift)
    .planning/phases/03-persona-explorer/03-CONTEXT.md D-08/D-09 (seed dataset stays ~10 rows; a handful of personas get 1-2 additional past-role rows)
  </read_first>
  <action>
    In src/lib/validation/seed.ts: add `seniorityEnum` to the existing schema import (alongside `signalTypeEnum, signalStrengthEnum, revenueBandEnum, ownershipTypeEnum`). Add `optionalSeniority`, mirroring `optionalRevenueBand`'s exact blank-to-undefined-then-pipe shape but piping into `z.enum(seniorityEnum.enumValues).optional()`. Add `optionalEmailString`: same blank-to-undefined transform as `optionalSafeCsvString`, plus the same `startsWithDangerousPrefix` formula-injection refine, piped into `z.email().optional()` (Zod v4's top-level `z.email()`, not the deprecated `.string().email()` form). Extend `personaRowSchema` with `seniority: optionalSeniority`, `email: optionalEmailString`, `linkedin_url: optionalSafeCsvString`.
    In src/scripts/seed.ts: extend the persona insert's `.values({...})` call to include `seniority: row.seniority, email: row.email, linkedinUrl: row.linkedin_url`.
    Replace data/seed/personas.csv content — header `name,title,seniority,email,linkedin_url`, backfilling the existing 10 rows in place (D-08: no row-count change) with these exact values: Jordan Sample,VP of Finance,vp,jordan.sample@acmetestco.example,https://linkedin.com/in/jordan-sample-placeholder / Taylor Placeholder,Chief Financial Officer,c_level,taylor.placeholder@betasample.example,https://linkedin.com/in/taylor-placeholder / Morgan Testcase,Head of Global Business Services,director,morgan.testcase@gammaplaceholder.example,https://linkedin.com/in/morgan-testcase / Casey Fakename,Finance Systems Analyst,ic,casey.fakename@deltasample.example,https://linkedin.com/in/casey-fakename / Riley Sampledata,Chief Financial Officer,c_level,riley.sampledata@epsilontest.example,https://linkedin.com/in/riley-sampledata / Avery Placeholderson,Director of Finance Transformation,director,avery.placeholderson@zetasample.example,https://linkedin.com/in/avery-placeholderson / Drew Testfield,Chief Operating Officer,c_level,drew.testfield@etaplaceholder.example,(leave linkedin_url blank — exercises the email-only contact path) / Reese Sampleton,VP of Shared Services,vp,(leave email blank),https://linkedin.com/in/reese-sampleton (exercises the linkedin-only path) / Quinn Fakeworth,GBS Program Manager,manager,(leave email blank),(leave linkedin_url blank — exercises the "No contact info on record" empty state) / Sydney Placeholdt,Head of GBS Transformation,director,sydney.placeholdt@gammaplaceholder.example,https://linkedin.com/in/sydney-placeholdt. This changes Casey Fakename's title from "VP of Operations" to "Finance Systems Analyst" (ic tier) and Quinn Fakeworth's title from "Chief Financial Officer" to "GBS Program Manager" (manager tier) so all 5 seniority tiers get exercised across the fixed 10-row set (D-08's discretion note permits this).
    Replace data/seed/company_persona_roles.csv content — keep all 11 existing rows, but update the Delta Sample Retail Group / Casey Fakename row's title to "Finance Systems Analyst" and the Iota Test Solutions / Quinn Fakeworth row's title to "GBS Program Manager" (matching the personas.csv title changes above), then append 2 new historical rows per D-09: Beta Sample Inc,Jordan Sample,Finance Manager,false,2021-06-01,2023-12-31 and Zeta Sample Logistics,Taylor Placeholder,Finance Director,false,2020-01-01,2023-05-31 — both end dates immediately precede that persona's existing current-role start date. This gives Jordan Sample and Taylor Placeholder (alongside the existing Sydney Placeholdt row) 3 total personas exercising the "current + history" rendering path, matching D-09's "a handful" guidance.
    After all 4 files are updated, run `npm run seed` once against the schema pushed in Task 2. If it fails, the CSV data or the schema push has a mismatch that must be fixed before this task is done.
  </action>
  <acceptance_criteria>
    - `wc -l data/seed/personas.csv` reports 11 (1 header + 10 data rows)
    - `wc -l data/seed/company_persona_roles.csv` reports 14 (1 header + 13 data rows)
    - `grep -c "seniorityEnum" src/lib/validation/seed.ts` is at least 1
    - `grep -c "z.email()" src/lib/validation/seed.ts` is at least 1
    - `grep -c ",ic," data/seed/personas.csv` is 1 and `grep -c ",manager," data/seed/personas.csv` is 1 (both new tiers present, alongside the pre-existing vp/director/c_level rows)
    - `npm run seed` exits 0 and prints "Inserted: 9 companies, 10 personas, 12 signals, 13 company_persona_roles"
  </acceptance_criteria>
  <verify>
    <automated>npm run seed</automated>
  </verify>
  <done>personas.csv has all 10 personas backfilled with seniority (all 5 tiers represented)/email/linkedin_url, including full/email-only/linkedin-only/no-contact variety; company_persona_roles.csv has 13 rows with 3 personas (Jordan Sample, Taylor Placeholder, Sydney Placeholdt) exercising the current+history path; `npm run seed` successfully loads them into Neon against the Task 2 schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CSV seed file → database | Seed CSVs are repo-committed but represent the same class of "external, untrusted-until-validated" input as any file-based data load — validated through zod before any insert |
| listPersonas(filters) caller → Drizzle query | Plans 02/03 will pass user-controlled `searchParams` values into `filters` — this plan establishes the parameterized-query contract, including the new two-hop EXISTS, those callers must rely on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering | `listPersonas(filters)` WHERE composition, incl. the two-hop `hasSignals` EXISTS | mitigate | Use only Drizzle's parameterized operators (`eq`, `ilike`, `exists`, `innerJoin`) as built in Task 1 — never `sql.raw()` with an interpolated filter value. Plan 02/03's UI-sourced filters rely on this contract (RESEARCH.md Pitfall 1 / Open Question #1 flags the two-hop query as the one to double-check mentally against seed data during implementation). |
| T-3-02 | Tampering | New CSV columns (`seniority`, `email`, `linkedin_url`) | mitigate | Extend the existing `safeCsvString`/`optionalSafeCsvString` formula-injection guards to all three new columns; `email` is additionally validated via Zod's top-level `z.email()`. |
| T-3-03 | Information Disclosure | `linkedinUrl` will be rendered as a raw `<a href>` in Plan 03 | mitigate (data-origin control, scoped here) | The only write path for `linkedinUrl` this phase is the CSV seed pipeline built in this task, which validates every value through `optionalSafeCsvString`'s formula-injection guard before insert — no unvalidated `javascript:` URI can reach the column this phase. |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan — Phase 3's Package Legitimacy Audit confirms zero new dependencies. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes after Task 1
- `npx drizzle-kit push` succeeds and is idempotent on a second run (Task 2)
- `npm run seed` loads 9 companies / 10 personas / 12 signals / 13 company_persona_roles without error (Task 3)
- `listPersonas()` (no callers outside this file today) and `getPersonaByName` still compile unchanged
</verification>

<success_criteria>
Neon's `persona` table has the 3 new columns and the `seniority` enum type live; `src/lib/db/queries/personas.ts` and `companyPersonaRoles.ts` export the filter/lookup functions Plans 02-03 depend on; the seed dataset is fully backfilled with seniority/contact variety and expanded career history, ready to be rendered by the UI plans.
</success_criteria>

<output>
Create `.planning/phases/03-persona-explorer/03-01-SUMMARY.md` when done
</output>
