---
phase: 03-persona-explorer
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/personas/persona-detail.tsx
  - src/app/personas/[id]/page.tsx
  - src/components/personas/persona-list.tsx
  - src/app/personas/page.tsx
autonomous: true
requirements: [PERS-01, PERS-02, PERS-03, PERS-04, DATA-01]

must_haves:
  truths:
    - "Clicking a Persona row navigates to /personas/[id] and opens that persona's full 360 detail while the list pane stays visible"
    - "The detail pane shows role/title and seniority, a Current Company block (name + role title, linked to /companies/[id]), a Career History section listing all non-current roles with date ranges, and Contact Info (email as mailto:, LinkedIn as an external link) with correct empty-state copy for personas missing any of these"
    - "Pasting a copied /personas/[id] URL into a fresh browser tab restores the exact same selected persona"
    - "On narrow viewports, selecting a persona replaces the list with the detail pane; a back-to-list link restores the list without breaking the deep-linked URL"
    - "An unauthenticated visitor hitting /personas/[id] directly is redirected to /sign-in, never sees persona data"
  artifacts:
    - path: "src/app/personas/[id]/page.tsx"
      provides: "/personas/[id] route: awaits params+searchParams, renders PersonaList (selectedId set) + PersonaDetail"
      contains: "notFound"
    - path: "src/components/personas/persona-detail.tsx"
      provides: "Role & Seniority / Current Company / Career History / Contact Info sections — PERS-01/02/03/04"
      exports: ["PersonaDetail"]
  key_links:
    - from: "src/components/personas/persona-list.tsx"
      to: "src/app/personas/[id]/page.tsx"
      via: "Link href=/personas/{id} per row, preserving selectedId highlight state"
      pattern: "href=.*personas/"
    - from: "src/components/personas/persona-detail.tsx"
      to: "src/lib/db/queries/personas.ts getPersonaById"
      via: "async Server Component data fetch"
      pattern: "getPersonaById\\("
    - from: "src/components/personas/persona-detail.tsx"
      to: "src/lib/db/queries/companyPersonaRoles.ts listCompanyRolesForPersona"
      via: "async Server Component data fetch, split into current + history"
      pattern: "listCompanyRolesForPersona\\("
    - from: "src/components/personas/persona-detail.tsx"
      to: "src/app/companies/[id]/page.tsx"
      via: "Current Company block links to /companies/{id}"
      pattern: "companies/\\$\\{"
---

<objective>
Add the `/personas/[id]` route and `PersonaDetail` component so clicking any Persona row opens its full 360 detail — Role & Seniority, Current Company (linked to the Company explorer), Career History with date ranges, and Contact Info — in the master-detail pane while the list stays visible and the URL becomes the shareable source of truth for the selection (D-03, D-04, EXPL-04/EXPL-07 patterns reused from Phase 2).

Purpose: Plan 02 proved staff can browse and filter the list; this plan delivers the actual "full 360 detail" promise from the phase goal — PERS-01 through PERS-04 in full, completing DATA-01's "browsable end-to-end" requirement across both explorers.
Output: `/personas/[id]/page.tsx`, `PersonaDetail`, an updated `PersonaList` with per-row links + selected-row highlight + mobile responsive swap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-persona-explorer/03-CONTEXT.md
@.planning/phases/03-persona-explorer/03-RESEARCH.md
@.planning/phases/03-persona-explorer/03-UI-SPEC.md
@.planning/phases/03-persona-explorer/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the Persona detail pane and /personas/[id] route</name>
  <files>src/components/personas/persona-detail.tsx, src/app/personas/[id]/page.tsx</files>
  <read_first>
    src/components/companies/company-detail.tsx (full section-per-concern layout: humanizeEnum, dateFormatter, FirmographicField helpers, the notFound() 404 pattern, section spacing — the exact template to clone)
    src/app/companies/[id]/page.tsx (Promise-based params/searchParams pattern, notFound() on NaN id, two-column grid + mobile back-link)
    src/lib/db/queries/personas.ts (getPersonaById, from Plan 03-01)
    src/lib/db/queries/companyPersonaRoles.ts (listCompanyRolesForPersona, from Plan 03-01 — returns { company, role } rows)
    .planning/phases/03-persona-explorer/03-UI-SPEC.md "Component Inventory" (PersonaDetail section spec) and "Copywriting Contract" (Career History / Current Company / Contact Info empty-state copy) and "Color" (contact-info link treatment, indigo-600)
    .planning/phases/03-persona-explorer/03-CONTEXT.md D-03/D-04/D-05 (LinkedIn stored/rendered as a full URL as-is; Current Company shown separate from Career History; Company detail's existing Linked Personas section is explicitly NOT modified this phase)
  </read_first>
  <action>
    Create src/components/personas/persona-detail.tsx: copy `company-detail.tsx`'s `humanizeEnum`, `dateFormatter`, and `FirmographicField` helpers verbatim (local per-file copies, matching the established duplication convention — do not extract a shared util). Export `async function PersonaDetail({ id }: { id: number })`: call `getPersonaById(id)`, call Next's `notFound()` if it returns `undefined`; call `listCompanyRolesForPersona(id)`, split the result into `current = roles.find((r) => r.role.isCurrent)` and `history = roles.filter((r) => !r.role.isCurrent)` (D-04). Render, with the same `space-y-12` section rhythm as `CompanyDetail`: (1) header — persona name (Display 24/600) plus title (Body, slate-500 subtitle); (2) "Role & Seniority" section — two `FirmographicField`s: Title (`persona.title ?? '—'`), Seniority (`humanizeEnum(persona.seniority)`); (3) "Current Company" section (D-04, kept separate from Career History) — if `current` exists, render `current.company.name` as an indigo-600 `<Link href={`/companies/${current.company.id}`}>` plus `current.role.title` as Body text; if absent, render "No current company on record." (UI-SPEC empty-state copy); (4) "Career History" section — if `history.length > 0`, render a `<ul className="space-y-2">` of entries each showing the company name, role title, and `{dateFormatter.format(new Date(startDate))} – {endDate ? dateFormatter.format(new Date(endDate)) : 'Present'}` (PERS-02); if empty, render "No career history recorded." (UI-SPEC empty-state copy); (5) "Contact Info" section — render `persona.email` as `<a href={`mailto:${persona.email}`}>` and `persona.linkedinUrl` as `<a href={persona.linkedinUrl} target="_blank" rel="noopener noreferrer">` (D-03: full URL, rendered as-is, no parsing), each only when non-null, both indigo-600 Body-role text links; if both are null render "No contact info on record." (UI-SPEC empty-state copy). Do not modify `src/components/companies/company-detail.tsx`'s existing "Linked Personas" section — explicitly out of scope this phase per D-05.
    Create src/app/personas/[id]/page.tsx: mirror src/app/companies/[id]/page.tsx exactly — reuse the same `parsePersonaFilters` shape as Plan 02's `personas/page.tsx`, `params: Promise<{ id: string }>` and `searchParams: Promise<{ [key: string]: string | string[] | undefined }>` both awaited, `await requireStaffAccess()` first, `Number(id)` plus `notFound()` on `Number.isNaN`, same two-column grid rendering `<PersonaList filters={filters} selectedId={personaId} />` plus a mobile-only `<Link href="/personas">← Back to list</Link>` plus `<PersonaDetail id={personaId} />`.
  </action>
  <acceptance_criteria>
    - `grep -c "notFound" src/components/personas/persona-detail.tsx` is at least 1
    - `grep -c "requireStaffAccess" src/app/personas/[id]/page.tsx` is 1
    - `grep -c "await params\|await searchParams" src/app/personas/[id]/page.tsx` is 2
    - `grep -c "getPersonaById\|listCompanyRolesForPersona" src/components/personas/persona-detail.tsx` is 2
    - `grep -c "mailto:" src/components/personas/persona-detail.tsx` is 1
    - `grep -c "No current company on record\|No career history recorded\|No contact info on record" src/components/personas/persona-detail.tsx` is 3
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PersonaDetail renders role/title/seniority, a Current Company block linked to /companies/[id], Career History with date ranges, and Contact Info (mailto/external link) with correct empty-state copy for a valid id, and calls notFound() for an invalid/nonexistent one; the [id] route gates access and awaits both Promise-based props correctly; company-detail.tsx's Linked Personas section is untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire list-to-detail navigation, selected-row highlight, and mobile responsive swap</name>
  <files>src/components/personas/persona-list.tsx, src/app/personas/page.tsx</files>
  <read_first>
    src/components/personas/persona-list.tsx (current Plan 03-02 version — no row Link or selectedId highlight yet)
    src/app/personas/page.tsx (current Plan 03-02 version — passes selectedId={undefined} unconditionally)
    src/components/companies/company-list.tsx (Link + selectedId highlight + `hidden md:block` pattern at lines 106-161, the exact template)
    src/app/companies/page.tsx (the right-hand placeholder pane's `hidden ... md:flex` responsive class, for parity)
    .planning/phases/02-company-explorer/02-RESEARCH.md Anti-Pattern note against `router.back()` (a shared/deep-linked URL must not depend on browser history)
  </read_first>
  <action>
    Modify src/components/personas/persona-list.tsx: wrap each row's Name cell in `<Link href={`/personas/${persona.id}`}>` (plain Next.js `Link`, no client-side selection handler — the same route-param navigation Phase 2 established for Companies). Apply the `border-l-2 border-l-indigo-600 bg-indigo-50/50` highlight class to the row where `persona.id === selectedId`, matching `company-list.tsx`'s exact class shape.
    Modify src/app/personas/page.tsx only if its right-hand placeholder pane ("Select a persona to view details") is missing the `hidden ... md:flex` responsive class that `companies/page.tsx`'s equivalent placeholder pane has — apply the identical class if missing, for mobile-layout parity with the Company explorer. `selectedId={undefined}` on `PersonaList` stays unchanged (list-only view).
  </action>
  <acceptance_criteria>
    - `grep -c "Link href" src/components/personas/persona-list.tsx` is at least 1
    - `grep -c "selectedId" src/components/personas/persona-list.tsx` is at least 2
    - `grep -c "border-l-indigo-600" src/components/personas/persona-list.tsx` is 1
    - `grep -c "router.back" src/app/personas/[id]/page.tsx src/components/personas/persona-detail.tsx src/components/personas/persona-list.tsx` totals 0 (anti-pattern must not appear)
    - `npx tsc --noEmit` and `npm run build` both exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <human-check>Sign in, visit /personas, click a persona row: confirm the URL becomes /personas/{id}, the list stays visible with the clicked row highlighted, and the detail pane shows role/title/seniority, a Current Company block with a working link to that company's /companies/[id] page, Career History (open Sydney Placeholdt, Jordan Sample, or Taylor Placeholder — confirm a past company and date range shows), and Contact Info (confirm Drew Testfield shows email-only, Reese Sampleton shows LinkedIn-only, Quinn Fakeworth shows "No contact info on record"). Copy the URL, open it in a fresh tab: confirm the same persona opens directly. Narrow the browser window below the md breakpoint with a persona selected: confirm the list hides and only the detail pane (with a working "Back to list" link) shows. Visit /personas/999999: confirm a 404, not a crash. Then browse both /companies and /personas end-to-end once more to confirm the full seed dataset is browsable across both explorers (DATA-01).</human-check>
  </verify>
  <done>Clicking any persona row opens /personas/{id} with the list still visible and the row highlighted; deep-linked URLs restore the exact same view; mobile viewports swap list/detail with a URL-preserving back link; invalid ids 404 cleanly — PERS-01 through PERS-04 and DATA-01 are fully demonstrable end-to-end across both explorers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous browser → `/personas/[id]` route | Second authenticated persona surface — must be gated independently of `/personas`, same "easy to miss on the second route" risk Phase 2 flagged |
| URL-supplied `id` route param → `getPersonaById` | User-controlled input reaching a Drizzle query |
| Manually-entered `linkedinUrl` rendered as a raw `href` | New render-time risk surface this phase — no user-facing write path exists yet, only the CSV seed pipeline (Plan 01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-07 | Elevation of Privilege | `/personas/[id]` route | mitigate | `await requireStaffAccess()` as the first line of `PersonaDetailPage`, in addition to the already-gating `personas/layout.tsx` from Plan 02 — verified via the Task 2 human-check redirect test. |
| T-3-08 | Tampering | `id` route param passed to `getPersonaById` | mitigate | `Number(id)` + `Number.isNaN` guard before any DB call; `getPersonaById` uses Drizzle's parameterized `eq()`, never string-interpolated SQL — a malformed/non-numeric id 404s via `notFound()` rather than reaching the query layer. |
| T-3-09 | Tampering / Information Disclosure | `linkedinUrl` rendered as a raw `<a href={persona.linkedinUrl}>` — a manually-entered `javascript:` URI would execute on click if unvalidated | accept | The only write path for `linkedinUrl` is the CSV seed pipeline (Plan 03-01), which validates every value through `optionalSafeCsvString`'s formula-injection guard before insert; no user-facing form exists this phase to introduce an unvalidated write path — re-evaluate if/when a write UI ships in a future milestone. |
| T-3-10 | Information Disclosure | Sequential integer persona IDs exposed via `/personas/{id}` | accept | Same accepted-risk determination Phase 2 already made for `/companies/[id]` — the "any authenticated Clerk user = full access" model (FOUND-04) is unchanged this phase. |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` both pass after Task 2
- Human-check: row click → detail pane opens with list visible; all four PERS requirements render correctly with proper empty states; URL round-trips on fresh tab; mobile swap + back-link work; nonexistent id 404s; signed-out visitor redirected on both /personas and /personas/[id]
</verification>

<success_criteria>
Staff can click any Persona row to open its complete 360 detail (role/title/seniority, current company linked to the Company explorer, career history with date ranges, contact info) in a master-detail pane synced to a shareable URL — completing full browsability of the seed dataset across both Companies and Personas (DATA-01) and closing out PERS-01 through PERS-04.
</success_criteria>

<output>
Create `.planning/phases/03-persona-explorer/03-03-SUMMARY.md` when done
</output>
