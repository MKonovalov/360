---
phase: 03-persona-explorer
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/personas/persona-search-input.tsx
  - src/components/personas/persona-filters.tsx
  - src/components/personas/persona-list.tsx
  - src/app/personas/layout.tsx
  - src/app/personas/page.tsx
  - src/app/personas/loading.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements: [PERS-01, DATA-01]

must_haves:
  truths:
    - "A signed-in staff member navigating to /personas sees a collapsible/resizable left nav with both Companies and Key Personas sections real and clickable, and the currently active section highlighted based on the URL path"
    - "The Personas list shows all 10 seeded personas with name, title, humanized seniority, and current company"
    - "Typing in the persona search box narrows the list by name/title/current-company after a debounce, reflected in the URL"
    - "Selecting seniority, current company, or has-signals filters narrows the list; multiple active filters AND-combine"
    - "An unauthenticated visitor hitting /personas is redirected to /sign-in, never sees persona data"
  artifacts:
    - path: "src/app/personas/page.tsx"
      provides: "/personas route rendering the persona list via listPersonas(filters)"
      contains: "requireStaffAccess"
    - path: "src/components/personas/persona-list.tsx"
      provides: "shadcn Table rendering name/title/seniority/current company per row"
      exports: ["PersonaList"]
    - path: "src/components/personas/persona-search-input.tsx"
      provides: "nuqs debounced search Client Component"
      exports: ["PersonaSearchInput"]
    - path: "src/components/personas/persona-filters.tsx"
      provides: "nuqs-driven seniority/currentCompany/hasSignals Select filters"
      exports: ["PersonaFilters"]
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Client Component, usePathname()-driven active state for both Companies and Key Personas sections"
      contains: "usePathname"
  key_links:
    - from: "src/components/personas/persona-list.tsx"
      to: "src/lib/db/queries/personas.ts listPersonas()"
      via: "async Server Component data fetch"
      pattern: "listPersonas\\("
    - from: "src/app/personas/page.tsx"
      to: "src/lib/auth/requireStaffAccess.ts"
      via: "await requireStaffAccess() as the page's first line"
      pattern: "requireStaffAccess\\(\\)"
    - from: "src/components/layout/app-sidebar.tsx"
      to: "next/navigation usePathname"
      via: "isActive={pathname.startsWith('/personas')} / isActive={pathname.startsWith('/companies')}"
      pattern: "usePathname"
---

<objective>
Build the Persona search box, the seniority/current-company/has-signals filter Selects (D-06, D-07), and a real, gated `/personas` list page showing all 10 seeded personas' name/title/seniority/current-company (PERS-01 at list scope, DATA-01's "browsable" half) — and convert `AppSidebar` from its hardcoded-active Server Component into a `usePathname()`-driven Client Component so both explorer sections work correctly (the exact trigger `app-sidebar.tsx`'s own Phase-2 comment anticipated).

Purpose: Plan 01 proved the data model and query layer exist; this plan delivers the first user-observable slice of the Persona Explorer — after this plan, a signed-in staff member can navigate to a dedicated Personas page, search and filter the full seed set, and see both sidebar sections correctly reflect which one is active.
Output: `PersonaSearchInput`, `PersonaFilters`, `PersonaList`, `/personas` route (layout/page/loading), and a Client Component `AppSidebar` usable by both `/companies` and `/personas`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-persona-explorer/03-CONTEXT.md
@.planning/phases/03-persona-explorer/03-RESEARCH.md
@.planning/phases/03-persona-explorer/03-UI-SPEC.md
@.planning/phases/03-persona-explorer/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build Persona search/filter/list components</name>
  <files>src/components/personas/persona-search-input.tsx, src/components/personas/persona-filters.tsx, src/components/personas/persona-list.tsx</files>
  <read_first>
    src/components/companies/company-search-input.tsx (verbatim shape to mirror)
    src/components/companies/company-filters.tsx (EnumFilterSelect helper + composition pattern, including the local per-file humanizeEnum duplication convention)
    src/components/companies/company-list.tsx (Table/error-state/empty-state/hasActiveFilters pattern, and the acceptable per-row N+1 fetch precedent at lines 96-104)
    src/lib/db/queries/personas.ts (from Plan 03-01: listPersonas(filters), PersonaFilters shape, listDistinctCurrentCompanyNames)
    src/lib/db/queries/companyPersonaRoles.ts (from Plan 03-01: listCompanyRolesForPersona, for resolving each row's current company)
    src/lib/db/schema.ts (seniorityEnum.enumValues, from Plan 03-01)
    .planning/phases/03-persona-explorer/03-UI-SPEC.md "Component Inventory" (PersonaList column spec, PersonaFilters control spec) and "Copywriting Contract" (empty/error-state copy)
  </read_first>
  <action>
    Create src/components/personas/persona-search-input.tsx: copy src/components/companies/company-search-input.tsx verbatim, renaming the exported function to `PersonaSearchInput` and changing the placeholder text to "Search personas..."; same `'search'` URL param key so it composes into `PersonaFilters.search`.
    Create src/components/personas/persona-filters.tsx: copy `company-filters.tsx`'s `EnumFilterSelect` helper and its local `humanizeEnum` verbatim. Export `PersonaFilters({ currentCompanies }: { currentCompanies: string[] })` rendering three controls in the same `flex flex-wrap gap-3` wrapper (D-07's three filters): `<EnumFilterSelect paramKey="seniority" placeholder="Seniority" options={seniorityEnum.enumValues} />`, `<EnumFilterSelect paramKey="currentCompany" placeholder="Current company" options={currentCompanies} humanize={false} />` (company names aren't slugs, mirroring the industry filter's `humanize={false}` treatment), and a dedicated `hasSignals` Select — not `EnumFilterSelect`, since `"true"`/`"false"` values need explicit "Yes"/"No" labels rather than `humanizeEnum`'s slug-capitalization: `useQueryState('hasSignals', parseAsStringEnum(['true', 'false']).withOptions({ shallow: false }))` driving a shadcn `Select` with a `SelectTrigger`/`SelectValue placeholder="Has signals"` and two `SelectItem`s, `value="true"` labeled "Yes" and `value="false"` labeled "No".
    Create src/components/personas/persona-list.tsx: mirror `company-list.tsx`'s structure — a local `humanizeEnum` copy; `try/catch` around `listPersonas(filters)` rendering the UI-SPEC "Couldn't load personas" error copy on failure; empty-state branching on `hasActiveFilters` (`Boolean(filters?.search || filters?.seniority || filters?.currentCompany || filters?.hasSignals)`) between "No personas match your filters" and "No personas yet" (UI-SPEC exact copy strings); and a shadcn `Table` with columns Name, Title, Seniority (`humanizeEnum(persona.seniority)`), Current Company. For the Current Company column, call `listCompanyRolesForPersona(persona.id)` per row (N+1 acceptable at this seed-data scale, same rationale as `company-list.tsx`'s per-row signal fetch), find the role where `role.isCurrent`, and render `current?.company.name ?? '—'`. Accept a `selectedId?: number` prop and apply the same `hidden md:block` mobile-wrapper class as `company-list.tsx`, but do NOT add a row `Link` or selected-row highlight yet — that lands in Plan 03-03 alongside the detail route (mirrors Phase 2's 02-02→02-03 sequencing, where the bare list ships before row navigation).
  </action>
  <acceptance_criteria>
    - `grep -c "PersonaSearchInput" src/components/personas/persona-search-input.tsx` is at least 1
    - `grep -c "seniorityEnum.enumValues" src/components/personas/persona-filters.tsx` is 1
    - `grep -c "hasSignals" src/components/personas/persona-filters.tsx` is at least 2
    - `grep -c "listPersonas" src/components/personas/persona-list.tsx` is at least 1
    - `grep -c "No personas match your filters\|No personas yet" src/components/personas/persona-list.tsx` is 2
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>PersonaSearchInput/PersonaFilters/PersonaList exist, URL-synced via nuqs; seniority/currentCompany/hasSignals filters (D-07) AND-compose through listPersonas(filters); the list renders name/title/humanized-seniority/current-company with correct error/empty-state copy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire the /personas route and convert AppSidebar to a pathname-aware Client Component</name>
  <files>src/app/personas/layout.tsx, src/app/personas/page.tsx, src/app/personas/loading.tsx, src/components/layout/app-sidebar.tsx</files>
  <read_first>
    src/app/companies/layout.tsx (verbatim shell shape: requireStaffAccess + cookie-driven sidebar width + SidebarProvider/AppSidebar/SidebarResizeHandle/SidebarInset composition)
    src/app/companies/page.tsx (firstValue/parseCompanyFilters + two-column grid pattern, to mirror for parsePersonaFilters)
    src/app/companies/loading.tsx (Skeleton fallback shape to mirror)
    src/components/layout/app-sidebar.tsx (current hardcoded-isActive Server Component and its own comment flagging this exact Phase 3 conversion trigger)
    src/lib/db/queries/personas.ts (listDistinctCurrentCompanyNames, from Plan 03-01)
    .planning/phases/03-persona-explorer/03-RESEARCH.md "Pattern 5" (AppSidebar conversion) and "Pitfall 3" (both nav items must use .startsWith(), not exact equality, or one/both highlights break)
  </read_first>
  <action>
    Create src/app/personas/layout.tsx: copy src/app/companies/layout.tsx verbatim, renaming the function to `PersonasLayout` — same `requireStaffAccess()` gate, same cookie-driven `sidebar_width` read, same `SidebarProvider`/`AppSidebar`/`SidebarResizeHandle`/`SidebarInset` composition (the sidebar shell and resize behavior are shared infrastructure already built in Phase 2, not re-decided this phase).
    Create src/app/personas/page.tsx: mirror src/app/companies/page.tsx's `firstValue`/filter-parsing/grid-layout shape, renamed to `parsePersonaFilters` mapping `search`, `seniority`, `currentCompany`, and `hasSignals` (`firstValue(params.hasSignals) === 'true'`) into a `PersonaFilters` object. Call `await requireStaffAccess()` first (belt-and-suspenders alongside the layout gate). Fetch `listDistinctCurrentCompanyNames()` and map the rows to a `string[]` of company names for `PersonaFilters`' `currentCompanies` prop. Render `<PersonaSearchInput />` + `<PersonaFilters currentCompanies={...} />` above `<PersonaList filters={filters} selectedId={undefined} />` in the left column, with the same right-hand placeholder pane copy pattern ("Select a persona to view details").
    Create src/app/personas/loading.tsx: copy src/app/companies/loading.tsx verbatim, renamed to `PersonasLoading`.
    Modify src/components/layout/app-sidebar.tsx: add `'use client'` at the top; import `usePathname` from `next/navigation`; replace the hardcoded `isActive` on the Companies `SidebarMenuButton` with `isActive={pathname.startsWith('/companies')}`; replace the `disabled` Key Personas `SidebarMenuButton` with the same `asChild`/accent-class shape as Companies, wrapped around `<Link href="/personas">Key Personas</Link>` and `isActive={pathname.startsWith('/personas')}`. Remove the outdated "Server Component... Revisit with real pathname matching once Phase 3's /personas route exists" comment and the D-11 disabled-button comment; replace with a short comment citing this exact conversion (Phase 3's RESEARCH.md Pattern 5). Use `.startsWith()`, not exact equality, so `/companies/[id]` and `/personas/[id]` (the latter added in Plan 03-03) both still highlight the correct single item.
  </action>
  <acceptance_criteria>
    - `grep -c "requireStaffAccess" src/app/personas/layout.tsx` is 1 and `grep -c "requireStaffAccess" src/app/personas/page.tsx` is 1
    - `grep -c "PersonaSearchInput\|PersonaFilters" src/app/personas/page.tsx` is at least 2
    - `grep -c "'use client'" src/components/layout/app-sidebar.tsx` is 1
    - `grep -c "usePathname" src/components/layout/app-sidebar.tsx` is 1
    - `grep -c "startsWith('/companies')" src/components/layout/app-sidebar.tsx` is 1
    - `grep -c "startsWith('/personas')" src/components/layout/app-sidebar.tsx` is 1
    - `grep -c "disabled" src/components/layout/app-sidebar.tsx` is 0 (Key Personas is no longer disabled)
    - `npx tsc --noEmit` and `npm run build` both exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <human-check>Sign in, visit /personas: confirm the sidebar shows Key Personas as the active/highlighted item and Companies as inactive-but-clickable, the table lists all 10 seeded personas with title/seniority/current company, and typing in search plus selecting each filter (seniority, current company, has signals) narrows the list correctly, both individually and combined. Then visit /companies: confirm Companies is now the active item and Key Personas is inactive-but-clickable (no regression from the sidebar conversion). Sign out and visit /personas directly: confirm redirect to /sign-in.</human-check>
  </verify>
  <done>Staff can navigate to /personas via a real sidebar link, see the full seeded persona list with working debounced search plus AND-combined seniority/currentCompany/hasSignals filters, and both sidebar sections correctly reflect active state via usePathname() on every route; unauthenticated visitors are redirected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous browser → `/personas` route tree | First authenticated surface for the Persona Explorer — must be gated before any persona data is fetched or rendered |
| Browser URL query string → `PersonaFilters` → `listPersonas` WHERE clause | User-controlled filter input, including the two-hop `hasSignals` EXISTS built in Plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | Elevation of Privilege | `/personas` route (`layout.tsx` + `page.tsx`) | mitigate | `await requireStaffAccess()` is the first line of both `personas/layout.tsx` and `personas/page.tsx` — verified via the human-check redirect test in Task 2. |
| T-3-05 | Tampering | `searchParams` → `PersonaFilters` → `listPersonas(filters)`, incl. the two-hop `hasSignals` EXISTS | mitigate | `persona-filters.tsx`'s enum-typed Selects (`parseAsStringEnum`) are the client-side validation boundary; server-side, `listPersonas` (Plan 03-01) only uses Drizzle's parameterized `eq`/`ilike`/`exists`/`innerJoin` — never `sql.raw()` with interpolated values. |
| T-3-06 | Tampering / Information Disclosure | `AppSidebar` Server→Client Component conversion changing existing `/companies` active-state behavior | mitigate | Use `.startsWith()` (not exact equality) for both sections' `isActive` computation, manually verified on `/companies`, `/companies/[id]`, and `/personas` via the Task 2 human-check (RESEARCH.md Pitfall 3). |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` both pass after Task 2
- Human-check: signed-in staff sees the full persona list with working search/filters at /personas; sidebar correctly reflects active state on both /companies and /personas; signed-out visitors are redirected
</verification>

<success_criteria>
Staff can navigate to /personas, browse the full seeded persona list, and search/filter it (seniority, current company, has signals — AND-combined), with the sidebar correctly reflecting which section (Companies or Key Personas) is active — the first tangible Persona Explorer surface.
</success_criteria>

<output>
Create `.planning/phases/03-persona-explorer/03-02-SUMMARY.md` when done
</output>
