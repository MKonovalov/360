---
phase: 30-shared-data-model-seed
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "All 9 new Offerings/Signals tables exist in the live Neon database with the exact column shapes specified in CONTEXT.md (audit columns, status enums, FKs)"
    - "The signal_offering_link discriminator column reuses the existing record_type Postgres enum type, never a newly declared signal_type-named type"
    - "A second, immediate re-run of `npm run db:push` reports no further changes (idempotent push)"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "catalogStatusEnum, practiceAreaStatusEnum, offerTypeEnum + practiceArea, domain, offering, buyerRole, offeringBuyerRole, trigger, companySignal, personaSignal, signalOfferingLink table definitions"
      contains: "export const practiceArea = pgTable('practice_area'"
  key_links:
    - from: "signalOfferingLink.signalType column"
      to: "recordTypeEnum (Postgres type record_type)"
      via: "recordTypeEnum('signal_type')"
      pattern: "recordTypeEnum\\('signal_type'\\)"
    - from: "offering.practiceAreaId / domain.practiceAreaId / companySignal.practiceAreaId / personaSignal.practiceAreaId"
      to: "practiceArea.id"
      via: ".references(() => practiceArea.id)"
      pattern: "references\\(\\(\\) => practiceArea\\.id\\)"
---

<objective>
Add all 9 new Offerings/Signals tables and their supporting enums to the single existing Drizzle schema file, then push that schema directly to the live Neon database. This is the foundation every other Phase 30 plan (query modules, seed script) builds against — nothing else in the phase can proceed until these tables physically exist in Postgres.

Purpose: Establish the shared data model per CONTEXT.md Section 2 (spec Sections 2.1/2.2) with zero enum-name collisions and full audit-column coverage, matching this repo's exact existing conventions (verified in 30-RESEARCH.md).
Output: `src/lib/db/schema.ts` updated with 9 new tables; live Neon database has all 9 tables created via `npm run db:push`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-shared-data-model-seed/30-CONTEXT.md
@.planning/phases/30-shared-data-model-seed/30-RESEARCH.md
@.planning/specs/v1.4-signals-offerings.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Offerings + Signals enums and 9 tables to schema.ts</name>
  <files>src/lib/db/schema.ts</files>
  <read_first>
    src/lib/db/schema.ts (existing full file — read completely; append new code after the last export, `userModelSettings`, at line 296)
    .planning/phases/30-shared-data-model-seed/30-CONTEXT.md (exact field shapes, Section "Schema — Offerings feature entities" / "Schema — Signals feature entities" / "Audit columns")
    .planning/specs/v1.4-signals-offerings.md (Section 2, lines 36-122 — canonical field-by-field spec)
  </read_first>
  <action>
    Append to `src/lib/db/schema.ts` (do not create a new file — single-file schema convention). Add 3 new enums:
    - `catalogStatusEnum = pgEnum('catalog_status', ['active', 'draft', 'retired'])` — shared 3-value lifecycle enum reused by `offering`, `companySignal`, `personaSignal` (per RESEARCH.md Assumption A3 / Alternatives Considered — DRY, matches the `recordTypeEnum` cross-table-reuse precedent already in this file).
    - `practiceAreaStatusEnum = pgEnum('practice_area_status', ['active', 'draft'])` — `practice_area` has only 2 states, needs its own type.
    - `offerTypeEnum = pgEnum('offer_type', ['entry', 'core', 'programme', 'retainer', 'on_request', 'operator_differentiator', 'productised'])` — exactly these 7 values, no others.

    Do NOT declare any new enum named `signal_type` — that Postgres type name is already taken at schema.ts:6 by the unrelated buying-signal enum. For `signal_offering_link`'s discriminator, reuse the existing `recordTypeEnum` (Postgres type `record_type`, values `'company'|'persona'`) — the Drizzle column can still be named `signal_type` (e.g. `signalType: recordTypeEnum('signal_type').notNull()`); only the underlying Postgres CREATE TYPE name matters for the collision, not the column name.

    Add 9 `pgTable` exports, each with `id: serial('id').primaryKey()`, then domain columns, then `createdBy: text('created_by').notNull()`, `updatedBy: text('updated_by').notNull()`, `createdAt: timestamp('created_at').defaultNow().notNull()`, `updatedAt: timestamp('updated_at').defaultNow().notNull()` — no exceptions, matching Pattern 1 in 30-RESEARCH.md:

    1. `practiceArea` → table `practice_area`: `name: text('name').notNull().unique('practice_area_name_unique')`, `shortCode: text('short_code').notNull().unique('practice_area_short_code_unique')`, `sortOrder: integer('sort_order').notNull()`, `description: text('description')` (nullable), `status: practiceAreaStatusEnum('status').notNull().default('active')`.
    2. `domain` → table `domain`: `practiceAreaId: integer('practice_area_id').notNull().references(() => practiceArea.id)`, `name: text('name').notNull()`, `sortOrder: integer('sort_order').notNull()`.
    3. `offering` → table `offering`: `practiceAreaId: integer('practice_area_id').notNull().references(() => practiceArea.id)`, `domainId: integer('domain_id').references(() => domain.id)` (nullable), `name: text('name').notNull()`, `offerType: offerTypeEnum('offer_type').notNull()`, `description: text('description').notNull()`, `commercialModelText: text('commercial_model_text')` (nullable), `sortOrder: integer('sort_order').notNull()`, `status: catalogStatusEnum('status').notNull().default('active')`.
    4. `buyerRole` → table `buyer_role`: `name: text('name').notNull().unique('buyer_role_name_unique')`, `description: text('description')` (nullable).
    5. `offeringBuyerRole` → table `offering_buyer_role`: `offeringId: integer('offering_id').notNull().references(() => offering.id)`, `buyerRoleId: integer('buyer_role_id').notNull().references(() => buyerRole.id)`, `rank: integer('rank').notNull()`. Add a table-config `uniqueIndex('offering_buyer_role_unique_idx').on(table.offeringId, table.buyerRoleId)` (same third-argument-callback shape as the existing `signal` table's `uniqueIndex`, schema.ts:108-115) to prevent duplicate buyer-role links on the same offering.
    6. `trigger` → table `trigger`: `offeringId: integer('offering_id').notNull().references(() => offering.id)`, `triggerText: text('trigger_text').notNull()`, `sortOrder: integer('sort_order').notNull()`.
    7. `companySignal` → table `company_signal`: `practiceAreaId: integer('practice_area_id').notNull().references(() => practiceArea.id)`, `name: text('name').notNull()`, `category: text('category').notNull()` (free text, NOT an enum), `description: text('description').notNull()`, `status: catalogStatusEnum('status').notNull().default('active')`.
    8. `personaSignal` → table `persona_signal`: `practiceAreaId: integer('practice_area_id').notNull().references(() => practiceArea.id)`, `buyerRoleId: integer('buyer_role_id').notNull().references(() => buyerRole.id)`, `name: text('name').notNull()`, `category: text('category').notNull()` (free text), `description: text('description').notNull()`, `status: catalogStatusEnum('status').notNull().default('active')`.
    9. `signalOfferingLink` → table `signal_offering_link`: `signalType: recordTypeEnum('signal_type').notNull()` (reused enum, see above), `signalId: integer('signal_id').notNull()` (bare integer, NO `.references()` — polymorphic, points at `company_signal.id` or `persona_signal.id` depending on `signalType`, exact pattern as `recentlyViewed.recordId`/`importLog.recordId`), `offeringId: integer('offering_id').notNull().references(() => offering.id)`, `relevanceNote: text('relevance_note')` (nullable).

    Add a 1-4 line comment above each new enum/table explaining the non-obvious decision, tagged with `DATA-01`/`DATA-02` as appropriate, matching this file's existing comment density (e.g. the `recordTypeEnum`/`importBatch` comment blocks). Explicitly comment on `signalOfferingLink.signalType` that it reuses `recordTypeEnum`/`record_type` and why (collision avoidance with the existing `signal_type` enum at line 6).
  </action>
  <acceptance_criteria>
    - `grep -c "export const practiceArea = pgTable('practice_area'" src/lib/db/schema.ts` equals 1
    - `grep -c "export const catalogStatusEnum = pgEnum('catalog_status'" src/lib/db/schema.ts` equals 1
    - `grep -c "export const practiceAreaStatusEnum = pgEnum('practice_area_status'" src/lib/db/schema.ts` equals 1
    - `grep -c "export const offerTypeEnum = pgEnum('offer_type'" src/lib/db/schema.ts` equals 1
    - `grep -c "pgEnum('signal_type'" src/lib/db/schema.ts` equals exactly 1 (only the pre-existing D-07 buying-signal enum at line 6 — no new `signal_type`-named `pgEnum` was declared)
    - `grep -c "export const signalOfferingLink = pgTable('signal_offering_link'" src/lib/db/schema.ts` equals 1
    - `grep -c "recordTypeEnum('signal_type')" src/lib/db/schema.ts` equals 1 (the reused-enum column inside `signalOfferingLink`)
    - All 9 new `pgTable(` calls present: `practice_area`, `domain`, `offering`, `buyer_role`, `offering_buyer_role`, `trigger`, `company_signal`, `persona_signal`, `signal_offering_link`
    - `npx tsc --noEmit` exits 0 (schema file type-checks cleanly)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>schema.ts contains all 3 new enums + 9 new tables with audit columns, compiles clean, and no `signal_type` enum-name collision exists.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Push schema to live Neon database</name>
  <files>none (CLI operation only)</files>
  <read_first>
    src/lib/db/schema.ts (the file just edited in Task 1 — confirm final state before pushing)
    package.json (confirms `"db:push": "drizzle-kit push"` script — line 16)
    .planning/phases/30-shared-data-model-seed/30-RESEARCH.md (Common Pitfalls → Pitfall 5, Pitfall 7 — push-only migration flow, `trigger`/`domain` reserved-word safety)
  </read_first>
  <action>
    Run `npm run db:push` against the real `DATABASE_URL` from `.env.local`. This applies the 9 new tables + 3 new enums from Task 1 directly to the live Neon Postgres instance — no `drizzle/` migration files exist in this repo and none should be created (do not run `drizzle-kit generate` or `drizzle-kit migrate`). If `drizzle-kit push` enters an interactive prompt asking to confirm a destructive change, STOP and inspect the diff — a purely additive schema change (new tables/enums only) should never trigger a destructive-change prompt; a prompt appearing here signals something is wrong with the diff (e.g. an accidental rename) and must be investigated before proceeding, never blindly confirmed. Immediately re-run `npm run db:push` a second time to confirm idempotency — the second run must report no pending changes.
  </action>
  <acceptance_criteria>
    - First `npm run db:push` run exits 0 and its output confirms 9 new tables created (or equivalent "changes applied" summary) with zero destructive-change prompts
    - Second, immediate `npm run db:push` run exits 0 and reports no changes pending (idempotent — confirms the schema now matches the live DB exactly)
    - No interactive prompt was auto-confirmed blindly — if one appeared, the diff was inspected first
  </acceptance_criteria>
  <verify>
    <automated>npm run db:push</automated>
  </verify>
  <done>All 9 new tables and 3 new enums exist in the live Neon database; a second `npm run db:push` reports no changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Schema file → live database | `drizzle-kit push` applies the Drizzle schema directly to production Neon Postgres — no review gate between file edit and live DDL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Repudiation | `created_by`/`updated_by` columns on all 9 new tables | mitigate | `NOT NULL` constraint at the schema level (Task 1) — a write literally cannot succeed without an attributed userId/sentinel; no writes are possible pre-DATA-09 wiring since this phase ships no Server Actions |
| T-30-04 | Tampering | `drizzle-kit push` destructive-change auto-confirmation | mitigate | Task 2 explicitly forbids blindly confirming any destructive-change prompt on a schema change that should be purely additive; requires manual inspection first |
| T-30-05 | Tampering | Enum type-name collision (`signal_type`) silently corrupting an unrelated existing column's values | mitigate | Task 1 reuses `recordTypeEnum` instead of declaring a colliding `pgEnum('signal_type', ...)`; acceptance criteria grep-asserts exactly one `pgEnum('signal_type'` declaration exists (the pre-existing one) |
</threat_model>

<verification>
- `npx tsc --noEmit` passes after Task 1
- `npm run db:push` succeeds and is idempotent on second run after Task 2
- All 9 tables visible via any Postgres client against the live `DATABASE_URL`
</verification>

<success_criteria>
- `src/lib/db/schema.ts` contains `practiceArea`, `domain`, `offering`, `buyerRole`, `offeringBuyerRole`, `trigger`, `companySignal`, `personaSignal`, `signalOfferingLink` table exports with full audit columns
- Live Neon database has all 9 tables and 3 new enum types (`catalog_status`, `practice_area_status`, `offer_type`) created, with `record_type` reused (not duplicated) for the link discriminator
- No downstream plan (02-06) can proceed until this plan's Task 2 completes
</success_criteria>

<output>
Create `.planning/phases/30-shared-data-model-seed/30-01-SUMMARY.md` when done
</output>
