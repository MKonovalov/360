---
phase: 30-shared-data-model-seed
plan: 03
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/lib/db/queries/offerings.ts
  - src/lib/db/queries/offerings.test.ts
  - src/lib/db/queries/offerings.integration.test.ts
autonomous: true
requirements: [DATA-01, DATA-09, DATA-10]

must_haves:
  truths:
    - "offering has CRUD query functions plus offering_buyer_role and trigger insert/list helpers, all accepting userId/createdBy/updatedBy as plain parameters"
    - "A distinct 'active only' picker query exists separate from the 'all offerings' admin query, so status=draft offerings can be excluded from future pickers (spec Section 3)"
    - "Deleting an offering with dependent offering_buyer_role, trigger, or signal_offering_link rows is rejected, never silently cascaded"
  artifacts:
    - path: "src/lib/db/queries/offerings.ts"
      provides: "insertOffering, updateOffering, listAllOfferingsForPracticeArea, listActiveOfferingsForPracticeArea, insertOfferingBuyerRole, insertTrigger, listTriggersForOffering, listBuyerRolesForOffering, hasOfferingDependents, deleteOffering"
  key_links:
    - from: "offerings.ts hasOfferingDependents"
      to: "offeringBuyerRole, trigger, signalOfferingLink tables"
      via: "eq(...offeringId, id) existence checks"
      pattern: "hasOfferingDependents"
    - from: "offerings.ts listActiveOfferingsForPracticeArea"
      to: "offering.status"
      via: "and(eq(practiceAreaId, id), eq(status, 'active'))"
      pattern: "listActiveOfferingsForPracticeArea"
---

<objective>
Build the `offerings.ts` query module: full `offering` CRUD, the picker-vs-admin active/all split required by the "draft offerings excluded from pickers" business rule (spec Section 3), the `offering_buyer_role` (ranked join) and `trigger` (1-to-many) insert/list helpers that live alongside `offering` per this repo's single-file-per-concern convention, and the `offering` leg of the DATA-10 delete-guard (checking all three of `offeringBuyerRole`, `trigger`, `signalOfferingLink` for dependents).

Purpose: `offering` is the most-referenced entity in the schema — the seed script (Plan 06) and both `signalOfferingLinks.ts` (Plan 05) and future Phase 31/32 UI depend on this module's exact shape.
Output: `offerings.ts` + matching unit and integration test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-shared-data-model-seed/30-CONTEXT.md
@.planning/phases/30-shared-data-model-seed/30-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: offering CRUD + active/all picker split</name>
  <files>src/lib/db/queries/offerings.ts, src/lib/db/queries/offerings.test.ts</files>
  <read_first>
    src/lib/db/queries/proposals.ts (filtered-list idiom, lines 35-61 — `.where(eq(signalProposal.status, 'pending'))`)
    src/lib/db/queries/userModelSettings.ts (explicit-set-on-update discipline, lines 18-34)
    src/lib/db/queries/proposals.test.ts (unit-test structure)
    src/lib/db/schema.ts (`offering`, `offeringBuyerRole`, `trigger`, `buyerRole`, `signalOfferingLink` table shapes after Plan 01)
  </read_first>
  <behavior>
    - `insertOffering` inserts a row with `createdBy`/`updatedBy` both set from input, including all spec fields (`practiceAreaId`, `domainId?`, `name`, `offerType`, `description`, `commercialModelText?`, `sortOrder`, `status?`)
    - `updateOffering` explicitly sets `updatedAt`/`updatedBy` in every `.set()` call
    - `listAllOfferingsForPracticeArea(practiceAreaId)` returns every offering for that practice area regardless of `status`, ordered by `sortOrder`
    - `listActiveOfferingsForPracticeArea(practiceAreaId)` returns only `status = 'active'` offerings for that practice area — the picker-safe query
  </behavior>
  <action>
    Create `src/lib/db/queries/offerings.ts` with relative imports. Export:
    - `insertOffering(input: { practiceAreaId: number; domainId?: number; name: string; offerType: 'entry'|'core'|'programme'|'retainer'|'on_request'|'operator_differentiator'|'productised'; description: string; commercialModelText?: string; sortOrder: number; status?: 'active'|'draft'|'retired'; createdBy: string })` — `.insert(offering).values({ ...input, updatedBy: input.createdBy }).returning()`.
    - `updateOffering(id: number, patch: Partial<typeof offering.$inferInsert>, updatedBy: string)` — mirrors `userModelSettings.ts`'s explicit-set pattern: `.update(offering).set({ ...patch, updatedAt: new Date(), updatedBy }).where(eq(offering.id, id)).returning()`.
    - `listAllOfferingsForPracticeArea(practiceAreaId: number)` — `.select().from(offering).where(eq(offering.practiceAreaId, practiceAreaId)).orderBy(offering.sortOrder)`.
    - `listActiveOfferingsForPracticeArea(practiceAreaId: number)` — same `.where(and(eq(offering.practiceAreaId, practiceAreaId), eq(offering.status, 'active')))`, ordered by `sortOrder`. This is the Phase 31/32-consumed "servable offerings for picker" query (spec Section 3's draft-exclusion rule) — build it now even though no UI calls it yet.
    No try/catch.

    Create `offerings.test.ts` (mocked db, mirrors `proposals.test.ts`'s `vi.hoisted`/`vi.mock('../index', ...)` structure): cover insert sets `updatedBy = createdBy`, `listActiveOfferingsForPracticeArea` includes the `status='active'` filter in its `.where()` call, `updateOffering` always includes `updatedAt`/`updatedBy` in its `.set()` payload.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/db/queries/offerings.test.ts` passes
    - `grep -c "requireStaffAccess" src/lib/db/queries/offerings.ts` equals 0
    - `grep -c "listActiveOfferingsForPracticeArea" src/lib/db/queries/offerings.ts` equals 1 (function defined once) and test file references it at least once
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/db/queries/offerings.test.ts</automated>
  </verify>
  <done>offering CRUD + active/all picker split implemented and unit-tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: offering_buyer_role + trigger helpers, delete-guard, integration test</name>
  <files>src/lib/db/queries/offerings.ts, src/lib/db/queries/offerings.test.ts, src/lib/db/queries/offerings.integration.test.ts</files>
  <read_first>
    src/lib/db/queries/offerings.ts (Task 1 output — extend, do not replace)
    src/lib/db/queries/importBatches.ts (delete-guard pattern, lines 101-116 — extend to a three-table check for `offering`)
    src/lib/db/queries/userModelSettings.integration.test.ts (integration test scaffold pattern, full file)
    src/lib/db/schema.ts (`offeringBuyerRole`, `trigger`, `signalOfferingLink` shapes)
  </read_first>
  <behavior>
    - `insertOfferingBuyerRole` inserts one ranked buyer-role link for an offering
    - `insertTrigger` inserts one trigger row for an offering
    - `listTriggersForOffering(offeringId)` returns triggers ordered by `sortOrder`
    - `listBuyerRolesForOffering(offeringId)` returns the offering's buyer roles joined with `buyerRole.name`, ordered by `rank`
    - `hasOfferingDependents(id)` returns `true` if any `offeringBuyerRole`, `trigger`, OR `signalOfferingLink` row references this `offeringId`, `false` if none do
    - `deleteOffering(id)` returns `{ ok: false, reason: 'has_dependents' }` when any of the three dependent tables has a match, `{ ok: true }` and removes the row otherwise
  </behavior>
  <action>
    Extend `src/lib/db/queries/offerings.ts` (same file as Task 1) with:
    - `insertOfferingBuyerRole(input: { offeringId: number; buyerRoleId: number; rank: number; createdBy: string })` — `.insert(offeringBuyerRole).values({ ...input, updatedBy: input.createdBy }).returning()`.
    - `insertTrigger(input: { offeringId: number; triggerText: string; sortOrder: number; createdBy: string })` — `.insert(trigger).values({ ...input, updatedBy: input.createdBy }).returning()`.
    - `listTriggersForOffering(offeringId: number)` — `.select().from(trigger).where(eq(trigger.offeringId, offeringId)).orderBy(trigger.sortOrder)`.
    - `listBuyerRolesForOffering(offeringId: number)` — join `offeringBuyerRole` to `buyerRole` on `buyerRoleId`, select `{ buyerRoleId: buyerRole.id, name: buyerRole.name, rank: offeringBuyerRole.rank }`, `.where(eq(offeringBuyerRole.offeringId, offeringId))`, `.orderBy(offeringBuyerRole.rank)`.
    - `hasOfferingDependents(id: number): Promise<boolean>` — three sequential `LIMIT 1` existence checks against `offeringBuyerRole.offeringId`, `trigger.offeringId`, `signalOfferingLink.offeringId`, short-circuiting on first hit (extend the two-table `importBatches.ts` pattern to three tables).
    - `export type DeleteOfferingResult = { ok: true } | { ok: false; reason: 'has_dependents' }`
    - `deleteOffering(id: number): Promise<DeleteOfferingResult>` — guard-then-delete.
    No try/catch anywhere in the file.

    Extend `offerings.test.ts` with unit tests for `hasOfferingDependents` (true via each of the three tables independently — three distinct test cases) and `deleteOffering`'s discriminated-union behavior.

    Create `offerings.integration.test.ts` mirroring `userModelSettings.integration.test.ts`'s full structure (gated on `TEST_DATABASE_URL`, `@/lib/db` + `@/lib/db/schema` path-alias imports inside the test, `ids: number[]` cleanup array in `afterAll`). Cover: insert an offering under a real practice area, insert a trigger and a buyer-role link for it, confirm `listTriggersForOffering`/`listBuyerRolesForOffering` return them, confirm `hasOfferingDependents` is true, confirm `deleteOffering` is blocked. This integration test is the one that exercises the `trigger`/`domain` reserved-word-safety concern flagged as MEDIUM confidence in 30-RESEARCH.md (Pitfall 7) — a passing insert into the `trigger` table here confirms Drizzle's auto-quoted identifiers work correctly against the live DB.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/db/queries/offerings.test.ts` passes with all `hasOfferingDependents` cases (3 independent dependent-table cases) present
    - `grep -c "try {" src/lib/db/queries/offerings.ts` equals 0
    - `offerings.integration.test.ts` contains an insert into the `trigger` table proving the reserved-word table name pushes/inserts cleanly
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/db/queries/offerings.test.ts</automated>
  </verify>
  <done>offering_buyer_role/trigger helpers implemented, three-table delete-guard unit-tested, integration scaffold created and gated correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Future Signals UI (Phase 31) picker → offerings.ts | The picker query (`listActiveOfferingsForPracticeArea`) is the only safe source for offering options; `listAllOfferingsForPracticeArea` must never be substituted into a picker context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Tampering / Repudiation | `deleteOffering` | mitigate | `hasOfferingDependents()` pre-check across all three dependent tables (`offeringBuyerRole`, `trigger`, `signalOfferingLink`) before any delete — silent-cascade-delete threat per 30-RESEARCH.md Security Domain; Postgres FK `ON DELETE RESTRICT` is the backstop |
| T-30-06 | Information Disclosure | `listAllOfferingsForPracticeArea` vs `listActiveOfferingsForPracticeArea` | accept | Low risk — both queries are internal, staff-only reads (no public endpoint exists yet); the distinction exists to prevent a *future* picker misuse, not to gate current access |
</threat_model>

<verification>
- `npx vitest run src/lib/db/queries/offerings.test.ts` passes
- `npm test` full suite green (integration skips without `TEST_DATABASE_URL`)
</verification>

<success_criteria>
- `offerings.ts` exports the full function set: `insertOffering`, `updateOffering`, `listAllOfferingsForPracticeArea`, `listActiveOfferingsForPracticeArea`, `insertOfferingBuyerRole`, `insertTrigger`, `listTriggersForOffering`, `listBuyerRolesForOffering`, `hasOfferingDependents`, `deleteOffering`
- Three-table delete-guard is unit-tested per dependent table independently
- Integration test proves the `trigger` reserved-word table name works against a live push
</success_criteria>

<output>
Create `.planning/phases/30-shared-data-model-seed/30-03-SUMMARY.md` when done
</output>
