---
phase: 26-settings-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/model-picker-logic.ts
  - src/components/settings/model-picker-logic.test.ts
  - src/app/(dashboard)/settings/page.tsx
autonomous: true
requirements: [SET-01, SET-02, SET-03, SET-04, SET-05, SET-06]

must_haves:
  truths:
    - "The pure logic module (model-picker-logic.ts) exposes an endpoint field on ServableModel, an endpointLabel() helper, a Hermes capability caption keyed on the resolved nousresearch provider (never bare family), a single rowCaption() composer joining endpoint -> suffix -> hermes in that order, a searchValue() that includes the endpoint token, and a resolveBadgeProvider() helper that resolves a model id's TRUE provider from the union list"
    - "model-picker-logic.test.ts proves every new function plus a 4-provider fixture case for primaryAfterProviderSwitch and groupByProvider (closing the SET-02/SET-06 verification gap)"
    - "page.tsx's trimRow derives the endpoint field server-side from the matched catalog row, flowing into BOTH servableByProvider and unionServableModels through the single shared trimRow call site"
  artifacts:
    - path: "src/components/settings/model-picker-logic.ts"
      provides: "endpoint field + endpointLabel/hermesCaptionLabel/rowCaption/resolveBadgeProvider + endpoint-aware searchValue"
      contains: "endpoint: 'zen' | 'go' | null"
    - path: "src/components/settings/model-picker-logic.test.ts"
      provides: "unit coverage for every new function + 4-provider fixture"
      contains: "resolveBadgeProvider"
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "server-side endpoint derivation in trimRow"
      contains: "endpoint:"
  key_links:
    - from: "src/app/(dashboard)/settings/page.tsx trimRow"
      to: "src/components/settings/model-picker-logic.ts ServableModel type"
      via: "return-object field assignment"
      pattern: "endpoint:"
    - from: "src/components/settings/model-picker-logic.test.ts"
      to: "src/components/settings/model-picker-logic.ts"
      via: "named imports + assertions"
      pattern: "resolveBadgeProvider|endpointLabel|rowCaption|hermesCaptionLabel"
---

<objective>
Extend the pure client-safe decision module (`model-picker-logic.ts`) with the four genuinely-new pieces of logic Phase 26 requires — OpenCode endpoint captions (SET-03), NousResearch Hermes captions (SET-04), and the badge-accuracy fix's testable core (SET-05, D-26-11) — plus the 4-provider test-fixture extension that closes the SET-01/02/06 verification gap RESEARCH.md's Pitfall 6 identified. Then wire the one server-side derivation point (`page.tsx`'s `trimRow`) that supplies the new `endpoint` field to both the provider-scoped and union picker props.

This plan defines the CONTRACT (types + pure functions, TDD) that Plan 26-02's client components consume — interface-first ordering. No `.tsx` rendering changes happen in this plan.

Purpose: give the client components in Plan 26-02 a fully-tested, drop-in logic surface so their edits are pure wiring, not decision-making.
Output: `model-picker-logic.ts` extended + unit-tested; `page.tsx` emitting the `endpoint` field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-settings-ui/26-CONTEXT.md
@.planning/phases/26-settings-ui/26-RESEARCH.md
@.planning/phases/26-settings-ui/26-PATTERNS.md
@.planning/phases/26-settings-ui/26-UI-SPEC.md
@.planning/phases/26-settings-ui/26-VALIDATION.md
@src/components/settings/model-picker-logic.ts
@src/components/settings/model-picker-logic.test.ts
@src/app/(dashboard)/settings/page.tsx
@src/lib/models/catalog.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend model-picker-logic.ts with endpoint/Hermes/badge-resolution logic + full test coverage</name>
  <files>src/components/settings/model-picker-logic.ts, src/components/settings/model-picker-logic.test.ts</files>
  <read_first>
    src/components/settings/model-picker-logic.ts (current 158-line module — every new export extends this file in place)
    src/components/settings/model-picker-logic.test.ts (current 351-line suite — `fixture`/`servableByProvider`/`defaults` are the existing 4-provider-shaped-but-2-populated test data)
    src/lib/models/catalog.ts (PROVIDER_PRECEDENCE l.123, SNAPSHOT_PROVIDER_IDS l.108-113, dedupeProviderRows l.130-135 — ground truth for why the Zen-wins dedup guarantees dual-listed opencode ids resolve `providerID === 'opencode'`, never `'opencode-go'`)
    .planning/phases/26-settings-ui/26-CONTEXT.md (D-26-01, D-26-03, D-26-04, D-26-05 [CORRECTED], D-26-06, D-26-07, D-26-11 — all locked, implement verbatim)
    .planning/phases/26-settings-ui/26-RESEARCH.md Pattern 1/2/3, Pitfall 2/3/4, Code Examples section
    .planning/phases/26-settings-ui/26-UI-SPEC.md "Row Anatomy" + "Copywriting Contract" sections (exact caption ordering and text)
  </read_first>
  <behavior>
    - `endpointLabel('zen')` returns `'Zen'`; `endpointLabel('go')` returns `'Go'`; `endpointLabel(null)` returns `null`.
    - `hermesCaptionLabel('nousresearch')` returns `'chat/reasoning-tuned'` (D-26-04's uniform descriptor, keyed on the RESOLVED `providerID`, never on `family` — Pitfall 4); `hermesCaptionLabel('openrouter')` returns `null` (the mirror row does NOT get the capability caption, even though it carries `family: 'hermes'` too); `hermesCaptionLabel('anthropic')` and `hermesCaptionLabel('opencode')` both return `null`.
    - `rowCaption({ id, providerID, endpoint })` composes `[endpointLabel(endpoint), suffixLabel(id), hermesCaptionLabel(providerID)].filter(Boolean).join(' · ')` and returns `null` when all three parts are null. Test the synthetic compound case (no live row has this today, per RESEARCH Anti-Patterns): a row with `endpoint: 'zen'` and `id: 'x:free'` produces exactly `'Zen · free tier — 50 req/day shared'` (endpoint first, suffix second per D-26-01 — use the literal `suffixLabel` return string, not an abbreviated paraphrase). A plain opencode-Go row (`endpoint: 'go'`, no suffix, `providerID: 'opencode'`) produces `'Go'`. A NousResearch row (`endpoint: null`, no suffix, `providerID: 'nousresearch'`) produces `'chat/reasoning-tuned'`. A row with none of the three (plain anthropic row) produces `null`.
    - `searchValue()` gains an optional `endpoint` field on its param type; `searchValue({ id: 'x', name: 'X', family: 'f', endpoint: 'go' })` includes the lowercase raw token `'go'` in the composite (append after family, same `filter(Boolean).join(' ').toLowerCase()` shape); `searchValue({ id: 'x', name: 'X', family: 'f' })` (no endpoint key, matching every EXISTING call site) produces byte-identical output to today — this is a strictly additive, backward-compatible change; verify the 3 existing `searchValue` tests in the file still pass unmodified.
    - `resolveBadgeProvider(value, unionModels, fallbackProvider)` returns `unionModels.find((m) => m.id === value)?.providerID ?? fallbackProvider`. Test the two verified real collisions: given a union fixture where `claude-sonnet-4-6` resolves to `providerID: 'anthropic'`, `resolveBadgeProvider('claude-sonnet-4-6', unionFixture, 'opencode')` returns `'anthropic'` (NOT `'opencode'` — the dropdown value is a decoy). Given a union fixture where `nousresearch/hermes-4-70b` resolves to `providerID: 'nousresearch'`, `resolveBadgeProvider('nousresearch/hermes-4-70b', unionFixture, 'openrouter')` returns `'nousresearch'`. Given a value absent from the union list, `resolveBadgeProvider('dropped-id', unionFixture, 'anthropic')` returns `'anthropic'` (the fallback — preserves current behavior for the empty/stale case).
    - `primaryAfterProviderSwitch`/`groupByProvider` 4-provider fixture: extend the test file's `servableByProvider` fixture (currently `nousresearch: []` and `opencode: []`) with real rows — one nousresearch row (`id: 'nousresearch/hermes-4-70b'`, `providerID: 'nousresearch'`, `endpoint: null`) and two opencode rows (a Zen-dual-listed-shaped id e.g. `'deepseek-v4-flash'`, `providerID: 'opencode'`, `endpoint: 'zen'`; a Go-exclusive-shaped id e.g. `'hy3'`, `providerID: 'opencode'`, `endpoint: 'go'`). Add a `primaryAfterProviderSwitch` case switching into `'nousresearch'` and into `'opencode'` (both keep-if-valid and reset-to-default branches) and a `groupByProvider` case proving all 4 provider keys bucket correctly when the fixture spans all 4.
  </behavior>
  <action>
    Add `endpoint: 'zen' | 'go' | null;` as a required field on the `ServableModel` type (l.15-22) — update ALL 5 existing fixture object literals in the test file to add `endpoint: null` (they are all non-opencode rows).

    Add `export function endpointLabel(endpoint: 'zen' | 'go' | null): string | null` immediately after `suffixLabel` (l.57), following the same single-input/string-or-null shape and comment density (cite D-26-01/D-26-03). Add `export function hermesCaptionLabel(providerID: ModelProviderId): string | null` right after it, citing D-26-04 and Pitfall 4 (the `providerID === 'nousresearch'` key, never `family`). Add `export function rowCaption(m: { id: string; providerID: ModelProviderId; endpoint: 'zen' | 'go' | null }): string | null` composing the three in D-26-01's locked order (endpoint, suffix, hermes) via `.filter((p): p is string => p !== null).join(' · ')`.

    Widen `searchValue`'s param type (l.46) to `{ id: string; name: string; family: string; endpoint?: 'zen' | 'go' | null }` and append `m.endpoint ?? ''` to the array before `.filter(Boolean).join(' ')` — the raw lowercase `'zen'`/`'go'` token per D-26-03, not the capitalized label.

    Add `export function resolveBadgeProvider(value: string, unionModels: ServableModel[], fallbackProvider: ModelProviderId): ModelProviderId` after `pinnedSelection` (end of file), citing D-26-11 and the two verified collision ids (`claude-sonnet-4-6`, the hermes pair) directly in the comment — this is the SET-05 fix's testable core; the `.tsx` (Plan 26-02) becomes a one-line call site.

    Do NOT touch `PROVIDER_NAMES`, `providerName`, `suffixLabel`, `isHighCost`, `staleIds`, `optionsForSlot`, or `triggerLabel` — all already correct/complete per D-26-06/07/08 (RESEARCH "Don't Hand-Roll").
  </action>
  <verify>
    <automated>npx vitest run src/components/settings/model-picker-logic.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/settings/model-picker-logic.test.ts` exits 0 with the existing ~35 assertions AND every new `describe` block green (no skipped tests).
    - `grep -n "export function resolveBadgeProvider" src/components/settings/model-picker-logic.ts` returns exactly one match.
    - `grep -n "export function rowCaption" src/components/settings/model-picker-logic.ts` returns exactly one match.
    - `grep -n "export function hermesCaptionLabel" src/components/settings/model-picker-logic.ts` returns exactly one match.
    - `grep -c "endpoint: null" src/components/settings/model-picker-logic.test.ts` returns a count >= 5 (all pre-existing fixture rows updated, none left as compile errors).
    - `hermesCaptionLabel('openrouter')` is asserted `null` in the test file (source assertion the mirror row is excluded from the capability caption, per Pitfall 4) — `grep -n "hermesCaptionLabel('openrouter')" src/components/settings/model-picker-logic.test.ts` returns a match.
    - **Scoped type-check (this task's gate — NOT the full project):** `npx tsc --noEmit 2>&1 | grep -v "settings/page.tsx"` produces no output. This task widens `ServableModel.endpoint` to a REQUIRED field, which by design breaks `page.tsx`'s `trimRow` return object (it doesn't populate `endpoint` yet — that's Task 2's job) with exactly one expected error: `src/app/(dashboard)/settings/page.tsx(58,5): error TS2741: Property 'endpoint' is missing`. That single, known, Task-2-owned line is excluded from this task's gate; every other file (including `model-picker-logic.ts` and its test file, which this task fully owns) must be clean. The full, unscoped `npx tsc --noEmit` (zero errors, no exclusions) is re-asserted at the plan level after Task 2 lands — see `<verification>` below.
  </acceptance_criteria>
  <done>model-picker-logic.ts exports endpointLabel, hermesCaptionLabel, rowCaption, resolveBadgeProvider, and an endpoint-aware searchValue; every function is unit-tested including the two verified real-collision cases and the 4-provider primaryAfterProviderSwitch/groupByProvider fixture; tsc is clean for every file this task owns (the single expected `page.tsx` error, caused by the now-required `endpoint` field, is resolved by Task 2 — see plan-level `<verification>` for the full-project tsc gate).</done>
</task>

<task type="auto">
  <name>Task 2: Derive the endpoint field server-side in page.tsx's trimRow</name>
  <files>src/app/(dashboard)/settings/page.tsx</files>
  <read_first>
    src/app/(dashboard)/settings/page.tsx (current 133-line file — `trimRow` at l.56-66 is the single shared function feeding both `servableByProvider` l.69-74 and `unionServableModels` l.83-85)
    src/components/settings/model-picker-logic.ts (as modified by Task 1 — the `endpoint` field this task must populate is now REQUIRED on `ServableModel`, so this file will fail `tsc` until this task lands)
    .planning/phases/26-settings-ui/26-RESEARCH.md Pitfall 1 (CONTEXT.md's `settings.ts` reference is a documentation error — the correct file is THIS one) and Pattern 1 (the exact derivation expression)
  </read_first>
  <action>
    In `trimRow`'s return object (l.58-65), add one field: `endpoint: provider === 'opencode' ? (m?.providerID === 'opencode-go' ? 'go' : 'zen') : null,`. This reads the ALREADY-matched row `m` (from `dedupeProviderRows(catalogJson, provider).find(...)` on l.57) — no new lookup, no new import. Since `provider === 'opencode'` is the caller's LOGICAL provider argument (not the snapshot row's raw providerID), and the Zen-wins dedup (`dedupeProviderRows`) guarantees a dual-listed id's matched row has `providerID === 'opencode'` (never `'opencode-go'`) unless it's one of the 5 Go-exclusive ids, the ternary is exhaustive and correct for all opencode-scoped rows; every non-opencode `provider` value yields `endpoint: null`.

    Update the comment block immediately above `trimRow` (or add a new one directly above the new line) citing SET-03 — describe the derivation rule in prose (opencode-go row wins 'go', else 'zen', else null) rather than hardcoding a specific row-count split (RESEARCH Pitfall 5 — the live snapshot's zen/go counts drift on every catalog refresh, do not commit a stale number to a comment).

    This single change point flows into BOTH `servableByProvider` (l.69-74, per-provider primary picker) AND `unionServableModels` (l.83-85, union fallback picker + saved-chain recap), since both already call this shared `trimRow` — satisfies SET-03's "BOTH the provider-scoped primary and union fallback pickers" with zero duplication.

    Do NOT touch `src/app/actions/settings.ts` — its union-membership save gate is already provider-count-agnostic (REG-07, verified Complete); this phase adds zero new save-path code (RESEARCH Pitfall 1 correction).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "endpoint: provider === 'opencode'" src/app/\(dashboard\)/settings/page.tsx` returns exactly one match inside `trimRow`.
    - `npx tsc --noEmit` exits 0 — the FULL, unscoped project-wide check (both `servableByProvider` and `unionServableModels` type-check against the widened `ServableModel` with zero additional edits needed at either call site, and Task 1's single known-expected error is now resolved — this is the plan's full tsc-clean gate).
    - `grep -n "settings\.ts" src/app/(dashboard)/settings/page.tsx` returns no match (confirms this task did not touch the Server Action file, per the Pitfall 1 correction).
  </acceptance_criteria>
  <done>trimRow returns a fully-populated `endpoint: 'zen' | 'go' | null` on every ServableModel it produces; both servableByProvider and unionServableModels carry the field with zero other code changes; tsc is clean project-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog.json (server-only) -> model-picker-logic.ts (client-bundle) | The 1000+-row committed catalog snapshot (incl. cost data) must never cross into a `'use client'` module — only pre-trimmed, server-validated `ServableModel[]` data may cross |
| page.tsx (server component) -> client props | The new `endpoint` field is derived server-side from a matched catalog row and threaded as plain data — never re-derived client-side from the id string (RESEARCH Anti-Patterns: the id itself carries no Zen/Go signal) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | `model-picker-logic.ts` | mitigate | Every new function (`endpointLabel`, `hermesCaptionLabel`, `rowCaption`, `resolveBadgeProvider`, extended `searchValue`) takes only already-trimmed `ServableModel`/primitive params — zero new imports added to the file beyond the existing type-only `ModelProviderId` import (T-17-09); `npx tsc --noEmit` plus the file's unchanged import list is the verification |
| T-26-02 | Information Disclosure | `page.tsx` trimRow | mitigate | The `endpoint` derivation reads only the already-in-scope matched row `m` from the existing `dedupeProviderRows(...).find(...)` call — no new catalog read, no client-side re-derivation; the field is plain string/null data threaded through the existing props boundary exactly like `family`/`providerID` were in Phase 21 |
| T-26-03 | Information Disclosure | any new comment/string literal in this plan's files | mitigate | Neither `NOUSRESEARCH_API_KEY` nor `OPENCODE_API_KEY` (nor any other secret name) is introduced — only lowercase provider ids (`'zen'`, `'go'`, `'nousresearch'`, `'opencode'`) and existing display names are used (Pitfall 8); acceptance criteria include a grep-verifiable absence check in Plan 26-02 for the client-rendering half |
</threat_model>

<verification>
1. `npx vitest run src/components/settings/model-picker-logic.test.ts` — full new + existing suite green.
2. `npx tsc --noEmit` — clean project-wide, FULL and unscoped (proves the ServableModel field-widening propagates correctly through every existing consumer without additional edits outside this plan's 2 files; this is the gate Task 1's own acceptance criteria intentionally scope around, since it can only genuinely pass once Task 2 has landed).
3. `npm test` — full suite green (catches any accidental regression in `catalog.test.ts`'s unrelated canaries).
</verification>

<success_criteria>
- `ServableModel` carries a required `endpoint: 'zen' | 'go' | null` field, populated server-side only.
- `endpointLabel`, `hermesCaptionLabel`, `rowCaption`, `resolveBadgeProvider` are exported, pure, and fully unit-tested including the two verified real-provider-collision cases.
- `searchValue` is backward-compatible (existing call sites unchanged) and endpoint-aware for new call sites.
- `primaryAfterProviderSwitch`/`groupByProvider` have 4-provider fixture coverage (SET-02/SET-06 verification gap closed).
- `page.tsx`'s `trimRow` is the single, correct derivation point for `endpoint` — no duplication, no client-side re-derivation.
</success_criteria>

<output>
Create `.planning/phases/26-settings-ui/26-01-SUMMARY.md` when done
</output>
