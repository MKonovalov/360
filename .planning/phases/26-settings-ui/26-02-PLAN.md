---
phase: 26-settings-ui
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/components/settings/model-picker.tsx
  - src/components/settings/model-settings-form.tsx
autonomous: true
requirements: [SET-02, SET-03, SET-04, SET-05, SET-06]

must_haves:
  truths:
    - "The primary model picker's closed trigger badge always shows the model's TRUE resolved provider (derived via resolveBadgeProvider from unionServableModels), never the raw AI Provider dropdown selection — fixing the D-26-11 bug for the two verified real collisions (claude-sonnet-4-6 anthropic-vs-opencode; the two hermes ids nousresearch-vs-openrouter)"
    - "OpenCode rows render a single composed caption (endpoint first, suffix second per D-26-01) in both the primary and union fallback pickers; NousResearch Hermes rows render the honest chat/reasoning-tuned capability caption AND their real per-MTok cost caption (D-26-05 corrected — no suppression); the OpenRouter Hermes mirror row shows real cost but never the capability caption"
    - "The saved-chain recap shows the endpoint caption per row after Save, disambiguating Zen vs Go post-save (D-26-02)"
    - "The provider-switch reset hint states the true routing fact for any keep-if-valid collision case (generalized beyond the single known claude-sonnet-4-6 case), never a false routing claim (D-26-09 corrected, Pitfall 7)"
    - "Typing 'go' or 'zen' in the Command search filters to the matching endpoint's rows in both pickers (D-26-03)"
  artifacts:
    - path: "src/components/settings/model-picker.tsx"
      provides: "composed row-caption rendering (endpoint + suffix + hermes) via rowCaption()"
      contains: "rowCaption("
    - path: "src/components/settings/model-settings-form.tsx"
      provides: "resolveBadgeProvider-driven primary trigger badge, endpoint-aware saved-chain recap, corrected reset-hint copy"
      contains: "resolveBadgeProvider("
  key_links:
    - from: "src/components/settings/model-settings-form.tsx primary ModelPicker badge prop"
      to: "src/components/settings/model-picker-logic.ts resolveBadgeProvider"
      via: "function call"
      pattern: "badge={resolveBadgeProvider\\("
    - from: "src/components/settings/model-picker.tsx CommandItem caption span"
      to: "src/components/settings/model-picker-logic.ts rowCaption"
      via: "function call"
      pattern: "rowCaption\\(m\\)"
    - from: "src/components/settings/model-settings-form.tsx saved-chain recap"
      to: "src/components/settings/model-picker-logic.ts endpointLabel"
      via: "function call on the union-resolved row"
      pattern: "endpointLabel\\("
---

<objective>
Wire the two client components (`model-picker.tsx`, `model-settings-form.tsx`) to the pure logic surface Plan 26-01 built: render the composed endpoint/Hermes caption in picker rows, fix the primary trigger badge bug (SET-05/D-26-11 — the phase's one real, load-bearing correctness fix), add the endpoint caption to the saved-chain recap (D-26-02), and correct the provider-switch reset-hint copy so it never claims a routing change that doesn't happen (D-26-09 corrected, Pitfall 7).

This is the phase's ONLY rendering-layer change — all decision logic already exists and is tested (Plan 26-01). Every edit here is a call-site wiring change to files/lines RESEARCH.md and PATTERNS.md already pinpointed exactly.

Purpose: deliver the phase's user-visible outcome — honest, unambiguous 4-provider Settings UI.
Output: model-picker.tsx + model-settings-form.tsx extended; phase-level verification (full suite, tsc, build) green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-settings-ui/26-CONTEXT.md
@.planning/phases/26-settings-ui/26-RESEARCH.md
@.planning/phases/26-settings-ui/26-PATTERNS.md
@.planning/phases/26-settings-ui/26-UI-SPEC.md
@.planning/phases/26-settings-ui/26-VALIDATION.md
@src/components/settings/model-picker.tsx
@src/components/settings/model-settings-form.tsx
@src/components/settings/model-picker-logic.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the endpoint/Hermes caption into model-picker.tsx's row rendering</name>
  <files>src/components/settings/model-picker.tsx</files>
  <read_first>
    src/components/settings/model-picker.tsx (current 220-line file — the caption slot at l.164-193 inside the row `CommandItem`, the current `suffixLabel`-only span at l.172-174, the grouped badge at l.166-170)
    src/components/settings/model-picker-logic.ts (as extended by Plan 26-01 — `rowCaption`, `endpointLabel`, `hermesCaptionLabel` are the new imports this task consumes; `searchValue` is unchanged in signature at call sites since `m: ServableModel` now structurally includes `endpoint`)
    .planning/phases/26-settings-ui/26-UI-SPEC.md "Row Anatomy (model picker rows) — EXTENDED" section (the exact prescriptive row structure and D-26-01 ordering)
  </read_first>
  <action>
    Add `rowCaption` to the existing named import from `./model-picker-logic` (l.24-32, alongside `groupByProvider`, `isHighCost`, etc.) — `suffixLabel` import can stay or be removed; if `rowCaption` fully subsumes the caption-slot rendering, remove the now-unused `suffixLabel` import to keep `tsc`/lint clean (verify with a grep after the edit — no other call site in this file references `suffixLabel` directly once replaced).

    Replace the inline suffix-only conditional at l.172-174 (`{suffixLabel(m.id) ? (<span ...> {suffixLabel(m.id)}</span>) : null}`) with a single composed-caption span driven by `rowCaption(m)`: `{rowCaption(m) ? (<span className="text-[12px] font-normal leading-[1.4] text-slate-500"> · {rowCaption(m)}</span>) : null}`. `rowCaption` already internally joins its parts with `' · '`, so this renders exactly `· Zen · free tier — 50 req/day shared` for a synthetic compound row, `· Go` for a plain Go-exclusive row, `· chat/reasoning-tuned` for a NousResearch row, and nothing (`null`, no span) for a plain row with none of the three — this is the SAME insertion point (before the cost caption span at l.175-182), and D-26-01's "endpoint first, suffix second" ordering is already guaranteed by `rowCaption`'s internal composition (built and tested in Plan 26-01), so no ordering logic is needed here.

    The cost-caption span at l.175-182 stays UNCHANGED — per D-26-05 (corrected), no suppression logic applies; the existing unconditional `isHighCost`/`text-amber-700`/`text-slate-500` rendering already produces the correct honest behavior for BOTH native NousResearch rows and their OpenRouter mirror (both carry real, non-zero cost in the live snapshot). Add a one-line comment directly above this span citing D-26-05 (corrected) so a future reader does not "fix" this into a suppression rule.

    Do NOT modify the grouped-mode provider badge (l.166-170) — already correct per D-26-06/07, uniform `secondary` variant, provider-only text, no endpoint compositing into the badge. Do NOT modify the `searchValue(m)` call at l.150 — since `m: ServableModel` now structurally carries `endpoint` (Plan 26-01), passing the whole object already includes it in the composite with zero code change here; verify this by confirming the search-filter behavior in acceptance criteria below rather than editing the call site.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "rowCaption(m)" src/components/settings/model-picker.tsx` returns at least one match inside the `CommandItem` render.
    - `grep -c "suffixLabel(m.id)" src/components/settings/model-picker.tsx` returns 0 (the old inline conditional is fully replaced, not duplicated alongside the new one).
    - `npx tsc --noEmit` exits 0.
    - Source assertion: the cost-caption span (`{' '}· \\$\\{m.costInput\\}`) is present unmodified and unconditioned on `m.providerID` — `grep -n "costInput} / \\$" src/components/settings/model-picker.tsx` shows no surrounding `providerID ===` conditional on the same or adjacent line (confirms D-26-05's "no suppression" resolution was implemented, not silently reversed).
    - Behavior (deferred to end-of-phase manual verification per `human_verify_mode: end-of-phase` — see plan-level `<verification>`): opening the OpenCode-scoped primary picker shows `· Zen` on Zen-dual-listed rows and `· Go` on Go-exclusive rows; opening the NousResearch-scoped primary picker shows `· chat/reasoning-tuned` on both Hermes rows with real per-MTok cost; the same Hermes ids under the OpenRouter-scoped picker show real cost but NOT the capability caption.
  </acceptance_criteria>
  <done>model-picker.tsx renders one composed caption per row via rowCaption(), replacing the old suffix-only span, with the cost caption unchanged and unconditional; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the primary trigger badge, add the recap endpoint caption, correct the reset-hint copy in model-settings-form.tsx</name>
  <files>src/components/settings/model-settings-form.tsx</files>
  <read_first>
    src/components/settings/model-settings-form.tsx (current 401-line file — the bug at l.258 `badge={provider}`, the already-correct fallback pattern at l.308, the recap block at l.368-385, `handleProviderChange` at l.156-180)
    src/components/settings/model-picker-logic.ts (as extended by Plan 26-01 — `resolveBadgeProvider`, `endpointLabel`, `providerName` are the imports this task adds/uses)
    .planning/phases/26-settings-ui/26-UI-SPEC.md "Primary trigger badge (SET-05, D-26-11)" and "Provider-switch reset hint" rows of the Copywriting Contract (the exact locked corrected copy string)
    .planning/phases/26-settings-ui/26-RESEARCH.md Pitfall 3 (the verified bug) and Pitfall 7 (the verified factual-accuracy problem in the originally-proposed hint copy)
  </read_first>
  <action>
    Add `resolveBadgeProvider` and `endpointLabel` to the existing named import from `./model-picker-logic` (l.21-26, alongside `optionsForSlot`, `primaryAfterProviderSwitch`, `providerName`, `staleIds as computeStaleIds`).

    **Badge fix (D-26-11):** change l.258 from `badge={provider}` to `badge={resolveBadgeProvider(primary, unionServableModels, provider)}`. This mirrors the already-correct fallback-picker pattern at l.308 but with `resolveBadgeProvider`'s `?? provider` fallback (not `?? undefined`) since the primary slot always has SOME value (never the empty-fallback-row sentinel `''` the fallback picker's `?? undefined` exists for).

    **Recap endpoint caption (D-26-02):** in the `.map()` body at l.373-383, capture the union lookup ONCE per iteration instead of calling `.find()` twice: `const resolved = unionServableModels.find((m) => m.id === id);` then use `resolved?.providerID ?? 'anthropic'` for the existing `Badge`/`providerName` call (replacing the current inline `.find()`), and add a new inline span AFTER the name (`{savedChain?.find((sc) => sc.id === id)?.name ?? id}`) rendering `resolved?.endpoint` via `endpointLabel`: `{resolved?.endpoint ? (<span className="text-[12px] font-normal leading-[1.4] text-slate-500"> · {endpointLabel(resolved.endpoint)}</span>) : null}`.

    **Reset-hint correction (D-26-09 corrected, Pitfall 7):** in `handleProviderChange` (l.156-180), the keep-if-valid branch currently at l.173-175 (`} else { setResetHint(null); }`) must detect the collision case generically — not hardcode `claude-sonnet-4-6` — so it stays correct if a future catalog refresh introduces a new overlapping id. Replace that `else` branch with: look up `const resolvedProvider = unionServableModels.find((m) => m.id === result.primary)?.providerID;`. If `resolvedProvider` is defined AND `resolvedProvider !== next`, call `setResetHint` with the template literal `` `${unionServableModels.find((m) => m.id === result.primary)?.name ?? result.primary} stays routed through ${providerName(resolvedProvider)} — ${providers.find((p) => p.id === next)?.name ?? next}'s copy isn't used while a higher-priority provider serves the same id.` ``. Otherwise (no divergence — the genuinely-unchanged case), `setResetHint(null)` exactly as today. For the ONLY live collision today (`claude-sonnet-4-6` switching into `opencode` while `anthropic` still resolves it), this produces the EXACT locked UI-SPEC copy: `"Claude Sonnet 4.6 stays routed through Anthropic — OpenCode's copy isn't used while a higher-priority provider serves the same id."` — verify this literal string is producible by the template before marking done.

    Do NOT add a "only N models available" note anywhere for the NousResearch (2-row) or Anthropic (1-row) sparse pickers — D-26-10 is a locked "stay silent" decision; the existing `pinnedSelection`/`onlyModel` explanation (unchanged from Phase 21, WR-02) already covers the single-model case and needs no extension.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "badge={provider}" src/components/settings/model-settings-form.tsx` returns 0 (the bug is gone, not duplicated).
    - `grep -n "badge={resolveBadgeProvider(primary, unionServableModels, provider)}" src/components/settings/model-settings-form.tsx` returns exactly one match.
    - `grep -n "endpointLabel(resolved.endpoint)" src/components/settings/model-settings-form.tsx` returns exactly one match (recap caption wired).
    - `grep -n "stays routed through" src/components/settings/model-settings-form.tsx` returns exactly one match (the corrected hint template, not the old "now serves via OpenCode Zen" copy — `grep -c "now serves via" src/components/settings/model-settings-form.tsx` returns 0).
    - `grep -n "only.*models\? available\|only available.*model" src/components/settings/model-settings-form.tsx` — no NEW occurrence beyond the pre-existing `pinnedSelection`/`onlyModel` render in `model-picker.tsx` (D-26-10 — this file itself should have zero such strings; the existing "only available {provider} model" string lives in `model-picker.tsx`, not here, and stays untouched).
    - `npx tsc --noEmit` exits 0.
    - Behavior (deferred to end-of-phase manual verification): selecting `claude-sonnet-4-6` as primary, then switching the AI Provider dropdown to OpenCode, shows the closed trigger badge "Anthropic" (not "OpenCode") AND the reset hint reads the corrected routed-through copy verbatim.
  </acceptance_criteria>
  <done>The primary trigger badge always reflects the true resolved provider; the saved-chain recap shows an endpoint caption per applicable row; the provider-switch reset hint generalizes the collision detection and never claims a false routing change; tsc is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings form draft state -> displayed badge/hint | The badge and hint are UI-only trust signals a staff member relies on to infer which provider/API key will actually serve a request — no server round-trip validates them at render time (the underlying data IS server-validated `unionServableModels`, but the RENDERING logic choosing which field to display is where the Pitfall 3 bug lived) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | Spoofing (of routing intent, not identity) | `model-settings-form.tsx` primary `ModelPicker` `badge` prop | mitigate | D-26-11 fix: `badge={resolveBadgeProvider(primary, unionServableModels, provider)}` replaces the raw dropdown value with the precedence-resolved TRUE provider — closes the real, verified bug where a staff member could believe `claude-sonnet-4-6` or a Hermes id routes through OpenCode/OpenRouter when it actually always routes through Anthropic/NousResearch (real cost/billing/data-residency implications given 4 distinct external inference providers with separate API keys) |
| T-26-05 | Information Disclosure | `model-settings-form.tsx` reset-hint copy | mitigate | The corrected D-26-09 hint template only interpolates provider DISPLAY NAMES (`providerName()`, already client-bundle-safe per T-17-09) and model display names already present in `unionServableModels`/`savedChain` — never an env var key name or raw catalog internals; acceptance criteria grep for the absence of `NOUSRESEARCH_API_KEY`/`OPENCODE_API_KEY` literals (Pitfall 8) |
| T-26-06 | Tampering (false-honesty regression) | `model-picker.tsx` cost-caption span | accept (verified non-issue) | D-26-05's corrected resolution requires the cost caption to render UNCONDITIONALLY (no suppression) for both native NousResearch rows and their OpenRouter mirror — Task 1's acceptance criteria include a grep proving no `providerID ===` conditional was added around the cost span, locking this against a future "helpful" suppression regression |
</threat_model>

<verification>
1. `npx vitest run src/components/settings/model-picker-logic.test.ts` — Plan 26-01's suite stays green (no regression from the new import call sites in this plan's files).
2. `npm test` — full suite green, including `src/app/actions/settings.test.ts`'s existing REG-07 4-provider save-path test (verified pre-existing, `settings.ts` untouched by this phase) and `catalog.test.ts`'s row-count canaries (catches any accidental hardcoding of stale counts, Pitfall 5).
3. `npx tsc --noEmit` — clean project-wide.
4. `npm run build` (or `next build`) — exit 0, confirming the client bundle compiles with the new caption/badge logic and no accidental value-import of `catalog.ts` in a `'use client'` file (T-17-09).

### Manual-Only Verifications (deferred to end-of-phase per `workflow.human_verify_mode: end-of-phase` — see `.planning/phases/26-settings-ui/26-VALIDATION.md` "Manual-Only Verifications" table for the full instructions)
- Full 4-provider Select -> Picker -> Save round trip in the live browser (SET-01/02/06).
- OpenCode Zen/Go caption rendering in the actual Combobox UI, both picker and recap (SET-03).
- Reset-hint copy accuracy for the claude-sonnet-4-6 collision case (SET-05/D-26-09).
- Trigger badge accuracy for both verified collision ids (SET-05/D-26-11).
</verification>

<success_criteria>
- The primary trigger badge bug (D-26-11) is fixed and unit-coverage-backed via `resolveBadgeProvider` (Plan 26-01).
- Picker rows (primary + union fallback) render the composed endpoint/Hermes/suffix caption per D-26-01's locked ordering.
- The saved-chain recap disambiguates Zen vs Go post-save (D-26-02).
- The reset hint never states a false routing claim, and generalizes beyond the single known collision id (D-26-09 corrected).
- Full automated suite + tsc + build all green; manual verification checklist handed off for end-of-phase execution.
</success_criteria>

<output>
Create `.planning/phases/26-settings-ui/26-02-SUMMARY.md` when done
</output>
