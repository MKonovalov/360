---
phase: 29-signals-ui-v1-6-queued
plan: 08
type: execute
wave: 5
depends_on: ["29-07"]
files_modified: []
autonomous: false
requirements: [SIG-02, SIG-04, SIG-05, SIG-06, SIG-07]

must_haves:
  truths:
    - "A human has visually confirmed the two-tab layout, table columns, and Sheet form UX match the UI-SPEC contract against live GBS seed data"
  artifacts: []
  key_links: []
---

<objective>
Close out the phase with the manual QA pass explicitly called for in `29-VALIDATION.md`'s "Manual-Only Verifications" table — the visual/interaction correctness of the two-tab layout, table columns (both entity kinds), and the Sheet-based create/edit form, none of which are meaningfully unit-testable and all of which the UI-SPEC locks an exact contract for.

Purpose: Final human sign-off on SIG-02, SIG-04, SIG-05, SIG-06, SIG-07's visual/UX dimensions before the phase is considered shippable — automated coverage (Plans 29-01 through 29-07) already proves the logic/data correctness; this closes the "looks and feels right" gap.
Output: Confirmed/flagged UAT result; no code changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md
@.planning/phases/29-signals-ui-v1-6-queued/29-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual QA — two-tab layout, table columns, Sheet form UX (SIG-02/04/05/06/07)</name>
  <files>none (visual/interaction verification only, no code changes)</files>
  <read_first>
    .planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md (full file — the exact visual/copy/spacing contract being verified)
    .planning/phases/29-signals-ui-v1-6-queued/29-VALIDATION.md (Manual-Only Verifications table — the source of this checklist)
  </read_first>
  <action>
    Claude confirms the dev server is running against a staff-authenticated session (or starts `npm run dev` and signs in), navigates to `/signals`, and walks through the 7-step verification checklist below, reporting the result of each step to the human for final sign-off. This is a verification task, not an implementation task — no files are modified.
  </action>
  <what-built>
    A new `/signals` screen (Manage sidebar → Signals) with a Company Signals / Persona Signals two-tab layout, each tab showing a filterable table (Practice Area / Category / Status / free-text search) backed by the live GBS seed data (27 company signals, 12 persona signals, 11 offerings, 5 buyer roles), a "New [Kind] Signal" Sheet-based create form, per-row Edit (same Sheet, pre-populated) and Archive (confirm Dialog, soft status flip to `retired`) actions, and a Linked Offerings count/expand disclosure per row.
  </what-built>
  <how-to-verify>
    1. Sign in and navigate to the app's `Manage` sidebar group — confirm a new `Signals` item (Radar icon) appears as a sibling to `Reviews`/`Settings`, and clicking it navigates to `/signals` with the item highlighted active.
    2. On `/signals`, confirm two tabs render: `Company Signals` (active/default) and `Persona Signals`. Confirm the Company Signals table shows columns Name, Category, Practice Area, Linked Offerings, Status, Last updated for real seeded rows; switch to the Persona Signals tab and confirm the same columns plus a Buyer Role column, populated with real buyer role names (CFO, COO, Head of GBS, Transformation Sponsor, CIO).
    3. Test each filter independently (Practice Area, Category, Status, free-text search) and confirm the table narrows correctly; clear all filters and confirm the full seeded set returns.
    4. Click "New Company Signal" — confirm a right-side Sheet opens with fields in order Name → Practice Area → Category → Description → Linked Offerings (checkbox list, scoped to the selected Practice Area's active offerings) → Status. Fill required fields and Save — confirm the Sheet closes and the new row appears in the table. Repeat for "New Persona Signal", confirming the Buyer Role field is required (Save is disabled/blocked until a role is selected) and appears directly after Practice Area.
    5. Click "Edit" on an existing row — confirm the Sheet opens pre-populated with that row's current values (including its currently-checked Linked Offerings). Change a field, Save, and confirm the table reflects the update.
    6. Click "Archive" on a row — confirm a confirm Dialog opens with title "Archive this signal?" and the reversible-language body copy, with a near-black (NOT red) "Archive" confirm button. Confirm, and verify the row remains visible in the table with a "Retired"/muted treatment rather than disappearing.
    7. Click a non-zero Linked Offerings count badge — confirm it discloses the linked offering names.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass as described, or describe which check failed and what was observed instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan performs no code changes; it is a verification pass over the write paths and rendering already threat-modeled in Plans 29-01 through 29-07.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08-01 | N/A | Manual QA checkpoint | accept | No new attack surface introduced by this plan; the full STRIDE register for the phase's write paths lives in Plan 29-03 (Server Actions) |

</threat_model>

<verification>
Human-in-the-loop confirmation per the 7-step checklist above; this is the phase's final gate before considering Phase 29 shippable.
</verification>

<success_criteria>
All 7 manual checks pass, confirming SIG-02, SIG-04, SIG-05, SIG-06, SIG-07's visual/interaction contracts are met against live data, completing the phase alongside the automated coverage from Plans 29-01 through 29-07.
</success_criteria>

<output>
Create `.planning/phases/29-signals-ui-v1-6-queued/29-08-SUMMARY.md` when done
</output>
