---
phase: 29-signals-ui-v1-6-queued
plan: 04
type: execute
wave: 2
depends_on: ["29-03"]
files_modified:
  - src/components/ui/checkbox.tsx
  - src/components/signals/linked-offerings-picker.tsx
  - src/components/signals/signal-form.tsx
autonomous: true
requirements: [SIG-06, SIG-07, SIG-09]

must_haves:
  truths:
    - "Staff can open a Sheet-based form (D-02) to create or edit a Company Signal or a Persona Signal — one component, signalKind prop"
    - "The Persona Signal form requires a Buyer Role selection before it can be submitted"
    - "The Linked Offerings picker only ever shows active offerings scoped to the signal's Practice Area (SIG-09) — never draft offerings"
  artifacts:
    - path: "src/components/signals/signal-form.tsx"
      provides: "SignalForm({signalKind, mode, signal?, practiceAreas, buyerRoles, categories, activeOfferingsByPracticeAreaId, existingLinkedOfferingIds?, trigger}) — the first CRUD form component in this codebase"
    - path: "src/components/signals/linked-offerings-picker.tsx"
      provides: "LinkedOfferingsPicker — a pure-renderer checkbox list scoped to the form's selected Practice Area"
  key_links:
    - from: "src/components/signals/signal-form.tsx"
      to: "src/app/actions/signals.ts"
      via: "createCompanySignalAction / updateCompanySignalAction / createPersonaSignalAction / updatePersonaSignalAction called inside useTransition"
      pattern: "SignalAction"
---

<objective>
Build the Sheet-based create/edit CRUD form — `SignalForm`, parameterized by `signalKind: 'company' | 'persona'` per D-02's "one form, two modes" — and its `LinkedOfferingsPicker` checkbox-list dependency. This is the first CRUD form in the codebase (RESEARCH.md: "no direct full-CRUD-form analog exists"); composed from `reject-dialog.tsx`'s controlled-form-state/`useTransition`/error-handling shape plus `sheet.tsx`'s container primitives.

Purpose: SIG-06/SIG-07 (create/edit both signal kinds with the exact field list), SIG-09 (Linked Offerings scoped to active + selected Practice Area only).
Output: `checkbox.tsx` (vendored), `linked-offerings-picker.tsx`, `signal-form.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-signals-ui-v1-6-queued/29-CONTEXT.md
@.planning/phases/29-signals-ui-v1-6-queued/29-RESEARCH.md
@.planning/phases/29-signals-ui-v1-6-queued/29-PATTERNS.md
@.planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor Checkbox primitive + LinkedOfferingsPicker</name>
  <files>src/components/ui/checkbox.tsx, src/components/signals/linked-offerings-picker.tsx</files>
  <read_first>
    src/components/ui/scroll-area.tsx (vendored, bounding container to reuse verbatim)
    src/components/companies/company-filters.tsx (the "options always come from props, never re-filtered client-side" principle to preserve — EnumFilterSelect's design intent, not its code)
    .planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md (Component Inventory section — Checkbox vendoring instruction; Copywriting Contract — "No active offerings for this Practice Area yet." empty caption)
  </read_first>
  <action>
    Run `npx shadcn add checkbox` to vendor `src/components/ui/checkbox.tsx` from the official shadcn registry (do not hand-write it). Create `src/components/signals/linked-offerings-picker.tsx` as a client component (`'use client'`) exporting `LinkedOfferingsPicker({ offerings, selectedIds, onChange }: { offerings: Array<{ id: number; name: string }>; selectedIds: number[]; onChange: (nextIds: number[]) => void })`. Render a `ScrollArea` (from `@/components/ui/scroll-area`, max-height container, e.g. `h-40`) wrapping one row per offering: a `Checkbox` (`checked={selectedIds.includes(offering.id)}`, `onCheckedChange={(checked) => onChange(checked ? [...selectedIds, offering.id] : selectedIds.filter(id => id !== offering.id))}`) paired with a `<label>{offering.name}</label>`, each row `flex items-center gap-2`. If `offerings.length === 0`, render the empty caption "No active offerings for this Practice Area yet." instead of an empty scroll box (UI-SPEC Copywriting Contract). This component is a PURE RENDERER — it receives `offerings` already scoped (active + selected Practice Area) as a prop from the parent `SignalForm` and must never re-filter or re-fetch internally (RESEARCH.md's `EnumFilterSelect` "options always from props" principle, applied here).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "checkbox\.tsx|linked-offerings-picker\.tsx" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ui/checkbox.tsx` exists and exports `Checkbox` (vendored file, do not hand-author its internals)
    - `npx tsc --noEmit` reports zero errors in `linked-offerings-picker.tsx`
    - `grep -n "'use client'" src/components/signals/linked-offerings-picker.tsx` confirms the directive
    - `grep -n "No active offerings for this Practice Area yet" src/components/signals/linked-offerings-picker.tsx` confirms the exact UI-SPEC copy
    - The component's props type has no internal `fetch`/`useEffect` data-loading — grep confirms no `fetch(` or query-layer import in this file
  </acceptance_criteria>
  <done>`LinkedOfferingsPicker` renders a scoped, pre-fetched checkbox list with the correct empty-state copy, never re-filtering client-side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SignalForm — Sheet-based create/edit, both entity kinds</name>
  <files>src/components/signals/signal-form.tsx</files>
  <read_first>
    src/components/reviews/reject-dialog.tsx (full file, 150 lines — the controlled-form-state + useTransition + error-handling shape to adapt: open state, reset-on-close, startTransition wrapping the action call, inline `text-red-600` error)
    src/components/ui/sheet.tsx (full file — Sheet/SheetTrigger/SheetContent/SheetHeader/SheetFooter/SheetTitle/SheetDescription; default `side="right"`, default `sm:max-w-sm`; do NOT edit this file — SheetTitle's `text-base font-medium` is an accepted vendor exception, Pitfall 4)
    src/components/ui/select.tsx, src/components/ui/input.tsx, src/components/ui/textarea.tsx, src/components/ui/button.tsx (vendored primitives to compose the form fields from)
    src/app/actions/signals.ts (the 4 create/update actions this form calls — exact signatures from Plan 29-03)
    src/lib/db/schema.ts lines 298-306 (`catalogStatusEnum.enumValues` for the Status `Select`)
    .planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md (Row Anatomy — Sheet form field order; Copywriting Contract — CTA labels, error copy, Category placeholder; Color section — primary CTA uses `variant="default"`)
  </read_first>
  <behavior>
    - `signalKind='company'`, `mode='create'`: submitting valid required fields (Name, Practice Area, Category, Description) calls `createCompanySignalAction` with the form values mapped to its input shape, including the array of checked `LinkedOfferingsPicker` ids as `offeringIds`
    - `signalKind='persona'`, `mode='create'`: the form additionally renders a required Buyer Role `Select`; submitting without a Buyer Role selected is blocked client-side (Save button disabled) before any action call
    - `mode='edit'`: the form is pre-populated from the `signal` prop (including its currently-linked offering ids from `existingLinkedOfferingIds`); submitting calls `updateCompanySignalAction(signal.id, ...)` / `updatePersonaSignalAction(signal.id, ...)`
    - A failed action result (`ok:false`) renders the inline error copy and keeps the Sheet open; a successful result closes the Sheet and calls `router.refresh()` so the caller's list re-renders with the new/updated row
    - Closing the Sheet (Cancel or overlay dismiss) resets all local field state, mirroring `RejectDialog.handleOpenChange`'s reset-on-close pattern
  </behavior>
  <action>
    Create `src/components/signals/signal-form.tsx` as a client component. Props: `{ signalKind: 'company' | 'persona'; mode: 'create' | 'edit'; signal?: { id: number; practiceAreaId: number; name: string; category: string; description: string; status: 'active'|'draft'|'retired'; buyerRoleId?: number }; existingLinkedOfferingIds?: number[]; practiceAreas: Array<{id:number; name:string}>; buyerRoles: Array<{id:number; name:string}>; categories: string[]; activeOfferingsByPracticeAreaId: Record<number, Array<{id:number; name:string}>>; trigger: React.ReactNode }`. Internal state: `open` (boolean), one controlled field per form value (`practiceAreaId`, `buyerRoleId`, `name`, `category`, `description`, `status`, `offeringIds: number[]`), `error` (string|null), `pending`/`startTransition` from `useTransition`, and `router` from `next/navigation`'s `useRouter`. Initialize field state from `signal`/`existingLinkedOfferingIds` when `mode==='edit'` (via `useState` initializers, re-synced on `open` transitioning true if the Sheet is reused across renders — simplest: reset state inside `handleOpenChange` when opening in edit mode).

    Field order per UI-SPEC Row Anatomy — Company: Name (`Input`) → Practice Area (`Select`, options from `practiceAreas` prop) → Category (`Input` with a native `<datalist>` populated from the `categories` prop, placeholder `"e.g. GBS-state, Financial & commercial…"`) → Description (`Textarea`) → Linked Offerings (`<LinkedOfferingsPicker offerings={activeOfferingsByPracticeAreaId[practiceAreaId] ?? []} selectedIds={offeringIds} onChange={setOfferingIds} />` — plain `Select`/checkbox-list per D-04, NOT the vendored Command+Popover combobox used by Settings' model picker) → Status (`Select`, `catalogStatusEnum.enumValues`, default `'active'` on create). Persona: same, with Buyer Role (`Select`, required, options from `buyerRoles` prop) inserted directly after Practice Area — a PLAIN `Select` with no inline "manage buyer roles" shortcut per D-03 (the OFR-06 lookup CRUD panel is explicitly Phase 30 scope, a deliberate trim not an oversight); if `buyerRoles` is ever empty, the `Select` shows placeholder text "No buyer roles available" rather than a broken/empty dropdown (UI-SPEC Copywriting Contract).

    Save button (`variant="default"`, UI-SPEC Color section — near-black primary, not the indigo accent) is disabled while `pending`, and (persona only) while `buyerRoleId` is unset. On submit, `startTransition(async () => { const action = signalKind==='company' ? (mode==='create'?createCompanySignalAction:updateCompanySignalAction) : (mode==='create'?createPersonaSignalAction:updatePersonaSignalAction); const payload = {practiceAreaId, name, category, description, status, offeringIds, ...(signalKind==='persona'?{buyerRoleId}:{})}; const result = mode==='create' ? await action(payload) : await action(signal!.id, payload); if (!result.ok) { setError('Could not save this signal. Please try again.'); return; } setOpen(false); router.refresh(); })`. Sheet title: `"New Company Signal"`/`"Edit Company Signal"` (swap "Company"→"Persona" per `signalKind`), per UI-SPEC's CTA copy contract. Wrap in `<Sheet open={open} onOpenChange={handleOpenChange}><SheetTrigger asChild>{trigger}</SheetTrigger><SheetContent>...</SheetContent></Sheet>` — do not override `side`/width unless the field list genuinely overflows in QA.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "signal-form\.tsx" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors in `signal-form.tsx`
    - `grep -n "'use client'" src/components/signals/signal-form.tsx` confirms the directive
    - `grep -n "createCompanySignalAction\|updateCompanySignalAction\|createPersonaSignalAction\|updatePersonaSignalAction" src/components/signals/signal-form.tsx` shows all 4 actions imported and referenced
    - `grep -n "buyerRoleId" src/components/signals/signal-form.tsx` confirms the Persona-only required field is handled (both in the disabled-Save condition and the payload)
    - `grep -n "router.refresh()" src/components/signals/signal-form.tsx` confirms the post-save refresh call
    - `grep -n "text-red-600" src/components/signals/signal-form.tsx` confirms the inline error copy styling matches `RejectDialog`'s convention
    - No edits to `src/components/ui/sheet.tsx` (Pitfall 4) — `git diff --stat src/components/ui/sheet.tsx` shows no changes
  </acceptance_criteria>
  <done>SignalForm renders both entity kinds via one parameterized component, validates the Persona Buyer Role requirement client-side, calls the correct create/update action, and closes+refreshes on success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (staff) → `SignalForm`'s Sheet inputs → `signals.ts` Server Actions (Plan 29-03) | Client-side form state is untrusted until it crosses into the Server Action, which independently re-validates via zod (this plan does not weaken or duplicate that validation — client-side required-field checks here are UX only, never the security boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-04-01 | Tampering | Client-side "Save disabled until Buyer Role selected" check | accept | This is a UX affordance only — the real enforcement is `personaSignalInputSchema`'s `buyerRoleId: z.number().int().positive()` in `signals.ts` (Plan 29-03); a bypassed client-side check (e.g. a crafted direct action call) still fails server-side validation |
| T-29-04-02 | Information Disclosure | Inline error copy on save failure | mitigate | Generic "Could not save this signal. Please try again." — never surfaces the raw `reason` string or a stack trace from the Server Action's discriminated-union result |
| T-29-04-03 | Tampering | `LinkedOfferingsPicker`'s displayed options (`activeOfferingsByPracticeAreaId[practiceAreaId]`) | accept | Pre-fetched server-side (Plan 29-07) via `listActiveOfferingsForPracticeArea` — already scoped active+practice-area; even if a client somehow rendered a stale/wrong id, `insertSignalOfferingLink`'s server-side guard is the actual enforcement point (T-29-03-03) |

</threat_model>

<verification>
`npx tsc --noEmit` clean on both new component files; manual Sheet-open/field-order/save-flow smoke deferred to the phase-level checkpoint (Plan 29-08).
</verification>

<success_criteria>
SIG-06, SIG-07 (form UI half) and SIG-09 (picker scoping) satisfied: one Sheet-based form serves both signal kinds, Persona's Buyer Role is enforced, and Linked Offerings never shows draft/wrong-practice-area offerings.
</success_criteria>

<output>
Create `.planning/phases/29-signals-ui-v1-6-queued/29-04-SUMMARY.md` when done
</output>
