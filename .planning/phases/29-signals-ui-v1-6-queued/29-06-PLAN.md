---
phase: 29-signals-ui-v1-6-queued
plan: 06
type: execute
wave: 3
depends_on: ["29-04", "29-05"]
files_modified:
  - src/components/signals/signal-table.tsx
autonomous: true
requirements: [SIG-04, SIG-05]

must_haves:
  truths:
    - "Company Signals table shows Name, Category, Practice Area, Linked Offerings (count, expandable), Status, Last updated"
    - "Persona Signals table shows the same six columns plus Buyer Role"
    - "Archived (status='retired') rows remain visible in the list, visually de-emphasized, never removed"
  artifacts:
    - path: "src/components/signals/signal-table.tsx"
      provides: "SignalTable({signalKind, rows, practiceAreas, buyerRoles, categories, activeOfferingsByPracticeAreaId, offeringNamesById}) — plain shadcn Table, D-06 (NOT ExplorerAccordionTable)"
  key_links:
    - from: "src/components/signals/signal-table.tsx"
      to: "src/components/signals/signal-form.tsx"
      via: "Edit button renders <SignalForm mode=\"edit\" signal={row} .../> per row, reusing the same Sheet component built in Plan 29-04"
      pattern: "SignalForm"
    - from: "src/components/signals/signal-table.tsx"
      to: "src/components/signals/archive-signal-dialog.tsx"
      via: "Archive button renders <ArchiveSignalDialog signalKind={signalKind} signalId={row.id} .../> per row"
      pattern: "ArchiveSignalDialog"
---

<objective>
Build `SignalTable` — the plain shadcn `Table` list view (D-06, explicitly NOT `ExplorerAccordionTable`), parameterized by `signalKind` so one component serves both tabs. Each row wires the `SignalForm` (edit mode) and `ArchiveSignalDialog` built in Plans 29-04/29-05, plus a Linked Offerings count/expand disclosure and empty/error state cards matching house copy conventions.

Purpose: SIG-04 (Company Signals columns), SIG-05 (Persona Signals columns + Buyer Role).
Output: `signal-table.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-signals-ui-v1-6-queued/29-CONTEXT.md
@.planning/phases/29-signals-ui-v1-6-queued/29-RESEARCH.md
@.planning/phases/29-signals-ui-v1-6-queued/29-PATTERNS.md
@.planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SignalTable shell — columns, row actions, empty/error states</name>
  <files>src/components/signals/signal-table.tsx</files>
  <read_first>
    src/components/companies/company-list.tsx lines 1-45 (container/error-state shape to reuse verbatim — NOT its `ExplorerAccordionTable` internals, D-06)
    src/components/ui/table.tsx (full file — `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableHead`/`TableCell` primitives, use as-is)
    src/components/ui/badge.tsx (full file — `Badge` variants; use `variant="outline"`/`"secondary"` for Status chips, no new per-status color hues per UI-SPEC)
    src/components/signals/signal-form.tsx (Plan 29-04 output — the `SignalForm` component this table's Edit button instantiates in edit mode)
    src/components/signals/archive-signal-dialog.tsx (Plan 29-05 output — the `ArchiveSignalDialog` component this table's Archive button instantiates)
    .planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md (Row Anatomy — column list, row-action button styling; Copywriting Contract — empty/error state exact copy for both filtered-to-zero and genuinely-empty states)
  </read_first>
  <action>
    Create `src/components/signals/signal-table.tsx` exporting `SignalTable({ signalKind, rows, hasActiveFilters, practiceAreas, buyerRoles, categories, activeOfferingsByPracticeAreaId, offeringNamesById, linkedOfferingIdsByRowId }: {...})` where `rows` is `Array<CompanySignalRow | PersonaSignalRow>` (fields: `id, practiceAreaId, name, category, description, status, updatedAt, buyerRoleId?`), `offeringNamesById: Record<number,string>` (for resolving linked-offering names), `linkedOfferingIdsByRowId: Record<number, number[]>` (pre-computed link ids per signal row, passed from the server page). No async/DB calls inside this component — pure renderer of already-fetched props (EXPL-06 convention). If `rows.length === 0`: render the fixed error-shape empty-state card (`rounded-lg border border-slate-200 bg-white p-8 text-center` wrapper) with copy branching on `hasActiveFilters` — `"No signals match your filters"` / `"Try removing a filter or clearing your search."` when filtered, else `"No Company Signals yet"` / `"No Persona Signals yet"` with the "Signals will appear here..." body (swap CTA name per `signalKind`) per UI-SPEC's Copywriting Contract. Otherwise render `<div className="rounded-lg border border-slate-200 bg-white"><Table>...</Table></div>`.

    Columns — Company: Name, Category, Practice Area (resolve `row.practiceAreaId` → name via the `practiceAreas` prop), Linked Offerings, Status, Last Updated, then a trailing Actions cell. Persona: same six plus Buyer Role (resolve `row.buyerRoleId` → name via `buyerRoles` prop) inserted after Category. Status column: a `Badge` (`variant="outline"` for `active`/`draft`, or a `variant="secondary"` "Retired" badge for `retired` — no new color hues). Last Updated: format `row.updatedAt` with the existing `dateFormatter` pattern (check `src/components/explorer/explorer-format.ts` for the shared formatter before hand-rolling a new `Intl.DateTimeFormat`). Retired rows get a slightly muted row treatment (`opacity-70` on the `TableRow`, or rely on the Status badge alone if `opacity-70` reads poorly against the badge — planner/executor's call, document the choice) — retired rows are NEVER filtered out of this component's `rows` prop, only visually de-emphasized.

    Actions cell: two `Button`s (`variant="outline" size="sm"`), right-aligned — `<SignalForm signalKind={signalKind} mode="edit" signal={row} existingLinkedOfferingIds={linkedOfferingIdsByRowId[row.id] ?? []} practiceAreas={practiceAreas} buyerRoles={buyerRoles} categories={categories} activeOfferingsByPracticeAreaId={activeOfferingsByPracticeAreaId} trigger={<Button variant="outline" size="sm">Edit</Button>} />` and `<ArchiveSignalDialog signalKind={signalKind} signalId={row.id} />`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "signal-table\.tsx" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors in `signal-table.tsx`
    - `grep -n "ExplorerAccordionTable" src/components/signals/signal-table.tsx` returns no matches (D-06)
    - `grep -n "Buyer Role" src/components/signals/signal-table.tsx` shows the column is conditionally rendered only when `signalKind === 'persona'`
    - `grep -n "No signals match your filters\|No Company Signals yet\|No Persona Signals yet" src/components/signals/signal-table.tsx` confirms all three empty-state copy variants are present
    - `grep -n "<SignalForm" src/components/signals/signal-table.tsx` and `grep -n "<ArchiveSignalDialog" src/components/signals/signal-table.tsx` both show usages inside the row-mapping loop
  </acceptance_criteria>
  <done>SignalTable renders the correct column set per entity kind, wires Edit/Archive to the Plan 29-04/29-05 components, and shows the three distinct empty/error states verbatim per UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Linked Offerings count/expand disclosure</name>
  <files>src/components/signals/signal-table.tsx</files>
  <read_first>
    src/components/ui/popover.tsx (vendored — `Popover`/`PopoverTrigger`/`PopoverContent`, already installed, used elsewhere for Settings' model picker's Command combobox — reusing the bare primitive here is NOT the same as D-04's excluded Command+Popover searchable combobox, this is a read-only disclosure)
    src/components/ui/badge.tsx (the `Badge` used as the count trigger)
    .planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md (Row Anatomy — "Linked Offerings (count, expandable — a small inline disclosure, e.g. a Badge showing the count that expands the row or reveals a tooltip on click)")
  </read_first>
  <action>
    In the Linked Offerings `TableCell` from Task 1, render a `Badge` (`variant="outline"`) showing `linkedOfferingIdsByRowId[row.id]?.length ?? 0` wrapped in a `Popover`/`PopoverTrigger asChild`. If the count is 0, render the badge as plain non-interactive text/badge (no Popover needed for zero-state — nothing to disclose). If the count is > 0, `PopoverContent` renders a small `<ul>` of the linked offering names, resolved via `linkedOfferingIdsByRowId[row.id].map(id => offeringNamesById[id] ?? \`#${id}\`)` (fallback to the bare id string if a name can't be resolved — never crash on a stale/missing lookup entry).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "signal-table\.tsx" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors
    - `grep -n "Popover" src/components/signals/signal-table.tsx` confirms the disclosure primitive is used
    - `grep -n "offeringNamesById" src/components/signals/signal-table.tsx` confirms name resolution with a fallback (not a bare unguarded index into the map)
  </acceptance_criteria>
  <done>Linked Offerings column shows an accurate count and, on click, discloses the linked offering names by resolving pre-fetched props — no client-side fetch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-fetched props (Plan 29-07) → `SignalTable` rendering | Trusted — all data is server-fetched and passed as props; this component performs no independent data access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-06-01 | Information Disclosure | Free-text `name`/`category`/`description` rendered in table cells and Popover content | mitigate | React's default JSX escaping handles this automatically — no `dangerouslySetInnerHTML` is used anywhere in this component (verified by absence, not by an explicit sanitizer) |
| T-29-06-02 | Tampering | A retired signal row still rendering its Edit/Archive actions | accept | Editing a retired row back to `active` is the documented restore path (UI-SPEC Archive dialog body copy) — this is intentional functionality, not a gap |

</threat_model>

<verification>
`npx tsc --noEmit` clean.
</verification>

<success_criteria>
SIG-04, SIG-05 satisfied: both tabs' table columns render the spec'd fields, retired rows stay visible, and the Linked Offerings disclosure works from pre-fetched data only.
</success_criteria>

<output>
Create `.planning/phases/29-signals-ui-v1-6-queued/29-06-SUMMARY.md` when done
</output>
