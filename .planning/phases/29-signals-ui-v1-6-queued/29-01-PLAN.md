---
phase: 29-signals-ui-v1-6-queued
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/nav.ts
  - src/lib/nav.test.ts
  - src/lib/sidebar-collapse.ts
  - src/components/layout/app-sidebar.tsx
autonomous: true
requirements: [SIG-01]

must_haves:
  truths:
    - "The Manage sidebar group shows a new Signals nav item, sibling to Reviews and Settings (D-01) — not nested under Reviews"
    - "Visiting /signals highlights the Signals nav item; visiting /companies etc. never false-highlights Signals"
  artifacts:
    - path: "src/lib/nav.ts"
      provides: "NavKey widened to include 'signals'; getActiveNavKey('/signals') === 'signals'"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "New SidebarMenuItem/SidebarMenuButton block linking to /signals inside the Manage SidebarGroup"
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/lib/nav.ts"
      via: "getActiveNavKey(pathname) === 'signals' drives isActive on the new SidebarMenuButton"
      pattern: "activeKey === 'signals'"
---

<objective>
Add the `Signals` sidebar nav item (D-01): a new sibling top-level entry in the `Manage` `SidebarGroup`, next to `Reviews`/`Settings`, routing to `/signals`. Extend the pure `getActiveNavKey` function and its locked regression-test suite with the new route.

Purpose: SIG-01 — staff need a discoverable entry point into the new Signals screen, matching the existing nav item pattern exactly (no new interaction idiom).
Output: `NavKey` widened, `getActiveNavKey`/`getNavTooltipLabel` extended, sidebar renders the new item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-signals-ui-v1-6-queued/29-CONTEXT.md
@.planning/phases/29-signals-ui-v1-6-queued/29-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend NavKey + getActiveNavKey + regression test suite</name>
  <files>src/lib/nav.ts, src/lib/nav.test.ts</files>
  <read_first>
    src/lib/nav.ts (full file, 18 lines — the exact type + function to extend)
    src/lib/nav.test.ts (full file, 56 lines — the existing 13-case Vitest suite whose structure/style must be matched exactly)
  </read_first>
  <behavior>
    - `getActiveNavKey('/signals')` returns `'signals'`
    - `getActiveNavKey('/signals/1')` returns `'signals'` (prefix-match style, mirrors `/companies`, even though no detail sub-route ships this phase — matches PATTERNS.md's documented adaptation)
    - `getActiveNavKey('/signals-archive')` returns `null` (sibling-prefix boundary guard, mirrors the existing `/companies-archive`/`/settings-archive` guards)
    - All 13 existing cases in `nav.test.ts` remain unchanged and still pass
  </behavior>
  <action>
    Widen the `NavKey` union type in `src/lib/nav.ts` from `'start' | 'companies' | 'personas' | 'reviews' | 'settings'` to add `'signals'`. Insert a new branch in `getActiveNavKey` immediately before the `/settings` branch (prefix-match style like `/companies`/`/personas`/`/reviews`): `if (pathname === '/signals' || pathname.startsWith('/signals/')) return 'signals';`. Add a one-line comment matching the existing inline-comment density explaining the prefix-match choice (mirror the existing `/companies` boundary-guard comment tone). Extend `nav.test.ts` by appending three new `it(...)` cases after the existing `'reviews'` cases (before the `'settings'` case), matching the exact `describe`/`it`/`expect` style already in the file — one for `/signals` → `'signals'`, one for `/signals/1` → `'signals'`, one for `/signals-archive` → `null` boundary guard (placed alongside the existing `/companies-archive`/`/settings-archive` guard cases).
  </action>
  <verify>
    <automated>npx vitest run src/lib/nav.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/nav.test.ts` passes with 16 total cases (13 existing + 3 new), 0 failures
    - `grep -n "'signals'" src/lib/nav.ts` shows the type union AND the new branch
    - The existing 13 assertions in `nav.test.ts` are byte-identical to before this task (diff shows only additions, no modified lines)
  </acceptance_criteria>
  <done>`getActiveNavKey` correctly resolves `/signals` routes to `'signals'` with sibling-prefix boundary protection; full existing suite stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Signals nav item into the sidebar</name>
  <files>src/lib/sidebar-collapse.ts, src/components/layout/app-sidebar.tsx</files>
  <read_first>
    src/lib/sidebar-collapse.ts (full file, 17 lines — `getNavTooltipLabel` must accept the widened `NavKey` from Task 1)
    src/components/layout/app-sidebar.tsx (full file, 282 lines — the exact `Reviews`/`Settings` `SidebarMenuItem` blocks to copy the pattern from, lines 169-214)
  </read_first>
  <action>
    In `src/lib/sidebar-collapse.ts`, add a `signals: 'Signals'` entry to the object-literal lookup inside `getNavTooltipLabel` (the `{ start, companies, personas, settings }` record) — `'signals'` needs no pending-count special case like `'reviews'`. In `src/components/layout/app-sidebar.tsx`: (1) add `Radar` to the `lucide-react` import list (alongside `Building2, Inbox, LayoutDashboard, Mail, PanelLeftClose, PanelLeftOpen, Settings, Users`); (2) insert a new `SidebarMenuItem` block as a sibling to the existing `Reviews`/`Settings` items inside the same `Manage` `SidebarGroup` (after `Reviews`, before `Settings`, or after `Settings` — either position is fine since `Signals` is a new top-level item, not nested), copying the exact `SidebarMenuButton` prop shape (`asChild`, `isActive={activeKey === 'signals'}`, `tooltip={getNavTooltipLabel('signals', pendingCount)}`, `className="h-[30px] p-0 px-2 gap-2.5 rounded-[4px] text-[15px] font-normal"`) wrapping a `<Link href="/signals"><Radar /><span>Signals</span></Link>`. No `SidebarMenuBadge`/pending-count treatment — Signals is a plain nav link.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "nav\.ts|sidebar-collapse\.ts|app-sidebar\.tsx" || echo "no type errors in touched files"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors in `sidebar-collapse.ts` or `app-sidebar.tsx`
    - `grep -n "Radar" src/components/layout/app-sidebar.tsx` shows both the import and the JSX usage
    - `grep -n "href=\"/signals\"" src/components/layout/app-sidebar.tsx` shows exactly one match
    - `grep -n "signals: 'Signals'" src/lib/sidebar-collapse.ts` (or equivalent object-literal key) confirms the tooltip mapping
  </acceptance_criteria>
  <done>The `Manage` sidebar group renders a `Signals` item with a `Radar` icon linking to `/signals`, active-highlighted via the extended `getActiveNavKey`, tooltip-labeled via the extended `getNavTooltipLabel`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (staff) → sidebar nav link | Client-rendered `Link` to `/signals`; no data crosses this boundary in this plan (pure navigation UI) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01-01 | Spoofing | `/signals` route access via the new nav link | accept (mitigated elsewhere) | The link itself grants no access — `/signals` route protection (page-level `requireStaffAccess()`) is delivered in Plan 29-07, not this plan; an unauthenticated user clicking this link still hits the auth gate downstream |
| T-29-01-02 | Tampering | `getActiveNavKey` active-state logic | mitigate | Pure function, no external input beyond `pathname` (browser-controlled, not attacker-exploitable — worst case is a cosmetic mis-highlight, never an access-control decision) — regression-locked by the extended Vitest suite (Task 1) |

</threat_model>

<verification>
`npx vitest run src/lib/nav.test.ts` green (16 cases); `npx tsc --noEmit` clean on both touched files; manual sanity check optional (sidebar visually renders the new item) — deferred to the phase-level checkpoint in Plan 29-08.
</verification>

<success_criteria>
SIG-01 satisfied: the `Signals` nav item exists, links to `/signals`, active-highlights correctly, and the underlying pure-function contract is regression-locked by tests.
</success_criteria>

<output>
Create `.planning/phases/29-signals-ui-v1-6-queued/29-01-SUMMARY.md` when done
</output>
