---
phase: 29-signals-ui-v1-6-queued
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/params/signalFilters.ts
  - src/lib/params/signalFilters.test.ts
autonomous: true
requirements: [SIG-03]

must_haves:
  truths:
    - "Signals list filter query params (practiceArea, category, status, search) parse into a typed, safely-coerced filters object"
    - "A malformed practiceArea param (non-numeric) never produces a NaN that could reach a Drizzle query"
  artifacts:
    - path: "src/lib/params/signalFilters.ts"
      provides: "firstValue, parseSignalFilters — mirrors src/lib/params/companyFilters.ts's exact shape"
  key_links:
    - from: "src/lib/params/signalFilters.ts"
      to: "src/lib/db/schema.ts"
      via: "status field typed against catalogStatusEnum.enumValues, NOT practiceAreaStatusEnum (Pitfall 5)"
      pattern: "catalogStatusEnum"
---

<objective>
Build the URL-searchParams parsing module for the Signals filter bar — `parseSignalFilters` — mirroring `companyFilters.ts`'s exact `firstValue`/parse shape, with safe numeric coercion for `practiceAreaId` and the correct status enum import.

Purpose: SIG-03 — the filter bar (Practice Area / Category / Status / free-text search) needs a single, tested, typed parsing function the server page calls before querying — this is a pure-function TDD candidate, isolated from the DB layer.
Output: `src/lib/params/signalFilters.ts` + `signalFilters.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-signals-ui-v1-6-queued/29-CONTEXT.md
@.planning/phases/29-signals-ui-v1-6-queued/29-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: signalFilters.ts — parseSignalFilters + firstValue, TDD</name>
  <files>src/lib/params/signalFilters.ts, src/lib/params/signalFilters.test.ts</files>
  <read_first>
    src/lib/params/companyFilters.ts (full file, 33 lines — the exact `firstValue`/`parseCompanyFilters`/`parseSelectedId` pattern to mirror)
    src/lib/nav.test.ts (full file — closest structural analog for a plain, mock-free Vitest suite on a pure function, since no `src/lib/params/*.test.ts` file exists yet to copy from — RESEARCH.md "No Analog Found")
    src/lib/db/schema.ts lines 298-306 (`catalogStatusEnum` at line 301 vs. `practiceAreaStatusEnum` at line 305 — Pitfall 5: import the former, never the latter)
  </read_first>
  <behavior>
    - `firstValue(['a','b'])` returns `'a'`; `firstValue('a')` returns `'a'`; `firstValue(undefined)` returns `undefined`
    - `parseSignalFilters({})` returns `{ search: undefined, practiceAreaId: undefined, category: undefined, status: undefined }`
    - `parseSignalFilters({ practiceArea: '3' })` returns `practiceAreaId: 3` (numeric coercion)
    - `parseSignalFilters({ practiceArea: 'abc' })` returns `practiceAreaId: undefined` (NaN guard — a malformed param must never leak a `NaN` into the returned object)
    - `parseSignalFilters({ category: 'GBS-state', status: 'active', search: 'CFO' })` returns all three fields passed through as strings unmodified
    - `parseSignalFilters({ practiceArea: ['5','9'] })` uses the first array element (`practiceAreaId: 5`) — mirrors `firstValue`'s array-takes-first-element contract
  </behavior>
  <action>
    Write `signalFilters.test.ts` FIRST (RED) covering every case in `<behavior>` above, using plain `describe`/`it`/`expect` from `vitest` (no mocks — pure function, same style as `nav.test.ts`). Then implement `signalFilters.ts` (GREEN): export `firstValue(value: string | string[] | undefined): string | undefined` (byte-identical logic to `companyFilters.ts`'s version — `Array.isArray(value) ? value[0] : value`); export a `SignalFiltersShape` type/interface with `search?: string; practiceAreaId?: number; category?: string; status?: (typeof catalogStatusEnum.enumValues)[number]`; export `parseSignalFilters(params: { [key: string]: string | string[] | undefined }): SignalFiltersShape` that computes `practiceAreaId` via `Number(firstValue(params.practiceArea))`, guarding `Number.isNaN` to fall back to `undefined` (do NOT coerce an empty-string/undefined raw value to `Number(undefined)` = `NaN` without the same guard). Import `catalogStatusEnum` from `@/lib/db/schema` for the type only (type-only import, erased at compile — do not import `practiceAreaStatusEnum`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/params/signalFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/params/signalFilters.test.ts` passes, 0 failures, covering every case in `<behavior>`
    - `grep -n "practiceAreaStatusEnum" src/lib/params/signalFilters.ts` returns no matches (Pitfall 5 guard)
    - `grep -n "catalogStatusEnum" src/lib/params/signalFilters.ts` returns at least one match
    - `parseSignalFilters({ practiceArea: 'abc' }).practiceAreaId === undefined` is asserted explicitly in the test file (not just implied)
  </acceptance_criteria>
  <done>`parseSignalFilters` is implemented, tested, and safely coerces/guards every filter field per SIG-03's four filter dimensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (staff) → `/signals?practiceArea=...&category=...&status=...&search=...` query string | Untrusted input — a staff user (or a tampered bookmark/shared link) fully controls every query param value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02-01 | Tampering | `parseSignalFilters`'s `practiceAreaId` numeric coercion | mitigate | `Number.isNaN` guard ensures a non-numeric `practiceArea` param degrades to `undefined` (no filter applied) rather than a `NaN` reaching a downstream Drizzle `eq()` call — locked by the Task 1 test case |
| T-29-02-02 | Tampering | `status` param value (free string, not yet validated against the enum) | accept (deferred) | This module returns the raw string typed as the enum union but does NOT runtime-validate it against `catalogStatusEnum.enumValues` — the consuming page (Plan 29-07) applies the actual filter via a controlled `Select` sourced from the enum, so an out-of-range value simply matches zero rows rather than causing a query error; documented here as an accepted low-risk gap since this module is parsing-only, not query-execution |

</threat_model>

<verification>
`npx vitest run src/lib/params/signalFilters.test.ts` green; no `practiceAreaStatusEnum` import present.
</verification>

<success_criteria>
SIG-03 (parsing half) satisfied: filter query params parse into a safe, typed shape ready for the server page to consume.
</success_criteria>

<output>
Create `.planning/phases/29-signals-ui-v1-6-queued/29-02-SUMMARY.md` when done
</output>
