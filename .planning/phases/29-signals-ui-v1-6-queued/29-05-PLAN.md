---
phase: 29-signals-ui-v1-6-queued
plan: 05
type: execute
wave: 2
depends_on: ["29-03"]
files_modified:
  - src/components/signals/archive-signal-dialog.tsx
  - src/components/signals/signal-filters.tsx
autonomous: true
requirements: [SIG-03, SIG-08]

must_haves:
  truths:
    - "A signal's row-level Archive action opens a confirm Dialog and, on confirm, sets status='retired' — never a hard delete, never silently applied without confirmation"
    - "The Signals filter bar lets staff filter by Practice Area, Category, Status, and free-text search, URL-synced via nuqs"
  artifacts:
    - path: "src/components/signals/archive-signal-dialog.tsx"
      provides: "ArchiveSignalDialog({signalKind, signalId, signalName?}) — Dialog confirm calling archiveCompanySignalAction/archivePersonaSignalAction"
    - path: "src/components/signals/signal-filters.tsx"
      provides: "SignalFilters — nuqs URL-synced Practice Area/Category/Status Selects + free-text search Input"
  key_links:
    - from: "src/components/signals/archive-signal-dialog.tsx"
      to: "src/app/actions/signals.ts"
      via: "archiveCompanySignalAction(signalId) / archivePersonaSignalAction(signalId) called inside useTransition on confirm"
      pattern: "archive.*SignalAction"
---

<objective>
Build two independent, self-contained client components: `ArchiveSignalDialog` (row-level soft-archive confirm, adapted from `RejectDialog` but reversible-styled per UI-SPEC) and `SignalFilters` (the nuqs URL-synced filter bar for Practice Area/Category/Status/search, mirroring `CompanyFilters`'s `EnumFilterSelect` pattern).

Purpose: SIG-08 (archive is a confirmed, reversible soft status flip — never a hard delete) and SIG-03 (the four-dimension filter bar).
Output: `archive-signal-dialog.tsx`, `signal-filters.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-signals-ui-v1-6-queued/29-CONTEXT.md
@.planning/phases/29-signals-ui-v1-6-queued/29-RESEARCH.md
@.planning/phases/29-signals-ui-v1-6-queued/29-PATTERNS.md
@.planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ArchiveSignalDialog — reversible-styled confirm</name>
  <files>src/components/signals/archive-signal-dialog.tsx</files>
  <read_first>
    src/components/reviews/reject-dialog.tsx (full file, 150 lines — the exact Dialog/state/useTransition shape to adapt: `open`/`pending`/`error` state, `DialogTrigger asChild`, `DialogFooter` Cancel/Confirm layout)
    src/app/actions/signals.ts (`archiveCompanySignalAction(id)` / `archivePersonaSignalAction(id)` — Plan 29-03's exact signatures)
    .planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md (Copywriting Contract — Archive dialog exact title/body copy; Color section — `variant="default"` NOT `"destructive"`, because archive is reversible)
  </read_first>
  <action>
    Create `src/components/signals/archive-signal-dialog.tsx` as a client component exporting `ArchiveSignalDialog({ signalKind, signalId, onArchived }: { signalKind: 'company' | 'persona'; signalId: number; onArchived?: () => void })`. Adapt `RejectDialog`'s shape exactly: `useState` for `open`/`pending` (via `useTransition`)/`error`; `useRouter` from `next/navigation` for the post-success refresh. `DialogTrigger asChild` wraps a `<Button variant="outline" size="sm">Archive</Button>`. `DialogContent` renders `DialogTitle`: `"Archive this signal?"`, `DialogDescription`: `"It will no longer appear in active views, but its history and any linked offerings are preserved. You can restore it later by editing its status back to Active."` (verbatim UI-SPEC copy) — no reason `Select`/note `Input` fields, unlike `RejectDialog`. `DialogFooter`: `Cancel` (`variant="outline"`, `DialogClose asChild`) / `Archive` button — **`variant="default"` (near-black), explicitly NOT `variant="destructive"`** (UI-SPEC Color section — this is the one deliberate deviation from `RejectDialog`'s red confirm button, because archiving is reversible). On confirm: `startTransition(async () => { const action = signalKind === 'company' ? archiveCompanySignalAction : archivePersonaSignalAction; const result = await action(signalId); if (!result.ok) { setError('Could not archive this signal. Please try again.'); return; } setOpen(false); onArchived?.(); router.refresh(); })`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "archive-signal-dialog\.tsx" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors in `archive-signal-dialog.tsx`
    - `grep -n "variant=\"destructive\"" src/components/signals/archive-signal-dialog.tsx` returns NO matches (the confirm button must never be destructive-styled)
    - `grep -n "variant=\"default\"" src/components/signals/archive-signal-dialog.tsx` returns at least one match (the Archive confirm button)
    - `grep -n "Archive this signal?" src/components/signals/archive-signal-dialog.tsx` confirms the exact UI-SPEC title copy
    - `grep -n "router.refresh()" src/components/signals/archive-signal-dialog.tsx` confirms the post-archive refresh
  </acceptance_criteria>
  <done>ArchiveSignalDialog confirms before every archive, calls the correct action per `signalKind`, and never uses destructive red styling.</done>
</task>

<task type="auto">
  <name>Task 2: SignalFilters — nuqs URL-synced filter bar</name>
  <files>src/components/signals/signal-filters.tsx</files>
  <read_first>
    src/components/companies/company-filters.tsx (full file, 88 lines — the exact `EnumFilterSelect` generic component + composition pattern to mirror)
    src/components/companies/company-search-input.tsx (full file, 26 lines — the debounced free-text search `Input` + `useQueryState` pattern for the free-text search field)
    src/lib/db/schema.ts lines 298-306 (`catalogStatusEnum.enumValues` — Status filter options; Pitfall 5: never `practiceAreaStatusEnum`)
    .planning/phases/29-signals-ui-v1-6-queued/29-UI-SPEC.md (Row Anatomy — filter bar wrapper `flex flex-wrap gap-3`; D-05 — Practice Area defaults to "All", GBS simply appears first in the option list)
  </read_first>
  <action>
    Create `src/components/signals/signal-filters.tsx` as a client component. Copy `company-filters.tsx`'s `EnumFilterSelect({paramKey, placeholder, options, humanize})` helper verbatim (a third copy is consistent with existing house convention per PATTERNS.md — `company-filters.tsx`/`persona-filters.tsx` already duplicate it once). Export `SignalFilters({ practiceAreas, categories }: { practiceAreas: Array<{id: number; name: string}>; categories: string[] })` rendering `<div className="flex flex-wrap gap-3">`: (1) a free-text search `Input` using the exact `company-search-input.tsx` debounce pattern (`useQueryState('search', parseAsString.withDefault('').withOptions({shallow:false}))`, `limitUrlUpdates: debounce(300)` except when clearing); (2) `EnumFilterSelect` for Practice Area with `paramKey="practiceArea"`, `options={practiceAreas.map(pa => String(pa.id))}` — since these are numeric ids not enum slugs, render the `SelectItem`'s visible label from a lookup (`practiceAreas.find(pa => String(pa.id) === opt)?.name`) rather than `humanizeEnum`, so pass `humanize={false}` and inline a custom label-resolution instead of the raw id string (do not display raw numeric ids to the user); (3) `EnumFilterSelect` for Category with `paramKey="category"`, `options={categories}`, `humanize={false}`; (4) `EnumFilterSelect` for Status with `paramKey="status"`, `options={catalogStatusEnum.enumValues}` (imported from `@/lib/db/schema` — Pitfall 5, never `practiceAreaStatusEnum`). No pre-forced default value on any filter — Practice Area defaults to "All" (no value set), with GBS simply appearing first/normally since it is the only populated practice area today (D-05) — do not hardcode a GBS default.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "signal-filters\.tsx" || echo "no type errors"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports zero errors in `signal-filters.tsx`
    - `grep -n "practiceAreaStatusEnum" src/components/signals/signal-filters.tsx` returns no matches
    - `grep -n "catalogStatusEnum" src/components/signals/signal-filters.tsx` returns at least one match
    - `grep -n "shallow: false" src/components/signals/signal-filters.tsx` shows the pattern applied to all four filter params (server re-fetch on change)
    - `grep -n "flex flex-wrap gap-3" src/components/signals/signal-filters.tsx` confirms the UI-SPEC wrapper class
  </acceptance_criteria>
  <done>SignalFilters renders Practice Area/Category/Status/search controls, all URL-synced via nuqs with `shallow: false`, with Practice Area labels resolved from real names (never raw ids).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (staff) → `/signals?...` URL query params (via `SignalFilters`) | Untrusted — a staff user or a tampered/shared link fully controls every param value written here |
| Browser (staff) → `ArchiveSignalDialog` confirm → `signals.ts` archive actions | Confirmed user intent crosses into a staff-gated Server Action (Plan 29-03 re-validates independently) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05-01 | Tampering | `SignalFilters`'s emitted query-param values | mitigate | Every `EnumFilterSelect` instance sources its `options` from a schema enum (`catalogStatusEnum.enumValues`) or a server-fetched array (`practiceAreas`, `categories`) — never a hardcoded/free-form list — so the emitted param can only ever be a value the UI itself offered; the actual filtering happens server-side (Plan 29-07) which still treats the param as untrusted and safely no-ops on an out-of-range value |
| T-29-05-02 | Repudiation | Archive confirm bypassed via direct action call | accept (mitigated elsewhere) | This dialog is a UX confirmation only — the real audit trail (`updatedBy`/`updatedAt` stamp) is enforced at the query layer regardless of whether this specific dialog was used (T-29-03-04) |

</threat_model>

<verification>
`npx tsc --noEmit` clean on both new component files.
</verification>

<success_criteria>
SIG-08 (confirmed, reversible-styled archive) and SIG-03 (filter bar UI half) satisfied.
</success_criteria>

<output>
Create `.planning/phases/29-signals-ui-v1-6-queued/29-05-SUMMARY.md` when done
</output>
