---
phase: 09-analytic-agent-observability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - package.json
  - src/lib/env.ts
  - .env.example
  - src/lib/telemetry/langfuse.ts
  - src/lib/validation/airsRules.ts
  - src/lib/validation/validateReport.ts
  - src/lib/validation/fixtures/sample-valid.json
  - src/lib/agents/types.ts
  - src/lib/agents/runAgent.ts
  - src/lib/agents/tools.ts
  - src/lib/agents/prompt.ts
  - src/lib/agents/dedup.ts
  - src/lib/agents/runAgent.test.ts
  - src/lib/agents/dedup.test.ts
  - src/lib/validation/airsRules.test.ts
autonomous: true
requirements: [ANLZ-01, ANLZ-02, ANLZ-05]

must_haves:
  truths:
    - "signal_proposal, agent_run, and correction tables exist in schema.ts and are pushed to the live Neon DB"
    - "The 5 new server-only env keys are optional (unset degrades gracefully — app never crashes at import)"
    - "The ported AIRS gate fails closed: a run whose proposals have orphan citations / R3·C3 strong claims / empty key_uncertainties / empty-signals-with-intent is rejected with errors[]"
    - "runAgent is the mockable seam — its test mocks 'ai' and 'firecrawl' and never registers telemetry"
    - "dedupProposals drops any proposal whose (companyId, signalType) already exists as a live signal"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "signal_proposal + agent_run + correction tables, proposalStatusEnum, correctionReasonEnum, unique index on signal(company_id, signal_type)"
      contains: "signal_proposal"
    - path: "src/lib/telemetry/langfuse.ts"
      provides: "initLangfuse() (test-guarded), getTraceUrl(), mirrorCorrectionAnnotation()"
      exports: ["initLangfuse", "getTraceUrl", "mirrorCorrectionAnnotation"]
    - path: "src/lib/validation/validateReport.ts"
      provides: "validateRunArtifacts() — fail-closed gate port of validate_report.py (D-03)"
      exports: ["validateRunArtifacts"]
    - path: "src/lib/agents/runAgent.ts"
      provides: "ToolLoopAgent v7 wrapper + generateText call with Output.object"
      exports: ["runAgent"]
    - path: "src/lib/agents/dedup.ts"
      provides: "dedupProposals(proposals, liveSignals) — pure filter"
      exports: ["dedupProposals"]
    - path: "src/lib/agents/types.ts"
      provides: "proposalSignalSchema + outputSchema (zod) typed to DB enums, includes evidenceAppendix"
  key_links:
    - from: "src/lib/agents/runAgent.ts"
      to: "'ai' (ToolLoopAgent, isStepCount, Output)"
      via: "import + generateText"
      pattern: "ToolLoopAgent|Output\\.object|isStepCount"
    - from: "src/lib/validation/validateReport.ts"
      to: "src/lib/agents/types.ts (outputSchema)"
      via: "z.infer<typeof outputSchema>"
      pattern: "outputSchema"
    - from: "src/lib/agents/dedup.ts"
      to: "src/lib/db/queries/signals.ts (live signal list)"
      via: "(companyId, signalType) comparison"
      pattern: "signalType"
---

<objective>
Foundation for Phase 9: (1) push the new `signal_proposal` / `agent_run` / `correction` tables plus a `signal(company_id, signal_type)` unique index (D-09/D-14/ANLZ-05); (2) install the AI SDK v7 + Langfuse + Firecrawl dependencies and add the server-only env keys (D-15); (3) port the AIRS validation gate to TypeScript, fail-closed (D-03); (4) build the agent-core seam: zod output schema, `runAgent` (ToolLoopAgent v7 wrapper — the mockable boundary), Firecrawl search tool, prompt builder, and the pure dedup filter (ANLZ-01/02/05). Wave 1 is independently shippable and ends with the verification loop (`npm test` + `npx tsc --noEmit`).

Purpose: everything downstream (route handler, accept/reject writes, UI) consumes these contracts; the mockable seam is what lets the whole phase test without live API calls (D-16).
Output: pushed schema, installed deps, env keys, telemetry bootstrap, gate port + fixtures, agent core + 4 validation-anchored test files (09-01-01 runAgent, 09-01-02 gate, 09-01-04 dedup).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-analytic-agent-observability/09-CONTEXT.md
@.planning/phases/09-analytic-agent-observability/09-RESEARCH.md
@.planning/phases/09-analytic-agent-observability/09-PATTERNS.md
@.planning/phases/09-analytic-agent-observability/09-VALIDATION.md
@src/lib/db/schema.ts
@src/lib/env.ts
@src/lib/db/queries/signals.ts
@.env.example
@package.json
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1 [BLOCKING]: Push signal_proposal / agent_run / correction schema + unique index</name>
  <files>src/lib/db/schema.ts</files>
  <action>
  In `src/lib/db/schema.ts`, following the existing `importBatchStatusEnum`/`importLogActionEnum` (L148-156) and `signal` table (L96-105) patterns, add (per D-09/D-14):

  1. `proposalStatusEnum` — pgEnum('proposal_status', ['pending', 'accepted', 'rejected'])
  2. `correctionReasonEnum` — pgEnum('correction_reason', ['wrong_signal_type', 'missed_criteria', 'hallucinated_no_evidence', 'other'])
  3. `agentRun` table (`agent_run`): `id` (serial pk), `companyId` (integer fk → company.id, not null), `traceId` (text, nullable), `traceUrl` (text, nullable), `verdict` (text, nullable — 'active'|'emerging'|'no_intent' analog only if it falls out of the proposal set per D-04), `usageTokens` (jsonb, nullable), `evidenceAppendix` (jsonb, nullable — derived server-side from real webSearch tool results, NOT model-recited), `hypotheses` (jsonb, nullable), `createdAt` (timestamp default now())
  4. `signalProposal` table (`signal_proposal`): `id` (serial pk), `companyId` (integer fk → company.id, not null), `runId` (integer fk → agent_run.id, nullable), `signalType` (signalTypeEnum, not null), `strength` (signalStrengthEnum, not null), `detectedAt` (date, not null), `evidenceUrl` (text, not null), `reliability` (text not null — 'R1'|'R2'|'R3'), `confidence` (text not null — 'C1'|'C2'|'C3'), `evidenceSnippet` (text, not null), `reasoning` (text, not null), `status` (proposalStatusEnum default 'pending'), `resolvedAt` (timestamp, nullable), `createdAt` (timestamp default now())
  5. `correction` table: `id` (serial pk), `proposalId` (integer fk → signal_proposal.id, not null), `reason` (correctionReasonEnum, not null), `note` (text, nullable), `traceId` (text, not null — links to the Langfuse run trace per OBSV-02/D-14), `createdAt` (timestamp default now())
  6. On the existing `signal` table add a UNIQUE index: `uniqueIndex('signal_company_type_idx').on(signal.companyId, signal.signalType)` — verified safe against live data (0 duplicate pairs; 12 rows). This is the concurrency backstop for the Accept path (T-09-07) since neon-http has no transaction support.
  7. Add a comment above each new table tagging D-09 (durable queue, never auto-write to signal) / D-14 (correction traceId) / D-02 (evidenceAppendix derived from real tool results).

  Then push: `npx drizzle-kit push` (DATABASE_URL already in .env.local; no npm script exists — run the npx command directly). Confirm drizzle-kit reports CREATE TABLE for the 3 new tables + the unique index, and that it does NOT attempt to re-run anything from Phase 7/8.
  </action>
  <verify>
  `npx drizzle-kit check` clean; `npx drizzle-kit push` reports the 3 CREATE TABLE + CREATE UNIQUE INDEX statements; `npx tsc --noEmit` clean; `grep -n "signal_proposal\|agent_run\|correction" src/lib/db/schema.ts`
  </verify>
  <done>New tables + unique index pushed to live Neon DB; schema.ts compiles; ANLZ-05 index backstop in place</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install AI SDK v7 + Langfuse + Firecrawl deps; add env keys; telemetry bootstrap</name>
  <files>package.json, src/lib/env.ts, .env.example, src/lib/telemetry/langfuse.ts</files>
  <action>
  Install EXACT pinned versions verified by research (npm registry 2026-07-31, slopcheck 10/10 OK):
  `npm install ai@7.0.45 @ai-sdk/anthropic@4.0.26 firecrawl@4.32.0 @langfuse/vercel-ai-sdk@5.9.1 @langfuse/client@5.10.0`
  (The research summary claims these are installed — they are NOT in package.json; this install fixes that. Verify versions landed exactly. AI SDK v7 ONLY: do NOT install `@langfuse/vercel` — it 404s (Pitfall 2).)

  In `src/lib/env.ts` (self-analog: `APOLLO_API_KEY` optional block L19-23), add 5 OPTIONAL server-only keys, never `PUBLIC_`-prefixed, never logged (D-15):
  `ANTHROPIC_API_KEY: z.string().optional()`, `FIRECRAWL_API_KEY: z.string().optional()`, `LANGFUSE_PUBLIC_KEY: z.string().optional()`, `LANGFUSE_SECRET_KEY: z.string().optional()`, `LANGFUSE_TRACE_BASE_URL: z.string().optional()` — each with a comment: optional/degrade-gracefully; unset disables Analyze with a "not configured" message, never crashes at import.
  In `.env.example`, add the 5 keys with one-line comments (server-only; get from respective dashboards; keys already provisioned per D-15).

  Create `src/lib/telemetry/langfuse.ts` following the RESEARCH.md L374-404 skeleton EXACTLY:
  - `initLangfuse()`: `if (process.env.NODE_ENV === 'test') return;` FIRST (D-16 — never register in tests); module-singleton guard (idempotent); `new LangfuseVercelAiSdkIntegration()` + `registerTelemetry({ telemetry: new LangfuseSpanProcessor(integration) })` from `@langfuse/vercel-ai-sdk`; `new Client({ publicKey, secretKey, baseUrl: env.LANGFUSE_TRACE_BASE_URL ?? 'https://cloud.langfuse.com' })` from `@langfuse/client`. No `instrumentation.ts` (D-13).
  - `getTraceUrl(traceId)`: delegates to `langfuseClient.getTraceUrl(traceId)` (verified API on `@langfuse/client`).
  - `mirrorCorrectionAnnotation(traceId, { reason, note })`: `langfuseClient?.feedback.create({ traceId, name: 'correction', value: 0, comment: JSON.stringify({ reason, note }) })` — D-14 mirror; DB is source of truth, this is the mirror. Optional-chained (no-op when keys unset or in tests).
  </action>
  <verify>
  `node -e "console.log(require('ai/package.json').version)"` → `7.0.45`; `node -e "console.log(require('@langfuse/vercel-ai-sdk/package.json').version)"` → `5.9.1`; `npx tsc --noEmit` clean; `grep -c "ANTHROPIC_API_KEY\|FIRECRAWL_API_KEY\|LANGFUSE_PUBLIC_KEY\|LANGFUSE_SECRET_KEY\|LANGFUSE_TRACE_BASE_URL" src/lib/env.ts .env.example`
  </verify>
  <done>Deps installed at pinned versions; 5 optional server-only env keys declared; langfuse.ts bootstrap + trace-URL + mirror helpers exist and are test-guarded</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Port the AIRS validation gate to TypeScript (fails closed)</name>
  <files>src/lib/validation/airsRules.ts, src/lib/validation/validateReport.ts, src/lib/validation/fixtures/sample-valid.json, src/lib/validation/airsRules.test.ts</files>
  <behavior>
    - Test 1: `validateRunArtifacts(sampleValidOutput)` returns `{ valid: true, errors: [] }` (fixture copied verbatim from sibling `standards/examples/sample-valid.json`)
    - Test 2: a proposal whose `evidenceUrl` is NOT in `evidenceAppendix` → invalid, error mentions citation (rule `every_citation_must_resolve`)
    - Test 3: R/C enums out of range → invalid
    - Test 4: R3·C3 pair on a "strong" claim → invalid (rule `bluf_cannot_use_R3C3`)
    - Test 5: `keyUncertainties` empty → invalid
    - Test 6: empty proposals with a non-`no_intent` verdict → invalid (rule `empty_signals_implies_no_intent`)
  </behavior>
  <action>
  Create `src/lib/validation/airsRules.ts`: port the rule set from the sibling standards `airs-validation-rules.json` + `validate_report.py` verbatim (D-03), scoped to the hybrid shape (D-01):
  1. `every_citation_must_resolve` — every proposal `evidenceUrl` must appear in `evidenceAppendix` (D-02; appendix derived from REAL tool results server-side, so the model cannot invent citations that pass — T-09-03)
  2. R/C enums in range (`R1|R2|R3`, `C1|C2|C3`)
  3. `bluf_cannot_use_R3C3` — no R3·C3 on "strong" claims
  4. `key_uncertainties` non-empty
  5. `empty_signals_implies_no_intent` — empty proposals ⇒ verdict must be `no_intent`
  Full-report rules (`compliance_footer.approved_by`, dissemination semantics) are intentionally NOT ported (D-03 — approval here is per-proposal, not per-report).

  Create `src/lib/validation/validateReport.ts`: `validateRunArtifacts(input: z.infer<typeof outputSchema> & { evidenceAppendix: EvidenceAppendix }): GateResult` where `GateResult = { valid: boolean; errors: string[] }` — FAILS CLOSED: any rule violation ⇒ `{ valid: false, errors[] }`, never warnings-then-accept (Pitfall 4). Wire it to the outputSchema from `src/lib/agents/types.ts` (created in Task 4 — see Task 4's types contract; if Task 4 has not landed yet, define the same shapes here per the RESEARCH.md L349-361 sketch and re-export from types.ts later — keep ONE source of truth in types.ts).

  Copy `standards/examples/sample-valid.json` verbatim from the sibling repo `/Users/mkonovalov/Projects/arclumen-int360/standards/examples/sample-valid.json` into `src/lib/validation/fixtures/sample-valid.json` — it is the canonical test (D-03).

  Create `src/lib/validation/airsRules.test.ts` (anchor 09-01-02): fixture-driven — passes `sample-valid.json`, fails each rule independently (construct minimal per-rule-violating objects; no live calls).
  </action>
  <verify>npx vitest run src/lib/validation/airsRules.test.ts</verify>
  <done>Gate ported, fail-closed, fixture-driven tests green; validates against outputSchema + evidenceAppendix</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Agent core — types, runAgent (ToolLoopAgent v7 seam), tools, prompt, dedup</name>
  <files>src/lib/agents/types.ts, src/lib/agents/runAgent.ts, src/lib/agents/tools.ts, src/lib/agents/prompt.ts, src/lib/agents/dedup.ts, src/lib/agents/runAgent.test.ts, src/lib/agents/dedup.test.ts</files>
  <behavior>
    - Test 1 (09-01-01): `runAgent` — with `vi.mock('ai')` + `vi.mock('firecrawl')` + env stubbed, calling runAgent returns `{ output, usage, steps }` and `generateText` was invoked with `Output.object({ schema: outputSchema })` — zero live calls
    - Test 2 (09-01-04): `dedupProposals(proposals, liveSignals)` drops proposals whose (companyId, signalType) matches a live signal; keeps the rest; also drops within-set duplicates
    - Test 3: `buildAnalyzePrompt(company, liveSignals)` includes the known-signal skip list (D-11) and the no-fabrication citation rule (D-02)
  </behavior>
  <action>
  `src/lib/agents/types.ts` — single source of truth (zod; self-analog: `src/lib/enrichment/reviewProposal.ts` enums derived from DB enums):
  - `signalTypeValues` / `signalStrengthValues` mirrors of `signalTypeEnum`/`signalStrengthEnum` (4 + 3 values)
  - `reliabilitySchema` = z.enum(['R1','R2','R3']), `confidenceSchema` = z.enum(['C1','C2','C3'])
  - `proposalSignalSchema` = { signalType, strength, detectedAt (z.string() ISO date), evidenceUrl (z.string().url()), reliability, confidence, evidenceSnippet, reasoning } (per RESEARCH L349-361)
  - `outputSchema` = { proposals: z.array(proposalSignalSchema).min(0), keyUncertainties: z.array(z.string()) } PLUS `evidenceAppendix: z.array(z.object({ url: z.string().url(), title: z.string(), snippet: z.string() }))` — the D-02 appendix, populated server-side from real webSearch tool results (NOT model-recited)
  - `EvidenceAppendix = z.infer<...>` type export

  `src/lib/agents/tools.ts` — Firecrawl search tool (first Firecrawl usage; self-analog env-gate from `src/lib/arcpedia.ts` L31-33 but DIVERGE: throw on unset key — do NOT copy the silent-`[]` envelope, D-06/Pitfall 5):
  - lazy `getFirecrawl()`: `if (!env.FIRECRAWL_API_KEY) throw new Error('FIRECRAWL_API_KEY not configured')`; `new Firecrawl({ apiKey: env.FIRECRAWL_API_KEY })`
  - `webSearchTool = tool({ description, inputSchema: z.object({ query: z.string() }), execute: async ({ query }) => { const res = await getFirecrawl().search(query, { limit: 5 }); return res.data.map(r => ({ url: r.url, title: r.title, snippet: r.description ?? '' })); } })` — tool errors surface to the AI SDK tool loop (do NOT swallow)

  `src/lib/agents/prompt.ts` — `buildAnalyzePrompt(company, liveSignals)` pure module (no deps; self-analog `mergePlan.ts`): instructs search for the 4 buying-intent signal types, NEVER fabricate evidence (every claim needs a search-result URL), skip signal types already covered (D-11), fit the 60s budget (D-07 — lean, no multi-page dives).

  `src/lib/agents/runAgent.ts` — the mockable seam (09-01-01; self-analog wrapper shape `src/lib/enrichment/apollo.ts` but DIVERGE: fail-loud per D-06), per RESEARCH L321-372 EXACTLY:
  - `runAgent({ company, liveSignals, model = anthropic('claude-sonnet-4-20250514') })` — verify the EXACT model string against `@ai-sdk/anthropic` docs at implementation (research re-verify window 2026-08-07; fast model family preferred for D-07's 60s budget)
  - `const agent = new ToolLoopAgent({ instructions, stopWhen: isStepCount(12), tools: { webSearch: webSearchTool } })` — v7 API ONLY (v6 `Experimental_Agent` shapes are STALE, Pitfall 1)
  - `const { result } = await generateText({ model, agent, prompt: buildAnalyzePrompt(...), output: Output.object({ schema: outputSchema }) })`
  - returns `{ output: result.object, usage: result.usage, steps: result.steps }` (usage/steps feed OBSV-01 + appendix derivation in Plan 02)
  - NO telemetry option needed here — the global `registerTelemetry` (Task 2) captures it; never call `initLangfuse` inside runAgent (test-guard boundary)

  `src/lib/agents/dedup.ts` — pure: `dedupProposals(proposals, liveSignals)` filters out proposals whose (companyId, signalType) exists in liveSignals (D-11/ANLZ-05) and within-set duplicates; also export `alreadyCoveredSignalTypes(liveSignals)` for the UI's "already covered" message.

  Tests: `runAgent.test.ts` (anchor 09-01-01) — `vi.mock('ai')`, `vi.mock('firecrawl')`, stub env, assert generateText called with `Output.object` + ToolLoopAgent; assert structured `{ output, usage, steps }` return; never calls `initLangfuse`. `dedup.test.ts` (anchor 09-01-04) — pre/post dedup cases incl. within-set. Mock pattern: `vi.hoisted` + `vi.mock` per `src/app/actions/enrichment.test.ts`.
  </action>
  <verify>npx vitest run src/lib/agents/runAgent.test.ts src/lib/agents/dedup.test.ts</verify>
  <done>Agent core seam exists: zod types (incl. evidenceAppendix), env-gated fail-loud Firecrawl tool, prompt builder with skip-list, ToolLoopAgent v7 wrapper, pure dedup; runAgent + dedup tests green with zero live calls</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web content → agent | Untrusted fetched HTML/snippets enter via the webSearch tool result only |
| client → analyze API | Staff-only trigger; unauthenticated callers must be rejected (T-09-01) |
| app → Langfuse | Trace data (company names, evidence) leaves the app; keys are server-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | `POST /api/companies/[id]/analyze` + `/reviews` + actions | mitigate | `requireStaffAccess()` is the FIRST call in every handler/action/route (D-06); implemented Plan 02/03 |
| T-09-02 | Tampering | web content → agent prompt | mitigate | Fetched content enters ONLY as tool-call results (search snippets); structured-output schema + gate constrain what the model can emit; never spliced into `instructions` (implemented Task 3/4 + Plan 02) |
| T-09-03 | Tampering | fabricated/orphan citations | mitigate | D-03 gate `every_citation_must_resolve` ported to TS, fail-closed 422 (Task 3; route wiring Plan 03); appendix derived server-side from real tool results |
| T-09-04 | Information Disclosure | Anthropic/Firecrawl/Langfuse API keys | mitigate | D-15: server-only `env.ts` keys, never `PUBLIC_`-prefixed, never logged (Task 2) |
| T-09-05 | DoS / cost | paid-API run amplification | mitigate | Single company per run, `maxDuration = 60`, `isStepCount(12)` cap, lean prompt, staff-gated trigger (Task 4 + Plan 03) |
| T-09-06 | Tampering | mass-proposal flooding of queue | mitigate | Proposals only from gated runs (Plan 02) + post-dedup filter (Task 4); accept path staff-only (Plan 03) |
| T-09-07 | Tampering | race: duplicate signal writes | mitigate | Accept guarded by proposal status check (Plan 02) + unique index `signal(company_id, signal_type)` (Task 1); neon-http has no transactions — no `db.transaction()` anywhere |
| T-09-08 | Information Disclosure | trace data leakage | mitigate | Retention tags on evidence appendix entries (`public_biz`/`personal_data`); trace naming uses internal company name; staff-only trace access |
| T-09-SC | Tampering | npm installs (ai, firecrawl, langfuse) | mitigate | slopcheck 10/10 [OK] (2026-07-31) in RESEARCH.md Package Legitimacy Audit; versions pinned in Task 2; re-verify model string before runAgent ships |
</threat_model>

<verification>
Per VALIDATION.md sampling: after each task commit run `npm test`; after this wave run `npm test` + `npx tsc --noEmit`. Anchors: 09-01-01 `npx vitest run src/lib/agents/runAgent.test.ts` · 09-01-02 `npx vitest run src/lib/validation/airsRules.test.ts` · 09-01-04 `npx vitest run src/lib/agents/dedup.test.ts`. Wave 0 requirements from VALIDATION.md are satisfied by Tasks 3-4 (test files created). 09-01-03 (analyzeCompany) is deliberately deferred to Plan 02 (needs gate + dedup + runAgent all green).
</verification>

<success_criteria>
- [ ] `npx drizzle-kit check` clean; schema pushed; `npx tsc --noEmit` clean
- [ ] Deps at pinned versions; env keys optional-degrade; `initLangfuse()` test-guarded
- [ ] Gate fail-closed tests green; sample-valid.json fixture copied verbatim
- [ ] runAgent/dedup tests green (zero live API calls — D-16)
- [ ] `npm test` (full suite) still green (no regressions in existing 14 test files)
</success_criteria>

<output>
Create `.planning/phases/09-analytic-agent-observability/09-01-SUMMARY.md` when done
</output>
