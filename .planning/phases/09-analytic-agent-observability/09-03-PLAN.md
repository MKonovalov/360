---
phase: 09-analytic-agent-observability
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - src/app/api/companies/[id]/analyze/route.ts
  - src/app/actions/reviews.ts
  - src/app/actions/reviews.test.ts
  - src/app/(dashboard)/reviews/page.tsx
  - src/components/reviews/review-queue.tsx
  - src/components/reviews/reject-dialog.tsx
  - src/components/companies/proposal-badge.tsx
  - src/components/enrichment/enrichment-review-dialog.tsx
  - src/components/companies/company-detail.tsx
  - src/components/layout/app-sidebar.tsx
autonomous: false
requirements: [ANLZ-01, ANLZ-02, ANLZ-03, ANLZ-04, ANLZ-05, OBSV-01, OBSV-02]

must_haves:
  truths:
    - "Menu → Analyze on a Company triggers POST /api/companies/[id]/analyze and shows the run feedback strip (running / success / fail-loud error / already-covered)"
    - "The route handler calls requireStaffAccess() first, sets maxDuration = 60, and keeps the AI-domain try/catch separate from the DB-write try/catch (D-06/D-07/D-08)"
    - "A gate-failed run returns 422 gate_failed; an LLM/tool failure returns 502 analysis_failed; a DB failure returns 502 persist_failed — the client renders each distinctly"
    - "/reviews lists pending proposals with evidence URL + snippet + R/C + reasoning inline (ANLZ-03)"
    - "Accept writes exactly one live signal and marks the proposal accepted (idempotent); Reject opens the correction-reason dialog (OBSV-02)"
    - "The Company detail page shows an amber pending-proposals badge with the count from countPendingProposals (ANLZ-04)"
    - "Each proposal card shows the Langfuse 'View trace' link from traceUrl (OBSV-01)"
  artifacts:
    - path: "src/app/api/companies/[id]/analyze/route.ts"
      provides: "First Route Handler in the codebase — staff-gated, maxDuration=60, fail-loud, separate AI/DB domains"
      exports: ["POST", "maxDuration"]
    - path: "src/app/actions/reviews.ts"
      provides: "acceptProposalAction + rejectProposalAction server actions (staff-gated, fail-loud)"
      exports: ["acceptProposalAction", "rejectProposalAction"]
    - path: "src/app/(dashboard)/reviews/page.tsx"
      provides: "Review queue page listing pending proposals with evidence"
    - path: "src/components/reviews/review-queue.tsx"
      provides: "Client component — Accept/Reject controls + correction-reason dialog + trace link"
    - path: "src/components/reviews/reject-dialog.tsx"
      provides: "Correction reason selector (4 enum options + optional note) per OBSV-02"
    - path: "src/components/companies/proposal-badge.tsx"
      provides: "Amber pending-count badge (ANLZ-04)"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Reviews nav item (startsWith('/reviews') active state)"
  key_links:
    - from: "src/app/api/companies/[id]/analyze/route.ts"
      to: "src/lib/auth/requireStaffAccess.ts"
      via: "await requireStaffAccess() — first call (D-06)"
      pattern: "requireStaffAccess"
    - from: "src/app/api/companies/[id]/analyze/route.ts"
      to: "src/lib/agents/analyzeCompany.ts"
      via: "analyzeCompany(id) then validateRunArtifacts"
      pattern: "analyzeCompany|validateRunArtifacts"
    - from: "src/components/enrichment/enrichment-review-dialog.tsx"
      to: "/api/companies/[id]/analyze"
      via: "fetch POST on Analyze click (L177 slot)"
      pattern: "api/companies/\\$\\{.*\\}/analyze|/analyze"
    - from: "src/app/(dashboard)/reviews/page.tsx"
      to: "src/lib/db/queries/proposals.ts"
      via: "listPendingProposals + countPendingProposals"
      pattern: "listPendingProposals|countPendingProposals"
---

<objective>
The delivery slice of Phase 9 (Wave 3): wire the full user-facing flow — Menu → Analyze route handler (first Route Handler in the codebase, D-06), staff-gated accept/reject server actions, the `/reviews` review queue with evidence inline and correction-reason capture (ANLZ-03/OBSV-02), the amber pending badge on Company detail (ANLZ-04), and the Langfuse trace link on proposal cards (OBSV-01). Ends with the phase verification loop: full suite + `npx tsc --noEmit` + `npm run build` + manual UAT per VALIDATION.md Manual-Only table (live AI run, Langfuse trace, reject annotation mirror).

Purpose: this is what the user actually sees and operates; every earlier plan exists to make this slice correct and observable.
Output: route handler, reviews actions + test, /reviews page, 3 new components, 3 modified components (Analyze slot, badge mount, sidebar item).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-analytic-agent-observability/09-CONTEXT.md
@.planning/phases/09-analytic-agent-observability/09-RESEARCH.md
@.planning/phases/09-analytic-agent-observability/09-PATTERNS.md
@.planning/phases/09-analytic-agent-observability/09-VALIDATION.md
@.planning/phases/09-analytic-agent-observability/09-UI-SPEC.md
@.planning/phases/09-analytic-agent-observability/09-01-SUMMARY.md
@.planning/phases/09-analytic-agent-observability/09-02-SUMMARY.md
@src/lib/agents/analyzeCompany.ts
@src/lib/db/queries/proposals.ts
@src/lib/db/queries/corrections.ts
@src/app/actions/enrichment.ts
@src/components/enrichment/enrichment-review-dialog.tsx
@src/components/companies/company-detail.tsx
@src/components/layout/app-sidebar.tsx
@src/app/companies/import/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Handler POST /api/companies/[id]/analyze (first in codebase)</name>
  <files>src/app/api/companies/[id]/analyze/route.ts</files>
  <behavior>
    - Test 1 (09-03-01, runtime/manual): authenticated staff POST → 201 with run JSON; unauthenticated → 401/redirect (requireStaffAccess throws)
    - Test 2: gate-failed run → 422 { error: 'gate_failed', errors: [...] } (from analyzeCompany ok:false path)
    - Test 3: LLM/tool throw → 502 { error: 'analysis_failed' }; DB write throw → 502 { error: 'persist_failed' } — distinct bodies prove separate try/catch domains (D-08)
  </behavior>
  <action>
  Create `src/app/api/companies/[id]/analyze/route.ts` — the FIRST Route Handler in the codebase (glob confirmed: no `src/app/api/**` exists; call this out as a new architectural pattern). Follow the RESEARCH.md L427-453 skeleton EXACTLY:

  - `export const maxDuration = 60;` (Hobby ceiling, D-07)
  - `export async function POST(req: Request, { params }: { params: Promise<{ id: string }> })` — Next 16 App Router: `params` is a Promise; `await` it.
  - FIRST statement: `await requireStaffAccess();` (D-06/T-09-01 — same single gate as every page/action).
  - Parse + validate `id` (zod, matches company id type — integer per schema.ts serial pk; 400 on invalid).
  - `initLangfuse()` once (idempotent singleton from 09-01; never in tests).
  - TWO SEPARATE try/catch scopes (D-08):
    - Domain A (AI/tool): `const result = await analyzeCompany(id);` — if `!result.ok`: `result.reason === 'gate_failed'` → 422 `{ error: 'gate_failed', errors: result.errors }`; `reason === 'not_configured'` → 503 `{ error: 'not_configured' }` (client shows "not configured" message, D-15). Any throw → 502 `{ error: 'analysis_failed', message: String(err) }`.
    - Domain B (DB): only when Domain A succeeded — `const run = await persistRunAndProposals(id, result);` — createRun(traceId/traceUrl/usage/evidenceAppendix/hypotheses) + insertProposals(runId, id, result.proposals). Any throw → 502 `{ error: 'persist_failed', message: String(err) }` — never reported as an AI error.
  - Success → `Response.json(run, { status: 201 })` (fail-loud on any throw above; do NOT copy arcpedia.ts's silent-`[]` shape, D-06/Pitfall 5).
  - Add a comment noting: first Route Handler — the `proxy.ts` matcher already covers `/api`; `requireStaffAccess` is the single gate (D-06).

  Persist helper: put `persistRunAndProposals(companyId, result)` inline in this file (or in a small `src/lib/agents/persistRun.ts` if it exceeds ~40 lines) — it consumes `createRun` + `insertProposals` from 09-02 and returns the run row.
  </action>
  <verify>
  `npx tsc --noEmit` clean; `grep -n "maxDuration\|requireStaffAccess" src/app/api/companies/[id]/analyze/route.ts`; `npm test` (suite) green. Manual runtime check (needs live keys): with dev server up, unauthenticated POST → requireStaffAccess throws (401/redirect) — full live-flow UAT deferred to the end-of-phase manual pass per VALIDATION.md.
  </verify>
  <done>Route handler exists: staff-gated first, maxDuration=60, separate AI/DB try-catch domains, fail-loud bodies for gate_failed / analysis_failed / persist_failed / not_configured</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reviews server actions — accept + reject (staff-gated, fail-loud)</name>
  <files>src/app/actions/reviews.ts, src/app/actions/reviews.test.ts</files>
  <behavior>
    - Test 1: acceptProposalAction calls requireStaffAccess first, then acceptProposal(id); returns { ok: true } on success and { ok: false, reason } when already_resolved / duplicate_signal (idempotent no-error)
    - Test 2: rejectProposalAction validates reason against correctionReasonEnum via zod before calling rejectProposal; invalid reason → { ok: false, reason: 'invalid_reason' } with no write
    - Test 3: both actions never throw to the client — fail-loud { ok: false } envelope (Phase 8 convention)
  </behavior>
  <action>
  Create `src/app/actions/reviews.ts` — `'use server'`, exact analog `src/app/actions/enrichment.ts` (`commitEnrichment` L95-129: requireStaffAccess → safeParse → fail-loud `{ ok: false, reason }` → revalidatePath):
  - `acceptProposalAction(proposalId: number): Promise<{ ok: true } | { ok: false; reason: string }>` — `await requireStaffAccess()` first; `await acceptProposal(proposalId)` (09-02); return `{ ok: true }` or `{ ok: false, reason }` (already_resolved / duplicate_signal — both fine to surface as info, not errors); `revalidatePath('/reviews')` + `revalidatePath('/companies')` on success so the badge + queue refresh.
  - `rejectProposalAction(proposalId: number, input: { reason: string; note?: string })` — `await requireStaffAccess()` first; zod-validate `reason` against correctionReasonEnum ('wrong_signal_type' | 'missed_criteria' | 'hallucinated_no_evidence' | 'other') + `note` optional string (trim, max ~500 chars) — invalid → `{ ok: false, reason: 'invalid_reason' }` before any write (T-09-03-adjacent input validation); `await rejectProposal(proposalId, { reason, note, traceId })` where traceId comes from the proposal's run (join via getRunById or a proposal→run lookup — resolve at implementation against 09-02's query exports); `revalidatePath('/reviews')`.
  - Server actions only — never expose keys, never touch Langfuse client code outside the mirror helper (D-14/D-15).

  Create `src/app/actions/reviews.test.ts` (exact analog `src/app/actions/enrichment.test.ts` vi.hoisted + vi.mock): mock `requireStaffAccess`, mock `@/lib/db/queries/proposals` + `@/lib/db/queries/corrections` + `@/lib/telemetry/langfuse`; assert the three behaviors; zero live calls (D-16).
  </action>
  <verify>npx vitest run src/app/actions/reviews.test.ts</verify>
  <done>accept + reject actions staff-gated, zod-validated, fail-loud; tests green; revalidatePath wired</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review queue UI — /reviews page, components, Analyze wiring, badge, sidebar</name>
  <files>src/app/(dashboard)/reviews/page.tsx, src/components/reviews/review-queue.tsx, src/components/reviews/reject-dialog.tsx, src/components/companies/proposal-badge.tsx, src/components/enrichment/enrichment-review-dialog.tsx, src/components/companies/company-detail.tsx, src/components/layout/app-sidebar.tsx</files>
  <action>
  Build the UI per 09-UI-SPEC.md (approved). Components: ONLY Dialog/Select/Input/Badge/Button from the existing shadcn set + lucide-react icons — zero new component installs.

  1. `src/app/(dashboard)/reviews/page.tsx` — Server Component (exact analog `src/app/companies/import/page.tsx`): `await requireStaffAccess()` is implicit via the (dashboard) layout (verified: layout.tsx gates the whole group — T-09-01 covered at layout level); fetch `listPendingProposals()`; render `<ReviewQueue proposals={...} />`.
  2. `src/components/reviews/review-queue.tsx` — 'use client': pending proposal cards showing evidenceUrl (link), evidenceSnippet, R/C rating, reasoning inline (ANLZ-03); Accept button → `acceptProposalAction` (pending → success | already_resolved | duplicate_signal states); Reject button → opens `<RejectDialog>`; per-card "View trace" link → `traceUrl` (OBSV-01) when present; empty state ("No pending proposals").
  3. `src/components/reviews/reject-dialog.tsx` — 'use client' (role-match `src/components/import/rollback-dialog.tsx` confirm-dialog state machine): Select with the 4 correction reasons (labels: Wrong signal type / Missed inclusion-exclusion criteria / Hallucinated — no real evidence / Other), optional Input note, Confirm → `rejectProposalAction`; loading + error states.
  4. `src/components/companies/proposal-badge.tsx` — presentational (exact analog `src/components/companies/signal-badge.tsx`): amber Badge showing pending count; renders nothing when count is 0 (or "No pending" muted state per UI-SPEC — follow the spec).
  5. `src/components/enrichment/enrichment-review-dialog.tsx` L177 — replace `<DropdownMenuItem disabled>Analyze</DropdownMenuItem>` with the live Menu Analyze slot: on click, `fetch('/api/companies/[id]/analyze', { method: 'POST' })`; drive the run feedback strip with 4 states per UI-SPEC: running (spinner + "Analyzing…"), success (201 → "N proposals queued"), already-covered / gate failure (422 → show `errors` inline), not_configured (503 → "Analysis not configured — contact admin"), analysis/persist failure (502 → show message). lucide-react icons only. Never navigate away; results surface via the strip + the queue page.
  6. `src/components/companies/company-detail.tsx` — mount `<ProposalBadge count={...} />` in the Buying Signals section (self-analog L113-133): fetch `countPendingProposals()` for this company (add a `countPendingProposalsForCompany(companyId)` export to proposals.ts in 09-02 if the global count doesn't suffice — prefer the per-company count for the badge per ANLZ-04 wording) and render the run feedback strip slot per UI-SPEC.
  7. `src/components/layout/app-sidebar.tsx` — add a Reviews nav item after Key Personas (L56): `<SidebarMenuItem><SidebarMenuButton isActive={pathname.startsWith('/reviews')} asChild><Link href="/reviews">Reviews</Link></SidebarMenuButton></SidebarMenuItem>` + a lucide icon (e.g. Inbox) — matches the existing Start/Companies/Key Personas pattern (verified L27-56).

  If `countPendingProposalsForCompany(companyId)` is needed, add it to `src/lib/db/queries/proposals.ts` (09-02 output) — documented as a small additive change.
  </action>
  <verify>
  `npx tsc --noEmit` clean; `npm test` (full suite) green; `npm run build` green. Manual UAT per VALIDATION.md Manual-Only table: authenticated staff → open a Company → Menu → Analyze → synchronous response → proposals appear in /reviews with evidence; Langfuse trace visible with "View trace" link; reject a proposal with a reason → correction row has traceId + annotation appears on the Langfuse trace.
  </verify>
  <done>Full user flow works end-to-end: Analyze trigger with feedback strip, /reviews queue with evidence + Accept/Reject + trace link, amber pending badge, sidebar entry; build green; manual UAT passed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → analyze API | Staff-only; unauthenticated rejected at requireStaffAccess (T-09-01); request carries no secrets |
| client → server actions | Accept/Reject — staff-only; proposal ids and reason enums validated server-side |
| app → Langfuse | Trace + annotation data leaves the app (D-14 mirror) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | `POST /api/companies/[id]/analyze` + reviews actions | mitigate | `await requireStaffAccess()` FIRST in the handler (Task 1) + layout-level gate on (dashboard) (verified layout.tsx L9); no anonymous path |
| T-09-02 | Tampering | web content → agent | mitigate | Content stays tool-result-only (09-01/09-02); this plan adds no prompt surface |
| T-09-03 | Tampering | fabricated citations / malformed input | mitigate | 422 gate_failed path surfaces errors to the client but never persists (Task 1); zod validation on route id (Task 1) + reject reason (Task 2) |
| T-09-04 | Information Disclosure | API keys | mitigate | No key ever reaches client components — server-only env (09-01); actions/route are server-side only; nothing PUBLIC_-prefixed |
| T-09-05 | DoS / cost | paid-API amplification | mitigate | maxDuration=60 (Task 1); isStepCount(12) cap (09-01); staff-gated trigger; per-company runs only |
| T-09-06 | Tampering | mass-proposal flooding of queue | mitigate | Queue only populated by gated runs (09-02); accept/reject actions staff-gated (Task 2); UI read-only for non-staff (layout gate) |
| T-09-07 | Tampering | race: duplicate signal writes | mitigate | acceptProposal status-guarded + unique index (09-02); duplicate_signal surfaced idempotently in action (Task 2) |
| T-09-08 | Information Disclosure | trace data leakage | mitigate | "View trace" link only visible to staff; evidenceAppendix retention tags (09-01); internal company names only |
| T-09-SC | Tampering | npm installs | mitigate | No new packages in this plan; existing installs audited 10/10 [OK] |
</threat_model>

<verification>
Per VALIDATION.md sampling: after each task commit run `npm test`; after this wave run `npm test` + `npx tsc --noEmit` + `npm run build`; before `/gsd-verify-work` full suite + build + manual UAT (live AI run, Langfuse trace visibility, reject annotation mirror). Anchor 09-03-01 is runtime/manual (browser UAT + npm test). Auto-advance disabled for this plan's final checkpoint — the human must approve the live end-to-end flow.
</verification>

<success_criteria>
- [ ] Route handler live: Menu → Analyze shows the run feedback strip with all 4+ failure states distinct
- [ ] /reviews lists pending proposals with evidence inline; Accept/Reject work with idempotent behavior
- [ ] Amber pending badge on Company detail reflects countPendingProposalsForCompany
- [ ] Sidebar Reviews entry navigates and highlights correctly
- [ ] `npm test` + `npx tsc --noEmit` + `npm run build` all green
- [ ] Manual UAT passed: live run creates a Langfuse trace; reject captures reason + traceId + annotation mirror (OBSV-01/OBSV-02)
</success_criteria>

<output>
Create `.planning/phases/09-analytic-agent-observability/09-03-SUMMARY.md` when done
</output>
