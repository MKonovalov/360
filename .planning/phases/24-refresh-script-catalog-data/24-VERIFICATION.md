---
phase: 24-refresh-script-catalog-data
verified: 2026-08-04T12:20:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "The committed snapshot has a 25-row opencode-go roster (17 → 25 live rows)"
    reason: "models.dev/CLI lags the live Go roster by exactly 7 ids (live-verified 2026-08-04: all 7 pinned ids present in live, absent from CLI parse). The user approved the D-24-07 pinned GO_KNOWN_LIVE_ONLY_IDS exception (acceptance-only: any NEW live-only id or ANY CLI-only id still aborts; Zen stays fully strict; accepted drift logged every run, never silent). The snapshot ships the 18 CLI rows — the honest maximum without injecting live ids, which would violate the strict-drift doctrine and CAT-04's no-injection contract."
    accepted_by: "Mikhail Konovalov (blocking human checkpoint approval, D-24-07 strictness revisit + D-24-11 re-lock)"
    accepted_at: "2026-08-04"
gaps: []
deferred:
  - truth: "instantiateModel still throws 'unsupported provider' for nousresearch/opencode ids (24-REVIEW WR-01) — the phase's data change makes the hermes pins union-servable before the run path is wired"
    addressed_in: "Phase 25 (Run Path / modelFactory Seam)"
    evidence: "Phase 25 goal: 'The Analytic Agent instantiates and runs cross-provider chains across all four providers safely' — RUN-01/RUN-02 wire the nousresearch/opencode createOpenAICompatible instances and zen-vs-go dispatch. Phase boundary (24-CONTEXT.md): 'no run-path/instantiation changes (Phase 25)'. Not a Phase 24 blocker; the phase's data half landed as intended."
---

# Phase 24: Refresh Script + Catalog Data Verification Report

**Phase Goal:** The committed catalog snapshot ships current NousResearch rows and a refreshed OpenCode roster, generated by an extended dev-time refresh script.
**Verified:** 2026-08-04T12:20:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Snapshot is grouped `{ generatedAt, providers: { <providerID>: [...] } }` — 9 provider groups, 1427 rows, no top-level `models` key, `generatedAt` top-level, sorted keys | ✓ VERIFIED | `node -e` probe: groups `anthropic,google,kilo,nousresearch,openai,opencode,opencode-go,openrouter,vercel`, totalRows 1427, `models` undefined, generatedAt `2026-08-04T09:44:37.964Z`, sortedKeys true |
| 2 | `catalog.ts` exposes `ModelCatalog` grouped type + `getAllModels()` flattening helper; all direct `.models` reads routed through it (grep = 0); registry gates/precedence/union/dedup unchanged | ✓ VERIFIED | `catalog.ts:6-18` (`ModelCatalog`, `getAllModels`), `:45` (`getModelDisplayName`), `:132` (`dedupeProviderRows`); `grep -rn "catalogJson\.models" src/` → 0 matches; only remaining `.models` in src/ is `model-picker.tsx:140`'s local CommandGroup (not catalog shape, per 24-REVIEW.md) |
| 3 | `modelFactory.ts` openrouter row lookup uses `getAllModels(catalogJson).find(...)` with the provider-scoped predicate intact | ✓ VERIFIED | `modelFactory.ts:66-68`: `getAllModels(catalogJson).find((m) => m.id === id && m.providerID === 'openrouter')`; `getAllModels` on import line 4 |
| 4 | Refresh script fetches the anonymous Nous roster (292 live rows) mapping to `providerID: 'nousresearch'`, `api.url https://inference-api.nousresearch.com/v1`, `api.npm @ai-sdk/openai-compatible` (CAT-01) | ✓ VERIFIED | `scripts/refresh-model-catalog.ts:173-192` (`fetchNousRoster`, throws-not-degrades), `:216-239` (`nousPreMap`); live curl 2026-08-04: 292 rows; snapshot ships 292 nousresearch rows with mandated api mapping |
| 5 | `perMTok()` converts per-token → per-MTok (×1e6) and `structuredOutputs` live-joins `supported_parameters`; any fetch failure aborts WITHOUT writing (CAT-02, throws-not-degrades) | ✓ VERIFIED | `refresh-model-catalog.ts:207-209` (`perMTok` ×1e6, 6-dp rounding), `:237` (structured_outputs join); live hermes pricing `0.00000005/0.0000002` → snapshot `0.05/0.2`; hermes `supported_parameters` lacks `structured_outputs` → snapshot `structuredOutputs: false`; fetch errors throw before `writeFileSync` (`:178-187`, `:274-283`); `verifyZenGoRosters` awaited at `:360` before write at `:375-378` |
| 6 | `deriveNousFamily()` derives family from id prefix; snapshot regenerated and committed with the new nousresearch rows + refreshed Go roster (CAT-03) | ✓ VERIFIED | `refresh-model-catalog.ts:198-202`; snapshot has 292 nous rows; Go roster refreshed 17→18 (+qwen3.8-max). **Nuance:** the roadmap's literal "17 → 25 live rows" was superseded by the user-approved D-24-07 amendment — see Override + Honest Assessment below |
| 7 | `verifyZenGoRosters()` strict drift check per D-02; Zen-wins dedup expressed once and survives regeneration (CAT-04) | ✓ VERIFIED | `refresh-model-catalog.ts:265-323`: Set-diff with per-id Live-only/CLI-only reporting; Zen fully strict; Go narrowed ONLY by the user-approved pinned `GO_KNOWN_LIVE_ONLY_IDS` (7 ids, `:247-255`, accepted drift logged every run `:307-311`, never silent); dedup expressed once in `dedupeProviderRows` (`catalog.ts:130-135`) and NO-FLIP canary proves no endpoint flips post-regeneration |
| 8 | Script writes the grouped `{ generatedAt, providers }` shape with sorted keys | ✓ VERIFIED | `refresh-model-catalog.ts:364-372`; committed snapshot matches this shape exactly |
| 9 | Canaries re-locked to ACTUAL regenerated numbers in the same commit as the snapshot (D-24-11); boundary canary flipped to hermes pins; full NOUSRESEARCH (D-24-12) group locked | ✓ VERIFIED | `catalog.test.ts`: COUNT-STABILITY 40 {compat 23, anthropic 17} (`:441,447-448`), NO-FLIP pool 66, dual 12, go-exclusive 6 (`:473,475-502`), boundary = hermes pins (`:392-395`), NOUSRESEARCH (D-24-12) group (`:511-580`); independently re-derived from snapshot: dual 12, go-ex 6, servable 40/23/17 all match; commit `56d9fdaa` contains BOTH catalog.json + catalog.test.ts (2 files, 7657+/1719−) |
| 10 | Build/tests: tsc 0 errors; catalog.test.ts 45/45; full suite 403 passed / 6 skipped / 1 failed (1 = pre-existing VER-03, not a Phase 24 regression) | ✓ VERIFIED | `npx tsc --noEmit` exit 0; `npx vitest run src/lib/models/catalog.test.ts` 45/45; `npm test` → 1 failed | 403 passed | 6 skipped — failure is `openrouter-only-chain.test.ts` VER-03 live billing assertion (`out.ok` true requires credited OPENROUTER_API_KEY; uncredited → 402), documented pre-existing in STATE.md:197, PROJECT.md, and all four plan summaries; unchanged by this phase |

**Score:** 10/10 truths verified

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases (from Step 9b).

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | `instantiateModel` throws "unsupported provider" for nousresearch/opencode ids — hermes pins are now union-servable (the phase's own canary asserts it) but the run path isn't wired (24-REVIEW WR-01; would 500 on an Analyze with a saved hermes pin) | Phase 25 | Phase 25 goal: "The Analytic Agent instantiates and runs cross-provider chains across all four providers safely" — RUN-01/RUN-02 wire the three `createOpenAICompatible` instances + zen-vs-go dispatch. Phase boundary (24-CONTEXT.md): "no run-path/instantiation changes (Phase 25)". Not a Phase 24 blocker — the phase's data half shipped as intended and Phase 25 is the immediate next phase |

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/lib/models/catalog.json` | Grouped snapshot `{ generatedAt, providers }` | ✓ VERIFIED | 9 groups, 1427 rows, 292 nousresearch (hermes pins 0.05/0.2 & 0.09/0.37 @ 131072 ctx, structuredOutputs false), 60 opencode, 18 opencode-go, no top-level models |
| `src/lib/models/catalog.ts` | `ModelCatalog` type + `getAllModels()` helper; direct reads routed through it | ✓ VERIFIED | Lines 6-18, 45, 132; registry logic byte-identical (git diff = intended hunks only) |
| `src/lib/models/catalog.test.ts` | Grouped fixture + hermes live re-values; re-locked COUNT-STABILITY/NO-FLIP; flipped boundary; NOUSRESEARCH (D-24-12) group | ✓ VERIFIED | 45/45 green; fixture hermes rows carry live values (0.05/0.2, 0.09/0.37, 131072, structuredOutputs false); mirror rows unchanged (0.2/0.6, 0.8/1.2) |
| `src/lib/agents/modelFactory.ts` | openrouter lookup via `getAllModels` with provider-scoped predicate | ✓ VERIFIED | Line 4 import; lines 66-68 scoped find |
| `scripts/refresh-model-catalog.ts` | `fetchNousRoster`, `deriveNousFamily`, `perMTok`, `nousPreMap`, `verifyZenGoRosters` + grouped write | ✓ VERIFIED | All present and wired (see Truths 4-8); keep-as-is functions byte-identical (D-24-10) |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| `catalog.ts:getModelDisplayName` | `getAllModels` | flattening helper | ✓ WIRED | `getAllModels(catalogJson).find(...)` (line 45) |
| `catalog.ts:dedupeProviderRows` | `getAllModels` | flattening helper | ✓ WIRED | `getAllModels(catalog).filter(...)` (line 132) |
| `modelFactory.ts:instantiateModel` | `getAllModels` | registry import | ✓ WIRED | `getAllModels(catalogJson).find((m) => m.id === id && m.providerID === 'openrouter')` (lines 66-68) |
| `main()` | `verifyZenGoRosters(parsed)` | await before writeFileSync | ✓ WIRED | Line 360 awaits verify; write at 375-378; any throw → `main().catch` exit 1 (382-387), snapshot untouched |
| `fetchNousRoster` rows | `nousPreMap` | perMTok ×1e6 + deriveNousFamily + structured_outputs join | ✓ WIRED | Lines 216-239; live-verified conversion 0.00000005→0.05, 0.0000002→0.2 |
| `catalog.test.ts` NOUSRESEARCH group | `catalogJson.providers.nousresearch` + `getServableIdsForProvider` | committed-snapshot reads | ✓ WIRED | Lines 520, 529, 537, 564-567 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `catalog.json` nousresearch group | `providers.nousresearch` | live Nous roster (anonymous GET) | ✓ Real — 292 live rows; live curl 2026-08-04 confirms count, pricing, context, structured_outputs all match the committed snapshot | ✓ FLOWING |
| `catalog.json` opencode-go group | `providers['opencode-go']` | CLI parse of models.dev (18 rows) | ✓ Real — regenerated from live CLI state; 7-id gap vs live Go is the pinned, logged, user-approved drift (never injected) | ✓ FLOWING (accepted drift) |
| `catalog.ts` servable outputs | `getServableIdsForProvider(catalogJson, ...)` | committed snapshot via registry | ✓ Real — 40 opencode servable, 2 hermes pins, 336 openrouter all from committed data; independently re-derived | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Snapshot structure + counts | `node -e "..."` probe | 292 nous / 18 go / 60 zen / 1427 total / 9 groups / no models key / sorted | ✓ PASS |
| No direct `.models` reads | `grep -rn "catalogJson\.models" src/` | 0 matches (exit 1) | ✓ PASS |
| Type check | `npx tsc --noEmit` | exit 0, no errors | ✓ PASS |
| Canary suite | `npx vitest run src/lib/models/catalog.test.ts` | 45/45 passed | ✓ PASS |
| Full suite | `npm test` | 403 passed / 6 skipped / 1 failed — failure is pre-existing VER-03 openrouter-only-chain billing (uncredited key → 402), documented in STATE.md:197 | ✓ PASS (1 pre-existing, note-not-fix) |
| D-24-11 atomic commit | `git show 56d9fdaa --stat` | 2 files: catalog.json + catalog.test.ts, 7657+/1719− | ✓ PASS |
| Nous roster live state | `curl https://inference-api.nousresearch.com/v1/models` | 292 rows; hermes pricing 0.00000005/0.0000002 (×1e6 → 0.05/0.2), ctx 131072, structured_outputs NOT advertised | ✓ PASS |
| Zen/Go roster live state | `curl https://opencode.ai/zen/v1/models` + `/zen/go/v1/models` | Zen 60/60; Go 25 live, all 7 pinned ids present live (exception set exactly matches the live-only ids) | ✓ PASS |

Note: a live `npm run models:fetch` re-run was NOT executed here — it writes the snapshot (mutating state, forbidden by the spot-check contract). The abort-without-write doctrine and the accepted-drift path are instead evidenced by: (a) code reading (throw paths precede writeFileSync; the known-drift log is unconditional when knownDrift > 0), (b) 24-REVIEW.md's independent live end-to-end run ("Zen 60/60 clean… Go 25-live vs 18-CLI with missing = exactly the 7 pinned ids, all accepted… no throw"), and (c) today's live curls confirming the exact drift set.

### Probe Execution

| Probe | Command | Result | Status |
| ----- | ------- | ------ | ------ |
| N/A — no probe scripts declared in plans or present under scripts/*/tests/ | — | — | SKIPPED (no probe scripts; per-plan verification is vitest/tsc/smoke, all covered above) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| CAT-01 | 24-02, 24-04 | Script gains anonymous `GET https://inference-api.nousresearch.com/v1/models` (HTTP 200, 292 rows) mapping rows to `providerID: 'nousresearch'`, mandated api.url/npm | ✓ SATISFIED | `fetchNousRoster()` + `nousPreMap()` (`refresh-model-catalog.ts:173-239`); live curl: 292 rows; snapshot: 292 rows all `providerID === 'nousresearch'`, hermes pin api.url/npm exact (canary `catalog.test.ts:519-526`) |
| CAT-02 | 24-02, 24-03, 24-04 | Pricing per-token → per-MTok (×1e6); `supported_parameters` live-joined to `structuredOutputs`; throws-not-degrades on failure | ✓ SATISFIED | `perMTok()` (`:207-209`); join at `:237`; live 0.00000005/0.0000002 → 0.05/0.2 (canary `:535-543`); hermes `false` + non-vacuous 214/78 (canary `:545-553`); fetch errors throw before write |
| CAT-03 | 24-01, 24-02, 24-03, 24-04 | Nous `family` derived from id prefix; snapshot regenerated and committed with `nousresearch` rows + refreshed Go roster | ✓ SATISFIED (nuance) | `deriveNousFamily()` (`:198-202`); hermes → 'hermes', qwen3.8-max → 'qwen3.8' (canary `:555-560`); snapshot regenerated + committed (commit `56d9fdaa`) with 292 nous rows; Go roster refreshed 17→18 — literal "25 live rows" superseded by user-approved D-24-07 amendment (see Override; requirement marked Complete in REQUIREMENTS.md with the drift state documented in STATE.md:181) |
| CAT-04 | 24-01, 24-02, 24-03 | Zen/Go roster-verify per D-02; Zen-wins dedup expressed once and survives regeneration | ✓ SATISFIED | `verifyZenGoRosters()` strict Set-diff with per-id reporting (`:265-323`), Zen fully strict, Go narrowed only by the user-approved pinned exception (documented + logged every run — never silent); dedup once in `dedupeProviderRows`; NO-FLIP canary green post-regeneration (dual 12 Zen-kept, go-ex 6, no endpoint flips) |

**Orphaned requirements:** none — all four CAT IDs appear in at least one plan's frontmatter (24-01: CAT-03/CAT-04; 24-02: CAT-01..04; 24-03: CAT-02/03/04; 24-04: CAT-01/02/03), and all four are marked Complete in REQUIREMENTS.md traceability, matching the shipped evidence.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | No TBD/FIXME/XXX debt markers in any phase-modified file | ℹ️ None | Clean |
| `scripts/refresh-model-catalog.ts:59-77` | WR-02 (24-REVIEW) | `parseModels` silently drops single-line compact JSON records | ⚠️ Warning | Pre-existing latent gap (CLI emits multi-line JSON today); Zen/Go drift check would catch opencode/opencode-go loss; other groups uncounted. Not a Phase 24 regression, not a goal blocker |
| `scripts/refresh-model-catalog.ts:207-209` | WR-03 (24-REVIEW) | `perMTok` propagates NaN (renders null cost) on non-numeric pricing strings | ⚠️ Warning | Robustness gap; live Nous roster is verified numeric-string so not a live bug; surfaced to the next maintainer via the committed review |
| `src/lib/agents/modelFactory.ts:77-79` | WR-01 (24-REVIEW) | "unreachable post-gate" backstop comment now false for hermes pins | ⚠️ Warning | Deferred to Phase 25 (see Deferred Items); the throw is real if a user saves a hermes pin before Phase 25 wires the provider |

### Human Verification Required

None — the phase's two blocking human checkpoints were executed and approved during execution with recorded evidence:
1. **D-24-07 strictness revisit** (Plan 02 Task 2 escalation branch) — user chose the deliberate strictness revisit; implemented as the pinned `GO_KNOWN_LIVE_ONLY_IDS` exception (commit `444bb9ed`), recorded in STATE.md:181.
2. **D-24-11 canary re-lock review** (Plan 03 Task 3) — human approved the re-locked numbers as-is (2026-08-04), recorded in 24-03-SUMMARY.md.

The one live re-run (`npm run models:fetch`) mutates the committed snapshot, so it was not executed by the verifier; its behavior is established by code reading, the independent live run in 24-REVIEW.md, and today's live roster curls.

### Gaps Summary

No gaps. All 10 must-haves verified against the codebase with live evidence. The single full-suite failure is the pre-existing, documented VER-03 billing assertion (uncredited `OPENROUTER_API_KEY` → 402) — not a Phase 24 regression and plan-mandated to note-not-fix.

### Honest Assessment: the "refreshed OpenCode roster" (25-row target vs 18-row shipped state)

The phase goal — "the committed catalog snapshot ships current NousResearch rows and a refreshed OpenCode roster" — **is met**. Evidence:

- **NousResearch rows: fully current** — 292 live roster rows shipped verbatim with correct ×1e6 pricing, live-joined `structuredOutputs`, derived family, and verbatim `~latest` aliases; live curl today confirms the snapshot matches the live roster exactly.
- **OpenCode roster: refreshed, not inflated** — the roster regenerated from the current CLI state, 17 → 18 rows (+qwen3.8-max, which is why COUNT-STABILITY moved 39→40 and NO-FLIP go-exclusive 5→6). The planned 25-row state was **unattainable without injecting live ids**: models.dev itself lags the live Go roster by `hy3-preview`, and the CLI (1.18.12 = npm latest) filters 6 more — live-verified today that exactly the 7 pinned ids exist live but not in the CLI parse. The user-approved D-24-07 amendment accepts + logs this drift on every run and deliberately **never injects** the 7 ids (injection would violate the strict drift doctrine D-24-07, the "snapshot reflects CLI-parsed reality" contract, and CAT-04's no-silent-injection intent). The exception is self-healing: when models.dev/CLI closes the gap, the pinned set becomes inert and the Go group naturally reaches 25 (noted in 24-02-SUMMARY.md).

The 25-row figure in the roadmap and requirements was a plan-time estimate of the live roster, superseded by the user's explicit, documented decision. The shipped 18-row state is the correct, honest maximum — the roster was genuinely refreshed, and the gap is surfaced (logged + STATE.md + requirement traceability), never hidden.

---

_Verified: 2026-08-04T12:20:00Z_
_Verifier: Claude (gsd-verifier)_
