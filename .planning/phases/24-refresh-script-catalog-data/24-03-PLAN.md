---
phase: 24-refresh-script-catalog-data
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [src/lib/models/catalog.json, src/lib/models/catalog.test.ts]
autonomous: false
requirements: [CAT-02, CAT-03, CAT-04]
must_haves:
  truths:
    - "The committed snapshot regenerates in the grouped shape with 292 nousresearch rows and a 25-row opencode-go roster"
    - "COUNT-STABILITY / NO-FLIP / boundary canaries are re-locked to the ACTUAL regenerated numbers in the SAME commit as the regenerated snapshot (never auto-derived, never research estimates)"
  artifacts:
    - path: "src/lib/models/catalog.json"
      provides: "regenerated grouped snapshot"
      contains: "nousresearch"
    - path: "src/lib/models/catalog.test.ts"
      provides: "re-locked COUNT-STABILITY + NO-FLIP canaries; flipped nousresearch boundary canary"
      contains: "nousresearch/hermes-4-70b"
  key_links:
    - from: "catalog.test.ts"
      to: "catalog.json providers"
      via: "getServableIdsForProvider / dedupeProviderRows on the committed snapshot"
      pattern: "getServableIdsForProvider\\(catalogJson, 'nousresearch'\\)"
---

<objective>
Run the extended refresh (`npm run models:fetch`) to regenerate and commit the grouped snapshot with the new `nousresearch` rows (292) and the refreshed Go roster (25 live rows), then re-lock the Phase 23 canaries (COUNT-STABILITY, NO-FLIP) to the ACTUAL regenerated numbers and flip the `nousresearch = []` boundary canary to the live hermes pins — all in the SAME commit as the regenerated snapshot (D-24-11). A human checkpoint reviews the re-locked numbers before final approval (the D-24-11 "deliberate, reviewed re-lock").

Purpose: This is the data half of the phase — the committed snapshot ships the new data, and the canaries are deliberately re-locked so they keep catching unintended future shape changes (never auto-derived from the snapshot — that would defeat them).

Output: Committed regenerated `catalog.json` (grouped, 292 nous rows, 25 go rows) + re-locked canaries green in one commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-refresh-script-catalog-data/24-CONTEXT.md
@.planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md
@.planning/phases/24-refresh-script-catalog-data/24-PATTERNS.md
@.planning/phases/24-refresh-script-catalog-data/24-VALIDATION.md
@.planning/phases/24-refresh-script-catalog-data/24-02-SUMMARY.md

# Files being modified — read first, per task
@src/lib/models/catalog.json
@src/lib/models/catalog.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate the snapshot (npm run models:fetch) and verify the new data lands in the grouped shape</name>
  <files>src/lib/models/catalog.json</files>
  <read_first>
    - scripts/refresh-model-catalog.ts (the extended pipeline from Plan 02 — what this run executes)
    - src/lib/models/catalog.json (pre-run state: grouped, 8 provider groups, no nousresearch)
    - .planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md §API Shapes (expected Nous row counts/values) + §Landmine 1 (drift-implies-blocked)
  </read_first>
  <action>
    Run `npm run models:fetch` — the ONE atomic refresh command (D-24-10): CLI parse → OpenRouter structured-output join → Nous roster fetch (292) → Zen/Go strict drift check → grouped write. The Plan 02 checkpoint confirmed the CLI's go roster matches live, so the drift check passes.

    Prerequisite check: if the run ABORTS at the Go drift check (CLI still short of 25 go rows), the refresh is BLOCKED by design (throws-not-degrades) — do NOT relax the check; capture the per-id drift list and escalate (Plan 02 checkpoint branch). The committed snapshot stays usable.

    Verify the regenerated snapshot structurally before any commit:
    - `node -e "const s=require('./src/lib/models/catalog.json');console.log('topLevelModelsRemoved:',s.models===undefined);console.log('generatedAtTopLevel:',typeof s.generatedAt==='string');console.log('nousRows:',s.providers.nousresearch?.length);console.log('goRows:',s.providers['opencode-go']?.length);console.log('zenRows:',s.providers.opencode?.length);console.log('sortedKeys:',Object.keys(s.providers).every((k,i,a)=>i===0||a[i-1]<k));"` — expect `nousRows: 292`, `goRows: 25`, `zenRows: 60`, `topLevelModelsRemoved: true`, `generatedAtTopLevel: true`, `sortedKeys: true`.
    - Spot-check a hermes pin: `node -e "const s=require('./src/lib/models/catalog.json');const h=s.providers.nousresearch.find(m=>m.id==='nousresearch/hermes-4-70b');console.log(JSON.stringify({cost:h.cost,ctx:h.limit.context,so:h.structuredOutputs,family:h.family,url:h.api.url,npm:h.api.npm}));"` — expect `{"cost":{"input":0.05,"output":0.2},"ctx":131072,"so":false,"family":"hermes","url":"https://inference-api.nousresearch.com/v1","npm":"@ai-sdk/openai-compatible"}`.
    - `git diff --stat src/lib/models/catalog.json` — large expected diff (+~292 nous rows; opencode-go block 17 → 25).
    Do NOT commit yet — the canary re-lock (Task 2) must land in the SAME commit (D-24-11).
  </action>
  <verify>
    <automated>bash -c "npm run models:fetch"</automated>
    <automated>node -e "const s=require('./src/lib/models/catalog.json');const ok=s.providers.nousresearch.length===292&&s.providers['opencode-go'].length===25&&s.models===undefined&&typeof s.generatedAt==='string';console.log('structuralCheck:',ok);process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run models:fetch` exits 0; console shows `Wrote src/lib/models/catalog.json: <N> models` where N = (CLI-parsed rows incl. 60 opencode + 25 opencode-go) + 292 nous.
    - Structural check passes: 292 nousresearch rows, 25 opencode-go rows, 60 opencode rows, `models` key removed, `generatedAt` top-level, keys sorted.
    - Hermes pin spot-check matches live values exactly (0.05/0.2, 131072, structuredOutputs false, family hermes, mandated api.url/npm).
    - `git status --short` shows only `src/lib/models/catalog.json` modified (plus nothing else) at this point.
  </acceptance_criteria>
  <done>The regenerated grouped snapshot holds the full Nous roster (292) + refreshed Go roster (25) with correct hermes values, uncommitted pending the canary re-lock.</done>
</task>

<task type="auto">
  <name>Task 2: Re-lock COUNT-STABILITY + NO-FLIP to actual regenerated numbers, flip the nousresearch boundary canary — SAME commit as the snapshot</name>
  <files>src/lib/models/catalog.test.ts</files>
  <read_first>
    - src/lib/models/catalog.test.ts (COUNT-STABILITY describe l.407-435; NO-FLIP describe l.437-471; the `nousresearch = []` boundary canary l.369-371)
    - .planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md §Open Question 2 (compute re-lock numbers from the ACTUAL regenerated snapshot — never hardcode research estimates) + §Pitfall 1 (canary count drift is EXPECTED and re-locked deliberately)
    - .planning/phases/24-refresh-script-catalog-data/24-PATTERNS.md §catalog.test.ts items 3-4 (exact re-lock structure)
  </read_first>
  <action>
    Compute the ACTUAL post-refresh numbers from the regenerated snapshot (NOT the research estimates — the executor MUST read the real values):

    1. Run the computation one-liner:
       `npx tsx -e "import { getServableIdsForProvider, dedupeProviderRows } from './src/lib/models/catalog'; import catalogJson from './src/lib/models/catalog.json'; const ids=getServableIdsForProvider(catalogJson,'opencode'); const pool=dedupeProviderRows(catalogJson,'opencode'); const gated=pool.filter(m=>m.status!=='deprecated'&&['@ai-sdk/openai-compatible','@ai-sdk/anthropic'].includes(m.api.npm)); const split=gated.reduce((a,m)=>{a[m.api.npm]=(a[m.api.npm]??0)+1;return a},{}); const zen=new Set(catalogJson.providers.opencode.map(m=>m.id)); const go=catalogJson.providers['opencode-go'].map(m=>m.id); const dual=[...new Set(go.filter(id=>zen.has(id)))].sort(); const goEx=go.filter(id=>!zen.has(id)).sort(); console.log(JSON.stringify({servableCount:ids.length,npmSplit:split,poolCount:pool.length,dualIds:dual,goExclusiveIds:goEx,goTotal:go.length},null,2));"`
       Record: `servableCount`, `npmSplit` ({'@ai-sdk/openai-compatible': N, '@ai-sdk/anthropic': M}), `poolCount`, `dualIds` (expected ~16), `goExclusiveIds` (expected ~9 — includes the new `mimo-v2-pro`, `mimo-v2-omni`, `hy3-preview`), `goTotal` (25).

    2. COUNT-STABILITY (l.407-435): replace the hardcoded numbers with the computed ones — `expect(ids).toHaveLength(<servableCount>)`; the npm-split assertions `toHaveLength(<N>)` / `toHaveLength(<M>)`; KEEP unchanged the zero GPT/Gemini leak check and the slash-free assertion (l.424-433). Update the comment (l.409-411) to the new post-dedup servable count and today's date.

    3. NO-FLIP (l.437-471): replace `expect(pool).toHaveLength(65)` with `<poolCount>`; replace the `dualIds` array (l.444-457) with the computed `dualIds`; replace the `goExclusiveIds` array (l.464) with the computed `goExclusiveIds`. KEEP the per-id loop assertions (keep-Zen-row / keep-Go-row + url) byte-identical in structure. Update the comment to note the re-lock (D-24-11, post-refresh date).

    4. Flip the boundary canary (l.369-371): `expect(getServableIdsForProvider(catalogJson, 'nousresearch')).toEqual([])` → `expect(getServableIdsForProvider(catalogJson, 'nousresearch')).toEqual(['nousresearch/hermes-4-70b', 'nousresearch/hermes-4-405b'])`; rename the test to reflect the live hermes canary (D-23-07 — the rows landed in Phase 24) and update the comment.

    5. Commit BOTH the regenerated `catalog.json` (Task 1) and these test changes in ONE commit (D-24-11 — re-lock in the SAME commit as the regenerated snapshot). Message like `data(models): regenerate catalog snapshot (nousresearch 292 rows, go 17->25) + re-lock canaries (D-24-11)`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/models/catalog.test.ts && npx tsc --noEmit</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/models/catalog.test.ts` — green, including the re-locked COUNT-STABILITY (<servableCount> + npm split), NO-FLIP (<poolCount> + computed dual/go-exclusive sets), and the flipped hermes-pins boundary canary.
    - `npx tsc --noEmit` — 0 errors.
    - `npm test` — full suite green (all other consumers — modelConfig/runAgent/settings/modelFactory tests — pass against the regenerated snapshot via registry fns; the openrouter ≥300 assertion still holds at 336/337 rows).
    - One commit contains BOTH `src/lib/models/catalog.json` AND `src/lib/models/catalog.test.ts` (D-24-11); `git show --stat HEAD` proves it.
    - The re-locked numbers are the ACTUAL computed values — cross-check the committed test file against the Task 1 computation output (never research estimates).
  </acceptance_criteria>
  <done>The regenerated snapshot and the explicitly re-locked canaries are committed together and green; the nousresearch boundary canary now asserts the live hermes pins.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Review the re-locked canary numbers + regenerated snapshot diff (D-24-11 reviewed re-lock)</name>
  <files>none (review of committed artifacts)</files>
  <read_first>
    - .planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md §Open Question 2 + §Pitfall 1 (why the re-lock is deliberate and explicit, never auto-derived)
  </read_first>
  <action>
    Claude automates: present the committed re-lock evidence — `git show HEAD --stat` (both files in one commit), the re-locked numbers from `src/lib/models/catalog.test.ts` (COUNT-STABILITY count + npm split; NO-FLIP pool + dual/go-exclusive sets), and the snapshot spot-check node command output (nous/go/zen counts + generatedAt). The human reviews and approves the numbers (or requests a corrective follow-up commit).
  </action>
  <what-built>
    The regenerated grouped snapshot (292 nousresearch rows, opencode-go 17 → 25) and the re-locked COUNT-STABILITY/NO-FLIP canaries, committed together per D-24-11. The exact numbers were computed from the ACTUAL regenerated snapshot at execution time (research Open Question 2) and are presented for the deliberate, reviewed re-lock the doctrine requires.
  </what-built>
  <how-to-verify>
    1. Read the committed re-lock: `git show HEAD --stat` (both files in one commit) then inspect the re-locked numbers in `src/lib/models/catalog.test.ts` (COUNT-STABILITY count + npm split; NO-FLIP pool + dual/go-exclusive sets).
    2. Spot-check the snapshot: `node -e "const s=require('./src/lib/models/catalog.json');console.log('nous:',s.providers.nousresearch.length,'go:',s.providers['opencode-go'].length,'zen:',s.providers.opencode.length,'generatedAt:',s.generatedAt)"`.
    3. Confirm the canaries still catch drift: the numbers are hardcoded constants (not derived from the snapshot inside the test) — verify by reading the test file.
    4. Approve, or describe any number that looks wrong (e.g. npm split that contradicts the models.dev npm metadata).
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe the specific numbers/issues to fix (a follow-up commit adjusts them).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-machine → external rosters | Anonymous public GETs during the refresh run (same boundary as Plan 02) |
| committed catalog.json → registry/test consumers | The regenerated snapshot is now the data contract every runtime consumer reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-08 | Integrity | canary re-lock (COUNT-STABILITY/NO-FLIP) | mitigate | Numbers are re-locked to the ACTUAL regenerated snapshot values computed at execution (never auto-derived inside the test, never research estimates — D-24-11); the human checkpoint (Task 3) is the deliberate, reviewed re-lock the doctrine mandates; structure of the assertions (zero-leak, slash-free, per-id keep-row loops) unchanged so the canary keeps catching unintended shape changes |
| T-24-09 | Tampering | regenerated catalog.json (silent row loss) | mitigate | Structural gate before commit: 292 nous / 25 go / 60 zen / models key removed / generatedAt top-level / sorted keys + hermes pin spot-check (0.05/0.2, 131072, structuredOutputs false, family, api.url/npm); COUNT-STABILITY/NO-FLIP/union/openrouter canaries all green on the committed data |
| T-24-10 | Tampering | boundary canary flip | mitigate | The flip is to the LOCKED hermes pins (`nousresearch/hermes-4-70b` + `-405b`, D-23-05) — never `~latest` (D-07 "never `~`/`:free`/auto in pins"); the D-24-12 group (Plan 04) adds the ~latest self-exclusion proof |

No package installs — package-legitimacy gate (T-24-SC) not triggered. No secrets, no env additions.
</threat_model>

<verification>
- `npm run models:fetch` exits 0 (post-CLI-upgrade); structural assertions pass (292/25/60, models removed, generatedAt top-level, sorted keys, hermes spot-check).
- `npx vitest run src/lib/models/catalog.test.ts` + `npx tsc --noEmit` + `npm test` — green after the re-lock.
- One commit contains the regenerated `catalog.json` AND the re-locked `catalog.test.ts` (D-24-11) — verified via `git show HEAD --stat`.
- Re-locked numbers = actual computed values (cross-checked against the Task 1 computation output).
- If the refresh aborted on Go drift: snapshot unchanged, `git status` clean, escalation recorded — success criteria for THIS plan are then NOT met and the phase blocks (throws-not-degrades, D-24-07 never relaxed).
</verification>

<success_criteria>
- The committed snapshot ships 292 nousresearch rows + a 25-row opencode-go roster in the grouped shape with correct hermes pricing/context/structuredOutputs (CAT-02/03).
- COUNT-STABILITY and NO-FLIP are re-locked to explicit actual numbers, and the `nousresearch = []` boundary canary now asserts the live hermes pins — all in the SAME commit as the regenerated snapshot (D-24-11) and human-reviewed (D-24-11 reviewed re-lock).
- Full suite green; the no-flip doctrine holds — the Zen-wins dedup (expressed once in `dedupeProviderRows`) deterministically produces the new dual/go-exclusive sets (CAT-04).
</success_criteria>

<output>
Create `.planning/phases/24-refresh-script-catalog-data/24-03-SUMMARY.md` when done
</output>
