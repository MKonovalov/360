---
phase: 24-refresh-script-catalog-data
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [scripts/refresh-model-catalog.ts]
autonomous: false
requirements: [CAT-01, CAT-02, CAT-03, CAT-04]
must_haves:
  truths:
    - "The refresh script fetches the anonymous Nous roster (HTTP 200, 292 rows) mapping rows to providerID 'nousresearch' with api.url https://inference-api.nousresearch.com/v1 and api.npm @ai-sdk/openai-compatible (D-24-01: ALL 292 live rows ship — the full catalog is the menu, the Hermes allowlist is the gate; D-24-02: api mapping)"
    - "Any live-roster fetch failure or Zen/Go id-set drift aborts the run WITHOUT writing (throws-not-degrades)"
    - "The script writes the grouped { generatedAt, providers } snapshot shape"
    - "Nous rows ship verbatim with NO alias flag field — ~latest-ness stays derivable from the id string (D-24-08, D-24-09)"
  artifacts:
    - path: "scripts/refresh-model-catalog.ts"
      provides: "fetchNousRoster, deriveNousFamily, verifyZenGoRosters + grouped write path"
      contains: "fetchNousRoster"
    - path: "scripts/refresh-model-catalog.ts"
      provides: "strict drift check with per-id reporting"
      contains: "Go roster drift"
    - path: "scripts/refresh-model-catalog.ts"
      provides: "abort-without-write doctrine"
      contains: "snapshot NOT regenerated"
  key_links:
    - from: "main()"
      to: "verifyZenGoRosters(parsed)"
      via: "await before writeFileSync"
      pattern: "await verifyZenGoRosters\\(parsed\\)"
    - from: "fetchNousRoster rows"
      to: "trimRecord"
      via: "nous pre-map (perMTok ×1e6, deriveNousFamily, structured_outputs live join)"
      pattern: "structured_outputs"
---

<objective>
Extend the dev-time refresh script (`scripts/refresh-model-catalog.ts`) with the three new functions and the grouped write path: `fetchNousRoster()` (CAT-01), `deriveNousFamily()` (CAT-03), `verifyZenGoRosters()` strict drift check (CAT-04, D-24-06/07), the Nous pre-map with per-token → per-MTok pricing conversion and `supported_parameters` → `structuredOutputs` live join (CAT-02), and the grouped `{ generatedAt, providers }` snapshot write (D-24-03). Then run the pre-flight `opencode` CLI upgrade with a HUMAN CHECKPOINT — the strict Go drift check cannot pass with the current CLI (v1.18.11: 18 go rows vs 25 live) and must NOT be silently relaxed (research Landmine 1 / Open Question 1).

Purpose: This is the generator half of the phase. The smoke test before the CLI upgrade proves throws-not-degrades end-to-end (abort + no write + per-id drift report) against the CURRENT CLI; the checkpoint upgrades the machine state so Plan 03's regeneration can pass.

Output: Extended script (verified by a pre-upgrade abort smoke), CLI upgraded to a Go roster that matches live (25 rows).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-refresh-script-catalog-data/24-CONTEXT.md
@.planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md
@.planning/phases/24-refresh-script-catalog-data/24-PATTERNS.md
@.planning/phases/24-refresh-script-catalog-data/24-VALIDATION.md
@.planning/phases/24-refresh-script-catalog-data/24-01-SUMMARY.md

# The file being modified — read in full before touching
@scripts/refresh-model-catalog.ts

# Prior plan produced the grouped snapshot shape this script must now write
@src/lib/models/catalog.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fetchNousRoster, deriveNousFamily, perMTok, verifyZenGoRosters + grouped write path</name>
  <files>scripts/refresh-model-catalog.ts</files>
  <read_first>
    - scripts/refresh-model-catalog.ts (in full — the throws-not-degrades template `fetchOpenRouterStructuredOutputs` l.121-146, `trimRecord` l.86-114, `main()` l.157-193)
    - .planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md §Implementation Approach (target code for all three functions + the Nous pre-map) + §Code Examples l.415-446
    - .planning/phases/24-refresh-script-catalog-data/24-PATTERNS.md §refresh-model-catalog.ts (the exact conventions to copy)
  </read_first>
  <action>
    Extend `scripts/refresh-model-catalog.ts` with the following, mirroring the existing `fetchOpenRouterStructuredOutputs` conventions (try/catch → throw with the `— snapshot NOT regenerated` suffix; non-OK → throw with `(HTTP ${res.status})`; defensive shape casts with `?? []` + `typeof` guards). KEEP `resolveOpencodeBin`, `braceDelta`, `parseModels`, `trimRecord`, `fetchOpenRouterStructuredOutputs`, `familyFallbackStructuredOutputs` AS-IS (D-24-10: no refactors, no per-source subcommands).

    1. `fetchNousRoster(): Promise<Array<{ id: string; name?: string; context_length?: number; pricing?: { prompt?: string; completion?: string }; top_provider?: { max_completion_tokens?: number | null }; supported_parameters?: string[] }>>` — anonymous `GET https://inference-api.nousresearch.com/v1/models`; fetch error → throw `Failed to fetch NousResearch model roster from ${url} — snapshot NOT regenerated`; non-OK → throw with `(HTTP ${res.status})`; parse body as `{ data?: Array<Record<string, unknown>> }`; return `(body.data ?? []).filter((r) => typeof r.id === 'string')`.
    2. `deriveNousFamily(id: string): string` (CAT-03) — `const stripped = id.startsWith('~') ? id.slice(1) : id; const modelPart = stripped.split('/')[1] ?? ''; return modelPart.split('-')[0] || '';` (hermes-4-70b → hermes; qwen3.8-max → qwen3.8; ~anthropic/claude-sonnet-latest → claude).
    3. `perMTok(perToken: string | undefined): number` (CAT-02 / Pitfall 2) — `return Math.round(parseFloat(perToken ?? '0') * 1e6 * 1e6) / 1e6;` (Nous `pricing.prompt/completion` are STRING-typed per-token dollars; "0.0000016000" → 1.6; 6-dp rounding kills float noise).
    4. A Nous pre-map that shapes each raw roster row through `trimRecord`'s field contract (via `trimRecord(m, structuredOutputs)` — it already defaults missing fields):
       - `providerID: 'nousresearch'`, `api: { npm: '@ai-sdk/openai-compatible', url: 'https://inference-api.nousresearch.com/v1' }` (D-24-02/CAT-01), `status: 'active'` (roster has no status field; never 'deprecated'), `family: deriveNousFamily(r.id)`, ids VERBATIM incl. `~latest` (D-24-08, never stripped — D-04).
       - `cost: { input: perMTok(r.pricing?.prompt), output: perMTok(r.pricing?.completion) }`.
       - `limit: { context: r.context_length ?? 0, output: r.top_provider?.max_completion_tokens ?? 0 }`.
       - `structuredOutputs: (r.supported_parameters ?? []).includes('structured_outputs')` — the live `supported_parameters` join mirroring the OpenRouter join (research Don't-Hand-Roll: never family-name heuristics; hermes pair lands `false` — Pitfall 5).
    5. `verifyZenGoRosters(parsed: Record<string, unknown>[]): Promise<void>` (CAT-04, D-24-06/07) — inner `compare(url, cliIds, label)` helper: fetch → throw on error/non-OK with the same message convention; `const liveIds = (body.data ?? []).map((r) => r.id).filter((x): x is string => typeof x === 'string');` Set-diff against `cliIds`; if `missing.length > 0 || extra.length > 0` throw `` `${label} roster drift — snapshot NOT regenerated. Live-only ids (${missing.length}): ${missing.join(', ')}. CLI-only ids (${extra.length}): ${extra.join(', ')}. Update the opencode CLI (opencode upgrade) and re-run.` ``. Wire: Zen = `compare('https://opencode.ai/zen/v1/models', parsed.filter((m) => m.providerID === 'opencode').map((m) => m.id as string), 'Zen')`; Go = `compare('https://opencode.ai/zen/go/v1/models', parsed.filter((m) => m.providerID === 'opencode-go').map((m) => m.id as string), 'Go')`. STRICT semantics: ANY difference throws (no count tolerance, no warn-on-extra — D-24-07).
    6. `main()` wiring (l.157-193): keep the CLI parse + OpenRouter join + existing openrouter mapping as-is; then `const nousRows = (await fetchNousRoster()).map(nousPreMap); const allModels = [...models, ...nousRows];` then `await verifyZenGoRosters(parsed)` BEFORE the write (throw → no write, D-24-10); then the grouped snapshot:
       `const snapshot = { generatedAt: new Date().toISOString(), providers: Object.fromEntries([...new Set(allModels.map((m) => m.providerID))].sort().map((p) => [p, allModels.filter((m) => m.providerID === p)])) };`
       (`generatedAt` stays top-level — settings/page.tsx l.129; grouping key = the row's own providerID — D-24-05, `opencode`/`opencode-go` separate; sorted keys for diff stability — research A4.) Update the console.log to report `allModels.length`.
    7. NO dedup logic in the script (D-23-08) — Zen-wins dedup stays expressed once in `dedupeProviderRows` in catalog.ts; the script is format-only.

    Then run the pre-upgrade SMOKE (proves the new code paths + throws-not-degrades end-to-end): `npm run models:fetch; echo "EXIT=$?"`. With the current CLI (v1.18.11, 18 go rows) this MUST abort at the Go drift check after successfully fetching the Nous roster — exit 1, no write. If (unexpectedly) the run SUCCEEDS and writes (CLI already at 25 go rows), immediately restore with `git checkout -- src/lib/models/catalog.json` and note the CLI is already current for Task 2.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>bash -c "npm run models:fetch 2&gt;&amp;1; echo EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` — 0 errors (tsconfig includes `**/*.ts`, so the script is type-checked).
    - `npm run models:fetch` → `EXIT=1`; stderr contains `snapshot NOT regenerated` AND `Go roster drift` with `Live-only ids (7): minimax-m2.5, kimi-k2.5, glm-5, qwen3.5-plus, mimo-v2-pro, mimo-v2-omni, hy3-preview` (research-verified list — if the CLI/registry shifted, at minimum assert `Go roster drift` + a non-empty live-only list). The `git status --short src/lib/models/catalog.json` is EMPTY (no write happened — throws-not-degrades, the committed snapshot stays usable).
    - `grep -n "fetchNousRoster\|deriveNousFamily\|verifyZenGoRosters\|perMTok" scripts/refresh-model-catalog.ts` shows all four defined and called.
    - The Nous fetch succeeded before the abort (the abort is the Go drift, not a Nous fetch failure) — confirm by the absence of `Failed to fetch NousResearch` in stderr.
  </acceptance_criteria>
  <done>The script fetches the Nous roster, converts pricing ×1e6, derives family, live-joins structuredOutputs, strict-checks Zen/Go with per-id diffs, writes the grouped shape — and provably aborts without writing on drift (smoke-verified against the current CLI).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Pre-flight opencode CLI upgrade + go-roster re-verify (research Landmine 1)</name>
  <files>none (external tool state on the dev machine)</files>
  <read_first>
    - .planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md §Landmine 1 + §CLI flags verified + §Open Questions 1/3
  </read_first>
  <action>
    Claude automates: run `opencode upgrade` and `opencode models --refresh` via the shell (the human approves the machine-state change when prompted), then count the go roster with `opencode models --verbose | grep -c '"providerID": "opencode-go"'`. Do NOT relax D-24-07 if the count is not 25 — capture the per-id drift list by running `npm run models:fetch` once and report it for escalation. Present the count result to the human for verification.
  </action>
  <what-built>
    Claude has automated the CLI upgrade attempt and will verify the go-roster count. The opencode CLI (v1.18.11) parses only 18 opencode-go rows from models.dev (which itself lags live by hy3-preview); the strict D-24-07 drift check therefore aborts the refresh today. Upgrading the CLI (and refreshing its models.dev cache) is the pre-flight required for Plan 03's regeneration to pass. If the upgrade does NOT close the gap, the strict check is NOT relaxed — the refresh stays blocked (throws-not-degrades) and the phase escalates.
  </what-built>
  <how-to-verify>
    1. Claude runs `opencode upgrade` (approve this machine-level tool state change) and then `opencode models --refresh`.
    2. Re-count the go roster: run `opencode models --verbose | grep -c '"providerID": "opencode-go"'` — EXPECT `25`.
    3. If the count is 25: approve — Plan 03's `npm run models:fetch` will pass the Go drift check.
    4. If the count is NOT 25: do NOT relax D-24-07. Run `npm run models:fetch` once to capture the exact per-id drift list (`Go roster drift — Live-only ids (N): ...`), record it in the summary, and escalate to the user (options: newer release/registry update later, or a deliberate strictness revisit — never a silent relax).
    5. Also verify `opencode models --verbose | grep -c '"providerID": "opencode"'` still returns 60 (Zen unchanged — the CLI's Zen block already matches live exactly).
  </how-to-verify>
  <resume-signal>Type "approved — Go roster = 25" to proceed, or describe what you observe (count, error, or drift list) for escalation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-machine → external public rosters (Nous / OpenRouter / Zen / Go) | Anonymous public GETs over HTTPS; responses are untrusted input to the parser, never evaluated as code |
| dev-machine → opencode CLI upgrade | External tool state change on the developer machine (human-approved via checkpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | fetchNousRoster / verifyZenGoRosters (roster body shape injection) | mitigate | Defensive shape casts (`?? []`, `typeof id === 'string'`, typed `data` access) mirror `fetchOpenRouterStructuredOutputs`; malformed JSON → throw → abort before write (throws-not-degrades, research security domain "Roster body shape injection"); the drift check bounds the id-set so a poisoned roster cannot silently change servability |
| T-24-05 | Information Disclosure | refresh-model-catalog.ts | mitigate | NO API keys anywhere — all four roster GETs are anonymous (live-verified); script stays in `scripts/` (never deployed; Phase 18 gate greps zero `exec\|spawn\|child_process` in src/ — this plan adds NO such calls to src/); no env var additions this phase (keys are Phase 23/25 concerns) |
| T-24-06 | Integrity | main() write path | mitigate | Single atomic `writeFileSync` AFTER all live fetches + the strict drift check succeed; any throw exits 1 via the existing `main().catch` (l.195-199) before `writeFileSync` — the committed snapshot stays usable (Pitfall 3 / T-19-06) |
| T-24-07 | Tampering | Zen/Go roster drift (CLI vs live) | mitigate | Strict D-24-07 Set-diff with per-id `Live-only ids`/`CLI-only ids` reporting aborts the run; the CLI-upgrade pre-flight (Task 2) closes the known v1.18.11 gap; the drift check is NEVER silently relaxed (research Landmine 1 — escalates to the user instead) |

No package installs in this plan (global `fetch` + existing `tsx` — research A5) — the package-legitimacy gate (T-24-SC) is not triggered.
</threat_model>

<verification>
- `npx tsc --noEmit` — 0 errors (script included in tsconfig).
- Pre-upgrade smoke: `npm run models:fetch` exits 1 with `Go roster drift` + per-id diffs + `snapshot NOT regenerated`; `src/lib/models/catalog.json` NOT modified.
- Post-checkpoint: `opencode models --verbose | grep -c '"providerID": "opencode-go"'` = 25 (and `opencode` block = 60).
- Script untouched by this plan: `parseModels`, `trimRecord`, `fetchOpenRouterStructuredOutputs`, `familyFallbackStructuredOutputs` byte-identical (D-24-10).
</verification>

<success_criteria>
- `scripts/refresh-model-catalog.ts` contains `fetchNousRoster` (CAT-01), `perMTok` ×1e6 + `structured_outputs` live join (CAT-02), `deriveNousFamily` (CAT-03), `verifyZenGoRosters` strict drift (CAT-04/D-24-06/07), and writes the grouped `{ generatedAt, providers }` shape (D-24-03).
- Throws-not-degrades proven by smoke: abort + no write on Go drift (current CLI).
- The opencode CLI is upgraded and its go roster re-verified at 25 rows via the human checkpoint — OR the drift is documented and the phase escalates (strictness never silently relaxed).
</success_criteria>

<output>
Create `.planning/phases/24-refresh-script-catalog-data/24-02-SUMMARY.md` when done
</output>
