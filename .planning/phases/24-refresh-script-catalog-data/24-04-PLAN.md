---
phase: 24-refresh-script-catalog-data
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified: [src/lib/models/catalog.test.ts]
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
must_haves:
  truths:
    - "A full NousResearch canary group locks: ~292 rows present, hermes pins servable through the gate, pricing ×1e6 correct, family derived, ~latest aliases present and self-excluded"
  artifacts:
    - path: "src/lib/models/catalog.test.ts"
      provides: "NOUSRESEARCH (D-24-12) describe block with snapshot + fixture canaries"
      contains: "NOUSRESEARCH (D-24-12)"
    - path: "src/lib/models/catalog.test.ts"
      provides: "pricing ×1e6 pin assertions"
      contains: "0.05"
  key_links:
    - from: "NOUSRESEARCH (D-24-12) canary group"
      to: "catalogJson.providers.nousresearch"
      via: "committed-snapshot reads"
      pattern: "catalogJson\\.providers\\.nousresearch"
    - from: "NOUSRESEARCH (D-24-12) canary group"
      to: "getServableIdsForProvider(catalogJson, 'nousresearch')"
      via: "gate servability"
      pattern: "getServableIdsForProvider\\(catalogJson, 'nousresearch'\\)"
---

<objective>
Ship the full NousResearch canary group (D-24-12) in `catalog.test.ts`, mirroring the existing dual-canary convention (fixture-level semantics + committed-snapshot canaries): all nousresearch rows present with the mandated api mapping, hermes pins servable through the gate, per-token → per-MTok pricing conversion correct (hermes 0.05/0.2 and 0.09/0.37), `family` derived from the id prefix, and the `~latest` aliases present + self-excluded from servable.

Purpose: This locks the new data shape the phase shipped — the canary group is what makes the 292-row roster + live-verified hermes values + family derivation + `~latest` behavior a reviewed, permanent contract instead of a silent assumption (D-24-12, research Pitfall 2/4/5).

Output: Green `NOUSRESEARCH (D-24-12)` canary group added to the suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-refresh-script-catalog-data/24-CONTEXT.md
@.planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md
@.planning/phases/24-refresh-script-catalog-data/24-PATTERNS.md
@.planning/phases/24-refresh-script-catalog-data/24-VALIDATION.md
@.planning/phases/24-refresh-script-catalog-data/24-03-SUMMARY.md

# The file being modified — read in full before touching
@src/lib/models/catalog.test.ts

# The regenerated snapshot the committed-snapshot canaries assert against
@src/lib/models/catalog.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the NOUSRESEARCH (D-24-12) canary group — snapshot + fixture dual canaries</name>
  <files>src/lib/models/catalog.test.ts</files>
  <read_first>
    - src/lib/models/catalog.test.ts (the dual-canary convention — inline fixture l.32-203 + committed-snapshot canaries; the COUNT-STABILITY l.407-435 and NO-FLIP l.437-471 describe blocks as the placement/format template; the imports at l.1-19)
    - .planning/phases/24-refresh-script-catalog-data/24-CONTEXT.md D-24-12 (the full canary group mandate) + D-24-08/09 (~latest verbatim, no alias flag field)
    - .planning/phases/24-refresh-script-catalog-data/24-RESEARCH.md §API Shapes (live-verified values: hermes pair 0.05/0.2 & 0.09/0.37, context_length 131072, NO structured_outputs advertised, 214/292 advertise, 11 ~latest) + §Pitfalls 2/4/5
    - src/lib/models/catalog.json (the actual regenerated snapshot — re-lock counts from here, never research estimates)
  </read_first>
  <action>
    Add a `describe('NOUSRESEARCH (D-24-12): ...')` block in `src/lib/models/catalog.test.ts` at the same level as the `COUNT-STABILITY` / `NO-FLIP` describes, containing BOTH fixture canaries and committed-snapshot canaries (the dual-canary convention). Before writing, compute the ACTUAL counts from the regenerated snapshot (research Open Question 2 — re-lock, never auto-derive inside the test):

    - `nousCount` = `catalogJson.providers.nousresearch.length` (research verified 292 — confirm against the actual file at execution).
    - `latestCount` = number of nousresearch rows whose id starts with `~` (research verified 11 — confirm against the actual file).
    - The exact hermes pin rows: read the `nousresearch/hermes-4-70b` and `nousresearch/hermes-4-405b` rows from `catalogJson.providers.nousresearch` (their cost/limit/structuredOutputs/family/api are the assertion values).

    Assertions to include:
    1. Rows present + api mapping (CAT-01): `catalogJson.providers.nousresearch` has `nousCount` rows; every row's `providerID === 'nousresearch'`; the hermes-4-70b row's `api.url === 'https://inference-api.nousresearch.com/v1'` and `api.npm === '@ai-sdk/openai-compatible'`.
    2. Hermes pins servable through the gate (D-23-05/D-24-12): `getServableIdsForProvider(catalogJson, 'nousresearch')` equals `['nousresearch/hermes-4-70b', 'nousresearch/hermes-4-405b']` — the same pins the flipped boundary canary asserts (Plan 03); the dual-canary redundancy is intentional.
    3. Pricing ×1e6 conversion (CAT-02/Pitfall 2): hermes-4-70b row `cost` equals `{ input: 0.05, output: 0.2 }`; hermes-4-405b row `cost` equals `{ input: 0.09, output: 0.37 }`; both rows `limit.context === 131072`.
    4. structuredOutputs live join (CAT-02/Pitfall 5): both hermes pin rows have `structuredOutputs === false` (hermes advertises `response_format`, NOT `structured_outputs`); the group is non-vacuous — `catalogJson.providers.nousresearch.some((m) => m.structuredOutputs === true)` (214/292 rows) and `.some((m) => m.structuredOutputs === false)` both true.
    5. family derived from id prefix (CAT-03): both pin rows `family === 'hermes'`; add one counter-example assertion proving the derivation generalizes, e.g. the `qwen/qwen3.8-max` row's `family === 'qwen3.8'` (first dash-token of the model part) — pick the exact id from the actual snapshot.
    6. `~latest` present + self-excluded (D-24-08/12): exactly `latestCount` rows in the nousresearch group match `/^~/`; the intersection of those ids with `getServableIdsForProvider(catalogJson, 'nousresearch')` is EMPTY (the allowlist pins concrete ids — D-23-05, D-07 "never `~`/`:free`/auto in pins").
    7. Fixture-level canaries (dual-canary convention — the fixture was re-valued in Plan 01): the fixture's hermes-4-70b row has `cost { input: 0.05, output: 0.2 }`, `structuredOutputs === false`, `family === 'hermes'`; and `getServableIdsForProvider(fixture, 'nousresearch')` equals the two pins (redundant with the existing REG-04 test l.250-255 — the D-24-12 group states the pricing/family/structuredOutputs facts the REG-04 test does not).

    Do NOT modify any other describe block in this plan (Plan 03's re-lock already landed). All counts are hardcoded explicit numbers with a comment noting the re-lock date and the actual snapshot — never derived from the snapshot inside the test (D-24-11 doctrine).
  </action>
  <verify>
    <automated>npx vitest run src/lib/models/catalog.test.ts -t "NOUSRESEARCH"</automated>
    <automated>npx vitest run src/lib/models/catalog.test.ts && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/models/catalog.test.ts -t "NOUSRESEARCH"` — all new `it()` blocks pass (rows count + api mapping, hermes pins servable, pricing 0.05/0.2 & 0.09/0.37 + context 131072, structuredOutputs false both + non-vacuous some-true/some-false, family hermes + counter-example, ~latest count + empty servable intersection, fixture re-value facts).
    - `npx vitest run src/lib/models/catalog.test.ts` — full file green (COUNT-STABILITY/NO-FLIP re-locked numbers from Plan 03 still pass).
    - `npx tsc --noEmit` — 0 errors; `npm test` — full suite green.
    - `grep -n "NOUSRESEARCH (D-24-12)" src/lib/models/catalog.test.ts` shows the describe block.
    - No changes to files other than `src/lib/models/catalog.test.ts`.
  </acceptance_criteria>
  <done>The full Nous canary group is committed and green: 292 rows, hermes pins servable, ×1e6 pricing, derived family, ~latest self-exclusion — all locked as explicit re-locked constants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| catalog.test.ts assertions → committed snapshot data | The canary group reads `catalogJson.providers.nousresearch` and the servable gate output; the snapshot is committed, reviewed data (no runtime boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11 | Integrity | NOUSRESEARCH canary group (non-vacuousness) | mitigate | Every assertion is concrete: exact row count, exact hermes cost/context/structuredOutputs values, exact family strings, exact ~latest count, and a non-vacuous structuredOutputs some-true/some-false check — the group cannot pass on an empty or stale nousresearch group (research Pitfall 1 canary-regression-trap doctrine) |
| T-24-12 | Tampering | hardcoded canary constants (auto-derive trap) | mitigate | All counts/values are EXPLICIT re-locked constants computed from the actual regenerated snapshot at execution (D-24-11/12) with re-lock-date comments — never derived from the snapshot inside the test (that would defeat the canary's purpose); the ~latest self-exclusion is proven by an explicit empty-intersection assertion (D-24-08) |
| T-24-13 | Information Disclosure | ~latest alias handling | accept | Alias rows are already in the committed public snapshot (D-24-08, verbatim); no alias flag field is added (D-24-09 — derivable from the id string); no client exposure beyond the existing snapshot consumers |

No package installs — package-legitimacy gate (T-24-SC) not triggered. No secrets, no network calls.
</threat_model>

<verification>
- `npx vitest run src/lib/models/catalog.test.ts -t "NOUSRESEARCH"` — green (the D-24-12 group).
- `npx vitest run src/lib/models/catalog.test.ts` + `npx tsc --noEmit` + `npm test` — full green.
- Phase gate (final): `npm test` green AND `npm run models:fetch` has run cleanly (Plan 03) — per 24-VALIDATION.md §Sampling Rate.
- Counts re-locked from the ACTUAL regenerated snapshot (292 / 11 / hermes values from the committed file), not research estimates.
</verification>

<success_criteria>
- The `NOUSRESEARCH (D-24-12)` describe block exists in `catalog.test.ts` with both fixture and committed-snapshot canaries covering: 292 rows + api mapping (CAT-01), pricing ×1e6 + structuredOutputs live join incl. hermes `false` (CAT-02), family derivation with counter-example (CAT-03), and ~latest present + self-excluded (D-24-08/12).
- All assertions pass against the committed regenerated snapshot; the full Vitest suite + tsc are green.
- Phase 24's success criteria are collectively met by Plans 01-04: grouped snapshot regenerated by the extended script with Nous rows + refreshed Go roster, canaries re-locked and the full Nous group locked.
</success_criteria>

<output>
Create `.planning/phases/24-refresh-script-catalog-data/24-04-SUMMARY.md` when done
</output>
