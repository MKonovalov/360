---
phase: 36-agent-management-end-to-end-verification
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - src/app/actions/analysisTemplates.ts
  - src/app/actions/analysisTemplates.test.ts
  - src/lib/db/queries/analysisTemplates.ts
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "Every management mutation independently verifies staff access and derives actor identity on the server."
    - "A content save changes the future current version immediately and preserves all existing run snapshots."
    - "Activate, retire, and reactivate are template-level operations and do not create content versions."
  artifacts:
    - path: "src/app/actions/analysisTemplates.ts"
      provides: "Staff-gated save and lifecycle Server Actions"
      exports: ["saveAnalysisTemplateAction", "setAnalysisTemplateStatusAction"]
    - path: "src/app/actions/analysisTemplates.test.ts"
      provides: "Action validation/auth/revalidation regression coverage"
  key_links:
    - from: "src/app/actions/analysisTemplates.ts"
      to: "requireStaffAccess"
      via: "gate before parsing or mutation"
      pattern: "requireStaffAccess"
    - from: "src/app/actions/analysisTemplates.ts"
      to: "src/lib/db/queries/analysisTemplates.ts"
      via: "server-owned actor and fixed key passed to query layer"
      pattern: "saveAnalysisTemplateVersion|setAnalysisTemplateStatus"
---

<objective>Expose the safe Server Action boundary for fixed-template content and lifecycle management.</objective>
<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/36-agent-management-end-to-end-verification/36-CONTEXT.md
@.planning/phases/36-agent-management-end-to-end-verification/36-RESEARCH.md
@src/app/actions/settings.ts
@src/app/actions/reviews.ts
@src/lib/auth/requireStaffAccess.ts
@src/lib/analysis/templateContracts.ts

Scope fence: actions must not accept userId/actorId, template name/target/budget, version ID, run ID, provider/model controls, or any direct Signal/Offering/review write. The public route remains `/agents`; never introduce a nested agent-management route.
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement gate-first content and lifecycle actions</name>
  <files>src/app/actions/analysisTemplates.ts, src/app/actions/analysisTemplates.test.ts</files>
  <behavior>
    - Unauthenticated or non-staff callers are rejected before any DB mutation.
    - Valid content save passes only fixed key, instruction, default effort, and server actor to the query; successful changed save returns the new version.
    - Lifecycle action supports active/retired/reactivated state without version creation and returns a safe conflict/not-found outcome.
  </behavior>
  <action>Follow settings/reviews action conventions: call requireStaffAccess() first, parse unknown input with the Phase 36 contract, derive actor from the returned Clerk userId, call the query functions from Plan 01, revalidate `/agents` and only the target/review pages whose cached reads are actually affected. Return a discriminated safe result that never exposes raw DB/provider errors. Add tests with mocked gate/query boundaries for auth ordering, fixed-field rejection, changed/no-op save, lifecycle-only behavior, reactivation, and safe conflict handling. Cite D-36-02, D-36-03, D-36-05, and D-36-06.</action>
  <verify><automated>npm test -- src/app/actions/analysisTemplates.test.ts</automated></verify>
  <done>Actions are independently staff-gated, server-actor-owned, closed-input validated, safe on errors, and connected to atomic query operations.</done>
</task>

<task type="auto">
  <name>Task 2: Lock action-to-query immutability and revalidation behavior</name>
  <files>src/app/actions/analysisTemplates.ts, src/app/actions/analysisTemplates.test.ts, src/lib/db/queries/analysisTemplates.ts</files>
  <action>Use integration-compatible return shapes and revalidation paths that allow the management page to refresh latest/history/status after each operation. Add regression assertions that action execution does not call any run, packet, finding, source, review, Signal, Offering, or signal_offering_link mutation seam, and that a concurrent version conflict is surfaced as reloadable rather than retried by mutating an older version. Keep lifecycle changes version-neutral per D-36-06.</action>
  <verify><automated>npm test -- src/app/actions/analysisTemplates.test.ts src/lib/db/queries/analysisTemplates.test.ts</automated></verify>
  <done>The action/query boundary has explicit no-write and version-neutral lifecycle regression coverage.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| client form → Server Action | Client may submit hostile fields or forged actor/template identity. |
| Server Action → DB query | Authorized mutation must preserve immutable and no-live-write boundaries. |
| action error → UI | DB/provider details must not leak to browser. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-04 | Spoofing | action actor | mitigate | Derive actor only from requireStaffAccess(); ignore client actor fields. |
| T-36-05 | Tampering | action inputs | mitigate | Parse closed contracts and reject immutable/template identity fields. |
| T-36-06 | Information disclosure | action errors | mitigate | Return safe discriminated outcomes; do not serialize raw DB/provider errors. |
| T-36-SC | Tampering | package installs | accept | No package installation. |
</threat_model>
<verification>Run action/query focused tests and inspect that no action imports legacy proposal or direct live-data mutation paths.</verification>
<success_criteria>Server Actions provide the only mutation entry for `/agents`, with strict authorization and no historical-run or live-signal side effects.</success_criteria>
<output>Create `.planning/phases/36-agent-management-end-to-end-verification/36-02-SUMMARY.md` when done.</output>
