---
phase: 36-agent-management-end-to-end-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analysis/templateContracts.ts
  - src/lib/analysis/templateContracts.test.ts
  - src/lib/db/queries/analysisTemplates.ts
  - src/lib/db/queries/analysisTemplates.test.ts
  - src/lib/db/queries/analysisTemplates.integration.test.ts
autonomous: true
requirements: [UX-03, VER-01]
must_haves:
  truths:
    - "Management reads return exactly the two fixed templates with one latest version and an ordered read-only history."
    - "Only instruction and defaultEffort are editable; names, target types, supported efforts, and budget are rejected as tampering."
    - "Content edits append the next immutable version, while no-op and lifecycle-only changes do not append a version."
  artifacts:
    - path: "src/lib/analysis/templateContracts.ts"
      provides: "Closed fixed-template keys, editable input, lifecycle result, and safe management read contracts"
      exports: ["FIXED_ANALYSIS_TEMPLATES", "TemplateManagementInput", "TemplateManagementResult"]
    - path: "src/lib/db/queries/analysisTemplates.ts"
      provides: "Latest/history reads and Neon-safe append/lifecycle mutations"
      exports: ["listManagedAnalysisTemplates", "saveAnalysisTemplateVersion", "setAnalysisTemplateStatus"]
  key_links:
    - from: "src/lib/db/queries/analysisTemplates.ts"
      to: "src/lib/db/schema.ts"
      via: "analysisTemplate and analysisTemplateVersion queries"
      pattern: "analysisTemplateVersion"
    - from: "saveAnalysisTemplateVersion"
      to: "analysis_run snapshots"
      via: "append-only version insert with no run update"
      pattern: "insert.*analysisTemplateVersion"
---

<objective>
Establish the fixed-template management contracts and database query boundary that every later action, UI, and verification plan consumes.

Purpose: Make UX-03 deterministic without redesigning the already-shipped schema or weakening immutable run snapshots.
Output: Strict contracts, latest/history projections, atomic content-version append, and template-level lifecycle mutation with focused tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-agent-management-end-to-end-verification/36-CONTEXT.md
@.planning/phases/36-agent-management-end-to-end-verification/36-RESEARCH.md
@.planning/REQUIREMENTS.md
@src/lib/db/schema.ts
@src/lib/db/queries/analysisTemplates.ts
@src/scripts/seedAnalysisTemplates.ts

Scope fence: do not add a template, target type, effort, budget, current-version pointer, migration, dynamic schema, or update/delete path for analysis_template_version. Do not mutate analysis_run, packet, finding, source, review, Signal, Offering, or signal_offering_link rows.
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define fixed-template management contracts and invariant tests</name>
  <files>src/lib/analysis/templateContracts.ts, src/lib/analysis/templateContracts.test.ts</files>
  <behavior>
    - Fixed keys resolve only to Company Buying Signal Analysis/company and Persona Buying Signal Analysis/persona per D-36-01.
    - Editable input accepts instruction plus supported default effort only; attempts to supply name, targetType, supportedEfforts, futureBudget, actor, or version are rejected.
    - Lifecycle status accepts only active or retired; content and lifecycle operations are distinguishable per D-36-06.
  </behavior>
  <action>Create named Zod/TypeScript contracts for the two fixed template keys, editable content, lifecycle commands, latest/history read rows, and safe action/query outcomes. Keep actor identity server-owned and keep seed-defined supported efforts/budget outside client input. Add pure tests for valid Company/Persona inputs, invalid fixed-field tampering, invalid effort, empty instruction, and closed lifecycle values. Cite D-36-01, D-36-02, and D-36-06 in test descriptions or comments where the invariant is encoded.</action>
  <verify><automated>npm test -- src/lib/analysis/templateContracts.test.ts</automated></verify>
  <done>Contracts compile under strict TypeScript and the pure suite proves the closed two-template/editable-field/lifecycle boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add latest/history and atomic template query operations</name>
  <files>src/lib/db/queries/analysisTemplates.ts, src/lib/db/queries/analysisTemplates.test.ts, src/lib/db/queries/analysisTemplates.integration.test.ts</files>
  <behavior>
    - Management listing returns exactly two template rows, latest version once per template, and descending or explicitly ordered immutable history.
    - Changed instruction/defaultEffort appends max(version)+1 atomically; concurrent losers return a safe reloadable conflict and never duplicate a version.
    - Retire/reactivate updates only template lifecycle/audit fields, permits retiring the only active target template, and preserves latest version/history.
  </behavior>
  <action>Extend analysisTemplates.ts with explicit latest-version and ordered-history projections plus Neon HTTP-safe mutation queries. Derive the next version server-side, use the existing unique template/version invariant, and make content save no-op when both editable values are unchanged. Implement lifecycle-only update without version insertion. Add pure query-shape tests and guarded TEST_DATABASE_URL integration tests for exact-two fixtures, concurrent save conflict, no-op/lifecycle version counts, fixed-field preservation, and the absence of writes outside template/template-version rows. Do not alter src/lib/db/schema.ts unless a test proves an existing constraint is absent; if so, stop and document the blocker instead of inventing a migration. Cite D-36-02 through D-36-06.</action>
  <verify><automated>npm test -- src/lib/db/queries/analysisTemplates.test.ts && TEST_DATABASE_URL="${TEST_DATABASE_URL:?TEST_DATABASE_URL required for integration evidence}" npm test -- src/lib/db/queries/analysisTemplates.integration.test.ts</automated></verify>
  <done>Query functions expose one current row plus immutable history, content edits append exactly one safe next version, lifecycle changes do not version, and integration assertions show existing data remains intact.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser → management action | Untrusted editable fields and attempted immutable-field tampering enter the server. |
| action → Neon | Server-derived actor and fixed-template identity cross into persistent writes. |
| template rows → future run launcher | Lifecycle/latest-version reads control future availability without rewriting snapshots. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-01 | Tampering | template management input | mitigate | Closed contract rejects fixed identity, budget, supported-effort, actor, and version fields. |
| T-36-02 | Elevation | template query mutation | mitigate | Query accepts server actor and fixed key only; later action applies requireStaffAccess(). |
| T-36-03 | Tampering | immutable version history | mitigate | Append-only insert plus unique `(templateId, version)` and no update/delete operation. |
| T-36-SC | Tampering | package installs | accept | No package installation is planned; existing dependency set is reused. |
</threat_model>

<verification>Run focused pure tests first; run the guarded integration suite only with TEST_DATABASE_URL and record missing-database evidence as blocked rather than passed.</verification>
<success_criteria>Fixed-template contracts and database operations prove the immutable version/lifecycle boundary without any application schema or historical-run mutation.</success_criteria>
<output>Create `.planning/phases/36-agent-management-end-to-end-verification/36-01-SUMMARY.md` when done.</output>
