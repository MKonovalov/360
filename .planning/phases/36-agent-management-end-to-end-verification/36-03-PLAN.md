---
phase: 36-agent-management-end-to-end-verification
plan: 03
type: execute
wave: 3
depends_on: [36-02]
files_modified:
  - src/app/(dashboard)/agents/page.tsx
  - src/components/agents/agent-management.tsx
  - src/components/agents/agent-template-card.tsx
  - src/components/agents/agent-template-card.test.tsx
autonomous: true
requirements: [UX-03]
must_haves:
  truths:
    - "Staff visiting `/agents` see exactly the Company and Persona fixed templates with target labels, current version, lifecycle status, and read-only history."
    - "Editing instruction/default effort and saving visibly creates the next version; prior versions are distinguishable and not editable/deletable."
    - "Retire/reactivate controls change future launch availability while preserving history and existing-run inspection."
  artifacts:
    - path: "src/app/(dashboard)/agents/page.tsx"
      provides: "Staff-gated `/agents` server page with safe management data fetch"
      exports: ["default"]
    - path: "src/components/agents/agent-management.tsx"
      provides: "Two-template management surface and action refresh wiring"
    - path: "src/components/agents/agent-template-card.tsx"
      provides: "Current-version editor, lifecycle controls, and read-only history"
  key_links:
    - from: "src/app/(dashboard)/agents/page.tsx"
      to: "listManagedAnalysisTemplates"
      via: "server-side query after requireStaffAccess"
      pattern: "listManagedAnalysisTemplates"
    - from: "src/components/agents/agent-template-card.tsx"
      to: "saveAnalysisTemplateAction"
      via: "form action for editable fields only"
      pattern: "saveAnalysisTemplateAction"
---

<objective>Build the dedicated staff management page at `/agents` for the two fixed templates.</objective>
<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/36-agent-management-end-to-end-verification/36-CONTEXT.md
@.planning/phases/36-agent-management-end-to-end-verification/36-RESEARCH.md
@src/app/(dashboard)/settings/page.tsx
@src/components/settings/model-settings-form.tsx
@src/components/reviews/run-review-card.tsx

Scope fence: this is a management surface, not a construction playground. Render exactly two fixed target-scoped templates; do not add template creation, schema editing, provider/model controls, run-flow picker, or legacy proposal Accept/Reject semantics. Historical versions are read-only per D-36-04.
</context>
<tasks>
<task type="auto">
  <name>Task 1: Add staff-gated `/agents` server page and two-template composition</name>
  <files>src/app/(dashboard)/agents/page.tsx, src/components/agents/agent-management.tsx</files>
  <action>Implement the page using the settings pattern: call requireStaffAccess() in the page itself, fetch managed templates with latest/history data, degrade to a safe error card on read failure, and pass only safe template fields to the client component. Render exactly two cards/rows keyed by the fixed Company and Persona template keys, with current version/status, editable instruction/default-effort controls, lifecycle action, and expanded/read-only history. Keep the route public URL `/agents` and cite D-36-01, D-36-04, D-36-07, and D-36-08.</action>
  <verify><automated>npm test -- src/components/agents/agent-template-card.test.tsx && npx tsc --noEmit</automated></verify>
  <done>Authenticated staff can load `/agents`; the page contains exactly two fixed template management entries and no construction playground behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement version editor, lifecycle controls, and history presentation</name>
  <files>src/components/agents/agent-template-card.tsx, src/components/agents/agent-template-card.test.tsx</files>
  <behavior>
    - Current instruction/default effort fields are editable; name, target, supported efforts, budget, and historical rows are visibly non-editable.
    - Successful content save reports the new version and retains the prior version in read-only history.
    - Retire disables future-launch state, and reactivate restores the same latest version without showing a new version.
  </behavior>
  <action>Compose the existing Button, Textarea, Select, Dialog/Sheet, and feedback primitives. Submit only the fixed key and editable fields to actions, show pending/error/success states, distinguish current from prior versions, and make lifecycle transitions explicit. Do not hide historical rows after save or delete them. Add component tests for exact two-card rendering, editable-field boundaries, version/history labels, lifecycle state, action result handling, and no template picker in the management UI. Cite D-36-02 through D-36-06 and D-36-08.</action>
  <verify><automated>npm test -- src/components/agents/agent-template-card.test.tsx</automated></verify>
  <done>The management surface visibly implements append-only content editing and template-level lifecycle without exposing forbidden fields or controls.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| server page → client component | Server-derived safe management data crosses into browser rendering. |
| client controls → action | User-controlled form values are submitted for server validation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-07 | Information disclosure | agents page props | mitigate | Pass only management fields; never raw provider output, secrets, private reasoning, or unrestricted Persona data. |
| T-36-08 | Tampering | history/editor UI | mitigate | Historical rows render read-only and action payload contains only editable fields; server contract remains authoritative. |
| T-36-SC | Tampering | package installs | accept | Existing UI primitives only; no package installation. |
</threat_model>
<verification>Run the component suite, typecheck, and manually inspect the generated DOM contract for exactly two cards and `/agents` route semantics.</verification>
<success_criteria>UX-03 management UI is complete at `/agents`, with immutable history visible and lifecycle/version behavior accurately represented.</success_criteria>
<output>Create `.planning/phases/36-agent-management-end-to-end-verification/36-03-SUMMARY.md` when done.</output>
