---
phase: 36-agent-management-end-to-end-verification
plan: 04
type: execute
wave: 3
depends_on: [36-02, 36-03]
files_modified:
  - src/lib/nav.ts
  - src/lib/nav.test.ts
  - src/components/layout/app-sidebar.tsx
  - e2e/36-agent-management.spec.ts
autonomous: true
requirements: [UX-03, VER-01]
must_haves:
  truths:
    - "Agents appears directly under Manage and links to `/agents`, not within Reviews."
    - "`/agents` gets the active Manage nav state without false matches for sibling paths."
    - "Expanded and collapsed sidebar behavior preserves the existing tooltip/icon contract for Agents."
  artifacts:
    - path: "src/lib/nav.ts"
      provides: "agents NavKey and exact/boundary-safe active route matcher"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Manage-group Agents link and active/collapsed rendering"
    - path: "src/lib/nav.test.ts"
      provides: "route matching regression matrix"
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "getActiveNavKey"
      via: "usePathname active key"
      pattern: "getActiveNavKey"
    - from: "AppSidebar Agents item"
      to: "/agents"
      via: "href"
      pattern: "href=\"/agents\""
---

<objective>Wire the canonical `/agents` route into the Manage navigation and lock active-route behavior.</objective>
<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/36-agent-management-end-to-end-verification/36-CONTEXT.md
@.planning/phases/36-agent-management-end-to-end-verification/36-RESEARCH.md
@src/lib/nav.ts
@src/components/layout/app-sidebar.tsx

Scope fence: use only `/agents`. Do not create or mention any nested Reviews agent route. Do not change existing Reviews, Signals, Offerings, Settings routes or vendored sidebar primitives.
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add agents active-route contract and navigation item</name>
  <files>src/lib/nav.ts, src/lib/nav.test.ts, src/components/layout/app-sidebar.tsx</files>
  <behavior>
    - getActiveNavKey('/agents') returns agents and getActiveNavKey('/agents-archive') returns null.
    - Existing route keys continue to pass their exact/boundary matrix.
    - AppSidebar renders Agents directly under the Manage label with href `/agents`, monochrome icon, active state, and collapsed tooltip label.
  </behavior>
  <action>Add the `agents` NavKey and exact leaf matcher, preserving existing prefix behavior for detail-capable routes. Insert Agents as the first item directly beneath Manage before Reviews, using existing SidebarMenuButton/tooltip/collapse patterns. Add route regression cases for `/agents`, `/agents-archive`, `/agents/anything` according to the chosen exact leaf policy, `/settings`, and unknown paths. Cite D-36-07.</action>
  <verify><automated>npm test -- src/lib/nav.test.ts</automated></verify>
  <done>Navigation links directly to `/agents`, active matching is boundary-safe, and existing sidebar behavior remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated browser route and sidebar assertions</name>
  <files>e2e/36-agent-management.spec.ts</files>
  <action>In the Phase 36 Playwright suite, add a staff-authenticated route smoke that opens `/agents`, asserts no redirect to sign-in, verifies the Agents link href/active state, and checks expanded/collapsed sidebar visibility or tooltip semantics using the existing storage state. Keep this route/navigation test independent from the database fixture flow so it can diagnose routing separately. Do not intercept the app route to fake page HTML.</action>
  <verify><automated>npm exec playwright test e2e/36-agent-management.spec.ts --grep "route|navigation|sidebar"</automated></verify>
  <done>Authenticated browser evidence proves the canonical route and Manage placement while preserving the existing sidebar interaction contract.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| pathname → active key | Untrusted URL text determines only visual navigation state. |
| authenticated browser → dashboard route | Clerk session must be enforced by the dashboard/page gate. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-09 | Spoofing | `/agents` navigation/page | mitigate | Browser assertion plus page-level requireStaffAccess() from Plan 03. |
| T-36-10 | Tampering | active route matching | mitigate | Exact/boundary matcher tests prevent sibling-path false activation. |
| T-36-SC | Tampering | package installs | accept | Existing sidebar primitives only; no package installation. |
</threat_model>
<verification>Run nav unit tests and the authenticated route/sidebar Playwright subset; retain missing Clerk setup as a fail-closed environmental blocker.</verification>
<success_criteria>Agents is discoverable directly under Manage and only `/agents` is treated as its canonical public route.</success_criteria>
<output>Create `.planning/phases/36-agent-management-end-to-end-verification/36-04-SUMMARY.md` when done.</output>
