---
phase: 36-agent-management-end-to-end-verification
plan: 07
type: execute
wave: 6
depends_on: [36-05, 36-06]
files_modified:
  - scripts/phase36-scope-audit.ts
  - scripts/phase36-scope-audit.test.ts
  - .planning/phases/36-agent-management-end-to-end-verification/36-VERIFICATION.md
  - .planning/phases/36-agent-management-end-to-end-verification/36-VALIDATION.md
autonomous: true
requirements: [UX-03, VER-01]
must_haves:
  truths:
    - "The final evidence ledger distinguishes passing deterministic/unit/integration/browser evidence from unavailable DB/Clerk prerequisites and optional live smoke."
    - "Typecheck, build, focused tests, workflow tests, scope audit, and authenticated Playwright commands are reproducible and recorded without fabricated success."
    - "A final scope audit confirms no forbidden route, provider, package, schema, or direct-write leakage entered tracked implementation scope."
  artifacts:
    - path: ".planning/phases/36-agent-management-end-to-end-verification/36-VERIFICATION.md"
      provides: "Sanitized UX-03/VER-01 evidence ledger and requirement map"
    - path: ".planning/phases/36-agent-management-end-to-end-verification/36-VALIDATION.md"
      provides: "Observed command order, blockers, and optional-smoke disposition"
  key_links:
    - from: "scripts/phase36-scope-audit.ts"
      to: "36-VERIFICATION.md"
      via: "zero-finding tracked implementation audit result recorded before final claims"
      pattern: "scope audit"
---

<objective>Close Phase 36 with reproducible final gates and sanitized verification evidence.</objective>
<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/36-agent-management-end-to-end-verification/36-CONTEXT.md
@.planning/phases/36-agent-management-end-to-end-verification/36-RESEARCH.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-company-persona-analysis-experiences/35-04-SUMMARY.md

Scope fence: verification artifacts only. Do not convert missing TEST_DATABASE_URL/Clerk state or unavailable provider policy into a pass. Do not make provider/Firecrawl smoke gating. Do not modify application source in this final plan except if an earlier plan's verified gate exposes a scoped defect; record such a defect and stop rather than silently broadening scope.
</context>
<tasks>
<task type="auto">
  <name>Task 1: Run the complete Phase 36 automated gate and audit evidence</name>
  <files>scripts/phase36-scope-audit.ts, scripts/phase36-scope-audit.test.ts, .planning/phases/36-agent-management-end-to-end-verification/36-VERIFICATION.md</files>
  <action>Run focused contract/query/action/component/nav tests, guarded Neon integration and Workflow suites, typecheck, build, phase36 scope audit, and authenticated Playwright when prerequisites are available. Record counts, statuses, sanitized reasons, and UX-03/VER-01 traceability. Include lifecycle recovery, grounding, adversarial, duplicate, review race, confirmed-only, no-live-write, management, and both target-flow evidence. If a gate is unavailable, mark it blocked with the exact missing prerequisite; never infer database/browser success from unit tests. Cite D-36-10 through D-36-15.</action>
  <verify><automated>
npm test
npx tsc --noEmit
npm run build
npm exec tsx -- scripts/phase36-scope-audit.ts
npm test -- scripts/phase36-scope-audit.test.ts
if [ -n "${TEST_DATABASE_URL:-}" ]; then TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow; else printf '%s\n' 'BLOCKED: TEST_DATABASE_URL unavailable; workflow/database evidence must be recorded as unavailable'; fi
if [ -n "${TEST_DATABASE_URL:-}" ] &amp;&amp; [ -f e2e/.clerk/user.json ] &amp;&amp; [ -n "${PHASE36_COMPANY_ID:-}" ] &amp;&amp; [ -n "${PHASE36_PERSONA_ID:-}" ]; then TEST_DATABASE_URL="$TEST_DATABASE_URL" PHASE36_FIXTURE_ONLY=1 npm exec playwright test e2e/36-agent-management.spec.ts; else printf '%s\n' 'BLOCKED: Clerk storage state, TEST_DATABASE_URL, or Phase 36 fixture IDs unavailable; authenticated Playwright evidence must be recorded as blocked'; fi
  </automated></verify>
  <done>Final deterministic and build evidence is recorded, all available gates are run, and unavailable prerequisites are explicitly fail-closed.</done>
</task>

<task type="auto">
  <name>Task 2: Record validation order, optional smoke disposition, and final scope result</name>
  <files>.planning/phases/36-agent-management-end-to-end-verification/36-VALIDATION.md, .planning/phases/36-agent-management-end-to-end-verification/36-VERIFICATION.md, scripts/phase36-scope-audit.test.ts</files>
  <action>Document the exact rerun order and commands, the requirement coverage table, scope-audit categories, browser fixture prerequisites, and optional live provider/Firecrawl result as `not_run` or `policy_or_credentials_unavailable` when not approved. Ensure the ledger contains no credentials, raw prompts, PII, private reasoning, or fabricated live evidence. The scope audit must scan only tracked implementation scope (source, scripts, e2e, manifests, and schema/query files selected by the audit) and must explicitly exclude `.planning` context, research, discussion, plan, summary, validation, and verification history; do not require zero findings across planning documents. Add a regression test that the implementation-scope audit remains zero-finding. Cite D-36-07, D-36-08, D-36-12, and D-36-14.</action>
  <verify><automated>
npm exec tsx -- scripts/phase36-scope-audit.ts
npm test -- scripts/phase36-scope-audit.test.ts
  </automated></verify>
  <done>Phase 36 verification artifacts are sanitized, reproducible, requirement-mapped, and explicit about all unresolved environmental risks.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| source tree → scope audit | Tracked files are scanned for forbidden scope/security regressions. |
| command output → evidence ledger | Test results and blockers must be represented accurately without sensitive data. |
| optional provider smoke → release claim | External evidence must never become a hidden gate. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-17 | Repudiation | verification ledger | mitigate | Record command, sanitized result, timestamp/status, and missing-prerequisite reason. |
| T-36-18 | Information disclosure | evidence artifacts | mitigate | Explicitly prohibit credentials, raw prompts, PII, private reasoning, and unrestricted packet content. |
| T-36-19 | Tampering | final scope claim | mitigate | Require zero-finding tracked-source audit before claiming scope compliance. |
| T-36-SC | Tampering | package installs | accept | No package installation; audit detects unexpected manifest changes. |
</threat_model>
<verification>Execute the final gate in the documented order and review the ledger for truthful pass/blocked/deferred distinctions before marking Phase 36 complete.</verification>
<success_criteria>UX-03 and VER-01 have auditable, sanitized evidence with no scope reduction and no hidden external-provider dependency.</success_criteria>
<output>Create `.planning/phases/36-agent-management-end-to-end-verification/36-07-SUMMARY.md` when done.</output>
