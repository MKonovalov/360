---
phase: 36-agent-management-end-to-end-verification
plan: 06
type: execute
wave: 5
depends_on: [36-03, 36-04, 36-05]
files_modified:
  - e2e/36-agent-management.spec.ts
  - e2e/phase36-fixture-reset.ts
  - src/lib/verification/phase36Fixtures.ts
autonomous: true
requirements: [UX-03, VER-01]
user_setup:
  - service: test-database
    why: "Real authenticated Playwright Company/Persona flows and post-browser DB assertions"
    env_vars:
      - name: TEST_DATABASE_URL
        source: "Disposable Neon/Postgres test database connection"
      - name: PHASE36_COMPANY_ID
        source: "Deterministic fixture reset output"
      - name: PHASE36_PERSONA_ID
        source: "Deterministic fixture reset output"
  - service: clerk
    why: "Authenticated browser storage state"
    env_vars:
      - name: E2E_CLERK_USER_EMAIL
        source: "Existing Clerk E2E account configuration"
    dashboard_config:
      - task: "Provide the existing authenticated e2e/.clerk/user.json storage state or run the repository auth setup"
        location: "Clerk test account / repository e2e auth setup"
must_haves:
  truths:
    - "Authenticated Playwright against the real app/database proves `/agents` management, Company preview→launch→reload→result→review→candidate, and Persona preview→launch→reload→result→review→candidate flows."
    - "Deterministic executor/fixture packet makes the browser gate independent of provider or Firecrawl credentials."
    - "Existing runs and review/source/candidate views remain inspectable after template edit, retire, and reactivate."
  artifacts:
    - path: "e2e/36-agent-management.spec.ts"
      provides: "Authenticated real-app Phase 36 browser proof"
    - path: "e2e/phase36-fixture-reset.ts"
      provides: "Repeatable DB seed/reset and deterministic executor setup"
  key_links:
    - from: "e2e/36-agent-management.spec.ts"
      to: "/agents, Company, Persona, /reviews"
      via: "real browser navigation and real app requests"
      pattern: "page.goto|/agents|/reviews"
    - from: "e2e/phase36-fixture-reset.ts"
      to: "TEST_DATABASE_URL"
      via: "disposable fixture reset before browser tests"
      pattern: "TEST_DATABASE_URL"
---

<objective>Prove the complete authenticated Company and Persona flows in the real app and database using deterministic execution data.</objective>
<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/36-agent-management-end-to-end-verification/36-CONTEXT.md
@.planning/phases/36-agent-management-end-to-end-verification/36-RESEARCH.md
@.planning/phases/35-company-persona-analysis-experiences/35-04-SUMMARY.md
@e2e/auth.setup.ts
@playwright.config.ts
@e2e/35-analysis-experiences.spec.ts

Scope fence: use real browser→app→database boundaries. Do not mock API responses or bypass workflow persistence for the required flows. Inject determinism only below the provider boundary. Do not require live model/Firecrawl smoke; if optionally run, record it separately as non-gating.
</context>
<tasks>
<task type="auto">
  <name>Task 1: Add repeatable real-app fixture reset and deterministic executor wiring</name>
  <files>e2e/phase36-fixture-reset.ts, src/lib/verification/phase36Fixtures.ts</files>
  <action>Provide a command-scoped reset that seeds known Company, Persona, compatible active signals/checklists, both fixed templates, and clean run/review/candidate state in TEST_DATABASE_URL, returning only sanitized fixture IDs. Wire the deterministic executor/packet at the existing execution seam so real launch requests still claim, persist, complete, reconcile, and expose the packet through the application. Ensure reset is idempotent and does not touch production DATABASE_URL. Cite D-36-10, D-36-11, and D-36-12.</action>
  <verify><automated>
TEST_DATABASE_URL="${TEST_DATABASE_URL:?TEST_DATABASE_URL required}" npm exec tsx e2e/phase36-fixture-reset.ts --check
npm test -- src/lib/verification/phase36Fixtures.test.ts
  </automated></verify>
  <done>A guarded, repeatable fixture reset and deterministic execution path can prepare the real application without provider calls or production DB mutation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement authenticated management and Company/Persona end-to-end browser proof</name>
  <files>e2e/36-agent-management.spec.ts</files>
  <action>Extend the spec to use existing Clerk storage state and serial execution. Cover: `/agents` exactly two templates; content save creates next version and refresh preserves prior history; retire blocks future compatible launch and reactivate restores latest version without version increment; Company and Persona each show resolved instruction, Practice Area, active checklist, and effort; launch through real UI; navigate away/reload; observe durable status; inspect settled findings/sources; open `/reviews`; perform exactly one attributable Confirm or Dismiss; return to the target and assert confirmed-only candidates. Query the test DB after browser actions for immutable snapshots, review identity, source linkage, and unchanged live Signal/link rows. Block forbidden provider/Firecrawl/legacy requests in deterministic mode, but do not replace the app responses with route mocks. Cite D-36-03, D-36-05, D-36-09, D-36-11, and D-36-13.</action>
  <verify><automated>TEST_DATABASE_URL="${TEST_DATABASE_URL:?TEST_DATABASE_URL required}" PHASE36_FIXTURE_ONLY=1 npm exec playwright test e2e/36-agent-management.spec.ts</automated></verify>
  <done>Authenticated real-app evidence passes for `/agents`, Company, Persona, Reviews, durable reload/navigation, one decision, and confirmed-only candidate visibility with deterministic packet data.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Clerk-authenticated browser → app | Real staff session crosses into protected page/actions. |
| browser → real API/workflow/DB | E2E must preserve the actual durable path rather than mocked responses. |
| deterministic executor → evidence/review/candidate reads | Fixture packet must obey the same grounding and review boundaries as production output. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-14 | Spoofing | authenticated E2E | mitigate | Reuse Clerk storage state and assert protected pages do not redirect while fixture reset remains server/DB gated. |
| T-36-15 | Tampering | browser fixture path | mitigate | No route/API mocking for required flow; deterministic injection occurs only below provider boundary and DB is queried afterward. |
| T-36-16 | Information disclosure | E2E evidence | mitigate | Store only sanitized IDs/statuses/counts; never log credentials, raw prompts, private reasoning, or unrestricted Persona data. |
| T-36-SC | Tampering | package installs | accept | No package installation. |
</threat_model>
<verification>Run fixture reset check, focused Playwright route tests, then the serial full Phase 36 authenticated spec. Missing DB/Clerk fixtures are environmental blockers, not passes.</verification>
<success_criteria>VER-01 browser proof demonstrates both target-scoped flows end to end against the real app/database while external providers remain non-gating.</success_criteria>
<output>Create `.planning/phases/36-agent-management-end-to-end-verification/36-06-SUMMARY.md` when done.</output>
