---
phase: 36-agent-management-end-to-end-verification
plan: 05
type: execute
wave: 4
depends_on: [36-02]
files_modified:
  - src/lib/verification/phase36Fixtures.ts
  - src/lib/verification/phase36Fixtures.test.ts
  - src/lib/verification/phase36Adversarial.integration.test.ts
  - src/lib/db/queries/analysisRuns.integration.test.ts
  - src/lib/db/queries/analysisReviews.integration.test.ts
  - src/lib/db/queries/confirmedCandidates.integration.test.ts
  - scripts/phase36-scope-audit.ts
  - scripts/phase36-scope-audit.test.ts
autonomous: true
requirements: [VER-01]
must_haves:
  truths:
    - "Deterministic Company and Persona fixtures prove claim/recovery/safe failure, grounded packet persistence, and duplicate-active-run protection."
    - "Prompt injection, unsafe/unsupported/URL-only/duplicate evidence, and forbidden write/tool attempts fail closed with safe audit state and unchanged Signal/link rows."
    - "Confirm/Dismiss has one winner under replay/race and confirmed-only aggregation excludes every non-confirmed or non-source-backed case for both target types."
  artifacts:
    - path: "src/lib/verification/phase36Fixtures.ts"
      provides: "Disposable DB fixture factory and deterministic executor/packet contract"
    - path: "src/lib/verification/phase36Adversarial.integration.test.ts"
      provides: "Automated fail-closed and no-live-write matrix"
    - path: "scripts/phase36-scope-audit.ts"
      provides: "Tracked-source Phase 36 boundary audit"
  key_links:
    - from: "phase36Fixtures deterministic executor"
      to: "src/workflows/analysisRun.ts"
      via: "existing execution seam, not browser/API route mock"
      pattern: "analysisRun"
    - from: "confirmedCandidates queries"
      to: "analysis_run_review decision = confirmed"
      via: "database-authoritative candidate predicate"
      pattern: "confirmed"
---

<objective>Build the deterministic database/workflow verification gate for VER-01, including adversarial no-write proof.</objective>
<execution_context>@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md</execution_context>
<context>
@.planning/phases/36-agent-management-end-to-end-verification/36-CONTEXT.md
@.planning/phases/36-agent-management-end-to-end-verification/36-RESEARCH.md
@.planning/phases/33-grounded-analysis-execution-evidence/33-CONTEXT.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-CONTEXT.md
@src/workflows/analysisRun.ts
@src/lib/db/queries/analysisRuns.ts
@src/lib/db/queries/analysisReviews.ts
@src/lib/db/queries/confirmedCandidates.ts

Scope fence: fixture-only automated adversarial coverage; no live provider or Firecrawl call, no new provider, no browser-only security demo, no direct Signal/Offering/link write, and no weakening of TEST_DATABASE_URL guards. The scope audit scans only selected tracked implementation files and explicitly excludes all `.planning` context, research, discussion, plan, summary, validation, and verification history. Optional provider smoke is not part of this plan's pass condition.
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create deterministic Company/Persona fixture and lifecycle matrix</name>
  <files>src/lib/verification/phase36Fixtures.ts, src/lib/verification/phase36Fixtures.test.ts, src/lib/db/queries/analysisRuns.integration.test.ts</files>
  <behavior>
    - Company and Persona fixtures create compatible templates, active-signal checklist snapshots, subject records, and known packet/source rows without provider calls.
    - Claim, interrupted/expired recovery, safe terminal failure, and event audit are proven without a permanently running run.
    - Concurrent starts for the same subject/template produce one active run and one active_run_exists outcome for both target types; independent subjects/types remain independent.
  </behavior>
  <action>Implement a disposable fixture factory with deterministic IDs, safe packet contents, and an executor injection seam below the workflow/provider boundary. Reuse real analysisRun, claim/transition, packet persistence, and duplicate guard paths; do not mock the browser/API/database boundary. Extend guarded integration tests to assert lifecycle events, actor/timestamps/reasons, immutable run snapshots, grounded finding/source linkage, and both Company/Persona duplicate cases. Cite D-36-10 and D-36-13.</action>
  <verify><automated>TEST_DATABASE_URL="${TEST_DATABASE_URL:?TEST_DATABASE_URL required}" npm test -- src/lib/verification/phase36Fixtures.test.ts src/lib/db/queries/analysisRuns.integration.test.ts && TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow</automated></verify>
  <done>The deterministic fixture proves durable lifecycle and grounded execution through real persistence/workflow seams for both target contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add adversarial, review, aggregation, and scope-boundary gates</name>
  <files>src/lib/verification/phase36Adversarial.integration.test.ts, src/lib/db/queries/analysisReviews.integration.test.ts, src/lib/db/queries/confirmedCandidates.integration.test.ts, scripts/phase36-scope-audit.ts, scripts/phase36-scope-audit.test.ts</files>
  <behavior>
    - Malicious prompt injection, unsafe citation, unsupported URL, URL-only citation, duplicate evidence, and forbidden write/tool request each fail closed with safe error/audit state.
    - Before/after hashes of live Company/Persona Signals, Offerings, and signal_offering_link rows are identical for every adversarial case and for Confirm/Dismiss.
    - Confirm-vs-Dismiss races yield one attributable winner; replay is idempotent; confirmed-only SQL includes only source-backed strong/weak findings for matching target type/ID.
  </behavior>
  <action>Extend integration matrices using the fixture factory and existing normalize/persist/review/candidate seams. Assert no accepted proof from URL alone or untrusted citation, no forbidden tool/write side effect, packet/review immutability, one-winner decision identity, and Company/Persona confirmed-only exclusions. Add a tracked-source scope audit modeled on Phases 33/34 that fails on nested route strings, provider/Firecrawl leakage, direct live Signal/Offering writes, packet mutation, legacy proposal reuse, client secret exposure, and new packages/schema unless explicitly authorized. Keep audit evidence sanitized. Cite D-36-13, D-36-14, and D-36-15.</action>
  <verify><automated>
TEST_DATABASE_URL="${TEST_DATABASE_URL:?TEST_DATABASE_URL required}" npm test -- src/lib/verification/phase36Adversarial.integration.test.ts src/lib/db/queries/analysisReviews.integration.test.ts src/lib/db/queries/confirmedCandidates.integration.test.ts
npm exec tsx -- scripts/phase36-scope-audit.ts
npm test -- scripts/phase36-scope-audit.test.ts
  </automated></verify>
  <done>VER-01 automated fixture coverage proves fail-closed security, no-live-write, review idempotency, and confirmed-only aggregation with a zero-finding scope audit.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| fixture/executor → workflow | Deterministic output enters the real claim/normalize/persist path. |
| research content → packet validator | Untrusted narrative, citations, URLs, and tool requests cross the evidence boundary. |
| review action → live candidate projection | A whole-run decision may influence only confirmed candidate reads, never source tables. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-36-11 | Tampering | evidence packet | mitigate | Existing normalization/allowlist rejects unsafe, duplicate, unsupported, and unlinked evidence before completion. |
| T-36-12 | Elevation | forbidden tool/write attempt | mitigate | Deterministic adversarial fixture asserts fail-closed safe audit and unchanged live rows. |
| T-36-13 | Tampering | confirmed candidates | mitigate | SQL projection requires confirmed review plus source-backed eligible finding and target discriminator. |
| T-36-SC | Tampering | package installs | accept | No package installation; scope audit blocks unexpected package changes. |
</threat_model>
<verification>Run guarded DB/workflow tests and the scope audit; absence of TEST_DATABASE_URL must fail closed and must not be reported as database evidence.</verification>
<success_criteria>All deterministic VER-01 invariants are executable and no adversarial fixture can mutate live Signal/Offering/link state or create ungrounded proof.</success_criteria>
<output>Create `.planning/phases/36-agent-management-end-to-end-verification/36-05-SUMMARY.md` when done.</output>
