---
phase: 10-sidebar-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/globals.css]
autonomous: true
requirements: [PANE-01, PANE-02, PANE-03, PANE-04, QLTY-02]

must_haves:
  truths:
    - "The desktop expanded sidebar renders the light near-white #fbfcfd panel with a 0.5px #e5e7eb hairline right border (PANE-01)"
    - "The mobile sheet and icon-collapsed rail render the same light panel — no .dark class, no dark: variants introduced anywhere (PANE-03)"
    - "Light content areas (pages, lists, detail panes) render identically — sidebar tokens are consumed exclusively by the sidebar subtree (PANE-04)"
    - "The entire theme is one scoped --sidebar-* token block on [data-sidebar=\"sidebar\"]; zero new packages; sidebar.tsx and @theme inline untouched (PANE-02)"
    - "The 8-token set is a complete verified AA set — text >= 4.5:1 on panel, active pill and ring >= 3:1, /70 labels still passing (QLTY-02)"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Scoped [data-sidebar=\"sidebar\"] token block (8 hex tokens) + 2 unlayered companion rules"
      contains: "[data-sidebar=\"sidebar\"]"
      min_lines: 130
  key_links:
    - from: "src/app/globals.css [data-sidebar=\"sidebar\"] block"
      to: "src/components/ui/sidebar.tsx sidebar-inner (line 242) + SheetContent (line 186)"
      via: "attribute hook — custom properties resolve per-element through the @theme inline var() mapping (globals.css:13-20)"
      pattern: "data-sidebar=\"sidebar\""
    - from: "src/app/globals.css [data-slot=\"sidebar-container\"] rule"
      to: "src/components/ui/sidebar.tsx data-slot=\"sidebar-container\" (line 229) with border-r and no color class (line 236)"
      via: "unlayered border-color / border-right-width override beats @layer base * { @apply border-border }"
      pattern: "data-slot=\"sidebar-container\""
---

<objective>
Ship the complete light-theme sidebar token foundation in `src/app/globals.css`: one scoped `--sidebar-*` token block on `[data-sidebar="sidebar"]` plus two unlayered companion rules — the entire v1.2 Exa-sidebar theme (PANE-01, PANE-03), delivered with zero new packages, zero edits to the vendored `src/components/ui/sidebar.tsx`, and the load-bearing `@theme inline` block byte-identical (PANE-02).

Purpose: This is the mechanism every later v1.2 phase builds on. The attribute-hook scope means the token block themes all three sidebar surfaces (desktop expanded, icon-collapsed rail, mobile sheet — `sidebar.tsx:186,242`) while the light content area keeps resolving `:root` values unchanged (PANE-04). The AA-compliant values (QLTY-02) are pre-verified in RESEARCH.md (all ratios independently recomputed) and frozen by the approved UI-SPEC §Token Contract; this plan writes them verbatim — hex, not oklch.

UI-SPEC decisions D1 (logo treatment — text wordmark), D2 (feedback destination — `mailto:hello@arclumenpartners.com?subject=360%20sidebar%20feedback`), D3 (collapse target width — stock `--sidebar-width-icon: 3rem`, no override), D4 (portal policy — portaled, light app-theme, no carve-out) are locked context for Phases 11-13. Phase 10 implements none of them; the D3/D4 preservation constraints that touch this plan are respected here: no `--sidebar-width-icon` override, and no alteration of the global `--popover`/`--background` tokens that portaled content depends on.

Output: `src/app/globals.css` gains ~30 lines — the 8-token scoped block + 2 companion rules — and nothing else in the repo changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md (§Token Contract — the verbatim values and mechanism notes)
@.planning/phases/10-sidebar-token-foundation/10-RESEARCH.md (§Architecture Patterns Pattern 1; §Common Pitfalls 1-3; §Security Domain)
@.planning/phases/10-sidebar-token-foundation/10-PATTERNS.md (§globals.css pattern — insertion point, structural template, cascade rival)
@.planning/phases/10-sidebar-token-foundation/10-VALIDATION.md (per-task verification map 10-01-01 / 10-01-02)
@src/app/globals.css (the file being modified — 130 lines; current state)
@src/components/ui/sidebar.tsx (consumer hooks — READ ONLY, do not edit)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the scoped sidebar token block + companion rules into src/app/globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css — the file being modified. Current structure: `@theme inline` (7-49), `:root` (51-84, sidebar tokens 76-83), `.dark` (86-118, dead code), `@layer base` (120-130). Insertion point is AFTER the `:root` closing brace (line 84), BEFORE `.dark` (line 86).
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Token Contract — the verbatim block to write (hex values, companion rules, "do not touch" list).
    - .planning/phases/10-sidebar-token-foundation/10-PATTERNS.md §globals.css — the exact block text, insertion-point verification, and why the companion rules must stay unlayered.
    - .planning/phases/10-sidebar-token-foundation/10-RESEARCH.md §Pitfalls 1-3 — what must NOT be touched and why (byte-identical `@theme inline`, `:root` 76-83, `.dark`; no oklch conversion; unlayered companion rules).
  </read_first>
  <action>
    Insert one block into `src/app/globals.css` between the `:root` closing brace (currently line 84) and `.dark` (currently line 86), as a single atomic edit. The block contains exactly three rules, copied verbatim from UI-SPEC §Token Contract — hex values, NOT oklch:

    1. A `[data-sidebar="sidebar"]` rule declaring all 8 tokens with these exact values: `--sidebar: #fbfcfd`; `--sidebar-foreground: #333333`; `--sidebar-primary: #333333`; `--sidebar-primary-foreground: #ffffff`; `--sidebar-accent: #909090`; `--sidebar-accent-foreground: #111111`; `--sidebar-border: #e5e7eb`; `--sidebar-ring: #787878`. Keep the UI-SPEC's inline value annotations (contrast ratios) as comments.
    2. An UNLAYERED `[data-slot="sidebar-container"]` rule declaring `border-color: var(--sidebar-border)` and `border-right-width: 0.5px`.
    3. An UNLAYERED `[data-sidebar="sidebar"] *` rule declaring `outline-color: var(--sidebar-ring)`.
    Include a why-comment header line naming Phase 10 and stating the block is scoped with zero vendored edits and `@theme inline` untouched.

    Do NOT wrap rules 2 and 3 in any `@layer` — unlayered author CSS is what beats the global `@layer base * { @apply border-border outline-ring/50 }` (globals.css:121-123) and the layered `border-r` utility on `sidebar.tsx:236`. Wrapping them re-loses the cascade (RESEARCH Pitfall 2).

    Do NOT touch: `@theme inline` (7-49), `:root` sidebar tokens (76-83), `.dark` (86-118), `@layer base` (120-130), the global `--ring`/`--border`/`--popover`/`--background` tokens, or any file other than `globals.css`. No `dark:` variants anywhere. No `--sidebar-width-icon` override (D3 — stock 3rem preserved).
  </action>
  <verify>
    <automated>
      # All 8 tokens present with exact hex values (one declaration line each):
      grep -c -- '--sidebar: #fbfcfd' src/app/globals.css
      grep -c -- '--sidebar-foreground: #333333' src/app/globals.css
      grep -c -- '--sidebar-primary: #333333' src/app/globals.css
      grep -c -- '--sidebar-primary-foreground: #ffffff' src/app/globals.css
      grep -c -- '--sidebar-accent: #909090' src/app/globals.css
      grep -c -- '--sidebar-accent-foreground: #111111' src/app/globals.css
      grep -c -- '--sidebar-border: #e5e7eb' src/app/globals.css
      grep -c -- '--sidebar-ring: #787878' src/app/globals.css
      # (each must print 1)
      # Companion rules present:
      grep -c -- 'border-color: var(--sidebar-border)' src/app/globals.css
      grep -c -- 'border-right-width: 0.5px' src/app/globals.css
      grep -c -- 'outline-color: var(--sidebar-ring)' src/app/globals.css
      # (each must print 1)
      # Placement: token block sits between :root and .dark, and no @layer/dark: added:
      T=$(grep -n -- '--sidebar: #fbfcfd' src/app/globals.css | cut -d: -f1); R=$(grep -n '^:root' src/app/globals.css | cut -d: -f1); D=$(grep -n '^\.dark' src/app/globals.css | cut -d: -f1); test "$T" -gt "$R" && test "$T" -lt "$D" && echo "placement-ok"
      git diff src/app/globals.css | grep '^+' | grep -c '@layer'
      git diff src/app/globals.css | grep '^+' | grep -c 'dark:'
      # (both must print 0)
    </automated>
  </verify>
  <acceptance_criteria>
    - Each of the 8 grep commands for token declarations prints exactly 1 (all 8 tokens present, each declared once, exact hex values from UI-SPEC §Token Contract).
    - `border-color: var(--sidebar-border)`, `border-right-width: 0.5px`, and `outline-color: var(--sidebar-ring)` each appear exactly once.
    - The `[data-sidebar="sidebar"]` token block's line number is greater than the `:root` line and less than the `.dark` line; the shell placement test prints `placement-ok`.
    - `git diff` of `globals.css` shows 0 added lines containing `@layer` and 0 added lines containing `dark:`.
    - `git diff --stat src/app/globals.css` shows only additions (no removed lines) — `@theme inline`, `:root`, and `.dark` blocks byte-identical.
  </acceptance_criteria>
  <done>The 8-token scoped block + 2 unlayered companion rules are inserted between `:root` and `.dark` in globals.css; hex values match the UI-SPEC verbatim; no @layer, no dark:, no edits to any other block.</done>
</task>

<task type="auto">
  <name>Task 2: Zero-drift diff gates + build verification (PANE-04, QLTY-02)</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css — the edited file; verify the diff shape (additions only).
    - .planning/phases/10-sidebar-token-foundation/10-VALIDATION.md — per-task verification map for 10-01-02 (QLTY-02: 8-token AA set; PANE-04: content unchanged).
    - .planning/phases/10-sidebar-token-foundation/10-RESEARCH.md §Validation Architecture (PANE-01..04 diff-rule) and §Security Domain (negative security behavior: global tokens --background/--popover/--ring/--border must not change).
    - package.json — scripts (`build`: `next build`).
  </read_first>
  <action>
    Run the regression-gate battery that proves PANE-02/PANE-04 and the QLTY-02 token set:

    1. `git diff --stat` — the change set must contain exactly `src/app/globals.css` from this plan (plus whatever Plan 10-02 has created: `src/lib/nav.ts`, `src/lib/nav.test.ts`). No other files.
    2. `git diff src/components/ui/sidebar.tsx src/components/layout/app-sidebar.tsx src/components/layout/app-shell-layout.tsx src/components/layout/sidebar-resize-handle.tsx` — must be empty (PANE-02: vendored primitive + consumers untouched).
    3. `git diff package.json package-lock.json` — must be empty (zero new packages; PANE-02 hard constraint).
    4. `git diff src/app/globals.css | grep '^-' | grep -v '^---'` — must print nothing (only additions → `@theme inline`, `:root`, `.dark` byte-identical; no global token altered).
    5. `npm run build` — must exit 0 (proves the CSS parses under Tailwind v4 and the app compiles).
    This plan is the ONLY plan in Wave 1 that runs `npm run build` (Plan 10-02 runs vitest gates only, avoiding `.next` contention between parallel executors).
  </action>
  <verify>
    <automated>
      git diff --stat
      # Expect: src/app/globals.css plus at most src/lib/nav.ts, src/lib/nav.test.ts. No other paths.
      test -z "$(git diff src/components/ui/sidebar.tsx src/components/layout/app-sidebar.tsx src/components/layout/app-shell-layout.tsx src/components/layout/sidebar-resize-handle.tsx)" && echo "vendored-clean"
      test -z "$(git diff package.json package-lock.json)" && echo "packages-clean"
      test -z "$(git diff src/app/globals.css | grep '^-' | grep -v '^---')" && echo "additions-only"
      npm run build
    </automated>
    <human-check>
      Optional (non-blocking; live-browser audit is Phase 14's contract): `npm run dev`, open http://localhost:3000, confirm the left sidebar panel renders near-white `#fbfcfd` with a ~0.5px `#e5e7eb` hairline on its right edge, and confirm page/list/detail content looks identical to before the change. On mobile viewport, confirm the sheet drawer uses the same light panel.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `git diff --stat` lists only `src/app/globals.css` (+ this plan's 8 tokens + 2 companion rules, ~30 lines) and at most the two `src/lib/nav*` files from Plan 10-02.
    - The vendored-clean shell test prints `vendored-clean` (empty diff for `sidebar.tsx` and the three layout consumers).
    - The packages-clean shell test prints `packages-clean` (empty diff for `package.json`/`package-lock.json`).
    - The additions-only shell test prints `additions-only` — zero removed lines in `globals.css`; `@theme inline` (7-49), `:root` sidebar tokens (76-83), and `.dark` (86-118) byte-identical.
    - `npm run build` exits 0.
    - QLTY-02: the 8 token values present in source are exactly the UI-SPEC §Token Contract hex set, whose AA ratios (text 12.30:1, /70 labels 4.89:1, pill 3.11:1, ring 4.30:1, etc.) were independently recomputed and confirmed in RESEARCH.md.
  </acceptance_criteria>
  <done>Diff shows only the scoped block added to globals.css; vendored sidebar.tsx, all layout consumers, and package files byte-identical; build green; the AA-verified 8-token set is in source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `[data-sidebar="sidebar"]` scoped block → global `:root` tokens | The new block lives in the same file as the global theme; a misplaced edit could alter `--background`/`--popover`/`--ring`/`--border` that content and portals depend on (RESEARCH §Security Domain: the security-relevant behavior is negative). |
| Build artifact → vendored primitives + package.json | `sidebar.tsx` (702-line vendored shadcn primitive) and the dependency manifest are frozen; drift breaks the PANE-02 token-swap-only guarantee. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `src/app/globals.css` token block | mitigate | Insertion-point discipline (after `:root` line 84, before `.dark` line 86) + `additions-only` git-diff gate (Task 2 step 4) proving zero removed lines; PANE-04 attribute-hook scoping means content resolves `:root` values unchanged; `--popover`/`--background`/`--ring`/`--border` untouched per RESEARCH §Security Domain. |
| T-10-02 | Tampering | `[data-slot="sidebar-container"]` + `[data-sidebar="sidebar"] *` companion rules | mitigate | Rules written unlayered so they beat `@layer base * { @apply border-border outline-ring/50 }` (globals.css:121-123) and the layered `border-r` utility (`sidebar.tsx:236`); verified by the Task 1 `@layer`-free grep gate and Task 2 build gate (RESEARCH Pitfall 2). |
| T-10-03 | Integrity | vendored `sidebar.tsx` + layout consumers + `package.json` | mitigate | Task 2 git-diff gates assert empty diffs for `sidebar.tsx`, `app-sidebar.tsx`, `app-shell-layout.tsx`, `sidebar-resize-handle.tsx`, `package.json`, `package-lock.json` (PANE-02 hard constraint; zero new npm packages per RESEARCH §Package Legitimacy Audit). |
| T-10-04 | Spoofing | CSS token values | accept | Values are hardcoded build-time hex constants committed to source; no user input reaches the CSS block (ASVS V8). No `var(--user-input)` paths exist — nothing to inject. |
| T-10-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed this phase (RESEARCH §Package Legitimacy Audit exemption; `components.json` declares `registries: {}`). Any executor-initiated install is a PANE-02 plan violation and requires a blocking human checkpoint before proceeding. |
</threat_model>

<verification>
Phase-level checks after both Wave-1 plans complete:
- `npm test` full suite green (22 existing test files + Plan 10-02's `nav.test.ts`)
- `npm run build` exits 0 (run by this plan)
- `git diff --stat` shows exactly three files: `src/app/globals.css`, `src/lib/nav.ts`, `src/lib/nav.test.ts`
- `@theme inline` (globals.css:7-49), `:root` sidebar tokens (76-83), `.dark` (86-118), and `src/components/ui/sidebar.tsx` byte-identical
- QLTY-02 AA ratios for the 8 shipped tokens confirmed (independent recomputation in RESEARCH.md; live audit deferred to Phase 14)
</verification>

<success_criteria>
Measurable completion:
- The 8-token scoped block and both companion rules exist in `src/app/globals.css` with the exact UI-SPEC hex values, placed between `:root` and `.dark`, unlayered
- Zero new npm packages; zero edits to `src/components/ui/sidebar.tsx`; `@theme inline` byte-identical (roadmap SC #2)
- Sidebar tokens are scoped to the sidebar subtree — content areas resolve unchanged `:root` values (roadmap SC #3, PANE-04)
- The AA-compliant 8-token set is in source — text ≥4.5:1 on panel, active pill and ring ≥3:1, /70 labels passing (roadmap SC #4, QLTY-02)
- Expanded, icon-collapsed, and mobile-sheet surfaces all resolve the same scoped block via the `data-sidebar="sidebar"` hook (roadmap SC #1, PANE-03); live visual confirmation is Phase 14's UAT matrix
- `npm run build` green (roadmap SC #2's diff rule satisfied)
</success_criteria>

<output>
Create `.planning/phases/10-sidebar-token-foundation/10-01-SUMMARY.md` when done
</output>
