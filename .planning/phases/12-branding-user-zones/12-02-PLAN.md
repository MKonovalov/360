---
phase: 12-branding-user-zones
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: []
autonomous: true
requirements: [BRND-01, BRND-02, BRND-03, BRND-04]

must_haves:
  truths:
    - "QLTY-04 sweep gate passes — zero indigo/amber/hex/dark: matches across src/components/layout/ after the Phase 12 diff (BRND-04)"
    - "Fence gates pass — sidebar.tsx, dropdown-menu.tsx, tooltip.tsx, globals.css, (dashboard)/layout.tsx, app-shell-layout.tsx, package.json, package-lock.json are all byte-identical (BRND-04 hard constraints)"
    - "The full regression battery is green — nav.test.ts 11/11, user.test.ts 8/8, npm test, npm run build, npx tsc --noEmit"
  artifacts:
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Phase 12's single source-file change (branding zone + user zone) — verified swept and fence-clean"
      contains: "SidebarFooter"
    - path: "src/lib/user.ts"
      provides: "Pure display functions — verified present and covered by the user.test.ts lock"
      exports: ["getUserDisplayName", "getUserInitials"]
  key_links:
    - from: "sweep gate"
      to: "src/components/layout/"
      via: "grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' over the whole directory — must print nothing (QLTY-04)"
      pattern: "sweep-clean"
    - from: "fence gate"
      to: "8 protected files (sidebar.tsx, dropdown-menu.tsx, tooltip.tsx, globals.css, (dashboard)/layout.tsx, app-shell-layout.tsx, package.json, package-lock.json)"
      via: "git diff must be empty on every protected file — zero vendored/global/config drift"
      pattern: "fence-clean"
    - from: "regression battery"
      to: "src/lib/nav.test.ts (11/11) + src/lib/user.test.ts (8/8)"
      via: "vitest targeted suites — the QLTY-01 nav lock and the new BRND-02 nullability lock both green"
      pattern: "11/11"
---

<objective>
Close Phase 12 with the full scope-fence verification: run the QLTY-04 sweep gate across `src/components/layout/` (zero `indigo`/`amber`/hex/`dark:` — the extended Phase 11 precedent), run the fence gates on all 8 protected files (vendored primitives, globals.css token block, dashboard layout, app-shell layout, package manifests — all byte-identical), verify the whole-phase diff scope (exactly `app-sidebar.tsx` + `src/lib/user.ts` + `src/lib/user.test.ts`), and run the complete regression battery (`npx tsc --noEmit`, `nav.test.ts` 11/11, `user.test.ts` 8/8, `npm test`, `npm run build`).

Purpose: This is the Wave-2 closing plan of Phase 12 (depends on 12-01 because its sweep gate scans the whole `src/components/layout/` directory, which only reaches its final state after 12-01 commits the two zones; the fence gates need 12-01's committed diff to diff against). It mirrors the Phase 11 11-02 structure — the plan owns NO source changes; it proves the phase landed clean: BRND-01..04 implemented without collateral damage to the frozen contract (routes, pendingCount gating, collapse/resize/cookie contract, vendored primitives, token block, dependency manifests).

Hard constraints honored: zero new npm packages (fence gate asserts `package.json`/`package-lock.json` empty diffs); zero edits to vendored `src/components/ui/sidebar.tsx`/`dropdown-menu.tsx`/`tooltip.tsx`; zero edits to `src/app/globals.css`; zero edits to `src/app/(dashboard)/layout.tsx` and `app-shell-layout.tsx`; no `.dark`/`dark:` variants.

Output: a verified phase — sweep gates = 0 across the layout directory, fence gates = clean on all 8 protected files, and the phase's full regression battery green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (§Phase 12 — success criteria 1-4; hard-constraint contract)
@.planning/REQUIREMENTS.md (BRND-01..04)
@.planning/phases/12-branding-user-zones/12-UI-SPEC.md (§Hard constraints — the 8-file fence list; §Verification Gates — sweep + fence + regression commands)
@.planning/phases/12-branding-user-zones/12-RESEARCH.md (§Scope fence — the phase touch-list is exactly app-sidebar.tsx + user.ts + user.test.ts; §Security Domain; §Package Legitimacy Audit — zero installs N/A by exemption)
@.planning/phases/12-branding-user-zones/12-VALIDATION.md (per-task map row 12-02-01 — BRND-01..04 regression; sampling rate: per-wave npm test + npm run build)
@.planning/phases/12-branding-user-zones/12-01-PLAN.md (the Wave-1 prerequisite — its acceptance criteria: sweep-clean per task, both unit locks green, diff scope = 3 files)
@.planning/phases/11-nav-items-restyle/11-02-PLAN.md (the sweep/fence/regression precedent this plan mirrors — sweep gate idiom, fence-clean idiom, comment-hygiene Rule 1)
@src/components/layout/ (the swept directory: app-sidebar.tsx — 12-01's diff; app-shell-layout.tsx, sidebar-resize-handle.tsx — must stay clean)
@package.json (scripts: `test` = `vitest run`, `build` = `next build`)
</context>

<tasks>

<task type="auto">
  <name>Task 1: QLTY-04 sweep gate + fence gates + whole-phase diff scope verification (BRND-04)</name>
  <files></files>
  <read_first>
    - .planning/phases/12-branding-user-zones/12-UI-SPEC.md §Hard constraints (the exact 8-file fence list: `src/components/ui/sidebar.tsx`, `dropdown-menu.tsx`, `tooltip.tsx`, `src/app/globals.css`, `src/app/(dashboard)/layout.tsx`, `app-shell-layout.tsx`, `package.json`, `package-lock.json`) and §Verification Gates (QLTY-04 sweep: `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"` → 0).
    - .planning/phases/11-nav-items-restyle/11-02-PLAN.md Task 2 — the exact verification idiom: `test -z "$(...)" && echo sweep-clean` / `echo fence-clean` shell-test gates, plus `git diff --stat` scope proof.
    - .planning/phases/12-branding-user-zones/12-VALIDATION.md row 12-01-04 (BRND-04 grep gate) and row 12-02-01 (regression sweep).
    - .planning/phases/12-branding-user-zones/12-01-PLAN.md Task 3 acceptance criteria — the sweep already ran clean per task in 12-01; this task re-runs it at phase scope.
  </read_first>
  <action>
    Run the phase-closing scope-fence gates. NO source-code changes in this task — verification only:
    1. QLTY-04 sweep gate across the whole layout directory: `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)" && echo sweep-clean` — must print `sweep-clean`. This scans `app-sidebar.tsx` (12-01's diff — branding/user zones add zero palette colors: `--sidebar-primary #333333` initials circle is the letter-mark language, not a palette utility), `app-shell-layout.tsx`, and `sidebar-resize-handle.tsx`. If any line matches, fix the offending source line in the file it came from (keeping the comment-hygiene rule: do not introduce the words into comments — 11-02 Rule 1) and re-run until zero matches.
    2. Fence gates on all 8 protected files: `test -z "$(git diff src/components/ui/sidebar.tsx src/components/ui/dropdown-menu.tsx src/components/ui/tooltip.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx package.json package-lock.json)" && echo fence-clean` — must print `fence-clean` (quote the `(dashboard)` path — parens are shell-special). Vendored primitives, the Phase 10 token block + companion rules, the auth gate, the server shell, and both dependency manifests must be byte-identical to HEAD. If any protected file has a diff, investigate whether 12-01's executor violated the fence (do NOT silently revert a legitimate change — check the diff first, then restore byte-identical state).
    3. Whole-phase diff scope: `git diff <PHASE_BASE_SHA> HEAD --stat` — must show exactly `src/components/layout/app-sidebar.tsx`, `src/lib/user.ts`, `src/lib/user.test.ts` for the phase. `<PHASE_BASE_SHA>` = the phase-start commit (the commit immediately before 12-01 Task 1's first source commit; resolve via `git log --oneline` — it is the `docs(12): create phase plan` commit, currently `a79cd5ac` in this repo). Because 12-01 commits per task, an UNQUALIFIED `git diff --stat` (working tree vs HEAD) is vacuous — the phase files are already committed, so always use the base-vs-HEAD range. (Plus `git status --short` must show no other modified/untracked source files; the 12-01 SUMMARY and any planning artifacts are docs, not source.)
  </action>
  <verify>
    <automated>
      test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)" && echo sweep-clean
      test -z "$(git diff src/components/ui/sidebar.tsx src/components/ui/dropdown-menu.tsx src/components/ui/tooltip.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx package.json package-lock.json)" && echo fence-clean
      git diff <PHASE_BASE_SHA> HEAD --stat
      # <PHASE_BASE_SHA> = phase-start commit (immediately before 12-01 Task 1's first source commit; currently a79cd5ac)
      git status --short
    </automated>
  </verify>
  <acceptance_criteria>
    - The shell test prints `sweep-clean` — zero `indigo`/`amber`/`#[0-9a-fA-F]{3,8}` hex/`dark:` matches across `src/components/layout/` (BRND-04, QLTY-04 — the whole Phase 12 diff is token-only).
    - The shell test prints `fence-clean` — all 8 protected files are byte-identical to HEAD: `sidebar.tsx`, `dropdown-menu.tsx`, `tooltip.tsx`, `globals.css`, `(dashboard)/layout.tsx`, `app-shell-layout.tsx`, `package.json`, `package-lock.json` (hard constraints: zero vendored edits, zero token-block edits, zero new packages, zero config drift).
    - `git diff <PHASE_BASE_SHA> HEAD --stat` (base-vs-HEAD range; `<PHASE_BASE_SHA>` = phase-start commit, currently `a79cd5ac`) shows exactly the 3 phase files; `git status --short` shows no other modified/untracked source files.
  </acceptance_criteria>
  <done>Sweep gate clean (indigo/amber/hex/dark: = 0 across src/components/layout/); fence gates clean on all 8 protected files; the whole-phase diff is exactly app-sidebar.tsx + user.ts + user.test.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression battery — tsc + nav lock 11/11 + user lock 8/8 + npm test + npm run build (BRND-01..04)</name>
  <files></files>
  <read_first>
    - .planning/phases/12-branding-user-zones/12-VALIDATION.md — sampling rate (full suite + build per wave), row 12-02-01 (regression: `npm test`, `npx vitest run src/lib/nav.test.ts --bail=1`, `npm run build`), Wave 0 note (user.test.ts created in-phase by 12-01 Task 1).
    - package.json — scripts (`test`: `vitest run`; `build`: `next build`). Phase 11 baseline: 23 files, 224 passed, 2 skipped.
    - .planning/phases/12-branding-user-zones/12-01-PLAN.md — Task 1 (user.test.ts 8/8) and Task 3 acceptance criteria (both locks green per task).
    - src/lib/nav.test.ts + src/lib/user.test.ts — the two targeted suites this battery re-runs.
  </read_first>
  <action>
    Run the phase-closing regression battery. No production code changes — verification only:
    1. `npx tsc --noEmit` — must exit 0 (type gate over the whole tree, including the new user.ts + the app-sidebar.tsx zones).
    2. `npx vitest run src/lib/nav.test.ts --bail=1` — must exit 0 with all 11 cases (the QLTY-01 lock: `/companies/[id]` highlight + `/companies-archive` boundary guard survive the phase).
    3. `npx vitest run src/lib/user.test.ts -x` — must exit 0 with all 8 cases (the new BRND-02 nullability lock).
    4. `npm test` — must exit 0 with the full suite green and NO fewer passing tests than the Phase 11 baseline (23 files, 224 passed / 2 skipped — the phase may not drop a single existing test).
    5. `npm run build` — must exit 0 (SSR production build type-checks and bundles the restyled sidebar under Next).
    If any gate fails, diagnose and fix at its root (a test failure means a behavior regression in 12-01's diff; a build failure means a type/import problem) — do not weaken gates to pass.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      npx vitest run src/lib/nav.test.ts --bail=1
      npx vitest run src/lib/user.test.ts -x
      npm test
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 with 11/11 — the nav active-key regression lock holds.
    - `npx vitest run src/lib/user.test.ts -x` exits 0 with 8/8 — the BRND-02 display-name/initials nullability lock holds.
    - `npm test` exits 0 with the full suite green and ≥ Phase 11 baseline pass count (224 passed / 2 skipped).
    - `npm run build` exits 0 — the production build succeeds with the Phase 12 diff in place.
  </acceptance_criteria>
  <done>Full regression battery green: tsc clean, nav.test.ts 11/11, user.test.ts 8/8, npm test ≥ baseline, npm run build clean — Phase 12 verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase diff → protected files | The 8-file fence list (vendored primitives, globals.css token block, dashboard/auth layout, app-shell layout, package manifests) must be byte-identical — a drift here would silently alter the frozen contract (auth gate, token theme, dependency set) |
| New Phase 12 code → sweep scope | The branding/user-zone diff lives inside `src/components/layout/` — the QLTY-04 sweep gate must catch any hardcoded palette/hex/`dark:` utility re-introduced by the zones |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Integrity | hardcoded palette/hex/`dark:` utilities in `src/components/layout/` | mitigate | Task 1's sweep gate (`test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"`) asserts zero matches across the whole directory, including 12-01's app-sidebar.tsx diff. Comment-hygiene rule (11-02 Rule 1) prevents the swept words from re-entering via prose. Fix-at-root required if any match appears. |
| T-12-06 | Integrity | the 8 protected files (fence list) | mitigate | Task 1's fence gate asserts byte-identical `git diff` on `sidebar.tsx`, `dropdown-menu.tsx`, `tooltip.tsx`, `globals.css`, `(dashboard)/layout.tsx`, `app-shell-layout.tsx`, `package.json`, `package-lock.json` — proving the hard constraints (zero vendored edits, zero token-block edits, zero new packages, zero config/auth drift) held through the phase. Any diff is investigated before restore (never silently revert a legitimate change). |
| T-12-07 | Regression | nav active-key + user display logic + full suite + build | mitigate | Task 2 runs the complete battery: `nav.test.ts` 11/11 (QLTY-01 lock — `/companies/[id]` highlight and `/companies-archive` guard), `user.test.ts` 8/8 (BRND-02 lock — nullability chains), `npm test` (≥ 224 passed baseline), `npm run build`, `npx tsc --noEmit`. Any failure is fixed at root, not gate-weakened. |
| T-12-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed this phase (hard constraint). The fence gate asserts `package.json`/`package-lock.json` diffs are empty. Any executor-initiated install is a plan violation and requires a blocking human checkpoint. |
</threat_model>

<verification>
Phase-level checks after Wave 2 (12-02) completes:
- `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"` → `sweep-clean` (BRND-04, QLTY-04)
- `test -z "$(git diff src/components/ui/sidebar.tsx src/components/ui/dropdown-menu.tsx src/components/ui/tooltip.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx package.json package-lock.json)"` → `fence-clean` (hard constraints)
- `git diff <PHASE_BASE_SHA> HEAD --stat` across the whole phase shows exactly `src/components/layout/app-sidebar.tsx`, `src/lib/user.ts`, `src/lib/user.test.ts` (`<PHASE_BASE_SHA>` = phase-start commit, currently `a79cd5ac`; base-vs-HEAD range because the phase files are already committed per task)
- `npx tsc --noEmit` green; `npx vitest run src/lib/nav.test.ts --bail=1` green (11/11); `npx vitest run src/lib/user.test.ts -x` green (8/8); `npm test` green (≥ 224 passed); `npm run build` green
</verification>

<success_criteria>
Measurable completion (roadmap SC #1-#4 + hard constraints):
- Zero hardcoded indigo/amber/hex/dark: utilities remain in `src/components/layout/` — the sweep gate prints `sweep-clean` (BRND-04, QLTY-04)
- All 8 protected files byte-identical — the fence gate prints `fence-clean`: vendored `sidebar.tsx`/`dropdown-menu.tsx`/`tooltip.tsx`, `globals.css` token block + companion rules, `(dashboard)/layout.tsx` auth gate, `app-shell-layout.tsx` server shell, `package.json`/`package-lock.json` all untouched (zero new packages)
- The whole-phase diff is exactly `app-sidebar.tsx` + `src/lib/user.ts` + `src/lib/user.test.ts`
- Full regression battery green: `npx tsc --noEmit`, `nav.test.ts` 11/11, `user.test.ts` 8/8, `npm test` ≥ baseline, `npm run build`
</success_criteria>

<output>
Create `.planning/phases/12-branding-user-zones/12-02-SUMMARY.md` when done
</output>
