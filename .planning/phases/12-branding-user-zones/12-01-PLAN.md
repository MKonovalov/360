---
phase: 12-branding-user-zones
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/layout/app-sidebar.tsx, src/lib/user.ts, src/lib/user.test.ts]
autonomous: true
requirements: [BRND-01, BRND-02, BRND-03, BRND-04]

must_haves:
  truths:
    - "Top zone: ArcLumen 360 wordmark (15px/600, text-sidebar-foreground) + ArcLumen Partners org label (12px/400, text-sidebar-foreground/70) render in a SidebarHeader (BRND-01, D1)"
    - "Wordmark block fades in the collapsed rail via group-data-[collapsible=icon]:opacity-0 transition-opacity duration-200, matching the primitive's label-fade contract (Q4)"
    - "Bottom zone: signed-in user identity from useUser() — 24px avatar (user.imageUrl when hasImage, else bg-sidebar-primary initials circle) + display name from the tested getUserDisplayName chain (BRND-02)"
    - "A full-width 'Give us feedback' pill above the user zone routes to the D2 static mailto via a semantic <a>, with a SidebarSeparator divider (BRND-03, Q5)"
    - "User dropdown portals to document.body with global popover tokens — display name + 'Signed in as {email}' + Sign out → /sign-in (Q1, D4)"
    - "Branding and user zones use sidebar tokens only; the portaled dropdown uses global tokens; zero indigo/amber/hex/dark: in the diff (BRND-04)"
  artifacts:
    - path: "src/lib/user.ts"
      provides: "getUserDisplayName/getUserInitials pure functions + UserDisplayFields structural interface (nullability lock for the sidebar user zone)"
      exports: ["getUserDisplayName", "getUserInitials"]
      contains: "interface UserDisplayFields"
    - path: "src/lib/user.test.ts"
      provides: "Vitest nullability lock for the display-name/initials fallback chains (mirrors nav.test.ts convention)"
      contains: "describe('getUserDisplayName'"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "SidebarHeader branding zone + SidebarFooter user zone (feedback pill + divider + user dropdown)"
      contains: "SidebarHeader"
      min_lines: 170
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/lib/user.ts"
      via: "import { getUserDisplayName, getUserInitials } from '@/lib/user' — display logic delegates to tested pure functions"
      pattern: "getUserDisplayName"
    - from: "src/components/layout/app-sidebar.tsx (SidebarFooter user zone)"
      to: "@clerk/nextjs useUser"
      via: "useUser() discriminated-union guard — identity comes from the Clerk client context behind requireStaffAccess; server frame renders the identical empty state"
      pattern: "useUser"
    - from: "src/components/layout/app-sidebar.tsx (feedback pill)"
      to: "FEEDBACK_MAILTO (module-level static constant)"
      via: "href={FEEDBACK_MAILTO} — D2 mailto, never user-interpolated"
      pattern: "FEEDBACK_MAILTO"
    - from: "src/components/layout/app-sidebar.tsx (DropdownMenuContent)"
      to: "src/components/ui/dropdown-menu.tsx (Portal)"
      via: "bg-popover text-popover-foreground at document.body — global tokens outside the [data-sidebar=sidebar] subtree (D4)"
      pattern: "DropdownMenuContent"
---

<objective>
Complete the sidebar anatomy per the APPROVED Phase 12 UI-SPEC (the locked design contract): add a top branding zone in a `SidebarHeader` — ArcLumen 360 wordmark (15px/600) + ArcLumen Partners org label (12px/400, `/70`) with the Q4 collapsed-rail fade (BRND-01, D1) — and a bottom user zone in a `SidebarFooter` — a full-width "Give us feedback" pill routing to the D2 static mailto (BRND-03), a `SidebarSeparator` divider (Q5), and a `DropdownMenu`-wrapped user row showing the signed-in Clerk identity via `useUser()`: 24px avatar (imageUrl when `hasImage`, else a `bg-sidebar-primary` initials circle) + display name, with a sign-out item that redirects to `/sign-in` (BRND-02, Q1/Q2, D4). The display-name/initials logic is extracted into tested pure functions in `src/lib/user.ts` (BRND-02 regression lock, mirroring the Phase 10 `getActiveNavKey` convention).

Purpose: This is the final consumer phase of the sidebar restyle — it consumes the Phase 10 `--sidebar-*` token set and the Phase 11 nav anatomy to complete the panel chrome (brand top, identity bottom), closing the app's zero-sign-out gap (grep-verified: no `SignOutButton`/`signOut`/`useClerk` anywhere in `src/` — this dropdown is the first in-app sign-out path). Hard constraints honored: zero new npm packages; zero edits to `src/components/ui/sidebar.tsx`, `dropdown-menu.tsx`, `tooltip.tsx`; zero edits to the `globals.css` token block; no `.dark`/`dark:` variants; routes, `pendingCount` gating, and the collapse/resize/cookie contract frozen (Phase 13 owns collapse — Phase 12 only pre-wires dormant `group-data-[collapsible=icon]:` classes).

Output: `src/lib/user.ts` + `src/lib/user.test.ts` (pure functions + nullability lock) and the two new zones in `src/components/layout/app-sidebar.tsx` — the complete Phase 12 source diff. Live-browser rendering verification is Phase 14's UAT contract (Q6); this plan verifies via type gates, unit locks, and source-assertion grep gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (§Phase 12 — success criteria 1-4)
@.planning/STATE.md (§Accumulated Context — Phase 10/11 decisions; §Session Continuity — 12-UI-SPEC approved)
@.planning/REQUIREMENTS.md (BRND-01..04)
@.planning/phases/12-branding-user-zones/12-UI-SPEC.md (§Phase Decisions D1-D4; §Open-Question Resolutions Q1-Q6; §Typography; §Spacing; §Color; §Interaction & State Contract; §Copywriting Contract; §Verification Gates)
@.planning/phases/12-branding-user-zones/12-RESEARCH.md (§Summary; §Architectural Responsibility Map; §Standard Stack — zero installs; §Pattern 1 useUser guard; §Pattern 2 dormant collapse classes; §Don't Hand-Roll; §Common Pitfalls 1-6; §Code Examples; §Security Domain)
@.planning/phases/12-branding-user-zones/12-PATTERNS.md (§src/lib/user.ts template — structural UserDisplayFields, NOT @clerk/types; §app-sidebar.tsx insertion points; §Vendored primitives contracts; §DropdownMenu composition — explorer-menu.tsx / enrichment-review-dialog.tsx; §Client-identity guard chain)
@.planning/phases/12-branding-user-zones/12-VALIDATION.md (per-task map rows 12-01-01..04 — grep-gate expectations; sampling rate)
@.planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md (§D1-D4 locked decisions — wordmark treatment, mailto destination, 48px collapse, portal policy)
@src/components/layout/app-sidebar.tsx (the file being modified — 109 lines, current state)
@src/lib/nav.ts + @src/lib/nav.test.ts (the pure-function/test precedent this plan mirrors)
@src/components/ui/sidebar.tsx (vendored primitive — READ ONLY; SidebarHeader 331-340, SidebarFooter 342-351, SidebarSeparator 353-365, sidebarMenuButtonVariants 468-488, SidebarGroupLabel fade 404)
@src/components/ui/dropdown-menu.tsx (vendored — READ ONLY; DropdownMenuContent Portal + bg-popover at 34-51, DropdownMenuLabel 161-179, DropdownMenuSeparator 181-192)
@src/components/explorer/explorer-menu.tsx (canonical DropdownMenu composition — trigger asChild + item asChild)
@src/components/enrichment/enrichment-review-dialog.tsx (onSelect action-item pattern — the sign-out fallback shape)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract src/lib/user.ts pure display functions + src/lib/user.test.ts nullability lock (BRND-02 regression lock)</name>
  <files>src/lib/user.ts, src/lib/user.test.ts</files>
  <read_first>
    - src/lib/nav.ts — the exact analog: why-comment block, named exports only, total pure function, no imports. The shape `user.ts` must mirror (CONVENTIONS: single quotes, semicolons, 2-space indent, no JSDoc).
    - src/lib/nav.test.ts — the exact analog test: `import { describe, it, expect } from 'vitest';` + relative import of the module under test + one `describe` per function + `expect(...).toBe(...)`.
    - .planning/phases/12-branding-user-zones/12-PATTERNS.md §src/lib/user.ts — the VERIFIED finding that `@clerk/types` is NOT directly resolvable (no node_modules/@clerk/types at top level or under @clerk/nextjs) and the mandated `interface UserDisplayFields` structural slice (username/fullName/firstName/lastName: string|null + primaryEmailAddress: { emailAddress: string } | null), plus the exact function bodies.
    - .planning/phases/12-branding-user-zones/12-UI-SPEC.md §Copywriting Contract (avatar fallback name row — the terminal 'User' fallback chain) and §Typography (Display 10px/600 initials glyph — Geist sans, not mono).
    - vitest.config.ts — include `src/**/*.test.ts` (line 12): the test file MUST live at exactly `src/lib/user.test.ts` to be auto-discovered.
  </read_first>
  <action>
    Create `src/lib/user.ts` — a dependency-free pure-function module mirroring `src/lib/nav.ts`:
    - Open with a 3-4 line why-comment: every Clerk display field (username/fullName/firstName/lastName) is individually nullable and often unset for email/social sign-ins, so the fallback chain is the regression lock — a null field must never render a blank row. `@clerk/types` is not directly resolvable, so the param is a local structural interface matching only the fields the display logic reads.
    - Declare and export `interface UserDisplayFields { username: string | null; fullName: string | null; firstName: string | null; lastName: string | null; primaryEmailAddress: { emailAddress: string } | null; }` — do NOT import `UserResource` from `@clerk/types` (verified not resolvable; the real Clerk UserResource is structurally compatible with these fields).
    - `export function getUserDisplayName(user: UserDisplayFields): string` returning `user.username ?? user.fullName ?? user.primaryEmailAddress?.emailAddress ?? 'User'` — terminal fallback 'User' means the function never returns an empty string.
    - `export function getUserInitials(user: UserDisplayFields): string` — trim firstName/lastName; if either is non-empty return `${first?.[0] ?? ''}${last?.[0] ?? ''}`.toUpperCase(); else if email is non-empty return `email.slice(0, 2).toUpperCase()`; else return `'A'` — terminal fallback, never empty.
    - Named exports only; no default export; no imports from the repo; no JSDoc.
    Create `src/lib/user.test.ts` — mirroring `src/lib/nav.test.ts`:
    - `import { describe, it, expect } from 'vitest';` and ONE merged import line `import { getUserDisplayName, getUserInitials, type UserDisplayFields } from './user';` (relative import — the repo test convention; the inline `type` modifier keeps the module under test on exactly one `'./user'` import line, so the gate `grep -Fc "from './user'"` = 1 holds).
    - A `baseUser` fixture typed `UserDisplayFields` with all five fields null — NO `as any` (the exported interface makes casts unnecessary).
    - `describe('getUserDisplayName')` — four cases: prefers username when present; falls back to fullName when username is null; falls back to email when both names are null; never returns an empty string (all-null → 'User').
    - `describe('getUserInitials')` — four cases: combines first+last initials (firstName 'Jane', lastName 'Doe' → 'JD'); single name yields one letter (firstName 'Jane' only → 'J'); derives from email when names are null ('jane@x.com' → 'JA'); all-null → 'A'.
    Run `npx vitest run src/lib/user.test.ts -x` — both files land together (function-first order, NOT a RED tree: the repo's next build type-checks test files, so the tree must never be red between commits).
  </action>
  <verify>
    <automated>
      npx vitest run src/lib/user.test.ts -x
      npx tsc --noEmit
      grep -Fc 'interface UserDisplayFields' src/lib/user.ts
      # expect 1
      grep -Fc "from '@clerk/types'" src/lib/user.ts
      # expect 0 — structural interface mandated, Clerk type import forbidden
      grep -Fc "from './user'" src/lib/user.test.ts
      # expect 1 — relative import per repo test convention
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/user.test.ts -x` exits 0 with all 8 cases passing (4 display-name + 4 initials).
    - `npx tsc --noEmit` exits 0.
    - `interface UserDisplayFields` present in user.ts; zero `@clerk/types` imports anywhere in user.ts.
    - `getUserDisplayName` uses the exact chain `username ?? fullName ?? primaryEmailAddress?.emailAddress ?? 'User'` (terminal 'User'); `getUserInitials` uses first/last trimmed initials → email slice(0,2) → 'A'; both never return ''.
    - Named exports only; no default export; no JSDoc; single quotes + semicolons per CONVENTIONS.
  </acceptance_criteria>
  <done>user.ts extracts the pure display functions with the structural UserDisplayFields interface; user.test.ts locks the nullability fallback chains green (8/8); tsc clean; the file pair is committed atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Add branding zone (SidebarHeader wordmark + org label + Q4 fade) + feedback pill + SidebarSeparator (BRND-01, BRND-03, BRND-04)</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx — the file being modified. Current state: `<Sidebar>` at line 31, `<SidebarContent>` at line 32 (flex-1, sidebar.tsx:373), `</Sidebar>` at line 107. Import block lines 1-16. The pendingCount why-comments at lines 18-25 stay untouched.
    - .planning/phases/12-branding-user-zones/12-UI-SPEC.md §D1 (wordmark "ArcLumen 360" 15px/600 text-sidebar-foreground + org label "ArcLumen Partners" 12px/400 text-sidebar-foreground/70), §D2 (mailto constant verbatim), §Q3 (pill anatomy — SidebarMenuButton asChild, h-9 rounded-[6px] border border-sidebar-border text-[14px] font-normal, zero explicit hover classes, dormant Mail icon + aria-label), §Q4 (fade wrapper group-data-[collapsible=icon]:opacity-0 transition-opacity duration-200, header gap-1 p-3), §Q5 (SidebarSeparator mx-2 w-auto bg-sidebar-border).
    - src/components/ui/sidebar.tsx lines 331-340 (SidebarHeader = bare `flex flex-col gap-2 p-2` div — self-apply collapse classes), 342-351 (SidebarFooter, same bare shape), 353-365 (SidebarSeparator — already tokenized), 468-488 (sidebarMenuButtonVariants — default variant carries hover:bg-sidebar-accent hover:text-sidebar-accent-foreground; the pill must add ZERO explicit hover classes). READ ONLY.
    - .planning/phases/12-branding-user-zones/12-RESEARCH.md §Common Pitfalls Pitfall 6 (grep-gate hygiene — comments must NOT contain the literal class strings the gates count) and Pitfall 5 (semantic <a>, static mailto constant).
  </read_first>
  <action>
    Edit `src/components/layout/app-sidebar.tsx` to add the branding zone and the feedback pill. Changes:
    1. Extend the existing `'@/components/ui/sidebar'` import (lines 7-16) to add `SidebarHeader`, `SidebarFooter`, and `SidebarSeparator` (alphabetical placement within the block). All three are used in this task — the footer element itself is rendered in step 5 — so no import is unused between commits.
    2. Add `Mail` to the existing lucide-react import (line 5): `import { Building2, Inbox, LayoutDashboard, Mail, Users } from 'lucide-react';` — bare-name style, matches the file's existing import shape.
    3. Add a module-level constant ABOVE `export function AppSidebar` (after the existing why-comments): `const FEEDBACK_MAILTO = 'mailto:hello@arclumenpartners.com?subject=360%20sidebar%20feedback';` with a 2-3 line why-comment above it stating: the destination is locked (D2) and is a static module-level constant so it can never be user-interpolated into a URL/header (ASVS V5). CRITICAL GREP HYGIENE: this comment and ALL comments in this file must NOT contain the literal mailto string, the copy strings (`ArcLumen 360`, `ArcLumen Partners`, `Give us feedback`), or any class strings the gates count — describe mechanisms without quoting literals (11-02 Rule 1).
    4. Insert the branding zone immediately after `<Sidebar>` (line 31), BEFORE `<SidebarContent>`:
       - `<SidebarHeader className="gap-1 p-3">` (Q4 spacing — 4px wordmark→org gap, 12px header padding).
       - Inside, ONE wrapper div with `className="group-data-[collapsible=icon]:opacity-0 transition-opacity duration-200"` (Q4 fade — mirrors SidebarGroupLabel's own label-fade at sidebar.tsx:404; the wrapper is the Phase 13 letter-mark swap seam). The div contains exactly two `<p>` elements (decorative chrome — NOT `<h1>`/`<h2>`, per research Anti-Pattern "no heading semantics"):
         - `<p className="text-[15px] font-semibold text-sidebar-foreground">ArcLumen 360</p>` (D1 wordmark — 15px/600, token color)
         - `<p className="text-xs font-normal text-sidebar-foreground/70">ArcLumen Partners</p>` (D1 org label — 12px/400, /70 opacity)
    5. Insert the feedback pill into the footer: append a `<SidebarFooter>` after `</SidebarContent>` (line 106) and before `</Sidebar>` (line 107) containing:
       - `<SidebarMenu>` with one `<SidebarMenuItem>` holding `<SidebarMenuButton asChild className="h-9 rounded-[6px] border border-sidebar-border text-[14px] font-normal">` (Q3 anatomy — 36px height, 6px radius, gray-200 hairline border, 14px/400; NO hover classes — the primitive's default variant supplies hover:bg-sidebar-accent hover:text-sidebar-accent-foreground automatically). The asChild child is a semantic `<a href={FEEDBACK_MAILTO} aria-label="Give us feedback">` (Q3 — the aria-label is the accessible name for the collapsed-rail icon-only form) containing:
         - `<span className="group-data-[collapsible=icon]:hidden">Give us feedback</span>` (text span — hidden in the collapsed rail)
         - `<Mail aria-hidden="true" className="hidden group-data-[collapsible=icon]:block size-4" />` (dormant icon — hidden expanded, visible in the 48px rail; aria-hidden since the `<a>` carries the label)
       - `<SidebarSeparator />` immediately after the pill (Q5 — one line; the vendored primitive already supplies `mx-2 w-auto bg-sidebar-border` = Exa gray-200 divider).
       - Leave the user-zone slot EMPTY after the separator — Task 3 fills it.
    Do NOT touch: the Explore/Manage groups, `getActiveNavKey` logic, the pendingCount badge block, the existing why-comments, or any file other than app-sidebar.tsx.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      grep -Fc 'ArcLumen 360' src/components/layout/app-sidebar.tsx
      # expect 1 (wordmark p; comments must not add a second)
      grep -Fc 'ArcLumen Partners' src/components/layout/app-sidebar.tsx
      # expect 1 (org label p)
      grep -Fc 'text-[15px] font-semibold text-sidebar-foreground' src/components/layout/app-sidebar.tsx
      # expect 1 (wordmark — scoped pattern; Task 3's dropdown name span uses text-[15px] font-semibold WITHOUT text-sidebar-foreground, so this stays 1 through the phase)
      grep -Fc 'text-sidebar-foreground/70' src/components/layout/app-sidebar.tsx
      # expect 1 (org label)
      grep -Fc 'mailto:hello@arclumenpartners.com?subject=360%20sidebar%20feedback' src/components/layout/app-sidebar.tsx
      # expect 1 (FEEDBACK_MAILTO constant line only — href uses the constant, comments must not quote the literal)
      grep -Fc 'FEEDBACK_MAILTO' src/components/layout/app-sidebar.tsx
      # expect 2 (constant declaration + href usage)
      grep -Fc 'Give us feedback' src/components/layout/app-sidebar.tsx
      # expect 2 (visible span + aria-label; comments must not add a third)
      grep -Fc 'group-data-[collapsible=icon]:opacity-0' src/components/layout/app-sidebar.tsx
      # expect 1 (Q4 fade wrapper)
      grep -Fc 'hidden group-data-[collapsible=icon]:block' src/components/layout/app-sidebar.tsx
      # expect 1 (Mail icon dormant form)
      grep -Fc '<Mail' src/components/layout/app-sidebar.tsx
      # expect 1 (icon usage)
      grep -Fc 'SidebarSeparator' src/components/layout/app-sidebar.tsx
      # expect 2 (import + usage)
      grep -Fc 'SidebarFooter' src/components/layout/app-sidebar.tsx
      # expect 2 (import + usage — the footer element rendered in step 5)
      npx vitest run src/lib/nav.test.ts --bail=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - D1 copy/typography: `ArcLumen 360` = 1, `ArcLumen Partners` = 1, `text-[15px] font-semibold text-sidebar-foreground` = 1, `text-sidebar-foreground/70` = 1 — the wordmark block matches D1 verbatim.
    - Q4 fade: `group-data-[collapsible=icon]:opacity-0 transition-opacity duration-200` wrapper present on the wordmark+org block inside `SidebarHeader className="gap-1 p-3"`.
    - D2 mailto: the FEEDBACK_MAILTO module-level constant contains the exact locked string (grep = 1); `href={FEEDBACK_MAILTO}` uses the constant, never an interpolated literal.
    - Q3 pill: `SidebarMenuButton asChild` with `h-9 rounded-[6px] border border-sidebar-border text-[14px] font-normal`, semantic `<a>` child with `aria-label="Give us feedback"`, text span hidden in collapsed rail, Mail icon visible only in collapsed rail (`hidden group-data-[collapsible=icon]:block size-4 aria-hidden`); ZERO explicit hover classes on the pill.
    - Q5 divider: `<SidebarSeparator />` present between pill and the (empty, Task-3) user zone.
    - `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 with 11/11 — nav regression lock holds.
    - No file other than app-sidebar.tsx modified in this task; vendored primitives untouched.
  </acceptance_criteria>
  <done>Branding zone (D1 wordmark + org label, Q4 fade) in SidebarHeader; feedback pill (D2 mailto via FEEDBACK_MAILTO, Q3 dormant collapsed form) + SidebarSeparator (Q5) in SidebarFooter; nav lock 11/11 green.</done>
</task>

<task type="auto">
  <name>Task 3: Add user zone — useUser() identity guard + avatar/initials + sign-out dropdown (BRND-02, BRND-04)</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx — post-Task-2 state: SidebarHeader branding zone, SidebarFooter with pill + SidebarSeparator and an EMPTY user-zone slot after the separator; import block now includes SidebarHeader/SidebarFooter/SidebarSeparator + Mail.
    - .planning/phases/12-branding-user-zones/12-UI-SPEC.md §Q1 (user-zone trigger anatomy — full-width SidebarMenuButton size="lg", 24px avatar + 15px/400 display name, gap-2.5; dropdown items exactly: label name 15px/600 + "Signed in as {email}" 12px/400 text-muted-foreground / separator / Sign out via SignOutButton redirectUrl="/sign-in"; side="top" align="start" w-56; NO "Manage account"), §Q2 (avatar — plain img src={user.imageUrl} alt="" size-6 rounded-full when hasImage, else initials circle bg-sidebar-primary text-sidebar-primary-foreground text-[10px] font-semibold; NOT next/image, NOT Clerk UserAvatar), §D4 (portaled content uses GLOBAL tokens bg-popover/text-popover-foreground/text-muted-foreground — correct because the portal leaves the [data-sidebar=sidebar] subtree), §Interaction & State Contract (user trigger aria-label={getUserDisplayName(user)}; collapsed: name span hidden, avatar survives; loading: identical empty frame).
    - .planning/phases/12-branding-user-zones/12-PATTERNS.md §Client-identity guard chain (useUser() three-branch discriminated union — isLoaded/isSignedIn/user; `user` is undefined before isLoaded; any user.* access before the guard throws), §DropdownMenu composition (explorer-menu.tsx:24-50 trigger asChild + item asChild; enrichment-review-dialog.tsx:183-199 onSelect action-item — the SignOutButton fallback shape), §Vendored primitives (DropdownMenuContent 34-51 portal + bg-popover; DropdownMenuLabel 161-179 default text-xs font-medium text-muted-foreground — override for 15px/600 name).
    - src/components/ui/dropdown-menu.tsx — READ ONLY; verify the exported names DropdownMenu/DropdownMenuTrigger/DropdownMenuContent/DropdownMenuLabel/DropdownMenuSeparator/DropdownMenuItem at the export block (line 253+).
    - .planning/phases/12-branding-user-zones/12-RESEARCH.md §Common Pitfalls Pitfall 1 (hydration — never render Skeleton on server + content on client; identical empty frame), Pitfall 4 (avatar colors from tokens only — Exa's #C3ECFF/text-blue-800 is a QLTY-04 violation), Pitfall 6 (comment hygiene), Assumption A3 (SignOutButton Slot-clone fallback: useClerk().signOut({ redirectUrl: '/sign-in' }) on onSelect — verified available).
    - src/app/sign-in/[[...sign-in]]/page.tsx — exists; the redirectUrl="/sign-in" target route is real.
  </read_first>
  <action>
    Edit `src/components/layout/app-sidebar.tsx` to fill the user-zone slot. Changes:
    1. Extend imports: `SidebarHeader`, `SidebarFooter`, `SidebarSeparator` are already imported from `'@/components/ui/sidebar'` (added in Task 2 — do NOT re-add). Add `import { useUser, SignOutButton } from '@clerk/nextjs';`; add `import { DropdownMenu, DropdownMenuContent, DropdownMenuLabel, DropdownMenuSeparator, DropdownMenuItem, DropdownMenuTrigger } from '@/components/ui/dropdown-menu';`; add `import { getUserDisplayName, getUserInitials } from '@/lib/user';` (matches the file's @/ alias style).
    2. In the AppSidebar function body (after `const activeKey = getActiveNavKey(pathname);`), add the identity hook and guard chain with a 3-4 line why-comment. The comment must NOT contain the literal substring `useUser` (the acceptance gate counts it — describe the hook as "the identity hook" instead; 11-02 Rule 1). Comment content: the identity hook returns a three-branch discriminated union — before `isLoaded` the server frame and first hydrate tick must render the identical empty user zone (no hydration mismatch, no blank flash); the route is server-gated by requireStaffAccess so isSignedIn is true after load, but the guards are still mandatory for TS narrowing. The hook call itself is `const { isLoaded, isSignedIn, user } = useUser();`. Render the user zone CONDITIONALLY in the footer (`{isLoaded && isSignedIn && user && (…user zone JSX…)}`) so the server frame and loading tick render nothing for the zone while the nav renders normally — this is the "identical empty frame" contract scoped to the zone, not the whole sidebar.
    3. In the SidebarFooter (post-Task-2), after the `<SidebarSeparator />`, wrap the user zone in `<SidebarMenu>` + `<SidebarMenuItem>` and fill it:
       - `<DropdownMenu>` containing:
         - `<DropdownMenuTrigger asChild>` wrapping `<SidebarMenuButton size="lg" aria-label={getUserDisplayName(user)} className="gap-2.5 group-data-[collapsible=icon]:justify-center">` (Q1 trigger — primitive size lg = h-12; gap-2.5 = 10px Exa gap; aria-label = display name per the Interaction contract; the dormant justify-center centers the avatar in the 32px collapsed rail box). Inside the button, in order:
           - avatar: `{user.hasImage ? (<img src={user.imageUrl} alt="" className="size-6 rounded-full" />) : (<span className="flex size-6 items-center justify-center rounded-full bg-sidebar-primary text-sidebar-primary-foreground text-[10px] font-semibold">{getUserInitials(user)}</span>)}` (Q2 — plain img, NOT next/image [no images.remotePatterns] and NOT Clerk UserAvatar [no className prop → BRND-04 blocker]; alt="" is correct — the adjacent name carries identity; initials circle = letter-mark language bg-sidebar-primary/text-sidebar-primary-foreground 12.63:1; the 24px avatar survives the collapsed rail)
           - `<span className="group-data-[collapsible=icon]:hidden text-[15px] font-normal text-sidebar-foreground">{getUserDisplayName(user)}</span>` (Q1 display name 15px/400 — hidden in the collapsed rail)
         - `<DropdownMenuContent side="top" align="start" className="w-56">` (Q1 — portals to document.body via the primitive's Portal with bg-popover text-popover-foreground, D4 global tokens; side=top opens upward over the light panel; w-56 = 224px fixed width). Inside, exactly three items (Q1):
           - `<DropdownMenuLabel className="flex flex-col gap-0.5">` containing `<span className="text-[15px] font-semibold">{getUserDisplayName(user)}</span>` (name 15px/600 — overrides the primitive's default text-xs font-medium text-muted-foreground; color inherits text-popover-foreground from the content) and `<span className="text-xs font-normal text-muted-foreground">Signed in as {user.primaryEmailAddress?.emailAddress}</span>` (email 12px/400 muted — nullable chain, renders nothing when email is null)
           - `<DropdownMenuSeparator />`
           - `<DropdownMenuItem asChild><SignOutButton redirectUrl="/sign-in">Sign out</SignOutButton></DropdownMenuItem>` (Q1 item ③ — SignOutButton with redirectUrl="/sign-in" (route exists at src/app/sign-in/[[...sign-in]]); the app's first sign-out path). FALLBACK if Slot-cloning a Clerk component warns (RESEARCH A3): replace with `<DropdownMenuItem onSelect={() => useClerk().signOut({ redirectUrl: '/sign-in' })}>Sign out</DropdownMenuItem>` and import `useClerk` from '@clerk/nextjs' — the enrichment-review-dialog.tsx:183-199 onSelect precedent. Do NOT invent a custom logout endpoint; Clerk owns the session lifecycle.
       - Do NOT add a "Manage account" item (Q1 exclusion — verified no /user-profile route exists).
    4. Add a 3-4 line why-comment above the DropdownMenu explaining D4: the content portals to document.body OUTSIDE the [data-sidebar="sidebar"] subtree, so scoped sidebar tokens do not resolve there — the menu intentionally uses global popover tokens (this is correct per D4, not a token leak).
    5. CRITICAL GREP HYGIENE: no comment in this file may contain the literal class strings the gates count (`size-6`, `bg-sidebar-primary`, `text-sidebar-primary-foreground`, `w-56`, `text-[15px]`, `Signed in as`, `useUser`, `SignOutButton`, `hasImage`), the copy strings, or swept tokens (indigo/amber/hex/dark:) — describe mechanisms without quoting literals (11-02 Rule 1). The dropdown name span is `text-[15px] font-semibold` WITHOUT `text-sidebar-foreground` so the Task-2 D1 typography gate (scoped pattern with text-sidebar-foreground) stays at 1.
    Do NOT touch: the branding zone, nav groups, pendingCount badge, or any file other than app-sidebar.tsx.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      grep -Fc 'useUser' src/components/layout/app-sidebar.tsx
      # expect 2 (import + hook call)
      grep -Fc 'hasImage' src/components/layout/app-sidebar.tsx
      # expect 1 (avatar conditional)
      grep -Fc 'bg-sidebar-primary' src/components/layout/app-sidebar.tsx
      # expect 1 (initials circle fill)
      grep -Fc 'text-sidebar-primary-foreground' src/components/layout/app-sidebar.tsx
      # expect 1 (initials circle glyph)
      grep -Fc 'size-6' src/components/layout/app-sidebar.tsx
      # expect 2 (avatar img + initials circle)
      grep -Fc 'SignOutButton' src/components/layout/app-sidebar.tsx
      # expect 2 (import + usage) — OR useClerk fallback: grep -Fc 'useClerk' expect 2 (import + usage)
      grep -cE "redirectUrl[=:].*sign-in" src/components/layout/app-sidebar.tsx
      # expect 1 — matches EITHER sanctioned sign-out form: SignOutButton's redirectUrl="/sign-in" (=) or the useClerk().signOut({ redirectUrl: '/sign-in' }) fallback (:)
      grep -Fc 'Signed in as' src/components/layout/app-sidebar.tsx
      # expect 1 (dropdown label email line)
      grep -Fc 'w-56' src/components/layout/app-sidebar.tsx
      # expect 1 (DropdownMenuContent className)
      grep -Fc 'side="top"' src/components/layout/app-sidebar.tsx
      # expect 1
      grep -Fc 'Manage account' src/components/layout/app-sidebar.tsx
      # expect 0 — Q1 explicit exclusion
      grep -Fc 'getUserDisplayName' src/components/layout/app-sidebar.tsx
      # expect >= 4 (import + trigger aria-label + trigger name span + dropdown label name span)
      test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)" && echo sweep-clean
      npx vitest run src/lib/user.test.ts -x
      npx vitest run src/lib/nav.test.ts --bail=1
      git diff <PHASE_BASE_SHA> HEAD --stat
      # <PHASE_BASE_SHA> = phase-start commit (immediately before 12-01 Task 1's first source commit; currently a79cd5ac)
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `useUser` import + hook call present (grep = 2); `hasImage` = 1; the user zone renders conditionally on `isLoaded && isSignedIn && user` — the server frame and loading tick render an identical empty zone (no hydration mismatch).
    - Avatar: plain `<img src={user.imageUrl} alt="" className="size-6 rounded-full" />` when hasImage, else initials circle `bg-sidebar-primary text-sidebar-primary-foreground text-[10px] font-semibold` — token-only (12.63:1), no next/image, no Clerk UserAvatar, no hardcoded colors.
    - Trigger: `SidebarMenuButton size="lg"` with `aria-label={getUserDisplayName(user)}`, `gap-2.5`, dormant `group-data-[collapsible=icon]:justify-center`; name span `group-data-[collapsible=icon]:hidden text-[15px] font-normal text-sidebar-foreground`.
    - Dropdown: `DropdownMenuContent side="top" align="start" className="w-56"` with exactly three items — label (name 15px/600 + "Signed in as {email}" 12px/400 text-muted-foreground, nullable email chain), separator, Sign out via `SignOutButton redirectUrl="/sign-in"` (or the useClerk onSelect fallback). `Manage account` = 0.
    - Sweep gate prints `sweep-clean` — zero indigo/amber/hex/dark: across src/components/layout/ (BRND-04, QLTY-04).
    - `npx vitest run src/lib/user.test.ts -x` green (8/8) and `npx vitest run src/lib/nav.test.ts --bail=1` green (11/11) — both regression locks hold.
    - `git diff <PHASE_BASE_SHA> HEAD --stat` shows exactly `src/components/layout/app-sidebar.tsx`, `src/lib/user.ts`, `src/lib/user.test.ts` from this plan — vendored primitives, globals.css, app-shell-layout.tsx, package.json untouched. `<PHASE_BASE_SHA>` = the phase-start commit (the commit immediately before 12-01 Task 1's first source commit; resolve via `git log --oneline` — it is the `docs(12): create phase plan` commit, currently `a79cd5ac` in this repo). Because 12-01 commits per task, an UNQUALIFIED `git diff --stat` (working tree vs HEAD) is vacuous — the phase files are already committed, so always use the base-vs-HEAD range.
  </acceptance_criteria>
  <done>User zone shows the signed-in identity from useUser() — 24px avatar (image or token initials circle) + display name — with a portaled sign-out dropdown (D4 global tokens); the app's first in-app sign-out path redirects to /sign-in; sweep + both unit locks green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clerk user profile fields → sidebar DOM | `user.username`/`fullName`/`firstName`/`lastName`/`emailAddress` are user-controlled profile data rendered as React text nodes in the footer zone and dropdown |
| mailto href → OS mail client | The feedback pill's `href` leaves the app; the D2 destination is a static constant |
| avatar `img` src → img.clerk.com CDN | `user.imageUrl` is served from Clerk's controlled CDN; the src is only rendered when `user.hasImage` |
| Portaled dropdown content → document.body | DropdownMenuContent renders OUTSIDE the `[data-sidebar="sidebar"]` subtree — sidebar tokens do not resolve there; global popover tokens are used by design (D4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering / Info Disclosure | `{user.*}` interpolations in app-sidebar.tsx (display name, email, initials) | mitigate | Stored XSS via a malicious profile field: React auto-escapes all text-node interpolations (Task 3 renders every user field as `{expr}` JSX text); the plan forbids `dangerouslySetInnerHTML` anywhere and dynamic class strings; the display functions in src/lib/user.ts return plain strings, never markup. Grep gates assert the pure-function delegation (`getUserDisplayName` >= 4) and the tsc gate validates the discriminated-union narrowing so `user` is never accessed before `isLoaded`. |
| T-12-02 | Tampering | mailto href (feedback pill) | mitigate | Mailto header/URL injection: `FEEDBACK_MAILTO` is a module-level static constant (D2 verbatim, grep = 1) and the pill uses `href={FEEDBACK_MAILTO}` — no user data is ever concatenated into the mailto. A future edit parameterizing it must be flagged in review (comment warns: static, never user-interpolated). |
| T-12-03 | Spoofing | avatar `<img src>` | mitigate | Identity spoofing via avatar URL: `src` is only `user.imageUrl` (Clerk's controlled CDN) and only when `user.hasImage` is true (grep = 1); `alt=""` (decorative — the adjacent display name carries identity). No user-supplied URL string reaches the src. A failed image renders a broken element (no onError handler per locked Q2) — accepted cosmetic edge, Phase 14 visual audit surface. |
| T-12-04 | Elevation of Privilege | user zone visibility | accept | Auth-state regression (user zone for anonymous visitors): structurally impossible — the (dashboard) route group is server-gated by `requireStaffAccess()` before AppSidebar renders, and the client zone additionally guards `isLoaded && isSignedIn && user` (renders nothing otherwise). No client-side authorization decision is made; the server gate stays authoritative. |
| T-12-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed this phase (hard constraint, UI-SPEC §Registry Safety — no `npx shadcn add`, no `npm install`; package.json/package-lock.json diffs must be empty). Any executor-initiated install is a plan violation and requires a blocking human checkpoint before proceeding. |
</threat_model>

<verification>
Phase-level checks for Plan 12-01 (Wave 1 — single Wave-1 plan, no build contention):
- `npx tsc --noEmit` exits 0 (run per task)
- `npx vitest run src/lib/user.test.ts -x` exits 0 with 8/8 (new BRND-02 nullability lock)
- `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 with 11/11 (QLTY-01 regression lock — run in Tasks 2 and 3)
- Grep gates per UI-SPEC §Verification Gates: `ArcLumen 360` = 1, `ArcLumen Partners` = 1, `text-[15px] font-semibold text-sidebar-foreground` = 1, `text-sidebar-foreground/70` = 1, D2 mailto = 1, `Give us feedback` = 2 (visible + aria-label), `useUser` = 2, `hasImage` = 1, `Manage account` = 0
- Sweep gate: `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"` → `sweep-clean`
- `git diff <PHASE_BASE_SHA> HEAD --stat` for this plan shows exactly `src/components/layout/app-sidebar.tsx`, `src/lib/user.ts`, `src/lib/user.test.ts`; vendored `sidebar.tsx`, `dropdown-menu.tsx`, `tooltip.tsx`, `globals.css`, `app-shell-layout.tsx`, `package.json`, `package-lock.json` byte-identical (full fence re-check in 12-02). `<PHASE_BASE_SHA>` = phase-start commit (currently `a79cd5ac`); always use the base-vs-HEAD range because the phase files are already committed per task.
</verification>

<success_criteria>
Measurable completion (roadmap SC #1-#4):
- Top zone: ArcLumen 360 wordmark (15px/600 `text-sidebar-foreground`) + ArcLumen Partners org label (12px/400 `text-sidebar-foreground/70`) in a `SidebarHeader` (gap-1 p-3), with the Q4 collapsed-rail fade wrapper (SC #1, BRND-01, D1)
- Bottom zone: signed-in user identity from `useUser()` — 24px avatar (imageUrl when hasImage, else `bg-sidebar-primary` initials circle) + display name from the tested `getUserDisplayName` chain (SC #2, BRND-02)
- Full-width "Give us feedback" pill above the user zone, semantic `<a>` → D2 static mailto constant, `SidebarSeparator` divider (SC #3, BRND-03)
- Branding + user zones styled with sidebar tokens only in the sidebar subtree; the portaled dropdown uses global popover tokens per D4; zero indigo/amber/hex/dark: in the diff (SC #4, BRND-04)
- Zero new npm packages; zero edits to vendored primitives; zero edits to the globals.css token block; no `.dark`/`dark:` variants (hard constraints)
</success_criteria>

<output>
Create `.planning/phases/12-branding-user-zones/12-01-SUMMARY.md` when done
</output>
