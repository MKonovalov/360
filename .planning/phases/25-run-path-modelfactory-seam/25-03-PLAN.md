---
phase: 25-run-path-modelfactory-seam
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/agents/modelConfig.test.ts]
autonomous: true
requirements: [RUN-04]

must_haves:
  truths:
    - "rate_limited advances ONLY on a cross-provider hop across all 4 providers — the 4×4 matrix has exactly 4 same-provider false cells (anthropic, openrouter, nousresearch, opencode) and 12 cross-provider true cells"
    - "OpenCode Zen↔Go is SAME-provider (one key): a 429 between two opencode models never advances — both snapshot providerIDs resolve to logical 'opencode'"
    - "402 billing stays never-eligible; a null provider identity fail-closes a 429 advance"
  artifacts:
    - path: "src/lib/agents/modelConfig.test.ts"
      provides: "Data-driven 16-cell shouldAdvance matrix + Zen↔Go collision canary"
      contains: "SERVABLE_PROVIDERS"
  key_links:
    - from: "shouldAdvance tests"
      to: "SERVABLE_PROVIDERS (catalog.ts l.102)"
      via: "data-driven loop over the 4-provider set"
      pattern: "SERVABLE_PROVIDERS"
    - from: "modelConfig.ts"
      to: "zero production change"
      via: "verify-only contract (D-25-04)"
      pattern: "git diff"
---

<objective>
Deliver RUN-04 as a **verify-only** widening: zero production code change to `shouldAdvance` (modelConfig.ts stays byte-identical — D-25-04). The deliverable is extending the existing 4-cell matrix tests (modelConfig.test.ts l.151-177) into a data-driven 16-cell matrix over the 4-provider set, plus the OpenCode Zen↔Go same-provider collision canary and the widened non-429/never-eligible loops.

Purpose: RUN-04 — prove `shouldAdvance`'s provider-agnostic predicate (`cls !== 'rate_limited'` → true; else `from !== null && to !== null && from !== to`) already treats Zen↔Go as SAME-provider because `getProviderForModelId` returns logical `opencode` for BOTH `opencode` and `opencode-go` snapshot rows (catalog.ts SNAPSHOT_PROVIDER_IDS l.108-113, verified). Cross-provider 429 advances; same-provider never-advance preserved; 402 billing never eligible.
Output: 16-cell data-driven matrix + canaries, expressed as tests only (Anti-Pattern 3: data-driven, never a 16-branch switch).

No AI-SPEC.md: agent-extension phase per the planning constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-run-path-modelfactory-seam/25-CONTEXT.md
@.planning/phases/25-run-path-modelfactory-seam/25-RESEARCH.md
@.planning/phases/25-run-path-modelfactory-seam/25-PATTERNS.md
@.planning/phases/25-run-path-modelfactory-seam/25-VALIDATION.md
@src/lib/agents/modelConfig.ts
@src/lib/agents/modelConfig.test.ts
@src/lib/models/catalog.ts

# Wave 0 test-gap reference (25-VALIDATION.md): modelConfig.test.ts 4-cell → 16-cell widening is a Wave 0 gap; task ID 25-03-01 (RUN-04).
# D-25-04 verify-only: modelConfig.ts must NOT be modified. The 4-cell matrix lives at modelConfig.test.ts l.151-177.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the 4-cell shouldAdvance matrix to a data-driven 16-cell matrix over the 4-provider set (Wave 0 gap 25-03-01)</name>
  <files>src/lib/agents/modelConfig.test.ts</files>
  <read_first>
    - src/lib/agents/modelConfig.test.ts (the file being modified — the 4-cell matrix l.151-157, the data-driven loop style l.159-164, never-eligible loop l.166-170, fail-closed null tests l.172-176)
    - src/lib/agents/modelConfig.ts (the verify-only target — shouldAdvance l.100-107; READ ONLY, do not edit)
    - src/lib/models/catalog.ts (SERVABLE_PROVIDERS l.102 — the 4-entry iteration order; SNAPSHOT_PROVIDER_IDS l.108-113 — the opencode dual-id collapse)
    - 25-PATTERNS.md §modelConfig.test.ts assignment (l.272-296) — the widening shape + data-driven loop to copy
  </read_first>
  <action>
    Widen the `shouldAdvance — FAL-03 4-cell matrix` describe block (l.151-177) into the 16-cell data-driven matrix. Data-driven loops only — NEVER hand-encode a 16-branch switch (Anti-Pattern 3 / research ARCHITECTURE.md Anti-Pattern 3).

    1. Add `SERVABLE_PROVIDERS` to the existing `import { FAST_MODEL_ID } from '@/lib/models/catalog';` (l.11) — the import becomes `import { FAST_MODEL_ID, SERVABLE_PROVIDERS } from '@/lib/models/catalog';`.
    2. Replace the single `it` at l.152-157 (the 4 hardcoded expect lines) with a data-driven 4×4 loop:
       - `for (const from of SERVABLE_PROVIDERS) { for (const to of SERVABLE_PROVIDERS) { expect(shouldAdvance('rate_limited', from, to)).toBe(from !== to); } }` — this yields exactly 4 false cells (same-provider diagonal: anthropic, openrouter, nousresearch, opencode) and 12 true cells. Keep the v1.3/FAL-03 why-comments (D-20-07 provider-keyed, D-01/D-03 same-provider never-advance).
       - Add the Zen↔Go collision canary as its own `it`: `expect(shouldAdvance('rate_limited', 'opencode', 'opencode')).toBe(false)` with a comment that this MODELS the Zen↔Go collapse — `getProviderForModelId` returns logical `opencode` for both snapshot providerIDs (SNAPSHOT_PROVIDER_IDS), so a Zen→Go 429 hop arrives at shouldAdvance as from='opencode', to='opencode' and never advances (one key, D-25-04).
    3. Widen the non-429 eligible loop (l.159-164) to iterate the full provider set: for each of `['model_not_found', 'server_error', 'connection']` and for each `from`/`to` in SERVABLE_PROVIDERS (or at minimum add nousresearch + opencode to the existing same/cross pair assertions) — non-429 eligible classes advance regardless of provider (v1.3 preserved).
    4. Keep the never-eligible loop (l.166-170) byte-identical — `isFailoverEligible(cls) === false` for `['billing', 'input', 'output', 'config', 'auth']` IS the billing lock. NOTE: do NOT add a `shouldAdvance('billing', a, b)` cross-provider assertion as a "billing lock" — shouldAdvance returns true for every non-rate_limited class; the 402 never-eligible property is enforced by `isFailoverEligible('billing') === false` at the loop gate (runAgent.ts l.109), and that loop assertion already exists.
    5. Keep the fail-closed null tests (l.172-176) byte-identical, optionally adding a nousresearch-null case: `expect(shouldAdvance('rate_limited', 'nousresearch', null)).toBe(false)`.
    6. Do NOT touch modelConfig.ts. Do NOT touch any other test file.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agents/modelConfig.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/agents/modelConfig.test.ts` imports `SERVABLE_PROVIDERS` from `@/lib/models/catalog`
    - The rate_limited matrix is data-driven over SERVABLE_PROVIDERS: the test asserts exactly 4 same-provider false cells and 12 cross-provider true cells (a `from !== to` expectation inside the nested loop)
    - A dedicated Zen↔Go canary asserts `shouldAdvance('rate_limited', 'opencode', 'opencode') === false` with the logical-collapse why-comment
    - The non-429 eligible loop covers nousresearch + opencode providers
    - `git diff src/lib/agents/modelConfig.ts` is EMPTY (verify-only contract — D-25-04)
    - `npx vitest run src/lib/agents/modelConfig.test.ts` exits 0 — all existing classifyModelError + resolveModelChain tests remain green (byte-identical)
  </acceptance_criteria>
  <done>16-cell data-driven matrix + Zen↔Go canary green; modelConfig.ts byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Verify RUN-04 end-to-end — targeted suite + zero-production-change proof</name>
  <files>src/lib/agents/modelConfig.test.ts</files>
  <read_first>
    - src/lib/agents/modelConfig.ts (READ ONLY — confirm shouldAdvance l.100-107 is untouched)
    - src/lib/agents/modelConfig.test.ts (the widened matrix from Task 1)
  </read_first>
  <action>
    Verify-only task — NO code changes. Run the RUN-04 proof:
    1. Run the targeted suite: `npx vitest run src/lib/agents/modelConfig.test.ts src/lib/agents/modelFactory.test.ts src/lib/agents/analyzeCompany.test.ts src/lib/agents/runAgent.test.ts src/lib/models/catalog.test.ts` — all green.
    2. Prove the verify-only contract: `git diff src/lib/agents/modelConfig.ts` is empty AND `git status --short src/lib/agents/modelConfig.ts` shows no modification.
    3. Confirm no `shouldAdvance` production change snuck in: `git diff --stat` on the modelConfig files shows test-only changes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agents/modelConfig.test.ts src/lib/agents/modelFactory.test.ts src/lib/agents/analyzeCompany.test.ts src/lib/agents/runAgent.test.ts src/lib/models/catalog.test.ts && test -z "$(git diff src/lib/agents/modelConfig.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - The 5-file targeted vitest command exits 0
    - `git diff src/lib/agents/modelConfig.ts` outputs nothing (exit code of the chained test is 0)
    - No production file (modelConfig.ts, runAgent.ts, env.ts, catalog.ts) appears in the plan's working diff
  </acceptance_criteria>
  <done>RUN-04 delivered as verify-only: 16-cell matrix green, zero production change proven by git diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runAgent loop → shouldAdvance | Pure provider-identity predicate; no network boundary. The risk surface is a REGRESSION in failover semantics — a "wider" 4-provider refactor accidentally relaxing same-provider never-advance or billing never-eligible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-08 | Tampering | shouldAdvance predicate | mitigate | Verify-only contract (D-25-04): modelConfig.ts is byte-identical (git diff proof in Task 2). The 16-cell matrix locks the exact semantics — 4 same-provider false cells incl. opencode↔opencode (Zen↔Go), 12 cross-provider true, billing never-eligible via `isFailoverEligible('billing') === false`, null identity fail-closes a 429. Data-driven loop (Anti-Pattern 3) — no hand-encoded branches that could drift. |
| T-25-09 | Denial of Service | failover loop behavior | mitigate | Same-provider 429 never-advance preserved (D-01/D-03): a Zen↔Go 429 hop arrives as from='opencode', to='opencode' → no advance → fail-loud throw, never an endless fallback burn. The 16-cell matrix is the permanent lock against a future relaxation. |
</threat_model>

<verification>
- Per-task: the `<automated>` commands in each task.
- After wave: `npm test` (full suite).
- git diff proof: `git diff src/lib/agents/modelConfig.ts` empty (the RUN-04 verify-only deliverable).
</verification>

<success_criteria>
- RUN-04: 16-cell data-driven matrix green; Zen↔Go same-provider canary green; billing never-eligible preserved; modelConfig.ts byte-identical (git-diff proven).
</success_criteria>

<output>
Create `.planning/phases/25-run-path-modelfactory-seam/25-03-SUMMARY.md` when done
</output>
