---
phase: 25-run-path-modelfactory-seam
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/agents/runAgent.test.ts]
autonomous: true
requirements: [RUN-05]

must_haves:
  truths:
    - "A cross-provider 429 involving the new providers advances — anthropic→opencode and nousresearch→anthropic hops serve the fallback"
    - "A Zen↔Go same-provider pair (two distinct model ids BOTH resolving to logical 'opencode') never advances on 429 — single attempt, throws"
    - "model_used records the served opencode/nousresearch model's bare id verbatim — no prefix surgery, no endpoint label"
  artifacts:
    - path: "src/lib/agents/runAgent.test.ts"
      provides: "Extended getProviderForModelId mock + opencode/nousresearch 429 hop tests + Zen↔Go same-provider canary + bare-id audit test"
      contains: "m5"
  key_links:
    - from: "runAgent loop"
      to: "getProviderForModelId (mocked)"
      via: "from/to hop identity"
      pattern: "getProviderForModelId"
    - from: "modelIdOf(models[i])"
      to: "modelUsed audit"
      via: "bare id verbatim for object-form models"
      pattern: "modelIdOf"
---

<objective>
Prove RUN-05 at the loop level: `model_used`/`model_chain` record the served provider accurately for all 4 providers, and the 429 hop decision extends correctly to the new providers — all with ZERO production changes to runAgent.ts (the audit + hop logic flows through unchanged; `modelIdOf` l.35-37 returns `.modelId` verbatim for object-form models, so opencode/nousresearch instances carry bare ids for free).

Purpose: RUN-05 — extend runAgent.test.ts's `getProviderForModelId` mock to cover opencode/nousresearch hop cases and lock the cross-provider 429 advance + Zen↔Go same-provider never-advance at the loop, plus a provider-identity smoke over the real committed snapshot. The instance-level dispatch accuracy is already locked by plan 25-01's collision canaries; this plan locks the loop-level behavior that consumes it.
Output: Extended runAgent failover tests + identity smoke; runAgent.ts untouched.

No AI-SPEC.md: agent-extension phase per the planning constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-run-path-modelfactory-seam/25-CONTEXT.md
@.planning/phases/25-run-path-modelfactory-seam/25-RESEARCH.md
@.planning/phases/25-run-path-modelfactory-seam/25-PATTERNS.md
@.planning/phases/25-run-path-modelfactory-seam/25-VALIDATION.md
@src/lib/agents/runAgent.ts
@src/lib/agents/runAgent.test.ts
@src/lib/models/catalog.ts

# Wave 0 test-gap reference (25-VALIDATION.md): runAgent.test.ts mock extension + hop cases are a Wave 0 gap; task ID 25-04-01 (RUN-05).
# Zero-change contract: runAgent.ts is READ-ONLY this phase (RUN-05 audit + Output.object flow through unchanged).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the getProviderForModelId mock + add opencode/nousresearch 429 hop tests and the Zen↔Go same-provider canary (Wave 0 gap 25-04-01)</name>
  <files>src/lib/agents/runAgent.test.ts</files>
  <read_first>
    - src/lib/agents/runAgent.test.ts (the file being modified — getProviderForModelId mock l.18-20, cross-provider 429 test template l.301-317, bare-id audit template l.328-336, D-16 mock conventions l.9-43)
    - src/lib/agents/runAgent.ts (the read-only production target — the loop's hop decision l.106-110, modelIdOf l.35-37)
    - 25-PATTERNS.md §runAgent.test.ts assignment (l.300-324) — the mock extension mapping + new hop cases
    - 25-RESEARCH.md §Wave 0 Gaps (l.433) — the 'm3' → opencode / 'm4' → nousresearch mock mapping
  </read_first>
  <action>
    Extend runAgent.test.ts's failover-loop coverage (describe `'runAgent failover loop (FAL-03/04)'`). No production code changes — runAgent.ts is read-only.

    1. Extend the hoisted `getProviderForModelId` mock (l.18-20) so the new hop cases resolve: keep the existing slashed-id/'m2' → 'openrouter' and default → 'anthropic' mapping, and add `id === 'm3' ? 'opencode' : id === 'm4' ? 'nousresearch' : id === 'm5' ? 'opencode' : ...` — 'm3' and 'm5' BOTH resolve 'opencode' so a two-id chain of them models the Zen↔Go same-provider pair (both snapshot providerIDs collapse to logical 'opencode', D-25-04).
    2. Add cross-provider 429 advance tests mirroring the l.301-308 template:
       a. `['m1', 'm3']` (anthropic → opencode): `generateText` rejects once with `apiErr(429)` then resolves → `generateText` called 2 times, result equals `{ ...resolvedRun, modelUsed: 'm3', usedFallback: true }`.
       b. `['m4', 'm1']` (nousresearch → anthropic): same pattern → `modelUsed: 'm1'`, `usedFallback: true`.
    3. Add the Zen↔Go same-provider canary: `['m3', 'm5']` (both resolve 'opencode') with a 429 reject → `generateText` called exactly 1 time and the call rejects (never advances — same logical provider, one key, D-25-04/RUN-04). Add a why-comment explaining 'm3'/'m5' model two distinct opencode model ids (a Zen row and a Go row) whose from/to identity both collapse to 'opencode'.
    4. Add a bare-id audit test: a chain `['m1', 'm3']` where the 429 advances to the opencode fallback → `result.modelUsed` equals the bare `'m3'` (no prefix surgery, no endpoint label — RUN-05; mirrors the FAL-05 template l.328-336).
    5. Keep all existing tests byte-identical (the default mock mapping preserves every existing same-provider case).
  </action>
  <verify>
    <automated>npx vitest run src/lib/agents/runAgent.test.ts -t "429" && npx vitest run src/lib/agents/runAgent.test.ts -t "modelUsed"</automated>
  </verify>
  <acceptance_criteria>
    - The getProviderForModelId mock maps 'm3' → 'opencode', 'm4' → 'nousresearch', 'm5' → 'opencode'
    - Cross-provider 429 tests: ['m1','m3'] advances with modelUsed 'm3' + usedFallback true; ['m4','m1'] advances with modelUsed 'm1' + usedFallback true
    - The Zen↔Go canary: ['m3','m5'] with a 429 rejects after exactly 1 generateText call (never-advance)
    - A bare-id audit assertion: modelUsed equals the bare opencode id 'm3' verbatim
    - `npx vitest run src/lib/agents/runAgent.test.ts` exits 0 — all existing tests green (byte-identical)
    - `git diff src/lib/agents/runAgent.ts` is EMPTY (zero-change contract)
  </acceptance_criteria>
  <done>Loop-level 4-provider hop tests + Zen↔Go canary + bare-id audit green; runAgent.ts untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Provider-identity smoke over the real committed snapshot + full RUN-05 verification</name>
  <files>src/lib/agents/runAgent.test.ts</files>
  <read_first>
    - src/lib/models/catalog.ts (getProviderForModelId l.174-179 — the priority-ordered resolver under smoke)
    - src/lib/models/catalog.json (the committed snapshot — providers key order alphabetical; the 6 spot-check ids below are all present)
    - 25-RESEARCH.md §Sources "Live computed" (l.466) — the 6 spot-check expectations, all verified correct
  </read_first>
  <action>
    Verify-only task — NO code changes. Run the RUN-05 identity proof:
    1. tsx identity smoke over the REAL committed snapshot — proves provider derivation is accurate for all 4 providers (the priority-ordered registry in action):
       `npx tsx -e "import { getProviderForModelId } from './src/lib/models/catalog'; import catalogJson from './src/lib/models/catalog.json'; const cases: [string, string][] = [['claude-sonnet-4-6','anthropic'],['claude-sonnet-5','opencode'],['deepseek-v4-flash','opencode'],['hy3','opencode'],['nousresearch/hermes-4-70b','nousresearch'],['big-pickle','opencode']]; const bad = cases.filter(([id,exp]) => getProviderForModelId(catalogJson, id) !== exp); if (bad.length) { console.error('MISMATCH', JSON.stringify(bad)); process.exit(1); } console.log('6/6 provider identities correct');"` — expect `6/6 provider identities correct`, exit 0.
    2. Run THIS plan's own file (scoped — parallel Wave-1 plans mid-editing their test files must not cause transient reds): `npx vitest run src/lib/agents/runAgent.test.ts` — all green.
    3. Prove the zero-change contract: `git diff src/lib/agents/runAgent.ts src/lib/env.ts src/lib/models/catalog.ts` is empty.
  </action>
  <verify>
    <automated>npx tsx -e "import { getProviderForModelId } from './src/lib/models/catalog'; import catalogJson from './src/lib/models/catalog.json'; const cases: [string, string][] = [['claude-sonnet-4-6','anthropic'],['claude-sonnet-5','opencode'],['deepseek-v4-flash','opencode'],['hy3','opencode'],['nousresearch/hermes-4-70b','nousresearch'],['big-pickle','opencode']]; const bad = cases.filter(([id,exp]) => getProviderForModelId(catalogJson, id) !== exp); if (bad.length) { console.error('MISMATCH', JSON.stringify(bad)); process.exit(1); } console.log('6/6 provider identities correct');" && npx vitest run src/lib/agents/runAgent.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The tsx smoke prints `6/6 provider identities correct` and exits 0 (all 6 real-snapshot spot-checks match the priority-ordered resolver's expected provider)
    - `npx vitest run src/lib/agents/runAgent.test.ts` exits 0
    - `git diff src/lib/agents/runAgent.ts src/lib/env.ts src/lib/models/catalog.ts` is empty
  </acceptance_criteria>
  <done>RUN-05 delivered: 6/6 real-snapshot identity smoke green, runAgent suite green, zero production change proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runAgent loop → fallback decision | No network boundary; the risk is a hop-decision regression (silent wrong-provider failover) or an audit-identity corruption (wrong model_used) that would poison agent_run persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-10 | Tampering | runAgent 429 hop decision | mitigate | The from/to identity is catalog-derived via `getProviderForModelId` on model ids only (D-20-07), never the error body. New loop tests lock the 4-provider semantics: cross-provider (anthropic→opencode, nousresearch→anthropic) advances; Zen↔Go (both ids → logical 'opencode') never advances on 429 (Task 1 canary). runAgent.ts is byte-identical (git-diff proof, Task 2) — the tests lock EXISTING behavior, not new code. |
| T-25-11 | Integrity | model_used audit identity | mitigate | `modelIdOf` (runAgent.ts l.35-37) returns `.modelId` verbatim for object-form models — opencode/nousresearch instances carry bare ids (dispatch locked in plan 25-01 canaries), so model_used is provider-accurate with no prefix surgery. Task 1's bare-id audit test locks `modelUsed === 'm3'` (bare). Provider derivation for model_chain flows through the priority-ordered registry — proven by the 6/6 real-snapshot identity smoke (Task 2). |
| T-25-12 | Spoofing | provider identity at the hop | mitigate | Provider identity comes only from the registry (`PROVIDER_PRECEDENCE`), never from the error body or client input; fail-closed null identity never advances a 429 (locked in plan 25-03's 16-cell matrix). The identity smoke re-verifies the real snapshot resolution each run. |
</threat_model>

<verification>
- Per-task: the `<automated>` commands in each task — scoped to THIS plan's file (`runAgent.test.ts`) so parallel Wave-1 plans mid-editing their own test files cannot cause transient reds.
- After wave (wave-merge gate, per 25-VALIDATION.md sampling rate): `npm test` — the full suite including the security-grep gate D-22-07. The 5-file targeted suite is NOT run inside individual Wave-1 plans.
- git diff proof: `git diff src/lib/agents/runAgent.ts src/lib/env.ts src/lib/models/catalog.ts src/lib/agents/modelConfig.ts` must be EMPTY (the phase's zero-change contract).
</verification>

<success_criteria>
- RUN-05: cross-provider 429 hops over the new providers advance; Zen↔Go same-provider never advances; model_used records bare ids verbatim; 6/6 real-snapshot provider identities correct.
- runAgent.ts, modelConfig.ts, env.ts, catalog.ts byte-identical.
</success_criteria>

<output>
Create `.planning/phases/25-run-path-modelfactory-seam/25-04-SUMMARY.md` when done
</output>
