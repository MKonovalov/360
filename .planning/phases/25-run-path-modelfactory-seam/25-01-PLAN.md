---
phase: 25-run-path-modelfactory-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/agents/modelFactory.ts, src/lib/agents/modelFactory.test.ts, package.json, package-lock.json]
autonomous: true
requirements: [RUN-01, RUN-02, RUN-06]

must_haves:
  truths:
    - "An opencode or nousresearch model id in a resolved chain instantiates to the correct provider instance — zen vs go chosen by the matched row's api.url, openai-compatible vs anthropic chosen by the matched row's api.npm — never a wrong-endpoint or wrong-protocol dispatch"
    - "The three new createOpenAICompatible instances start with supportsStructuredOutputs UNSET (false) — the safe json_object fallback path; runAgent.ts's Output.object call is untouched"
    - "modelFactory.ts remains the ONLY module importing provider SDKs (constraint 11)"
  artifacts:
    - path: "src/lib/agents/modelFactory.ts"
      provides: "5 module-scope provider instances + 4-provider instantiateModel dispatch"
      contains: "createOpenAICompatible"
    - path: "src/lib/agents/modelFactory.test.ts"
      provides: "Mock seam (createAnthropic + createOpenAICompatible) + RUN-01/02/06 dispatch tests + minimax collision canary"
      contains: "minimax-m2.7"
    - path: "package.json"
      provides: "@ai-sdk/openai-compatible dependency"
      contains: "@ai-sdk/openai-compatible"
  key_links:
    - from: "instantiateModel (modelFactory.ts)"
      to: "getAllModels scoped row find"
      via: "m.providerID === 'opencode' || m.providerID === 'opencode-go'"
      pattern: "providerID === 'opencode'"
    - from: "opencode dispatch branch"
      to: "instance selection"
      via: "api.url (zen/go) then api.npm (anthropic vs openai-compatible)"
      pattern: "api.url === 'https://opencode.ai/zen/go/v1'"
    - from: "modelFactory.ts"
      to: "SDK modules"
      via: "only SDK-importing module (constraint 11)"
      pattern: "from '@ai-sdk/openai-compatible'"
---

<objective>
Make all four providers instantiable through the modelFactory seam. Three module-scope `createOpenAICompatible` instances (`nousresearch`, `opencode-zen`, `opencode-go`) and two `createAnthropic` baseURL-override instances (`anthropicZen`, `anthropicGo`) are added with EXPLICIT `apiKey` (no SDK env auto-load), and `instantiateModel` grows a 4-provider dispatch that routes OpenCode rows by the matched row's `api.url` + `api.npm` (Anti-Pattern 1 scoped-row find). The new openai-compatible instances start with `supportsStructuredOutputs` UNSET (false) per D-25-03.

Purpose: RUN-01/02/06 — the instantiation seam that makes NousResearch + OpenCode chains runnable through the Analytic Agent with zero runAgent.ts changes.
Output: 5 new provider instances, widened dispatch, the phase's single new dependency, and the test seam + canaries that lock the dispatch behavior.

No AI-SPEC.md: this is an agent-extension phase (SDK instance construction + dispatch), not new AI-system design — per the phase planning constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-run-path-modelfactory-seam/25-CONTEXT.md
@.planning/phases/25-run-path-modelfactory-seam/25-RESEARCH.md
@.planning/phases/25-run-path-modelfactory-seam/25-PATTERNS.md
@.planning/phases/25-run-path-modelfactory-seam/25-VALIDATION.md
@src/lib/agents/modelFactory.ts
@src/lib/agents/modelFactory.test.ts
@src/lib/models/catalog.ts

# Wave 0 test-gap reference (25-VALIDATION.md): modelFactory.test.ts mock seam extension + RUN-01/02/06 tests are Wave 0 gaps; task IDs 25-01-01 (RUN-01), 25-01-02 (RUN-02), 25-01-03 (RUN-06).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the modelFactory test mock seam + add RUN-01/RUN-06 constructor-flag tests (Wave 0 gap 25-01-01 / 25-01-03)</name>
  <files>src/lib/agents/modelFactory.test.ts</files>
  <read_first>
    - src/lib/agents/modelFactory.test.ts (the file being modified — current mock seam l.7-19, canary template l.41-50, beforeEach returns l.34-39)
    - src/lib/agents/modelFactory.ts (the production target the tests will drive — current imports l.1-5, openrouter singleton l.17)
    - 25-RESEARCH.md §Code Examples "Verified test-mock seam" (l.325-342) and §Pattern 1 (l.159-185) for the exact mock shapes and instance settings
    - 25-PATTERNS.md §modelFactory.test.ts assignment (l.179-223) for the callable-provider return convention
  </read_first>
  <action>
    Extend the vi.hoisted mock seam and add the constructor/flag tests. Do NOT implement production code in this task — these tests must FAIL against the current 2-provider modelFactory (RED phase).

    1. Grow the hoisted mocks object (currently `{ anthropic, openrouter }`) with two new vi.fn() entries: `createAnthropic` and `createOpenAICompatible`.
    2. Change `vi.mock('@ai-sdk/anthropic', () => ({ anthropic: mocks.anthropic }))` to `vi.mock('@ai-sdk/anthropic', () => ({ anthropic: mocks.anthropic, createAnthropic: mocks.createAnthropic }))`.
    3. Add `vi.mock('@ai-sdk/openai-compatible', () => ({ createOpenAICompatible: mocks.createOpenAICompatible }))`.
    4. Extend the defensive `vi.mock('@/lib/env', ...)` object to also carry `NOUSRESEARCH_API_KEY: 'test-key'` and `OPENCODE_API_KEY: 'test-key'`.
    5. In `beforeEach`, add callable-provider returns so dispatch assertions can distinguish instances. Use `mockImplementation` keyed on the constructor options — baseURL for createAnthropic (`https://opencode.ai/zen/go/v1` → provider `'anthropic-go'`, else `'anthropic-zen'`), `name` for createOpenAICompatible (`'opencode-go'` → provider `'opencode-go'`, `'opencode-zen'` → provider `'opencode-zen'`, `'nousresearch'` → provider `'nousresearch'`). Each returns `(id: string) => ({ provider: <marker>, modelId: id })` — the loop contract only reads modelId (runAgent.ts modelIdOf); the distinct provider markers let dispatch assertions tell instances apart.
    6. Add RUN-01 constructor tests (describe `'createOpenAICompatible'` per the VALIDATION task 25-01-01 command): assert `mocks.createOpenAICompatible` was called exactly 3 times, and that the options objects contain the EXACT values — `{ name: 'nousresearch', baseURL: 'https://inference-api.nousresearch.com/v1' }`, `{ name: 'opencode-zen', baseURL: 'https://opencode.ai/zen/v1' }`, `{ name: 'opencode-go', baseURL: 'https://opencode.ai/zen/go/v1' }` — and that each options object HAS an `apiKey` property (assert `Object.keys(opts)` contains `'apiKey'` — explicit pass, not env auto-load; assert the property exists, NOT its value, since process.env is not controlled in the test). Assert `mocks.createAnthropic` was called exactly 2 times with `{ baseURL: 'https://opencode.ai/zen/v1' }` and `{ baseURL: 'https://opencode.ai/zen/go/v1' }` + apiKey property present.
    7. Add RUN-06 structured-output false-start tests (describe `'structuredOutputs'` per task 25-01-03): for each of the 3 createOpenAICompatible call args, assert `opts.supportsStructuredOutputs === undefined` (flag UNSET = false default, D-25-03) — the false-start must be the ABSENCE of the key, never an explicit `true`.
    8. Keep all existing tests byte-identical apart from the beforeEach return additions (the existing anthropic/openrouter returns stay).
  </action>
  <verify>
    <automated>npx vitest run src/lib/agents/modelFactory.test.ts -t "createOpenAICompatible" && npx vitest run src/lib/agents/modelFactory.test.ts -t "structuredOutputs"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/agents/modelFactory.test.ts` contains `createAnthropic: vi.fn()` and `createOpenAICompatible: vi.fn()` inside the hoisted mocks object
    - `src/lib/agents/modelFactory.test.ts` contains `vi.mock('@ai-sdk/openai-compatible', () => ({ createOpenAICompatible: mocks.createOpenAICompatible }))`
    - The `vi.mock('@/lib/env', ...)` object includes `NOUSRESEARCH_API_KEY: 'test-key'` and `OPENCODE_API_KEY: 'test-key'`
    - beforeEach sets callable returns for createAnthropic and createOpenAICompatible keyed on baseURL/name
    - RUN-01 tests assert exactly 3 createOpenAICompatible calls with exact name+baseURL values and an `apiKey` property present on each options object, and exactly 2 createAnthropic calls with exact baseURL values
    - RUN-06 tests assert `supportsStructuredOutputs === undefined` on all 3 openai-compatible options objects
    - The RUN-01/06 tests FAIL when run against the current production code (RED) — the constructor tests fail because the instances do not exist yet
  </acceptance_criteria>
  <done>Mock seam extended; RUN-01/RUN-06 tests authored and failing against current code (RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RUN-02 dispatch tests + collision canaries (Wave 0 gap 25-01-02)</name>
  <files>src/lib/agents/modelFactory.test.ts</files>
  <read_first>
    - src/lib/agents/modelFactory.test.ts (the file being modified — existing collision canary l.41-50 is the template)
    - 25-RESEARCH.md §Pitfall 1 (minimax npm trap, l.256-260) and §Pitfall 4 (locked-id dispatch tests, l.274-278) and §Common Pitfalls counts (l.262-266)
    - 25-PATTERNS.md §modelFactory.test.ts "Collision canary template" (l.209-218) for the exact new-canary id list
  </read_first>
  <action>
    Add RUN-02 dispatch tests (describe `'dispatch'` per task 25-01-02) using LOCKED real snapshot ids (never enumerate the servable set — Pitfall 4). The tests call `instantiateModel(id)` and assert which callable provider mock was invoked with the bare id verbatim. All ids below are verified present in the committed snapshot with the stated rows (checked 2026-08-04):

    1. `nousresearch/hermes-4-70b` → the `'nousresearch'` marker callable (its nousresearch row is openai-compatible npm) — assert `mocks.createOpenAICompatible.mock.calls` resolved the returned callable and it was invoked with `'nousresearch/hermes-4-70b'`.
    2. `nousresearch/hermes-4-405b` → `'nousresearch'` marker (second Hermes pin).
    3. `hy3` → `'opencode-go'` marker (go-exclusive row, openai-compatible npm) — the openaiCompatibleGo dispatch.
    4. `qwen3.8-max` → `'anthropic-go'` marker (go-exclusive row, anthropic npm) — the anthropicGo dispatch.
    5. `qwen3.6-plus` → `'anthropic-zen'` marker (dual-listed, BOTH rows anthropic npm; Zen row wins via flatten order) — the anthropicZen dispatch.
    6. `claude-sonnet-5` → `'anthropic-zen'` marker (its opencode row is Zen + anthropic npm; it resolves opencode because it is NOT in ANTHROPIC_ALLOWLIST) — second anthropicZen proof.
    7. `deepseek-v4-flash` → `'opencode-zen'` marker (dual-listed, both rows openai-compatible; Zen wins).
    8. COLLISION CANARY (the minimax npm-mismatch trap, research Pitfall 1): `minimax-m2.7` → `'opencode-zen'` marker AND `mocks.createAnthropic` NOT invoked — the Zen row is openai-compatible while the Go row is anthropic; Zen-wins dedup + scoped find must route it to openaiCompatibleZen, NEVER anthropicGo. Same for `minimax-m3` → `'opencode-zen'`.
    9. Assert every dispatch passes the id VERBATIM (bare id, no prefix surgery) — e.g. `hy3` invoked with `'hy3'`, `qwen3.8-max` invoked with `'qwen3.8-max'`.
    10. Keep the existing `claude-sonnet-4-6 → anthropic` canary untouched (regression lock). Extend the FAIL-LOUD backstop test if needed: an unknown id still throws `unsupported provider for model` and no provider mock is touched.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agents/modelFactory.test.ts -t "dispatch"</automated>
  </verify>
  <acceptance_criteria>
    - Dispatch tests cover at least: hermes-4-70b → nousresearch, hermes-4-405b → nousresearch, hy3 → opencode-go, qwen3.8-max → anthropic-go, qwen3.6-plus → anthropic-zen, claude-sonnet-5 → anthropic-zen, deepseek-v4-flash → opencode-zen, minimax-m2.7 → opencode-zen (NOT anthropic-go), minimax-m3 → opencode-zen
    - The minimax canary asserts `mocks.createAnthropic` was NOT called for minimax ids
    - All dispatch assertions use bare ids verbatim
    - The dispatch tests FAIL against the current 2-provider production code (RED) — `instantiateModel('hy3')` currently throws `unsupported provider for model hy3`
    - No test iterates `getServableIdsForProvider(catalogJson, 'opencode')` to enumerate all servable ids (Pitfall 4)
  </acceptance_criteria>
  <done>Dispatch tests + minimax collision canary authored and failing against current code (RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Install @ai-sdk/openai-compatible, add the 5 module-scope instances, widen instantiateModel to 4 providers (GREEN)</name>
  <files>package.json, package-lock.json, src/lib/agents/modelFactory.ts</files>
  <read_first>
    - src/lib/agents/modelFactory.ts (the file being modified — openrouter singleton l.7-17 is the module-singleton template; dispatch l.57-80; the deliberate no-@/lib/env why-comment l.15-16)
    - 25-RESEARCH.md §Pattern 1 (l.159-185), §Pattern 2 (l.187-204), §Pattern 3 (l.206-233) — the exact instance settings + dispatch shape, dist-verified
    - 25-PATTERNS.md §modelFactory.ts assignment (l.28-88) — imports pattern, instance list, dispatch extension
    - 25-CONTEXT.md D-25-01 (two anthropic instances — 20 anthropic-npm rows span BOTH endpoints), D-25-02 (dispatch order), D-25-03 (supportsStructuredOutputs UNSET)
    - 25-RESEARCH.md §Package Legitimacy Audit (l.92-102): `@ai-sdk/openai-compatible` [OK]/Approved — no blocking-human checkpoint needed
  </read_first>
  <action>
    1. Install the phase's single new runtime dep (node_modules already holds an extraneous 3.0.22 from the researcher's slopcheck revert — the npm install below records it properly): `npm install @ai-sdk/openai-compatible@^3.0.20` (range resolves to 3.0.22, dist-verified).
    2. In modelFactory.ts: extend the l.1 import to `import { anthropic, createAnthropic } from '@ai-sdk/anthropic';` and add `import { createOpenAICompatible } from '@ai-sdk/openai-compatible';`.
    3. Add 5 module-scope instances AFTER the openrouter singleton (l.17), each with a why-comment in the house convention (module-singleton, sanity-client pattern; apiKey EXPLICIT — openai-compatible builds `Authorization: Bearer ${apiKey}` only from the passed option, dist l.1749, NO env auto-load unlike createOpenRouter; apiKey read via `process.env.*` directly — modelFactory deliberately does NOT import @/lib/env, D-11):
       - `const nousresearch = createOpenAICompatible({ name: 'nousresearch', apiKey: process.env.NOUSRESEARCH_API_KEY, baseURL: 'https://inference-api.nousresearch.com/v1' });`
       - `const openaiCompatibleZen = createOpenAICompatible({ name: 'opencode-zen', apiKey: process.env.OPENCODE_API_KEY, baseURL: 'https://opencode.ai/zen/v1' });`
       - `const openaiCompatibleGo = createOpenAICompatible({ name: 'opencode-go', apiKey: process.env.OPENCODE_API_KEY, baseURL: 'https://opencode.ai/zen/go/v1' });`
       - `const anthropicZen = createAnthropic({ baseURL: 'https://opencode.ai/zen/v1', apiKey: process.env.OPENCODE_API_KEY });`
       - `const anthropicGo = createAnthropic({ baseURL: 'https://opencode.ai/zen/go/v1', apiKey: process.env.OPENCODE_API_KEY });`
       - `name` is REQUIRED on createOpenAICompatible (becomes the provider metadata key); `supportsStructuredOutputs` MUST be UNSET on all three (D-25-03 false-start); `createAnthropic` baseURL/apiKey are constructor options, NOT per-call — instance-per-endpoint is the only correct topology (D-25-01).
    4. Widen `instantiateModel` (keep the existing anthropic + openrouter branches byte-identical):
       - After the openrouter branch, add `if (provider === 'nousresearch') return nousresearch(id);`
       - Add the opencode branch (dispatch order D-25-02: anthropic → openrouter → nousresearch → opencode):
         - Anti-Pattern 1 scoped-row find (comment explaining WHY — dual-listed ids exist in both groups with different api.npm, e.g. minimax-m2.7: Zen=openai-compatible, Go=anthropic; flatten order is alphabetical so `opencode` sorts before `opencode-go` and the find returns the ZEN row first, matching Zen-wins): `const row = getAllModels(catalogJson).find((m) => m.id === id && (m.providerID === 'opencode' || m.providerID === 'opencode-go'));`
         - `if (!row) throw new Error(\`unsupported provider for model ${id}\`);` (fail-loud backstop, same message as l.79)
         - `const go = row.api.url === 'https://opencode.ai/zen/go/v1';` then `return row.api.npm === '@ai-sdk/anthropic' ? (go ? anthropicGo(id) : anthropicZen(id)) : (go ? openaiCompatibleGo(id) : openaiCompatibleZen(id));`
    5. Do NOT touch runAgent.ts, modelConfig.ts, env.ts, catalog.ts, or defaultChain() (zero-change contract). defaultChain() STAYS `[anthropic(FAST_MODEL_ID)]` — D-25-06: PROVIDER_DEFAULT_MODELS (incl. NOUSRESEARCH_DEFAULT_MODEL_ID, OPENCODE_DEFAULT_MODEL_ID) remain Phase 26's reset targets, NOT the no-settings default; zero behavior change.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agents/modelFactory.test.ts && npx tsc --noEmit && grep -rn "from '@ai-sdk/openai-compatible'" src/ --include="*.ts" -l | grep -v "modelFactory" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"@ai-sdk/openai-compatible": "^3.0.20"` and `npm ls @ai-sdk/openai-compatible` resolves to 3.0.22 non-extraneous
    - `src/lib/agents/modelFactory.ts` contains exactly 5 new `const` instances: `nousresearch`, `openaiCompatibleZen`, `openaiCompatibleGo`, `anthropicZen`, `anthropicGo` — each passing `apiKey: process.env.NOUSRESEARCH_API_KEY` (nousresearch) or `apiKey: process.env.OPENCODE_API_KEY` (the other four) EXPLICITLY
    - The 3 createOpenAICompatible calls have `name` set and NO `supportsStructuredOutputs` key (grep `supportsStructuredOutputs` in modelFactory.ts → 0 matches outside comments)
    - `instantiateModel` contains `if (provider === 'nousresearch')` and the opencode branch with the scoped-row find on `m.providerID === 'opencode' || m.providerID === 'opencode-go'` and the `api.url`/`api.npm` dispatch
    - The existing anthropic + openrouter branches are unchanged (git diff shows no hunks in l.57-76)
    - All modelFactory tests pass (GREEN): `npx vitest run src/lib/agents/modelFactory.test.ts` exits 0
    - `npx tsc --noEmit` exits 0 (A3: LanguageModelV4 assignable to the LanguageModel union)
    - Constraint 11 grep returns 0: no other src/ module imports `@ai-sdk/openai-compatible` (the third grep term above outputs 0)
  </acceptance_criteria>
  <done>Dependency installed; 5 instances + 4-provider dispatch live; the Task 1/2 RED tests now pass (GREEN); constraint 11 holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyzeCompany → modelFactory (instantiation seam) | Internal module boundary; dispatch input (model ids) arrives from resolved settings chains, never raw client input — but a dispatch bug would send requests to the wrong provider endpoint |
| modelFactory instances → NousResearch/OpenCode external APIs | Untrusted-network boundary; requests carry `Authorization: Bearer <key>`; a missing key means NO auth header → 401 at request time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Spoofing | openai-compatible instances (apiKey) | mitigate | `apiKey` passed EXPLICITLY at construction from `process.env.*` (dist l.1749 builds the Bearer header only from the passed option — no env auto-load). Unset-key case made unreachable at run entry by the Phase 25 chain-aware gate (plan 25-02, RUN-03). Dist-verified no `NEXT_PUBLIC_` exposure; keys are server-only non-prefixed env vars (env.ts l.47/54). |
| T-25-02 | Tampering | instantiateModel dispatch | mitigate | Anti-Pattern 1 scoped-row find on providerID ('opencode'/'opencode-go') — a bare id find would read the wrong dual-listed row and misroute minimax-m2.7/m3 to anthropicGo (wrong protocol). Locked by the minimax collision canary (Task 2, canary 8). Dispatch is catalog-derived (getProviderForModelId), never client input (D-25-02). Fail-loud backstop throws on row miss. |
| T-25-03 | Integrity | supportsStructuredOutputs flag | mitigate | Flag UNSET (false) on all three openai-compatible instances (D-25-03) — schema requests degrade to `response_format: {type: 'json_object'}` + warning + client-side parse/validate (dist l.525/557). No code path can flip it in Phase 25; the live probe is Phase 27 VER-05 (roadmap-locked). Task 1 RUN-06 tests lock the unset state. |
| T-25-04 | Information Disclosure | process.env key reads | mitigate | Keys read via `process.env.*` in modelFactory.ts — the security-grep gate's ALLOWED set already covers `lib/agents/modelFactory.ts` (D-22-07, verified 3-file baseline); no new client-reachable key references introduced. |
| T-25-SC | Tampering | npm install of `@ai-sdk/openai-compatible` | mitigate | Package Legitimacy Audit table present and well-formed (RESEARCH.md l.92-102): `[OK]`/Approved, official Vercel AI SDK family, 2.3M+/wk downloads, source github.com/vercel/ai. No `[SUS]`/`[ASSUMED]` packages — no blocking-human checkpoint required; install runs autonomously at ^3.0.20 (resolves 3.0.22). |
</threat_model>

<verification>
- Per-task: the `<automated>` commands in each task (targeted vitest runs, tsc, constraint-11 grep).
- Per-plan: `npx vitest run src/lib/agents/modelFactory.test.ts src/lib/agents/analyzeCompany.test.ts src/lib/agents/modelConfig.test.ts src/lib/agents/runAgent.test.ts src/lib/models/catalog.test.ts` — the VALIDATION.md sampling command.
- After wave: `npm test` (full suite incl. the security-grep gate D-22-07).
- git diff proof: `git diff src/lib/agents/runAgent.ts src/lib/agents/modelConfig.ts src/lib/env.ts src/lib/models/catalog.ts` must be EMPTY (zero-change contract).
</verification>

<success_criteria>
- RUN-01: three `createOpenAICompatible` instances with explicit apiKey exist; constraint 11 holds (grep proof).
- RUN-02: opencode rows dispatch by api.url (zen/go) + api.npm (anthropic vs openai-compatible) via scoped-row find; minimax collision canary green.
- RUN-06: `supportsStructuredOutputs` unset on all three openai-compatible instances; runAgent.ts Output.object untouched.
- All Task 1/2 RED tests green after Task 3; full targeted suite green.
</success_criteria>

<output>
Create `.planning/phases/25-run-path-modelfactory-seam/25-01-SUMMARY.md` when done
</output>
