---
phase: 25-run-path-modelfactory-seam
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/agents/analyzeCompany.ts, src/lib/agents/analyzeCompany.test.ts]
autonomous: true
requirements: [RUN-03]

must_haves:
  truths:
    - "A resolved chain containing a nousresearch model names NOUSRESEARCH_API_KEY when that key is unset (all-or-nothing, exact-key naming)"
    - "A resolved chain containing an opencode model names OPENCODE_API_KEY when that key is unset (the dual snapshot providerIDs opencode + opencode-go both collapse to logical 'opencode' → one key, free mapping)"
    - "An opencode-only chain runs with ONLY OPENCODE_API_KEY set — ANTHROPIC/NOUSRESEARCH not blanket-required"
  artifacts:
    - path: "src/lib/agents/analyzeCompany.ts"
      provides: "missingProviderKey widened to 4 provider guards"
      contains: "NOUSRESEARCH_API_KEY"
    - path: "src/lib/agents/analyzeCompany.test.ts"
      provides: "RUN-03 gate tests (nousresearch/opencode missing-key + opencode-only pass)"
      contains: "OPENCODE_API_KEY"
  key_links:
    - from: "missingProviderKey"
      to: "env.NOUSRESEARCH_API_KEY"
      via: "guard clause"
      pattern: "providers.has('nousresearch') && !env.NOUSRESEARCH_API_KEY"
    - from: "missingProviderKey"
      to: "env.OPENCODE_API_KEY"
      via: "guard clause"
      pattern: "providers.has('opencode') && !env.OPENCODE_API_KEY"
    - from: "missingProviderKey type filter"
      to: "getProviderForModelId"
      via: "ModelProviderId type predicate (p !== null)"
      pattern: "p is ModelProviderId"
---

<objective>
Widen the chain-aware env gate (`missingProviderKey` in analyzeCompany.ts) from 2 to 4 provider guards so a resolved chain containing a nousresearch model requires `NOUSRESEARCH_API_KEY` and a chain containing an opencode model requires `OPENCODE_API_KEY` — all-or-nothing at run entry, naming the exact missing key (D-25-05).

Purpose: RUN-03 — an OpenCode-only chain runs with only `OPENCODE_API_KEY` set; a NousResearch-only chain with only `NOUSRESEARCH_API_KEY`. The dual snapshot providerIDs (`opencode` + `opencode-go`) already collapse to logical `opencode` via `getProviderForModelId` (catalog.ts SNAPSHOT_PROVIDER_IDS), so the dual-id→single-key mapping is free.
Output: 4-guard gate + RUN-03 gate tests.

No AI-SPEC.md: agent-extension phase per the planning constraints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-run-path-modelfactory-seam/25-CONTEXT.md
@.planning/phases/25-run-path-modelfactory-seam/25-RESEARCH.md
@.planning/phases/25-run-path-modelfactory-seam/25-PATTERNS.md
@.planning/phases/25-run-path-modelfactory-seam/25-VALIDATION.md
@src/lib/agents/analyzeCompany.ts
@src/lib/agents/analyzeCompany.test.ts
@src/lib/env.ts
@src/lib/models/catalog.ts

# Wave 0 test-gap reference (25-VALIDATION.md): analyzeCompany.test.ts mocks.env extension + RUN-03 gate tests are Wave 0 gaps; task ID 25-02-01 (RUN-03).
# D-25-05 target: keep the existing anthropic/openrouter guards; add nousresearch → NOUSRESEARCH_API_KEY and opencode → OPENCODE_API_KEY; widen the Set type filter to ModelProviderId.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RUN-03 gate tests — mocks.env keys + nousresearch/opencode missing-key and provider-only-pass cases (Wave 0 gap 25-02-01)</name>
  <files>src/lib/agents/analyzeCompany.test.ts</files>
  <read_first>
    - src/lib/agents/analyzeCompany.test.ts (the file being modified — mocks.env l.8-16, missing-key test template l.302-313, openrouter-only pass template l.331-344, restore convention l.311-312)
    - src/lib/agents/analyzeCompany.ts (the production target — missingProviderKey l.54-63, gate call site l.91-94)
    - 25-RESEARCH.md §Code Examples "Verified missingProviderKey widening target" (l.309-322) for the exact guard shape
    - 25-PATTERNS.md §analyzeCompany.test.ts assignment (l.227-268) for the mocks.env additions + test templates
  </read_first>
  <action>
    Add RUN-03 gate tests (describe `'missing'` per task 25-02-01). Do NOT implement production code in this task — the new tests must FAIL against the current 2-guard gate (RED phase). Use real snapshot ids in the settings chains (real resolveModelChain + real getProviderForModelId resolve them).

    1. Extend the hoisted `mocks.env` object (l.10-16) with `NOUSRESEARCH_API_KEY: 'test-key' as string | undefined` and `OPENCODE_API_KEY: 'test-key' as string | undefined` — typed `string | undefined` so the not_configured tests can clear a key at runtime.
    2. Add tests mirroring the D-20-01 template (l.302-313), each clearing a key, asserting, and RESTORING it (vi.clearAllMocks clears call history but NOT directly-assigned property values):
       a. nousresearch-only chain: `getModelSettingsForUser` resolves `{ primaryModel: 'nousresearch/hermes-4-70b', fallbackModels: [] }`; clear `mocks.env.NOUSRESEARCH_API_KEY` → result equals `{ ok: false, reason: 'not_configured', missingKey: 'NOUSRESEARCH_API_KEY' }`; `mocks.runAgent` NOT called; restore.
       b. opencode-only chain: `{ primaryModel: 'deepseek-v4-flash', fallbackModels: [] }`; clear `mocks.env.OPENCODE_API_KEY` → result equals `{ ok: false, reason: 'not_configured', missingKey: 'OPENCODE_API_KEY' }`; `mocks.runAgent` NOT called; restore.
       c. opencode-only chain with ONLY OPENCODE set (mirror the D-20-03 template l.331-344): settings `{ primaryModel: 'hy3', fallbackModels: [] }`; clear BOTH `mocks.env.ANTHROPIC_API_KEY` and `mocks.env.NOUSRESEARCH_API_KEY` (opencode chain must not blanket-require them) → `result.ok` is true; `mocks.instantiateChain` called with `['hy3']`; restore both keys.
       d. mixed 4-provider chain all-keys-set passes (extend the FAL-04 pattern l.346-361): settings `{ primaryModel: 'claude-sonnet-4-6', fallbackModels: ['nousresearch/hermes-4-70b'] }` with all four keys set → `result.ok` true and `mocks.instantiateChain` called with the resolved chain. (Optionally also an opencode+nousresearch mixed case with keys set.)
    3. Keep all existing tests byte-identical — the new mocks.env keys default to 'test-key' so existing tests are unaffected.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agents/analyzeCompany.test.ts -t "missing"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/agents/analyzeCompany.test.ts` mocks.env contains `NOUSRESEARCH_API_KEY: 'test-key' as string | undefined` and `OPENCODE_API_KEY: 'test-key' as string | undefined`
    - Tests cover: nousresearch-only chain missing NOUSRESEARCH → names `NOUSRESEARCH_API_KEY`; opencode-only chain missing OPENCODE → names `OPENCODE_API_KEY`; opencode-only chain with only OPENCODE set (ANTHROPIC + NOUSRESEARCH cleared) passes with `instantiateChain(['hy3'])`; mixed 4-provider all-keys-set chain passes
    - Every key-clearing test restores the key afterward
    - The new tests FAIL against the current 2-guard production code (RED) — `missingProviderKey(['nousresearch/hermes-4-70b'])` currently returns null because the nousresearch guard does not exist
  </acceptance_criteria>
  <done>RUN-03 gate tests authored and failing against the 2-guard gate (RED state).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen missingProviderKey to 4 provider guards (GREEN)</name>
  <files>src/lib/agents/analyzeCompany.ts</files>
  <read_first>
    - src/lib/agents/analyzeCompany.ts (the file being modified — the import at l.14, missingProviderKey l.54-63, the D-20-01/02 why-comment l.48-53)
    - src/lib/models/catalog.ts (SNAPSHOT_PROVIDER_IDS l.108-113 — the opencode dual-id→logical collapse source; ModelProviderId type l.59)
    - 25-RESEARCH.md §Code Examples "Verified missingProviderKey widening target" (l.309-322) — the exact target code
  </read_first>
  <action>
    Widen `missingProviderKey` (l.54-63) per D-25-05 with zero special-casing:

    1. Change the l.14 import to `import { getProviderForModelId, type ModelProviderId } from '@/lib/models/catalog';` (A2: the widened type predicate needs the type import; catalog is NOT a provider SDK — constraint 11 intact).
    2. Widen the Set type-filter predicate (l.58) from `(p): p is 'anthropic' | 'openrouter' => p !== null` to `(p): p is ModelProviderId => p !== null`.
    3. Add two guards AFTER the existing anthropic/openrouter guards (keep them verbatim; first-hit-wins order preserved):
       - `if (providers.has('nousresearch') && !env.NOUSRESEARCH_API_KEY) return 'NOUSRESEARCH_API_KEY';`
       - `if (providers.has('opencode') && !env.OPENCODE_API_KEY) return 'OPENCODE_API_KEY';`
    4. Update the function's why-comment (l.48-53) to note the 4-provider scope: the dual snapshot providerIDs collapse to logical `opencode` via SNAPSHOT_PROVIDER_IDS, so the dual-id→single-key mapping is free; unknown ids (null provider) remain skipped.
    5. Do NOT touch the gate call site (l.91-94), env.ts, or runAgent.ts — reads only.
  </action>
  <verify>
    <automated>npx vitest run src/lib/agents/analyzeCompany.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/agents/analyzeCompany.ts` l.14 imports `type ModelProviderId` from `@/lib/models/catalog`
    - `missingProviderKey` contains exactly 4 guards in order: ANTHROPIC_API_KEY, OPENROUTER_API_KEY, NOUSRESEARCH_API_KEY, OPENCODE_API_KEY — each shaped `providers.has('<provider>') && !env.<KEY>` returning the exact key name
    - The type predicate is `(p): p is ModelProviderId => p !== null`
    - The gate call site (l.91-94) is unchanged
    - All analyzeCompany tests pass (GREEN): `npx vitest run src/lib/agents/analyzeCompany.test.ts` exits 0 — including the existing D-20-01/02/03 tests (byte-identical)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>4-guard gate live; Task 1 RED tests now pass (GREEN); opencode-only chain runs with only OPENCODE_API_KEY set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analyzeCompany run entry → provider SDKs | The gate is the last server-side check before any SDK call; a gate bug (missing guard) would let a chain run with an unset key → SDK sends NO Authorization header → 401 at request time (Spoofing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05 | Spoofing | missingProviderKey gate | mitigate | All-or-nothing at run entry: every provider present in the RESOLVED chain must have its key, or the gate returns `{ ok: false, reason: 'not_configured', missingKey }` naming the EXACT missing key (D-20-01/02, D-25-05). Guard pattern `has(provider) && !key → return key`, first-hit wins. OpenCode's dual snapshot providerIDs collapse to logical `opencode` (SNAPSHOT_PROVIDER_IDS) → one key, no special-casing. Task 1 RED tests lock each guard. |
| T-25-06 | Information Disclosure | key names in not_configured responses | accept | The gate returns only the KEY NAME (e.g. `'OPENCODE_API_KEY'`), never a key VALUE; values live in server-only env (env.ts l.47/54, non-PUBLIC_); message values are fixed server-side constants (T-20-09 doctrine — D-20-10 execution). Low-value exposure (a config instruction), no credential leak. |
| T-25-07 | Tampering | provider-set derivation | mitigate | The provider set is catalog-derived via `getProviderForModelId` (priority-ordered, fail-closed null), never client input; unknown ids are skipped (union servable gate upstream excludes them). Widen the filter predicate to the full `ModelProviderId` type — no literal-union drift between the gate and the registry. |
</threat_model>

<verification>
- Per-task: the `<automated>` commands in each task.
- Per-plan: `npx vitest run src/lib/agents/modelFactory.test.ts src/lib/agents/analyzeCompany.test.ts src/lib/agents/modelConfig.test.ts src/lib/agents/runAgent.test.ts src/lib/models/catalog.test.ts`.
- After wave: `npm test` (full suite incl. the security-grep gate D-22-07).
- git diff proof: `git diff src/lib/agents/runAgent.ts src/lib/agents/modelConfig.ts src/lib/env.ts src/lib/models/catalog.ts` must be EMPTY.
</verification>

<success_criteria>
- RUN-03: `missingProviderKey` names NOUSRESEARCH_API_KEY / OPENCODE_API_KEY exactly; an opencode-only chain requires only OPENCODE_API_KEY; all-or-nothing preserved.
- Both analyzeCompany tests (new + existing) green; tsc clean.
</success_criteria>

<output>
Create `.planning/phases/25-run-path-modelfactory-seam/25-02-SUMMARY.md` when done
</output>
