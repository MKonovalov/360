---
phase: 25-run-path-modelfactory-seam
audited: 2026-08-04
auditor: gsd-security-auditor
asvs_level: 1
block_on: high
threats_open: 0
threats_total: 13
status: SECURED
---

# Phase 25: Run Path / modelFactory Seam — Security Audit

**Auditor:** gsd-security-auditor (retroactive mitigation verification)
**Audited:** 2026-08-04
**ASVS Level:** 1
**Block-on:** high (open threats)
**Result:** **SECURED** — 13/13 threats closed (12 mitigated + 1 documented accepted risk), 0 open, 0 unregistered flags.

## Threat Register (verification per disposition)

Register source: PLAN.md threat models 25-01 (T-25-01..04, T-25-SC), 25-02 (T-25-05..07), 25-03 (T-25-08..09), 25-04 (T-25-10..12). Each `mitigate` threat confirmed by direct file/grep/exec evidence in the cited implementation files; the `accept` threat (T-25-06) documented in the Accepted Risks log below.

| Threat ID | Category | Component | Disposition | Evidence (file:line) | Status |
|-----------|----------|-----------|-------------|----------------------|--------|
| T-25-01 | Spoofing | openai-compatible instances (apiKey) | mitigate | EXPLICIT `apiKey: process.env.*` on all 5 instances (`src/lib/agents/modelFactory.ts:34-56` — nousresearch `:36`, openaiCompatibleZen `:41`, openaiCompatibleGo `:46`, anthropicZen `:51`, anthropicGo `:55`). No SDK env auto-load verified for the 3 openai-compatible instances (`node_modules/@ai-sdk/openai-compatible/dist/index.js:1749` — `...options.apiKey && { Authorization: \`Bearer ${options.apiKey}\` }` builds the header ONLY from the passed option; no `loadApiKey` call in the module). Unset-key unreachable at run entry: `missingProviderKey` guard `providers.has('opencode') && !env.OPENCODE_API_KEY` (`src/lib/agents/analyzeCompany.ts:66`) + gate call site `:96-99` (returns `not_configured`) strictly precede `instantiateChain` at `:111`; `instantiateChain`'s only production caller is analyzeCompany (grep: no other src/ call site). Keys server-only non-prefixed (`src/lib/env.ts:47,54`, `.env.example:40-41` — no NEXT_PUBLIC_). WR-01 fallback claim VERIFIED (see Audit Trail): `createAnthropic` would fall back to `ANTHROPIC_API_KEY` when the passed key is undefined (`@ai-sdk/anthropic/dist/index.js:6542-6544`), but that state is unreachable through the only production path (RUN-03 gate) — verifier's claim holds. | CLOSED |
| T-25-02 | Tampering | instantiateModel dispatch | mitigate | Anti-Pattern 1 scoped-row find on `m.providerID === 'opencode' || 'opencode-go'` (`src/lib/agents/modelFactory.ts:126-128`); dispatch catalog-derived via `getProviderForModelId` at `:97` (never client input); fail-loud throw on row miss (`:131`) and on unhandled provider (`:139`). Minimax collision canary non-vacuous — snapshot probe confirms the npm trap is real: `minimax-m2.7`/`minimax-m3` Zen row = `@ai-sdk/openai-compatible` (opencode group), Go row = `@ai-sdk/anthropic` (opencode-go group); canaries assert `opencode-zen` NEVER `anthropic-go` (`src/lib/agents/modelFactory.test.ts:296-317`). | CLOSED |
| T-25-03 | Integrity | supportsStructuredOutputs flag | mitigate | Flag UNSET (false) on all three createOpenAICompatible calls (`src/lib/agents/modelFactory.ts:34-48` — no `supportsStructuredOutputs` key; grep across src/ shows the token only in a why-comment `:28` and the test file); RUN-06 test locks `opts.supportsStructuredOutputs === undefined` on all 3 captured options (`src/lib/agents/modelFactory.test.ts:232-238`). No code path flips it in Phase 25. | CLOSED |
| T-25-04 | Information Disclosure | process.env key reads | mitigate | Keys read via `process.env.*` ONLY in `src/lib/agents/modelFactory.ts` (`:36,41,46,51,55`). Security-grep ALLOWED set covers modelFactory.ts (`src/lib/verification/security-grep.test.ts:12` — `lib/agents/modelFactory.ts` ∈ ALLOWED). Grep across src/ for `NOUSRESEARCH_API_KEY|OPENCODE_API_KEY` → 7 files, all server-context or tests: env.ts (declarations), modelFactory.ts + analyzeCompany.ts (server reads/guards), modelFactory.test.ts / analyzeCompany.test.ts / runAgent.test.ts / modelConfig.test.ts (test mocks + comments). No client component, no Server Action, no `NEXT_PUBLIC_` exposure of the new keys (grep + `.env.example:40-41`). | CLOSED |
| T-25-SC | Tampering | npm install of `@ai-sdk/openai-compatible` | mitigate | Package Legitimacy Audit table present and well-formed (`25-RESEARCH.md:92-102`): `[OK]`/Approved, official Vercel AI SDK family, ~4 yrs, 2.3M+/wk downloads, source `github.com/vercel/ai (packages/openai-compatible)`; no `[SUS]`/`[ASSUMED]` packages. `package.json:21` declares `"@ai-sdk/openai-compatible": "^3.0.22"`; `package-lock.json:12` + lockfile node resolves 3.0.22 (Apache-2.0); `npm ls` → 3.0.22 non-extraneous. | CLOSED |
| T-25-05 | Spoofing | missingProviderKey gate | mitigate | All-or-nothing at run entry: exactly 4 guards in order ANTHROPIC / OPENROUTER / NOUSRESEARCH / OPENCODE, each `providers.has('<p>') && !env.<KEY> → return '<KEY>'` (`src/lib/agents/analyzeCompany.ts:63-66`), first-hit wins, runAgent never reached on gate failure (`:97-99`). opencode + opencode-go collapse to logical 'opencode' via SNAPSHOT_PROVIDER_IDS (`src/lib/models/catalog.ts:112`) — one key, no special-casing. RUN-03 gate tests green: nousresearch-only names `NOUSRESEARCH_API_KEY`, opencode-only names `OPENCODE_API_KEY`, opencode-only with ANTHROPIC+NOUSRESEARCH cleared passes, mixed 4-provider passes (`src/lib/agents/analyzeCompany.test.ts:367-451`). | CLOSED |
| T-25-06 | Information Disclosure | key names in not_configured responses | accept | Gate returns key NAME only — string constants (`analyzeCompany.ts:63-66`), `AnalyzeResult.missingKey` is a name, never a value; values live in server-only env (`src/lib/env.ts:47,54`, non-PUBLIC_); fixed server-side constants (no client input echo). Documented in the Accepted Risks log below (AR-25-01). | CLOSED |
| T-25-07 | Tampering | provider-set derivation | mitigate | Provider set catalog-derived via `getProviderForModelId` (`src/lib/agents/analyzeCompany.ts:60`), priority-ordered + fail-closed null (`src/lib/models/catalog.ts:174-179`); filter predicate widened to full `ModelProviderId` type (`analyzeCompany.ts:14,61` — `(p): p is ModelProviderId => p !== null`), no literal-union drift between gate and registry. | CLOSED |
| T-25-08 | Tampering | shouldAdvance predicate | mitigate | Verify-only contract (D-25-04): `git diff src/lib/agents/modelConfig.ts` EMPTY (zero-change proof, verified live — exit 0, no hunks, no working-tree status). 16-cell data-driven matrix over `SERVABLE_PROVIDERS` — exactly 4 same-provider false cells + 12 cross-provider true (`src/lib/agents/modelConfig.test.ts:161-167`, import `SERVABLE_PROVIDERS` at `:11`); billing never-eligible via `isFailoverEligible('billing') === false` loop (`:187-191`); null identity fail-closes a 429 (`:193-198`). Data-driven loop — no hand-encoded branch drift. | CLOSED |
| T-25-09 | Denial of Service | failover loop behavior | mitigate | Same-provider 429 never-advance preserved (`src/lib/agents/modelConfig.ts:105-106`); Zen↔Go canary `shouldAdvance('rate_limited','opencode','opencode') === false` (`src/lib/agents/modelConfig.test.ts:169-175` — a Zen→Go hop arrives as from='opencode', to='opencode' via SNAPSHOT_PROVIDER_IDS); fail-loud throw on chain exhaustion (`src/lib/agents/runAgent.ts:110,113`) — never an endless fallback burn. 16-cell matrix is the permanent regression lock. | CLOSED |
| T-25-10 | Tampering | runAgent 429 hop decision | mitigate | from/to identity catalog-derived via `getProviderForModelId` on model ids only, never the error body (`src/lib/agents/runAgent.ts:107-108`); `git diff src/lib/agents/runAgent.ts` EMPTY (verified live) — tests lock EXISTING behavior. New loop tests: anthropic→opencode and nousresearch→anthropic advance on 429 (`src/lib/agents/runAgent.test.ts:331-347`); Zen↔Go same-provider canary `['m3','m5']` rejects after exactly 1 generateText call (`:354-361`); mock maps 'm3'/'m5'→opencode, 'm4'→nousresearch (`:22-32`). | CLOSED |
| T-25-11 | Integrity | model_used audit identity | mitigate | `modelIdOf` returns `.modelId` verbatim for object-form models / the id string as-is (`src/lib/agents/runAgent.ts:35-37`); bare-id audit test locks `modelUsed === 'm3'` with no prefix surgery, no endpoint label (`src/lib/agents/runAgent.test.ts:367-374`); provider derivation for model_chain flows through the priority-ordered registry — 6/6 real-snapshot identity smoke re-run and passing (`npx tsx` → `6/6 provider identities correct`, exit 0: claude-sonnet-4-6→anthropic, claude-sonnet-5/deepseek-v4-flash/hy3/big-pickle→opencode, hermes-4-70b→nousresearch). | CLOSED |
| T-25-12 | Spoofing | provider identity at the hop | mitigate | Identity only from `PROVIDER_PRECEDENCE` registry (`src/lib/models/catalog.ts:123,174-179`), never error body/client input; fail-closed: `to === null` (last model / catalog drift) never advances a 429 (`runAgent.ts:108` + `shouldAdvance` null checks, locked in `modelConfig.test.ts:193-198`); 6/6 identity smoke re-verifies real-snapshot resolution each run (verified live above). | CLOSED |

**Closed: 13/13 | Open: 0/13**

## Accepted Risks

| Risk ID | Threat | Description | Rationale / Owner | Status |
|---------|--------|-------------|-------------------|--------|
| AR-25-01 | T-25-06 | The chain-aware env gate's `not_configured` response names the exact MISSING KEY (`'NOUSRESEARCH_API_KEY'` / `'OPENCODE_API_KEY'` / `'ANTHROPIC_API_KEY'` / `'OPENROUTER_API_KEY'`) when a resolved chain's provider key is unset | Accepted by design (T-20-09 doctrine, D-20-10 execution, D-25-05): the gate returns only the KEY NAME — a fixed server-side constant string, never a key VALUE and never client input echoed back. Values live exclusively in server-only non-PUBLIC_ env vars (`src/lib/env.ts:47,54`) and are never logged, never serialized to the client, never included in persisted audit rows. Exposure is a provider-configuration instruction (which key to set in Vercel), not a credential leak. The named-key UX is required so staff can tell which provider to configure (D-20-04 — no Settings UI until Phase 26). Owner: Phase 25 (accepted at plan time, T-25-06 row). | ACCEPTED |

## Audit Trail

- **Register source:** PLAN.md threat models for plans 01-04 (T-25-01..T-25-SC, T-25-05..07, T-25-08..09, T-25-10..12), authored at plan time; dispositions unchanged. 13 threats total.
- **Unregistered flags:** NONE — all four SUMMARY.md files (25-01..25-04) declare `## Threat Flags: None`, and no new attack surface was observed during verification beyond the register (new surface — 5 instances' explicit `process.env.*` key reads + the opencode dispatch branch — is covered by T-25-01..04/T-25-SC; test-only files carry no runtime surface).
- **WR-01 verification (as directed):** the code-review claim that `createAnthropic` falls back to `ANTHROPIC_API_KEY` when `OPENCODE_API_KEY` is undefined is CONFIRMED at the SDK level (`node_modules/@ai-sdk/anthropic/dist/index.js:6542-6544` — `loadApiKey({ environmentVariableName: "ANTHROPIC_API_KEY" })`); the reviewer's "unreachable behind the RUN-03 gate" claim is ALSO CONFIRMED: `instantiateChain` has exactly one production caller (`analyzeCompany.ts:111`), and the 4-guard `missingProviderKey` (`:63-66`, checked `:96-99`) returns `not_configured` before any opencode chain (including the two anthropic-endpoint instances' ids, which resolve to logical 'opencode') can reach construction. Not an open threat: the mitigation's declared protective mechanism (explicit pass + gate unreachability) is present and verified. **Note for the record:** the mitigation plan's "no SDK env auto-load" wording is accurate for the 3 openai-compatible instances (dist l.1749, verified) but NOT for the 2 `createAnthropic` instances — they would transmit the Anthropic credential to the OpenCode endpoints if ever constructed with an unset key. The RUN-03 gate is the load-bearing control; WR-01's recommended constructor fail-loud (`if (!opencodeApiKey) throw`) remains an optional hardening for a future non-gated caller.
- **WR-02 / WR-03 (code-review findings, assessed, non-blocking):** WR-02 (apiKey-presence tests assert presence, not value) weakens the test lock on T-25-01 but the production mitigation (explicit `process.env.*` pass) is present in the implementation — the tests are not the mitigation. WR-03 (manual key-restore in gate tests risks cascade on mid-test failure) is a test-isolation robustness gap, not a security threat; the gate code itself is correct. Both are maintainability debt, not open threats.
- **Verification method:** each `mitigate` threat confirmed by direct grep/read evidence in the cited files plus live execution: `npx vitest run src/lib/agents/modelFactory.test.ts src/lib/agents/modelConfig.test.ts src/lib/agents/analyzeCompany.test.ts src/lib/agents/runAgent.test.ts src/lib/verification/security-grep.test.ts` → 5 files, 104 tests passed; `npx tsc --noEmit` → exit 0; `npm ls @ai-sdk/openai-compatible` → 3.0.22 non-extraneous; lockfile node resolves 3.0.22 (Apache-2.0); 6/6 real-snapshot identity smoke → exit 0.
- **Zero-change contract (live proof):** `git diff src/lib/agents/runAgent.ts src/lib/agents/modelConfig.ts src/lib/env.ts src/lib/models/catalog.ts` → EMPTY (exit 0, no hunks); `git status --short` on the same 4 files → no entries. Phase 25's production changes are confined to modelFactory.ts + analyzeCompany.ts (and package.json/package-lock.json), matching the plan's files_modified.
- **Snapshot spot-check (independent probe):** dual-listed npm trap confirmed real — `minimax-m2.7`/`minimax-m3`: opencode (Zen) row `api.npm = @ai-sdk/openai-compatible`, opencode-go (Go) row `api.npm = @ai-sdk/anthropic`; `qwen3.6-plus` both rows anthropic-npm; `hy3` go-exclusive openai-compatible. The dispatch canaries assert against meaningful data, not vacuous rows.
- **Pre-existing items noted (not phase regressions, not open threats):** VER-03 `openrouter-only-chain.test.ts` live billing failure (uncredited OPENROUTER_API_KEY → 402, documented STATE.md + deferred-items.md, fails identically at baseline — not a 25-xx regression). Vercel env declaration of `NOUSRESEARCH_API_KEY` + `OPENCODE_API_KEY` remains a deferred operator action per STATE.md (until declared, opencode/nousresearch chains return `not_configured` naming the key — the intended fail-safe, D-15 doctrine).
- **Implementation files were READ-ONLY** during this audit; only this SECURITY.md was written.

---

_Audited: 2026-08-04 by gsd-security-auditor — result SECURED (13/13 closed, 0 open, 0 unregistered flags)_
