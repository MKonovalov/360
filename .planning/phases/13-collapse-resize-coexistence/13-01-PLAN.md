---
phase: 13-collapse-resize-coexistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/sidebar-collapse.ts, src/lib/sidebar-collapse.test.ts, src/components/layout/app-sidebar.tsx, src/components/layout/sidebar-resize-handle.tsx]
autonomous: true
requirements: [COLR-01, COLR-02, COLR-03]

must_haves:
  truths:
    - "User can click the collapse button (top-right of the sidebar header, ALWAYS visible) and the sidebar animates to a 48px icon rail — icons stay, labels fade (COLR-01, D-01/D-02/D-03)"
    - "Clicking again (or ⌘B, or the topbar trigger) re-expands the sidebar to the last persisted sidebar_width — the two cookie contracts never fight (COLR-02, D-05/D-06)"
    - "In the rail, every interactive icon (4 nav rows, feedback pill, user avatar, collapse button) shows a right-side ~200ms tooltip on hover AND keyboard focus; Reviews shows 'Reviews (N)' when pendingCount > 0 (COLR-03, D-07/D-08/D-09)"
    - "The rail header shows the 28px letter-mark centered below the collapse button while the wordmark fades; the active nav pill keeps the ≥3:1 token contrast (COLR-03, D-11)"
    - "Drag-resize still clamps 200-400px and persists sidebar_width, but the handle is hidden while collapsed; sidebar_state cookie + ⌘B machinery byte-identical (COLR-02, D-04)"
  artifacts:
    - path: "src/lib/sidebar-collapse.ts"
      provides: "Pure D-08 copy helpers — getCollapseToggleLabel + getNavTooltipLabel (reviews-count tooltip string)"
      exports: ["getCollapseToggleLabel", "getNavTooltipLabel"]
      contains: "export function getNavTooltipLabel"
    - path: "src/lib/sidebar-collapse.test.ts"
      provides: "Vitest lock on the D-08 copy contract ('Reviews (N)' only when pendingCount > 0)"
      contains: "describe('getNavTooltipLabel'"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Rail activation: collapsible=\"icon\", TooltipProvider mount, header collapse button + letter-mark, tooltip props on 6 SidebarMenuButtons"
      contains: "collapsible=\"icon\""
      min_lines: 250
    - path: "src/components/layout/sidebar-resize-handle.tsx"
      provides: "D-04 collapse hide via useSidebar() early return — clamp/cookie untouched"
      contains: "state === 'collapsed'"
  key_links:
    - from: "src/components/layout/app-sidebar.tsx (header collapse button)"
      to: "useSidebar().toggleSidebar (sidebar.tsx:91-93)"
      via: "onClick={toggleSidebar} — ONE shared open state with ⌘B and the topbar trigger; sidebar_state cookie write stays in the vendored setOpen"
      pattern: "toggleSidebar"
    - from: "src/components/layout/app-sidebar.tsx (6 SidebarMenuButtons)"
      to: "vendored SidebarMenuButton tooltip prop (sidebar.tsx:490-538)"
      via: "tooltip={getNavTooltipLabel(...)} / tooltip=\"Give us feedback\" / tooltip={getUserDisplayName(user)} — the built-in rail tooltip (hidden unless collapsed && !isMobile)"
      pattern: "tooltip="
    - from: "src/components/layout/app-sidebar.tsx (letter-mark)"
      to: "group-data-[collapsible=icon] rail activation"
      via: "hidden ... group-data-[collapsible=icon]:flex — dormant-toggle precedent from the Reviews dot (app-sidebar.tsx:130) and pill icon (:149)"
      pattern: "group-data-[collapsible=icon]:flex"
    - from: "src/components/layout/sidebar-resize-handle.tsx"
      to: "useSidebar().state (SidebarProvider containment at app-shell-layout.tsx:35-42)"
      via: "early return when collapsed — no imperative --sidebar-width write can run mid-collapse, so the cookie-threaded var stays for automatic restore (D-05)"
      pattern: "state === 'collapsed'"
    - from: "src/components/layout/app-sidebar.tsx (nav-row + collapse-button tooltips)"
      to: "src/lib/sidebar-collapse.ts"
      via: "getNavTooltipLabel / getCollapseToggleLabel — D-08 copy delegated to tested pure functions"
      pattern: "getNavTooltipLabel"
---

<objective>
Activate the dormant icon-rail collapse system and make the collapsed rail legible: flip the single `collapsible="icon"` switch on the vendored `<Sidebar>`, mount the vendored `TooltipProvider` with the ~200ms delay, add the always-visible Exa collapse button (lucide `PanelLeftClose` ⇄ `PanelLeftOpen`, top-right) driving shadcn's ONE shared open state, add the 28px letter-mark below it, add rail tooltips to all six interactive icons, and hide the drag-resize handle while collapsed — all while leaving the frozen drag-resize/cookie contract, the vendored primitives, and `globals.css` byte-identical. Delivers COLR-01, COLR-02, COLR-03.

Purpose: This is the activation plan of Phase 13. Research verified (HIGH confidence, against the actual files) that the entire icon-rail mechanism — `data-collapsible={state === "collapsed" ? collapsible : ""}` (sidebar.tsx:211), the `group-data-[collapsible=icon]:` width/label/tooltip classes, `--sidebar-width-icon: 3rem`, the `SidebarMenuButton` `tooltip` prop (sidebar.tsx:490-538), and `useSidebar()`'s shared state — shipped dormant in Phases 10-12 and in the vendored primitive. The whole activation is consumer work in the phase's own files: one prop, one provider mount, props on existing elements, a header restructure, and a two-line hide on the resize handle. The D-08 tooltip copy is contract-locked (10-UI-SPEC §Copywriting Contract), so the exact strings are extracted into a tested pure helper first (mirrors the Phase 12 `user.ts`-first ordering) to prevent a drive-by wording edit from silently breaking the copy contract.

Output: `src/lib/sidebar-collapse.ts` + `src/lib/sidebar-collapse.test.ts` (pure copy helpers + Vitest lock), the `app-sidebar.tsx` rail-activation diff (collapsible prop, provider mount, header button + letter-mark, six tooltip props), and the `sidebar-resize-handle.tsx` collapse-hide diff. Live-browser rendering (animation, tooltip timing) is Phase 14's UAT contract; this plan verifies via type gates, unit locks, and source-assertion grep/fence gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (§Phase 13 — success criteria 1-3)
@.planning/STATE.md (§Accumulated Context — Phase 10-12 decisions; §Session Continuity — Phase 13 context gathered)
@.planning/REQUIREMENTS.md (COLR-01, COLR-02, COLR-03)
@.planning/phases/13-collapse-resize-coexistence/13-CONTEXT.md (D-01..D-12 LOCKED; §Canonical References — frozen contracts; §Deferred Ideas — ignore)
@.planning/phases/13-collapse-resize-coexistence/13-RESEARCH.md (§Summary; §Pattern 1 activation switch; §Pattern 2 tooltip prop; §Pattern 3 collapse button; §Pattern 4 resize-hide; §Pattern 5 letter-mark; §Don't Hand-Roll; §Common Pitfalls 1-6; §Code Examples; §Security Domain)
@.planning/phases/13-collapse-resize-coexistence/13-PATTERNS.md (§app-sidebar.tsx insertion points + import block; §sidebar-resize-handle.tsx D-04 insertion; §sidebar-collapse.ts whole-file template; §Vendored primitives contracts; §No Analog Found planner notes 1-4)
@.planning/phases/13-collapse-resize-coexistence/13-VALIDATION.md (per-task map rows 13-01-01..04; sampling rate; Wave 0 note — the sidebar-collapse pair is this plan's only new test infra)
@.planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md (§D1 letter-mark treatment, §D3 48px rail stock 3rem, §D4 portal policy, §Interaction & State Contract Collapsed row, §Copywriting Contract — 'Reviews ({n})' row)
@.planning/phases/12-branding-user-zones/12-01-SUMMARY.md (the committed header/footer pre-wired dormant classes this plan activates: wordmark fade, pill icon-only, avatar center + name hidden)
@.planning/phases/11-nav-items-restyle/11-01-SUMMARY.md (dormant-class precedent, grep-gate hygiene Rule 1)
@src/components/layout/app-sidebar.tsx (the file being modified — 201 lines, current state)
@src/components/layout/sidebar-resize-handle.tsx (the file being modified — 91 lines, current state)
@src/components/ui/sidebar.tsx (vendored — READ ONLY; useSidebar 46-53, setOpen cookie write 84-85, toggleSidebar 91-93, data-collapsible switch 211, SidebarMenuButton tooltip prop 490-538, SidebarHeader 331-340)
@src/components/ui/tooltip.tsx (vendored — READ ONLY; TooltipProvider 8-19, TooltipContent 33-55)
@src/components/ui/button.tsx (vendored — READ ONLY; size="icon" = size-8 at :29)
@src/lib/nav.ts + @src/lib/nav.test.ts (the pure-function/test precedent this plan mirrors)
@src/lib/user.ts + @src/lib/user.test.ts (the getUserDisplayName consumer for the avatar tooltip)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract src/lib/sidebar-collapse.ts pure copy helpers + src/lib/sidebar-collapse.test.ts Vitest lock (COLR-03, D-08 copy contract)</name>
  <files>src/lib/sidebar-collapse.ts, src/lib/sidebar-collapse.test.ts</files>
  <read_first>
    - src/lib/nav.ts — the exact analog: why-comment block, named exports only, total pure function, no imports. The shape `sidebar-collapse.ts` must mirror (CONVENTIONS: single quotes, semicolons, 2-space indent, no JSDoc). Also the source of the `NavKey` union (line 6).
    - src/lib/nav.test.ts — the exact analog test: `import { describe, it, expect } from 'vitest';` + relative import of the module under test + one `describe` per function + `expect(...).toBe(...)`.
    - .planning/phases/13-collapse-resize-coexistence/13-PATTERNS.md §src/lib/sidebar-collapse.ts — the whole-file template (why-comment, `import type { NavKey } from '@/lib/nav'`, both function bodies) and §src/lib/sidebar-collapse.test.ts — the case set (verbatim labels for non-Reviews keys; the D-08 lock `('reviews', 0)` → 'Reviews' / `('reviews', 3)` → 'Reviews (3)'; `('expanded')` → 'Collapse' / `('collapsed')` → 'Expand').
    - .planning/phases/13-collapse-resize-coexistence/13-RESEARCH.md §Code Examples (the optional pure helper + test block) and §Phase Requirements COLR-03 row (the copy contract source).
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Copywriting Contract (the exact 'Reviews ({n})' row — the contract this test locks).
    - vitest.config.ts — include `src/**/*.test.ts` (line 12): the test file MUST live at exactly `src/lib/sidebar-collapse.test.ts` to be auto-discovered.
  </read_first>
  <action>
    Create `src/lib/sidebar-collapse.ts` — a dependency-free pure-function module mirroring `src/lib/nav.ts`:
    - Open with a 3-4 line why-comment: the collapsed-rail tooltip copy is contract-locked (D-08 / 10-UI-SPEC §Copywriting Contract: 'Reviews (N)' when the pending count is positive, else 'Reviews'), so the exact strings live here under Vitest — a drive-by wording edit in the sidebar can never silently break the copy contract. The comment must NOT quote the swept class strings, the copy strings ('Reviews (N)'), or `NavKey` (the tsc/grep gates count them) — describe the mechanism without literals (11-02 Rule 1).
    - `import type { NavKey } from '@/lib/nav';` — type-only import of the existing union (nav.ts:6); do NOT re-declare the union.
    - `export function getCollapseToggleLabel(state: 'expanded' | 'collapsed'): 'Collapse' | 'Expand'` returning `state === 'collapsed' ? 'Expand' : 'Collapse'` (D-08 collapse-button tooltip copy).
    - `export function getNavTooltipLabel(key: NavKey, pendingCount: number): string` — `if (key === 'reviews') return pendingCount > 0 ? \`Reviews (${pendingCount})\` : 'Reviews';` then return the verbatim label from the object literal `{ start: 'Start', companies: 'Companies', personas: 'Key Personas' }[key]` (D-08 nav-row copy; the reviews branch is the only count-aware one).
    - Named exports only; no default export; no imports from the repo beyond the type import; no JSDoc. Total functions — every input matching the declared shape maps to a string, never throws (matches nav.ts's terminal-return convention).
    Create `src/lib/sidebar-collapse.test.ts` — mirroring `src/lib/nav.test.ts`:
    - `import { describe, it, expect } from 'vitest';` and ONE import line `import { getCollapseToggleLabel, getNavTooltipLabel } from './sidebar-collapse';` (relative import — the repo test convention; the module under test must appear on exactly one line so the gate `grep -Fc "from './sidebar-collapse'"` = 1 holds). No fixtures needed — both functions are string/number-param pure functions.
    - `describe('getNavTooltipLabel')` — five cases: `('start', 0)` → 'Start'; `('companies', 5)` → 'Companies'; `('personas', 0)` → 'Key Personas' (verbatim labels, count ignored); the D-08 lock `('reviews', 0)` → 'Reviews' and `('reviews', 3)` → 'Reviews (3)'.
    - `describe('getCollapseToggleLabel')` — two cases: `('expanded')` → 'Collapse'; `('collapsed')` → 'Expand'.
    - `expect(...).toBe(...)` per case; one `it` per behavior; single quotes; semicolons.
    Run `npx vitest run src/lib/sidebar-collapse.test.ts --bail=1` and `npx tsc --noEmit` — both files land together (function-first order, NOT a RED tree: the repo's next build type-checks test files, so the tree must never be red between commits). Use `--bail=1`, NOT `-x` (the `-x` alias was removed in Vitest 4 — Phase 12 Deviation 1).
  </action>
  <verify>
    <automated>
      npx vitest run src/lib/sidebar-collapse.test.ts --bail=1
      npx tsc --noEmit
      grep -c 'export function getNavTooltipLabel' src/lib/sidebar-collapse.ts
      # expect 1
      grep -c 'export function getCollapseToggleLabel' src/lib/sidebar-collapse.ts
      # expect 1
      grep -Fc "from '@clerk/types'" src/lib/sidebar-collapse.ts
      # expect 0 — no Clerk type import; the helper only consumes NavKey
      grep -Fc "from './sidebar-collapse'" src/lib/sidebar-collapse.test.ts
      # expect 1 — relative import per repo test convention
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/sidebar-collapse.test.ts --bail=1` exits 0 with all 7 cases passing (5 nav-tooltip + 2 collapse-toggle).
    - `npx tsc --noEmit` exits 0.
    - `getNavTooltipLabel` uses the exact D-08 chain: `if (key === 'reviews') return pendingCount > 0 ? 'Reviews (${pendingCount})' : 'Reviews'` and the verbatim `{ start: 'Start', companies: 'Companies', personas: 'Key Personas' }` map — no other copy strings anywhere in the file.
    - `getCollapseToggleLabel` maps 'expanded' → 'Collapse' and 'collapsed' → 'Expand' exactly.
    - Named exports only; no default export; no JSDoc; single quotes + semicolons per CONVENTIONS; no repo import except the type-only `NavKey` import.
  </acceptance_criteria>
  <done>sidebar-collapse.ts extracts the D-08 copy helpers; sidebar-collapse.test.ts locks the copy contract green (7/7); tsc clean; the file pair is committed atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Rail activation in app-sidebar.tsx — collapsible="icon" + TooltipProvider mount + collapse button + letter-mark + tooltip props on 6 SidebarMenuButtons (COLR-01, COLR-02, COLR-03)</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx — the file being modified. Current state: `<Sidebar>` at line 56 (offcanvas default), `SidebarHeader className="gap-1 p-3"` at line 57, wordmark fade wrapper at line 58 (VERBATIM contract), nav rows at 68-77 / 80-89 / 92-101 / 109-118, Reviews badge/dot at 119-133, pill at 141-152, user trigger at 164-181. Import block lines 1-29. Existing why-comments at lines 31-42 and 48-52 stay untouched.
    - .planning/phases/13-collapse-resize-coexistence/13-CONTEXT.md — D-01 (button in SidebarHeader, top-right, NOT topbar), D-02 (always visible, icon swap by state, 32px square in the rail), D-03 (2-row flex header: button row + letter-mark row below, no overlap), D-05 (restore automatic — zero new width code), D-06 (button drives useSidebar().toggleSidebar — ONE state with ⌘B/topbar), D-07/D-08/D-09 (tooltips on all 6 interactive icons, locked copy, hover+focus, side right, ~200ms), D-10 (compose pre-wired dormant classes as-is), D-11 (letter-mark tokens verbatim), D-12 (p-2 header, 48px stock rail).
    - .planning/phases/13-collapse-resize-coexistence/13-PATTERNS.md §app-sidebar.tsx — the exact insertion points (imports, activation switch at line 56, header restructure block with the button + letter-mark + VERBATIM Q4 wordmark block, nav-row/pill/user tooltip prop additions, the useSidebar destructure) and §No Analog Found planner notes 1-4 (TooltipProvider mount in app-sidebar NOT app-shell-layout; manual Tooltip for the collapse button — NOT a SidebarMenuButton so the tooltip prop doesn't apply; nested Slot composition on the user trigger).
    - .planning/phases/13-collapse-resize-coexistence/13-RESEARCH.md §Common Pitfalls — Pitfall 1 (offcanvas default → must set collapsible="icon"), Pitfall 2 (TooltipProvider never mounted → 700ms default; the mount fixes D-09), Pitfall 3 (grep-gate arithmetic drift — `bg-sidebar-primary` already on the avatar at line 173 and `group-data-[collapsible=icon]:block` already on the dot at 130 / pill icon at 149 — line-scope the new-content gates), Pitfall 4 (wordmark fade must stay `opacity-0` verbatim — do NOT switch to `hidden`), Pitfall 6 (Slot-in-Slot on the user trigger — runtime-verify; fallback = manual Tooltip at DropdownMenuTrigger level).
    - src/components/ui/sidebar.tsx — READ ONLY. Verify the contracts this task consumes: `useSidebar` returns `{ state, open, setOpen, openMobile, setOpenMobile, isMobile, toggleSidebar }` (46-53, throws outside the provider — AppSidebar renders inside SidebarProvider at app-shell-layout.tsx:36, so legal); `toggleSidebar` (91-93); `setOpen` writes the `sidebar_state` cookie (84-85); `data-collapsible={state === "collapsed" ? collapsible : ""}` (211) — the switch all `group-data-[collapsible=icon]:` selectors hang off; `SidebarMenuButton` `tooltip` prop wraps in Tooltip/TooltipTrigger and renders `TooltipContent side="right" align="center" hidden={state !== "collapsed" || isMobile}` (490-538) — the built-in rail tooltip, armed only in the desktop collapsed rail; `SidebarHeader` is a bare `flex flex-col gap-2 p-2` div (331-340) — the className override comes from app-sidebar.
    - src/components/ui/tooltip.tsx — READ ONLY. `TooltipProvider` (8-19, default delayDuration = 0 at the vendored boundary — passing 200 overrides it); `TooltipContent` portals to document.body with `bg-foreground text-background` (33-55) — the D4 app-theme portal, correct as-is; exports Tooltip/TooltipTrigger/TooltipProvider/TooltipContent (57).
    - src/components/ui/button.tsx — READ ONLY. `size="icon"` = `size-8` (32px) at line 29; the button auto-sizes its child svg to 16px.
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §D1 (letter-mark: 28px rounded-md box, `bg-sidebar-primary text-sidebar-primary-foreground`, white 'A' glyph, Geist 600 13px), §D3 (48px rail, stock `--sidebar-width-icon: 3rem`, no override), §D4 (portaled tooltips use global tokens — correct by design), §Copywriting Contract (tooltip copy rows).
  </read_first>
  <action>
    Edit `src/components/layout/app-sidebar.tsx` to activate the rail. Changes, in order:
    1. Extend the lucide-react import (line 6) to add `PanelLeftClose, PanelLeftOpen` (bare-name style, alphabetical within the list).
    2. Add `import { Button } from '@/components/ui/button';`.
    3. Add `import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip';`.
    4. Add `useSidebar` to the existing `'@/components/ui/sidebar'` import block (lines 17-29), alphabetical placement.
    5. Add `import { getCollapseToggleLabel, getNavTooltipLabel } from '@/lib/sidebar-collapse';`.
    6. In the AppSidebar function body, after `const activeKey = getActiveNavKey(pathname);` (line 46), add `const { state, toggleSidebar } = useSidebar();` with a 3-4 line why-comment above it: the collapse button drives the ONE shared open state — the same toggle the vendored ⌘B handler and the topbar trigger call; the sidebar_state cookie write lives inside the vendored setOpen and stays byte-identical, so the button is just another caller (D-06), not a new state machine. The comment must NOT contain `useSidebar`, `toggleSidebar`, `collapsible="icon"`, or swept strings (11-02 Rule 1).
    7. Wrap the return with the TooltipProvider mount: change `return (` (line 55) to `return (<TooltipProvider delayDuration={200}>` and close `</TooltipProvider>` immediately after `</Sidebar>` (line 199). Add a 2-3 line why-comment above the return: the vendored tooltip provider is defined but never mounted anywhere today, so unmounted Radix tooltips would run at the ~700ms default; this mount sets the short ~200ms delay (D-09) and is usage of the vendored export — not an edit (the provider file stays untouched). The comment must NOT contain `delayDuration`, `TooltipProvider`, or swept strings.
    8. Change `<Sidebar>` (line 56) to `<Sidebar collapsible="icon">` — the single activation switch (COLR-01). Add a 1-2 line why-comment above it: the vendored component's outer div only carries the icon-collapse data attribute when this prop is set AND the state is collapsed, which is what arms every pre-wired collapsed-rail selector from Phases 11-12. Do NOT thread a collapsible prop through app-shell-layout.tsx (frozen contract — hardcoding here is the one-line change in the phase's own file). The comment must NOT contain the literal `collapsible="icon"`.
    9. Restructure the header (lines 57-62) to the D-03/D-12 2-row flex layout. Change the SidebarHeader className from `gap-1 p-3` to `gap-1 p-2` (D-12). Inside, in order:
       - Row 1: a `<div className="flex justify-end">` (D-03 — button top-right) containing a MANUAL Tooltip composition (the button is a plain `Button`, NOT a SidebarMenuButton, so the `tooltip` prop does not apply — No Analog Found note 2): `<Tooltip>` wrapping `<TooltipTrigger asChild>` wrapping a `<Button variant="ghost" size="icon" aria-label={state === 'collapsed' ? 'Expand sidebar' : 'Collapse sidebar'} onClick={toggleSidebar} className="text-sidebar-foreground">` whose children swap by state — `<PanelLeftOpen />` when collapsed, `<PanelLeftClose />` when expanded (D-02). Then `<TooltipContent side="right">{getCollapseToggleLabel(state)}</TooltipContent>` (D-08 copy; NO hidden gate — the tooltip shows in BOTH states per D-02). The tooltip content renders the helper result, not the aria-label strings.
       - Row 2: a `<div className="flex flex-col gap-1">` containing, FIRST, the D-11 letter-mark div — `aria-hidden="true"` (the faded wordmark below stays in the accessibility tree, so the mark is decorative — RESEARCH A3) with className exactly `hidden size-7 items-center justify-center rounded-md bg-sidebar-primary text-sidebar-primary-foreground text-[13px] font-semibold group-data-[collapsible=icon]:flex` and glyph `A`. THEN the existing wordmark block with its class list VERBATIM (line 58: `group-data-[collapsible=icon]:opacity-0 transition-opacity duration-200` and the two `<p>` elements unchanged — do NOT switch to `hidden`, do NOT reorder the letter-mark after it; Pattern 5 ordering: letter-mark snug under the button, faded wordmark trailing).
    10. Add a `tooltip` prop line to each of the 4 nav SidebarMenuButtons, consuming the helper (D-07/D-08): Start row (68-77) → `tooltip={getNavTooltipLabel('start', pendingCount)}`; Companies row (80-89) → `tooltip={getNavTooltipLabel('companies', pendingCount)}`; Key Personas row (92-101) → `tooltip={getNavTooltipLabel('personas', pendingCount)}`; Reviews row (109-118) → `tooltip={getNavTooltipLabel('reviews', pendingCount)}` (the helper's reviews branch yields the count-aware copy). Do NOT hand-roll these tooltips — the vendored `tooltip` prop is the entire rail mechanism (hidden unless collapsed && !isMobile, side right, D4 portal).
    11. Add `tooltip="Give us feedback"` to the pill's SidebarMenuButton (141-144) — D-08 lock matching its existing aria-label (line 145). The asChild `<a>` child is fine: the vendored tooltip wraps the rendered button regardless of the Slot child (research verified the prop works with asChild).
    12. Add `tooltip={getUserDisplayName(user)}` to the user-trigger SidebarMenuButton (165-168, inside `DropdownMenuTrigger asChild`) — D-08 avatar copy. NOTE (RESEARCH A2 / Pitfall 6): this stacks two Radix asChild boundaries (DropdownMenuTrigger → SidebarMenuButton's own TooltipTrigger); React 19's ref-as-prop makes it work per the documented composition. RUNTIME-VERIFY at the end of this task: with `npm run dev`, sign in, collapse, hover the avatar → tooltip shows the name; click the avatar → the dropdown still opens. If Slot-in-Slot misbehaves (dropdown fails to open / console warning), use the documented fallback: remove the `tooltip` prop from the trigger and wrap the `<DropdownMenu>` in a manual `<Tooltip><TooltipTrigger asChild>`... composition at the DropdownMenuTrigger level (same shape as the collapse button's manual Tooltip).
    13. CRITICAL GREP HYGIENE (11-02 Rule 1 / Pitfall 3): no new or modified comment in this file may contain the literal strings the gates count — `collapsible="icon"`, `tooltip=`, `delayDuration`, `PanelLeftClose`, `PanelLeftOpen`, `size="icon"`, `toggleSidebar`, `useSidebar`, `bg-sidebar-primary`, `text-sidebar-primary-foreground`, `group-data-[collapsible=icon]:flex`, `aria-hidden="true"`, `Collapse sidebar`, `Expand sidebar`, `SidebarHeader className="gap-1 p-2"`, or any swept token (indigo/amber/hex/dark:). Describe mechanisms, never quote literals.
    Do NOT touch: the nav groups' other props (isActive/className/href), the pendingCount badge block (119-133), the footer pill/user zone beyond the tooltip props, the existing why-comments, or any file other than app-sidebar.tsx. Zero new width/animation CSS, zero cookies, zero `setProperty`/`sidebar_width` in this file (COLR-02/D-05/D-06 — the rail width, fade, and restore are all dormant-class + frozen-var behavior).
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      grep -c 'collapsible="icon"' src/components/layout/app-sidebar.tsx
      # expect 1 — the single activation switch (COLR-01; VALIDATION row 13-01-01)
      grep -c '<TooltipProvider' src/components/layout/app-sidebar.tsx
      # expect 1 — the D-09 provider mount (VALIDATION row 13-01-03)
      grep -c 'delayDuration' src/components/layout/app-sidebar.tsx
      # expect 1 — the mount prop only (comments must not add a second)
      grep -c 'PanelLeftClose' src/components/layout/app-sidebar.tsx
      # expect >= 1 (import line + expanded-state usage) — VALIDATION row 13-01-02
      grep -c 'PanelLeftOpen' src/components/layout/app-sidebar.tsx
      # expect >= 1 (import line + collapsed-state usage) — VALIDATION row 13-01-02
      grep -c 'size="icon"' src/components/layout/app-sidebar.tsx
      # expect 1 — the 32px collapse button (D-02/D-12)
      grep -c 'toggleSidebar' src/components/layout/app-sidebar.tsx
      # expect 2 — destructure + onClick (D-06)
      grep -c 'useSidebar' src/components/layout/app-sidebar.tsx
      # expect >= 1 — first app-level consumer (VALIDATION row 13-01-02)
      grep -c 'tooltip=' src/components/layout/app-sidebar.tsx
      # expect 6 — 4 nav rows + pill + user trigger (COLR-03; VALIDATION row 13-01-01; comments must not add lines)
      grep -c 'getNavTooltipLabel' src/components/layout/app-sidebar.tsx
      # expect 5 — import + 4 nav-row usages
      grep -c 'getCollapseToggleLabel' src/components/layout/app-sidebar.tsx
      # expect 2 — import + TooltipContent usage
      grep -c 'bg-sidebar-primary text-sidebar-primary-foreground text-\[13px\] font-semibold' src/components/layout/app-sidebar.tsx
      # expect 1 — the letter-mark composite (line-scoped; the avatar initials circle uses text-[10px]) — VALIDATION row 13-01-03
      grep -c 'group-data-\[collapsible=icon\]:flex' src/components/layout/app-sidebar.tsx
      # expect 1 — letter-mark toggle (unique; the dot/pill use :block) — VALIDATION row 13-01-03
      grep -c 'group-data-\[collapsible=icon\]:opacity-0' src/components/layout/app-sidebar.tsx
      # expect 1 — Q4 wordmark fade VERBATIM (Pitfall 4)
      grep -c 'aria-hidden="true"' src/components/layout/app-sidebar.tsx
      # expect 3 — dot (129) + pill icon (148) + letter-mark
      grep -c 'SidebarHeader className="gap-1 p-2"' src/components/layout/app-sidebar.tsx
      # expect 1 — D-12 header padding
      grep -c 'Collapse sidebar' src/components/layout/app-sidebar.tsx
      # expect 1 — the aria-label ternary (comments must not add a second)
      grep -c 'Expand sidebar' src/components/layout/app-sidebar.tsx
      # expect 1 — the aria-label ternary (comments must not add a second)
      grep -cE 'setProperty|sidebar_width' src/components/layout/app-sidebar.tsx
      # expect 0 — COLR-02 negative gate: no width writes in the sidebar (VALIDATION row 13-01-02 / COLR-02)
      test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)" && echo sweep-clean
      npx vitest run src/lib/sidebar-collapse.test.ts --bail=1
      npx vitest run src/lib/nav.test.ts --bail=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `collapsible="icon"` present on exactly the single `<Sidebar>` (grep = 1) — COLR-01 activation; the 4 nav rows carry `tooltip={getNavTooltipLabel(...)}` with the correct per-row key, the pill carries `tooltip="Give us feedback"`, the user trigger carries `tooltip={getUserDisplayName(user)}` — `tooltip=` = 6 (COLR-03, D-07/D-08; VALIDATION 13-01-01).
    - `<TooltipProvider delayDuration={200}>` wraps `<Sidebar>` (mount = usage of the vendored export, tooltip.tsx untouched) — D-09 (VALIDATION 13-01-03).
    - Header restructure matches D-03/D-12: `SidebarHeader className="gap-1 p-2"`; row 1 = collapse button (ghost, `size="icon"` 32px, `aria-label` 'Collapse sidebar'/'Expand sidebar' by state, `onClick={toggleSidebar}`, `PanelLeftClose` ⇄ `PanelLeftOpen` swap, manual `Tooltip`/`TooltipTrigger asChild`/`TooltipContent side="right"` with `getCollapseToggleLabel(state)` and NO hidden gate); row 2 = `aria-hidden="true"` letter-mark div with the exact D-11 token composite + `group-data-[collapsible=icon]:flex`, THEN the Q4 wordmark block with its class list byte-verbatim (`group-data-[collapsible=icon]:opacity-0 transition-opacity duration-200` = 1, unchanged — Pitfall 4) (COLR-01/03; VALIDATION 13-01-02/03).
    - `aria-hidden="true"` = 3 (dot + pill icon + letter-mark — the mark is decorative per RESEARCH A3); `Collapse sidebar` / `Expand sidebar` = 1 each (aria-label only; tooltip copy stays the locked short form from the helper).
    - COLR-02 negative gate: `grep -cE 'setProperty|sidebar_width' src/components/layout/app-sidebar.tsx` = 0 — zero width code in the sidebar; D-05 restore is automatic via the untouched `--sidebar-width` var thread (app-shell-layout.tsx frozen).
    - Sweep gate prints `sweep-clean` — zero indigo/amber/hex/dark: across src/components/layout/ (QLTY-04; all new classes are `--sidebar-*` tokens or utility spacing).
    - `npx vitest run src/lib/sidebar-collapse.test.ts --bail=1` green (7/7) and `npx vitest run src/lib/nav.test.ts --bail=1` green (11/11) — both regression locks hold.
    - Runtime check (with `npm run dev`, signed in): the user-trigger dropdown still opens when the `tooltip` prop is added (A2/Pitfall 6) — if it does not, apply the documented manual-Tooltip fallback at the DropdownMenuTrigger level and re-run tsc + the grep gates (the `tooltip=` count drops to 5 in that case — the acceptance for the fallback path is: dropdown opens AND the avatar tooltip still shows via the manual wrapper).
  </acceptance_criteria>
  <done>The rail is activated in app-sidebar.tsx: collapsible="icon" + TooltipProvider mount (D-09), header collapse button (D-01/02/03/06) + letter-mark (D-11) + p-2 (D-12), tooltips on all 6 interactive icons (D-07/08) with the Q4 wordmark fade verbatim; COLR-02 negative gate clean (no width code); sweep + both unit locks green.</done>
</task>

<task type="auto">
  <name>Task 3: Hide the drag-resize handle when collapsed — useSidebar() early return (COLR-02, D-04)</name>
  <files>src/components/layout/sidebar-resize-handle.tsx</files>
  <read_first>
    - src/components/layout/sidebar-resize-handle.tsx — the file being modified (91 lines). Current hook layout: three `useRef` declarations at lines 17-19, three `useCallback` handlers at lines 21-28 (handlePointerMove), 30-49 (handlePointerUp), 51-70 (handlePointerDown), then the `return (` at line 72. The frozen contract: MIN_WIDTH=200 / MAX_WIDTH=400 (lines 5-6), COOKIE_NAME='sidebar_width' (line 7), the imperative `--sidebar-width` write (line 27), and the cookie write (line 45) — all byte-identical.
    - .planning/phases/13-collapse-resize-coexistence/13-CONTEXT.md — D-04 (handle hides when collapsed; the 48px rail is fixed-width with no resize affordance; resize is an expanded-state-only interaction) and D-05 (the handle must never write mid-collapse so the cookie-threaded var stays for automatic restore).
    - .planning/phases/13-collapse-resize-coexistence/13-PATTERNS.md §sidebar-resize-handle.tsx — the D-04 insertion shape (import + `const { state } = useSidebar()` + early return) AND the hook-order warning: the early return MUST come after ALL hooks (refs AND callbacks), not after line 19 — an early return before the `useCallback` declarations would make the collapsed render skip hook calls and throw "Rendered fewer hooks" when the user toggles while mounted.
    - .planning/phases/13-collapse-resize-coexistence/13-RESEARCH.md §Pattern 4 (the early-return example + provider containment at app-shell-layout.tsx:35-42 — the handle renders inside SidebarProvider, so useSidebar is legal) and §Common Pitfalls Pitfall 5 (handle fighting the collapsed width — if it stayed mounted, a drag could write the var mid-collapse and glitch the rail on expand).
    - src/components/ui/sidebar.tsx — READ ONLY. `useSidebar` (46-53) returns `state: "expanded" | "collapsed"` among other fields; `SidebarTrigger`/`SidebarRail` (253-302) are the existing components that call useSidebar inside the provider — the idiom to copy.
  </read_first>
  <action>
    Edit `src/components/layout/sidebar-resize-handle.tsx` — the ONLY production change beyond app-sidebar.tsx this phase. Changes:
    1. Add `import { useSidebar } from '@/components/ui/sidebar';` after the existing react import (line 3) — the ONLY new import.
    2. In the component body, after the three `useRef` declarations (line 19), add `const { state } = useSidebar();` (a hook call — it must be unconditional).
    3. Add the early return AFTER ALL hooks — i.e., after the last `useCallback` closes (line 70), immediately before the `return (` (line 72): `if (state === 'collapsed') return null;` (D-04). Placement rationale (why-comment, 2-3 lines, class-string-free): the rail is fixed-width with no resize affordance, so the handle must not render — and crucially its imperative width write must never run mid-collapse, because the cookie-threaded width variable must stay at its last persisted value for the automatic restore on expand (D-05 / Pitfall 5). The early return goes after every hook (the three callbacks are hooks too) so the hook count never varies between expanded and collapsed renders.
    4. Do NOT touch: MIN_WIDTH/MAX_WIDTH (200/400), COOKIE_NAME, the drag handlers' bodies, the cookie write (line 45), the `--sidebar-width` write (line 27), or the return JSX (lines 72-90).
    5. Comment hygiene: the new why-comment must not quote class strings, `sidebar_width`, `--sidebar-width`, or swept tokens (11-02 Rule 1).
    Commit this task's diff (only the resize handle) separately from Task 2's app-sidebar.tsx diff — single-file-diff discipline per Phase 11/12 precedent.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      grep -c 'useSidebar' src/components/layout/sidebar-resize-handle.tsx
      # expect 2 — import + destructure (VALIDATION row 13-01-04)
      grep -c "state === 'collapsed'" src/components/layout/sidebar-resize-handle.tsx
      # expect 1 — the D-04 early return (VALIDATION row 13-01-04)
      grep -c 'MIN_WIDTH' src/components/layout/sidebar-resize-handle.tsx
      # expect 2 — const declaration + Math.max usage (clamp untouched)
      grep -c 'MAX_WIDTH' src/components/layout/sidebar-resize-handle.tsx
      # expect 2 — const declaration + Math.min usage (clamp untouched)
      grep -c 'sidebar_width' src/components/layout/sidebar-resize-handle.tsx
      # expect 1 — COOKIE_NAME only (cookie contract untouched)
      git diff <PHASE_BASE_SHA> HEAD -- src/components/layout/sidebar-resize-handle.tsx
      # <PHASE_BASE_SHA> = the commit immediately before the first Phase-13 source commit (resolve at the start of Task 1 via `git rev-parse HEAD`; the docs chain currently ends at 35a30e34)
      # expect: the diff shows ONLY the import line + the useSidebar destructure + the early-return line + the why-comment — no other changes
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `const { state } = useSidebar();` is unconditional (placed with the other hooks); `if (state === 'collapsed') return null;` sits AFTER all six hook calls (the three useRefs and the three useCallbacks) and immediately before the return statement — hook order is invariant across expanded/collapsed renders (no "Rendered fewer hooks" error when toggling).
    - The drag contract is byte-identical: MIN_WIDTH = 200, MAX_WIDTH = 400, `COOKIE_NAME = 'sidebar_width'`, the `style.setProperty('--sidebar-width', ...)` write (line 27), and the `document.cookie` write (line 45) unchanged — the diff on this file adds only the hide lines (VALIDATION row 13-01-04).
    - `grep -c 'useSidebar'` = 2 and `grep -c "state === 'collapsed'"` = 1 in the handle file.
  </acceptance_criteria>
  <done>Resize handle hides when collapsed via useSidebar() early return after all hooks; the 200-400 clamp, sidebar_width cookie, and imperative width write are byte-identical (COLR-02, D-04/D-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (app-sidebar.tsx) → `sidebar_state` cookie | The new collapse button triggers the SAME vendored `setOpen` write ⌘B and the topbar trigger perform (sidebar.tsx:84-85) — no new cookie code |
| collapsed rail → tooltip content | Tooltip labels are static strings (nav labels, pill copy) or derived values (pendingCount from the server-gated prop, Clerk display name already rendered as an aria-label today) rendered as Radix text nodes portaled to document.body |
| client (app-sidebar.tsx) → `--sidebar-width` var | The sidebar must perform ZERO width writes — the var stays threaded from the frozen `sidebar_width` cookie via app-shell-layout.tsx; the letter-mark/rail widths are dormant-class behavior |
| client (sidebar-resize-handle.tsx) → `--sidebar-width` / `sidebar_width` cookie | The handle's imperative write is frozen; the D-04 early return guarantees it never runs while collapsed |
| letter-mark div → DOM | Static decorative 'A' glyph, `aria-hidden` (the faded wordmark remains the accessible brand announcement) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `sidebar_state` / `sidebar_width` cookie manipulation via the new collapse button | mitigate | Layout-preference cookies only — no authz/session impact (ASVS V3 N/A: session model untouched). The button calls the vendored `toggleSidebar` (sidebar.tsx:91-93); the cookie write stays inside the vendored, unchanged `setOpen` (sidebar.tsx:84-85). Zero new cookie code anywhere (COLR-02). Task 2's negative gate `grep -cE 'setProperty|sidebar_width' src/components/layout/app-sidebar.tsx` = 0 and Task 3's fence (only the hide lines on the handle) prove no new or altered cookie/write path. |
| T-13-02 | Tampering | XSS via tooltip/label content (nav labels, pill copy, Reviews count, display name) | mitigate | Radix `TooltipContent` renders children as text nodes — no `dangerouslySetInnerHTML`, no dynamic class strings. Labels are static strings or derived from the server-gated `pendingCount` prop (already shown in the expanded badge) and Clerk identity (already rendered as `aria-label` in Phase 12). The D-08 copy helpers in src/lib/sidebar-collapse.ts return plain strings, never markup; tsc + the unit lock validate the exact strings. |
| T-13-03 | Information Disclosure | pendingCount surfaced via the Reviews rail tooltip ('Reviews (N)') | mitigate | No new data exposure: the sidebar (and its pendingCount prop) renders only inside the server-gated auth layouts (`requireStaffAccess` — ASVS V4), and the tooltip shows the same count the expanded badge already shows to the same authenticated audience. The tooltip is additionally gated by the vendored `hidden={state !== "collapsed" \|\| isMobile}` check. |
| T-13-04 | Integrity | drag-resize contract (clamp/cookie) + the imperative `--sidebar-width` write mid-collapse | mitigate | The D-04 early return (after all hooks) removes the handle from the DOM while collapsed, so the line-27 imperative write can never execute during collapse — the var stays at its cookie-threaded value for the automatic restore (D-05/Pitfall 5). Task 3's fence gate proves MIN/MAX/cookie/write byte-identical. |
| T-13-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed this phase (hard constraint; 13-RESEARCH §Package Legitimacy Audit — N/A by exemption). package.json/package-lock.json diffs must be empty; the fence gates in 13-02 assert it. Any executor-initiated install is a plan violation and requires a blocking human checkpoint. |
</threat_model>

<verification>
Phase-level checks for Plan 13-01 (Wave 1 — single Wave-1 plan, no build contention):
- `npx tsc --noEmit` exits 0 (run per task — the type gate over the whole tree, including the new helper and both edited components)
- `npx vitest run src/lib/sidebar-collapse.test.ts --bail=1` exits 0 with 7/7 (new D-08 copy lock — VALIDATION row 13-01-01)
- `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 with 11/11 (QLTY-01 regression lock — run in Task 2)
- COLR-01 grep gates: `collapsible="icon"` = 1, `PanelLeftClose`/`PanelLeftOpen` ≥ 1 each, `size="icon"` = 1 (VALIDATION rows 13-01-01/02)
- COLR-03 grep gates: `tooltip=` = 6, letter-mark composite (`bg-sidebar-primary text-sidebar-primary-foreground text-[13px] font-semibold`) = 1, `group-data-[collapsible=icon]:flex` = 1, `delayDuration` = 1, `group-data-[collapsible=icon]:opacity-0` = 1 (VALIDATION rows 13-01-01/03)
- COLR-02 negative gate: `grep -cE 'setProperty|sidebar_width' src/components/layout/app-sidebar.tsx` = 0; resize-handle diff shows ONLY the hide lines (VALIDATION row 13-01-04)
- Sweep gate: `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"` → `sweep-clean` (QLTY-04)
- `git diff <PHASE_BASE_SHA> HEAD --stat` for this plan shows exactly `src/lib/sidebar-collapse.ts`, `src/lib/sidebar-collapse.test.ts`, `src/components/layout/app-sidebar.tsx`, `src/components/layout/sidebar-resize-handle.tsx`; vendored `sidebar.tsx`, `tooltip.tsx`, `dropdown-menu.tsx`, `button.tsx`, `globals.css`, `app-shell-layout.tsx`, `(dashboard)/layout.tsx`, `package.json`, `package-lock.json` byte-identical (full fence re-check in 13-02). `<PHASE_BASE_SHA>` = the commit immediately before the first Phase-13 source commit — capture `git rev-parse HEAD` at the START of Task 1 and reuse it for all base-vs-HEAD ranges.
</verification>

<success_criteria>
Measurable completion (roadmap SC #1-#3):
- Collapse button (lucide `panel-left-close` top-right, always visible, icon swaps to `panel-left-open` when collapsed) collapses the sidebar to a 48px icon rail via the primitive's animated width switch — labels fade while icons stay (SC #1, COLR-01, D-01/02/03)
- Collapse coexists with drag-to-resize: the 200-400px clamp, `sidebar_width` cookie persistence, and the ⌘B toggle (`sidebar_state` cookie) all continue to work unchanged — the button drives the same `toggleSidebar` state, the handle hides when collapsed, and re-expand restores the last width automatically (SC #2, COLR-02, D-04/05/06)
- The collapsed rail keeps nav legibility: ≥3:1 active pill (dormant `data-active:bg-sidebar-accent`), per-item right-side ~200ms tooltips on hover+focus for all 6 interactive icons (Reviews shows 'Reviews (N)' when pendingCount > 0), and the 28px D-11 letter-mark centered below the button (SC #3, COLR-03, D-07/08/09/10/11/12)
- Zero new npm packages; zero edits to vendored `sidebar.tsx`/`tooltip.tsx`/`dropdown-menu.tsx`/`button.tsx`; zero edits to `globals.css` or `app-shell-layout.tsx`; no `.dark`/`dark:` variants; no new CSS, cookies, or state machinery (hard constraints)
</success_criteria>

<output>
Create `.planning/phases/13-collapse-resize-coexistence/13-01-SUMMARY.md` when done
</output>
