---
phase: 13-collapse-resize-coexistence
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: []
autonomous: true
requirements: [COLR-01, COLR-02, COLR-03]

must_haves:
  truths:
    - "QLTY-04 sweep gate passes — zero indigo/amber/hex/dark: matches across src/components/layout/ after the Phase 13 diff (COLR-01..03 hard constraints)"
    - "Fence gates pass — sidebar.tsx, tooltip.tsx, dropdown-menu.tsx, button.tsx, globals.css, (dashboard)/layout.tsx, app-shell-layout.tsx, package.json, package-lock.json are all byte-identical (COLR-02 frozen contract)"
    - "The full regression battery is green — nav.test.ts 11/11, user.test.ts 8/8, sidebar-collapse.test.ts 7/7, npm test ≥ baseline, npm run build, npx tsc --noEmit"
  artifacts:
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Phase 13's primary source change (rail activation) — verified swept and fence-clean"
      contains: "collapsible=\"icon\""
    - path: "src/components/layout/sidebar-resize-handle.tsx"
      provides: "The D-04 collapse hide — verified as the ONLY non-app-sidebar production change, drag contract intact"
      contains: "state === 'collapsed'"
    - path: "src/lib/sidebar-collapse.ts"
      provides: "The D-08 copy helpers — verified present and covered by the sidebar-collapse.test.ts lock"
      exports: ["getCollapseToggleLabel", "getNavTooltipLabel"]
  key_links:
    - from: "sweep gate"
      to: "src/components/layout/"
      via: "grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' over the whole directory — must print nothing (QLTY-04)"
      pattern: "sweep-clean"
    - from: "fence gate"
      to: "9 protected files (sidebar.tsx, tooltip.tsx, dropdown-menu.tsx, button.tsx, globals.css, (dashboard)/layout.tsx, app-shell-layout.tsx, package.json, package-lock.json)"
      via: "git diff must be empty on every protected file — zero vendored/global/config drift; proves COLR-02's cookie/drag/⌘B surfaces byte-identical"
      pattern: "fence-clean"
    - from: "regression battery"
      to: "src/lib/nav.test.ts (11/11) + src/lib/user.test.ts (8/8) + src/lib/sidebar-collapse.test.ts (7/7)"
      via: "vitest targeted suites — the QLTY-01 nav lock, the BRND-02 nullability lock, and the new D-08 copy lock all green"
      pattern: "7/7"
---

<objective>
Close Phase 13 with the full scope-fence verification: run the QLTY-04 sweep gate across `src/components/layout/` (zero `indigo`/`amber`/hex/`dark:` — the extended Phase 11/12 precedent), run the fence gates on all 9 protected files (vendored primitives incl. `button.tsx`, globals.css token block, dashboard/auth layout, app-shell layout, package manifests — all byte-identical), verify the whole-phase diff scope (exactly `app-sidebar.tsx` + `sidebar-resize-handle.tsx` + `src/lib/sidebar-collapse.ts` + `src/lib/sidebar-collapse.test.ts`), and run the complete regression battery (`npx tsc --noEmit`, `nav.test.ts` 11/11, `user.test.ts` 8/8, `sidebar-collapse.test.ts` 7/7, `npm test`, `npm run build`).

Purpose: This is the Wave-2 closing plan of Phase 13 (depends on 13-01 because its sweep gate scans the whole `src/components/layout/` directory, which only reaches its final state after 13-01 commits the rail activation, and the fence gates need 13-01's committed diff to diff against). It mirrors the Phase 12 12-02 structure — the plan owns NO source changes; it proves the phase landed clean: COLR-01..03 implemented without collateral damage to the frozen contract. COLR-02's security-relevant property (the `sidebar_state` cookie write path, the `sidebar_width` cookie/`--sidebar-width` thread, the ⌘B handler) is proven byte-identical by the fence gates — this is the phase's core regression proof.

Hard constraints honored: zero new npm packages (fence gate asserts `package.json`/`package-lock.json` empty diffs); zero edits to vendored `src/components/ui/sidebar.tsx`/`tooltip.tsx`/`dropdown-menu.tsx`/`button.tsx`; zero edits to `src/app/globals.css`; zero edits to `src/app/(dashboard)/layout.tsx` and `app-shell-layout.tsx`; no `.dark`/`dark:` variants; the 200-400px clamp, `sidebar_width` cookie, MIN_WIDTH=200/MAX_WIDTH=400 constants, ⌘B toggle, and `sidebar_state` cookie remain byte-identical (COLR-02).

Output: a verified phase — sweep gates = 0 across the layout directory, fence gates = clean on all 9 protected files, and the phase's full regression battery green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (§Phase 13 — success criteria 1-3; hard-constraint contract)
@.planning/REQUIREMENTS.md (COLR-01, COLR-02, COLR-03)
@.planning/phases/13-collapse-resize-coexistence/13-CONTEXT.md (§Canonical References — frozen contracts: sidebar.tsx, sidebar-resize-handle.tsx, app-shell-layout.tsx READ ONLY; §Deferred Ideas — ignore)
@.planning/phases/13-collapse-resize-coexistence/13-RESEARCH.md (§Scope fence — the phase touch-list is exactly app-sidebar.tsx + sidebar-resize-handle.tsx + the optional sidebar-collapse pair; §Security Domain — COLR-02's security-relevant property proven by fence gates; §Package Legitimacy Audit — zero installs N/A by exemption)
@.planning/phases/13-collapse-resize-coexistence/13-VALIDATION.md (per-task map row 13-02-01 — COLR-01..03 regression: sweep + fence + full suite + build; sampling rate: per-wave npm test + npm run build)
@.planning/phases/13-collapse-resize-coexistence/13-01-PLAN.md (the Wave-1 prerequisite — its acceptance criteria: sweep-clean per task, both unit locks green, negative COLR-02 gate = 0, diff scope = 4 files; capture `PHASE_BASE_SHA` there and reuse it here)
@.planning/phases/12-branding-user-zones/12-02-PLAN.md (the sweep/fence/regression precedent this plan mirrors — sweep-gate idiom, fence-clean idiom, base-vs-HEAD range discipline)
@.planning/phases/11-nav-items-restyle/11-02-PLAN.md (the comment-hygiene Rule 1 and grep-gate arithmetic precedent)
@src/components/layout/ (the swept directory: app-sidebar.tsx + sidebar-resize-handle.tsx — 13-01's diff; app-shell-layout.tsx — must stay clean)
@package.json (scripts: `test` = `vitest run`, `build` = `next build`)
</context>

<tasks>

<task type="auto">
  <name>Task 1: QLTY-04 sweep gate + fence gates + whole-phase diff scope verification (COLR-01..03 hard constraints)</name>
  <files></files>
  <read_first>
    - .planning/phases/13-collapse-resize-coexistence/13-CONTEXT.md §Canonical References (the frozen contracts: `src/components/ui/sidebar.tsx`, `src/components/layout/sidebar-resize-handle.tsx` READ ONLY except the D-04 hide, `app-shell-layout.tsx` READ ONLY) — the fence list's rationale.
    - .planning/phases/13-collapse-resize-coexistence/13-VALIDATION.md row 13-02-01 (the exact sweep + fence + regression commands: `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"`; the 9-file fence `git diff <base> HEAD -- src/components/ui/sidebar.tsx src/components/ui/tooltip.tsx src/components/ui/dropdown-menu.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx package.json package-lock.json`).
    - .planning/phases/12-branding-user-zones/12-02-PLAN.md Task 1 — the exact verification idiom: `test -z "$(...)" && echo sweep-clean` / `echo fence-clean` shell-test gates, plus `git diff --stat` scope proof with the base-vs-HEAD range.
    - .planning/phases/13-collapse-resize-coexistence/13-01-PLAN.md Task 2/3 acceptance criteria — the sweep already ran clean per task in 13-01; this task re-runs it at phase scope. Also note: the phase fence list ADDS `src/components/ui/button.tsx` vs Phase 12's 8 files (Phase 13 consumes `Button size="icon"` for the collapse button).
  </read_first>
  <action>
    Run the phase-closing scope-fence gates. NO source-code changes in this task — verification only:
    1. QLTY-04 sweep gate across the whole layout directory: `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)" && echo sweep-clean` — must print `sweep-clean`. This scans `app-sidebar.tsx` (13-01's diff — the collapse button uses `text-sidebar-foreground`, the letter-mark uses the `--sidebar-primary` token pair, tooltips portal with global tokens per D4; zero palette utilities) and `sidebar-resize-handle.tsx`. If any line matches, fix the offending source line in the file it came from (keeping comment hygiene — never introduce the words into comments, 11-02 Rule 1) and re-run until zero matches.
    2. Fence gates on all 9 protected files: `test -z "$(git diff <PHASE_BASE_SHA> HEAD -- src/components/ui/sidebar.tsx src/components/ui/tooltip.tsx src/components/ui/dropdown-menu.tsx src/components/ui/button.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx package.json package-lock.json)" && echo fence-clean` — must print `fence-clean` (quote the `(dashboard)` path — parens are shell-special). Vendored primitives, the Phase 10 token block + companion rules, the auth gate, the server shell, and both dependency manifests must be byte-identical to `<PHASE_BASE_SHA>`'s tree, not just HEAD — the base-vs-HEAD range proves NOTHING in the frozen set changed during the phase. This is COLR-02's security-relevant proof: the `sidebar_state` cookie write (sidebar.tsx:84-85), ⌘B handler (96-109), `--sidebar-width` thread (app-shell-layout.tsx:18-22/35), and drag contract all byte-identical. If any protected file has a diff, investigate whether 13-01's executor violated the fence (do NOT silently revert a legitimate change — check the diff first, then restore byte-identical state).
    3. Whole-phase diff scope: `git diff <PHASE_BASE_SHA> HEAD --stat` — must show exactly `src/components/layout/app-sidebar.tsx`, `src/components/layout/sidebar-resize-handle.tsx`, `src/lib/sidebar-collapse.ts`, `src/lib/sidebar-collapse.test.ts` for the phase. `<PHASE_BASE_SHA>` = the commit captured at the START of 13-01 Task 1 (`git rev-parse HEAD` — the plan-approval commit, immediately before the first `feat(13-01)` source commit; the docs chain ends at 35a30e34 in the pre-plan state). Because 13-01 commits per task, an UNQUALIFIED `git diff --stat` (working tree vs HEAD) is vacuous — the phase files are already committed, so always use the base-vs-HEAD range. Plus `git status --short` must show no other modified/untracked source files (the 13-01 SUMMARY and planning artifacts are docs, not source).
  </action>
  <verify>
    <automated>
      test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)" && echo sweep-clean
      test -z "$(git diff <PHASE_BASE_SHA> HEAD -- src/components/ui/sidebar.tsx src/components/ui/tooltip.tsx src/components/ui/dropdown-menu.tsx src/components/ui/button.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx package.json package-lock.json)" && echo fence-clean
      git diff <PHASE_BASE_SHA> HEAD --stat
      # <PHASE_BASE_SHA> = the commit captured at the start of 13-01 Task 1 (the plan-approval commit, immediately before the first feat(13-01) source commit)
      git status --short
    </automated>
  </verify>
  <acceptance_criteria>
    - The shell test prints `sweep-clean` — zero `indigo`/`amber`/`#[0-9a-fA-F]{3,8}` hex/`dark:` matches across `src/components/layout/` (QLTY-04 — the whole Phase 13 diff is token-only).
    - The shell test prints `fence-clean` — all 9 protected files are byte-identical across the phase range: `sidebar.tsx`, `tooltip.tsx`, `dropdown-menu.tsx`, `button.tsx`, `globals.css`, `(dashboard)/layout.tsx`, `app-shell-layout.tsx`, `package.json`, `package-lock.json` (hard constraints: zero vendored edits, zero token-block edits, zero new packages, zero config drift — COLR-02's cookie/⌘B/drag surfaces proven unchanged).
    - `git diff <PHASE_BASE_SHA> HEAD --stat` (base-vs-HEAD range; `<PHASE_BASE_SHA>` = the 13-01 Task 1 capture) shows exactly the 4 phase files: `app-sidebar.tsx`, `sidebar-resize-handle.tsx`, `sidebar-collapse.ts`, `sidebar-collapse.test.ts`; `git status --short` shows no other modified/untracked source files.
  </acceptance_criteria>
  <done>Sweep gate clean (indigo/amber/hex/dark: = 0 across src/components/layout/); fence gates clean on all 9 protected files; the whole-phase diff is exactly app-sidebar.tsx + sidebar-resize-handle.tsx + sidebar-collapse.ts + sidebar-collapse.test.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression battery — tsc + nav lock 11/11 + user lock 8/8 + copy lock 7/7 + npm test + npm run build (COLR-01..03)</name>
  <files></files>
  <read_first>
    - .planning/phases/13-collapse-resize-coexistence/13-VALIDATION.md — sampling rate (full suite + build per wave), row 13-02-01 (regression: `npm test`, `npx vitest run src/lib/nav.test.ts --bail=1`, `npm run build`), Wave 0 note (sidebar-collapse.test.ts created in-phase by 13-01 Task 1).
    - package.json — scripts (`test`: `vitest run`; `build`: `next build`). Phase 12 baseline: 24 files, 232 passed, 2 skipped.
    - .planning/phases/13-collapse-resize-coexistence/13-01-PLAN.md — Task 1 (sidebar-collapse.test.ts 7/7) and Task 2 acceptance criteria (both nav + copy locks green per task).
    - src/lib/nav.test.ts + src/lib/user.test.ts + src/lib/sidebar-collapse.test.ts — the three targeted suites this battery re-runs.
    - .planning/phases/12-branding-user-zones/12-01-SUMMARY.md §Self-Check — the gate table precedent (each gate's exact command + expected output).
  </read_first>
  <action>
    Run the phase-closing regression battery. No production code changes — verification only:
    1. `npx tsc --noEmit` — must exit 0 (type gate over the whole tree, including the new helper + the edited app-sidebar.tsx with the useSidebar consumer and manual Tooltip composition, and the resize handle's useSidebar early-return-after-all-hooks).
    2. `npx vitest run src/lib/nav.test.ts --bail=1` — must exit 0 with all 11 cases (the QLTY-01 lock: `/companies/[id]` highlight + `/companies-archive` boundary guard survive the phase).
    3. `npx vitest run src/lib/user.test.ts --bail=1` — must exit 0 with all 8 cases (the BRND-02 display-name/initials nullability lock — the avatar tooltip depends on `getUserDisplayName`).
    4. `npx vitest run src/lib/sidebar-collapse.test.ts --bail=1` — must exit 0 with all 7 cases (the new D-08 copy lock — `Reviews (3)` and the Collapse/Expand mapping).
    5. `npm test` — must exit 0 with the full suite green and NO fewer passing tests than the Phase 12 baseline (24 files, 232 passed / 2 skipped — the phase adds 7 cases for 239 passed; it may not drop a single existing test).
    6. `npm run build` — must exit 0 (SSR production build type-checks and bundles the activated rail under Next).
    If any gate fails, diagnose and fix at its root (a test failure means a behavior regression in 13-01's diff; a build failure means a type/import problem) — do not weaken gates to pass.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      npx vitest run src/lib/nav.test.ts --bail=1
      npx vitest run src/lib/user.test.ts --bail=1
      npx vitest run src/lib/sidebar-collapse.test.ts --bail=1
      npm test
      npm run build
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 with 11/11 — the nav active-key regression lock holds.
    - `npx vitest run src/lib/user.test.ts --bail=1` exits 0 with 8/8 — the BRND-02 display-name/initials nullability lock holds.
    - `npx vitest run src/lib/sidebar-collapse.test.ts --bail=1` exits 0 with 7/7 — the D-08 copy contract lock holds.
    - `npm test` exits 0 with the full suite green and ≥ Phase 12 baseline pass count (232 passed / 2 skipped; expected 239 passed after the 7 new cases).
    - `npm run build` exits 0 — the production build succeeds with the Phase 13 diff in place.
  </acceptance_criteria>
  <done>Full regression battery green: tsc clean, nav.test.ts 11/11, user.test.ts 8/8, sidebar-collapse.test.ts 7/7, npm test ≥ baseline, npm run build clean — Phase 13 verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase diff → protected files | The 9-file fence list (vendored primitives incl. button.tsx, globals.css token block, dashboard/auth layout, app-shell layout, package manifests) must be byte-identical — a drift here would silently alter the frozen contract (auth gate, token theme, dependency set, and COLR-02's cookie/drag/⌘B surfaces) |
| New Phase 13 code → sweep scope | The rail-activation diff lives inside `src/components/layout/` — the QLTY-04 sweep gate must catch any hardcoded palette/hex/`dark:` utility re-introduced by the collapse button, letter-mark, or tooltip classes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Integrity | hardcoded palette/hex/`dark:` utilities in `src/components/layout/` | mitigate | Task 1's sweep gate (`test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"`) asserts zero matches across the whole directory, including 13-01's app-sidebar.tsx diff. Comment-hygiene rule (11-02 Rule 1) prevents the swept words from re-entering via prose. Fix-at-root required if any match appears. |
| T-13-06 | Integrity | the 9 protected files (fence list) | mitigate | Task 1's fence gate asserts byte-identical `git diff <PHASE_BASE_SHA> HEAD` on `sidebar.tsx`, `tooltip.tsx`, `dropdown-menu.tsx`, `button.tsx`, `globals.css`, `(dashboard)/layout.tsx`, `app-shell-layout.tsx`, `package.json`, `package-lock.json` — proving the hard constraints (zero vendored edits, zero token-block edits, zero new packages, zero config/auth drift) held through the phase. This is COLR-02's security-relevant proof: the `sidebar_state` cookie write path (sidebar.tsx:84-85), ⌘B handler (96-109), and `sidebar_width` → `--sidebar-width` thread (app-shell-layout.tsx:18-22/35) are unchanged. Any diff is investigated before restore (never silently revert a legitimate change). |
| T-13-07 | Regression | nav active-key + user display logic + D-08 copy + full suite + build | mitigate | Task 2 runs the complete battery: `nav.test.ts` 11/11 (QLTY-01 lock — `/companies/[id]` highlight and `/companies-archive` guard), `user.test.ts` 8/8 (BRND-02 lock — nullability chains feeding the avatar tooltip), `sidebar-collapse.test.ts` 7/7 (D-08 copy lock), `npm test` (≥ 232 passed baseline), `npm run build`, `npx tsc --noEmit`. Any failure is fixed at root, not gate-weakened. |
| T-13-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed this phase (hard constraint; 13-RESEARCH §Package Legitimacy Audit — N/A by exemption). The fence gate asserts `package.json`/`package-lock.json` diffs are empty. Any executor-initiated install is a plan violation and requires a blocking human checkpoint. |
</threat_model>

<verification>
Phase-level checks after Wave 2 (13-02) completes:
- `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"` → `sweep-clean` (QLTY-04)
- `test -z "$(git diff <PHASE_BASE_SHA> HEAD -- src/components/ui/sidebar.tsx src/components/ui/tooltip.tsx src/components/ui/dropdown-menu.tsx src/components/ui/button.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx package.json package-lock.json)"` → `fence-clean` (hard constraints; COLR-02's cookie/drag/⌘B surfaces byte-identical)
- `git diff <PHASE_BASE_SHA> HEAD --stat` across the whole phase shows exactly `src/components/layout/app-sidebar.tsx`, `src/components/layout/sidebar-resize-handle.tsx`, `src/lib/sidebar-collapse.ts`, `src/lib/sidebar-collapse.test.ts` (`<PHASE_BASE_SHA>` = the commit captured at the start of 13-01 Task 1; base-vs-HEAD range because the phase files are already committed per task)
- `npx tsc --noEmit` green; `npx vitest run src/lib/nav.test.ts --bail=1` green (11/11); `npx vitest run src/lib/user.test.ts --bail=1` green (8/8); `npx vitest run src/lib/sidebar-collapse.test.ts --bail=1` green (7/7); `npm test` green (≥ 232 passed); `npm run build` green
</verification>

<success_criteria>
Measurable completion (roadmap SC #1-#3 + hard constraints):
- Zero hardcoded indigo/amber/hex/dark: utilities remain in `src/components/layout/` — the sweep gate prints `sweep-clean` (QLTY-04)
- All 9 protected files byte-identical — the fence gate prints `fence-clean`: vendored `sidebar.tsx`/`tooltip.tsx`/`dropdown-menu.tsx`/`button.tsx`, `globals.css` token block + companion rules, `(dashboard)/layout.tsx` auth gate, `app-shell-layout.tsx` server shell, `package.json`/`package-lock.json` all untouched (zero new packages); the 200-400px clamp, `sidebar_width` cookie, MIN_WIDTH=200/MAX_WIDTH=400 constants, ⌘B toggle, and `sidebar_state` cookie byte-identical (COLR-02)
- The whole-phase diff is exactly `app-sidebar.tsx` + `sidebar-resize-handle.tsx` + `sidebar-collapse.ts` + `sidebar-collapse.test.ts`
- Full regression battery green: `npx tsc --noEmit`, `nav.test.ts` 11/11, `user.test.ts` 8/8, `sidebar-collapse.test.ts` 7/7, `npm test` ≥ baseline, `npm run build`
</success_criteria>

<output>
Create `.planning/phases/13-collapse-resize-coexistence/13-02-SUMMARY.md` when done
</output>
