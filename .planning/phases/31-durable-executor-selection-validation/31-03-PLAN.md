---
phase: 31-durable-executor-selection-validation
plan: 03
type: execute
wave: 3
depends_on: [31-02]
files_modified:
  - e2e/workflow-proof-runs.spec.ts
  - .planning/phases/31-durable-executor-selection-validation/31-VERIFICATION.md
autonomous: false
requirements: [RUN-03]
must_haves:
  truths:
    - "A deployed preview proof continues to a terminal database-backed status after its initiating browser navigates away or reloads."
    - "A safe production proof repeats the same synthetic lifecycle without analysis, AI, Firecrawl, or prospect data."
    - "Phase 31 has reproducible automated and deployed evidence for claim, bounded retry, recovery/safe failure, and audited lifecycle state."
  artifacts:
    - path: e2e/workflow-proof-runs.spec.ts
      provides: "Authenticated browser proof of start, navigation/reload, and database-backed terminal status"
      contains: "workflow-proof-runs"
    - path: .planning/phases/31-durable-executor-selection-validation/31-VERIFICATION.md
      provides: "Re-runnable RUN-03 evidence and preview/production smoke results"
      contains: "RUN-03"
  key_links:
    - from: e2e/workflow-proof-runs.spec.ts
      to: /api/workflow-proof-runs
      via: "authenticated POST then navigation/reload and authenticated GET polling"
      pattern: "workflow-proof-runs"
    - from: .planning/phases/31-durable-executor-selection-validation/31-VERIFICATION.md
      to: npm run test:workflow
      via: "recorded gate command and outcome"
      pattern: "test:workflow"
---

<objective>
Prove the synthetic durable executor in a deployed Vercel preview and production-safe smoke, with reproducible evidence and no Phase 32/33 product work.

Purpose: Release the Phase 31 gate only after automated lifecycle coverage and a live staff start → navigation/reload → terminal database-status proof satisfy D-31-09 and D-31-12.
Decision coverage: D-31-03, D-31-09, D-31-10, D-31-12, D-31-13, D-31-14.
Output: Authenticated smoke automation, recorded proof evidence, and a blocking human verification of preview plus production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-durable-executor-selection-validation/31-CONTEXT.md
@.planning/phases/31-durable-executor-selection-validation/31-RESEARCH.md
@.planning/phases/31-durable-executor-selection-validation/31-PATTERNS.md
@.planning/phases/31-durable-executor-selection-validation/31-VALIDATION.md
@.planning/phases/31-durable-executor-selection-validation/31-01-SUMMARY.md
@.planning/phases/31-durable-executor-selection-validation/31-02-SUMMARY.md

<interfaces>
The authenticated test harness already uses `e2e/auth.setup.ts` with `@clerk/testing/playwright` and the Chromium project’s `e2e/.clerk/user.json` storage state. Reuse it; never inject a Clerk cookie.

`POST /api/workflow-proof-runs` returns `{ applicationRunId }` only. `GET /api/workflow-proof-runs/[id]` returns database-authoritative lifecycle/events and optional diagnostic workflow metadata. These are the only proof interfaces exercised by this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add authenticated navigation-independent proof automation and evidence record</name>
  <files>e2e/workflow-proof-runs.spec.ts, .planning/phases/31-durable-executor-selection-validation/31-VERIFICATION.md</files>
  <read_first>
    - e2e/auth.setup.ts and playwright.config.ts — reuse the established real Clerk setup project and storage-state dependency.
    - e2e/ver-02-analyze.spec.ts — mirror existing authenticated request/browser assertion conventions without calling the legacy analyze route.
    - src/app/api/workflow-proof-runs/route.ts and src/app/api/workflow-proof-runs/[id]/route.ts — use the exact synthetic start/status response shapes; do not infer status from the Workflow platform.
    - .planning/phases/31-durable-executor-selection-validation/31-VALIDATION.md — preserve the validation matrix and required preview/production proof evidence.
  </read_first>
  <action>Create an authenticated Playwright smoke spec that uses the resolved staff-gated `POST /api/workflow-proof-runs`, captures only `applicationRunId`, immediately navigates away or reloads, and polls the resolved staff-gated `GET /api/workflow-proof-runs/[id]` until the database reaches completed or safely failed terminal status with lifecycle audit and diagnostic workflow ID present (D-31-03, D-31-09, D-31-12, D-31-13, D-31-14, D-31-16). It must assert a bounded terminal wait, never inspect Workflow DevKit status as product truth, and never call Company/Persona Analyze, `runAgent`, modelFactory, Firecrawl, or provider APIs (D-31-10). Create `31-VERIFICATION.md` as an evidence ledger mapping all three roadmap success criteria and RUN-03 to Plan 31-01/02 automated commands, the preview run, and the production safe-smoke run; record actual URLs, application IDs, terminal state, event sequence, workflow diagnostic ID, reconciliation outcome when a deterministic mismatch control is exercised, command output/outcome, and any blocker without claiming a gated result passed prematurely.</action>
  <verify>
    <automated>npm run e2e -- e2e/workflow-proof-runs.spec.ts</automated>
  </verify>
  <done>The repository can exercise the exact staff-authenticated start → leave/reload → database-terminal proof path and has an evidence template that cannot confuse executor diagnostics with lifecycle truth.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify preview and production synthetic durable-executor proof</name>
  <files>.planning/phases/31-durable-executor-selection-validation/31-VERIFICATION.md</files>
  <what-built>Automated database lifecycle, staff-only start/status boundaries, a bounded Workflow DevKit proof, and an authenticated smoke spec have been completed.</what-built>
  <read_first>
    - .planning/phases/31-durable-executor-selection-validation/31-VERIFICATION.md — fill only observed deployment evidence.
    - .planning/phases/31-durable-executor-selection-validation/31-VALIDATION.md — follow both required deployed-smoke scenarios.
    - e2e/workflow-proof-runs.spec.ts — confirm the smoke remains synthetic and uses only the proof routes.
  </read_first>
  <how-to-verify>
    1. Run the final local release gate: `npm test &amp;&amp; TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow &amp;&amp; npm run build &amp;&amp; npm run e2e -- e2e/workflow-proof-runs.spec.ts`. This is the only full-suite gate; the required workflow command must fail non-zero when TEST_DATABASE_URL is missing.
    2. Build Vercel’s preview artifact with `vercel build`, then deploy that artifact with `vercel deploy --prebuilt`; set the deployed preview as the Playwright base URL using the project’s supported environment override, and run `npm run e2e -- e2e/workflow-proof-runs.spec.ts` against it.
    3. In the authenticated preview session, start one proof through `POST /api/workflow-proof-runs`, immediately navigate to another dashboard URL or reload, then poll the staff-gated `GET /api/workflow-proof-runs/{applicationRunId}`. Confirm a terminal database status, an immutable event sequence showing claim → one controlled retry → terminal result, and a diagnostic Workflow run ID. Confirm no analysis/prospect/provider/Firecrawl work occurred.
    4. After preview passes, build the production artifact with `vercel build --prod`, then deploy it with `vercel deploy --prebuilt --prod` only when the release process authorizes production deployment; perform the same navigation/reload and authenticated status check on the production domain.
    5. Record preview and production URLs, application IDs, terminal statuses, event sequences, diagnostic IDs, and commands in `31-VERIFICATION.md`. If either environment leaves a run running, lacks audit metadata, retries more than once, or exposes product state from executor metadata, mark the gate failed and do not release Phase 31.
  </how-to-verify>
  <resume-signal>Type "approved" with the preview and production evidence recorded, or describe the failing command/behavior.</resume-signal>
  <action>Execute the listed preview and production-safe verification sequence only after the automated gates are green; record observed evidence in the verification ledger and block Phase 31 release on any mismatch.</action>
  <verify>
    <automated>npm test &amp;&amp; TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow &amp;&amp; npm run build &amp;&amp; npm run e2e -- e2e/workflow-proof-runs.spec.ts</automated>
  </verify>
  <done>Preview and production each have an auditable synthetic start → navigation/reload → database-terminal proof, or the verification record identifies the blocking failure and Phase 31 remains unreleased.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated Playwright/browser → deployed API | Smoke automation invokes a staff-only synthetic endpoint in preview and production. |
| Vercel deployment → Workflow internal execution | The deployed platform—not Local World—must schedule internal durable steps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Spoofing / Elevation | deployed smoke request | mitigate | Reuse real Clerk-authenticated Playwright setup; do not cookie-inject or provide unauthenticated proof paths. |
| T-31-08 | Information Disclosure / Cost abuse | smoke proof | mitigate | The spec calls only synthetic proof endpoints; checkpoint verifies zero Company/Persona/AI/Firecrawl/provider activity before recording evidence. |
| T-31-09 | Repudiation | release evidence | mitigate | Record exact deploy URL, application ID, terminal status, event sequence, diagnostic ID, and command outcome in the verification ledger. |
| T-31-10 | Denial of Service | deployed retry/lease behavior | mitigate | Block release unless preview and production prove at most two synthetic attempts, terminal database state, and no permanently running proof. |
| T-31-SC | Tampering | deployment artifacts | mitigate | Deploy only after Plan 31-01’s schema-push and Plan 31-02’s unit/Local World gates succeed; do not bypass high-severity route/state-machine controls. |
</threat_model>

<verification>
Final release-gate command only: `npm test && TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow && npm run build && npm run e2e -- e2e/workflow-proof-runs.spec.ts`. `npm run test:workflow` must fail non-zero when TEST_DATABASE_URL is absent; a skip-capable workflow command cannot satisfy RUN-03.

Exact deployed proof commands: `vercel build && vercel deploy --prebuilt` followed by `npm run e2e -- e2e/workflow-proof-runs.spec.ts` against the preview, then (only after preview approval) `vercel build --prod && vercel deploy --prebuilt --prod` followed by the same smoke against production.
</verification>

<success_criteria>
- Preview and production each demonstrate caller-independent synthetic proof completion or safe terminal failure after navigation/reload.
- Evidence proves database lifecycle/event state is authoritative and Workflow run ID is diagnostic metadata only, including the one-attempt reconciliation success or terminal-safe-failure outcome exercised by Plan 31-02.
- No real analysis UI, legacy `agent_run` mutation, AI model, Firecrawl, provider, review, candidate, or Phase 32/33 feature is introduced.
</success_criteria>

<output>
Create `.planning/phases/31-durable-executor-selection-validation/31-03-SUMMARY.md` when done.
</output>
