---
phase: 31-durable-executor-selection-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - src/proxy.ts
  - vitest.workflow.config.ts
  - src/lib/db/schema.ts
  - src/lib/db/queries/workflowProofRuns.ts
  - src/lib/db/queries/workflowProofRuns.test.ts
autonomous: true
requirements: [RUN-03]
must_haves:
  truths:
    - "A synthetic application proof run has database-authoritative queued, running, completed, and failed lifecycle states with an immutable audit trail."
    - "A queued claim is recoverable once after lease expiry and is terminally failed rather than left running when recovery is exhausted."
    - "A persisted workflow-diagnostic mismatch is detected and auditable, then reconciled once without changing database lifecycle authority or terminally failed when that single guarded reconciliation cannot complete safely."
    - "Workflow DevKit is compiled by Next.js and its internal routes bypass Clerk proxy interception."
  artifacts:
    - path: src/lib/db/schema.ts
      provides: "Additive workflow_proof_run and workflow_proof_run_event tables plus proof lifecycle enum"
      contains: "workflowProofRun"
    - path: src/lib/db/queries/workflowProofRuns.ts
      provides: "Guarded lifecycle and diagnostic-metadata query API"
      exports: [WORKFLOW_PROOF_LEASE_MS, createWorkflowProofRun, claimOrRecoverWorkflowProofRun, reconcileWorkflowProofRun, completeWorkflowProofRun, failWorkflowProofRun]
    - path: vitest.workflow.config.ts
      provides: "Workflow Local World Vitest configuration"
      contains: "workflow()"
  key_links:
    - from: next.config.ts
      to: workflow/next
      via: "withWorkflow wrapper around preserved NextConfig"
      pattern: "withWorkflow\\(nextConfig\\)"
    - from: src/lib/db/queries/workflowProofRuns.ts
      to: workflow_proof_run_event
      via: "successful guarded lifecycle mutation appends a distinct event"
      pattern: "workflowProofRunEvent"
---

<objective>
Install and configure the selected durable executor, then establish the isolated, database-authoritative synthetic proof ledger it will operate on.

Purpose: Make Vercel Workflow DevKit executable while ensuring product lifecycle, retries, recovery, and audit are governed by guarded Drizzle writes rather than executor state (D-31-01, D-31-02, D-31-05 through D-31-07, D-31-13 through D-31-16).
Decision coverage: D-31-01, D-31-02, D-31-05, D-31-06, D-31-07, D-31-08, D-31-10, D-31-13, D-31-14, D-31-15, D-31-16.
Output: Pinned Workflow dependencies, Next/proxy integration, proof-run schema/query contract, unit coverage, and a pushed schema before any proof workflow is run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-durable-executor-selection-validation/31-CONTEXT.md
@.planning/phases/31-durable-executor-selection-validation/31-RESEARCH.md
@.planning/phases/31-durable-executor-selection-validation/31-PATTERNS.md
@.planning/phases/31-durable-executor-selection-validation/31-VALIDATION.md

<interfaces>
From src/lib/auth/requireStaffAccess.ts: `requireStaffAccess(): Promise<{ userId: string }>` is the only authoritative staff gate; query modules must not invoke it.

From src/lib/db/queries/runs.ts: query modules use named exports, `db.insert(...).values(...).returning()`, and return the first inserted row. They do not catch database errors; the route/step caller owns classification.

From next.config.ts: `nextConfig` contains a VERCEL-conditional Turbopack root that must remain byte-equivalent in behavior when wrapped with `withWorkflow()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install pinned Workflow DevKit and preserve Next/Clerk integration</name>
  <files>package.json, package-lock.json, next.config.ts, src/proxy.ts, vitest.workflow.config.ts</files>
  <read_first>
    - package.json — preserve existing npm scripts and dependency placement.
    - next.config.ts — preserve the local-only Turbopack-root branch before wrapping its exact config object.
    - src/proxy.ts — preserve Clerk middleware while exempting internal workflow requests.
    - vitest.config.ts — mirror the alias and Node-environment settings in the dedicated workflow config.
    - .planning/phases/31-durable-executor-selection-validation/31-RESEARCH.md — verify the selected 4.8.0/4.0.16 API and package legitimacy audit before installing.
  </read_first>
  <action>Install exactly `workflow@4.8.0` in dependencies and `@workflow/vitest@4.0.16` in devDependencies with npm so package-lock.json records the resolved graph; do not add a queue, worker, DurableAgent, AI provider, or Firecrawl dependency (D-31-01, D-31-02, D-31-10). Wrap the existing `nextConfig` with `withWorkflow` from `workflow/next` without replacing its VERCEL-sensitive Turbopack configuration. Amend the existing Clerk proxy matcher so `/.well-known/workflow/` bypasses it while all current protected routes remain covered. Add `vitest.workflow.config.ts` using `workflow()` from `@workflow/vitest`, the existing `@` alias, Node environment, a 60-second timeout, and an include limited to `src/workflows/**/*.integration.test.ts`. Add `test:workflow:config` to load/check that isolated config without executing the suite, and make `test:workflow` the required Phase 31 integration gate: a cross-platform Node preflight must exit non-zero with a clear `TEST_DATABASE_URL is required` message when absent, then invoke `vitest run --config vitest.workflow.config.ts` only when it is present. Do not retain a skip-capable workflow command as proof.</action>
  <verify>
    <automated>npm run test:workflow:config</automated>
  </verify>
  <done>Workflow DevKit is pinned at the locked versions, the focused workflow-config check resolves, internal workflow routes bypass the Clerk proxy, and `npm run test:workflow` fails fast without TEST_DATABASE_URL before it can run the required Local World suite.</done>
</task>

<task type="auto">
  <name>Task 2: Define and unit-test the guarded synthetic proof ledger</name>
  <files>src/lib/db/schema.ts, src/lib/db/queries/workflowProofRuns.ts, src/lib/db/queries/workflowProofRuns.test.ts</files>
  <read_first>
    - src/lib/db/schema.ts — add tables adjacent to existing additive records; retain snake_case columns, serial IDs, enum style, timestamps, and do not alter `agentRun`.
    - src/lib/db/queries/runs.ts — mirror named-export and returning-row query conventions.
    - src/lib/db/queries/runs.test.ts — mirror the hoisted Drizzle mock and fluent-chain assertions.
    - .planning/phases/31-durable-executor-selection-validation/31-RESEARCH.md#Architecture-Patterns — implement the documented queued/running/terminal lease state machine and scalar-ID boundary.
  </read_first>
  <action>Create additive `workflow_proof_run` and append-only `workflow_proof_run_event` schema objects only; never modify legacy `agent_run` (D-31-10 and Claude-discretion isolation). Define and export the server-only `WORKFLOW_PROOF_LEASE_MS = 60_000`; every successful claim/recovery writes `leaseExpiresAt = now + WORKFLOW_PROOF_LEASE_MS`. Model deterministic proof controls/snapshots, product-facing numeric ID, queued/running/completed/failed lifecycle, lease expiry, one-time recovery counter, one persisted `reconciliationAttempts` ceiling, server-persisted `workflow_run_id` diagnostics, and safe diagnostic/error fields. Implement query-layer functions that create a fresh immutable proof row; attach executor metadata without promoting it to lifecycle truth; atomically claim queued work; atomically recover exactly one expired running lease; fail exhausted/invalid recovery; complete only an owned running lease; and fail a queued/running proof without resetting terminal rows. Implement `reconcileWorkflowProofRun(applicationRunId)` as a database-only, guarded operation: reload the proof row and persisted synthetic diagnostic control, detect a diagnostic workflow-ID/state mismatch, append a `workflow_metadata_mismatch` event, and conditionally increment `reconciliationAttempts` only from zero. On the one allowed attempt, either repair only the persisted diagnostic metadata and append `workflow_metadata_reconciled` while preserving the current guarded application lifecycle, or conditionally transition the still-nonterminal application row to failed with a safe `workflow_metadata_reconciliation_failed` reason/event. A second mismatch/reconciliation request must not mutate the lifecycle and must append no duplicate reconciliation outcome; executor metadata must never set application status (D-31-05, D-31-06, D-31-07, D-31-08, D-31-13, D-31-14, D-31-15, D-31-16). Write unit tests first for every legal transition, duplicate/replayed invocation, exactly-one recovery, exhausted recovery terminal failure, dispatch failure, immutable terminal rows, diagnostic-only workflow ID, mismatch → diagnostic → one successful guarded reconciliation, mismatch → diagnostic → terminal-safe failed reconciliation, reconciliation-attempt exhaustion, and event audit identity. Seed expired-claim fixtures directly with `leaseExpiresAt = new Date(Date.now() - 1)` rather than waiting 60 seconds; use deterministic controls, never randomness.</action>
  <verify>
    <automated>npm test -- src/lib/db/queries/workflowProofRuns.test.ts</automated>
  </verify>
  <done>The synthetic ledger is additive, every lifecycle and one-attempt reconciliation mutation is conditional and auditable, terminal records cannot be reset, successful reconciliation changes only diagnostics, unsafe reconciliation fails safely, and unit tests use the 60-second lease constant with seeded-expiry fixtures without touching `agent_run`.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push the additive proof schema before workflow proof verification</name>
  <files>src/lib/db/schema.ts</files>
  <read_first>
    - drizzle.config.ts — confirm the project’s configured schema and DATABASE_URL loading behavior.
    - src/lib/db/schema.ts — confirm only the additive proof enum/tables from Task 2 are pending.
  </read_first>
  <action>Run `npm run db:push` after the proof-ledger schema is complete and before any workflow Local World, route, preview, or production proof is attempted. Inspect the generated SQL/push prompt and apply only the additive proof enum/table/event changes; stop on destructive or unrelated drift rather than accepting it. This is the required database gate for D-31-03, D-31-13, and D-31-14.</action>
  <verify>
    <automated>npm run db:push</automated>
  </verify>
  <done>The configured database contains the additive proof ledger before any executor can claim a proof run; no legacy `agent_run` migration is applied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → database | Durable step replays and retries can attempt repeated lifecycle mutations. |
| Next/proxy → Workflow internal routes | Internal executor callbacks must reach generated Workflow routes without becoming anonymous application routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Denial of Service | `src/proxy.ts` workflow internal path | mitigate | Exclude only `/.well-known/workflow/` from Clerk proxy and prove Next build/workflow config resolves before proof execution. |
| T-31-02 | Tampering | `workflowProofRuns` transitions and reconciliation | mitigate | Use expected-status/lease/reconciliation-attempt predicates, preserve database authority during diagnostic repair, terminal-fail unsafe reconciliation, and append unique events; unit tests run before downstream verification. |
| T-31-03 | Repudiation | proof lifecycle audit | mitigate | Persist event action, attempt/recovery identity, timestamp, and safe reason on every guarded outcome. |
| T-31-SC | Tampering | npm installs | mitigate | Use research-audited `workflow@4.8.0` and `@workflow/vitest@4.0.16` only; both are Approved with no [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
Run `npm test -- src/lib/db/queries/workflowProofRuns.test.ts`, then the blocking `npm run db:push`. Do not run executor proof commands until the push succeeds.
</verification>

<success_criteria>
- Workflow DevKit 4.8.0 and @workflow/vitest 4.0.16 are the only new executor dependencies.
- The database—not Workflow DevKit metadata—owns proof-run state and immutable audit history.
- Claim/recovery/retry/reconciliation controls are deterministic, bounded, and cannot overwrite a terminal proof run; the one reconciliation attempt either repairs diagnostics only or records terminal safe failure.
</success_criteria>

<output>
Create `.planning/phases/31-durable-executor-selection-validation/31-01-SUMMARY.md` when done.
</output>
