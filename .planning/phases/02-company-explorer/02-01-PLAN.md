---
phase: 02-company-explorer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/queries/companies.ts
  - src/lib/db/queries/companyPersonaRoles.ts
  - src/lib/validation/seed.ts
  - src/scripts/seed.ts
  - data/seed/companies.csv
  - data/seed/personas.csv
  - data/seed/signals.csv
  - data/seed/company_persona_roles.csv
autonomous: true
requirements: [COMP-01, COMP-02, COMP-04]

must_haves:
  truths:
    - "The company table has revenue band, ownership type, employee count band, HQ location, and tech stack columns live in Neon Postgres"
    - "listCompanies() supports optional search/industry/signalType/revenueBand/ownershipType filters composed with AND, built with Drizzle's parameterized operators (no raw SQL string interpolation)"
    - "A getCompanyById lookup and a listPersonasForCompany join query exist for the detail pane plans to consume"
    - "The seed dataset contains 9 clearly-fake companies with varied industries, firmographics, signals (all 4 types, 3 strengths), and at least one linked persona each"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "revenueBandEnum, ownershipTypeEnum pgEnums; company table extended with employeeCountBand, hqLocation, revenueBand, ownershipType, techStack columns"
      contains: "revenueBandEnum"
    - path: "src/lib/db/queries/companies.ts"
      provides: "listCompanies(filters?), getCompanyById(id), listDistinctIndustries()"
      exports: ["listCompanies", "getCompanyById", "listDistinctIndustries", "CompanyFilters"]
    - path: "src/lib/db/queries/companyPersonaRoles.ts"
      provides: "listPersonasForCompany(companyId) join query"
      exports: ["listPersonasForCompany"]
    - path: "data/seed/companies.csv"
      provides: "9 seeded companies with full firmographics"
      min_lines: 10
  key_links:
    - from: "src/lib/db/queries/companies.ts"
      to: "src/lib/db/schema.ts company table"
      via: "Drizzle select/where against revenueBandEnum/ownershipTypeEnum columns"
      pattern: "and\\("
    - from: "src/scripts/seed.ts"
      to: "src/lib/db/queries/companies.ts's company insert"
      via: "db.insert(company).values({...}) with new firmographic fields"
      pattern: "employeeCountBand"
---

<objective>
Extend the `company` schema with Phase 2's firmographic fields (D-01 through D-04), push the migration to Neon, extend the Drizzle query layer with the filter/lookup functions every later plan in this phase depends on, and expand the placeholder seed dataset (D-12) so the explorer has real, varied data to search/filter/display.

Purpose: This is the data foundation the Company Explorer UI (Plans 02-04) is built on — no list/detail/filter UI can render meaningful firmographics, signals, or personas without these columns, query functions, and seed rows existing first.
Output: Migrated `company` table, extended `src/lib/db/queries/companies.ts` and `companyPersonaRoles.ts`, a 9-company seed dataset loaded into Neon.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-company-explorer/02-CONTEXT.md
@.planning/phases/02-company-explorer/02-RESEARCH.md
@.planning/phases/02-company-explorer/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend company schema and Drizzle query layer</name>
  <files>src/lib/db/schema.ts, src/lib/db/queries/companies.ts, src/lib/db/queries/companyPersonaRoles.ts</files>
  <read_first>
    src/lib/db/schema.ts (current signalTypeEnum/signalStrengthEnum pattern and the company table's Phase-2 placeholder comment)
    src/lib/db/queries/companies.ts (current listCompanies()/getCompanyByName() — both are called with zero args elsewhere, must keep compiling)
    src/lib/db/queries/signals.ts (listSignalsForCompany(companyId) — the exact "list child rows for one company id" shape to mirror in companyPersonaRoles.ts)
    src/lib/db/queries/companyPersonaRoles.ts (current insertCompanyPersonaRole — no eq import yet)
    .planning/phases/02-company-explorer/02-RESEARCH.md sections "Proposed Enum Values" and "Architecture Patterns > Pattern 3"
  </read_first>
  <action>
    In src/lib/db/schema.ts: add two new pgEnum exports alongside signalTypeEnum/signalStrengthEnum, using the same "cite the decision ID, explain migration cost" comment style: `revenueBandEnum = pgEnum('revenue_band', ['under_50m', '50m_250m', '250m_1b', '1b_5b', '5b_plus'])` (D-02) and `ownershipTypeEnum = pgEnum('ownership_type', ['private', 'public', 'pe_backed', 'family_owned', 'subsidiary'])` (D-02). Replace the company table's `// ... remaining firmographic fields land in Phase 2 per COMP-01` comment line with five new nullable columns: `employeeCountBand: text('employee_count_band')` (D-01, banded text e.g. "51-200"), `hqLocation: text('hq_location')` (D-03, freeform e.g. "Munich, Germany"), `revenueBand: revenueBandEnum('revenue_band')` (D-02), `ownershipType: ownershipTypeEnum('ownership_type')` (D-02), `techStack: text('tech_stack').array()` (D-04, no per-tool metadata). Do not add `.notNull()` to any of these — additive nullable columns keep the push non-destructive per the schema-push requirement.
    In src/lib/db/queries/companies.ts: add an exported `CompanyFilters` interface with optional fields `search`, `industry`, `signalType`, `revenueBand`, `ownershipType` (all `string`). Change `listCompanies()` to `listCompanies(filters: CompanyFilters = {})`, keeping the default so the two existing zero-arg call sites (src/app/page.tsx, src/app/actions.ts) keep compiling unchanged. Build the WHERE clause with Drizzle's `and(cond ?? undefined, ...)` pattern per RESEARCH.md Pattern 3: `ilike(company.name, \`%${filters.search}%\`)` for search, `eq(company.industry, filters.industry)` for industry, `eq(company.revenueBand, filters.revenueBand as typeof revenueBandEnum.enumValues[number])` for revenueBand, `eq(company.ownershipType, ...)` for ownershipType, and an `exists(db.select({ one: sql\`1\` }).from(signal).where(and(eq(signal.companyId, company.id), eq(signal.signalType, ...))))` subquery for signalType — use `exists`, never a plain `leftJoin`, to avoid duplicate company rows when a company has multiple signals of the requested type (Pitfall 5). Add `getCompanyById(id: number)` returning `rows[0]` (undefined if not found — mirror getCompanyByName's convention, never throw). Add `listDistinctIndustries()` using `db.selectDistinct({ industry: company.industry }).from(company)`.
    In src/lib/db/queries/companyPersonaRoles.ts: add `import { eq } from 'drizzle-orm';` (not yet imported in this file) and add `persona` to the existing schema import. Add `listPersonasForCompany(companyId: number)` returning `db.select({ persona, role: companyPersonaRole }).from(companyPersonaRole).innerJoin(persona, eq(companyPersonaRole.personaId, persona.id)).where(eq(companyPersonaRole.companyId, companyId))`.
  </action>
  <acceptance_criteria>
    - `grep -c "revenueBandEnum\|ownershipTypeEnum" src/lib/db/schema.ts` returns 4 or more (2 declarations + at least 2 usages on the company table)
    - `grep -c "techStack" src/lib/db/schema.ts` is at least 1
    - `grep -c "export function listCompanies\|export async function listCompanies" src/lib/db/queries/companies.ts` is 1, and the function signature includes `filters` with a default `= {}`
    - `grep -c "getCompanyById\|listDistinctIndustries" src/lib/db/queries/companies.ts` is 2 (both new exports present)
    - `grep -c "listPersonasForCompany" src/lib/db/queries/companyPersonaRoles.ts` is 1
    - `npx tsc --noEmit` exits 0 (no type errors from the new columns/enums/query signatures)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>src/lib/db/schema.ts has the two new pgEnums and five new company columns; companies.ts exports CompanyFilters/listCompanies(filters)/getCompanyById/listDistinctIndustries; companyPersonaRoles.ts exports listPersonasForCompany; project type-checks clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: [BLOCKING] Push schema to Neon Postgres</name>
  <files>none (database migration only, no source files modified)</files>
  <read_first>
    drizzle.config.ts (confirms `dialect: 'postgresql'`, out: './drizzle', dbCredentials from DATABASE_URL, .env.local loaded via dotenv config)
  </read_first>
  <action>
    Run `npx drizzle-kit push` from the repo root. This is a purely additive change (5 new nullable columns, 2 new pgEnum types) — no existing column is dropped, renamed, or made non-nullable, so `drizzle-kit push` should apply without an interactive destructive-change prompt. If a prompt does appear, confirm it is genuinely non-destructive (adding a column/enum, not dropping data) before proceeding with `npx drizzle-kit push --force`; do not use `--force` to push through a prompt you have not read. Do not proceed to Task 3 until this push completes successfully — Task 3's seed script inserts into the new columns and will fail if the schema hasn't been pushed.
  </action>
  <acceptance_criteria>
    - `npx drizzle-kit push` exits 0
    - Re-running `npx drizzle-kit push` immediately after reports no pending schema changes (idempotent — confirms Neon now matches src/lib/db/schema.ts)
  </acceptance_criteria>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <done>Neon's `company` table has the 5 new columns and the `revenue_band`/`ownership_type` Postgres enum types exist; a second `drizzle-kit push` run is a no-op.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Expand seed dataset to 9 companies and run seed pipeline</name>
  <files>src/lib/validation/seed.ts, src/scripts/seed.ts, data/seed/companies.csv, data/seed/personas.csv, data/seed/signals.csv, data/seed/company_persona_roles.csv</files>
  <read_first>
    src/lib/validation/seed.ts (safeCsvString/optionalSafeCsvString/optionalDateString patterns — the blank-to-undefined transform to mirror for the two new enum columns)
    src/scripts/seed.ts (the company-insert loop at the `.values({ name: row.name, industry: row.industry })` call, and the personaNameToId Map pattern two blocks below it)
    data/seed/companies.csv, data/seed/personas.csv, data/seed/signals.csv, data/seed/company_persona_roles.csv (current 2-row placeholder content and header conventions)
  </read_first>
  <action>
    In src/lib/validation/seed.ts: import `revenueBandEnum, ownershipTypeEnum` alongside the existing `signalTypeEnum, signalStrengthEnum` import from '../db/schema'. Extend `companyRowSchema` with five new fields: `employee_count_band: optionalSafeCsvString`, `hq_location: optionalSafeCsvString`, `revenue_band` and `ownership_type` each following the exact blank-to-undefined-then-validate shape already used by `optionalDateString` (lines 37-43) but piping into `z.enum(revenueBandEnum.enumValues)` / `z.enum(ownershipTypeEnum.enumValues)` instead of the date regex, and `tech_stack: optionalSafeCsvString` (raw pipe-delimited string — validated as safe text here, split into an array in seed.ts, not in the schema).
    In src/scripts/seed.ts: extend the company insert's `.values({...})` object to include `employeeCountBand: row.employee_count_band, hqLocation: row.hq_location, revenueBand: row.revenue_band, ownershipType: row.ownership_type, techStack: row.tech_stack ? row.tech_stack.split('|').map((s) => s.trim()) : undefined`. Do not change the fail-fast validate-everything-before-inserting-anything structure.
    Replace all 4 CSV files' contents with the following seed dataset (D-12: ~8-10 clearly-fake companies exercising every filter dimension). companies.csv header: `name,industry,employee_count_band,hq_location,revenue_band,ownership_type,tech_stack` with rows: Acme Test Co,Manufacturing,1001-5000,"Detroit, USA",250m_1b,private,SAP ERP|Excel|Concur / Beta Sample Inc,Financial Services,201-1000,"New York, USA",1b_5b,public,Oracle Financials|Workday|Tableau / Gamma Placeholder AG,Manufacturing,5001-20000,"Essen, Germany",1b_5b,subsidiary,SAP S/4HANA|Ariba|Power BI / Delta Sample Retail Group,Retail,1001-5000,"Chicago, USA",250m_1b,private,NetSuite|Salesforce|Shopify Plus / Epsilon Test Health Systems,Healthcare,5001-20000,"Boston, USA",1b_5b,public,Epic|Workday|ServiceNow / Zeta Sample Logistics,Logistics,201-1000,"Rotterdam, Netherlands",50m_250m,pe_backed,SAP TM|Excel / Eta Placeholder Energy Corp,Energy,20000+,"Houston, USA",5b_plus,public,SAP ERP|Palantir Foundry|Power BI / Theta Sample Brands,Consumer Goods,1001-5000,"London, UK",250m_1b,family_owned,Oracle NetSuite|HubSpot / Iota Test Solutions,SaaS,51-200,"Austin, USA",under_50m,private,QuickBooks|Airtable|HubSpot.
    personas.csv header: `name,title` with rows: Jordan Sample,VP of Finance / Taylor Placeholder,Chief Financial Officer / Morgan Testcase,Head of Global Business Services / Casey Fakename,VP of Operations / Riley Sampledata,Chief Financial Officer / Avery Placeholderson,Director of Finance Transformation / Drew Testfield,Chief Operating Officer / Reese Sampleton,VP of Shared Services / Quinn Fakeworth,Chief Financial Officer / Sydney Placeholdt,Head of GBS Transformation.
    signals.csv header: `company_name,signal_type,strength,source,detected_at,note` with rows (all note="Placeholder signal for pipeline testing", source="manual"): Acme Test Co,cost_pressure,high,manual,2026-01-15 / Beta Sample Inc,new_cfo_or_gbs_head,medium,manual,2026-02-01 / Gamma Placeholder AG,immature_gbs_org,high,manual,2026-02-10 / Gamma Placeholder AG,transformation_announcement,medium,manual,2026-03-01 / Delta Sample Retail Group,cost_pressure,medium,manual,2026-01-20 / Epsilon Test Health Systems,transformation_announcement,high,manual,2026-02-15 / Zeta Sample Logistics,immature_gbs_org,low,manual,2026-01-05 / Eta Placeholder Energy Corp,new_cfo_or_gbs_head,high,manual,2026-03-10 / Eta Placeholder Energy Corp,cost_pressure,low,manual,2026-01-25 / Theta Sample Brands,immature_gbs_org,medium,manual,2026-02-20 / Iota Test Solutions,transformation_announcement,low,manual,2026-03-05 / Iota Test Solutions,new_cfo_or_gbs_head,medium,manual,2026-02-28.
    company_persona_roles.csv header: `company_name,persona_name,title,is_current,start_date,end_date` with rows: Acme Test Co,Jordan Sample,VP of Finance,true,2024-01-01, / Beta Sample Inc,Taylor Placeholder,Chief Financial Officer,true,2023-06-01, / Gamma Placeholder AG,Morgan Testcase,Head of Global Business Services,true,2022-09-01, / Delta Sample Retail Group,Casey Fakename,VP of Operations,true,2023-03-15, / Epsilon Test Health Systems,Riley Sampledata,Chief Financial Officer,true,2021-11-01, / Zeta Sample Logistics,Avery Placeholderson,Director of Finance Transformation,true,2024-05-01, / Eta Placeholder Energy Corp,Drew Testfield,Chief Operating Officer,true,2020-01-01, / Theta Sample Brands,Reese Sampleton,VP of Shared Services,true,2023-08-01, / Iota Test Solutions,Quinn Fakeworth,Chief Financial Officer,true,2024-02-01, / Acme Test Co,Sydney Placeholdt,VP of Finance (Former),false,2019-01-01,2023-12-31 / Gamma Placeholder AG,Sydney Placeholdt,Head of GBS Transformation,true,2024-01-01,.
    After all 4 CSVs and both source files are updated, run `npm run seed` once to load the expanded dataset into the Neon instance pushed in Task 2. This is the first seed run against the new schema — if it fails, the CSV data or the schema push (Task 2) has a mismatch that must be fixed before this task is done.
  </action>
  <acceptance_criteria>
    - `wc -l data/seed/companies.csv` reports 10 (1 header + 9 data rows)
    - `wc -l data/seed/personas.csv` reports 11 (1 header + 10 data rows)
    - `wc -l data/seed/company_persona_roles.csv` reports 12 (1 header + 11 data rows)
    - `grep -c "revenueBandEnum\|ownershipTypeEnum" src/lib/validation/seed.ts` is at least 2
    - `npm run seed` exits 0 and prints "Inserted: 9 companies, 10 personas, 12 signals, 11 company_persona_roles"
  </acceptance_criteria>
  <verify>
    <automated>npm run seed</automated>
  </verify>
  <done>All 4 seed CSVs contain 9 companies / 10 personas / 12 signals / 11 role rows covering every industry, all 5 revenue bands, all 5 ownership types, and all 4 signal types across 3 strength levels; `npm run seed` successfully loads them into Neon against the Task 2 schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CSV seed file → database | Seed CSVs are repo-committed but represent the same class of "external, untrusted-until-validated" input as any file-based data load — validated through zod before any insert |
| listCompanies(filters) caller → Drizzle query | Plans 03/04 will pass user-controlled `searchParams` values into `filters` — this plan establishes the parameterized-query contract those callers must use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-2-01 | Tampering | `listCompanies(filters)` WHERE composition | mitigate | Use only Drizzle's parameterized operators (`eq`, `ilike`, `exists`) as built in Task 1 — never `sql.raw()` with an interpolated filter value. This is the query-layer contract that Plan 04's UI-sourced filters must rely on. |
| T-2-02 | Tampering | New CSV columns (`hq_location`, `employee_count_band`, `tech_stack`) | mitigate | Extend the existing `safeCsvString`/`optionalSafeCsvString` formula-injection guards (`src/lib/validation/seed.ts`) to every new column — no new column bypasses the guard. |
| T-2-SC | Tampering | npm installs (none this plan — no new package.json dependency added) | accept | No new npm packages installed in this plan; Package Legitimacy Gate is not triggered here (applies in Plans 02/04 where `shadcn`/`nuqs` are installed). |
</threat_model>

<verification>
- `npx tsc --noEmit` passes after Task 1
- `npx drizzle-kit push` succeeds and is idempotent on a second run (Task 2)
- `npm run seed` loads 9 companies / 10 personas / 12 signals / 11 roles without error (Task 3)
- Existing call sites `listCompanies()` (src/app/page.tsx, src/app/actions.ts) still compile unchanged
</verification>

<success_criteria>
Neon's `company` table has the 5 new firmographic columns and 2 new enum types live; `src/lib/db/queries/companies.ts` and `companyPersonaRoles.ts` export the filter/lookup functions Plans 02-04 depend on; the seed dataset has grown from 2 to 9 companies with full firmographic/signal/persona variety.
</success_criteria>

<output>
Create `.planning/phases/02-company-explorer/02-01-SUMMARY.md` when done
</output>
