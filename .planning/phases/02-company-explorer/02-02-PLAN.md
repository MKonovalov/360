---
phase: 02-company-explorer
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components.json
  - src/lib/utils.ts
  - src/app/globals.css
  - src/components/ui/sidebar.tsx
  - src/components/ui/table.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/input.tsx
  - src/components/ui/select.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/layout/app-sidebar.tsx
  - src/components/layout/sidebar-resize-handle.tsx
  - src/app/companies/layout.tsx
  - src/app/companies/page.tsx
  - src/app/companies/loading.tsx
  - src/components/companies/company-list.tsx
  - src/components/companies/signal-badge.tsx
autonomous: true
requirements: [COMP-01, EXPL-03, EXPL-05]

must_haves:
  truths:
    - "A signed-in staff member navigating to /companies sees a collapsible left nav with Companies (active) and Key Personas (visible, disabled) sections"
    - "A signed-in staff member can drag a resize handle on the sidebar's right edge to change its width between 200-400px, and the chosen width persists across a full page reload via a cookie (EXPL-03 'resizable' requirement)"
    - "The Companies list shows all 9 seeded companies with industry, employee count band, HQ location, revenue band, and ownership type"
    - "Each list row shows an amber 'attention' badge per buying signal the company has, without opening the record"
    - "An unauthenticated visitor hitting /companies is redirected to /sign-in, never sees company data"
  artifacts:
    - path: "src/app/companies/layout.tsx"
      provides: "requireStaffAccess() gate + shadcn SidebarProvider/SidebarInset shell for the whole /companies subtree, reads sidebar_width cookie server-side to set the initial --sidebar-width"
      contains: "requireStaffAccess"
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Two-section nav: Companies (active link), Key Personas (disabled, D-11)"
    - path: "src/components/layout/sidebar-resize-handle.tsx"
      provides: "Drag handle updating the --sidebar-width CSS custom property live during drag and persisting the final width via a sidebar_width cookie (EXPL-03 resizable requirement)"
    - path: "src/app/companies/page.tsx"
      provides: "/companies route rendering the company list via listCompanies()"
    - path: "src/components/companies/company-list.tsx"
      provides: "shadcn Table rendering firmographics + SignalBadge per row"
    - path: "src/components/companies/signal-badge.tsx"
      provides: "Single neutral amber badge style for all 4 signal types (UI-SPEC lock — no per-type color)"
  key_links:
    - from: "src/app/companies/layout.tsx"
      to: "src/lib/auth/requireStaffAccess.ts"
      via: "await requireStaffAccess() as the layout's first line"
      pattern: "requireStaffAccess\\(\\)"
    - from: "src/components/layout/sidebar-resize-handle.tsx"
      to: "src/app/companies/layout.tsx"
      via: "layout reads the sidebar_width cookie server-side (next/headers cookies()) to set SidebarProvider's initial --sidebar-width style; the client handle updates the same cookie + CSS var live on drag"
      pattern: "sidebar_width"
    - from: "src/components/companies/company-list.tsx"
      to: "src/lib/db/queries/companies.ts listCompanies()"
      via: "async Server Component data fetch"
      pattern: "listCompanies\\("
    - from: "src/components/companies/company-list.tsx"
      to: "src/components/companies/signal-badge.tsx"
      via: "per-row signal badge rendering from listSignalsForCompany or a joined signals list"
      pattern: "SignalBadge"
---

<objective>
Install shadcn/ui (Radix primitives, per UI-SPEC's locked `new-york` style), build the collapsible and resizable two-section sidebar shell (EXPL-03), and render a real, gated `/companies` list page showing every seeded company's firmographics (COMP-01) with signal badges on each row (EXPL-05).

Purpose: This is the first user-observable slice of the Company Explorer — after this plan, a signed-in staff member can navigate to a dedicated Companies page and scan real firmographic + signal data, rather than only the Phase 1 landing page's bare company count.
Output: `components.json` + shadcn primitives, `AppSidebar`, a drag-to-resize `SidebarResizeHandle`, `/companies` route with a real list, `SignalBadge` component reused by Plan 03's detail pane.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-company-explorer/02-CONTEXT.md
@.planning/phases/02-company-explorer/02-RESEARCH.md
@.planning/phases/02-company-explorer/02-UI-SPEC.md
@.planning/phases/02-company-explorer/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install shadcn/ui and build the collapsible sidebar shell</name>
  <files>components.json, src/lib/utils.ts, src/app/globals.css, src/components/ui/sidebar.tsx, src/components/ui/table.tsx, src/components/ui/badge.tsx, src/components/ui/input.tsx, src/components/ui/select.tsx, src/components/ui/separator.tsx, src/components/ui/skeleton.tsx, src/components/ui/scroll-area.tsx, src/components/layout/app-sidebar.tsx, src/app/companies/layout.tsx</files>
  <read_first>
    src/app/layout.tsx (root layout's ClerkProvider-wrapping shape — the pattern to follow for provider composition)
    src/app/globals.css (current bare background/foreground @theme block — shadcn init will extend this)
    src/lib/auth/requireStaffAccess.ts (the ONLY function allowed to make a gating decision — must be the first line of this new layout)
    .planning/phases/02-company-explorer/02-UI-SPEC.md sections "Design System" (Radix, new-york, slate/indigo) and "Color"
    .planning/phases/02-company-explorer/02-RESEARCH.md "Pattern 4: shadcn Sidebar Shell" and "Pitfall 1: shadcn CLI now defaults to Base UI, not Radix"
  </read_first>
  <action>
    Run `npx shadcn@latest init -b radix` (the `-b radix` flag is mandatory — the shadcn CLI defaults to Base UI as of July 2026, which would contradict UI-SPEC's locked Radix decision). When prompted interactively, select style "New York" and base color "Slate" to match UI-SPEC's dense-dashboard/slate+indigo lock. Immediately after init, verify `components.json` was created and that no generated file imports from `@base-ui-components/react` (grep check below) before proceeding. Then run `npx shadcn@latest add sidebar table badge input select separator skeleton scroll-area` to generate all shadcn primitives this phase needs into `src/components/ui/`.
    Create `src/components/layout/app-sidebar.tsx`: a named-export `AppSidebar` function composing `Sidebar`/`SidebarContent`/`SidebarGroup`/`SidebarMenu`/`SidebarMenuItem`/`SidebarMenuButton` from `@/components/ui/sidebar`. Two menu items: "Companies" as an `asChild` link to `/companies` (accent/active styling per UI-SPEC's indigo-600 selected-nav-item rule), and "Key Personas" rendered with the button's `disabled` prop set, no `href` — visible but non-interactive per D-11 (Phase 3 hasn't shipped its route yet). Add a one-line comment above the disabled item citing D-11.
    Create `src/app/companies/layout.tsx`: an `async` default-export `CompaniesLayout({ children })` that calls `await requireStaffAccess()` as its first line (Pitfall 4 — centralizing the gate here covers every route under `/companies`, including the `[id]` route Plan 03 adds later), then wraps `children` in `SidebarProvider` → `AppSidebar` + `SidebarInset` (with a `SidebarTrigger` for the collapse toggle) per RESEARCH.md Pattern 4's exact composition. Task 2 in this plan will edit this same file again to wire the resize handle — leave `SidebarProvider`'s `style` prop unset for now.
  </action>
  <acceptance_criteria>
    - `test -f components.json` succeeds
    - `grep -rL "@base-ui-components/react" src/components/ui/*.tsx | wc -l` equals the total count of generated ui files (i.e., zero files import from Base UI — confirms `-b radix` took effect)
    - `grep -c "requireStaffAccess" src/app/companies/layout.tsx` is 1, and it appears before any JSX return (auth-first ordering, Pitfall 4)
    - `grep -c "disabled" src/components/layout/app-sidebar.tsx` is at least 1 (Key Personas item)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>shadcn is initialized with Radix primitives and New York style; AppSidebar renders Companies (linked) + Key Personas (disabled); companies/layout.tsx gates the entire /companies subtree via requireStaffAccess() before rendering the Sidebar shell.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add drag-to-resize handle to the sidebar, persisted via cookie</name>
  <files>src/components/layout/sidebar-resize-handle.tsx, src/app/companies/layout.tsx</files>
  <read_first>
    src/components/ui/sidebar.tsx (generated by Task 1 — confirm the `--sidebar-width` CSS custom property name that `SidebarProvider` consumes via its `style` prop, and the existing `document.cookie`-based pattern shadcn's own generated code uses to persist collapse state)
    .planning/phases/02-company-explorer/02-RESEARCH.md "Pattern 4: shadcn Sidebar Shell" (the SidebarProvider composition this task extends)
    .planning/REQUIREMENTS.md EXPL-03 ("collapsible/resizable left nav") — Task 1 delivered "collapsible"; this task delivers "resizable"
  </read_first>
  <action>
    Create `src/components/layout/sidebar-resize-handle.tsx` as a `'use client'` named-export `SidebarResizeHandle` component: a thin (4px-wide) absolutely-positioned vertical strip at the sidebar's right edge, `cursor-col-resize`. On `pointerdown` it captures the pointer and attaches `pointermove`/`pointerup` listeners that track the horizontal drag delta from the starting width, clamp the resulting width between 200px and 400px, and write the live value directly to the `--sidebar-width` CSS custom property on the nearest `SidebarProvider` wrapper element via `style.setProperty` (imperative DOM write during drag, not React state per pixel, to avoid re-render jank). On `pointerup` it persists the final clamped width into a `sidebar_width` cookie (`document.cookie = 'sidebar_width=' + width + '; path=/; max-age=31536000'`) — alongside, not replacing, shadcn's own collapse-state cookie.
    Edit `src/app/companies/layout.tsx`: read the `sidebar_width` cookie server-side via `cookies()` from `next/headers` (default to `256` — shadcn's stock `--sidebar-width` default — if the cookie is absent or fails to parse as a number in the 200-400 range), pass it to `SidebarProvider` via `style={{ '--sidebar-width': `${width}px` } as React.CSSProperties}`, and render `<SidebarResizeHandle />` immediately after `<AppSidebar />` inside the provider so the handle sits at the sidebar's edge.
  </action>
  <acceptance_criteria>
    - `test -f src/components/layout/sidebar-resize-handle.tsx` succeeds
    - `grep -c "'use client'" src/components/layout/sidebar-resize-handle.tsx` is 1
    - `grep -c "pointermove\|pointerdown" src/components/layout/sidebar-resize-handle.tsx` is at least 1
    - `grep -c "sidebar_width" src/components/layout/sidebar-resize-handle.tsx` is at least 1
    - `grep -c "sidebar_width" src/app/companies/layout.tsx` is at least 1
    - `grep -c "SidebarResizeHandle" src/app/companies/layout.tsx` is 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <human-check>Sign in, visit /companies. Grab the thin drag handle at the sidebar's right edge and drag it wider/narrower — confirm the sidebar width follows the cursor smoothly between roughly 200-400px. Release, then hard-reload the page (Cmd/Ctrl+R) — confirm the sidebar keeps the dragged width instead of snapping back to the default.</human-check>
  </verify>
  <done>A visible drag handle on the sidebar's right edge lets a staff member resize the sidebar between 200-400px by dragging; the chosen width persists via a sidebar_width cookie and survives a full page reload — EXPL-03's "resizable" requirement is met alongside Task 1's "collapsible" delivery.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Render the gated Companies list with firmographics and signal badges</name>
  <files>src/app/companies/page.tsx, src/app/companies/loading.tsx, src/components/companies/company-list.tsx, src/components/companies/signal-badge.tsx</files>
  <read_first>
    src/app/page.tsx (the async-page "fetch then render" shape and the existing `if (userId)` pattern — note this page is the one documented exception that does NOT call requireStaffAccess(); companies/page.tsx is not an exception)
    src/lib/db/queries/companies.ts (listCompanies() signature from Plan 02-01 — CompanyFilters type, call with no filters for this task)
    src/lib/db/queries/signals.ts (listSignalsForCompany(companyId) — existing function to fetch each row's signals)
    .planning/phases/02-company-explorer/02-UI-SPEC.md sections "Typography", "Spacing Scale", "Color" (signal badges: amber-100/amber-800, one style for all 4 signal types, never color-coded per type), "Copywriting Contract" (true-empty state copy)
  </read_first>
  <action>
    Create `src/components/companies/signal-badge.tsx`: named export `SignalBadge({ signalType }: { signalType: string })` rendering a shadcn `Badge` with amber-100 background / amber-800 text classes (`bg-amber-100 text-amber-800`), Label-role typography (12px/400/1.4 per UI-SPEC). The badge's visible text is the signal type converted to human-readable form (e.g. `cost_pressure` → "Cost Pressure") — implement a small local label map for the 4 known values (`cost_pressure`, `immature_gbs_org`, `new_cfo_or_gbs_head`, `transformation_announcement`) rather than raw enum strings. Do not introduce a distinct hue per signal type — this is a locked UI-SPEC decision.
    Create `src/components/companies/company-list.tsx`: an `async` named-export `CompanyList({ filters }: { filters?: CompanyFilters })` Server Component. Call `listCompanies(filters)`, then for each company call `listSignalsForCompany(company.id)` to get its badges (acceptable N+1 at this seed-data scale of 9 rows per RESEARCH.md's ILIKE-over-tsvector rationale — do not add a batching optimization this task). Render a shadcn `Table` with columns: Name, Industry, Employee Count, HQ Location, Revenue Band, Ownership Type, Signals (a flex row of `SignalBadge` per distinct signalType). If `listCompanies` returns an empty array, render the UI-SPEC "true empty" copy ("No companies yet" / "Company data will appear here once the seed dataset is loaded.") instead of an empty table — this will not trigger with the Plan 02-01 seed data live, but must not crash if it ever is empty.
    Create `src/app/companies/page.tsx`: an `async` default-export `CompaniesPage` that calls `await requireStaffAccess()` first (belt-and-suspenders alongside the layout gate — RESEARCH.md's Pitfall 4 recommends both), renders `<CompanyList filters={undefined} />` inside a two-column CSS grid (`grid grid-cols-[minmax(320px,1fr)_2fr] gap-8` per RESEARCH.md Pattern 1), with a right-hand placeholder pane showing UI-SPEC copy "Select a company to view details" (Plan 03 replaces this with the real detail route).
    Create `src/app/companies/loading.tsx`: a Suspense fallback rendering shadcn `Skeleton` rows matching the Table's row height (48px min-height per UI-SPEC).
  </action>
  <acceptance_criteria>
    - `grep -c "amber-100" src/components/companies/signal-badge.tsx` is at least 1, and `grep -c "amber\|red\|blue\|green\|purple" src/components/companies/signal-badge.tsx | grep -v amber` shows no non-amber color classes (single neutral style, no per-type hue)
    - `grep -c "listCompanies" src/components/companies/company-list.tsx` is at least 1
    - `grep -c "requireStaffAccess" src/app/companies/page.tsx` is 1
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 (confirms the new route compiles into the App Router build)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <human-check>Sign in, visit /companies: confirm the sidebar shows Companies (active) and Key Personas (disabled), the table lists all 9 seeded companies with industry/employee count/HQ/revenue band/ownership type, and each row with signals shows amber badges labeled by signal type. Then sign out and visit /companies directly: confirm redirect to /sign-in with no company data ever visible.</human-check>
  </verify>
  <done>Signed-in staff visiting /companies see the full firmographics table for all 9 seeded companies with amber signal badges per row inside the collapsible, resizable sidebar shell; unauthenticated visitors are redirected to /sign-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous browser → `/companies` route tree | First new authenticated surface in this phase — every request must be gated before any company data is fetched or rendered |
| npm registry → shadcn CLI-generated files | New third-party generated code (`src/components/ui/*.tsx`) lands in the repo via `npx shadcn add` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-2-03 | Elevation of Privilege | `/companies` route (layout.tsx + page.tsx) | mitigate | `await requireStaffAccess()` is the first line of both `companies/layout.tsx` and `companies/page.tsx` (Pitfall 4) — verified via the human-check redirect test in Task 3. |
| T-2-04 | Information Disclosure | Sequential integer company IDs will be exposed once Plan 03 adds `/companies/[id]` | accept | Already an accepted, documented risk in `.planning/STATE.md`'s Blockers/Concerns — this app's "any authenticated user = full access" model (FOUND-04) makes enumerable IDs a non-issue within milestone 1 scope. No Plan 02 action needed; noted here since the sidebar/list built in this plan is the entry point to that later route. |
| T-2-SC | Tampering | `npx shadcn@latest init`/`add` package installs | mitigate | `shadcn` is already vetted in `.planning/phases/02-company-explorer/02-RESEARCH.md`'s Package Legitimacy Audit (`[OK]`, official docs confirmed) — no additional checkpoint needed for this specific package, since the audit table already covers it per the Package Legitimacy Gate protocol. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` both pass after Task 3
- `components.json` exists, all generated `src/components/ui/*.tsx` files import from `@radix-ui/react-*`, never `@base-ui-components/react`
- Human-check: signed-in staff sees the full firmographics + signal-badge table at /companies; signed-out visitors are redirected
- Human-check: sidebar collapse toggle works, and the drag handle resizes the sidebar between 200-400px with the chosen width surviving a full page reload
</verification>

<success_criteria>
Staff can navigate to /companies and see a real, gated, firmographics-complete list of all 9 seeded companies with signal badges, inside a collapsible and resizable two-section sidebar shell — the first tangible Company Explorer surface.
</success_criteria>

<output>
Create `.planning/phases/02-company-explorer/02-02-SUMMARY.md` when done
</output>
