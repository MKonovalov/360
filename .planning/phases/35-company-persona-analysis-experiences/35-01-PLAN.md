---
phase: 35-company-persona-analysis-experiences
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analysis/experienceContracts.ts
  - src/lib/analysis/experienceContracts.test.ts
  - src/lib/db/queries/analysisRuns.ts
  - src/lib/db/queries/analysisRuns.test.ts
  - src/lib/db/queries/confirmedCandidates.ts
  - src/lib/db/queries/confirmedCandidates.test.ts
  - src/lib/db/queries/confirmedCandidates.integration.test.ts
  - src/app/api/analysis-preview/route.ts
  - src/app/api/analysis-preview/route.test.ts
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "A staff-only preview request returns the one active template compatible with the target, the selected active Practice Area, the resolved instruction, the complete active target-specific checklist, and effort without accepting any of those values from the client."
    - "A subject-scoped history read returns every Company or Persona analysis run, including queued, running, completed, failed, cancelled, pending_review, confirmed, and dismissed rows, newest first, without reconciling or mutating review state."
    - "A confirmed-candidate read is constrained in SQL by both target discriminator and subject ID, includes offering name and source provenance, and preserves Persona retention visibility and confirmed-only semantics."
  artifacts:
    - path: src/app/api/analysis-preview/route.ts
      provides: "Staff-gated server-resolved preview boundary"
      exports: [POST]
    - path: src/lib/db/queries/analysisRuns.ts
      provides: "Read-only all-status subject-scoped history query"
      contains: "listAnalysisRunsForSubject"
    - path: src/lib/db/queries/confirmedCandidates.ts
      provides: "Subject-scoped confirmed candidate projection with offering name"
      contains: "listConfirmedCandidateOfferingsForSubject"
    - path: src/lib/analysis/experienceContracts.ts
      provides: "Shared Zod/type contracts for preview, history, and candidate display"
  key_links:
    - from: src/app/api/analysis-preview/route.ts
      to: src/lib/analysis/subjects.ts
      via: "server-side target/template/Practice Area resolution"
      pattern: "resolveAnalysisSubject|resolveAnalysisTemplate|resolveActivePracticeArea"
    - from: src/app/api/analysis-preview/route.ts
      to: src/lib/analysis/checklist.ts
      via: "shared active checklist derivation"
      pattern: "deriveActiveChecklist"
    - from: src/lib/db/queries/confirmedCandidates.ts
      to: analysis_run.subject_type + analysis_run.subject_id
      via: "SQL predicates, not React filtering"
      pattern: "subject_type.*subject_id"
---

<objective>
Establish the server-owned contracts and data seams that make Company and Persona analysis experiences trustworthy and subject-isolated.

Purpose: Phase 35 UI must display authoritative preview/history/candidate data while Phase 34's immutable packet, review, retention, and confirmed-only boundaries remain intact.
Output: Tested preview Route Handler, all-status subject query, subject-scoped candidate projection, offering names, and shared display contracts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/35-company-persona-analysis-experiences/35-CONTEXT.md
@.planning/phases/35-company-persona-analysis-experiences/35-RESEARCH.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-CONTEXT.md
@.planning/phases/34-whole-run-review-confirmed-candidates/34-RESEARCH.md
@src/app/api/analysis-runs/route.ts
@src/lib/analysis/checklist.ts
@src/lib/analysis/subjects.ts
@src/lib/db/queries/analysisRuns.ts
@src/lib/db/queries/confirmedCandidates.ts
@src/lib/db/queries/analysisResults.ts

<interfaces>
Existing launch remains `POST /api/analysis-runs` with `{templateVersionId, subject, practiceAreaId}`; it resolves the template, subject, Practice Area, checklist, and snapshots again on the server. Existing packet reads must continue through `getAnalysisPacket()` so Persona retention is enforced. Existing candidate identity is polymorphic and requires `signal_type + signal_id`.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define Phase 35 display contracts and adversarial contract tests</name>
  <files>src/lib/analysis/experienceContracts.ts, src/lib/analysis/experienceContracts.test.ts</files>
  <behavior>
    - Preview accepts only target type, positive subject ID, and positive Practice Area ID; its response contains exactly one compatible template summary, resolved instruction, Practice Area identity, ordered checklist items, and effort.
    - History rows retain target type plus subject ID, all lifecycle statuses, timestamps, safe reason, review decision metadata, and a nullable packet projection marker.
    - Candidate display rows require offering name, signal name, strong/weak evidence status, source links, and the Phase 34 run/finding/source/link provenance fields.
    - Equal numeric Company and Persona IDs remain distinct in every contract.
  </behavior>
  <action>Introduce named Zod schemas and inferred interfaces for the preview response, subject-scoped history row, and confirmed-candidate display row. Reuse the existing closed analysis status and target-type schemas rather than duplicating unions. Reject client-only instruction, checklist, actor, model, provider, or decision fields from launch/preview contracts. Add tests for malformed IDs, incompatible target types, every terminal/non-terminal status, candidate evidence status restrictions, and Company/Persona ID collision. This implements D-35-00, D-35-02, D-35-05, D-35-10 and preserves D-34-03/D-34-06.
  <verify><automated>npm test -- src/lib/analysis/experienceContracts.test.ts</automated></verify>
  <done>Downstream components can import strict Phase 35 contracts, all requested collision/status/evidence cases are locked by tests, and no preview contract permits client-authoritative snapshots or review decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add read-only subject-scoped history and candidate query seams</name>
  <files>src/lib/db/queries/analysisRuns.ts, src/lib/db/queries/analysisRuns.test.ts, src/lib/db/queries/confirmedCandidates.ts, src/lib/db/queries/confirmedCandidates.test.ts, src/lib/db/queries/confirmedCandidates.integration.test.ts</files>
  <behavior>
    - `listAnalysisRunsForSubject({targetType, subjectId})` applies both predicates in SQL, returns all statuses newest first, and performs no completed-to-pending-review reconciliation or other write.
    - `listConfirmedCandidateOfferingsForSubject({targetType, subjectId, now})` applies both predicates in SQL, positively requires confirmed run plus confirmed review, preserves strong/weak source-backed rows, and returns offering name plus source/provenance fields.
    - Pending, failed, cancelled, dismissed, unreviewed, no_evidence, inconclusive, and expired/tombstoned Persona fixtures return no candidate rows; a confirmed Company and confirmed Persona sharing the same numeric ID do not cross-resolve.
    - Duplicate active runs remain rejected by the existing create path; the new reads do not weaken that boundary.
  </behavior>
  <action>Add a read-only all-status subject query in the analysis-run query module, ordered by creation/run ID descending and carrying enough review state to render pending-review, confirmed, and dismissed rows without calling global `listRunReviewItems()`. Add a subject-filtered candidate query (or a clearly named subject overload) that keeps the existing confirmed-only SQL, Phase 34 retention predicate, discriminator-plus-snapshot-signal join, deterministic provenance rows, and historical link identity, while selecting `offering.name`. Never fetch a global result set and filter it in React. Extend unit tests and guarded Neon integration fixtures for the required excluded statuses, duplicate active runs, Company/Persona ID collision, offering name, and expired Persona retention. Keep query modules auth-free and do not add schema migrations or writes to Signals, Offerings, links, proposals, or packets. This implements D-35-04, D-35-05, D-35-06, D-35-09, D-35-10 and inherited D-34-03/D-34-04/D-34-06.
  <verify><automated>npm test -- src/lib/db/queries/analysisRuns.test.ts src/lib/db/queries/confirmedCandidates.test.ts &amp;&amp; if [ -n "$TEST_DATABASE_URL" ]; then npm test -- src/lib/db/queries/confirmedCandidates.integration.test.ts; else echo 'MISSING — TEST_DATABASE_URL required for subject/discriminator/retention database evidence'; exit 1; fi</automated></verify>
  <done>Both subject types have SQL-isolated all-status history and confirmed-candidate reads, all adversarial query cases pass when the guarded database is available, and the query diff contains no mutation or legacy proposal path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create the staff-gated server preview resolver</name>
  <files>src/app/api/analysis-preview/route.ts, src/app/api/analysis-preview/route.test.ts</files>
  <behavior>
    - Staff access is checked before parsing or database work; invalid IDs, missing/inactive Practice Areas, missing/inactive templates, unknown subjects, and incompatible subjects return safe existing-style errors.
    - A valid Company or Persona request resolves exactly one active target-compatible fixed template and returns instruction, Practice Area, ordered active checklist names/IDs, and effort.
    - The preview never returns secrets, provider/model credentials, chain-of-thought, raw provider output, unrestricted Persona fields, or a client-supplied instruction/checklist.
    - Existing `POST /api/analysis-runs` remains the authoritative launch boundary and is not replaced by preview data; stale preview data cannot define the persisted snapshot.
  </behavior>
  <action>Implement `POST /api/analysis-preview` as a small Route Handler accepting only `{subject, practiceAreaId}`. Gate with `requireStaffAccess()` first, resolve the one compatible active template server-side, resolve the subject and active Practice Area, derive the checklist via `deriveActiveChecklist()`, and return the shared preview contract including `templateVersionId` only as an opaque launch reference. Reuse existing resolution/error semantics and make the handler fail safe without provider or Firecrawl calls. Add route tests proving Company and Persona compatibility, one-template behavior, full preview fields, auth-first behavior, invalid input, and rejection of any attempted instruction/checklist/actor/model override. Do not alter Phase 36 template lifecycle or dynamic-agent construction. This implements D-35-00, D-35-02, D-35-03 and the server-authority portion of UX-01.
  <verify><automated>npm test -- src/app/api/analysis-preview/route.test.ts src/app/api/analysis-runs/route.test.ts</automated></verify>
  <done>The browser has a staff-gated, four-field-plus-template preview boundary; tests prove it is advisory only and the existing POST route remains authoritative.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → preview API | Untrusted target/Practice Area IDs and attempted override fields cross into server resolution. |
| Browser → subject history/candidate composition | A numeric ID is ambiguous without target discriminator. |
| Persona packet/candidate reads → staff UI | Retention and safe projection must prevent expired/private evidence disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-35-01 | Tampering | `analysis-preview` | mitigate | Server resolves template, subject, Practice Area, checklist, and effort; POST re-resolves and snapshots. |
| T-35-02 | Information disclosure | history/candidate queries | mitigate | SQL predicates require target type plus subject ID and candidate reads reuse Phase 34 retention visibility. |
| T-35-03 | Elevation | preview Route Handler | mitigate | `requireStaffAccess()` is the first operation and actor fields are never accepted. |
| T-35-04 | Tampering | query modules | mitigate | Read-only SELECTs and tests reject live Signal/Offering/link/proposal/packet writes. |
| T-35-SC | Tampering | package/runtime surface | mitigate | No dependency or schema changes; final scope audit covers manifests and forbidden providers. |
</threat_model>

<verification>
Run the focused contract, preview, history, and candidate tests. The integration command must fail closed when `TEST_DATABASE_URL` is absent; absence is not database evidence. No provider or Firecrawl call is permitted.
</verification>

<success_criteria>
Preview, history, and candidate server seams are deterministic, strictly typed, staff-gated where exposed, SQL-subject-isolated, retention-aware, and ready for the independent client/detail plans.
</success_criteria>

<output>
Create `.planning/phases/35-company-persona-analysis-experiences/35-01-SUMMARY.md` when done.
</output>
