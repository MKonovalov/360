---
phase: 35-company-persona-analysis-experiences
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/components/analysis/analysis-run-launcher.tsx
  - src/components/analysis/analysis-run-launcher.test.tsx
  - src/components/analysis/analysis-run-status.tsx
  - src/components/analysis/analysis-run-status.test.tsx
  - src/components/enrichment/enrichment-review-dialog.tsx
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "Analyze in the existing Menu opens a Dialog for both Company and Persona records; it does not dispatch or call the legacy Company proposal Analyze path."
    - "After Practice Area selection, the Dialog automatically shows the server-resolved instruction, Practice Area, complete active checklist, and effort, with no template picker and an immediately enabled Start button."
    - "Starting sends only the existing launch inputs to POST, surfaces duplicate-active-run and safe route errors, and never treats preview instruction/checklist data as authoritative."
    - "Queued/running history rows poll with abort cleanup and stop at completed, failed, cancelled, pending_review, confirmed, or dismissed."
  artifacts:
    - path: src/components/analysis/analysis-run-launcher.tsx
      provides: "Reusable Menu-triggered preview Dialog and durable POST launch"
      exports: [AnalysisRunLauncher]
    - path: src/components/analysis/analysis-run-status.tsx
      provides: "Polling status presentation with terminal stop and cleanup"
      contains: "setInterval"
    - path: src/components/enrichment/enrichment-review-dialog.tsx
      provides: "Analyze Menu action for both target types without legacy event dispatch"
  key_links:
    - from: src/components/enrichment/enrichment-review-dialog.tsx
      to: src/components/analysis/analysis-run-launcher.tsx
      via: "Analyze menu selection opens controlled Dialog"
      pattern: "AnalysisRunLauncher"
    - from: src/components/analysis/analysis-run-launcher.tsx
      to: /api/analysis-preview
      via: "preview fetch after Practice Area selection"
      pattern: "analysis-preview"
    - from: src/components/analysis/analysis-run-launcher.tsx
      to: /api/analysis-runs
      via: "authoritative POST launch with templateVersionId/subject/practiceAreaId only"
      pattern: "fetch.*analysis-runs"
---

<objective>
Build the fixed-template Menu→Dialog launch experience and safe polling primitive for Company and Persona records.

Purpose: Staff must see an authoritative preview before launch and follow durable runs after navigation/reload without reviving the legacy proposal-writing Analyze flow.
Output: Tested preview launcher, both-target Menu integration, and polling status component with terminal cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-company-persona-analysis-experiences/35-CONTEXT.md
@.planning/phases/35-company-persona-analysis-experiences/35-RESEARCH.md
@.planning/phases/35-company-persona-analysis-experiences/35-01-SUMMARY.md
@src/components/analysis/analysis-run-launcher.tsx
@src/components/analysis/analysis-run-status.tsx
@src/components/enrichment/enrichment-review-dialog.tsx
@src/app/api/analysis-preview/route.ts
@src/app/api/analysis-runs/route.ts

<interfaces>
`AnalysisRunLauncher` currently fetches options and posts `{templateVersionId, subject, practiceAreaId}`. `AnalysisRunStatus` currently fetches `/api/analysis-runs/{id}` once and renders safe lifecycle/audit data. The new contracts from 35-01 are the only browser response parsers; do not pass instruction/checklist/actor/model/provider values into POST.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Convert the launcher to a fixed-template server-preview Dialog</name>
  <files>src/components/analysis/analysis-run-launcher.tsx, src/components/analysis/analysis-run-launcher.test.tsx</files>
  <behavior>
    - Company and Persona launchers show Practice Area selection only; no template picker or client-side template filtering remains in the v1.7 flow.
    - Selecting a Practice Area requests the server preview and renders resolved instruction, Practice Area, all checklist signal names, and effort; loading, empty-checklist, stale-preview, invalid, and duplicate-active-run states have safe copy.
    - Start is enabled when the preview resolves, posts only the opaque template version reference plus subject and Practice Area, and refreshes the record after a successful 201 so history appears.
    - Closing/reopening aborts stale preview/post requests and resets state without retaining data from another subject.
  </behavior>
  <action>Refactor `AnalysisRunLauncher` into a controlled Dialog-capable client component that uses the 35-01 preview contract. Keep the exact one-compatible-template rule from D-35-00: the template version returned by the server is displayed as template context if useful but is never selectable. Render the complete read-only preview per D-35-02, enable Start immediately per D-35-03, and send only the existing POST payload so POST remains authoritative. Use existing Dialog, Select, Button, abort-generation, and safe error patterns; do not add a route/page, provider call, dynamic agent control, or client-side checklist resolver. Add component tests for both target types, preview fields, no template picker, immediate Start, abort/reset behavior, and stale/duplicate errors. This covers D-35-00 through D-35-03.
  <verify><automated>npm test -- src/components/analysis/analysis-run-launcher.test.tsx</automated></verify>
  <done>A reusable controlled Dialog gives both target types the exact server-resolved preview and starts only through the existing durable POST boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Analyze Menu for both records and add polling cleanup</name>
  <files>src/components/enrichment/enrichment-review-dialog.tsx, src/components/analysis/analysis-run-status.tsx, src/components/analysis/analysis-run-status.test.tsx</files>
  <behavior>
    - `EnrichMenu` enables the new v1.7 Analyze action for eligible Company and Persona records and opens the launcher Dialog; it never dispatches `arclumen:analyze:start` or imports `/api/companies/[id]/analyze` for this action.
    - Status fetches immediately, repeats only for queued/running rows, aborts in-flight work on unmount/ID change, and clears timers when the response leaves queued/running, including completed, failed, cancelled, pending_review, confirmed, and dismissed.
    - A terminal poll refreshes the server detail tree so a completed packet or review state becomes inspectable; polling errors remain safe and do not create writes.
    - Existing enrichment behavior and any explicitly separate legacy UI remain unchanged.
  </behavior>
  <action>Replace the current Company-only legacy Analyze trigger in the shared Menu with a controlled `AnalysisRunLauncher` instance for both `entityType` values. Preserve EnrichMenu's enrichment Dialog and state separately. Extend `AnalysisRunStatus` with an explicit polling mode used by Phase 35 history rows (preserving a non-polling option for unrelated launcher/status callers if needed), using one AbortController per request generation, a bounded interval, active-generation guards, and `router.refresh()` after the run leaves queued/running. Polling is entered only for queued/running and must clear on completed, failed, cancelled, pending_review, confirmed, or dismissed; never poll forever. Add tests for all listed stops, queued→running→completed, abort/unmount, stale response suppression, both target Menu actions, and static absence of the legacy Analyze endpoint/event from the new path. This implements D-35-01, D-35-03, D-35-06 and the legacy-write boundary inherited from D-34-05.
  <verify><automated>npm test -- src/components/analysis/analysis-run-status.test.tsx src/components/analysis/analysis-run-launcher.test.tsx</automated></verify>
  <done>Both record types open the fixed-template Dialog, the old proposal Analyze path is not reachable from it, and polling is abort-safe and terminal-aware.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Menu/Dialog → preview/POST | Client controls are untrusted and may not provide authoritative checklist, instruction, actor, or provider data. |
| Browser timer → status API | Polling is read-only and must not become an execution mechanism or leak across subjects. |
| New Analyze action → legacy proposal path | The old endpoint can write proposals/signals and is forbidden for Phase 35. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-35-05 | Tampering | launcher POST | mitigate | Send only server-resolved opaque template version, subject, and Practice Area; POST re-resolves all snapshots. |
| T-35-06 | Tampering | EnrichMenu | mitigate | Static/import test rejects legacy event and `/api/companies/[id]/analyze` from the Phase 35 Analyze path. |
| T-35-07 | Availability | polling component | mitigate | AbortController, generation guard, interval cleanup, and terminal-status stop. |
| T-35-SC | Tampering | dependency surface | mitigate | Existing Dialog/Radix/Zod/Vitest only; no package or provider changes. |
</threat_model>

<verification>
Run launcher and polling tests with no provider or Firecrawl credentials. Use mocked preview/status/POST responses only.
</verification>

<success_criteria>
Company and Persona staff can open Analyze from the existing Menu, inspect the full server preview, launch a durable run, and receive safe live status updates without legacy proposal or live Signal writes.
</success_criteria>

<output>
Create `.planning/phases/35-company-persona-analysis-experiences/35-02-SUMMARY.md` when done.
</output>
