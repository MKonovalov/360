---
phase: 35-company-persona-analysis-experiences
plan: 03
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/components/reviews/run-review-card.tsx
  - src/components/reviews/run-review-card.test.tsx
  - src/components/analysis/analysis-history.tsx
  - src/components/analysis/analysis-history.test.tsx
  - src/components/analysis/confirmed-candidate-offerings.tsx
  - src/components/analysis/confirmed-candidate-offerings.test.tsx
  - src/components/companies/company-detail.tsx
  - src/components/personas/persona-detail.tsx
autonomous: true
requirements: [UX-02]
must_haves:
  truths:
    - "Company and Persona detail pages contain an Analysis section with every subject-scoped run newest first, including safe empty/error states and non-terminal live status."
    - "Settled visible packets render through the shared RunReviewCard in read-only mode with findings, source links, provenance, and no Confirm/Dismiss controls."
    - "Pending-review runs show a Review in Reviews link, while the decision remains exclusively in /reviews; confirmed and dismissed states remain inspectable."
    - "Confirmed Candidate Offerings appears immediately after Buying Signals on both record types and shows offering name, triggering signal, strong/weak status, and persisted source links."
    - "Persona result and candidate rendering never bypasses Phase 34 retention visibility or exposes private reasoning/raw packet data."
  artifacts:
    - path: src/components/analysis/analysis-history.tsx
      provides: "Shared all-status record Analysis section"
    - path: src/components/analysis/confirmed-candidate-offerings.tsx
      provides: "Shared candidate offering provenance section"
    - path: src/components/reviews/run-review-card.tsx
      provides: "Interactive / read-only shared result card"
      contains: "readonly"
  key_links:
    - from: src/components/companies/company-detail.tsx
      to: src/lib/db/queries/analysisRuns.ts
      via: "server-side subject-scoped history load"
      pattern: "listAnalysisRunsForSubject"
    - from: src/components/personas/persona-detail.tsx
      to: src/lib/db/queries/confirmedCandidates.ts
      via: "server-side target discriminator + ID candidate load"
      pattern: "listConfirmedCandidateOfferingsForSubject"
    - from: src/components/analysis/analysis-history.tsx
      to: src/components/reviews/run-review-card.tsx
      via: "settled packet projection in read-only mode"
      pattern: "mode=\"readonly\""
    - from: src/components/analysis/confirmed-candidate-offerings.tsx
      to: persisted source rows
      via: "canonical source links with noopener"
      pattern: "noopener noreferrer"
---

<objective>
Compose the subject-scoped server reads into reusable Company and Persona detail experiences.

Purpose: Staff need durable history, inspectable evidence, review visibility, and confirmed candidate offerings directly from either eligible record while Phase 34 retains decision ownership.
Output: Read-only result/history/candidate components and both detail-page integrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-company-persona-analysis-experiences/35-CONTEXT.md
@.planning/phases/35-company-persona-analysis-experiences/35-RESEARCH.md
@.planning/phases/35-company-persona-analysis-experiences/35-01-SUMMARY.md
@src/components/companies/company-detail.tsx
@src/components/personas/persona-detail.tsx
@src/components/reviews/run-review-card.tsx
@src/app/(dashboard)/reviews/page.tsx
@src/lib/db/queries/analysisResults.ts
@src/lib/db/queries/analysisRuns.ts
@src/lib/db/queries/confirmedCandidates.ts

<interfaces>
CompanyDetail and PersonaDetail are Server Components with existing try/catch error cards and stacked sections. The Reviews page maps safe packet projections into `RunReviewCardData`; reuse that projection shape and `getAnalysisPacket()` retention boundary. The new history component receives server-loaded rows and uses the polling status component from 35-02 for queued/running rows.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add shared read-only result, history, and candidate presentation</name>
  <files>src/components/reviews/run-review-card.tsx, src/components/reviews/run-review-card.test.tsx, src/components/analysis/analysis-history.tsx, src/components/analysis/analysis-history.test.tsx, src/components/analysis/confirmed-candidate-offerings.tsx, src/components/analysis/confirmed-candidate-offerings.test.tsx</files>
  <behavior>
    - `RunReviewCard` preserves existing `/reviews` interactive behavior by default but `mode="readonly"` never renders `RunReviewActions` or any Confirm/Dismiss control.
    - Read-only pending-review cards show a normal `/reviews` link; no alternate action or decision semantics are introduced.
    - History renders all rows newest first, uses polling status for queued/running, retains failed/cancelled safe reason rows, and renders settled visible packets through the read-only card.
    - Candidate rows show offering name, signal name, evidence status, and every persisted canonical source link with safe external-link attributes; zero rows have an explicit empty state.
  </behavior>
  <action>Extend `RunReviewCard` with a backwards-compatible read-only mode and a pending-review link while retaining the exact shared finding/source/provenance markup and no chain-of-thought. Build `AnalysisHistory` as the shared composition boundary: map server-projected settled packets to `RunReviewCard mode="readonly"`, render failed/cancelled rows with safe lifecycle copy, and mount polling status only for queued/running rows so completion refreshes the Server Component tree. Build `ConfirmedCandidateOfferings` from the 35-01 subject-scoped contract, retaining separate provenance rows when sources/findings differ and showing offering name, triggering signal, strong/weak evidence status, and canonical links. Add tests for read-only controls, pending-review navigation, every lifecycle branch, source-link safety, duplicate provenance preservation, and empty/error states. This implements D-35-04, D-35-05, D-35-06, D-35-07, D-35-08, D-35-09, and D-35-10, plus inherited D-34-02/D-34-04/D-34-06.
  <verify><automated>npm test -- src/components/reviews/run-review-card.test.tsx src/components/analysis/analysis-history.test.tsx src/components/analysis/confirmed-candidate-offerings.test.tsx</automated></verify>
  <done>Shared components render the full safe record experience, use polling only for active statuses, preserve provenance, and make target-page review decisions impossible.</done>
</task>

<task type="auto">
  <name>Task 2: Compose Analysis and candidates into Company and Persona details</name>
  <files>src/components/companies/company-detail.tsx, src/components/personas/persona-detail.tsx</files>
  <action>Extend both Server Components' subject-specific `Promise.all` reads with `listAnalysisRunsForSubject` and `listConfirmedCandidateOfferingsForSubject`, passing the explicit discriminator (`company` or `persona`) and current record ID. Project visible settled packets through the same retention-aware `getAnalysisPacket()` boundary used by `/reviews`, never through raw packet JSON or global query results. Add the shared `Analysis` section after the record header/alongside existing stacked sections and place `Confirmed Candidate Offerings` immediately after `Buying Signals` per D-35-09; preserve existing Company/Persona error-card and not-found behavior with widget-safe empty/error sentinels where appropriate. Mount the 35-02 launcher through the existing Menu props for both records, remove the Company-only legacy Analyze ownership from the v1.7 surface, and do not remove unrelated legacy proposal display unless it is proven unreachable from the new action. Keep all decision actions out of these pages and link pending review to `/reviews`. This implements D-35-01, D-35-04, D-35-06, D-35-08, D-35-09, D-35-10 and UX-02 without entering Phase 36.
  <verify><automated>npx tsc --noEmit &amp;&amp; npm test -- src/components/reviews/run-review-card.test.tsx src/components/analysis/analysis-history.test.tsx src/components/analysis/confirmed-candidate-offerings.test.tsx</automated></verify>
  <done>Both target details show their own all-status history and confirmed candidates in the required order, use the fixed Menu launcher, retain safe independent failure behavior, and expose no target-page review controls.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DB query → Server Component | Only subject-scoped, retention-safe projections may cross into rendering. |
| Server Component → client history/polling | Client receives safe lifecycle/display data, never private reasoning or raw packet fields. |
| Record page → review decision | Target page is read-only; `/reviews` remains the sole Confirm/Dismiss surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-35-08 | Information disclosure | Company/Persona detail | mitigate | Use target-scoped SQL and `getAnalysisPacket()` retention boundary; never React-filter globals. |
| T-35-09 | Tampering | `RunReviewCard` | mitigate | `mode="readonly"` omits `RunReviewActions`; static tests assert no decision controls/imports on record surface. |
| T-35-10 | Information disclosure | source links | mitigate | Render only persisted canonical URLs/titles with `target="_blank" rel="noopener noreferrer"`. |
| T-35-11 | Tampering | candidate offerings | mitigate | Render only query-projected confirmed rows; no Signal, Offering, link, or packet write path exists. |
| T-35-SC | Tampering | scope | mitigate | No template lifecycle, dynamic agents, provider calls, or package changes in these components. |
</threat_model>

<verification>
Run focused component tests and typecheck. Use fixture props for settled/pending/failed/retained rows; do not invoke an analysis provider or Firecrawl.
</verification>

<success_criteria>
Both record types provide a complete, stacked, subject-isolated, read-only analysis experience with durable history, safe result/source visibility, review navigation, and confirmed candidate provenance.
</success_criteria>

<output>
Create `.planning/phases/35-company-persona-analysis-experiences/35-03-SUMMARY.md` when done.
</output>
