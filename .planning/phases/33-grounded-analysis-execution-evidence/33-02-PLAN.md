---
phase: 33-grounded-analysis-execution-evidence
plan: 02
type: execute
wave: 1
depends_on: [33-01]
files_modified:
  - src/lib/db/schema.ts
  - src/lib/db/queries/analysisResults.ts
  - src/lib/db/queries/analysisResults.test.ts
  - src/lib/db/queries/analysisResults.integration.test.ts
  - src/lib/db/analysisResultsSchema.integration.test.ts
autonomous: true
requirements: [EVD-01, EVD-02, EVD-03, EVD-04, EVD-05]
must_haves:
  truths:
    - "One completed run has at most one immutable result packet, and packet children are persisted atomically or not at all."
    - "Result, finding, source, and finding-source identity is additive and separate from legacy agent/proposal/review tables."
    - "A replay sees the existing authoritative packet and never overwrites or duplicates it."
    - "Persona artifacts have server-enforced expiry/tombstone visibility, and no Persona artifact or telemetry is retained when policy is unavailable."
  artifacts:
    - path: src/lib/db/schema.ts
      provides: "Additive result/finding/source/link tables and constraints"
      contains: "analysisRunResult"
    - path: src/lib/db/queries/analysisResults.ts
      provides: "DB-only immutable packet persistence and safe reads"
      contains: "persistAnalysisPacket"
    - path: src/lib/db/queries/analysisResults.integration.test.ts
      provides: "Neon HTTP atomicity, replay, uniqueness, and no-update/delete proof"
      contains: "TEST_DATABASE_URL"
  key_links:
    - from: src/lib/db/queries/analysisResults.ts
      to: src/lib/db/schema.ts
      via: "single statement/CTE insert into packet header and children"
      pattern: "analysis_run_result"
    - from: src/lib/db/queries/analysisResults.ts
      to: src/lib/analysis/groundedContracts.ts
      via: "validated packet input at persistence boundary"
      pattern: "safeParse"
---

<objective>
Add the immutable normalized packet persistence boundary using Neon-http-safe atomicity.

Purpose: Make EVD-01..05 durable without mutating Phase 32 intent snapshots or reusing legacy analysis structures (D-33-03, D-33-04).
Output: Additive schema, DB-only insert/read helpers, and database-backed atomicity/replay tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-grounded-analysis-execution-evidence/33-CONTEXT.md
@.planning/phases/33-grounded-analysis-execution-evidence/33-PATTERNS.md
@.planning/phases/33-grounded-analysis-execution-evidence/33-01-PLAN.md
@.planning/phases/32-template-snapshot-run-ledger/32-TRANSACTION-PROBE.md
@src/lib/db/schema.ts
@src/lib/db/queries/analysisRuns.ts
@src/lib/db/index.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the additive immutable packet schema</name>
  <files>src/lib/db/schema.ts, src/lib/db/analysisResultsSchema.integration.test.ts</files>
  <behavior>
    - Schema contains one result header keyed uniquely by `analysis_run_id`, normalized finding rows, canonical source rows, and finding-source link rows.
- Foreign keys/unique constraints prevent duplicate packet, duplicate canonical source identity within a run, and duplicate finding-source links; canonical input duplicates are collapsed before persistence.
    - No update/delete helper or migration path touches `agent_run`, `signal_proposal`, Reviews, live Signals/Offerings, or Phase 31 proof tables.
  </behavior>
  <action>Extend `src/lib/db/schema.ts` additively with enums/columns for packet schema version, evidence status/confidence, source classification, support role, and safe audit metadata. Store narrative, redacted raw audit, model/chain/trace/timing/count/hash metadata only in the result header; store snapshot signal identity and normalized claims in findings; store exact canonical URL/title/retrieval time/bounded excerpt/content hash/classification/provider metadata in sources; store finding-source support links separately. Add database constraints for one packet per run, immutable identity, required foreign keys, and duplicate prevention. Keep the Phase 32 ledger and all legacy tables structurally untouched. Add schema metadata assertions for additive-only objects and the required constraints. Do not use an interactive transaction API.</action>
  <verify><automated>if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 33 schema metadata evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/lib/db/analysisResultsSchema.integration.test.ts</automated></verify>
  <done>The additive result schema is represented in Drizzle with constraints matching the strict contracts and no protected legacy table is repurposed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic immutable packet persistence and replay reads</name>
  <files>src/lib/db/queries/analysisResults.ts, src/lib/db/queries/analysisResults.test.ts, src/lib/db/queries/analysisResults.integration.test.ts</files>
  <behavior>
    - Valid packet persists header, findings, sources, and links in one atomic Neon-http-safe statement/CTE.
    - Deliberate child insert failure rolls back the header and every child row.
    - Replaying the same run returns the existing packet without overwrite or duplicate rows; concurrent attempts converge on one packet.
    - Query module accepts only validated packet data and has no provider, auth, Workflow, or UI dependencies.
  </behavior>
  <action>Create `persistAnalysisPacket` and safe packet read helpers behind the grounded contracts. Use the already-proven one-statement data-modifying CTE or another explicitly proven Neon-http mechanism; never call `db.transaction(async ...)` and never split header/child inserts into independent requests. Make the run ID unique/idempotent, detect an existing packet as authoritative, reject conflicting packet hashes, and expose no update/delete function. Keep raw audit insertion allowlisted and ensure no prompt, secret, PII, unrestricted content, or chain-of-thought can enter JSONB. Canonical duplicate sources must collapse to one source identity; a repeated finding-source pair must fail validation or the unique constraint rather than create a second link. Add unit and TEST_DATABASE_URL-backed integration coverage for atomic rollback, replay/idempotency, canonical dedupe, duplicate-link rejection, and legacy-table preservation.</action>
  <verify><automated>npm test -- src/lib/db/queries/analysisResults.test.ts &amp;&amp; if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 33 persistence evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/lib/db/queries/analysisResults.integration.test.ts</automated></verify>
  <done>Persistence is atomic, insert-only, replay-safe, Neon-http compatible, and proven against deliberate failures and duplicate attempts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce Persona retention and no-retention fail-closed behavior</name>
  <files>src/lib/db/schema.ts, src/lib/db/queries/analysisResults.ts, src/lib/db/queries/analysisResults.test.ts, src/lib/db/queries/analysisResults.integration.test.ts</files>
  <behavior>
    - Persona packet/source/telemetry writes are rejected when the policy is deferred or has no approved retention duration.
    - Approved Persona artifacts carry policy version, classification, and expiry; reads exclude expired/tombstoned artifacts through a server-side query path.
    - A retention enforcement call records a safe tombstone/expiry state without mutating immutable packet contents, and subsequent reads cannot return the expired artifact as retained evidence.
  </behavior>
  <action>Add an additive retention/tombstone relation or equivalent server-side retention record keyed to Persona result artifacts, with policy version, classification, expiry, and safe tombstone reason. Make `persistAnalysisPacket` reject Persona persistence without an approved retention policy and make packet reads use the retention query path rather than relying on UI filtering. Add an idempotent `enforcePersonaArtifactRetention`/equivalent query helper that hides or tombstones expired artifacts while leaving immutable packet rows unchanged. Test missing-policy no-retention, approved expiry, repeated enforcement, safe telemetry absence, and non-Persona packet behavior. Do not invent a duration, delete immutable evidence directly, or expose a manual UI retention control.</action>
  <verify><automated>npm test -- src/lib/db/queries/analysisResults.test.ts &amp;&amp; if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 33 retention evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/lib/db/queries/analysisResults.integration.test.ts</automated></verify>
  <done>Persona retention is enforced server-side, absent policy retains nothing, approved expiry is auditable/idempotent, and immutable packet contents are never updated or returned after tombstoning.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| validated packet → database | A single server-owned write creates durable product artifacts. |
| retry/replay → immutable packet | Duplicate Workflow execution can repeat the persistence call. |
| packet DB → later phases | Phase 34/35 will consume rows as trusted read models. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-04 | Tampering | packet persistence | mitigate | Unique run key, packet hash conflict detection, no update/delete helper, atomic CTE. |
| T-33-05 | Tampering/DoS | child inserts | mitigate | All-or-nothing statement and deliberate-error rollback test prevents partial packets. |
| T-33-06 | Information Disclosure | JSONB audit/source fields | mitigate | Validate allowlists before SQL and assert secret/PII/private-reasoning rejection. |
| T-33-SC | Supply chain | schema/dependency changes | mitigate | No package/manifest changes; additive-only schema metadata audit. |
</threat_model>

<verification>
Run the focused unit suite and the guarded TEST_DATABASE_URL integration suite. A missing database URL is a failed prerequisite, not a skipped pass.
</verification>

<success_criteria>
The packet persistence boundary can be called safely by a later workflow step and cannot produce a completed run without an atomic immutable packet.
</success_criteria>

<output>
Create `.planning/phases/33-grounded-analysis-execution-evidence/33-02-SUMMARY.md` when done.
</output>
