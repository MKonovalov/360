---
phase: 33-grounded-analysis-execution-evidence
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/analysis/groundedContracts.ts
  - src/lib/analysis/groundedContracts.test.ts
  - src/lib/analysis/contracts.ts
  - src/lib/analysis/contracts.test.ts
  - src/lib/analysis/snapshots.ts
  - src/lib/analysis/snapshots.test.ts
  - src/lib/analysis/personaPolicy.ts
  - src/lib/analysis/personaPolicy.test.ts
  - src/app/api/analysis-runs/route.ts
  - src/app/api/analysis-runs/route.test.ts
  - .planning/phases/33-grounded-analysis-execution-evidence/33-CONTEXT.md
autonomous: false
requirements: [RUN-04, EVD-01, EVD-02, EVD-03, EVD-04, EVD-05]
must_haves:
  truths:
    - "The Phase 33 policy and packet contracts reject missing or unsafe execution policy before provider or Firecrawl calls."
    - "Finding identity, evidence status, confidence, source classification, safe audit, and Persona privacy fields are strict closed contracts rather than permissive JSON."
    - "Persona execution is disabled until an explicit approved version, redaction/classification policy, and retention duration are represented."
  artifacts:
    - path: src/lib/analysis/groundedContracts.ts
      provides: "Strict Zod contracts for execution policy, packet, finding, canonical source, source link, safe audit, and failure reasons"
      contains: "z.object"
    - path: src/lib/analysis/personaPolicy.ts
      provides: "Fail-closed Persona policy resolver and allowlist/redaction/classification contract"
      contains: "persona_policy_unavailable"
    - path: src/lib/analysis/groundedContracts.test.ts
      provides: "Contract matrix for safe/unsafe packet and policy inputs"
      contains: "safeParse"
  key_links:
    - from: src/lib/analysis/personaPolicy.ts
      to: src/lib/analysis/groundedContracts.ts
      via: "policy schema and safe redaction types"
      pattern: "Grounded.*Policy"
    - from: src/lib/analysis/groundedContracts.test.ts
      to: src/lib/analysis/groundedContracts.ts
      via: "runtime validation assertions"
      pattern: "safeParse"
---

<objective>
Lock the Phase 33 execution/evidence contract and resolve the product-policy gate before any real model or Firecrawl execution can be enabled.

Purpose: Prevent policy invention, provider drift, permissive evidence acceptance, and Persona privacy leakage at the earliest boundary (D-33-01, D-33-05, D-33-06, D-33-07).
Output: Strict reusable contracts, fail-closed Persona policy module, adversarial contract tests, and an explicit approved-policy decision record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-grounded-analysis-execution-evidence/33-RESEARCH.md
@.planning/phases/33-grounded-analysis-execution-evidence/33-CONTEXT.md
@.planning/phases/32-template-snapshot-run-ledger/32-01-SUMMARY.md
@src/lib/analysis/contracts.ts
@src/lib/analysis/snapshots.ts
</context>

<tasks>
<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Approve the bounded execution and Persona privacy policy</name>
  <files>.planning/phases/33-grounded-analysis-execution-evidence/33-CONTEXT.md</files>
  <decision>Record the exact versioned Phase 33 policy limits and whether Persona execution is enabled. The approval must specify attempts, tool calls, wall time, source/result bounds, spend, Persona allowlisted input fields, redaction/classification rules, retention duration, excerpt/full-content policy, and safe raw-audit fields.</decision>
  <context>Per D-33-06 and D-33-07, missing values are not implementation discretion. The executor may build fail-closed contracts, but it must not call modelFactory or Firecrawl or persist Persona artifacts until the values are explicitly approved.</context>
  <options>
    <option id="company-only-fail-closed-persona"><name>Approve bounded Company execution; keep Persona disabled</name><pros>Unblocks Company packet work without inventing Persona policy.</pros><cons>Persona execution remains a deliberate safe failure until a later approval.</cons></option>
    <option id="company-and-persona"><name>Approve bounded Company and Persona execution</name><pros>Enables both target kinds in this phase.</pros><cons>Requires explicit privacy/legal values and test vectors now.</cons></option>
  </options>
  <resume-signal>Record the selected option and exact approved values in 33-CONTEXT.md; if values are not approved, retain the fail-closed policy and continue contract-only work.</resume-signal>
  <action>Do not implement or infer policy values. Pause execution at this gate, obtain the explicit product/privacy decision, and append the selected option plus exact values or the explicit fail-closed deferral to 33-CONTEXT.md.</action>
  <verify><automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/phases/33-grounded-analysis-execution-evidence/33-CONTEXT.md','utf8');const m=s.match(/^status: (approved|deferred)$/m);if(!m)process.exit(1);if(m[1]==='deferred'&&!/^executionEnabled: false$/m.test(s))process.exit(1);if(m[1]==='approved'&&(!/^policyVersion: (?!null$).+/m.test(s)||!/^approvedBy: (?!null$).+/m.test(s)||!/^approvedAt: (?!null$).+/m.test(s)||/^limits: null$/m.test(s)||/^personaPolicy: null$/m.test(s)||/^retention: null$/m.test(s)))process.exit(1)"</automated></verify>
  <done>The policy decision is explicit in 33-CONTEXT.md; no execution path can treat missing approval as an approved default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the versioned Phase 33 policy snapshot handoff at run creation</name>
  <files>src/lib/analysis/contracts.ts, src/lib/analysis/contracts.test.ts, src/lib/analysis/snapshots.ts, src/lib/analysis/snapshots.test.ts, src/app/api/analysis-runs/route.ts, src/app/api/analysis-runs/route.test.ts</files>
  <behavior>
    - Existing `PHASE32_NOOP_POLICY` and already-created Phase 32 snapshots remain valid and immutable.
    - New Phase 33 run creation snapshots either a complete approved versioned grounded policy or an explicit `phase33_policy_deferred` policy with execution disabled and safe `policy_unavailable` outcome.
    - Snapshot creation never reads mutable settings/policy after insert and replay never rewrites policy, execution, subject, checklist, or template snapshots.
    - A deferred policy prevents model/Firecrawl dispatch; an approved policy is the only state that can enable bounded execution.
  </behavior>
  <action>Extend the existing snapshot contract with a versioned Phase 33 policy discriminated union and add a dedicated `buildPhase33AnalysisSnapshots` path at the server-side analysis-run creation boundary. Keep the existing Phase 32 builder and `PHASE32_NOOP_POLICY` unchanged for compatibility and already-created runs; do not mutate snapshots in place. The new builder reads the machine-readable policy decision record once before the insert, validates every approved limit/Persona field, or emits an explicit disabled `phase33_policy_deferred` snapshot when approval is absent. Thread that builder through the staff-gated create route without exposing policy controls to the client, and add tests for approved, deferred, immutable replay, and no-provider-dispatch behavior. Do not use research-only future budget values as defaults.</action>
  <verify><automated>npm test -- src/lib/analysis/contracts.test.ts src/lib/analysis/snapshots.test.ts src/app/api/analysis-runs/route.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>Future analysis runs carry an authoritative versioned Phase 33 policy handoff; old Phase 32 no-op snapshots and replay semantics remain untouched, and deferred policy cannot dispatch execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Define strict grounded execution, packet, and Persona policy contracts</name>
  <files>src/lib/analysis/groundedContracts.ts, src/lib/analysis/groundedContracts.test.ts, src/lib/analysis/personaPolicy.ts, src/lib/analysis/personaPolicy.test.ts</files>
  <behavior>
    - Missing/unapproved policy -> safe `policy_unavailable` or `persona_policy_unavailable`, with zero provider/tool calls permitted.
    - Finding signal identity absent from the immutable checklist -> rejected.
    - Evidence statuses are exactly `strong`, `weak`, `no_evidence`, or `inconclusive`; confidence is bounded and typed.
    - Source/link contracts reject credentials, fragments, unsupported schemes, unbounded excerpts, unrestricted raw audit, private reasoning, secrets, Clerk/session values, and full Persona rows.
    - Approved Persona fixtures retain only allowlisted minimum fields and redact email/phone/personal URLs/secrets before prompt, audit, source, or telemetry use.
  </behavior>
  <action>Using the approved values recorded for D-33-06 (or the fail-closed absence of those values), create strict Zod schemas and named constants for the versioned execution policy, serializable execution input, normalized packet, finding identity/status/confidence, canonical source, finding-source link, redacted safe audit, trace/timing metadata, safe failure reasons, and Persona policy/classification/retention. Preserve Phase 32 snapshot types rather than mutating them. Make schemas reject unknown keys where persisted data is concerned, bound strings/arrays/bytes, forbid prompts/private reasoning/provider keys/database URLs/Clerk values, and expose pure helpers for Persona allowlist/redaction/classification. Add tests for every behavior above plus duplicate finding IDs, canonical source dedupe, duplicate links, empty checklists, no-evidence findings, and policy-unavailable fail-closed behavior. Do not add default policy values when the approval is absent, and do not call any provider or tool from these modules.</action>
  <verify><automated>npm test -- src/lib/analysis/groundedContracts.test.ts src/lib/analysis/personaPolicy.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>All Phase 33 persisted and execution inputs have strict runtime contracts; Persona cannot execute or persist without explicit approved policy, and adversarial contract tests pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 32 snapshot → execution contract | Immutable JSON is untrusted until parsed by strict schemas. |
| Persona DB row → model/tool/audit | Sensitive fields cross into processing and must be allowlisted/redacted. |
| Model/tool output → packet contract | Untrusted generated/retrieved content must not become product truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-01 | Tampering/Elevation | grounded contracts | mitigate | Strict Zod schemas, snapshot checklist identity validation, closed enums, and policy version checks. |
| T-33-02 | Information Disclosure | personaPolicy | mitigate | Fail closed without approval; allowlist minimum fields and redact PII/secrets before downstream use. |
| T-33-03 | Information Disclosure | safe audit contract | mitigate | Persist only allowlisted diagnostics; reject prompts, unrestricted output, credentials, sessions, and private reasoning. |
| T-33-SC | Tampering | package/runtime surface | mitigate | No package or provider install; final source-scope audit blocks Exa/new SDK drift. |
</threat_model>

<verification>
The blocking policy decision is recorded before real execution is enabled. Focused contract and Persona tests, TypeScript diagnostics, and source inspection prove no provider/tool calls or production writes occur in this plan.
</verification>

<success_criteria>
The next plans can consume one decision-complete, strict, fail-closed contract without inventing execution limits or Persona policy.
</success_criteria>

<output>
Create `.planning/phases/33-grounded-analysis-execution-evidence/33-01-SUMMARY.md` when done.
</output>

## Execution Status

- [x] Task 1: Blocking policy checkpoint recorded as explicit deferred/fail-closed (`executionEnabled: false`); no policy limits or Persona retention values invented.
- [x] Task 2: Versioned Phase 33 policy handoff added at server-owned run creation; Phase 32 no-op snapshots remain compatible.
- [x] Task 3: Strict grounded packet/evidence and Persona policy contracts plus adversarial tests added.
- [x] Focused verification: 5 test files, 54 tests passed; `npx tsc --noEmit` passed.
- [x] No provider, Firecrawl, Langfuse, or external credentials invoked; no real execution enabled.
