---
phase: 33-grounded-analysis-execution-evidence
plan: 05
type: execute
wave: 3
depends_on: [33-02, 33-03, 33-04]
files_modified:
  - src/workflows/analysisRun.ts
  - src/workflows/analysisRun.integration.test.ts
  - src/lib/telemetry/langfuse.ts
  - src/lib/telemetry/langfuse.test.ts
  - src/lib/db/queries/analysisRuns.ts
  - src/lib/db/queries/analysisResults.ts
autonomous: true
requirements: [RUN-04, EVD-01, EVD-02, EVD-03, EVD-04, EVD-05]
must_haves:
  truths:
    - "A claimed scalar-ID Workflow reloads snapshots, executes, validates, persists the packet, and only then transitions the database run to completed."
    - "Retries and replays never duplicate or overwrite a packet; a pre-completion persistence failure leaves no completed ledger without a packet and records a safe failure audit."
    - "Langfuse receives only safe allowlisted metadata and is best-effort; telemetry failure cannot turn a valid persisted packet into a product failure."
    - "Persona policy failures and evidence failures fail closed through the database-authoritative lifecycle without provider/tool calls when policy is absent, and no Persona telemetry is retained in that state."
  artifacts:
    - path: src/workflows/analysisRun.ts
      provides: "Phase 33 durable claim→reload→execute→validate→persist→complete orchestration"
      contains: "persistAnalysisPacket"
    - path: src/workflows/analysisRun.integration.test.ts
      provides: "Workflow ordering, replay, failure, timeout, and no-write proof"
      contains: "completed"
    - path: src/lib/telemetry/langfuse.ts
      provides: "Safe Phase 33 trace/timing metadata and best-effort flush/lookup"
      contains: "trace"
  key_links:
    - from: src/workflows/analysisRun.ts
      to: src/lib/analysis/execution.ts
      via: "scalar run reload produces validated execution input"
      pattern: "executeGroundedAnalysis"
    - from: src/workflows/analysisRun.ts
      to: src/lib/analysis/results.ts
      via: "raw attempt normalization before persistence"
      pattern: "normalizeAnalysisPacket"
    - from: src/workflows/analysisRun.ts
      to: src/lib/db/queries/analysisResults.ts
      via: "packet persistence precedes completed transition"
      pattern: "persistAnalysisPacket"
    - from: src/lib/telemetry/langfuse.ts
      to: src/workflows/analysisRun.ts
      via: "safe metadata mirror, never product lifecycle authority"
      pattern: "trace"
---

<objective>
Replace the Phase 32 no-op completion branch with database-authoritative grounded execution and safe telemetry.

Purpose: Deliver the durable RUN-04/EVD-01..05 path while preserving scalar Workflow input, immutable snapshots, bounded safe failure, and completion-after-persistence ordering (D-33-02 through D-33-07).
Output: Workflow integration, replay-safe terminal behavior, safe Langfuse metadata, and database-backed proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-grounded-analysis-execution-evidence/33-CONTEXT.md
@.planning/phases/33-grounded-analysis-execution-evidence/33-PATTERNS.md
@.planning/phases/31-durable-executor-selection-validation/31-02-SUMMARY.md
@.planning/phases/31-durable-executor-selection-validation/31-03-SUMMARY.md
@src/workflows/analysisRun.ts
@src/lib/db/queries/analysisRuns.ts
@src/lib/telemetry/langfuse.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire the durable Workflow execution sequence and safe terminal paths</name>
  <files>src/workflows/analysisRun.ts, src/workflows/analysisRun.integration.test.ts, src/lib/db/queries/analysisRuns.ts, src/lib/db/queries/analysisResults.ts</files>
  <behavior>
    - Workflow accepts only `applicationRunId: number`; claim is guarded and every later step reloads database-authoritative state/snapshots.
    - Sequence is claim -> reload -> policy gate -> adapter -> normalize -> atomic packet persistence -> `running → completed`; no completion transition occurs before persistence success.
    - Existing immutable packet on replay is returned as authoritative; conflicting packet data, invalid evidence, policy unavailability, timeout, provider/tool failure, and persistence failure record bounded safe audit and transition to failed without partial packet.
    - Workflow metadata is never used to mark product status, and no agent step writes directly to any DB table.
  </behavior>
  <action>Replace only `completeNoOpRun`'s successful branch with explicit scalar Workflow steps that reload the Phase 32 row and immutable snapshots, validate the approved Phase 33 execution policy, call `executeGroundedAnalysis`, normalize with the fail-closed packet validator, call atomic `persistAnalysisPacket`, and then use the existing guarded `transitionAnalysisRun` for completion. Retain and adapt existing timeout/cancel/replay-safe observation behavior to the approved execution budget and bounded attempts. Persist safe failure/result audit through the existing lifecycle/query boundary without raw errors. Do not alter Phase 32 snapshots, create a second executor, use Workflow metadata as product truth, or write Reviews/candidates/live Signals/Offerings.</action>
  <verify><automated>if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 33 Workflow evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow</automated></verify>
  <done>Local World proves caller-independent claim, bounded execution, persistence-before-completion, safe failure, timeout, replay/idempotency, and no packet duplication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add safe Langfuse trace and timing metadata</name>
  <files>src/lib/telemetry/langfuse.ts, src/lib/telemetry/langfuse.test.ts, src/workflows/analysisRun.ts</files>
  <behavior>
    - Packet/audit metadata contains safe model ID/chain, actual model/fallback, trace ID/URL when available, timing, tool/result counts, packet schema/policy version, and no raw prompt/output/excerpt/Persona PII/private reasoning.
    - Langfuse initialization, lookup, and flush are best-effort and never change database lifecycle or packet validity.
    - Redaction tests reject provider keys, database URLs, Clerk/session values, names/emails/personal URLs where policy disallows them, unrestricted web content, and chain-of-thought.
  </behavior>
  <action>Extend the existing Langfuse singleton path rather than adding a second telemetry registration. Define a Phase 33 allowlisted metadata builder and redaction test vectors; record safe trace/timing identifiers in the packet audit only after normalization. Preserve null-safe behavior when Langfuse keys are unavailable and ensure telemetry errors are swallowed into safe diagnostics after product persistence, not used as completion authority. Do not send raw prompts, outputs, sources, Persona rows, secrets, or private reasoning.</action>
  <verify><automated>npm test -- src/lib/telemetry/langfuse.test.ts &amp;&amp; npx tsc --noEmit</automated></verify>
  <done>Telemetry is a safe observability mirror with explicit redaction coverage and cannot leak sensitive data or block product execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove adversarial workflow failure and replay matrices</name>
  <files>src/workflows/analysisRun.integration.test.ts, src/lib/db/queries/analysisResults.integration.test.ts</files>
  <behavior>
    - Prompt injection, SSRF/private URL, malformed/unlinked evidence, duplicate-link, missing support, citation mismatch, and Persona policy-unavailable fixtures produce no completed packet.
    - Canonical duplicate source discovery keeps one source and does not create duplicate finding-source links.
    - A valid packet persists exactly once before completion and remains readable after replay.
    - Provider/tool failure, timeout, persistence failure, and telemetry failure produce safe bounded outcomes with database-authoritative event history.
  </behavior>
  <action>Extend the isolated Workflow and Neon integration fixtures to exercise the full claim-to-terminal path with mocked model/Firecrawl/telemetry seams. Assert exact event ordering, packet row counts, no partial rows, no legacy/review/candidate/live-signal writes, safe reason codes, bounded attempts/tool calls, canonical source dedupe, duplicate-link rejection, Persona retention/tombstone behavior, and replay behavior. Keep live credentials out of automated tests and fail closed when `TEST_DATABASE_URL` is absent; when Persona policy is deferred, assert zero Persona packet/source/telemetry retention.</action>
  <verify><automated>if [ -z "${TEST_DATABASE_URL:-}" ]; then printf '%s\n' 'TEST_DATABASE_URL is required for Phase 33 integration evidence' >&amp;2; exit 1; fi; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm test -- src/lib/db/queries/analysisResults.integration.test.ts &amp;&amp; DATABASE_URL="$TEST_DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" npm run test:workflow</automated></verify>
  <done>Adversarial, failure, persistence-ordering, and replay cases prove the workflow never presents unsupported evidence or a completed run without its immutable packet.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Workflow runtime → application DB | Durable steps can replay and race; DB lifecycle is authoritative. |
| adapter output → packet persistence | Only normalized packets may become product artifacts. |
| application execution → Langfuse | Observability receives a restricted metadata projection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-33-15 | Tampering | Workflow replay/completion | mitigate | Guarded transitions, unique packet key, authoritative reload, persist-before-complete ordering. |
| T-33-16 | DoS | bounded execution | mitigate | Snapshot policy limits, timeout/attempt/tool ceilings, safe terminal transition. |
| T-33-17 | Information Disclosure | Langfuse | mitigate | Allowlisted metadata/redaction, no raw prompt/output/Persona/source content, best-effort errors. |
| T-33-18 | Elevation | direct agent writes | mitigate | Adapter has no DB capability; only dedicated persistence step can write validated packet rows. |
| T-33-SC | Tampering | dependency/provider scope | mitigate | Source audit proves existing seams only; no package changes or Exa path. |
</threat_model>

<verification>
The guarded database-backed Workflow suite and packet integration suite are mandatory. The existing Phase 31 scalar/deployed-origin pattern remains the durability precedent; Workflow metadata is diagnostic evidence only.
</verification>

<success_criteria>
A durable claimed run produces one immutable source-grounded packet and reaches completed only after that packet is durable; every unsafe/failure/replay path remains safe and auditable.
</success_criteria>

<output>
Create `.planning/phases/33-grounded-analysis-execution-evidence/33-05-SUMMARY.md` when done.
</output>
