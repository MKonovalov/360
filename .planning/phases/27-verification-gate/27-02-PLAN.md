---
phase: 27-verification-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agents/structured-outputs-probe.test.ts
  - src/lib/agents/modelFactory.ts
autonomous: true
requirements: [VER-05]
must_haves:
  truths:
    - "Each of the 3 new openai-compatible instances (nousresearch, opencode-zen, opencode-go) is independently probed with a live generateText/Output.object round trip against the REAL production outputSchema from types.ts — not a toy schema"
    - "Each instance's supportsStructuredOutputs constructor flag flips to true ONLY if its own probe passed — per-instance, never all-or-nothing (D-27-06)"
    - "An instance whose probe fails or skips (key absent) is left at false (the safe json_object fallback), documented with a dated comment, never force-flipped"
    - "instantiateModel's dispatch function (lines ~96-140) is untouched — the flag lives at the 3 constructor call sites only"
  artifacts:
    - path: "src/lib/agents/structured-outputs-probe.test.ts"
      provides: "3 independent skipIf-guarded live probes (nousresearch, opencode-zen, opencode-go) round-tripping the real outputSchema via generateText + Output.object with supportsStructuredOutputs forced true at the call site"
      contains: "outputSchema"
    - path: "src/lib/agents/modelFactory.ts"
      provides: "Per-instance supportsStructuredOutputs flip (true where probed-passing, unchanged/false + dated comment where not) and named exports of the 3 raw instances for the probe test to import"
      contains: "supportsStructuredOutputs"
  key_links:
    - from: "src/lib/agents/structured-outputs-probe.test.ts"
      to: "src/lib/agents/modelFactory.ts"
      via: "import { nousresearch, openaiCompatibleZen, openaiCompatibleGo } from './modelFactory'"
      pattern: "openaiCompatibleZen"
    - from: "src/lib/agents/structured-outputs-probe.test.ts"
      to: "src/lib/agents/types.ts"
      via: "import { outputSchema } from './types'"
      pattern: "outputSchema"
    - from: "src/lib/agents/modelFactory.ts"
      to: "@ai-sdk/openai-compatible"
      via: "createOpenAICompatible({ ..., supportsStructuredOutputs: true })"
      pattern: "supportsStructuredOutputs"
---

<objective>
Complete RUN-06's deferred live-probe flip (per D-27-05/06/07/08, folded into VER-05 by the roadmap's own phrasing): prove, per-instance, whether each of the 3 new openai-compatible provider instances (NousResearch, OpenCode Zen, OpenCode Go) genuinely honors `json_schema`-mode structured output against the REAL production schema, then flip `supportsStructuredOutputs` to `true` at each instance's constructor call site ONLY where its own probe passed.

Purpose: Phase 25 deliberately shipped these 3 instances with `supportsStructuredOutputs` unset (false — safe `json_object` fallback) because nobody had proven live whether the actual providers accept `response_format: json_schema`. This is the one genuinely-new production-code behavioral change this phase makes; everything else in the phase is test-only or client-bugfix.
Output: A new live-probe test file + a per-instance flag flip in `modelFactory.ts`, gated strictly on real probe results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-verification-gate/27-CONTEXT.md
@.planning/phases/27-verification-gate/27-RESEARCH.md
@.planning/phases/27-verification-gate/27-PATTERNS.md

@src/lib/agents/modelFactory.ts
@src/lib/agents/runAgent.ts
@src/lib/agents/types.ts
@src/lib/agents/openrouter-only-chain.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the live structuredOutputs probe + export the 3 raw instances</name>
  <files>src/lib/agents/structured-outputs-probe.test.ts, src/lib/agents/modelFactory.ts</files>
  <read_first>
    src/lib/agents/modelFactory.ts (lines 34-48 — the 3 `const` declarations for nousresearch/openaiCompatibleZen/openaiCompatibleGo; these need to become `export const`)
    src/lib/agents/runAgent.ts (line 74 — the exact production call site: `output: Output.object({ schema: outputSchema })`, and line 1's import of `Output`/`generateText` from `'ai'`)
    src/lib/agents/types.ts (lines 51-56 — `outputSchema`, the real schema to round-trip; it requires `proposals` (array, min 0), `keyUncertainties` (string array), `evidenceAppendix` (array))
    node_modules/@ai-sdk/openai-compatible/dist/index.d.ts (line ~73-103 — `OpenAICompatibleChatConfig.supportsStructuredOutputs?: boolean`; the provider's `.languageModel(modelId, config?: Partial<OpenAICompatibleChatConfig>)` method is how to force `supportsStructuredOutputs: true` at call time WITHOUT touching the constructor yet, for probing)
    src/lib/agents/openrouter-only-chain.test.ts (dotenv/skip-guard shape to reuse)
  </read_first>
  <action>
    In `src/lib/agents/modelFactory.ts`, change the 3 relevant `const` declarations (`nousresearch`, `openaiCompatibleZen`, `openaiCompatibleGo`) to `export const` — no other change in this task. Leave `anthropicZen`/`anthropicGo`/`openrouter` as plain `const` (they are out of scope for this flip: `createAnthropic` has no `supportsStructuredOutputs` option, and OpenRouter's structured-output flag is a different SDK shape already handled elsewhere).

    Create `src/lib/agents/structured-outputs-probe.test.ts` (D-27-07: a Vitest live-key test, consistent with the `openrouter-only-chain.test.ts` precedent — not a standalone manual script; runs via `npm test`). Load `.env.local` via `dotenv`'s `config({ path: '.env.local' })` at the top (same convention as `openrouter-only-chain.test.ts`). Import `generateText`, `Output` from `'ai'`; `outputSchema` from `'./types'`; `nousresearch`, `openaiCompatibleZen`, `openaiCompatibleGo` from `'./modelFactory'`.

    Define 3 independent probe cases, each `describe.skipIf(!process.env.<KEY>)` where `<KEY>` is `NOUSRESEARCH_API_KEY` for nousresearch and `OPENCODE_API_KEY` for both opencode-zen and opencode-go (per-instance skip guard, D-27-06 — never all-or-nothing):
    - nousresearch: model id `'nousresearch/hermes-4-70b'` (the raw catalog id, verbatim — matches `NOUSRESEARCH_DEFAULT_MODEL_ID`)
    - opencode-zen: model id `'deepseek-v4-flash'` (a real, committed-snapshot, npm-gated-servable Zen row)
    - opencode-go: model id `'hy3'` (a real, committed-snapshot, Go-EXCLUSIVE npm-gated-servable row — distinct from the Zen probe id so the Go endpoint is genuinely exercised, not just re-hit via a dual-listed id)

    For each case, build the model via `<instance>.languageModel(<modelId>, { supportsStructuredOutputs: true })` — this forces the per-call override WITHOUT yet touching the constructor, exactly proving whether the live endpoint honors `json_schema` mode before any permanent flip. Call `generateText({ model, prompt: <a short instruction to return an empty/minimal analysis: no proposals, no key uncertainties, no evidence appendix entries>, output: Output.object({ schema: outputSchema }) })` (D-27-08: the REAL production schema from `runAgent.ts`'s `Output.object` call — not a toy/minimal schema, proving the actual production path) with `{ timeout: 60_000 }` at the `it()` level. Assert the call does not throw and that `result.output` (NOT `experimental_output` — verify against how `runAgent.ts`'s own `Object.assign(Object.create(Object.getPrototypeOf(result)), result, ...)` pattern accesses it, since `output`/`usage`/`finishReason` are prototype getters) parses against `outputSchema` via `outputSchema.safeParse(result.output).success === true`.

    Do NOT touch `instantiateModel`'s dispatch logic (modelFactory.ts lines ~96-140) in this task — only the export-keyword addition on the 3 consts.
  </action>
  <acceptance_criteria>
    - `grep -c "^export const nousresearch\|^export const openaiCompatibleZen\|^export const openaiCompatibleGo" src/lib/agents/modelFactory.ts` equals 3
    - `src/lib/agents/structured-outputs-probe.test.ts` contains exactly 3 `describe.skipIf` blocks, each independently keyed on its own env var (not a single shared `hasLiveKeys` covering all 3)
    - The file imports `outputSchema` from `./types` (the real production schema, not a locally-redefined toy schema)
    - `npx vitest run src/lib/agents/structured-outputs-probe.test.ts` runs and reports real pass/fail/skip per instance (captured for Task 2's decision)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/agents/structured-outputs-probe.test.ts</automated>
  </verify>
  <done>The probe test exists with 3 independently-gated live cases against the real outputSchema, and the 3 raw instances are exported from modelFactory.ts for it to import.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the per-instance supportsStructuredOutputs flip based on real probe results</name>
  <files>src/lib/agents/modelFactory.ts</files>
  <read_first>
    src/lib/agents/modelFactory.ts (the 3 constructor call sites, post-Task-1 export change)
  </read_first>
  <action>
    Using Task 1's actual test run results (not assumptions), for each of the 3 instances independently: if its probe PASSED, add `supportsStructuredOutputs: true,` to that instance's `createOpenAICompatible({ ... })` constructor call with an inline comment `// RUN-06: flipped after a passing live json_schema probe (Phase 27, structured-outputs-probe.test.ts)`. If its probe FAILED or SKIPPED (key absent locally), leave the constructor unchanged (flag stays implicitly `false`) and add a comment noting the outcome, e.g. `// RUN-06: probed 2026-08-04, <provider> did not accept json_schema mode — stays false (json_object fallback); re-probe after upstream API changes` or `// RUN-06: probe skipped (no local key) — stays false until re-probed`.

    Do not flip all 3 together if only some passed — D-27-06 requires independent per-instance decisions. Do not touch `instantiateModel`'s dispatch function, `anthropicZen`/`anthropicGo`, or `openrouter` in this task.
  </action>
  <acceptance_criteria>
    - Each of the 3 instances' `supportsStructuredOutputs` state in `modelFactory.ts` matches its own probe's real recorded outcome from Task 1 (verifiable by comparing the constructor literal against the captured Task 1 test output)
    - `npx tsc --noEmit` exits 0 (no type errors introduced)
    - `npm test` (full suite) shows no new failures beyond the already-documented pre-existing OpenRouter billing failure
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/agents/modelFactory.test.ts 2>/dev/null; npx tsc --noEmit</automated>
  </verify>
  <done>Each of the 3 new instances' supportsStructuredOutputs flag accurately reflects its own live probe outcome, independently, with a dated comment recording the decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vitest probe → live NousResearch/OpenCode Zen/Go chat-completion APIs | Real HTTPS calls with real keys, forcing a specific response-format mode — genuine external network trust boundary |
| Probe test → production runAgent.ts schema | The probe deliberately reuses the REAL `outputSchema`, not a toy schema — must not silently diverge if `types.ts` changes later |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | Information Disclosure | `structured-outputs-probe.test.ts` | mitigate | The probe never logs the raw provider response body or key material — assertions operate on the parsed/validated shape only |
| T-27-05 | Tampering (capability-flag drift) | `modelFactory.ts` constructor flags | mitigate | Flip is gated strictly on a REAL, per-instance passing probe result recorded this session — never a blanket/assumed flip; a dated comment records the evidence for future audits |
| T-27-06 | Denial of Service (silent regression) | Future upstream API change silently stops honoring `json_schema` after this flip ships `true` | accept | Out of this phase's control (external API behavior); the existing `Output.object` JSON-mode fallback + client-side validation in `runAgent.ts`/`analyzeCompany.ts`'s gate (`validateRunArtifacts`) already fails closed on malformed output, so a future regression degrades to a gate failure, not a security issue |
</threat_model>

<verification>
Run `npx vitest run src/lib/agents/structured-outputs-probe.test.ts` and record the real pass/fail/skip per instance. Run `npx tsc --noEmit` and `npm test` after the flag flip to confirm no regressions.
</verification>

<success_criteria>
Each of the 3 new instances' `supportsStructuredOutputs` flag is set strictly according to its own live probe outcome, independently, with the decision documented inline for Plan 27-06's VERIFICATION.md to cite.
</success_criteria>

<output>
Create `.planning/phases/27-verification-gate/27-02-SUMMARY.md` when done
</output>
