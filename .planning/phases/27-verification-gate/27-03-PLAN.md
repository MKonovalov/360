---
phase: 27-verification-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/verification/security-grep.test.ts
autonomous: true
requirements: [VER-04]
must_haves:
  truths:
    - "The security-matrix grep scans for NOUSRESEARCH and OPENCODE key-name tokens with the same rigor already applied to OPENROUTER — client components, Server Actions, NEXT_PUBLIC_* leakage, .env.example"
    - "modelFactory.ts remains the sole exempted non-test server file for all 3 new-provider tokens (it already reads process.env.NOUSRESEARCH_API_KEY / process.env.OPENCODE_API_KEY directly, same exemption rationale as OPENROUTER)"
    - "The gate is non-vacuous for both new tokens — a canary proves the ALLOWED files genuinely contain NOUSRESEARCH_API_KEY / OPENCODE_API_KEY, so a future token rename cannot silently disable the gate"
    - "The SERVER_COMPONENT exemption set is left unchanged (company-detail.tsx only ever mentions OPENROUTER, never NOUSRESEARCH/OPENCODE) — no false canary requirement is added for tokens that file genuinely does not contain"
  artifacts:
    - path: "src/lib/verification/security-grep.test.ts"
      provides: "Widened VER-04 gate scanning OPENROUTER + NOUSRESEARCH + OPENCODE, non-vacuous for all 3"
      contains: "NOUSRESEARCH"
  key_links:
    - from: "src/lib/verification/security-grep.test.ts"
      to: "src/lib/agents/modelFactory.ts"
      via: "ALLOWED-set canary assertion"
      pattern: "NOUSRESEARCH_API_KEY"
    - from: "src/lib/verification/security-grep.test.ts"
      to: ".env.example"
      via: "readFileSync + toContain assertion"
      pattern: "OPENCODE_API_KEY"
---

<objective>
Widen the VER-04 security-matrix grep (the phase's primary ASVS V13/V14 control) to cover the two new provider key names — `NOUSRESEARCH` and `OPENCODE` — with the exact same rigor already proven for `OPENROUTER`: absent from client components, Server Action returns, and `NEXT_PUBLIC_*` anywhere; present (as `_API_KEY` suffixed literals) only in the already-allowlisted server files; non-vacuous canaries for both new tokens.

Purpose: this test is the ONE permanent, automated proof against server-only API keys leaking into the client bundle for these 2 new providers. Per D-27-13, this is additive-only — no rewrite, no new `it()` blocks required if a data-driven loop is chosen instead.
Output: `src/lib/verification/security-grep.test.ts`, extended, still green under `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-verification-gate/27-CONTEXT.md
@.planning/phases/27-verification-gate/27-RESEARCH.md

@src/lib/verification/security-grep.test.ts
@src/lib/agents/modelFactory.ts
@src/lib/agents/analyzeCompany.ts
@src/lib/env.ts
@.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the security-grep gate to NOUSRESEARCH + OPENCODE</name>
  <files>src/lib/verification/security-grep.test.ts</files>
  <read_first>
    src/lib/verification/security-grep.test.ts (the full 81-line file — the ALLOWED set, SERVER_COMPONENT set, the walk() helper, and all 5 existing it() blocks that currently hard-code the literal string 'OPENROUTER')
    src/lib/agents/modelFactory.ts (lines 34-56 — confirms process.env.NOUSRESEARCH_API_KEY and process.env.OPENCODE_API_KEY appear as literal strings directly in this file, same exemption class as OPENROUTER_API_KEY)
    src/lib/agents/analyzeCompany.ts (lines 57-68 — missingProviderKey — confirms this file also contains the literal strings 'NOUSRESEARCH_API_KEY' / 'OPENCODE_API_KEY')
    .env.example (lines 39-41 — confirms OPENROUTER_API_KEY/NOUSRESEARCH_API_KEY/OPENCODE_API_KEY are all already declared)
    src/components/companies/company-detail.tsx (line 74 — confirms this SERVER_COMPONENT-exempted file mentions ONLY env.OPENROUTER_API_KEY, never NOUSRESEARCH/OPENCODE — the SERVER_COMPONENT canary must NOT be widened to require tokens this file does not genuinely contain)
  </read_first>
  <action>
    Refactor the 5 existing `it()` blocks (or add parallel assertions within them — either is acceptable, planner's discretion per D-27-13) so the "no leakage" scan and the ".env.example presence" scan run for THREE tokens: `'OPENROUTER'`, `'NOUSRESEARCH'`, `'OPENCODE'`. Concretely:
    - The "no `<TOKEN>` in client components" test must fail if ANY of the 3 tokens appears in a `'use client'` file or a `src/components/` file (except the exempted `SERVER_COMPONENT` set, unchanged).
    - The "no `<TOKEN>` in Server Actions" test must check all 3 tokens against every `app/actions/` file.
    - The "no `NEXT_PUBLIC_<TOKEN>` anywhere" test must check `NEXT_PUBLIC_OPENROUTER`, `NEXT_PUBLIC_NOUSRESEARCH`, `NEXT_PUBLIC_OPENCODE` — all absent from `src/` and `.env.example`; assert `.env.example` DOES contain all 3 of `OPENROUTER_API_KEY`, `NOUSRESEARCH_API_KEY`, `OPENCODE_API_KEY`.
    - The ALLOWED-set canary (non-vacuous proof) must assert EACH of the 3 allowlisted files (`lib/env.ts`, `lib/agents/modelFactory.ts`, `lib/agents/analyzeCompany.ts`) contains ALL 3 of `OPENROUTER_API_KEY`, `NOUSRESEARCH_API_KEY`, `OPENCODE_API_KEY` as literal strings — this must genuinely pass given the confirmed content of those 3 files (env.ts declares all 3 in its zod schema; modelFactory.ts reads all 3 via process.env; analyzeCompany.ts's `missingProviderKey` checks all 3).
    - Leave the SERVER_COMPONENT canary test UNCHANGED (scoped to `OPENROUTER_API_KEY` only) — `company-detail.tsx` genuinely does not mention NousResearch or OpenCode, and adding a false requirement there would break a currently-correct test.
    Prefer expressing the 3-token scan as a data-driven `const TOKENS = ['OPENROUTER', 'NOUSRESEARCH', 'OPENCODE'] as const;` loop inside each relevant `it()` body (matches this codebase's existing "data-driven loops, never a hardcoded branch" convention used in `modelConfig.test.ts`'s 16-cell matrix) rather than 3 separate near-duplicate `it()` blocks — but duplicating the literal checks is also acceptable if it keeps the diff smaller; either satisfies D-27-13.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/verification/security-grep.test.ts` passes, all assertions green
    - The test file contains the literal substrings `'NOUSRESEARCH'` and `'OPENCODE'` in at least one scan assertion each (not just in a comment)
    - The ALLOWED-set canary test asserts `toContain('NOUSRESEARCH_API_KEY')` and `toContain('OPENCODE_API_KEY')` for the 3 allowlisted files, in addition to the existing `OPENROUTER_API_KEY` assertion
    - The SERVER_COMPONENT canary test's assertions remain scoped to `OPENROUTER_API_KEY` only (no new false requirement added for a token `company-detail.tsx` does not contain)
    - `npm test` full suite shows no new failures beyond the documented pre-existing OpenRouter billing failure
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/verification/security-grep.test.ts</automated>
  </verify>
  <done>The security-matrix grep scans NOUSRESEARCH and OPENCODE with the same non-vacuous rigor as OPENROUTER; the SERVER_COMPONENT exemption stays correctly scoped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-only source (`src/lib/`, `src/app/actions/`) → client bundle (`'use client'`, `src/components/`) | The exact boundary this gate polices — a server-only key NAME crossing into client-reachable source is the failure mode |
| Source tree → `.env.example` | Public documentation of required env vars must never imply a client-exposed (`NEXT_PUBLIC_*`) key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | Information Disclosure | `NOUSRESEARCH_API_KEY` / `OPENCODE_API_KEY` leaking into a client bundle or Server Action return value | mitigate | Widened `security-grep.test.ts` scan (this plan) — the same source-level string-match proof already used for `OPENROUTER`, now covering both new tokens |
| T-27-08 | Information Disclosure (gate rot) | A future token-name rename silently disabling the scan without failing loudly | mitigate | Non-vacuous canary requiring the 3 allowlisted files to genuinely contain both new tokens — a rename breaks the canary immediately, not silently |
| T-27-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan — Package Legitimacy Gate does not trigger |
</threat_model>

<verification>
`npx vitest run src/lib/verification/security-grep.test.ts` must pass; `npm test` full suite must show no new failures beyond the documented pre-existing OpenRouter billing failure.
</verification>

<success_criteria>
The security-matrix grep genuinely, non-vacuously proves no `NOUSRESEARCH`/`OPENCODE` key-name leakage to client-reachable code, matching the existing `OPENROUTER` proof's rigor exactly.
</success_criteria>

<output>
Create `.planning/phases/27-verification-gate/27-03-SUMMARY.md` when done
</output>
