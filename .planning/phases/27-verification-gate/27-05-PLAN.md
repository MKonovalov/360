---
phase: 27-verification-gate
plan: 05
type: execute
wave: 2
depends_on: ["27-04"]
files_modified:
  - e2e/ver-05-settings.spec.ts
autonomous: true
requirements: [VER-05]
must_haves:
  truths:
    - "The full 4-provider Select -> Picker -> Save round trip is proven live in a real browser with real Clerk auth (closes 26-HUMAN-UAT.md item 1)"
    - "OpenCode Zen/Go endpoint captions ('· Zen' / '· Go') render in BOTH the provider-scoped primary picker and the union fallback picker, and in the saved-chain recap (closes 26-HUMAN-UAT.md item 2)"
    - "The reset-hint copy for the claude-sonnet-4-6 collision case correctly states it 'stays routed through Anthropic', never a false 'now serves via OpenCode' claim (closes 26-HUMAN-UAT.md item 3)"
    - "The primary trigger badge correctly disambiguates BOTH verified real collision ids: claude-sonnet-4-6 reads 'Anthropic' (never 'OpenCode') when the AI Provider dropdown is set to OpenCode; nousresearch/hermes-4-70b reads 'NousResearch' (never 'OpenRouter') when the dropdown is set to OpenRouter (closes 26-HUMAN-UAT.md item 4)"
    - "NousResearch Hermes rows render the 'chat/reasoning-tuned' capability caption plus a per-MTok cost caption in the picker"
    - "A mid-save draft edit (CR-01's fixed race) no longer shows a false 'Saved.' confirmation over the edited value"
  artifacts:
    - path: "e2e/ver-05-settings.spec.ts"
      provides: "Extended with 3+ new test blocks covering the 4-provider selector, Zen/Go + Hermes captions, badge disambiguation, and the CR-01 save-race regression"
      contains: "NousResearch"
  key_links:
    - from: "e2e/ver-05-settings.spec.ts"
      to: "src/components/settings/model-settings-form.tsx"
      via: "page.getByLabel('AI provider') / page.getByLabel('Primary model') interactions"
      pattern: "AI provider"
    - from: "e2e/ver-05-settings.spec.ts"
      to: "src/components/settings/model-picker-logic.ts"
      via: "assertions on rendered endpointLabel/hermesCaptionLabel/resolveBadgeProvider output"
      pattern: "Zen"
---

<objective>
Extend the existing `e2e/ver-05-settings.spec.ts` Playwright spec (real Clerk test account, already provisioned) to prove the 4-provider selector, Zen/Go endpoint captions, Hermes capability captions, and badge disambiguation across all 4 providers — becoming a permanent automated regression gate rather than one-off manual verification (D-27-09). The 4 new/extended assertions map 1:1 to Phase 26's 4 pending `26-HUMAN-UAT.md` items (D-27-10); Plan 27-06 marks that file resolved once this plan's tests pass.

Purpose: this is VER-05's core deliverable — replacing 4 deferred manual checks with permanent automated coverage, and exercising Plan 27-04's CR-01/CR-02 fixes live.
Output: `e2e/ver-05-settings.spec.ts`, extended, green under `npx playwright test e2e/ver-05-settings.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-verification-gate/27-CONTEXT.md
@.planning/phases/27-verification-gate/27-RESEARCH.md
@.planning/phases/27-verification-gate/27-PATTERNS.md
@.planning/phases/26-settings-ui/26-HUMAN-UAT.md
@.planning/phases/26-settings-ui/26-VERIFICATION.md

@e2e/ver-05-settings.spec.ts
@src/components/settings/model-picker-logic.ts
@src/components/settings/model-picker.tsx
@src/components/settings/model-settings-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen setProvider + full 4-provider round trip + reset-hint/badge collision for claude-sonnet-4-6</name>
  <files>e2e/ver-05-settings.spec.ts</files>
  <read_first>
    e2e/ver-05-settings.spec.ts (full file — the setProvider/clearFallbacks helpers at lines 46-68, and the existing SET-03/SET-05 test bodies at lines 70-205 to mirror the interaction style)
    .planning/phases/26-settings-ui/26-HUMAN-UAT.md (items 1 and 3's exact expected text)
    .planning/phases/26-settings-ui/26-VERIFICATION.md (frontmatter human_verification block — the EXACT expected hint copy for item 3: "Claude Sonnet 4.6 stays routed through Anthropic — OpenCode's copy isn't used while a higher-priority provider serves the same id.")
    src/components/settings/model-settings-form.tsx (lines 158-197 — handleProviderChange, confirms selecting OpenCode while primary is claude-sonnet-4-6 triggers BOTH the reset-hint AND badge scenarios simultaneously, since claude-sonnet-4-6 is npm-gated servable under opencode too)
  </read_first>
  <action>
    Widen the `setProvider` helper's `target` parameter type from `'Anthropic' | 'OpenRouter'` to `'Anthropic' | 'OpenRouter' | 'NousResearch' | 'OpenCode'`.

    Add a new test `'VER-05: full 4-provider selector -> picker -> save round trip (closes 26-HUMAN-UAT item 1)'`: navigate to `/settings`, `clearFallbacks`, then in sequence switch the AI Provider dropdown through all 4 entries (Anthropic, OpenRouter, NousResearch, OpenCode) using `setProvider`, asserting after each switch that `page.getByLabel('Primary model')` re-renders with SOME non-empty option (the provider-scoped default) and the dropdown itself shows the correct provider name. End on OpenCode, save (`page.getByRole('button', { name: 'Save changes' }).click()`), and assert `page.getByText('Saved.')` becomes visible.

    Add a new test `'VER-05: reset-hint + trigger badge accuracy for the claude-sonnet-4-6 collision (closes 26-HUMAN-UAT items 3+4a)'`: set provider to Anthropic, confirm primary is `claude-sonnet-4-6` (the default), then switch provider to OpenCode via `setProvider`. Assert the reset hint text EXACTLY matches (or `toContainText`) `"Claude Sonnet 4.6 stays routed through Anthropic"` — never a claim that OpenCode now serves it. Assert `page.getByLabel('Primary model')`'s badge (`[data-slot="badge"]` inside the trigger) reads `'Anthropic'`, never `'OpenCode'`.
  </action>
  <acceptance_criteria>
    - `setProvider`'s type signature includes all 4 provider names
    - The new round-trip test exercises all 4 providers in one flow and ends with a visible "Saved." confirmation
    - The new collision test asserts the reset-hint text contains "stays routed through Anthropic" and the primary trigger badge reads "Anthropic" while the AI Provider dropdown shows "OpenCode"
    - `npx playwright test e2e/ver-05-settings.spec.ts -g "closes 26-HUMAN-UAT item 1|closes 26-HUMAN-UAT items 3"` passes
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/ver-05-settings.spec.ts -g "26-HUMAN-UAT"</automated>
  </verify>
  <done>The 4-provider round trip and the claude-sonnet-4-6 reset-hint/badge collision are proven live, closing HUMAN-UAT items 1 and 3.</done>
</task>

<task type="auto">
  <name>Task 2: OpenCode Zen/Go endpoint captions + Hermes capability captions</name>
  <files>e2e/ver-05-settings.spec.ts</files>
  <read_first>
    src/components/settings/model-picker-logic.ts (endpointLabel, hermesCaptionLabel, rowCaption — the exact caption strings: 'Zen', 'Go', 'chat/reasoning-tuned')
    src/components/settings/model-picker.tsx (lines 172-187 — confirms rowCaption renders inline after the model name, cost caption renders as "· $X / $Y per MTok" unconditionally)
    .planning/phases/26-settings-ui/26-HUMAN-UAT.md (item 2's exact expected text)
  </read_first>
  <action>
    Add a new test `'VER-05: OpenCode Zen/Go endpoint captions in primary + fallback pickers + saved-chain recap (closes 26-HUMAN-UAT item 2)'`: set provider to OpenCode, open the primary picker, search for `'big-pickle'` (a real Zen-endpoint servable id) and assert its option row contains the text `'Zen'`; clear search, search for `'hy3'` (a real Go-EXCLUSIVE servable id) and assert its option row contains the text `'Go'`. Select `'big-pickle'` as primary. Add a fallback, open the union fallback picker, search for `'hy3'`, assert its option row ALSO contains `'Go'` (same caption slot, both picker modes). Select it as the fallback. Save, then assert the "Saved chain:" recap text contains both `'Zen'` and `'Go'`.

    Add a new test `'VER-05: NousResearch Hermes capability + cost captions (SET-04)'`: set provider to NousResearch, open the primary picker (or inspect the default-selected row's visible text if the picker doesn't need opening), assert the visible row/trigger area for a hermes id contains `'chat/reasoning-tuned'` and a `'per MTok'` cost caption substring (do not assert the exact dollar figure — it can drift with catalog refreshes; assert the pattern via `toContainText(/\$[\d.]+ \/ \$[\d.]+ per MTok/)` or equivalent substring match).
  </action>
  <acceptance_criteria>
    - The endpoint-caption test asserts 'Zen' appears for `big-pickle` and 'Go' appears for `hy3` in BOTH the primary picker and the union fallback picker
    - The saved-chain recap assertion checks for both 'Zen' and 'Go' text after a save spanning both endpoints
    - The Hermes caption test asserts 'chat/reasoning-tuned' text is visible and a per-MTok cost pattern is present, without hard-coding a specific dollar amount
    - `npx playwright test e2e/ver-05-settings.spec.ts -g "26-HUMAN-UAT item 2|SET-04"` passes
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/ver-05-settings.spec.ts -g "item 2|SET-04"</automated>
  </verify>
  <done>OpenCode Zen/Go endpoint captions and NousResearch Hermes capability/cost captions are proven live in both picker modes and the saved-chain recap, closing HUMAN-UAT item 2.</done>
</task>

<task type="auto">
  <name>Task 3: hermes-4-70b badge collision + CR-01 mid-save-edit regression</name>
  <files>e2e/ver-05-settings.spec.ts</files>
  <read_first>
    e2e/ver-05-settings.spec.ts (lines 153-205 — the existing SET-05 badge-disambiguation test's exact `[data-slot="badge"]`-scoped locator pattern to mirror)
    .planning/phases/26-settings-ui/26-HUMAN-UAT.md (item 4's exact expected text, part b)
    src/components/settings/model-settings-form.tsx (post-Plan-27-04 fix — confirms the CR-01 gate now requires lastSaved draft-equality before rendering "Saved.")
  </read_first>
  <action>
    Add a new test `'VER-05: hermes-4-70b trigger badge accuracy under OpenRouter (closes 26-HUMAN-UAT item 4b)'`: set provider to NousResearch, set primary to `'nousresearch/hermes-4-70b'` (the default), then switch provider to OpenRouter via `setProvider`. Because `hermes-4-70b` is ALSO servable as an active OpenRouter mirror row with the identical id string, keep-if-valid should preserve it verbatim. Assert the primary trigger's `[data-slot="badge"]` text reads `'NousResearch'`, never `'OpenRouter'`, confirming `resolveBadgeProvider`'s precedence-resolved union lookup wins over the raw dropdown value.

    Add a new test `'VER-05: CR-01 mid-save-edit no longer shows a false Saved. confirmation'`: this is a browser-level approximation of the race Plan 27-04 fixed (Playwright cannot literally suspend mid-request the way a unit test could, but it CAN prove the render-gate logic is correct): stage a primary selection, click Save, and — WITHOUT waiting for the "Saved." text — immediately change the primary to a different value. Assert that once the page settles, "Saved." either does not appear at all, OR if it does appear, the "Saved chain:" recap immediately below it names the CURRENT (edited) primary, never the value that was in flight at click time (i.e., the fix's `lastSaved`-equality gate is what's actually rendering, not a stale unconditional confirmation). If Playwright's timing makes the true race window unreachable, assert instead that saving twice in a row (change primary -> save -> wait for "Saved." -> change primary again -> before its own "Saved." appears, assert the OLD "Saved." text is no longer visible) demonstrates the gate transitions correctly on each edit.
  </action>
  <acceptance_criteria>
    - The hermes-4-70b test asserts the primary trigger badge reads 'NousResearch' while the AI Provider dropdown shows 'OpenRouter'
    - The CR-01 regression test demonstrates the "Saved." confirmation is never shown for a primary value that differs from the currently-staged draft
    - `npx playwright test e2e/ver-05-settings.spec.ts` (full file) passes end-to-end
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test e2e/ver-05-settings.spec.ts</automated>
  </verify>
  <done>Both real collision ids (claude-sonnet-4-6, hermes-4-70b) prove badge accuracy under a mismatched dropdown provider, closing HUMAN-UAT item 4 fully, and a browser-level CR-01 regression check exercises Plan 27-04's fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Real Clerk-authenticated browser session -> /settings SSR page -> saveSettingsAction | The full authenticated user-facing path this spec drives end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-11 | Spoofing | Playwright auth | accept | Reuses the existing real-Clerk `storageState` from `e2e/auth.setup.ts` (Phase 22 precedent) — no cookie injection, no new auth surface introduced |
| T-27-12 | Repudiation-adjacent (data-trust) | Save confirmation accuracy | mitigate | The CR-01 regression test (Task 3) is the first automated browser-level proof that the "Saved." message never lies about what was actually persisted |
</threat_model>

<verification>
`npx playwright test e2e/ver-05-settings.spec.ts` (full file, all tests) must pass. This closes all 4 of `26-HUMAN-UAT.md`'s pending items.
</verification>

<success_criteria>
All 4 of `26-HUMAN-UAT.md`'s pending items have a corresponding, passing, automated Playwright assertion; Plan 27-06 can mark that file resolved citing these exact test names.
</success_criteria>

<output>
Create `.planning/phases/27-verification-gate/27-05-SUMMARY.md` when done
</output>
