---
phase: 27-verification-gate
plan: 06
type: execute
wave: 3
depends_on: ["27-01", "27-02", "27-03", "27-04", "27-05"]
files_modified:
  - .planning/phases/27-verification-gate/27-VERIFICATION.md
  - .planning/phases/26-settings-ui/26-HUMAN-UAT.md
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05]
must_haves:
  truths:
    - "27-VERIFICATION.md records all 5 roadmap success criteria with evidence — test commands, real pass/fail/skip results, and DB/live-run shapes (never key values)"
    - "VER-01's audit confirms the existing 4-provider collision matrix (catalog.test.ts) and 16-cell 429 hop matrix (modelConfig.test.ts) already fully cover the 4-provider set, per D-27-12 — documented as audit-confirmed, not rewritten"
    - "The OpenRouter billing pending-credit finding (D-27-02) is documented plainly as a non-regression, matching Phase 22's own disposition of the identical condition — never silenced, never force-fixed"
    - "26-HUMAN-UAT.md's 4 pending items are marked resolved, each citing the specific Plan 27-05 Playwright test name that proves it"
    - "The full test suite (npm test) and the full Playwright suite (npx playwright test) are actually run this task, and their real output (pass/fail counts) is what gets recorded — never assumed"
  artifacts:
    - path: ".planning/phases/27-verification-gate/27-VERIFICATION.md"
      provides: "Phase 27 proof record: 5/5 success criteria with evidence, matching the 19/20/21/22 VERIFICATION.md conventions"
      contains: "VER-02"
    - path: ".planning/phases/26-settings-ui/26-HUMAN-UAT.md"
      provides: "All 4 items marked resolved with evidence citations"
      contains: "resolved"
  key_links:
    - from: "27-VERIFICATION.md"
      to: "src/lib/agents/nousresearch-only-chain.test.ts"
      via: "VER-02/03 evidence citation"
      pattern: "nousresearch-only-chain"
    - from: "27-VERIFICATION.md"
      to: "e2e/ver-05-settings.spec.ts"
      via: "VER-05 evidence citation"
      pattern: "ver-05-settings"
    - from: "26-HUMAN-UAT.md"
      to: "e2e/ver-05-settings.spec.ts"
      via: "resolved-item test-name citation"
      pattern: "closes 26-HUMAN-UAT"
---

<objective>
Consolidate every proof this phase produced into `27-VERIFICATION.md` (mapping all 5 roadmap success criteria to real, executed evidence — mirroring the 19/20/21/22 VERIFICATION.md conventions per D-27-12), audit VER-01's matrices (confirming completeness rather than rewriting, per D-27-12/13), document the OpenRouter billing non-regression finding (D-27-02), and mark `26-settings-ui/26-HUMAN-UAT.md`'s 4 pending items resolved (D-27-10) now that Plan 27-05's Playwright assertions prove them.

Purpose: this is the milestone's final verification-gate deliverable — it closes v1.5. The phase's proofs mean nothing unless recorded durably with real evidence anyone can re-run.
Output: `27-VERIFICATION.md` (5/5 success criteria with evidence) + `26-HUMAN-UAT.md` (4/4 items resolved).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/27-verification-gate/27-CONTEXT.md
@.planning/phases/27-verification-gate/27-RESEARCH.md
@.planning/milestones/v1.4-phases/22-verification-gate/22-VERIFICATION.md
@.planning/phases/26-settings-ui/26-HUMAN-UAT.md
@.planning/phases/26-settings-ui/26-VERIFICATION.md

@src/lib/models/catalog.test.ts
@src/lib/agents/modelConfig.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit VER-01's matrices, run the full suite, and write 27-VERIFICATION.md</name>
  <files>.planning/phases/27-verification-gate/27-VERIFICATION.md</files>
  <read_first>
    src/lib/models/catalog.test.ts (lines 322-431 — the getProviderForModelId collision canaries; lines 433-509 — COUNT-STABILITY/NO-FLIP; confirms all 4 providers already covered)
    src/lib/agents/modelConfig.test.ts (lines 156-198 — the 16-cell shouldAdvance matrix, already SERVABLE_PROVIDERS-driven across all 4 providers)
    .planning/milestones/v1.4-phases/22-verification-gate/22-VERIFICATION.md (the format/structure to mirror: frontmatter + Goal Achievement truth table + evidence citations)
    .planning/phases/26-settings-ui/26-VERIFICATION.md (the OpenRouter billing failure note format already used once)
  </read_first>
  <action>
    Run the full automated verification surface for real and capture actual output (do not assume results):
    - `npm test` (full Vitest suite) — record the real pass/fail/skip count, and specifically confirm `src/lib/agents/openrouter-only-chain.test.ts` still fails with the SAME `billing`/`provider credits exhausted` reason documented in 27-RESEARCH.md (re-confirm it is the identical, still-unresolved pending-credit condition from Phase 22 — not a new regression). If NousResearch or OpenCode's own isolation tests (from Plan 27-01) also hit a billing wall, document that finding the same way — never force a fix, never weaken an assertion.
    - `npx playwright test e2e/ver-05-settings.spec.ts` — record the real pass count for all tests, including the 3 new/extended test blocks from Plan 27-05.
    - `npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelConfig.test.ts` — confirm and cite the specific existing test names that already lock the 4-provider collision matrix (`COLLISION CANARY`, hermes precedence, opencode dual-listed no-flip) and the 16-cell `shouldAdvance` matrix — VER-01 audit, no new test file needed per D-27-12.

    Write `.planning/phases/27-verification-gate/27-VERIFICATION.md` with frontmatter (`phase: 27-verification-gate`, `verified: <ISO timestamp>`, `status:`, `score:`) and a body structured like `22-VERIFICATION.md`: a "Goal Achievement" section with one row per ROADMAP Phase 27 success criterion (1-5), each citing the specific test file/line or live-run evidence that proves it; a "Behavioral Spot-Checks" table with the actual commands run and their actual output this session. Explicitly document:
    - VER-01: audit-confirmed complete, citing the specific existing test names (no rewrite).
    - VER-02/VER-03: the real pass/fail/skip outcome of Plan 27-01's NousResearch/OpenCode probes, and the RUN-06 structuredOutputs probe results from Plan 27-02 (per-instance: which flipped to true, which stayed false and why).
    - VER-03 (OpenRouter re-proof): the confirmed pending-credit disposition, matching Phase 22's own precedent — plain documentation, not a fix.
    - VER-04: the security-grep extension's real pass result.
    - VER-05: the real Playwright pass count, explicitly naming which test closes which `26-HUMAN-UAT.md` item.
  </action>
  <acceptance_criteria>
    - `27-VERIFICATION.md` exists with a Goal Achievement table covering all 5 Phase 27 success criteria from ROADMAP.md
    - Every evidence citation in the document names a real file/line or a real command output captured THIS session (no placeholder or assumed values)
    - The OpenRouter billing finding is documented as a non-regression, cross-referencing 27-RESEARCH.md's investigation
    - Each of the 3 new structuredOutputs probe results (nousresearch, opencode-zen, opencode-go) is individually recorded with its pass/fail/skip outcome
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/27-verification-gate/27-VERIFICATION.md</automated>
  </verify>
  <done>27-VERIFICATION.md exists, records all 5 success criteria with real, freshly-captured evidence, and plainly documents the OpenRouter billing non-regression without weakening any assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Mark 26-HUMAN-UAT.md's 4 items resolved</name>
  <files>.planning/phases/26-settings-ui/26-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/26-settings-ui/26-HUMAN-UAT.md (current frontmatter: status: partial, 4 items all result: [pending])
    e2e/ver-05-settings.spec.ts (post-Plan-27-05 — the exact names of the new/extended test blocks that prove each of the 4 items)
  </read_first>
  <action>
    Update `.planning/phases/26-settings-ui/26-HUMAN-UAT.md`'s frontmatter: `status: partial` → `status: resolved`, bump `updated:` to the current ISO timestamp. For each of the 4 numbered items under `## Tests`, change `result: [pending]` to a line citing the exact Plan 27-05 Playwright test name that proves it and its pass status (e.g., `result: resolved — proven by e2e/ver-05-settings.spec.ts "VER-05: full 4-provider selector -> picker -> save round trip (closes 26-HUMAN-UAT item 1)", passing`). Update the `## Summary` counts (`passed: 4`, `pending: 0`). Leave the `## Current Test` and `## Gaps` sections as-is unless a genuine gap was found during Task 1's audit, in which case note it there rather than silently closing an unproven item.
  </action>
  <acceptance_criteria>
    - Frontmatter `status:` is `resolved`
    - All 4 items under `## Tests` have their `result:` line changed from `[pending]` to a citation of a specific, real, passing Plan 27-05 test name
    - `## Summary`'s `passed`/`pending` counts are updated to reflect 4 resolved / 0 pending
    - No item is marked resolved without a genuinely passing test backing it (if Task 1's audit found any test still failing, that item stays pending with the failure documented, not silently closed)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "status: resolved" .planning/phases/26-settings-ui/26-HUMAN-UAT.md</automated>
  </verify>
  <done>All 4 of Phase 26's deferred HUMAN-UAT items are marked resolved, each citing the specific Plan 27-05 Playwright test that proves it, closing the loop D-27-10 required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| This plan's documentation ↔ the actual executed test evidence | The core integrity concern for a verification-gate closing plan — every claim must trace to a real command run this session, never an assumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-13 | Repudiation (false verification record) | `27-VERIFICATION.md` | mitigate | Every cited pass/fail/skip result comes from a command actually executed this task, never assumed from earlier plans' own SUMMARY claims without re-confirmation |
| T-27-14 | Repudiation (false HUMAN-UAT closure) | `26-HUMAN-UAT.md` | mitigate | An item is marked resolved ONLY if its citing Playwright test genuinely passed this session — a still-failing test keeps its item pending with the failure documented |
</threat_model>

<verification>
`test -f .planning/phases/27-verification-gate/27-VERIFICATION.md` and `grep -c "status: resolved" .planning/phases/26-settings-ui/26-HUMAN-UAT.md` (expect 1). The full `npm test` + `npx playwright test` runs performed in Task 1 are the actual verification substance.
</verification>

<success_criteria>
27-VERIFICATION.md records all 5 Phase 27 success criteria with real evidence; 26-HUMAN-UAT.md shows 4/4 items resolved, each citing a specific passing test. The v1.5 milestone's correctness claims are durably documented as proven end-to-end for all 4 providers.
</success_criteria>

<output>
Create `.planning/phases/27-verification-gate/27-06-SUMMARY.md` when done
</output>
