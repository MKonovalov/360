---
phase: 27-verification-gate
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/settings/model-settings-form.tsx
autonomous: true
requirements: [VER-05]
must_haves:
  truths:
    - "A save-in-flight edit (user changes the draft while a prior Save request is still pending) can no longer produce a false 'Saved.' confirmation over the unsaved edit (CR-01 fixed)"
    - "A rejected/failed Server Action transport call (not an application-level failure, a genuine promise rejection) degrades to the existing error UI state instead of stranding the form on 'Saving…' forever (CR-02 fixed)"
    - "markDirty()'s 'saving'-status exemption (the WR-01 fix it protects) is untouched by either fix"
    - "src/app/actions/settings.ts is untouched — its validated order (requireStaffAccess -> zod -> union servable check -> dedupe -> upsert) stays exactly as-is"
  artifacts:
    - path: "src/components/settings/model-settings-form.tsx"
      provides: "CR-01 (gate the top-level 'Saved.' message on lastSaved draft-equality, not just the recap sub-line) + CR-02 (try/catch inside the startTransition callback around the saveSettingsAction call)"
      contains: "catch"
  key_links:
    - from: "src/components/settings/model-settings-form.tsx"
      to: "src/app/actions/settings.ts"
      via: "await saveSettingsAction(...) inside a try/catch, inside startTransition"
      pattern: "saveSettingsAction"
---

<objective>
Fix Phase 26's two Critical code-review findings in `model-settings-form.tsx` — CR-01 (a save-in-flight race that can show a false "Saved." confirmation over an actually-unsaved edit) and CR-02 (a missing `try/catch` around the Server Action call that can strand the form on "Saving…" forever on transport failure) — per D-27-11. These are already-shipped Phase 26 defects that directly affect the Save action Plan 27-05's extended Playwright spec exercises live; leaving them unfixed would make the verification gate prove a flawed Save path.

Purpose: this is "fixing already-shipped work the verification directly touches," not new capability (per CONTEXT.md's explicit scope framing) — both fixes restore this codebase's own documented "fail safe, fail silent, fail toward a known-good UI state" convention (CLAUDE.md).
Output: `model-settings-form.tsx`, with both fixes applied using the EXACT snippets already specified in `26-REVIEW.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-verification-gate/27-CONTEXT.md
@.planning/phases/26-settings-ui/26-REVIEW.md

@src/components/settings/model-settings-form.tsx
@src/app/(dashboard)/settings/page.tsx
@src/app/actions/settings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix CR-01 (save-in-flight race) and CR-02 (missing try/catch)</name>
  <files>src/components/settings/model-settings-form.tsx</files>
  <read_first>
    src/components/settings/model-settings-form.tsx (lines 101-135 — handleSave and markDirty in full, and lines 383-419 — the status==='saved' JSX render block, exactly as they exist today)
    .planning/phases/26-settings-ui/26-REVIEW.md (CR-01 section lines 35-60 and CR-02 section lines 62-105 — the EXACT fix snippets to apply verbatim)
    src/app/(dashboard)/settings/page.tsx (lines 28-41 — the sibling file's already-correct try/catch fail-safe convention this fix mirrors)
    src/app/actions/settings.ts (confirm the validated order requireStaffAccess -> zod -> servable check -> dedupe -> upsert — read only to confirm it must NOT change; do not edit this file)
  </read_first>
  <action>
    Apply CR-02 first: wrap the body of `handleSave`'s `startTransition(async () => { ... })` callback in a `try { ... } catch { setStatus('error'); setErrorMsg(ERROR_COPY.action_failed); }` block. The `try` must wrap the `await saveSettingsAction(...)` call AND its existing `if (result.ok) { ... } else { ... }` branches — not the outer synchronous `setStatus('saving');` line, and the `try` keyword must be the FIRST statement INSIDE the `startTransition` arrow function's body, not before it (a try/catch around the outer synchronous function body would never catch the async rejection inside the transition callback).

    Apply CR-01 second: change the JSX condition currently gating the entire `status === 'saved'` block (around line 383) from `status === 'saved' ?` to also require `lastSaved && primary === lastSaved.primary && fallbacks.filter((f) => f !== '').join('|') === lastSaved.fallbacks.join('|')` — i.e., move the draft-equals-lastSaved check that today only gates the inner "Saved chain" recap sub-line UP to gate the entire block (both the "Saved." text AND the recap). The existing inner recap's own equality check becomes redundant-safe once the outer check already passed — leave it in place, do not remove it, just don't rely on it being the only gate.

    Do NOT touch `markDirty()` (its `'saving'`-status exemption is a different, already-correct fix protecting WR-01 — modifying it would risk reintroducing that bug). Do NOT touch `src/app/actions/settings.ts` — CR-01/CR-02 are 100% client-side fixes.
  </action>
  <acceptance_criteria>
    - `handleSave`'s `startTransition` callback contains a `try {` as its first statement and a matching `catch {` block that calls `setStatus('error')` and `setErrorMsg(ERROR_COPY.action_failed)`
    - The JSX condition gating the "Saved." `<p>` tag now includes `lastSaved &&` and the `primary === lastSaved.primary` check (verifiable: the string `status === 'saved' &&` no longer appears as a bare, unqualified ternary condition immediately followed by unconditional "Saved." render)
    - `markDirty`'s function body is byte-identical to before this task (unchanged)
    - `src/app/actions/settings.ts` has zero diff (`git diff --stat src/app/actions/settings.ts` reports no changes)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && git diff --stat src/app/actions/settings.ts</automated>
  </verify>
  <done>CR-01's false "Saved." confirmation and CR-02's unhandled-rejection stranding are both fixed exactly per 26-REVIEW.md's specified snippets, with markDirty and settings.ts untouched. Full behavioral proof (mid-save-edit scenario, live browser) happens in Plan 27-05's extended Playwright spec.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client draft state ↔ persisted server state | The exact boundary CR-01 concerns — the UI must never claim persistence that didn't happen |
| Client component ↔ Server Action (`saveSettingsAction`) | The exact boundary CR-02 concerns — a transport-level failure at this boundary must degrade safely, not strand the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-09 | Repudiation-adjacent (data-trust) | `model-settings-form.tsx` "Saved." confirmation | mitigate | CR-01 fix — gate the confirmation on `lastSaved` draft-equality so it can never render over an edit that was never sent |
| T-27-10 | Denial of Service (localized UI lockup) | `model-settings-form.tsx` `handleSave` unhandled rejection | mitigate | CR-02 fix — `try/catch` inside `startTransition`'s callback degrades to the existing error state instead of leaving the form permanently stuck on "Saving…" |
</threat_model>

<verification>
`npx tsc --noEmit` must pass. `git diff --stat src/app/actions/settings.ts` must report zero changes. Full save-flow behavioral proof (including the mid-save-edit race scenario) is exercised live in Plan 27-05's Playwright spec, which depends on this plan.
</verification>

<success_criteria>
Both Critical findings from 26-REVIEW.md are fixed exactly per their specified snippets, with zero collateral changes to markDirty or settings.ts.
</success_criteria>

<output>
Create `.planning/phases/27-verification-gate/27-04-SUMMARY.md` when done
</output>
