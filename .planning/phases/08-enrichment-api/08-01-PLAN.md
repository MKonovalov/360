---
phase: 08-enrichment-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - src/lib/env.ts
  - .env.example
autonomous: true
requirements: [ENRC-03]

must_haves:
  truths:
    - "company and persona each carry a fieldSources jsonb column (Record<string,'manual'|'apollo'>, default {}) and a nullable lastEnrichedAt timestamp"
    - "Existing rows need no backfill — absent fieldSources marker means 'manual'; default {} handles this"
    - "APOLLO_API_KEY is a server-only, optional env var — an unset key must NOT crash the app at import time"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "fieldSources + lastEnrichedAt on company & persona"
    - path: "src/lib/env.ts"
      provides: "APOLLO_API_KEY optional env"
    - path: ".env.example"
      provides: "APOLLO_API_KEY= documented"
---

<objective>
Add the provenance/staleness schema foundation (ENRC-03/D-07/D-08) and the Apollo env key (D-14). `fieldSources` jsonb + `lastEnrichedAt` on `company` and `persona`; `APOLLO_API_KEY` optional in `env.ts` + `.env.example`. Push the migration with `drizzle-kit push`. Zero-dependency foundation that unblocks the query layer (08-04) and client (08-03).
</objective>

<context>
@.planning/phases/08-enrichment-api/08-CONTEXT.md
@.planning/phases/08-enrichment-api/08-RESEARCH.md
@.planning/phases/08-enrichment-api/08-PATTERNS.md
@src/lib/db/schema.ts
@src/lib/env.ts
@.env.example
</context>

<tasks>

<task>
  <name>Task 1: Add fieldSources + lastEnrichedAt to company & persona</name>
  <steps>
  - In `src/lib/db/schema.ts`, import is already `jsonb` + `timestamp` (present). On `company` add:
    `fieldSources: jsonb('field_sources').$type<Record<string, 'manual' | 'apollo'>>().default({}),`
    `lastEnrichedAt: timestamp('last_enriched_at'),`
  - Add the identical two columns to `persona`.
  - Add a comment tagging D-07/D-08 (ENRC-03): per-field provenance, default {} = existing rows treated all-manual, no backfill.
  </steps>
  <verify>`npx tsc --noEmit` clean</verify>
</task>

<task>
  <name>Task 2: APOLLO_API_KEY env (optional, server-only)</name>
  <steps>
  - In `src/lib/env.ts`, inside the envSchema object, after the ARCPEDIA_* block, add: `APOLLO_API_KEY: z.string().optional(),` with a comment: optional/degrade-gracefully like Arcpedia keys — unset disables Enrich, never crashes at import (D-14). Non-PUBLIC = server-only, never logged.
  - In `.env.example`, add `APOLLO_API_KEY=` with a one-line comment (server-only; get from Apollo Settings → Integrations → API Keys).
  </steps>
  <verify>`npx tsc --noEmit` clean; `grep APOLLO_API_KEY src/lib/env.ts .env.example`</verify>
</task>

<task>
  <name>Task 3: Push migration</name>
  <steps>
  - Run `npx drizzle-kit push` (same as Phase 7's schema pushes — D-12). Confirm the two new columns land on both tables.
  </steps>
  <verify>drizzle-kit push reports the ALTER TABLE for field_sources + last_enriched_at on company and persona; `npx tsc --noEmit` clean</verify>
</task>

</tasks>
