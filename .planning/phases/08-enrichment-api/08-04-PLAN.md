---
phase: 08-enrichment-api
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - src/lib/db/queries/companies.ts
  - src/lib/db/queries/personas.ts
autonomous: true
requirements: [ENRC-02, ENRC-03]

must_haves:
  truths:
    - "applyCompanyEnrichment(id, acceptedFields, confidenceMeta?) writes ONLY the accepted field values via a targeted UPDATE, merges fieldSources so each written field is marked 'apollo' (existing markers preserved), and sets last_enriched_at = now()"
    - "Fields NOT in acceptedFields are never touched — neither value nor provenance"
    - "applyPersonaEnrichment mirrors the same contract for persona"
    - "No insert path — the record always already exists (enrichment is per existing record); a missing id is a caller error, not a silent create"
  artifacts:
    - path: "src/lib/db/queries/companies.ts"
      provides: "applyCompanyEnrichment"
      exports: ["applyCompanyEnrichment"]
    - path: "src/lib/db/queries/personas.ts"
      provides: "applyPersonaEnrichment"
      exports: ["applyPersonaEnrichment"]
---

<objective>
The committed-write query layer (ENRC-02/ENRC-03). A lighter cousin of Phase 7's upsert: a targeted `UPDATE ... SET <acceptedFields>, field_sources=<merged>, last_enriched_at=now() WHERE id=?` — no insert path (the record always exists). Merges per-field `apollo` provenance markers onto the existing `fieldSources` map without disturbing untouched fields.
</objective>

<context>
@.planning/phases/08-enrichment-api/08-CONTEXT.md
@.planning/phases/08-enrichment-api/08-RESEARCH.md
@src/lib/db/queries/companies.ts
@src/lib/db/queries/personas.ts
@src/lib/db/schema.ts
</context>

<tasks>

<task>
  <name>Task 1: applyCompanyEnrichment</name>
  <steps>
  - In `src/lib/db/queries/companies.ts`, add `export async function applyCompanyEnrichment(id: number, accepted: Record<string, string | string[]>): Promise<typeof company.$inferSelect>`.
  - Read existing row's `fieldSources` (default {}). Build `mergedSources = { ...existing, ...Object.fromEntries(Object.keys(accepted).map(f => [f, 'apollo' as const])) }`.
  - `UPDATE company SET ...accepted, field_sources = mergedSources, last_enriched_at = new Date() WHERE id = id` via drizzle `.set({...}).where(eq(company.id, id)).returning()`. Return the updated row. No try/catch (caller owns it — matches file convention).
  - Only whitelist enum/text/array columns that exist on company; the Server Action passes already-validated accepted fields.
  </steps>
  <verify>`npx tsc --noEmit` clean; `grep -n "applyCompanyEnrichment\|last_enriched_at\|field_sources\|lastEnrichedAt\|fieldSources" src/lib/db/queries/companies.ts`</verify>
</task>

<task>
  <name>Task 2: applyPersonaEnrichment</name>
  <steps>
  - Mirror Task 1 in `src/lib/db/queries/personas.ts` for `persona` (`title`, `seniority`, `linkedinUrl` writable; `name`/`email` never in accepted).
  </steps>
  <verify>`npx tsc --noEmit` clean; `grep -n "applyPersonaEnrichment" src/lib/db/queries/personas.ts`</verify>
</task>

</tasks>
