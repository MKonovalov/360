---
phase: 08-enrichment-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/enrichment/apolloMap.ts
  - src/lib/enrichment/apolloMap.test.ts
  - src/lib/enrichment/mergePlan.ts
  - src/lib/enrichment/mergePlan.test.ts
autonomous: true
requirements: [ENRC-02, ENRC-03, ENRC-05]

must_haves:
  truths:
    - "apolloMapCompany/apolloMapPersona are PURE functions mapping an Apollo response object to a partial company/persona field set + optional per-field confidence, with lossy-but-deterministic bucketing of employee count and revenue into the existing enums"
    - "Match key fields are never mapped for overwrite: company.domain, persona.email, persona.name are excluded from the mapped output"
    - "buildEnrichmentPlan compares incoming mapped fields against the current record and classifies each as 'fill' (target empty → pre-accepted) or 'conflict' (target populated & differs → opt-in), skipping identical values"
    - "Apollo seniority values map onto seniorityEnum (c_suite→c_level, entry/intern→ic, unknown→omitted)"
  artifacts:
    - path: "src/lib/enrichment/apolloMap.ts"
      provides: "apolloMapCompany, apolloMapPersona, EnrichedField type"
      exports: ["apolloMapCompany", "apolloMapPersona"]
    - path: "src/lib/enrichment/mergePlan.ts"
      provides: "buildEnrichmentPlan, EnrichmentPlanRow type"
      exports: ["buildEnrichmentPlan"]
  key_links:
    - from: "src/lib/enrichment/mergePlan.ts"
      to: "src/lib/enrichment/apolloMap.ts"
      via: "consumes EnrichedField[]"
      pattern: "from './apolloMap'"
---

<objective>
The pure-function core of enrichment (no network, no DB) — fully unit-tested. `apolloMap.ts` maps an Apollo org/person response into `EnrichedField[]` (field name, incoming value, optional confidence), excluding match-key + identity fields and bucketing employee-count/revenue into the existing enums (ENRC-05 confidence stays undefined for Apollo). `mergePlan.ts`'s `buildEnrichmentPlan` classifies each incoming field against the current record as `fill` (empty target, pre-accepted) or `conflict` (populated + differs, opt-in) — the ENRC-02 auto-fill-empty-only policy as testable logic.
</objective>

<context>
@.planning/phases/08-enrichment-api/08-CONTEXT.md
@.planning/phases/08-enrichment-api/08-RESEARCH.md
@.planning/phases/08-enrichment-api/08-PATTERNS.md
@src/lib/import/rowMapper.ts
@src/lib/import/dedupKeys.ts
@src/lib/db/schema.ts
</context>

<tasks>

<task>
  <name>Task 1: apolloMap.ts + tests</name>
  <steps>
  - Define `export type EnrichedField = { field: string; incomingValue: string | string[]; confidence?: number };`
  - `apolloMapCompany(org: unknown): EnrichedField[]` — read fields per 08-RESEARCH.md mapping table. Bucket `estimated_num_employees` (int) → band text; bucket `annual_revenue`/`organization_revenue_printed` → `revenueBandEnum` value (boundaries per schema.ts comments). Map `technology_names`/`current_technologies[].name` → string[] (dedupe). Exclude `domain`. Omit any field whose incoming value is empty/null. `confidence` stays undefined (Apollo enrich has no score — ENRC-05 documented resolution).
  - `apolloMapPersona(person: unknown): EnrichedField[]` — `title`, `linkedin_url`, and `seniority` (map Apollo seniority → seniorityEnum: c_suite→c_level, vp→vp, director→director, manager→manager, entry/intern→ic, unknown→omit). Exclude `email`, `name`.
  - Guard input with a permissive Zod `.passthrough()` object or plain optional-chaining so a missing/extra field degrades to "omitted", never throws (08-RESEARCH watch-item 1).
  - `apolloMap.test.ts`: cover employee/revenue bucketing (boundary cases), tech array dedupe, seniority mapping incl. c_suite→c_level + unknown-omit, empty-field omission, match-key exclusion (domain/email/name never appear in output).
  </steps>
  <verify>`npx vitest run src/lib/enrichment/apolloMap.test.ts` green</verify>
</task>

<task>
  <name>Task 2: mergePlan.ts + tests</name>
  <steps>
  - `export type EnrichmentPlanRow = { field: string; currentValue: string | string[] | null; incomingValue: string | string[]; confidence?: number; classification: 'fill' | 'conflict'; preAccepted: boolean };`
  - `buildEnrichmentPlan(current: Record<string, unknown>, incoming: EnrichedField[]): EnrichmentPlanRow[]` — for each incoming field: if current value is null/undefined/''/[] → `fill`, `preAccepted: true`; if current is populated and DIFFERS → `conflict`, `preAccepted: false`; if current equals incoming → skip (no row). Array equality is order-insensitive set-compare for techStack.
  - `mergePlan.test.ts`: empty→fill+preAccepted, populated-differ→conflict+not-preAccepted, identical→skipped, array set-equality, mixed record produces correct per-field split.
  </steps>
  <verify>`npx vitest run src/lib/enrichment` green (both files)</verify>
</task>

</tasks>
