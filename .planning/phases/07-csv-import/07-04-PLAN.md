---
phase: 07-csv-import
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/lib/validation/seed.ts
  - src/lib/validation/csvImport.ts
  - src/lib/validation/csvImport.test.ts
autonomous: true
requirements: [IMPT-03, IMPT-04]

must_haves:
  truths:
    - "A CSV row with a bad revenue_band value (or any other invalid field) never blocks the rest of the file from importing — validation partitions rows into valid and invalid, it never throws"
    - "Every invalid row is reported with its 1-based CSV row number (header = row 1, data starts row 2) and a human-readable reason"
    - "A Company CSV row can carry a domain column, validated with the same formula-injection guard used for every other free-text field"
  artifacts:
    - path: "src/lib/validation/csvImport.ts"
      provides: "partitionRows — never-throws valid/invalid row partitioner"
      exports: ["partitionRows"]
    - path: "src/lib/validation/seed.ts"
      provides: "companyRowSchema extended with a domain field"
      contains: "domain: optionalSafeCsvString"
  key_links:
    - from: "src/lib/validation/csvImport.ts"
      to: "src/lib/validation/seed.ts"
      via: "imports companyRowSchema/personaRowSchema as the per-row Zod schema partitionRows validates against"
      pattern: "from './seed'"
---

<objective>
Add `domain` to `companyRowSchema` in `src/lib/validation/seed.ts` (D-01's Zod-layer counterpart to Plan 07-01's DB column) and implement `src/lib/validation/csvImport.ts`'s `partitionRows` — a new, never-throwing valid/invalid row aggregator that sits beside (not replacing) `seed.ts`'s existing all-or-nothing `validateRows`, which stays untouched for the CLI seed tool.

Purpose: this is IMPT-03's partial-commit requirement at the validation layer — Plan 07-06's `validateImportBatch` Server Action calls `partitionRows` directly to split an uploaded file into rows that will import and rows that get reported with a reason, never rejecting the whole file over one bad row (Pitfall 1/3 in `07-RESEARCH.md`).
Output: `src/lib/validation/csvImport.ts` + its test file; `src/lib/validation/seed.ts` extended with a `domain` field.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-csv-import/07-CONTEXT.md
@.planning/phases/07-csv-import/07-RESEARCH.md
@.planning/phases/07-csv-import/07-PATTERNS.md

@src/lib/validation/seed.ts
@src/scripts/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add domain field to companyRowSchema in seed.ts</name>
  <files>src/lib/validation/seed.ts</files>
  <read_first>
    src/lib/validation/seed.ts (current file — optionalSafeCsvString's exact blank-to-undefined-then-guard shape, companyRowSchema's current field list)
    .planning/phases/07-csv-import/07-PATTERNS.md (seed.ts section — confirms domain needs no format-specific Zod validator, normalization happens in dedupKeys.ts, not here; explicit instruction not to touch validateRows in src/scripts/seed.ts)
    .planning/phases/07-csv-import/07-CONTEXT.md (D-01 — domain is a plain nullable text field at this validation layer)
  </read_first>
  <action>
    Add domain: optionalSafeCsvString as a new field on companyRowSchema in src/lib/validation/seed.ts, positioned directly after the name field (matching the column order csvTemplate.ts's generateCompanyTemplate uses: name, domain, industry, ...). No new Zod validator is needed — optionalSafeCsvString already exists in this file and applies the same blank-to-undefined transform plus formula-injection guard used for every other optional free-text field (industry, hq_location, tech_stack). Do not modify validateRows in src/scripts/seed.ts, and do not modify data/seed/companies.csv — this task only changes the Zod schema so the Import wizard's CSV rows can carry a domain column; the existing CLI seed pipeline is unaffected since its companies.csv simply omits the column (optional field, absent header is fine).
  </action>
  <acceptance_criteria>
    - companyRowSchema in seed.ts includes a domain field using optionalSafeCsvString, positioned after name
    - src/scripts/seed.ts's validateRows function is unmodified
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "domain: optionalSafeCsvString" src/lib/validation/seed.ts</automated>
  </verify>
  <done>companyRowSchema type-checks with the new optional domain field, and the CLI seed tool's validateRows/data files are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement partitionRows in csvImport.ts</name>
  <files>src/lib/validation/csvImport.ts, src/lib/validation/csvImport.test.ts</files>
  <read_first>
    src/scripts/seed.ts (validateRows, lines ~37-63 — the row-numbering convention (index + 2, header=row 1) and Zod safeParse loop shape to preserve exactly, but the aggregation behavior must diverge: never throw)
    .planning/phases/07-csv-import/07-RESEARCH.md (Pattern 3 — full partitionRows implementation to use directly)
    src/lib/validation/seed.ts (companyRowSchema, personaRowSchema — the per-row Zod schemas partitionRows is generic over)
  </read_first>
  <behavior>
    - partitionRows([{name:'Acme'}, {name:''}], companyRowSchema) returns validRows containing row 2 (data: {name:'Acme'}) and invalidRows containing row 3 with a reason mentioning 'name' (CSV row 1 is the header, so the first data row is row 2)
    - partitionRows never throws, even when every row in the input is invalid — it always returns { validRows, invalidRows }, both arrays, never partially populated then an exception
    - Each invalidRows entry's errors array contains one human-readable string per failed Zod field (path joined with '.', message from the Zod issue), matching seed.ts's validateRows error-string shape
  </behavior>
  <action>
    Implement partitionRows<T extends z.ZodTypeAny>(rows: Record<string, string>[], schema: T): { validRows: { row: number; data: z.infer<T> }[]; invalidRows: { row: number; errors: string[] }[] } exactly per RESEARCH.md Pattern 3 — iterate rows with forEach((row, index)), call schema.safeParse(row), push to validRows with row: index + 2 on success, push to invalidRows with row: index + 2 and errors mapped from result.error.issues (each issue formatted as `${issue.path.join('.')}: ${issue.message}`) on failure. This function must never throw and must never call console.log/console.error — it is a pure data transform, the caller (Plan 07-06's validateImportBatch) decides what to do with the result.
    Also export the RowResult<T> interface ({ validRows, invalidRows }) so Plan 07-05/07-06 can reference the return type without re-declaring it.
    Write csvImport.test.ts using companyRowSchema (imported from ./seed) as the concrete schema under test, covering the <behavior> cases above plus: a fully-valid multi-row input producing zero invalidRows, and a mixed valid/invalid input producing the correct row numbers for both.
  </action>
  <acceptance_criteria>
    - csvImport.ts exports partitionRows and RowResult as named exports
    - partitionRows never contains a throw statement or an uncaught call that could throw synchronously outside of schema.safeParse
    - Row numbers in both validRows and invalidRows use the header-is-row-1 convention (index + 2), matching seed.ts's validateRows numbering exactly
    - npx vitest run src/lib/validation/csvImport.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/validation/csvImport.test.ts && npx tsc --noEmit && ! grep -q "throw new Error" src/lib/validation/csvImport.ts</automated>
  </verify>
  <done>csvImport.ts exists, type-checks, never throws, and its test file passes — proving IMPT-03's partial-commit row partitioning is correct before Plan 07-06 wires it into a Server Action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Uploaded CSV row data -> partitionRows -> DB write path | Every field on every row passes through Zod validation here before Plan 07-05's upsert functions ever see it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | CSV/formula injection via the new domain field | mitigate | domain reuses optionalSafeCsvString unchanged — the same startsWithDangerousPrefix guard already applied to every other free-text company/persona field, so a domain cell starting with =, +, -, @, tab, or carriage return is rejected at validation time, never reaching the database |
| T-07-04 | Information Disclosure / DoS | A malformed row causing partitionRows to throw and crash the whole validate step | mitigate | partitionRows uses schema.safeParse (never schema.parse) exclusively — a single malformed row always produces a structured invalidRows entry, never an uncaught exception that would abort the entire batch's validation |
</threat_model>

<verification>
Run npx vitest run src/lib/validation/csvImport.test.ts and npx tsc --noEmit after Task 2. Confirm Task 1's grep check for the domain field after Task 1.
</verification>

<success_criteria>
- npx vitest run passes for csvImport.test.ts
- npx tsc --noEmit passes
- companyRowSchema carries the new domain field; seed.ts's CLI-facing validateRows/data files are unchanged
</success_criteria>

<output>
Create `.planning/phases/07-csv-import/07-04-SUMMARY.md` when done
</output>
