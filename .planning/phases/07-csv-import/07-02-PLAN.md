---
phase: 07-csv-import
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - next.config.ts
  - src/lib/import/dedupKeys.ts
  - src/lib/import/dedupKeys.test.ts
autonomous: false
requirements: [IMPT-04, IMPT-01]

must_haves:
  truths:
    - "This repo's first automated test suite runs via npx vitest run and passes"
    - "csv-parse is a production dependency (not devDependencies-only) since it will run inside a production Server Action, not just the local seed script"
    - "A CSV upload's multipart body can exceed Next.js's default 1MB Server Action body limit"
    - "normalizeDomain/normalizeEmail produce the exact normalized dedup keys D-02/D-04 require, and buildUpdatePatch implements D-09/D-10's blank-cell-untouched merge as a pure, independently testable function"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest harness config (node environment, src/**/*.test.ts)"
    - path: "src/lib/import/dedupKeys.ts"
      provides: "normalizeDomain, normalizeEmail, buildUpdatePatch pure functions"
      exports: ["normalizeDomain", "normalizeEmail", "buildUpdatePatch"]
    - path: "src/lib/import/dedupKeys.test.ts"
      provides: "unit coverage for dedupKeys.ts"
  key_links:
    - from: "src/lib/import/dedupKeys.test.ts"
      to: "src/lib/import/dedupKeys.ts"
      via: "imports normalizeDomain/normalizeEmail/buildUpdatePatch under test"
      pattern: "from './dedupKeys'"
---

<objective>
Bootstrap this repo's first automated test harness (Vitest), fix the `csv-parse` dependency-tier bug flagged in `07-RESEARCH.md`, add `csv-stringify` (this phase's template-generation dependency), raise the Server Action body-size limit so CSV uploads aren't rejected by Next.js's 1MB default, and implement `src/lib/import/dedupKeys.ts` — the first genuinely new pure-function module this phase needs (D-02/D-04's domain/email normalization, D-09/D-10's blank-cell-untouched merge patch builder) — as the harness's proof-of-life test.

Purpose: every later plan in this phase either needs the Vitest harness to run its own tests (07-03, 07-04) or needs `dedupKeys.ts`'s normalization/merge functions (07-05's upsert queries) — this is a zero-dependency foundation plan that unblocks both.
Output: `vitest.config.ts`, updated `package.json` (dependency tier fix + new deps + `test` script), updated `next.config.ts` (`bodySizeLimit`), and `src/lib/import/dedupKeys.ts` + its test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-csv-import/07-CONTEXT.md
@.planning/phases/07-csv-import/07-RESEARCH.md
@.planning/phases/07-csv-import/07-PATTERNS.md
@.planning/phases/07-csv-import/07-VALIDATION.md

@package.json
@next.config.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: [BLOCKING] Confirm vitest's package legitimacy before install</name>
  <what-built>07-RESEARCH.md's Package Legitimacy Audit table covers csv-parse and csv-stringify (both [OK]/Approved) but does not include vitest — RESEARCH.md's Metadata section flags vitest's version as confirmed via npm view only (not run through slopcheck), so per the package legitimacy gate protocol it is treated as [ASSUMED] until a human confirms it.</what-built>
  <how-to-verify>
    Visit https://www.npmjs.com/package/vitest and confirm: (1) the package is the genuine Vite-ecosystem test runner (maintained under the vitest-dev GitHub org), (2) weekly download count is in the millions (mainstream, widely-adopted), (3) the latest version resolves to 4.x, consistent with the ^4.1.10 range this plan installs. This is a one-time check for this codebase's first-ever devDependency test framework.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with npm install, or report a concern if the package does not match the above.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Bootstrap Vitest harness + fix package.json dependency tiers + raise Server Action body limit</name>
  <files>package.json, vitest.config.ts, next.config.ts</files>
  <read_first>
    package.json (current file — exact scripts/dependencies/devDependencies shape to preserve)
    next.config.ts (current file — the existing process.env.VERCEL conditional-spread structure that must be preserved, not restructured)
    .planning/phases/07-csv-import/07-RESEARCH.md (Standard Stack table — exact versions; Pattern 1's next.config.ts example; Pitfall 3 — bodySizeLimit is global, not scoped to Import)
    .planning/phases/07-csv-import/07-VALIDATION.md (Test Infrastructure section, Wave 0 Requirements)
  </read_first>
  <action>
    In package.json: move "csv-parse" from devDependencies to dependencies, keeping its existing "^7.0.1" version range (it will run inside a production Server Action starting in Plan 07-06, not just the local tsx seed script). Add "csv-stringify": "^6.8.1" to dependencies (sibling package to csv-parse, same node-csv maintainer, used for CSV template generation in Plan 07-03). Add "vitest": "^4.1.10" to devDependencies. Add "test": "vitest run" to the scripts object, placed alongside the existing dev/build/start/lint/seed keys. Run npm install afterward so package-lock.json reflects the change.
    Create vitest.config.ts at the repo root: import defineConfig from 'vitest/config', export default defineConfig with test.environment set to 'node' (no jsdom/React plugin needed — this phase's test scope is pure functions only, per 07-VALIDATION.md) and test.include set to ['src/**/*.test.ts'].
    In next.config.ts, add an experimental.serverActions.bodySizeLimit key set to '5mb' as a sibling top-level property alongside the existing VERCEL-conditional turbopack block — do not restructure or remove the existing conditional-spread logic, only add the new experimental key at the same nesting level. This is a global (not Import-scoped) config change per Pitfall 3 in 07-RESEARCH.md — every Server Action in the app gets the raised limit, not just Import's.
  </action>
  <acceptance_criteria>
    - package.json's dependencies object contains csv-parse and csv-stringify; devDependencies contains vitest but no longer contains csv-parse
    - package.json's scripts object contains a test key set to "vitest run"
    - vitest.config.ts exists, imports defineConfig from vitest/config, and sets test.environment to 'node'
    - next.config.ts's exported nextConfig object contains experimental.serverActions.bodySizeLimit set to '5mb', with the existing VERCEL-conditional turbopack block still present and unmodified
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q '"csv-stringify"' package.json && grep -q '"vitest"' package.json && ! grep -A2 '"devDependencies"' package.json | grep -q '"csv-parse"' && grep -q "bodySizeLimit: '5mb'" next.config.ts && grep -q "environment: 'node'" vitest.config.ts</automated>
  </verify>
  <done>package.json, vitest.config.ts, and next.config.ts all reflect the changes above; npx tsc --noEmit passes; npx vitest run exits 0 (no test files yet, so it reports zero tests, not an error).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement dedupKeys.ts (normalizeDomain, normalizeEmail, buildUpdatePatch) as the harness's first real test module</name>
  <files>src/lib/import/dedupKeys.ts, src/lib/import/dedupKeys.test.ts</files>
  <read_first>
    .planning/phases/07-csv-import/07-RESEARCH.md (Pattern 4 — exact normalizeDomain/normalizeEmail implementation; Assumption A4 — case-insensitive email semantics)
    .planning/phases/07-csv-import/07-PATTERNS.md (dedupKeys.ts section — "no analog" pure-new-logic guidance; extract buildUpdatePatch as its own pure function, do not inline the merge filter only inside the query-layer upsert)
    .planning/phases/07-csv-import/07-CONTEXT.md (D-02 domain normalization rules, D-09/D-10 full-overwrite-except-blank merge semantics)
  </read_first>
  <behavior>
    - normalizeDomain('HTTP://WWW.Foo.com/') -> 'foo.com' (strips protocol, leading www., trailing slash, lowercases, trims whitespace)
    - normalizeDomain('foo.com') -> 'foo.com' (already-normalized input is a no-op)
    - normalizeEmail('  Jane.Doe@Example.COM  ') -> 'jane.doe@example.com' (trims whitespace, lowercases per D-02/A4's case-insensitive semantics)
    - buildUpdatePatch({ name: 'Acme', domain: '', industry: undefined, hqLocation: 'Chicago' }) -> { name: 'Acme', hqLocation: 'Chicago' } (D-10: keys with an empty-string or undefined value are dropped entirely from the patch, never set to '' or null on the existing row)
    - buildUpdatePatch({}) -> {} (an all-blank row produces an empty patch, never touching any existing field)
  </behavior>
  <action>
    Implement normalizeDomain(raw: string): string — trims, lowercases, strips a leading http:// or https://, strips a leading www., strips a single trailing slash, in that order (RESEARCH.md Pattern 4's exact regex chain).
    Implement normalizeEmail(raw: string): string — trims and lowercases only (no other transformation).
    Implement buildUpdatePatch<T extends Record<string, unknown>>(row: T): Partial<T> — returns a new object built via Object.fromEntries, keeping only entries where the value is neither undefined nor an empty string (D-10's "blank cell leaves existing field untouched" — extracted here as its own pure, unit-testable function per PATTERNS.md's explicit instruction, not inlined only inside the query-layer upsert function that Plan 07-05 will write).
    Write dedupKeys.test.ts using Vitest's describe/it/expect (no mocking library), covering every case in the <behavior> block above plus at least one additional edge case per function (e.g. normalizeDomain with no protocol prefix at all; normalizeEmail with mixed-case-only input, no whitespace).
  </action>
  <acceptance_criteria>
    - dedupKeys.ts exports exactly normalizeDomain, normalizeEmail, buildUpdatePatch, all named exports (no default export)
    - npx vitest run src/lib/import/dedupKeys.test.ts exits 0 with every test passing
    - buildUpdatePatch never includes a key whose value is undefined or '' in its returned object
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/import/dedupKeys.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>dedupKeys.ts exists, type-checks, and its test file passes under Vitest — this is the harness's proof-of-life and Plan 07-05's dependency for its upsert functions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| next.config.ts's bodySizeLimit | Raising this global config increases the maximum request body every Server Action in the app will accept, not just Import's |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Denial of Service | next.config.ts experimental.serverActions.bodySizeLimit raised to 5mb globally | mitigate | 5mb is a conservative ceiling for this project's realistic CSV volume (hundreds to low thousands of rows, well under 1MB of plain text per 07-RESEARCH.md) rather than an oversized "just in case" value; Plan 07-06's uploadImportFile Server Action adds a second, feature-scoped row-count cap that does not depend on this global config alone |
| T-07-04 | Tampering | buildUpdatePatch's blank-vs-non-blank filtering logic | mitigate | Implemented as an isolated, unit-tested pure function (this plan) rather than inlined ad hoc inside a later DB-touching function — a regression here would silently corrupt live Company/Persona data on every future re-import (07-RESEARCH.md Validation Architecture rationale), so it is covered by an automated test from the moment it exists |
</threat_model>

<verification>
Run npx vitest run and npx tsc --noEmit after Task 2. Confirm package.json's dependency tiers and next.config.ts's bodySizeLimit are correct via the grep checks in Task 1's verify block.
</verification>

<success_criteria>
- npx vitest run passes (dedupKeys.test.ts green)
- npx tsc --noEmit passes
- package.json has csv-parse in dependencies, csv-stringify + vitest added, and a test script
- next.config.ts's Server Actions body size limit is raised to 5mb without disturbing the existing VERCEL-conditional block
</success_criteria>

<output>
Create `.planning/phases/07-csv-import/07-02-SUMMARY.md` when done
</output>
