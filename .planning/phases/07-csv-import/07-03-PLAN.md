---
phase: 07-csv-import
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/lib/import/columnMapping.ts
  - src/lib/import/columnMapping.test.ts
  - src/lib/import/csvTemplate.ts
  - src/lib/import/csvTemplate.test.ts
autonomous: true
requirements: [IMPT-02, IMPT-06]

must_haves:
  truths:
    - "A CSV's header row is auto-matched to schema field names (including enum-typed fields) using an alias dictionary, with unmatched columns clearly surfaced as unmapped rather than silently guessed (D-08 manual override)"
    - "A CSV cell's raw enum-value text (e.g. '50-250M') is auto-matched to the schema's canonical enum value ('50m_250m'), with unmatched values clearly surfaced as unmapped"
    - "Staff can download a CSV template whose header order and enum-value examples are read directly from schema.ts's *Enum.enumValues arrays, so the template can never drift from the schema (IMPT-06)"
  artifacts:
    - path: "src/lib/import/columnMapping.ts"
      provides: "suggestColumnMapping, suggestValueMapping + alias dictionaries"
      exports: ["suggestColumnMapping", "suggestValueMapping"]
    - path: "src/lib/import/csvTemplate.ts"
      provides: "generateCompanyTemplate, generatePersonaTemplate, enumHelpText"
      exports: ["generateCompanyTemplate", "generatePersonaTemplate", "enumHelpText"]
  key_links:
    - from: "src/lib/import/csvTemplate.ts"
      to: "src/lib/db/schema.ts"
      via: "imports revenueBandEnum/ownershipTypeEnum/seniorityEnum.enumValues directly, never hardcodes a value list"
      pattern: "Enum.enumValues"
---

<objective>
Implement the two pure-function modules behind IMPT-02 (column/enum-value auto-mapping) and IMPT-06 (schema-generated CSV template download): `src/lib/import/columnMapping.ts` (header-alias and enum-value-alias suggestion functions) and `src/lib/import/csvTemplate.ts` (template generator reading directly from `schema.ts`'s enum arrays via `csv-stringify`).

Purpose: Plan 07-06's Server Actions call these directly (uploadImportFile calls suggestColumnMapping; validateImportBatch/the Map step call suggestValueMapping; downloadImportTemplate calls the template generators) — both are pure, DB-free, and independently testable now that Plan 07-02's Vitest harness exists.
Output: `src/lib/import/columnMapping.ts`, `src/lib/import/csvTemplate.ts`, and their test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-csv-import/07-CONTEXT.md
@.planning/phases/07-csv-import/07-RESEARCH.md
@.planning/phases/07-csv-import/07-PATTERNS.md

@src/lib/db/schema.ts
@src/lib/validation/seed.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement columnMapping.ts (suggestColumnMapping, suggestValueMapping)</name>
  <files>src/lib/import/columnMapping.ts, src/lib/import/columnMapping.test.ts</files>
  <read_first>
    .planning/phases/07-csv-import/07-RESEARCH.md (Code Examples "Column & Enum-Value Auto-Mapping (IMPT-02)" — full COMPANY_FIELD_ALIASES/REVENUE_BAND_ALIASES dictionaries and suggestColumnMapping/suggestValueMapping implementations to use directly)
    src/lib/db/schema.ts (revenueBandEnum, ownershipTypeEnum, seniorityEnum — the enum families this module's value-alias dictionaries must stay consistent with)
    .planning/phases/07-csv-import/07-CONTEXT.md (D-08 — manual override is the safety net, not a smarter fuzzy guesser; this module never does fuzzy/Levenshtein matching, only exact match after normalization)
  </read_first>
  <behavior>
    - suggestColumnMapping(['Company Name', 'Website', 'Unknown Column'], COMPANY_FIELD_ALIASES) maps 'Company Name' -> 'name' and 'Website' -> 'domain' via alias match, and 'Unknown Column' -> null (unmapped, no fuzzy fallback)
    - normalizeHeader collapses underscores/hyphens/repeated whitespace to single spaces and lowercases before alias comparison, so 'company_name', 'Company Name', and 'company  name' all resolve identically
    - suggestValueMapping(['50-250M', 'Unknown Band'], REVENUE_BAND_ALIASES) maps '50-250M' -> '50m_250m' via case-insensitive alias match, and 'Unknown Band' -> null (unmapped)
  </behavior>
  <action>
    Create COMPANY_FIELD_ALIASES and REVENUE_BAND_ALIASES (or equivalently-named per-enum alias dictionaries covering revenue_band, ownership_type, and seniority) as module-level Record constants, using RESEARCH.md's drafted dictionaries as the starting content — these are a judgment call per RESEARCH.md's Assumptions Log (A2/A3), always overridable via the wizard's manual mapping UI (D-08), never the sole path to a correct import.
    Implement normalizeHeader(raw: string): string — trim, lowercase, collapse underscores/hyphens to a single space, collapse repeated whitespace to a single space.
    Implement suggestColumnMapping(headers: string[], aliases: Record<string, string[]>): Record<string, string | null> — for each header, normalize it and find the first alias-dictionary entry whose candidate list contains a normalized match; map to that entry's key, or null if no match (exact match only, no fuzzy/Levenshtein matching per D-08's anti-feature stance).
    Implement suggestValueMapping(rawValues: string[], aliases: Record<string, string>): Record<string, string | null> — for each raw value, normalize (trim + lowercase) and look up the alias dictionary; map to the matched canonical enum value, or null if unmatched.
    Write columnMapping.test.ts covering the <behavior> cases plus at least one case per function proving normalization equivalence (underscore vs. space vs. mixed-case header variants all resolving to the same mapped field).
  </action>
  <acceptance_criteria>
    - columnMapping.ts exports suggestColumnMapping and suggestValueMapping as named exports, plus the alias dictionaries used by Plan 07-06's Server Actions
    - suggestColumnMapping never returns a match for a header with no alias-dictionary entry (returns null, not a best-guess fuzzy match)
    - npx vitest run src/lib/import/columnMapping.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/import/columnMapping.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>columnMapping.ts exists, type-checks, and its test file passes under Vitest, proving both header and enum-value alias matching work per the behavior cases above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement csvTemplate.ts (generateCompanyTemplate, generatePersonaTemplate, enumHelpText)</name>
  <files>src/lib/import/csvTemplate.ts, src/lib/import/csvTemplate.test.ts</files>
  <read_first>
    .planning/phases/07-csv-import/07-RESEARCH.md ("CSV Template Generation (IMPT-06)" code example — full generateCompanyTemplate implementation and the companion enumHelpText export to use directly)
    src/lib/db/schema.ts (revenueBandEnum, ownershipTypeEnum, seniorityEnum — the single source of truth this generator must read from, never a hardcoded value list)
    src/scripts/seed.ts (readCsv's header-order convention — the template's header order should match seed.ts's own companies.csv/personas.csv header order for consistency)
  </read_first>
  <behavior>
    - generateCompanyTemplate() returns a CSV string whose header row is exactly: name,domain,industry,employee_count_band,hq_location,revenue_band,ownership_type,tech_stack (in that order), with one example data row beneath it
    - The example row's revenue_band and ownership_type cells always equal revenueBandEnum.enumValues[0] and ownershipTypeEnum.enumValues[0] respectively, so the template is immediately re-importable as-is
    - generatePersonaTemplate() returns a CSV string with header row name,title,seniority,email,linkedin_url and one example row whose seniority cell equals seniorityEnum.enumValues[0]
    - enumHelpText() returns an object whose revenue_band/ownership_type/seniority keys each list every value in the corresponding schema.ts enum's enumValues array, joined into one human-readable string
  </behavior>
  <action>
    Implement generateCompanyTemplate(): string using csv-stringify/sync's stringify() function (imported as `import { stringify } from 'csv-stringify/sync'`), with a single example row object and an explicit columns array fixing the header order specified in the behavior block above, header: true. Import revenueBandEnum, ownershipTypeEnum from '@/lib/db/schema' and read enumValues[0] for the example row's revenue_band/ownership_type cells — never hardcode an enum value string directly in this file.
    Implement generatePersonaTemplate(): string using the same stringify() approach, header row name,title,seniority,email,linkedin_url, example row's seniority cell from seniorityEnum.enumValues[0].
    Implement enumHelpText(): { revenue_band: string; ownership_type: string; seniority: string } — each value built as a template string joining the corresponding enum's enumValues array with ', ' (e.g. "Valid values: under_50m, 50m_250m, ..."), read from the same schema.ts imports, never a second hardcoded copy of the value list.
    Write csvTemplate.test.ts asserting the exact header order and first-data-row enum values for both generators (parse the returned string back with csv-parse/sync inside the test, or assert against the raw string's expected header line), plus an assertion that enumHelpText()'s three string values each contain every value currently in the corresponding schema.ts enumValues array (so the test itself fails loudly if schema.ts's enum values ever change without this file being revisited).
  </action>
  <acceptance_criteria>
    - csvTemplate.ts exports generateCompanyTemplate, generatePersonaTemplate, enumHelpText as named exports
    - Neither generator function contains a hardcoded enum-value string literal anywhere in its body — every enum value used comes from a schema.ts enumValues array reference
    - npx vitest run src/lib/import/csvTemplate.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/import/csvTemplate.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>csvTemplate.ts exists, type-checks, and its test file passes, proving the generated templates always read enum values from schema.ts rather than a hardcoded copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CSV template download -> spreadsheet software | The generated template is schema-controlled output, not user input, but is still CSV text that could be re-opened in a spreadsheet |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | Column/enum-value auto-mapping (columnMapping.ts) silently guessing wrong and committing an incorrect value | mitigate | suggestColumnMapping/suggestValueMapping only ever return an exact alias-dictionary match or null (never a fuzzy best-guess); the wizard's Map step (Plan 07-07) surfaces every unmapped column/value for mandatory manual staff confirmation before commit, per D-08 |
| T-07-01 | Tampering | csvTemplate.ts's generated output later re-imported unmodified | accept | Every value in the generated template comes directly from schema.ts's own enumValues arrays (schema-controlled, not user input), so formula-injection guarding (safeCsvString) is unnecessary here — this file only ever produces output, never parses untrusted input |
</threat_model>

<verification>
Run npx vitest run src/lib/import/columnMapping.test.ts src/lib/import/csvTemplate.test.ts and npx tsc --noEmit after both tasks.
</verification>

<success_criteria>
- npx vitest run passes for both new test files
- npx tsc --noEmit passes
- Neither module hardcodes an enum value that schema.ts already owns
</success_criteria>

<output>
Create `.planning/phases/07-csv-import/07-03-SUMMARY.md` when done
</output>
