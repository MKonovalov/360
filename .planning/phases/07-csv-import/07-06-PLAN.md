---
phase: 07-csv-import
plan: 06
type: execute
wave: 3
depends_on: ["07-03", "07-04", "07-05"]
files_modified:
  - src/app/actions/import.ts
autonomous: true
requirements: [IMPT-01, IMPT-02, IMPT-03, IMPT-04, IMPT-05, IMPT-06, IMPT-07]

must_haves:
  truths:
    - "Uploading a CSV creates a persisted import_batch row and returns headers + a suggested column mapping, without ever round-tripping the full parsed dataset back through the client on later steps"
    - "Validating a batch applies the confirmed mapping, partitions rows via partitionRows, and predicts created/updated/errored counts from live DB state"
    - "Committing a batch upserts every valid row via the query-layer upsert functions, logs each create/update to import_log, and computes final counts from actual commit outcomes, never re-displaying the validate step's predicted counts"
    - "Rollback previews and executes are two separate calls; execute tolerates a dependent row appearing between preview and execute by catching the resulting FK-violation and reporting it as skipped, not as an unhandled error"
    - "Every action in this file calls requireStaffAccess() first, unconditionally, and derives userId only from its return value, never from a caller-supplied parameter"
  artifacts:
    - path: "src/app/actions/import.ts"
      provides: "uploadImportFile, downloadImportTemplate, validateImportBatch, commitImportBatch, previewRollback, executeRollback"
      exports: ["uploadImportFile", "downloadImportTemplate", "validateImportBatch", "commitImportBatch", "previewRollback", "executeRollback"]
  key_links:
    - from: "src/app/actions/import.ts"
      to: "src/lib/db/queries/importBatches.ts"
      via: "every action reads/writes the import_batch row via createImportBatch/getImportBatchById/updateImportBatch"
      pattern: "ImportBatch\\("
    - from: "src/app/actions/import.ts"
      to: "src/lib/db/queries/companies.ts"
      via: "commitImportBatch calls upsertCompanyByDomain (or upsertPersonaByEmail) per valid row"
      pattern: "upsertCompanyByDomain\\("
    - from: "src/app/actions/import.ts"
      to: "src/lib/auth/requireStaffAccess.ts"
      via: "every exported action calls requireStaffAccess() first"
      pattern: "requireStaffAccess\\(\\)"
---

<objective>
Implement `src/app/actions/import.ts` — this phase's entire Server Action layer, composing every prior plan's pure functions and query-layer functions into the six actions the wizard UI (Plans 07-07 through 07-09) calls: `uploadImportFile`, `downloadImportTemplate`, `validateImportBatch`, `commitImportBatch`, `previewRollback`, `executeRollback`.

Purpose: this is the codebase's first CSV upload, first multi-step-wizard backend, and first real destructive-write flow (rollback) — every requirement in this phase (IMPT-01 through IMPT-07) becomes operational once this plan lands, even before any UI exists to call it.
Output: `src/app/actions/import.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-csv-import/07-CONTEXT.md
@.planning/phases/07-csv-import/07-RESEARCH.md
@.planning/phases/07-csv-import/07-PATTERNS.md

@src/app/actions.ts
@src/lib/auth/requireStaffAccess.ts
@src/lib/db/queries/importBatches.ts
@src/lib/db/queries/companies.ts
@src/lib/db/queries/personas.ts
@src/lib/import/columnMapping.ts
@src/lib/import/csvTemplate.ts
@src/lib/validation/csvImport.ts
@src/lib/validation/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: uploadImportFile + downloadImportTemplate</name>
  <files>src/app/actions/import.ts</files>
  <read_first>
    src/app/actions.ts (the mandatory requireStaffAccess()-first pattern, userId-derivation convention — every action in the new file must match this exactly)
    .planning/phases/07-csv-import/07-RESEARCH.md (Pattern 1 — full uploadImportFile code example including bom: true and the FormData/file.text() shape; Pitfall 2 — BOM handling; Pitfall 3 — row-count cap as a feature-scoped DoS guard alongside the global bodySizeLimit)
    src/lib/import/columnMapping.ts (Plan 07-03 output — suggestColumnMapping, the per-entity alias dictionaries)
    src/lib/import/csvTemplate.ts (Plan 07-03 output — generateCompanyTemplate/generatePersonaTemplate)
    src/lib/db/queries/importBatches.ts (Plan 07-05 output — createImportBatch)
  </read_first>
  <action>
    Create src/app/actions/import.ts with 'use server' as the first line. Import requireStaffAccess from '@/lib/auth/requireStaffAccess'; parse from 'csv-parse/sync'; createImportBatch from '@/lib/db/queries/importBatches'; suggestColumnMapping and the COMPANY_FIELD_ALIASES/PERSONA_FIELD_ALIASES-equivalent dictionaries from '@/lib/import/columnMapping'; generateCompanyTemplate, generatePersonaTemplate from '@/lib/import/csvTemplate'.
    Export uploadImportFile(entityType: 'company' | 'persona', formData: FormData): Promise<{ batchId: number; headers: string[]; suggestedMapping: Record<string, string | null>; sampleRows: Record<string, string>[] } | { error: string }>. Call requireStaffAccess() first, destructuring userId. Read the file from formData.get('file') as File, guard that it exists (return { error: 'No file provided' } if not). Wrap the parse in its own try/catch distinct from any later validation try/catch (Security Domain: "malformed CSV causing unhandled exception" threat) — call file.text(), then parse(text, { columns: true, skip_empty_lines: true, trim: true, bom: true }); on a parse exception, return { error: "Couldn't read this CSV file" } (matching the UI-SPEC's exact copy) rather than letting the exception propagate. Add a feature-scoped row-count cap: if the parsed rows array exceeds 5000 entries, return { error: 'File has too many rows (max 5000)' } before persisting anything (Pitfall 3's second, non-global guard). On success, extract headers from Object.keys(rows[0] ?? {}), call suggestColumnMapping(headers, the entity-appropriate alias dictionary), call createImportBatch({ entityType, rawCsv: text, createdBy: userId }), and return { batchId: batch.id, headers, suggestedMapping, sampleRows: rows.slice(0, 5) }.
    Export downloadImportTemplate(entityType: 'company' | 'persona'): Promise<{ filename: string; csv: string }>. Call requireStaffAccess() first (belt-and-suspenders, even for a read-only template generator — matches this file's universal convention). Return { filename: entityType === 'company' ? 'companies-template.csv' : 'personas-template.csv', csv: entityType === 'company' ? generateCompanyTemplate() : generatePersonaTemplate() }.
  </action>
  <acceptance_criteria>
    - Both exported functions call requireStaffAccess() as their first statement, before any other logic
    - uploadImportFile's csv-parse call passes bom: true
    - uploadImportFile wraps the parse call in a try/catch that returns a structured { error } object on failure, never letting a parse exception propagate unhandled
    - uploadImportFile rejects (with a structured error, not a thrown exception) any file producing more than 5000 parsed rows
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "bom: true" src/app/actions/import.ts && grep -q "export async function uploadImportFile" src/app/actions/import.ts && grep -q "export async function downloadImportTemplate" src/app/actions/import.ts</automated>
  </verify>
  <done>uploadImportFile and downloadImportTemplate exist, type-check, and follow this codebase's mandatory auth-first convention with structured (never-thrown) error returns for malformed input.</done>
</task>

<task type="auto">
  <name>Task 2: validateImportBatch + commitImportBatch</name>
  <files>src/app/actions/import.ts</files>
  <read_first>
    .planning/phases/07-csv-import/07-RESEARCH.md (Pattern 2 — full validateImportBatch code example; Pitfall 5 — commit-time counts must be computed from actual outcomes, never re-displaying validate-step predictions)
    src/lib/validation/csvImport.ts (Plan 07-04 output — partitionRows)
    src/lib/validation/seed.ts (Plan 07-04 output — companyRowSchema now includes domain; personaRowSchema)
    src/lib/db/queries/companies.ts, src/lib/db/queries/personas.ts (Plan 07-05 output — upsertCompanyByDomain, upsertPersonaByEmail)
    src/lib/db/queries/importBatches.ts (getImportBatchById, updateImportBatch, insertImportLog)
  </read_first>
  <action>
    Export validateImportBatch(batchId: number, mapping: Record<string, string | null>, valueMapping: Record<string, Record<string, string | null>>): Promise<{ counts: { created: number; updated: number; errored: number }; errorReport: { row: number; errors: string[] }[] } | { error: string }>. Call requireStaffAccess() first. Fetch const batch = await getImportBatchById(batchId); return { error: 'Import batch not found' } if undefined. Re-parse batch.rawCsv with the same csv-parse options as uploadImportFile. Apply mapping/valueMapping to each raw row to produce a canonical field-keyed row object (map each raw header to its mapped schema field name via `mapping`, and for any enum-typed field also substitute the raw cell value via the matching `valueMapping[column][rawValue]`; drop columns mapped to null/"don't import"). Call partitionRows(mappedRows, batch.entityType === 'company' ? companyRowSchema : personaRowSchema). For each valid row, predict created vs. updated: normalize its dedup key (domain or email) via the corresponding dedupKeys function and check for an existing row with that normalized key (a lightweight SELECT, not a write) — tally predictedCreated/predictedUpdated/predictedErrored. Call updateImportBatch(batchId, { mapping, valueMapping, validatedRows: validRows, errorReport: invalidRows, rowsTotal: mappedRows.length, predictedCreated, predictedUpdated, predictedErrored: invalidRows.length, status: 'validated' }). Return { counts: { created: predictedCreated, updated: predictedUpdated, errored: invalidRows.length }, errorReport: invalidRows }.
    Export commitImportBatch(batchId: number): Promise<{ created: number; updated: number; errored: number } | { error: string }>. Call requireStaffAccess() first, destructuring userId. Fetch the batch; return { error: 'Import batch not found' } if undefined or its status is not 'validated'. Read batch.validatedRows (the rows partitionRows already confirmed are individually valid). For each row's data, call upsertCompanyByDomain or upsertPersonaByEmail (based on batch.entityType) and, on success, call insertImportLog({ batchId, entityType: batch.entityType, recordId: result.record.id, action: result.action }); tally actual created/updated counts from each call's real action, per Pitfall 5 — never reuse the batch's predictedCreated/predictedUpdated. errored count is batch.errorReport?.length ?? 0 (rows already excluded before commit, per IMPT-03's partial-commit contract — they were never eligible to commit in the first place). Call updateImportBatch(batchId, { status: 'committed', committedAt: new Date(), actualCreated, actualUpdated, actualErrored: errored }). Return { created: actualCreated, updated: actualUpdated, errored }.
  </action>
  <acceptance_criteria>
    - Both functions call requireStaffAccess() first and fetch the batch via getImportBatchById before doing anything else
    - commitImportBatch's returned created/updated counts are tallied from each row's actual upsert action result, never copied from batch.predictedCreated/predictedUpdated
    - commitImportBatch calls insertImportLog once per successfully upserted row
    - validateImportBatch calls partitionRows (not seed.ts's validateRows) for row validation
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export async function validateImportBatch" src/app/actions/import.ts && grep -q "export async function commitImportBatch" src/app/actions/import.ts && grep -q "partitionRows(" src/app/actions/import.ts && grep -q "insertImportLog(" src/app/actions/import.ts && ! grep -q "batch.predictedCreated" src/app/actions/import.ts</automated>
  </verify>
  <done>validateImportBatch and commitImportBatch exist, type-check, and correctly separate predicted (validate-time) from actual (commit-time) counts per Pitfall 5.</done>
</task>

<task type="auto">
  <name>Task 3: previewRollback + executeRollback</name>
  <files>src/app/actions/import.ts</files>
  <read_first>
    .planning/phases/07-csv-import/07-RESEARCH.md (Pattern 5 — previewRollback/executeRollback shape; Pitfall 4 — no-transactions race tolerance, catch Postgres 23503 as a skip, not an error)
    src/lib/db/queries/importBatches.ts (Plan 07-05 output — findRollbackableRows, markRolledBack)
    src/lib/db/schema.ts (company, persona tables — the actual delete targets)
  </read_first>
  <action>
    Export previewRollback(batchId: number): Promise<{ willDelete: number; skipped: number }>. Call requireStaffAccess() first (read-only action, but still gated — matches every other action in this file). Call const { deletable, skipped } = await findRollbackableRows(batchId); return { willDelete: deletable.length, skipped: skipped.length }. This performs no writes (D-16's confirmation dialog data source).
    Export executeRollback(batchId: number): Promise<{ deleted: number; skipped: number }>. Call requireStaffAccess() first. Re-call findRollbackableRows(batchId) (race-safe re-check per Pitfall 4, not reusing a stale client-supplied count). For each deletable row, attempt to delete the corresponding company or persona row by id (based on entityType) inside a try/catch scoped to that single row; on success, call markRolledBack([row.id]) and increment a deleted counter; on a caught error whose Postgres error code is '23503' (foreign_key_violation — verified this is the correct code for this scenario in RESEARCH.md), increment a skipped counter instead of rethrowing (a dependent row appeared between the pre-check and this delete, per Pitfall 4); any other error type should be rethrown, not silently swallowed. Add the pre-check's own skipped.length to the final skipped tally. Return { deleted, skipped: skipped + raceSkipped }.
  </action>
  <acceptance_criteria>
    - Both functions call requireStaffAccess() first
    - executeRollback re-derives its deletable/skipped sets by calling findRollbackableRows again rather than trusting a client-supplied count from a prior previewRollback call
    - executeRollback catches a Postgres 23503 error per-row and treats it as an additional skip, without rethrowing that specific error class
    - executeRollback calls markRolledBack only for rows it actually deleted
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export async function previewRollback" src/app/actions/import.ts && grep -q "export async function executeRollback" src/app/actions/import.ts && grep -q "23503" src/app/actions/import.ts && grep -q "findRollbackableRows(batchId)" src/app/actions/import.ts</automated>
  </verify>
  <done>previewRollback and executeRollback exist, type-check, and executeRollback correctly re-checks dependents at execute time and tolerates the no-transactions race per Pitfall 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client browser -> every action in import.ts | Server Actions are directly callable even without a Route Handler; any client can invoke uploadImportFile/commitImportBatch/executeRollback directly, bypassing the wizard UI |
| Uploaded CSV bytes -> csv-parse -> DB write path | The first user-controlled file content this codebase parses into structured rows for a live write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Elevation of Privilege | Any of the 6 actions called by an unauthenticated client | mitigate | Every exported function calls requireStaffAccess() as its first statement, before any file read, DB read, or DB write — an unauthenticated caller is redirected server-side with no side effect |
| T-07-03 | Spoofing | createdBy (import_batch) / rolledBackBy-style audit fields | mitigate | userId is derived exclusively from requireStaffAccess()'s return value inside uploadImportFile/executeRollback, never accepted as a caller-supplied parameter — matches the established recordView convention |
| T-07-04 | Information Disclosure / DoS | A malformed or oversized CSV crashing uploadImportFile | mitigate | The csv-parse call is wrapped in its own try/catch returning a structured { error } object; a feature-scoped 5000-row cap (Pitfall 3) backstops the global 5mb bodySizeLimit |
| T-07-05 | Tampering / Data Loss | executeRollback deleting a row that gained a dependent Signal/company_persona_role after previewRollback's pre-check | mitigate | executeRollback re-runs findRollbackableRows immediately before deleting (race-safe re-check) and additionally catches a per-row Postgres 23503 FK violation as a skip rather than an error, per Pitfall 4's no-transactions-on-neon-http constraint |
| T-07-06 | Elevation of Privilege | Any authenticated staff member can commit or roll back any batch | accept | Matches this project's existing binary access model (no roles system); explicitly accepted per D-15/REQUIREMENTS.md ACCS-01 deferral |
</threat_model>

<verification>
Run npx tsc --noEmit after each task. Run npm run build after Task 3 to confirm the whole app (including every existing route) still compiles with the new Server Action file wired in — this is the load-bearing proof that no downstream plan can begin against a broken build.
</verification>

<success_criteria>
- All 6 actions exist, type-check, and call requireStaffAccess() first
- commitImportBatch's counts come from actual upsert outcomes, never predicted counts
- executeRollback tolerates the no-transactions race per Pitfall 4
- npm run build succeeds
</success_criteria>

<output>
Create `.planning/phases/07-csv-import/07-06-SUMMARY.md` when done
</output>
