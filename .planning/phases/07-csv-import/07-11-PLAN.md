---
phase: 07-csv-import
plan: 11
type: execute
wave: 5
depends_on: ["07-09"]
files_modified:
  - src/components/explorer/explorer-menu.tsx
  - src/app/companies/page.tsx
  - src/app/personas/page.tsx
  - src/app/companies/import/history/page.tsx
  - src/app/personas/import/history/page.tsx
autonomous: true
requirements: [IMPT-01, IMPT-07]

must_haves:
  truths:
    - "Menu -> Import on the Company list page navigates to /companies/import; Menu -> Import on the Persona list page navigates to /personas/import (MENU-01's Import action now wired live, no longer disabled)"
    - "Visiting /companies/import/history or /personas/import/history (signed in) shows that entity type's past import batches, with rollback controls, and correct Committed/Rolled back status sourced from listImportBatchesWithRollbackStatus"
  artifacts:
    - path: "src/components/explorer/explorer-menu.tsx"
      provides: "ExplorerMenu items prop extended with an optional href, rendering a Link when present"
    - path: "src/app/companies/import/history/page.tsx"
      provides: "Company import history route"
    - path: "src/app/personas/import/history/page.tsx"
      provides: "Persona import history route"
  key_links:
    - from: "src/app/companies/page.tsx"
      to: "src/app/companies/import/page.tsx"
      via: "ExplorerMenu's Import item href=\"/companies/import\""
      pattern: "href: '/companies/import'"
    - from: "src/app/companies/import/history/page.tsx"
      to: "src/lib/db/queries/importBatches.ts"
      via: "calls listImportBatchesWithRollbackStatus('company') and passes rows to ImportHistoryTable, so the page's Committed/Rolled back status is backed by real data (checker Blocker 3 fix — listImportBatches alone cannot supply isFullyRolledBack)"
      pattern: "listImportBatchesWithRollbackStatus\\("
---

<objective>
Wire the Phase 6-built `ExplorerMenu`'s "Import" item (currently a `disabled: true` placeholder on both `/companies` and `/personas`) into live links, and add the two Import History route pages that render Plan 07-09's `ImportHistoryTable`, sourced from Plan 07-05's `listImportBatchesWithRollbackStatus` so the "Rolled back" status is actually derivable.

Purpose: this is the last plan in the phase — it activates MENU-01's Import action end-to-end and completes IMPT-07's history requirement with a reachable route.
Output: `explorer-menu.tsx` (extended items shape), `companies/page.tsx`/`personas/page.tsx` (Import item enabled), two new history route pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-csv-import/07-CONTEXT.md
@.planning/phases/07-csv-import/07-UI-SPEC.md
@.planning/phases/07-csv-import/07-PATTERNS.md

@src/components/explorer/explorer-menu.tsx
@src/app/companies/page.tsx
@src/app/personas/page.tsx
@src/lib/db/queries/importBatches.ts
@src/components/import/import-history-table.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ExplorerMenu's items prop with an optional href</name>
  <files>src/components/explorer/explorer-menu.tsx</files>
  <read_first>
    src/components/explorer/explorer-menu.tsx (current file, full 46 lines — current items prop shape only supports { label, disabled }, no click/navigation behavior)
    .planning/phases/07-csv-import/07-PATTERNS.md (explorer-menu.tsx section — extend the items prop shape symmetrically for both call sites, do not diverge Companies vs. Personas wiring)
  </read_first>
  <action>
    Change ExplorerMenu's items prop type from { label: string; disabled?: boolean }[] to { label: string; disabled?: boolean; href?: string }[]. In the DropdownMenuItem render, when an item has an href and is not disabled, wrap the item's content in a Next.js Link (next/link) targeting that href using DropdownMenuItem's asChild pattern (matching the codebase's existing Link-based navigation convention rather than an onClick router.push) — when no href is present, render the DropdownMenuItem exactly as before (label-only, no navigation). No other visual/copy change to the component.
  </action>
  <acceptance_criteria>
    - ExplorerMenu's items prop type includes an optional href field
    - An item with an href renders as a Link-wrapped DropdownMenuItem; an item without href behaves exactly as it did before this change
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "href?: string" src/components/explorer/explorer-menu.tsx && grep -q "next/link" src/components/explorer/explorer-menu.tsx</automated>
  </verify>
  <done>ExplorerMenu supports an optional href per item, rendering a Link when present, with zero regression to existing disabled-only items.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Import menu item live on both list pages</name>
  <files>src/app/companies/page.tsx, src/app/personas/page.tsx</files>
  <read_first>
    src/app/companies/page.tsx, src/app/personas/page.tsx (current files — the exact ExplorerMenu items={[{ label: 'Import', disabled: true }]} line to change in each)
  </read_first>
  <action>
    In src/app/companies/page.tsx, change the ExplorerMenu items prop from [{ label: 'Import', disabled: true }] to [{ label: 'Import', href: '/companies/import' }] (drop the disabled key entirely — the item is now live, per Task 1's Link-wrapping behavior). In src/app/personas/page.tsx, change it identically to [{ label: 'Import', href: '/personas/import' }]. No other change to either file.
  </action>
  <acceptance_criteria>
    - companies/page.tsx's ExplorerMenu items no longer include disabled: true for Import, and include href: '/companies/import'
    - personas/page.tsx's ExplorerMenu items no longer include disabled: true for Import, and include href: '/personas/import'
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "href: '/companies/import'" src/app/companies/page.tsx && grep -q "href: '/personas/import'" src/app/personas/page.tsx && ! grep -q "label: 'Import', disabled: true" src/app/companies/page.tsx && ! grep -q "label: 'Import', disabled: true" src/app/personas/page.tsx</automated>
    <human-check>Sign in, visit /companies, open Menu, click Import, confirm navigation to /companies/import and the wizard renders. Repeat on /personas -> /personas/import.</human-check>
  </verify>
  <done>Menu -> Import is live and correctly targeted on both the Company and Persona list pages.</done>
</task>

<task type="auto">
  <name>Task 3: Import History route pages</name>
  <files>src/app/companies/import/history/page.tsx, src/app/personas/import/history/page.tsx</files>
  <read_first>
    src/app/companies/page.tsx (belt-and-suspenders requireStaffAccess() + p-8 page-shell pattern to mirror)
    src/lib/db/queries/importBatches.ts (Plan 07-05 output — listImportBatchesWithRollbackStatus's signature; this is the checker Blocker 3 fix, calling it here instead of the bare listImportBatches is what makes ImportHistoryTable's isFullyRolledBack field exist at all)
    src/components/import/import-history-table.tsx (Plan 07-09 output — ImportHistoryTable's exact props, now typed against listImportBatchesWithRollbackStatus's return shape)
  </read_first>
  <action>
    Create src/app/companies/import/history/page.tsx as an async default-exported Server Component: await requireStaffAccess() first, then const batches = await listImportBatchesWithRollbackStatus('company'), then render a p-8 shell (matching /companies/import/page.tsx's container class) with a page heading and <ImportHistoryTable batches={batches} entityType="company" />. Create src/app/personas/import/history/page.tsx identically with listImportBatchesWithRollbackStatus('persona') and entityType="persona". Both routes are automatically wrapped by the existing companies/layout.tsx / personas/layout.tsx — no new layout file needed.
  </action>
  <acceptance_criteria>
    - Both history pages call requireStaffAccess() before fetching batches
    - companies/import/history/page.tsx calls listImportBatchesWithRollbackStatus('company'); personas/import/history/page.tsx calls listImportBatchesWithRollbackStatus('persona')
    - Neither history page calls the bare listImportBatches (which has no isFullyRolledBack field) as its data source for ImportHistoryTable
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run build && grep -q "listImportBatchesWithRollbackStatus('company')" src/app/companies/import/history/page.tsx && grep -q "listImportBatchesWithRollbackStatus('persona')" src/app/personas/import/history/page.tsx</automated>
  </verify>
  <done>Both Import History routes exist, type-check, build successfully, and render ImportHistoryTable with correctly-aggregated Committed/Rolled back status data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Unauthenticated browser -> /companies/import/history, /personas/import/history | Both new routes must be as fully gated as every other page in the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Elevation of Privilege | /companies/import/history, /personas/import/history accessed while signed out | mitigate | Both new pages call requireStaffAccess() at the page level, on top of the existing layout-level gate — belt-and-suspenders, matching every other route in the app |
| T-07-06 | Elevation of Privilege | Any authenticated staff member can view any entity type's full import history and trigger rollback from it | accept | Matches this project's existing binary access model; explicitly accepted per D-15/ACCS-01 deferral |
</threat_model>

<verification>
Run npx tsc --noEmit after each task, npm run build after Task 3 (final build check for the entire phase). Manual smoke test per Task 2's human-check.
</verification>

<success_criteria>
- Menu -> Import is live and correctly routed on both /companies and /personas
- Both Import History routes exist, render real batch data with correct rollback-status aggregation, and build successfully
- npm run build succeeds for the whole app, completing Phase 7
</success_criteria>

<output>
Create `.planning/phases/07-csv-import/07-11-SUMMARY.md` when done
</output>
</output>
