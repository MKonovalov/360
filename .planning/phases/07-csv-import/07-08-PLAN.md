---
phase: 07-csv-import
plan: 08
type: execute
wave: 4
depends_on: ["07-06"]
files_modified:
  - src/components/import/validation-preview-step.tsx
  - src/components/import/done-step.tsx
autonomous: true
requirements: [IMPT-03, IMPT-05]

must_haves:
  truths:
    - "Staff sees will-create/will-update/error counts before committing, and can commit even when some rows errored (partial commit, IMPT-03) as long as at least one row will create or update"
    - "Staff sees a per-row error table with row number and reason when any rows errored, and can download that error report as a CSV"
    - "After commit, staff sees the actual (not predicted) created/updated/errored counts and links to the imported records and the import history page"
  artifacts:
    - path: "src/components/import/validation-preview-step.tsx"
      provides: "counts row + error table + Commit button"
      exports: ["ValidationPreviewStep"]
    - path: "src/components/import/done-step.tsx"
      provides: "final summary + links"
      exports: ["DoneStep"]
  key_links:
    - from: "src/components/import/validation-preview-step.tsx"
      to: "src/app/actions/import.ts"
      via: "Commit button calls commitImportBatch(batchId)"
      pattern: "commitImportBatch\\("
---

<objective>
Build the wizard's third and fourth screens per `07-UI-SPEC.md`: the Validate & Confirm step (predicted counts, conditional error table with CSV download, Commit button honoring the partial-commit contract) and the Done step (actual commit-time counts, links to the imported list and import history).

Purpose: these complete the wizard's user-facing requirements (IMPT-03's partial commit, IMPT-05's summary); Plan 07-10 assembles this alongside Plan 07-07's screens into the wizard shell.
Output: `validation-preview-step.tsx`, `done-step.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-csv-import/07-CONTEXT.md
@.planning/phases/07-csv-import/07-UI-SPEC.md
@.planning/phases/07-csv-import/07-PATTERNS.md

@src/app/actions/import.ts
@src/components/dashboard/stat-card.tsx
@src/components/ui/table.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ValidationPreviewStep (counts + error table + Commit)</name>
  <files>src/components/import/validation-preview-step.tsx</files>
  <read_first>
    .planning/phases/07-csv-import/07-UI-SPEC.md ("Validate & Confirm step" table, Copywriting Contract's counts/error-banner/Commit rows — exact copy and disabled-state rule)
    src/components/dashboard/stat-card.tsx (StatCard — reuse verbatim for the 3-count row, do not create a second stat-card component)
    src/app/actions/import.ts (Plan 07-06 output — commitImportBatch's signature and return shape)
  </read_first>
  <action>
    Create 'use client' ValidationPreviewStep({ batchId, counts, errorReport, entityType, onCommitted }: { batchId: number; counts: { created: number; updated: number; errored: number }; errorReport: { row: number; errors: string[] }[]; entityType: 'company' | 'persona'; onCommitted: (result: { created: number; updated: number; errored: number }) => void }). Render a full-width p-8 shell: a grid grid-cols-1 gap-4 sm:grid-cols-3 row of 3 StatCard elements captioned "Will create", "Will update", "Errors" with counts.created/updated/errored. If counts.errored > 0, render the amber-50/amber-200/amber-800 banner with heading "{N} rows will be skipped" and body "Fix these rows in your source file and re-upload, or continue — valid rows will still import.", directly above a Table listing every errorReport entry (Row column: Body/slate-900 showing the row number; Reason column: Body/slate-600 joining that entry's errors array with '; '). Include a Button variant="outline" with a DownloadIcon reading "Download error report" positioned top-right of this error table section — on click, build a CSV client-side from errorReport (row, reason columns) via a Blob + object-URL + temporary anchor click (same pattern as Plan 07-07's template download, no new dependency).
    Render the Commit button: Button variant="default" reading "Commit Import" — enabled whenever counts.created + counts.updated > 0, even when counts.errored > 0 (IMPT-03's partial-commit contract: a bad row never blocks the rest). If counts.created + counts.updated === 0 (every row errored), render it disabled with helper text "Fix the errors above before importing." On click (when enabled), call commitImportBatch(batchId) and, on success, call onCommitted(result) — do not manage step-navigation state inside this component itself.
  </action>
  <acceptance_criteria>
    - ValidationPreviewStep reuses the existing StatCard component for its 3-count row (no duplicate stat-card component created)
    - The error table and its download button only render when counts.errored > 0
    - The Commit button is enabled whenever created + updated > 0, regardless of errored count, and disabled only when created + updated === 0
    - Clicking Commit calls commitImportBatch(batchId) and onCommitted on success
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "'use client'" src/components/import/validation-preview-step.tsx && grep -q "commitImportBatch(" src/components/import/validation-preview-step.tsx && grep -q "StatCard" src/components/import/validation-preview-step.tsx && grep -q "Commit Import" src/components/import/validation-preview-step.tsx</automated>
  </verify>
  <done>ValidationPreviewStep exists, type-checks, and implements the partial-commit contract exactly per 07-UI-SPEC.md and IMPT-03.</done>
</task>

<task type="auto">
  <name>Task 2: DoneStep (final summary + links)</name>
  <files>src/components/import/done-step.tsx</files>
  <read_first>
    .planning/phases/07-csv-import/07-UI-SPEC.md ("Done step" table, Copywriting Contract's "Done step — success summary"/"Done step — links" rows — exact copy, centered max-w-xl layout matching the Upload step's framing)
  </read_first>
  <action>
    Create DoneStep({ result, entityType }: { result: { created: number; updated: number; errored: number }; entityType: 'company' | 'persona' }) as a plain presentational component (Server or Client, no data fetching — result is passed in from the wizard shell's own state, computed from the actual commitImportBatch response per Pitfall 5, never re-derived here). Render a centered max-w-xl mx-auto container: a CircleCheckIcon (size-8, slate-900) above the heading "Import complete", then a counts line using the Display typographic role matching the exact copy "{created} created · {updated} updated · {errored} skipped — errored" (em-dash-separated per UI-SPEC). Below, stacked space-y-2 indigo-600 text links: "View imported companies" or "View imported personas" (entity-specific noun) linking to /companies or /personas, and "View import history" linking to /companies/import/history or /personas/import/history (entity-specific per D-05's one-entity-type-per-flow convention).
  </action>
  <acceptance_criteria>
    - DoneStep accepts result and entityType props and renders the exact counts-line copy format from the Copywriting Contract
    - The two links point to the entity-specific routes (/companies vs /personas, /companies/import/history vs /personas/import/history), never a hardcoded single entity type
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export function DoneStep" src/components/import/done-step.tsx && grep -q "Import complete" src/components/import/done-step.tsx && grep -q "import/history" src/components/import/done-step.tsx</automated>
  </verify>
  <done>DoneStep exists, type-checks, and renders the exact summary/links contract per 07-UI-SPEC.md, using the actual (not predicted) commit-time counts passed in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Commit button click -> commitImportBatch Server Action | This is the wizard's actual write-triggering action; D-13's rollback provides the undo path, so no extra confirmation dialog gates this click, unlike Rollback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | Client-built error-report CSV download re-encoding row/reason text incorrectly | accept | The error-report CSV is built from data the server already validated and rejected (never committed) — a formatting issue in the client-side download is a UX defect, not a data-integrity risk, since this file is never re-imported as a valid source (it is explicitly the rejected-rows report) |
| T-07-06 | Information Disclosure | DoneStep displaying predicted counts instead of actual commit outcomes | mitigate | DoneStep receives result exclusively from the wizard shell's own state, populated only from commitImportBatch's real response (Plan 07-06's Pitfall 5 handling) — it never re-derives or re-displays the validate step's predicted counts |
</threat_model>

<verification>
Run npx tsc --noEmit after each task.
</verification>

<success_criteria>
- Both components exist, type-check, and match 07-UI-SPEC.md's Validate & Confirm / Done step contracts exactly
- The Commit button's enabled/disabled logic exactly matches IMPT-03's partial-commit rule
</success_criteria>

<output>
Create `.planning/phases/07-csv-import/07-08-SUMMARY.md` when done
</output>
