---
phase: 07-csv-import
plan: 09
type: execute
wave: 4
depends_on: ["07-05", "07-06"]
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/import/import-history-table.tsx
  - src/components/import/rollback-dialog.tsx
autonomous: true
requirements: [IMPT-07]

must_haves:
  truths:
    - "Staff can see a table of past import batches (date, entity, uploaded by, created/updated counts, status) for one entity type, with an empty state when none exist"
    - "Staff can trigger rollback on a still-rollbackable batch, sees a confirmation dialog with the exact N/M counts before anything is deleted, and sees the actual executed counts after confirming (D-16)"
    - "The rollback confirmation dialog populates its counts via a read-only previewRollback call, and executing never happens without that explicit confirm step"
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "shadcn Dialog primitive (first in this app)"
    - path: "src/components/import/import-history-table.tsx"
      provides: "history table + empty state"
      exports: ["ImportHistoryTable"]
    - path: "src/components/import/rollback-dialog.tsx"
      provides: "rollback confirmation dialog"
      exports: ["RollbackDialog"]
  key_links:
    - from: "src/components/import/rollback-dialog.tsx"
      to: "src/app/actions/import.ts"
      via: "calls previewRollback on open, executeRollback on confirm"
      pattern: "previewRollback\\("
---

<objective>
Install shadcn's `dialog` primitive (this app's first — the existing `sheet.tsx` is a slide-over, not a centered confirm/cancel surface) and build the Import History table plus its rollback confirmation dialog per `07-UI-SPEC.md`, wiring `previewRollback` (dry-run, populates the dialog) and `executeRollback` (the actual delete) from Plan 07-06.

Purpose: this is IMPT-07's history/rollback UI — the last piece needed before Plan 07-11 wires it into the `/companies/import/history` and `/personas/import/history` routes.
Output: `src/components/ui/dialog.tsx` (via `npx shadcn add dialog`), `import-history-table.tsx`, `rollback-dialog.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-csv-import/07-CONTEXT.md
@.planning/phases/07-csv-import/07-UI-SPEC.md
@.planning/phases/07-csv-import/07-PATTERNS.md

@src/app/actions/import.ts
@src/lib/db/queries/importBatches.ts
@src/components/dashboard/recently-viewed.tsx
@src/components/ui/sheet.tsx
@src/components/ui/skeleton.tsx
@src/components/ui/badge.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn dialog primitive</name>
  <files>src/components/ui/dialog.tsx</files>
  <read_first>
    src/components/ui/sheet.tsx (existing overlay primitive, for comparison — Dialog is the new, distinct centered-confirm surface, not a replacement for Sheet)
    components.json (confirms shadcn is already initialized in this repo — radix-nova style, neutral base color)
  </read_first>
  <action>
    Run npx shadcn add dialog from the repository root. This vendors src/components/ui/dialog.tsx from the official shadcn registry (no third-party registry involved, per 07-UI-SPEC.md's Registry Safety table — no vetting gate required). Do not hand-modify the generated file beyond what the CLI produces; if the CLI prompts for an overwrite/conflict decision on any shared file (e.g. globals.css), accept the default (no existing Dialog usage exists to conflict with).
  </action>
  <acceptance_criteria>
    - src/components/ui/dialog.tsx exists and exports Dialog, DialogTrigger, DialogContent, DialogHeader, DialogTitle, DialogFooter (the standard shadcn dialog export set)
    - npx tsc --noEmit passes with the new file in place
  </acceptance_criteria>
  <verify>
    <automated>test -f src/components/ui/dialog.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>The shadcn dialog primitive is installed and type-checks cleanly alongside the rest of the vendored ui/ components.</done>
</task>

<task type="auto">
  <name>Task 2: ImportHistoryTable</name>
  <files>src/components/import/import-history-table.tsx</files>
  <read_first>
    .planning/phases/07-csv-import/07-UI-SPEC.md ("Import History page" table — columns, empty state copy, Roll back trigger placement)
    src/components/dashboard/recently-viewed.tsx (closest existing "list of past timestamped events" component — empty/error-state card convention to reuse)
    src/lib/db/queries/importBatches.ts (Plan 07-05 output — listImportBatches's return shape)
  </read_first>
  <action>
    Create ImportHistoryTable({ batches, entityType }: { batches: Awaited<ReturnType<typeof listImportBatches>>; entityType: 'company' | 'persona' }) — a component receiving already-fetched batch rows as a prop (the page component, Plan 07-11, calls listImportBatches server-side and passes the result in; this component itself does no data fetching). If batches.length === 0, render the standard empty-state card (min-h-48 rounded-lg border border-slate-200 bg-white p-8 text-center) with heading "No imports yet" and body "Run your first CSV import from Menu -> Import on the Company or Persona list." Otherwise render a full-width Table with columns Date, Entity, Uploaded by, Created, Updated, Status (Badge variant="secondary" reading "Committed", or variant="destructive" reading "Rolled back" — derive "Rolled back" status from whether every one of that batch's created-action import_log rows has rolledBackAt set, otherwise "Committed"), and Actions (a RollbackDialog trigger, hidden once the batch's status is fully "Rolled back").
  </action>
  <acceptance_criteria>
    - ImportHistoryTable renders the exact empty-state copy when batches is empty
    - The Status badge uses variant="secondary" for Committed and variant="destructive" for Rolled back, never a hardcoded color class
    - The Actions column's rollback trigger is omitted (not just disabled) once a batch is fully rolled back
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export function ImportHistoryTable" src/components/import/import-history-table.tsx && grep -q "No imports yet" src/components/import/import-history-table.tsx && grep -q 'variant="destructive"' src/components/import/import-history-table.tsx</automated>
  </verify>
  <done>ImportHistoryTable exists, type-checks, and renders the exact table/empty-state contract per 07-UI-SPEC.md.</done>
</task>

<task type="auto">
  <name>Task 3: RollbackDialog</name>
  <files>src/components/import/rollback-dialog.tsx</files>
  <read_first>
    .planning/phases/07-csv-import/07-UI-SPEC.md ("Rollback confirmation Dialog" row in Copywriting Contract and Import History page's Interaction Contract — exact dialog title/body/button copy, dynamic N/M substitution, Skeleton-while-loading behavior)
    src/app/actions/import.ts (Plan 07-06 output — previewRollback/executeRollback signatures and return shapes)
    src/components/ui/skeleton.tsx (already-vendored Skeleton for the dialog's brief loading state)
    src/components/ui/dialog.tsx (Task 1 output — Dialog/DialogContent/DialogHeader/DialogTitle/DialogFooter composition)
  </read_first>
  <action>
    Create 'use client' RollbackDialog({ batchId, entityType }: { batchId: number; entityType: 'company' | 'persona' }). Render a Button variant="destructive" size="sm" reading "Roll back" as the Dialog's trigger. On Dialog open, call previewRollback(batchId) and hold the { willDelete, skipped } result in local state, showing a Skeleton in the dialog body while that call is in flight (per UI-SPEC: "a brief loading state inside the Dialog body is acceptable rather than blocking the Dialog's open animation"). Once loaded, render DialogTitle "Roll back this import?" and body copy "{N} {companies|personas} will be deleted. {M} rows skipped — has dependent data. This can't be undone." (substitute N=willDelete, M=skipped, and the correct plural noun for entityType). Render DialogFooter with a Button variant="outline" "Cancel" (closes the dialog, no action) and a Button variant="destructive" "Roll back {N} records" that calls executeRollback(batchId) on click; on success, close the dialog and surface an inline confirmation using the actual executed counts ("Rolled back {N} records. {M} skipped — has dependent data.") without a full page reload, per UI-SPEC's race-tolerance note (the executed N/M may differ slightly from the previewed N/M — always report the actual post-execute numbers, never the pre-check ones).
  </action>
  <acceptance_criteria>
    - RollbackDialog calls previewRollback(batchId) only on Dialog open, never eagerly on mount
    - The confirm button's label and the executed-result copy both use dynamic N/M substitution, never a hardcoded count
    - The post-rollback confirmation copy reflects executeRollback's actual returned counts, not the previewRollback pre-check counts
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "'use client'" src/components/import/rollback-dialog.tsx && grep -q "previewRollback(" src/components/import/rollback-dialog.tsx && grep -q "executeRollback(" src/components/import/rollback-dialog.tsx && grep -q "Roll back this import?" src/components/import/rollback-dialog.tsx</automated>
  </verify>
  <done>RollbackDialog exists, type-checks, and implements D-16's confirm-before-delete contract exactly, reporting actual (not predicted) counts after execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| RollbackDialog confirm click -> executeRollback | This is the app's first UI-triggered genuinely destructive action (a real DELETE against company/persona rows) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering / Data Loss | A staff member confirming rollback based on stale previewRollback counts | mitigate | D-16's mandatory confirmation dialog always shows previewRollback's live result (fetched fresh on every Dialog open, never cached across opens); executeRollback additionally re-checks dependents itself (Plan 07-06) rather than trusting the dialog's displayed count as authorization to delete a specific row set |
| T-07-06 | Elevation of Privilege | Any authenticated staff member can roll back any batch shown in the history table | accept | Matches this project's existing binary access model; explicitly accepted per D-15 |
</threat_model>

<verification>
Run npx tsc --noEmit after each task.
</verification>

<success_criteria>
- shadcn dialog is installed and type-checks
- ImportHistoryTable and RollbackDialog exist, type-check, and match 07-UI-SPEC.md's contracts exactly
- RollbackDialog never executes a delete without first showing the live preview count and requiring an explicit confirm click
</success_criteria>

<output>
Create `.planning/phases/07-csv-import/07-09-SUMMARY.md` when done
</output>
