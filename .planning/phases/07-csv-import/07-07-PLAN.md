---
phase: 07-csv-import
plan: 07
type: execute
wave: 4
depends_on: ["07-06"]
files_modified:
  - src/components/import/import-step-indicator.tsx
  - src/components/import/upload-step.tsx
  - src/components/import/column-mapping-step.tsx
autonomous: true
requirements: [IMPT-01, IMPT-02, IMPT-06]

must_haves:
  truths:
    - "Staff sees a 4-pill step indicator (Upload, Map, Validate & Confirm, Done) with the current step highlighted"
    - "Staff can drag-and-drop or click-to-browse a CSV file, which auto-submits and advances to the Map step, or shows an inline parse-failure banner on a malformed file"
    - "Staff can download a schema-generated CSV template directly from the Upload step, before preparing their own file"
    - "Staff sees every CSV column mapped to a schema field (or explicitly 'Unmapped'), with enum-typed columns showing a nested raw-value-to-enum-value mapping table covering every distinct raw value in the FULL uploaded file (not a preview sample), and cannot continue until every one of those values has an explicit mapping"
  artifacts:
    - path: "src/components/import/import-step-indicator.tsx"
      provides: "4-pill step indicator, entity-agnostic"
      exports: ["ImportStepIndicator"]
    - path: "src/components/import/upload-step.tsx"
      provides: "dropzone + template download + parse-failure banner"
      exports: ["UploadStep"]
    - path: "src/components/import/column-mapping-step.tsx"
      provides: "mapping table + enum-value sub-mapping"
      exports: ["ColumnMappingStep"]
  key_links:
    - from: "src/components/import/upload-step.tsx"
      to: "src/app/actions/import.ts"
      via: "calls uploadImportFile and downloadImportTemplate"
      pattern: "uploadImportFile\\("
    - from: "src/components/import/column-mapping-step.tsx"
      to: "src/app/actions/import.ts"
      via: "calls validateImportBatch on Continue"
      pattern: "validateImportBatch\\("
---

<objective>
Build the first two wizard screens per `07-UI-SPEC.md`'s Interaction Contract: the entity-agnostic step indicator, the Upload step (dropzone, template download, parse-failure banner, calling `uploadImportFile`/`downloadImportTemplate`), and the Map step (mapping table + enum-value sub-mapping driven by the FULL-file distinct-value data `uploadImportFile` now returns, calling `validateImportBatch`).

Purpose: these are the first 2 of the wizard's 4 screens; Plan 07-08 builds the remaining 2 (Validate & Confirm, Done); Plan 07-10 assembles all 4 into the wizard shell.
Output: `import-step-indicator.tsx`, `upload-step.tsx`, `column-mapping-step.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-csv-import/07-CONTEXT.md
@.planning/phases/07-csv-import/07-UI-SPEC.md
@.planning/phases/07-csv-import/07-PATTERNS.md

@src/app/actions/import.ts
@src/components/companies/company-filters.tsx
@src/components/ui/select.tsx
@src/components/ui/badge.tsx
@src/components/ui/button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: ImportStepIndicator</name>
  <files>src/components/import/import-step-indicator.tsx</files>
  <read_first>
    .planning/phases/07-csv-import/07-UI-SPEC.md ("Wizard shell" table — 4-pill horizontal indicator: completed/current/upcoming visual states, exact classes and icon usage)
    src/components/ui/badge.tsx (Badge component this indicator's pills are styled with)
  </read_first>
  <action>
    Create ImportStepIndicator({ currentStep }: { currentStep: 'upload' | 'map' | 'validate' | 'done' }) as a Client or Server Component (no interactivity needed — plain presentational, Server Component is fine since it takes no client-only hooks). Render a horizontal row of 4 pills (in order: Upload, Map, Validate & Confirm, Done) connected by a thin slate-200 line, per the UI-SPEC's exact layout. For each pill, compare its step key against currentStep and the step's array index against currentStep's index to determine completed/current/upcoming: completed steps render slate-500 text with a CircleCheckIcon (slate-400, lucide-react); the current step renders indigo-600 text with a border-b-2 border-indigo-600; upcoming steps render slate-400 text with no icon. Label text uses the Label typographic role (12px/400).
  </action>
  <acceptance_criteria>
    - ImportStepIndicator accepts exactly one prop, currentStep, typed to the 4 literal step values
    - The component renders exactly 4 pills regardless of currentStep's value
    - Completed steps render a CircleCheckIcon; current and upcoming steps do not
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "export function ImportStepIndicator" src/components/import/import-step-indicator.tsx && grep -q "CircleCheckIcon" src/components/import/import-step-indicator.tsx</automated>
  </verify>
  <done>ImportStepIndicator exists, type-checks, and renders the 4-pill indicator with correct completed/current/upcoming visual states per currentStep.</done>
</task>

<task type="auto">
  <name>Task 2: UploadStep (dropzone + template download + parse-failure banner)</name>
  <files>src/components/import/upload-step.tsx</files>
  <read_first>
    .planning/phases/07-csv-import/07-UI-SPEC.md ("Upload step" table, Copywriting Contract's Dropzone/Template-download/parse-failure-error rows — exact classes, exact copy strings)
    src/app/actions/import.ts (Plan 07-06 output — uploadImportFile's exact signature and return shape, now including columnValues and suggestedValueMapping alongside batchId/headers/suggestedMapping/sampleRows; downloadImportTemplate)
    src/components/ui/button.tsx (Button variant="outline" for the template-download button)
  </read_first>
  <action>
    Create 'use client' UploadStep({ entityType, onUploaded }: { entityType: 'company' | 'persona'; onUploaded: (result: { batchId: number; headers: string[]; suggestedMapping: Record<string, string | null>; sampleRows: Record<string, string>[]; columnValues: Record<string, string[]>; suggestedValueMapping: Record<string, Record<string, string | null>> }) => void }) — the result type is exactly uploadImportFile's success return shape (Plan 07-06 output), unabridged; do not drop columnValues/suggestedValueMapping when forwarding the result to onUploaded, since the Map step (Task 3 below) needs them to gate its Continue button correctly against the full uploaded file, not just a preview sample. Render the dropzone per UI-SPEC exactly: rounded-lg border-2 border-dashed border-slate-300 bg-slate-50 p-12 text-center, min-h-48, hover:border-slate-400, drag-active state border-indigo-600 bg-indigo-50/50 (track drag-active via onDragEnter/onDragLeave/onDragOver/onDrop handlers and local useState), UploadIcon (lucide-react, size-8 text-slate-400) centered above the copy "Drag and drop your CSV, or click to browse" (Body) and "Only .csv files, up to 5MB." (Label, slate-500). A hidden `<input type="file" accept=".csv">` is triggered by clicking anywhere in the dropzone (via a ref + programmatic .click()) or by a native drop event's dataTransfer.files.
    On a valid file selection (either path), immediately build a FormData with the file under the 'file' key and call uploadImportFile(entityType, formData) inside a startTransition or plain async handler; on a successful (non-error) result, call onUploaded(result) — passing through the full result object exactly as received — so the parent wizard shell (Plan 07-10) can advance to the Map step — do not manage step-advancement state inside this component itself. On an { error } result, render the parse-failure banner using the existing amber-50/amber-200/amber-800 pattern (copy company-list.tsx's exact class string) with heading "Couldn't read this CSV file" and body "Check that it's a valid CSV export and try again." above the dropzone, and leave the dropzone interactive for a retry.
    Below the dropzone (mt-4), render a Button variant="outline" with a DownloadIcon reading "Download template" that calls downloadImportTemplate(entityType) and, on success, triggers a client-side download of the returned csv string (construct a Blob with type 'text/csv', an object URL, and a temporary anchor click — no new dependency needed for this).
  </action>
  <acceptance_criteria>
    - UploadStep is a 'use client' component accepting entityType and onUploaded props
    - A valid file selection calls uploadImportFile(entityType, formData) and, on success, calls onUploaded(result) with the full result object (including columnValues and suggestedValueMapping) unaltered, without managing its own step-navigation state
    - A parse-failure ({ error }) response renders the exact UI-SPEC copy in the amber-50/amber-200/amber-800 banner pattern and does not call onUploaded
    - The template-download button never blocks file selection and works independently of upload state
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "'use client'" src/components/import/upload-step.tsx && grep -q "uploadImportFile(" src/components/import/upload-step.tsx && grep -q "downloadImportTemplate(" src/components/import/upload-step.tsx && grep -q "Couldn't read this CSV file" src/components/import/upload-step.tsx && grep -q "columnValues" src/components/import/upload-step.tsx && grep -q "suggestedValueMapping" src/components/import/upload-step.tsx</automated>
    <human-check>Open Menu -> Import once Plan 07-10 wires the route; drag a CSV over the dropzone and confirm the border/background tint changes; select a real CSV and confirm it advances; select a non-CSV/binary file and confirm the parse-failure banner appears without navigating away.</human-check>
  </verify>
  <done>UploadStep exists, type-checks, and implements the dropzone/template-download/parse-failure contract exactly per 07-UI-SPEC.md, forwarding uploadImportFile's full result (including the full-file column-value data) to the parent unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: ColumnMappingStep (mapping table + enum sub-mapping)</name>
  <files>src/components/import/column-mapping-step.tsx</files>
  <read_first>
    .planning/phases/07-csv-import/07-UI-SPEC.md ("Map step" table — CSV Column/Sample Value/Maps To columns, enum sub-mapping expandable row contract, Continue-button gating rule)
    src/components/companies/company-filters.tsx (Select/SelectTrigger/SelectContent composition pattern to reuse verbatim)
    src/components/ui/table.tsx (Table/TableHeader/TableBody/TableRow/TableCell primitives)
    src/app/actions/import.ts (Plan 07-06 output — validateImportBatch's exact signature and return shape)
  </read_first>
  <action>
    Create 'use client' ColumnMappingStep({ batchId, headers, suggestedMapping, sampleRows, columnValues, suggestedValueMapping, entityType, onValidated }: { batchId: number; headers: string[]; suggestedMapping: Record<string, string | null>; sampleRows: Record<string, string>[]; columnValues: Record<string, string[]>; suggestedValueMapping: Record<string, Record<string, string | null>>; entityType: 'company' | 'persona'; onValidated: (result: { counts: { created: number; updated: number; errored: number }; errorReport: { row: number; errors: string[] }[] }) => void }). columnValues and suggestedValueMapping come directly from uploadImportFile's response (Plan 07-06, forwarded unmodified by UploadStep/the wizard shell) — columnValues[header] is the complete distinct-raw-value set for that column across the ENTIRE uploaded file (not derived from sampleRows here; sampleRows is only used for the single-example "Sample Value" table column).
    Hold local useState for mapping (a Record<string, string | null>, initialized from suggestedMapping — header to schema field name or null) and valueMapping (a Record<string, Record<string, string | null>>, initialized from suggestedValueMapping — header to { rawValue: canonicalEnumValue | null }, only populated for headers suggestedMapping/suggestedValueMapping already covers; any other header starts with no entry). The set of "enum-typed columns currently needing a sub-mapping table" is derived live from the CURRENT mapping state (const ENUM_FIELD_NAMES = new Set(['revenue_band', 'ownership_type', 'seniority']); headers.filter(h => mapping[h] && ENUM_FIELD_NAMES.has(mapping[h]))) — not fixed at mount time, so if staff manually reassigns a column to revenue_band/ownership_type/seniority that suggestColumnMapping didn't originally match, that column's sub-mapping table appears immediately, seeded from columnValues[header] (already complete since it was computed for every header at upload time) with every value starting "Unmapped" (no pre-existing valueMapping[header] entry) until staff picks one.
    Render a full-width p-8 shell (no max-w) with a Table: one row per header in `headers`, columns CSV Column (Body, slate-900, the header text), Sample Value (Label, slate-500, truncate max-w-48, pulled from the first sampleRows entry containing that header), Maps To (a Select populated with every schema field name for entityType plus a "— Don't import —" null option; pre-selected from mapping[header]; unmapped headers show a Badge variant="outline" reading "Unmapped" instead of a pre-selection, per D-08). For any header currently mapped to an enum-typed field (per the live-derived set above), render an expandable nested row (ChevronDownIcon toggle) showing a 2-column mini-table: EVERY distinct raw value in columnValues[header] (never just the values visible in sampleRows) -> a Select of that enum's canonical values, with an "Unmapped" badge for any value not yet present as a key in valueMapping[header].
    Render a Button variant="default" reading "Continue to Validation", disabled until every header in the live-derived enum-mapped-columns set has an explicit valueMapping[header][value] entry for every one of that column's columnValues[header] entries (per UI-SPEC's exact gating rule — never silently defaults an unmapped enum value into commit, and correctly accounts for values that only appear later in the file, past any preview sample). On click, call validateImportBatch(batchId, mapping, valueMapping) and, on success, call onValidated(result) — do not manage step-navigation state inside this component itself.
  </action>
  <acceptance_criteria>
    - ColumnMappingStep is a 'use client' component accepting batchId, headers, suggestedMapping, sampleRows, columnValues, suggestedValueMapping, entityType, onValidated
    - Every header renders as a table row with a Select pre-populated from suggestedMapping, or an "Unmapped" badge if suggestedMapping's value for that header is null
    - The enum-typed-columns set (and therefore which columns render a sub-mapping table) is derived from the CURRENT mapping state, not fixed to suggestedMapping's original auto-matches — manually reassigning a column to an enum field immediately reveals its sub-mapping table
    - Each enum column's sub-mapping table iterates columnValues[header] (the full-file distinct-value set), never a value list derived only from sampleRows
    - The Continue button is disabled while any enum-mapped column has a distinct raw value (from columnValues) with no valueMapping entry
    - Clicking Continue (once enabled) calls validateImportBatch(batchId, mapping, valueMapping) and onValidated on success
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -q "'use client'" src/components/import/column-mapping-step.tsx && grep -q "validateImportBatch(" src/components/import/column-mapping-step.tsx && grep -q "Unmapped" src/components/import/column-mapping-step.tsx && grep -q "columnValues" src/components/import/column-mapping-step.tsx && grep -q "ENUM_FIELD_NAMES" src/components/import/column-mapping-step.tsx</automated>
  </verify>
  <done>ColumnMappingStep exists, type-checks, and implements the mapping table + enum sub-mapping + Continue-button gating contract exactly per 07-UI-SPEC.md, gated against the complete distinct-value set for every column in the uploaded file rather than a preview sample.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser file input -> uploadImportFile Server Action | The dropzone/file input is the entry point for untrusted file content into the Server Action layer built in Plan 07-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Information Disclosure / DoS | UploadStep accepting an arbitrarily large or malformed file client-side before the Server Action's own guards run | accept | The `accept=".csv"` file-input hint is a UX affordance only, not a security boundary — the real enforcement (bodySizeLimit, row-count cap, parse-failure handling) lives entirely in Plan 07-06's uploadImportFile, which this component calls unconditionally for any selected file |
| T-07-05 | Tampering | ColumnMappingStep silently committing an unmapped enum value | mitigate | The Continue button's disabled state is computed from an explicit check that every enum-mapped column's every distinct raw value in columnValues (the full-file set, not a sample) has a non-null valueMapping entry — no enum value can reach validateImportBatch as an implicit/default mapping. validateImportBatch (Plan 07-06) additionally treats any gap in this contract as a server-side per-row error, not a silent pass-through, as defense in depth |
</threat_model>

<verification>
Run npx tsc --noEmit after each task. Manual smoke test (per human-check above) once Plan 07-10 wires these into a live route: upload a real CSV, confirm the Map step's pre-populated mapping and enum sub-mapping render correctly, including for enum values that only appear past the first 5 rows.
</verification>

<success_criteria>
- All 3 components exist, type-check, and match 07-UI-SPEC.md's Upload/Map step contracts exactly
- ColumnMappingStep gates Continue against the complete per-column distinct-value set (columnValues), never a sampleRows-derived subset
- Neither component manages its own step-navigation state — both delegate via callback props to the wizard shell (Plan 07-10)
</success_criteria>

<output>
Create `.planning/phases/07-csv-import/07-07-SUMMARY.md` when done
</output>
</output>
