---
phase: 14-contrast-audit-uat-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/contrast.ts
  - src/lib/contrast.test.ts
  - .planning/phases/14-contrast-audit-uat-matrix/artifacts/
  - .planning/phases/14-contrast-audit-uat-matrix/14-UAT.md
  - .planning/phases/14-contrast-audit-uat-matrix/fixtures/seed-pending-proposal.ts
autonomous: false
requirements: [QLTY-03]
user_setup:
  - service: clerk (dev instance)
    why: "The Playwright driver must pass requireStaffAccess() → /sign-in to reach the sidebar under test (D-01/D-02, RESEARCH Open Question 1)"
    dashboard_config:
      - task: "Provide dev test-user credentials (email/password) for the hosted <SignIn /> flow, or an operator-established authenticated session the driver reuses (Phase-5 precedent)"
        location: "Clerk Dashboard → dev instance (pk_test_ keys in .env.local) → Users"

must_haves:
  truths:
    - "All 12 matrix cells (expanded/collapsed/mobile × /, /companies, /personas, /reviews) are recorded in 14-UAT.md with an active-pill assertion (exactly one data-active=true row whose href matches the route; other three rows data-active=false) and a screenshot in artifacts/"
    - "Interaction micro-tests are recorded: collapse/expand via header button + ⌘B (sidebar_state cookie), drag-resize 200-400 clamp + sidebar_width cookie, rail tooltips incl 'Reviews (N)', and pendingCount badge/dot gating (count=0 → no badge; fixture → '1 pending' badge + dot + 'Reviews (1)' tooltip, then cleanup)"
    - "The WCAG contrast math is unit-locked in src/lib/contrast.ts + contrast.test.ts (4 cases: 12.30 text-on-panel, 4.89 alpha-composited /70, 3.11 pill fill, 1.09 Exa-trap)"
    - "14-UAT.md is status: complete with Summary counts (total/passed/issues) and screenshot refs per cell, following the UAT.md template"
  artifacts:
    - path: "src/lib/contrast.ts"
      provides: "WCAG relative-luminance/contrast-ratio/composite-alpha pure functions (D-04 math lock)"
      exports: ["relativeLuminance", "contrastRatio", "compositeAlpha"]
    - path: "src/lib/contrast.test.ts"
      provides: "Vitest lock on the verified ratios"
      contains: "contrastRatio"
    - path: ".planning/phases/14-contrast-audit-uat-matrix/artifacts/"
      provides: "Screenshot evidence store (D-03)"
      contains: "cell-expanded-start.png, cell-collapsed-companies.png, cell-mobile-reviews.png (12 total)"
    - path: ".planning/phases/14-contrast-audit-uat-matrix/14-UAT.md"
      provides: "The 12-cell matrix + interaction micro-tests, status complete (D-08)"
      contains: "result: pass"
  key_links:
    - from: "14-UAT.md"
      to: "artifacts/cell-{state}-{route}.png"
      via: "screenshot filename refs"
      pattern: "artifacts/cell-"
    - from: "Playwright browser_evaluate"
      to: "[data-sidebar=\"menu-button\"]"
      via: "active-pill assertion (data-active)"
      pattern: "data-active"
    - from: "Playwright browser_evaluate"
      to: "document.cookie"
      via: "sidebar_state / sidebar_width cookie reads"
      pattern: "sidebar_(state|width)"
---

<objective>
Produce the live-browser UAT evidence for the shipped sidebar: the WCAG-math helper that locks the audit's core math, the 12-cell matrix (expanded/collapsed/mobile × 4 routes) with active/inactive assertions and screenshots, and the interaction micro-tests (collapse button, ⌘B, drag-resize clamp, rail tooltips, badge/dot gating) — all recorded in 14-UAT.md (status: complete).

Purpose: Roadmap SC #1 (live-browser UAT matrix) + QLTY-03. This is the v1.2 capstone's primary evidence artifact — the matrix the milestone audit will verify, replicating the v1.1 Phase-5 live-Playwright pattern.
Output: src/lib/contrast.ts + src/lib/contrast.test.ts, artifacts/cell-{state}-{route}.png × 12, fixtures/seed-pending-proposal.ts, 14-UAT.md (complete).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/templates/UAT.md
</execution_context>

<context>
@.planning/phases/14-contrast-audit-uat-matrix/14-CONTEXT.md
@.planning/phases/14-contrast-audit-uat-matrix/14-RESEARCH.md
@.planning/phases/14-contrast-audit-uat-matrix/14-PATTERNS.md
@.planning/phases/13-collapse-resize-coexistence/13-01-SUMMARY.md
@.planning/phases/13-collapse-resize-coexistence/13-CONTEXT.md
@.planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md
@src/components/layout/app-sidebar.tsx
@src/components/ui/sidebar.tsx
@src/components/layout/app-shell-layout.tsx
@src/lib/db/queries/proposals.ts
@src/lib/db/schema.ts

The Playwright driver surface is the `playwright` builtin skill — invoke its `browser_*` tools via `skill_mcp` (mcp_name="playwright"). NEVER add @playwright/test to package.json (zero-package hard constraint). Screenshots must land in artifacts/ (D-03) — the .playwright-mcp/ dir is gitignored (Pitfall 1).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract WCAG contrast math (src/lib/contrast.ts) + Vitest lock + create artifacts/ dir</name>
  <files>src/lib/contrast.ts, src/lib/contrast.test.ts, .planning/phases/14-contrast-audit-uat-matrix/artifacts/</files>
  <read_first>
    - src/lib/sidebar-collapse.ts (the phase-family pure-function template — whole-file analog)
    - src/lib/sidebar-collapse.test.ts (the test-file template — imports, describe/it shape)
    - .planning/phases/14-contrast-audit-uat-matrix/14-PATTERNS.md §contrast.ts (whole-file template verbatim, lines 137-175)
    - .planning/phases/14-contrast-audit-uat-matrix/14-RESEARCH.md §Code Examples (W3C WCAG 2.2 math, verified ratios table)
    - vitest.config.ts (include: ['src/**/*.test.ts'], node env)
  </read_first>
  <behavior>
    - Test 1: contrastRatio([0x33,0x33,0x33], [0xfb,0xfc,0xfd]) ≈ 12.30 (text-on-panel, ≥4.5)
    - Test 2: compositeAlpha([51,51,51], [251,252,253], 0.7) → [70,71,70]; contrastRatio([70,71,70], [0xfb,0xfc,0xfd]) ≈ 4.89 (the /70 label, Pitfall 2 lock)
    - Test 3: contrastRatio([0x90,0x90,0x90], [0xfb,0xfc,0xfd]) ≈ 3.11 (pill fill, ≥3.0)
    - Test 4: compositeAlpha([0,0,0], [0xfb,0xfc,0xfd], 0.04) ratio ≈ 1.09 (Exa trap — documents the D-07 deliberate divergence numerically)
  </behavior>
  <action>
    Create `.planning/phases/14-contrast-audit-uat-matrix/artifacts/` (mkdir -p) — the D-03 screenshot evidence store; the dir must exist before any cell runs.

    Create `src/lib/contrast.ts` per the 14-PATTERNS.md whole-file template: three named exports only (repo convention — no default exports), dependency-free total functions, strict TypeScript tuples `[number, number, number]`:
    - `relativeLuminance(rgb: [number, number, number]): number` — sRGB → linear (s <= 0.03928 ? s/12.92 : Math.pow((s+0.055)/1.055, 2.4)), then 0.2126/0.7152/0.0722 weighted sum (W3C WCAG 2.2 §1.4.3 definition).
    - `contrastRatio(a, b): number` — sort luminances desc, `(L1+0.05)/(L2+0.05)`.
    - `compositeAlpha(fg, bg, alpha): [number, number, number]` — `fg[i]*alpha + bg[i]*(1-alpha)` rounded; the /70 label renders rgba(51,51,51,0.7) and MUST be blended over the panel before any ratio (Pitfall 2).
    Style: single quotes, semicolons, 2-space indent, camelCase, no JSDoc. Add ONE why-comment above the exports explaining this locks the audit's math so the W3C formula cannot drift (mirroring sidebar-collapse.ts's comment density), mentioning the /70 alpha-compositing requirement. Do NOT reference `--sidebar-*` token names in the comment (QLTY-04 comment hygiene: no swept class/token strings).

    Create `src/lib/contrast.test.ts` mirroring sidebar-collapse.test.ts: `import { describe, it, expect } from 'vitest';` + `import { relativeLuminance, contrastRatio, compositeAlpha } from './contrast';` (relative import — repo test convention). One describe per export; one it per case; expect(...).toBeCloseTo(expected, 2) for ratios and toEqual for the compositeAlpha tuple. The four behavior cases above lock the RESEARCH-verified numbers (12.30 / 4.89 / 3.11 / 1.09).

    Run `npx vitest run src/lib/contrast.test.ts --bail=1` (NOT `-x` — removed in Vitest 4) — must pass. Then `npx tsc --noEmit` — must be exit 0. Commit both files atomically.
  </action>
  <verify>
    <automated>npx vitest run src/lib/contrast.test.ts --bail=1 && npx tsc --noEmit && test -d .planning/phases/14-contrast-audit-uat-matrix/artifacts</automated>
  </verify>
  <done>contrast.test.ts passes 4/4 (12.30, 4.89, 3.11, 1.09 within 0.01); tsc exit 0; artifacts/ dir exists.</done>
  <acceptance_criteria>
    - `npx vitest run src/lib/contrast.test.ts --bail=1` → exit 0, 4/4 passed
    - `npx tsc --noEmit` → exit 0
    - `grep -c 'export function relativeLuminance' src/lib/contrast.ts` = 1; `grep -c 'export function contrastRatio' src/lib/contrast.ts` = 1; `grep -c 'export function compositeAlpha' src/lib/contrast.ts` = 1
    - `grep -Fc "from './contrast'" src/lib/contrast.test.ts` = 1 (relative import)
    - `.planning/phases/14-contrast-audit-uat-matrix/artifacts/` exists and is tracked (empty dir needs a .gitkeep or the first PNG from Task 2; add .gitkeep now if git does not track empty dirs)
    - Zero edits to any fence file (the 4 vendored UI components, globals.css, (dashboard)/layout.tsx, app-shell-layout.tsx, sidebar-resize-handle.tsx, app-sidebar.tsx, package.json, package-lock.json)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the 12-cell live matrix (expanded/collapsed/mobile × 4 routes) → 14-UAT.md</name>
  <files>.planning/phases/14-contrast-audit-uat-matrix/artifacts/, .planning/phases/14-contrast-audit-uat-matrix/14-UAT.md</files>
  <read_first>
    - .planning/phases/14-contrast-audit-uat-matrix/14-RESEARCH.md §Pattern 1 (per-cell driver sequence), §Pattern 2 example, §Code Examples (active-pill assertion script, mobile cell script), §Pitfall 1 (screenshot dir), §Pitfall 6 (capture-then-assert)
    - .planning/phases/14-contrast-audit-uat-matrix/14-PATTERNS.md §artifacts/ (screenshot write mechanics + A5 fallback), §active-pill assertion (data-active sidebar.tsx:511), §collapse entry points, §mobile cell
    - ~/.claude/get-shit-done/templates/UAT.md (canonical template — frontmatter, per-test shape, Summary counts)
    - .planning/phases/13-collapse-resize-coexistence/13-01-SUMMARY.md (the shipped collapse/letter-mark/tooltip surface under test)
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Interaction & State Contract (expected states per route)
  </read_first>
  <action>
    Start the dev server in the background (`npm run dev` → http://localhost:3000; verify it responds before driving). This is a live-browser task: drive everything via the `playwright` builtin skill (`skill_mcp`, mcp_name="playwright" → browser_navigate/browser_resize/browser_wait_for/browser_evaluate/browser_take_screenshot/browser_click/browser_snapshot).

    AUTH (before any cell): navigate to http://localhost:3000. If already authenticated (session persisted from a prior Playwright run), proceed. If redirected to /sign-in, drive the real Clerk hosted sign-in with the dev test-user credentials from user_setup (browser_type the email + password fields, submit). If no credentials are available, raise a dynamic auth gate (per the auth-gates mechanism) for the operator to authenticate or provide credentials — never print secrets; never screenshot the sign-in form with filled credentials. Confirm authentication by landing on a dashboard route.

    THE 12 CELLS — for each (state, route) pair, capture-then-assert (Pitfall 6: screenshot BEFORE asserting so a failing cell still has evidence), and record one `### N.` block in 14-UAT.md per the template with `expected:` (the active-pill contract) and `result:` (pass, or issue + severity + screenshot ref). Routes: / (start), /companies, /personas, /reviews. States:
    - EXPANDED (4 cells): `browser_resize(1280, 800)`, navigate to route, `browser_wait_for` page render, then `browser_evaluate` the active-pill assertion:
      `() => [...document.querySelectorAll('[data-sidebar="menu-button"]')].map(r => ({ href: r.querySelector('a')?.getAttribute('href'), active: r.getAttribute('data-active') === 'true' }))`
      Expect exactly one row with active=true whose href matches the route; the other three false. Screenshot → `artifacts/cell-expanded-{start|companies|personas|reviews}.png`.
    - COLLAPSED (4 cells): at 1280x800, click the header button `browser_click(target: button "Collapse sidebar")` (aria-label, app-sidebar.tsx:87) → `browser_wait_for(time: 0.4)` for the 200ms width transition → assert `[data-slot="sidebar-wrapper"]` dataset.state === 'collapsed' and `getComputedStyle('[data-sidebar="sidebar-inner"]').width === '48px'` via browser_evaluate → assert the same one-active-row contract → screenshot → `artifacts/cell-collapsed-{route}.png`. Expand back before the next cell (click "Expand sidebar") or start each collapsed cell from a fresh navigate+click.
    - MOBILE (4 cells): `browser_resize(375, 800)` (below the 768px useIsMobile breakpoint) → navigate to route → `browser_click(target: button "Toggle Sidebar")` (SidebarTrigger, sidebar.tsx:253-277) to open the Sheet → `browser_wait_for` the sheet → assert SheetContent has `data-mobile="true"` and the same one-active-row contract inside the sheet → screenshot → `artifacts/cell-mobile-{route}.png`. Close the sheet before navigating to the next route (click "Toggle Sidebar" again or Esc).

    SCREENSHOT WRITE (Pitfall 1): pass `filename: ".planning/phases/14-contrast-audit-uat-matrix/artifacts/cell-{state}-{route}.png"` (type: "png", scale: "css") to browser_take_screenshot. If the MCP server confines filenames to `.playwright-mcp/` (A5), capture with the default name and `cp`/move the PNG from `.playwright-mcp/` into artifacts/ after each capture. Verify `git status` shows the artifacts PNGs tracked before finishing (gitignore:42 excludes .playwright-mcp/ only).

    WRITE 14-UAT.md: create it in this task with the 12 matrix cells recorded. Frontmatter: status: testing (flip to complete in Task 3), phase: 14-contrast-audit-uat-matrix, source: [13-01-SUMMARY.md, 11-01-SUMMARY.md, 12-01-SUMMARY.md, 10-02-SUMMARY.md], started/updated ISO timestamps. Per-test blocks: `### N. {State} viewport — {route} route active pill` with expected/result + evidence (screenshot ref + browser_evaluate output). Update Summary counts (total: 12, passed: N, issues: M) — a failed cell is a recorded issue, NOT a session abort (Pitfall 6). Leave the Gaps section empty (or filled with severity if a real issue is found — D-08 allows a minimal scoped fix if a defect is real; record the fix in the same cell's evidence).
  </action>
  <verify>
    <automated>test "$(ls .planning/phases/14-contrast-audit-uat-matrix/artifacts/cell-*.png | wc -l | tr -d ' ')" = "12" && grep -c "result:" .planning/phases/14-contrast-audit-uat-matrix/14-UAT.md</automated>
  </verify>
  <done>All 12 screenshots exist under artifacts/ with cell-{state}-{route}.png naming; 14-UAT.md has 12 recorded cells each with expected + result + screenshot ref; exactly one active row per route verified live.</done>
  <acceptance_criteria>
    - `ls .planning/phases/14-contrast-audit-uat-matrix/artifacts/cell-*.png` → exactly 12 files matching cell-{expanded|collapsed|mobile}-{start|companies|personas|reviews}.png
    - `git status --short .planning/phases/14-contrast-audit-uat-matrix/artifacts/` shows the PNGs tracked (not ignored)
    - 14-UAT.md contains 12 `### N.` test blocks; each has a non-pending result (pass/issue) and an `artifacts/cell-` reference
    - The recorded browser_evaluate output for every cell shows exactly one row with active=true and href matching the route under test
    - No credential values, __session cookie contents, or Clerk secret keys appear anywhere in 14-UAT.md (grep for the test user's email and any `sk_test_`/`pk_test_`-like strings → 0 matches)
    - Zero edits to any fence file (app-sidebar.tsx and sidebar-resize-handle.tsx are READ-ONLY objects under test this phase — do not touch)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Interaction micro-tests (collapse button + ⌘B, drag-resize, rail tooltips, badge/dot gating) → 14-UAT.md complete</name>
  <files>.planning/phases/14-contrast-audit-uat-matrix/14-UAT.md, .planning/phases/14-contrast-audit-uat-matrix/fixtures/seed-pending-proposal.ts</files>
  <read_first>
    - .planning/phases/14-contrast-audit-uat-matrix/14-RESEARCH.md §Pattern 2 (cookie/state assertions), §Code Examples (⌘B + drag-resize live checks), §Pitfall 3 (badge gating at count=0), §Pitfall 5 (live ⌘B/cookie checks, not grep-only)
    - .planning/phases/14-contrast-audit-uat-matrix/14-PATTERNS.md §collapse entry points, §state + cookie assertions (sidebar.tsx:84-85 sidebar_state, sidebar-resize-handle.tsx:47/90 sidebar_width + role=separator), §badge/dot gating (app-sidebar.tsx:174-188), §data-fixture + safety-gate precedent (08-06-UAT)
    - src/lib/db/schema.ts (signalProposal table shape — signalTypeEnum values, strength enum, NOT NULL fields)
    - src/lib/db/queries/proposals.ts (countPendingProposals — the count the badge reflects)
    - .planning/phases/13-collapse-resize-coexistence/13-01-SUMMARY.md (getNavTooltipLabel — 'Reviews (N)' only when pendingCount > 0)
    - ~/.claude/get-shit-done/templates/UAT.md (Summary/Gaps sections)
  </read_first>
  <action>
    Continue the live session from Task 2 (dev server + authenticated Playwright browser). Drive via the `playwright` builtin skill (`skill_mcp`). Record each micro-test as a `### N.` block appended to 14-UAT.md with expected/result.

    M1 — Collapse/expand via the header button: click `button "Collapse sidebar"` → assert wrapper data-state='collapsed' + rail width 48px → click `button "Expand sidebar"` → assert data-state='expanded' + width restores (256px default or the persisted sidebar_width).
    M2 — ⌘B toggle + sidebar_state cookie: `browser_press_key(key: "Meta+b")` → assert data-state='collapsed' AND `document.cookie` contains `sidebar_state=false`; press again → data-state='expanded' + `sidebar_state=true`. (Pitfall 5: the cookie write is inside the vendored setter — verify it live, don't rely on grep.)
    M3 — Drag-resize clamp + sidebar_width cookie: `browser_drag` from the handle (`role="separator"`, aria-label "Resize sidebar") — drag right ~+150px and left ~-150px; after each, assert the wrapper width clamps to [200, 400] and `document.cookie` contains `sidebar_width`. Verify the cookie persists across a page reload (navigate away and back, assert the width restores).
    M4 — Rail tooltips: collapse the sidebar; hover each of the 6 interactive icons (Start, Companies, Key Personas, Reviews, Give us feedback, user avatar) via browser_hover; assert a tooltip appears (~200ms delay, D-09) with the expected copy (verbatim labels; Reviews shows 'Reviews' at count=0, 'Reviews (N)' at count>0; feedback pill 'Give us feedback'; avatar shows getUserDisplayName). Screenshot at least the Reviews tooltip for evidence.
    M5 — pendingCount badge/dot gating (two-branch, per RESEARCH Open Question 2 — the fixture path proves both branches):
      Branch A (count=0, current live state): on /reviews assert NO `[role="status"]` badge and NO collapsed-rail dot (app-sidebar.tsx:174-188 `pendingCount > 0` gate).
      Branch B (count>0): create `fixtures/seed-pending-proposal.ts` — a one-off tsx script using `dotenv` (load .env.local) + `@neondatabase/serverless`/drizzle `db` (both installed; zero new packages). SAFETY GATE (T-14-02, 08-06-UAT precedent): before any write, compute `sha256(DATABASE_URL)` and confirm the connection is the dev/QA instance (the .env.local dev connection used by local dev — verify the host/branch matches the dev Neon project, NOT a production URL; log the SHA-256 prefix; abort with exit 1 if it cannot be confirmed as dev). The script inserts ONE signalProposal row: companyId = an existing company id (SELECT one), signalType 'cost_pressure', strength 'medium', detectedAt today's date, evidenceUrl 'https://example.com/evidence', reliability 'R1', confidence 'C1', evidenceSnippet 'Phase 14 UAT badge fixture', reasoning 'Phase 14 UAT fixture — badge gating proof'. status defaults to 'pending' at the DB level. Print only the inserted row id (never the connection string or secrets). Then reload /reviews in the browser → assert the badge `role="status"` with text "1 pending" + the collapsed-rail dot is visible on the collapsed sidebar + the Reviews tooltip shows 'Reviews (1)'. Screenshot for evidence. CLEANUP: delete the fixture row by id (or a `--cleanup` mode in the same script); reload and assert the badge is gone (count back to 0).
      If the DB gate cannot confirm the dev instance, DO NOT insert — record Branch B as `skipped` with reason 'dev-DB gate failed' and keep the count=0 assertion as the recorded branch (documented in 14-UAT.md).

    COMPLETE 14-UAT.md: set frontmatter status: complete, Current Test → "[testing complete]", update Summary counts to include all matrix + interaction tests (total/passed/issues/skipped). Append a Gaps section only if a real issue was found (severity + screenshot ref; a fix, if applied, is recorded in the same cell's evidence per D-08 discretion).
  </action>
  <verify>
    <automated>grep -c "status: complete" .planning/phases/14-contrast-audit-uat-matrix/14-UAT.md && test -f .planning/phases/14-contrast-audit-uat-matrix/fixtures/seed-pending-proposal.ts && npx vitest run src/lib/contrast.test.ts --bail=1</automated>
  </verify>
  <done>14-UAT.md is status: complete with matrix + interaction tests recorded; badge gating proven both branches (or Branch B explicitly skipped with reason); fixture script committed with SHA-256 gate; no fixture residue (count back to 0); no secrets in artifacts.</done>
  <acceptance_criteria>
    - `grep -c "status: complete" .planning/phases/14-contrast-audit-uat-matrix/14-UAT.md` = 1 and `grep -c "testing complete" .planning/phases/14-contrast-audit-uat-matrix/14-UAT.md` = 1
    - 14-UAT.md has ≥5 interaction test blocks (M1..M5) each with a non-pending result
    - fixtures/seed-pending-proposal.ts contains a SHA-256 gate (grep -c 'sha256\|createHash' = ≥1) and inserts exactly one row referencing a real companyId; cleanup deletes it
    - Evidence recorded: sidebar_state cookie flips (Meta+b), sidebar_width cookie written (drag), tooltip text for Reviews captured
    - `grep -cE "sk_test_|pk_test_|__session" .planning/phases/14-contrast-audit-uat-matrix/14-UAT.md` = 0 (no secrets — T-14-01)
    - Zero edits to any fence file (the 4 vendored UI components, globals.css, (dashboard)/layout.tsx, app-shell-layout.tsx, sidebar-resize-handle.tsx, app-sidebar.tsx, package.json, package-lock.json)
    - `npx vitest run src/lib/contrast.test.ts --bail=1` still green (4/4)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser driver → app | Untrusted automation driving the authenticated app; the driver handles real credentials during sign-in |
| app → Neon dev DB | The fixture script writes to the dev database — wrong-target writes corrupt QA data |
| evidence artifacts → repo | 14-UAT.md + screenshots are committed; credential material must never enter them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information Disclosure | 14-UAT.md, artifacts/*.png, fixtures/ | mitigate | Never print credential values / __session cookie contents / Clerk keys in artifacts (grep gate: `grep -cE "sk_test_\|pk_test_\|__session" 14-UAT.md` = 0). Screenshots of the authenticated app are fine (they show the app, not credentials — 08-06-UAT safety-gate precedent). The fixture script prints only the inserted row id |
| T-14-02 | Tampering | fixtures/seed-pending-proposal.ts → Neon dev DB | mitigate | SHA-256 gate on DATABASE_URL before any insert: confirm the connection is the dev/QA instance (host/branch matches the .env.local dev connection, NOT production); abort exit 1 on mismatch. Insert + assert + delete in the same task; verify count returns to 0 (no residue). Never commit the connection string or secrets |
| T-14-03 | Information Disclosure | Exa reference sampling (Plan 14-02) | mitigate | Element sampling only via browser_evaluate; never hotlink Exa assets or commit Exa item screenshots (divergence list documents what is NOT copied — our sidebar ships none, fence-proven) |
| T-14-04 | Spoofing (auth bypass) | requireStaffAccess gate (Plan 14-02 regression) | mitigate | Fence proves auth files byte-identical + a live negative check: unauthenticated navigation to a dashboard route redirects to /sign-in |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase (hard constraint); package.json/package-lock.json sit in the fence diff — any accidental install fails the fence. RESEARCH §Package Legitimacy Audit = N/A by exemption (nothing installed) |
</threat_model>

<verification>
- Task 1: `npx vitest run src/lib/contrast.test.ts --bail=1` (4/4) + `npx tsc --noEmit` (exit 0) — the math lock gates the audit in Plan 14-02.
- Task 2: 12 artifacts PNGs present + tracked; 14-UAT.md has 12 recorded cells with per-route active-pill evidence.
- Task 3: 14-UAT.md status: complete; interaction tests recorded; badge fixture inserted/asserted/cleaned (or Branch B skipped with reason); no secrets in artifacts.
- After each task commit: `npx tsc --noEmit` stays green; fence files untouched (`git diff 17852205dc2bd6fe3f4f8df593769f0e4075cd50 HEAD -- <11 frozen files>` empty — the base is the docs-chain end before this plan's source commits; resolve the exact base via `git log` as the commit immediately before the first Phase-14 source commit).
- Live-browser work runs as committed batches (per-cell capture-then-assert, Pitfall 6); no `&&`-chained verify where a red cell would abort the whole matrix.
</verification>

<success_criteria>
- All 12 cells pass live (or recorded issues with severity + screenshot refs — failure is evidence, not an abort)
- Interaction micro-tests M1-M5 recorded with live evidence (cookies actually flip, tooltips actually appear, badge actually gates)
- 14-UAT.md status: complete, Summary counts match the recorded tests, screenshot refs all resolve under artifacts/
- src/lib/contrast.ts + contrast.test.ts committed and green; artifacts/ PNGs tracked in git
- Zero edits to the 11 frozen files; zero new npm packages
</success_criteria>

<output>
Create `.planning/phases/14-contrast-audit-uat-matrix/14-01-SUMMARY.md` when done
</output>
