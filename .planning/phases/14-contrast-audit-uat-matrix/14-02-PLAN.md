---
phase: 14-contrast-audit-uat-matrix
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - .planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md
  - .planning/phases/14-contrast-audit-uat-matrix/14-SUMMARY.md
autonomous: true
requirements: [QLTY-03]

must_haves:
  truths:
    - "All 6 shipped token pairs pass their AA thresholds as verified live in the browser (text-on-panel ≥4.5:1, /70 alpha-composited label ≥4.5:1, active pill #909090/#111 ≥3:1, focus ring ≥3:1, badge chip ≥3:1, letter-mark #333/white ≥4.5:1) — recorded per pair with the sampled computed colors and ratio in 14-VERIFICATION.md"
    - "The Exa divergence review records an element-wise pass/fail (near-white panel, hairline border, gray active treatment, mono badge) against a live dashboard.exa.ai fetch (or the dated FEATURES.md fallback), plus a deliberate-divergences list (Exa nav items/assets NOT copied, the ~1.05:1 contrast trap) each with a rationale"
    - "Hard-constraint regression evidence is recorded: 11-file fence diff empty, QLTY-04 sweep clean, tsc/npm test/npm run build green, and the live regression rows (unauthenticated → /sign-in, ⌘B cookie, drag cookie, badge gating) cited from 14-UAT.md"
    - "14-SUMMARY.md exists with the phase closeout (QLTY-03 delivered, artifacts list, no production source changes beyond the optional contrast helper)"
  artifacts:
    - path: ".planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md"
      provides: "Contrast audit + Exa review + regression evidence (D-08)"
      contains: "12.30, 4.89, 3.11, 5.91, 4.30, 12.63"
    - path: ".planning/phases/14-contrast-audit-uat-matrix/14-SUMMARY.md"
      provides: "Phase closeout summary (D-08)"
      contains: "QLTY-03"
  key_links:
    - from: "14-VERIFICATION.md"
      to: "src/lib/contrast.ts"
      via: "the unit-locked WCAG math the live audit reuses"
      pattern: "contrast"
    - from: "14-VERIFICATION.md"
      to: "FEATURES.md"
      via: "the dated fallback evidence for the Exa fetch (Pitfall 4)"
      pattern: "FEATURES"
    - from: "14-VERIFICATION.md"
      to: "14-UAT.md"
      via: "cross-referenced live regression rows (⌘B cookie, drag cookie, badge gating)"
      pattern: "14-UAT"
---

<objective>
Produce the verification evidence: the live computed-style WCAG AA contrast audit of all 6 shipped token pairs (D-04/D-05), the Exa divergence review against a live dashboard.exa.ai fetch with the documented fallback (D-06/D-07), and the hard-constraint regression battery (fence/sweep/build/suite + live regression rows) — all recorded in 14-VERIFICATION.md, then close the phase with 14-SUMMARY.md.

Purpose: Roadmap SC #2 (contrast audit), SC #3 (divergence review), SC #4 (regression) + QLTY-03. This plan converts the shipped token contract's claimed AA ratios into proven, browser-sampled evidence and proves the frozen regression surface byte-identical plus behaviorally intact.
Output: 14-VERIFICATION.md, 14-SUMMARY.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-contrast-audit-uat-matrix/14-CONTEXT.md
@.planning/phases/14-contrast-audit-uat-matrix/14-RESEARCH.md
@.planning/phases/14-contrast-audit-uat-matrix/14-PATTERNS.md
@.planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md
@.planning/research/FEATURES.md
@.planning/phases/13-collapse-resize-coexistence/13-01-SUMMARY.md
@src/app/globals.css
@src/components/layout/app-sidebar.tsx
@src/lib/contrast.ts

Live-browser tasks reuse the `playwright` builtin skill (skill_mcp → browser_*). The contrast audit samples computed styles via browser_evaluate (Pattern 2); the Exa fetch goes through the real browser (curl is 429-challenge-gated — Pitfall 4). PHASE_BASE_SHA = the commit immediately before the first Phase-14 source commit; resolve it via `git log` at the start of 14-01 Task 1 (currently the plan-approval commit `fad02962` — the docs-chain end; verify the resolved base is the parent of the first feat(14-01) commit). Use the same resolved base for ALL base-vs-HEAD ranges.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live computed-style contrast audit of all 6 shipped token pairs → 14-VERIFICATION.md</name>
  <files>.planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/14-contrast-audit-uat-matrix/14-RESEARCH.md §Pattern 2 (computed-style sampling with alpha compositing), §Code Examples (verified ratios table + the audit script shape)
    - .planning/phases/14-contrast-audit-uat-matrix/14-PATTERNS.md §contrast-pair sampling elements (where each pair's computed styles are read)
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Token Contract + §Color (the claimed AA values being proven)
    - src/app/globals.css (token block lines ~87-96: --sidebar #fbfcfd, --sidebar-foreground #333333, --sidebar-accent #909090, --sidebar-accent-foreground #111111, --sidebar-primary #333333, --sidebar-primary-foreground #ffffff, --sidebar-ring #787878)
    - src/lib/contrast.ts (the unit-locked math — inline the same formulas in the browser_evaluate script)
  </read_first>
  <action>
    Reuse the dev server + authenticated browser session from Plan 14-01 (or restart `npm run dev` + authenticate via the same sign-in path). This is a live-browser task — drive via the `playwright` builtin skill (`skill_mcp`).

    Run ONE parametrized `browser_evaluate` script (Pattern 2) that, for each of the 6 D-05 pairs, resolves the two colors from getComputedStyle on the shipped rendered element, composites any alpha over the panel, computes the WCAG ratio (the same W3C formulas as src/lib/contrast.ts, inlined — the helper unit-locks the math, the browser script applies it to live styles), and returns `{pair, colorA, colorB, ratio, threshold, pass}`:

    1. text-on-panel: panel `getComputedStyle('[data-sidebar="sidebar"]').backgroundColor` (expect rgb(251,252,253)) vs a nav-row label span `color` (expect rgb(51,51,51)); threshold 4.5 (1.4.3); expect ratio ≈12.30
    2. /70 label: the org sub-label `text-sidebar-foreground/70` (app-sidebar.tsx:110) — its computed color is rgba(51,51,51,0.7); MUST alpha-composite `0.7*fg + 0.3*panel` BEFORE the ratio (Pitfall 2 — naive rgba-vs-panel is wrong); threshold 4.5; expect ≈4.89
    3. active pill fill (non-text): `getComputedStyle('[data-sidebar="menu-button"][data-active="true"]').backgroundColor` (expect rgb(144,144,144)) vs panel; threshold 3.0 (1.4.11); expect ≈3.11; ALSO sample the pill's `color` (expect rgb(17,17,17)) vs the pill fill — threshold 3.0; expect ≈5.91
    4. focus ring vs panel: `--sidebar-ring` value #787878 (globals.css:95) vs panel — sample via getComputedStyle on a focused row's outline-color if rendered, else resolve the token from a computed style probe; threshold 3.0 (1.4.11); expect ≈4.30
    5. badge chip: `bg-sidebar-accent` chip (app-sidebar.tsx:179) fill vs panel — threshold 3.0; expect ≈3.11; badge text `text-sidebar-accent-foreground` vs chip fill — threshold 4.5 (1.4.3); expect ≈5.91
    6. letter-mark: `bg-sidebar-primary text-sidebar-primary-foreground` box (app-sidebar.tsx:103) — #333333 vs #ffffff; threshold 4.5; expect ≈12.63

    If any element is not rendered in the current state (e.g. the badge needs pendingCount>0 — use the fixture from Plan 14-01 if needed, or sample the badge chip in the collapsed-rail letter-mark context), sample the closest rendered equivalent and note the substitution in the record. A live assertion failing its threshold is evidence of a real defect — record it with the sampled values (D-08 allows a minimal scoped fix; if you fix, re-run the pair and record both values).

    WRITE 14-VERIFICATION.md: frontmatter (phase: 14-contrast-audit-uat-matrix, verified: ISO timestamp, status: passed, score: N/6 pairs + M/7 elements verified, overrides_applied: 0), a Goal Achievement section with the Observable Truths table (05-VERIFICATION.md shape — one row per pair with ✓ VERIFIED + evidence), and the contrast-audit table: | Pair | Colors | Threshold | Verified ratio | Pass | (the 6 D-05 rows + the 2 sub-checks: pill text, badge text). Record the sampled computed colors, not just the ratios.
  </action>
  <verify>
    <automated>test "$(grep -cE '12\.30|4\.89|3\.11|5\.91|4\.30|12\.63' .planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md | tr -d ' ')" -ge "6" && test "$(grep -c 'ratio' .planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md | tr -d ' ')" -ge "7"</automated>
  </verify>
  <done>14-VERIFICATION.md records all 6 pairs + 2 sub-checks with sampled colors, ratio, threshold, pass; every pair's ratio ≥ its AA threshold; the /70 pair's record shows the alpha composite.</done>
  <acceptance_criteria>
    - `grep -cE "12\.30|4\.89|3\.11|5\.91|4\.30|12\.63" .planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md` ≥ 6 (all verified ratios present within 0.1)
    - The 14-VERIFICATION.md contrast table has ≥7 rows (6 D-05 pairs + ≥1 sub-check) each with: colors, threshold, verified ratio, pass verdict
    - The /70 row's evidence mentions alpha compositing (grep -i 'composite\|blend' 14-VERIFICATION.md ≥ 1) — proving Pitfall 2 was handled
    - No pair recorded below its threshold without an accompanying defect record + fix evidence (per D-08 discretion)
    - `npx vitest run src/lib/contrast.test.ts --bail=1` still green (the audit's math matches the locked helper)
    - No secrets in 14-VERIFICATION.md (grep -cE "sk_test_|pk_test_|__session" = 0)
    - Zero edits to any fence file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Exa divergence review (live dashboard.exa.ai fetch + element-wise comparison) → 14-VERIFICATION.md</name>
  <files>.planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/14-contrast-audit-uat-matrix/14-RESEARCH.md §Pattern 3 (live fetch with documented fallback), §Pitfall 4 (Vercel Security Checkpoint 429), §Code Examples (comparison targets table)
    - .planning/research/FEATURES.md (the dated 2026-08-01 captured reference values — the fallback evidence)
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Divergences table (the deliberate divergences: #909090 active fill, #333333 text, #787878 ring, /70 label)
    - .planning/phases/14-contrast-audit-uat-matrix/14-PATTERNS.md §Exa divergence table (element-wise comparison shape)
  </read_first>
  <action>
    LIVE FETCH (D-06): via the `playwright` builtin skill, `browser_navigate("https://dashboard.exa.ai")` — the real Chromium executes the Vercel Security Checkpoint JS that blocks curl (verified 429). `browser_wait_for` the app shell. If the app shell renders, sample via `browser_evaluate`:
    - panel background (expect ~#fbfcfd),
    - the sidebar's right border (hairline: width + color, expect ~0.5px gray-200 #e5e7eb),
    - the active nav row's fill treatment (expect the ~rgba(0,0,0,0.04) whisper-gray — record the sampled value),
    - the badge font (mono 10px/600 accent chip).
    Record the sampled values (colors/widths as computed) — do NOT hotlink Exa assets and do NOT commit Exa item screenshots (T-14-03; the divergence list documents what is NOT copied).

    FALLBACK (Pitfall 4): if the live fetch is blocked (Vercel Security Checkpoint page, HTTP 429, auth wall, or the shell never renders), record the block explicitly (what was observed — e.g. snapshot text, network status) and use the FEATURES.md captured values (dated 2026-08-01) as the documented fallback evidence. State clearly in 14-VERIFICATION.md that the values are the dated fallback, not a live sample.

    ELEMENT-WISE PASS/FAIL TABLE (D-07): compare each of the 4 matched elements against our shipped values —
    | Element | Exa reference (live or FEATURES.md fallback) | ArcLumen shipped | Verdict |
    - Panel: Exa ~#fbfcfd vs our --sidebar: #fbfcfd (globals.css:88) → match
    - Hairline: Exa 0.5px gray-200 vs our border-right-width: 0.5px + --sidebar-border: #e5e7eb (globals.css:100-103) → match
    - Active fill: Exa rgba(0,0,0,0.04) ≈1.09:1 (FAILS 1.4.11) vs our --sidebar-accent: #909090 3.11:1 (passes) → DELIBERATE DIVERGENCE
    - Badge: Exa mono 10px/600 accent chip vs our font-mono text-[10px] font-semibold + sidebar-accent (app-sidebar.tsx:179) → match (anatomy; no asset copied)

    DELIBERATE-DIVERGENCES LIST (D-07, each with a rationale): (1) the active-pill fill — Exa's ~1.05:1 trap fails WCAG 1.4.11; we ship #909090 3.11:1 for a daily-use internal tool (10-UI-SPEC divergence table). (2) Exa's own nav items / asset URLs are NOT copied — our 4 routes are unchanged (Start, Companies, Key Personas, Reviews; getActiveNavKey contract), zero hotlinked assets (grep evidence: no http(s) asset references in app-sidebar.tsx). (3) Exa's nav-item set/intent grouping is not copied — our Explore/Manage grouping ships with our own labels per the Copywriting Contract.
    Append both the element-wise table and the divergence list to 14-VERIFICATION.md under an "Exa Divergence Review" section.
  </action>
  <verify>
    <automated>test "$(grep -c 'DELIBERATE DIVERGENCE' .planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md | tr -d ' ')" -ge "1" && test "$(grep -cE 'FEATURES|live fetch|fallback' .planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md | tr -d ' ')" -ge "1"</automated>
  </verify>
  <done>14-VERIFICATION.md has the element-wise table (4 rows) + divergence list (≥3 items with rationale); the live fetch result or the explicit dated fallback is recorded.</done>
  <acceptance_criteria>
    - `grep -c "DELIBERATE DIVERGENCE" .planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md` ≥ 1 (the active-fill divergence documented)
    - The element-wise table has ≥4 rows (panel, hairline, active fill, badge) each with reference value + shipped value + verdict
    - The divergence list has ≥3 items, each with a rationale sentence
    - If the live fetch was blocked: the block is explicitly recorded (challenge/429/auth-wall observation) and the fallback source is named as FEATURES.md dated 2026-08-01
    - No Exa asset URLs copied into any repo file (grep -cE "exa\.ai/.*\.(png|svg|jpg|jpeg|webp)" src/ = 0)
    - No secrets; zero edits to any fence file
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Hard-constraint regression battery (fence/sweep/build/suite + live regression rows) + 14-SUMMARY.md closeout</name>
  <files>.planning/phases/14-contrast-audit-uat-matrix/14-VERIFICATION.md, .planning/phases/14-contrast-audit-uat-matrix/14-SUMMARY.md</files>
  <read_first>
    - .planning/phases/14-contrast-audit-uat-matrix/14-RESEARCH.md §Validation Architecture (requirement → test map), §Pitfall 5 (live checks, not grep-only)
    - .planning/phases/13-collapse-resize-coexistence/13-01-SUMMARY.md (the Phase 13 gate commands — sweep, fence, suite, build)
    - .planning/phases/14-contrast-audit-uat-matrix/14-PATTERNS.md §fence + sweep + static gates
    - ~/.claude/get-shit-done/templates/summary.md (the 14-SUMMARY.md shape)
  </read_first>
  <action>
    PHASE_BASE_SHA: resolve via `git log` as the commit immediately before the first Phase-14 source commit (currently the plan-approval commit `fad02962` — the docs-chain end; verify the resolved base is the parent of the first feat(14-01) commit). Use it for ALL base-vs-HEAD ranges below.

    Run the repo gate battery and record each result as a Behavioral Spot-Checks row in 14-VERIFICATION.md (05-VERIFICATION.md shape):
    1. FENCE: `git diff <PHASE_BASE_SHA> HEAD -- src/components/ui/sidebar.tsx src/components/ui/tooltip.tsx src/components/ui/dropdown-menu.tsx src/components/ui/button.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx src/components/layout/sidebar-resize-handle.tsx src/components/layout/app-sidebar.tsx package.json package-lock.json` → MUST be empty (11 files — note app-sidebar.tsx and sidebar-resize-handle.tsx ARE fenced this phase: READ-ONLY objects under test)
    2. SWEEP: `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"` → exit 0 (QLTY-04 clean)
    3. STATIC: `npx tsc --noEmit` → exit 0
    4. SUITE: `npm test` → exit 0 (239 passed baseline + the new contrast tests)
    5. BUILD: `npm run build` → exit 0 (12 routes, all server-rendered)

    LIVE REGRESSION ROWS (Pitfall 5 — grep proves source, the live layer proves behavior; cross-reference 14-UAT.md's already-recorded rows rather than re-running):
    - ⌘B toggles + sidebar_state cookie → cited from 14-UAT.md M2 (live-verified in Plan 14-01)
    - drag-resize clamp + sidebar_width cookie → cited from 14-UAT.md M3
    - pendingCount badge gating → cited from 14-UAT.md M5 (both branches)
    - NEGATIVE AUTH CHECK (run now, fresh browser context): open a NEW incognito-ish Playwright context (or clear cookies via browser_run_code_unsafe / a fresh browser session with no __session) and navigate to http://localhost:3000/companies → assert the request redirects to /sign-in (the requireStaffAccess gate holds — T-14-04). Record the redirect as the live auth-bypass regression row. No credentials involved; do not sign in for this check.

    WRITE 14-SUMMARY.md: the phase closeout per the summary.md template — objective, deliverables (14-UAT.md, 14-VERIFICATION.md, artifacts/, src/lib/contrast.ts + test), requirements-completed: [QLTY-03], the success-criteria checklist (all 4 roadmap SCs), key decisions (D-01..D-09 honored; the contrast helper extraction per RESEARCH Open Question 3), verification results digest (12 cells, 6 pairs, 4 elements, all gates), known limitations (the Exa fetch fallback if it was blocked; the badge fixture provenance), and the zero-production-change statement (only src/lib/contrast.ts + contrast.test.ts added — no fence file touched). Note the v1.1 deferred gaps are OUT of scope per D-09.
  </action>
  <verify>
    <automated>test -z "$(git diff fad02962 HEAD -- src/components/ui/sidebar.tsx src/components/ui/tooltip.tsx src/components/ui/dropdown-menu.tsx src/components/ui/button.tsx src/app/globals.css 'src/app/(dashboard)/layout.tsx' src/components/layout/app-shell-layout.tsx src/components/layout/sidebar-resize-handle.tsx src/components/layout/app-sidebar.tsx package.json package-lock.json)" && test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)" && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <done>All regression gates green and recorded; live negative auth check recorded; 14-SUMMARY.md exists with QLTY-03 delivered and the zero-production-change statement.</done>
  <acceptance_criteria>
    - The fence diff over the 11 files is empty (recorded in 14-VERIFICATION.md)
    - `test -z "$(grep -rnE 'indigo|amber|#[0-9a-fA-F]{3,8}|\bdark:' src/components/layout/)"` → exit 0 (sweep-clean recorded)
    - `npx tsc --noEmit` → exit 0; `npm test` → exit 0 (full suite incl. contrast tests); `npm run build` → exit 0 — all recorded as rows in 14-VERIFICATION.md
    - 14-VERIFICATION.md records the live negative auth check (unauthenticated → /sign-in redirect observed) — grep -c 'sign-in' 14-VERIFICATION.md ≥ 1
    - 14-VERIFICATION.md cross-references 14-UAT.md for the ⌘B / drag / badge live rows — grep -c '14-UAT' 14-VERIFICATION.md ≥ 1
    - 14-SUMMARY.md exists: `grep -c 'QLTY-03' 14-SUMMARY.md` ≥ 1; contains the artifacts list and the zero-production-change statement
    - git status clean except intended artifacts; no secrets anywhere
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → dashboard.exa.ai | Untrusted external site (Vercel-challenge-gated) — sampled read-only via browser_evaluate, never written to, assets never hotlinked |
| browser (fresh context) → app | Negative auth check drives an unauthenticated session against the real requireStaffAccess gate |
| evidence artifacts → repo | 14-VERIFICATION.md + 14-SUMMARY.md are committed; no secrets, no Exa asset copies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Information Disclosure / IP | Exa divergence review (browser_evaluate sampling of dashboard.exa.ai) | mitigate | Sample values only — never hotlink Exa assets, never commit Exa item screenshots. The divergence list explicitly documents what is NOT copied (nav items, assets, the ~1.05:1 trap). Grep gate: `grep -cE "exa\.ai/.*\.(png|svg|jpg|jpeg|webp)" src/` = 0 |
| T-14-04 | Spoofing (auth bypass) | requireStaffAccess gate + auth files | mitigate | 11-file fence diff empty proves auth/boundary files byte-identical (sidebar.tsx, (dashboard)/layout.tsx, app-shell-layout.tsx…), PLUS a live negative check: an unauthenticated fresh browser context navigating to /companies redirects to /sign-in |
| T-14-01 | Information Disclosure | 14-VERIFICATION.md / 14-SUMMARY.md | mitigate | No credential values, __session contents, or Clerk keys in artifacts (grep gate = 0). Screenshots of the authenticated app are fine; the negative auth check uses a fresh context with no credentials involved |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this phase; package.json/package-lock.json in the fence diff — any accidental install fails the fence. RESEARCH §Package Legitimacy Audit = N/A by exemption |
</threat_model>

<verification>
- Task 1: 6 pairs + sub-checks recorded with sampled colors/ratios; all ≥ AA thresholds; /70 alpha-composite evidenced; contrast.test.ts still green.
- Task 2: element-wise table (4 rows) + divergence list (≥3 items with rationale); live fetch result or dated FEATURES.md fallback recorded.
- Task 3: fence/sweep/tsc/test/build all green and recorded; live negative auth check recorded; 14-SUMMARY.md written.
- Sampling: per-task `npx tsc --noEmit` + targeted gates after each commit; full `npm test` + fence + sweep after the plan.
</verification>

<success_criteria>
- 14-VERIFICATION.md records: contrast audit (6 pairs + 2 sub-checks, all pass), Exa element-wise review (4 rows) + deliberate divergences (≥3, each rationaled), regression battery (5 repo gates + 4 live rows incl. the negative auth check)
- 14-SUMMARY.md exists with QLTY-03 delivered, artifacts listed, zero-production-change statement (only src/lib/contrast.ts + test added)
- Fence clean over all 11 files; sweep clean; tsc + npm test + npm run build green
- No secrets, no Exa asset copies, no fixture residue anywhere
</success_criteria>

<output>
Create `.planning/phases/14-contrast-audit-uat-matrix/14-02-SUMMARY.md` when done
</output>
