---
phase: 23-provider-registry-servable-sources
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - .env.example
autonomous: true
requirements: [REG-02]
user_setup:
  - service: vercel
    why: "REG-02/roadmap success criterion 5 names Vercel env declarations for NOUSRESEARCH_API_KEY + OPENCODE_API_KEY"
    env_vars:
      - name: NOUSRESEARCH_API_KEY
        source: "NousResearch portal (portal.nousresearch.com → API Keys) — provisioned at Phase 25, keys have no value yet"
      - name: OPENCODE_API_KEY
        source: "OpenCode (opencode.ai → Zen settings; one key shared Zen+Go) — provisioned at Phase 25, keys have no value yet"
    notes: "DEFERRED to Phase 25 key-provisioning per research Open Question 2 — the vercel CLI needs interactive auth and the keys have no real values this phase. Code declarations (this plan) land now; `vercel env add` (server-only, non-PUBLIC_, Preview + Production) is the Phase 25 ops step. Non-blocking for Phase 23."
must_haves:
  truths:
    - "NOUSRESEARCH_API_KEY and OPENCODE_API_KEY are declared optional server-only in env.ts, mirroring the OPENROUTER_API_KEY z.string().optional() degrade-gracefully precedent (D-15)"
    - "Both keys appear in .env.example with no value (placeholder xxxxxxxx), never NEXT_PUBLIC_-prefixed"
    - "The env schema still parses when the keys are absent (the app never crashes at import time)"
    - "No client-reachable code references the new keys (declaration-only; enforcement = Phase 25 chain-aware gate + Phase 27 VER-04 extension)"
  artifacts:
    - path: "src/lib/env.ts"
      provides: "Optional server-only declarations for both new keys"
      contains: "NOUSRESEARCH_API_KEY: z.string().optional()"
    - path: ".env.example"
      provides: "Placeholder declarations for both new keys"
      contains: "NOUSRESEARCH_API_KEY="
  key_links:
    - from: "src/lib/env.ts"
      to: "process.env"
      via: "envSchema.parse(process.env) at module load — .optional() keys never fail on absence"
      pattern: "envSchema.parse"
    - from: ".env.example"
      to: "src/lib/env.ts"
      via: "same key names, same server-only scope (no PUBLIC_ prefix)"
      pattern: "OPENCODE_API_KEY"
---

<objective>
Deliver REG-02's declaration half: `NOUSRESEARCH_API_KEY` + `OPENCODE_API_KEY` as optional server-only keys in `src/lib/env.ts` (mirroring `OPENROUTER_API_KEY` at line 41 — `z.string().optional()`, degrade-gracefully per D-15, non-`PUBLIC_` prefix = server-only, never logged, never sent to client) and in `.env.example` (no value). This is DECLARATION-ONLY — the CONTEXT.md line-11 phase boundary overrides REG-02's literal "@ai-sdk/openai-compatible is installed" text (install = Phase 25), and there is no consumption this phase (the chain-aware env gate is Phase 25; the Vercel env add is deferred to key-provisioning per research Open Question 2).

Purpose: The declarations are what let Phase 25's chain-aware gate read the keys by name without a schema change, and they keep the roadmap success criterion 5 honest — declared now, enforced later.
Output: Two env declarations in code + two placeholder lines in .env.example, proven parse-safe with keys absent AND present.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 23 success criterion 5)
@.planning/STATE.md (D-15 degrade-gracefully doctrine)
@.planning/phases/23-provider-registry-servable-sources/23-CONTEXT.md (phase boundary — declaration-only, D-23-XX)
@.planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md (Open Question 2 — Vercel env timing; env declaration Code Example l.343-348)
@.planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md (§env.ts l.408-423, §.env.example l.425-443)
src/lib/env.ts (current — 48 lines, OPENROUTER_API_KEY at :36-41)
.env.example (current — 41 lines, Analyze block at :29-41)
src/lib/verification/security-grep.test.ts (the permanent VER-04 gate that must stay green)
</context>

<tasks>

<task type="auto">
  <name>Task 1: env.ts — NOUSRESEARCH_API_KEY + OPENCODE_API_KEY optional server-only declarations</name>
  <files>src/lib/env.ts</files>
  <read_first>
    src/lib/env.ts (current state — the file being modified; OPENROUTER_API_KEY block at :36-41 is the byte-for-byte template)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §src/lib/env.ts (l.408-423)
    .planning/research/SUMMARY.md finding 3 (no env auto-load in openai-compatible — the keys must be passed explicitly at construction in Phase 25)
  </read_first>
  <action>
    Add two keys to the `envSchema` object in src/lib/env.ts, immediately after the `OPENROUTER_API_KEY: z.string().optional(),` line (l.41), mirroring its comment pattern:

    - `NOUSRESEARCH_API_KEY: z.string().optional(),` with a comment block citing: Phase 23 (REG-02) NousResearch direct-inference key. Optional/degrade-gracefully like the OpenRouter key — an unset key must not crash the app at import time; the chain-aware env gate lands in Phase 25. Non-PUBLIC_ prefix = server-only. Never logged, never sent to client. Phase 25 passes it EXPLICITLY at construction (no SDK env auto-load — v1.5 SUMMARY finding 3).
    - `OPENCODE_API_KEY: z.string().optional(),` with a comment block citing: Phase 23 (REG-02) OpenCode key — ONE key shared by the Zen and Go endpoints (verified). Same optional/degrade-gracefully scope; chain-aware gate in Phase 25; explicit apiKey at construction.

    Do NOT change: the schema parse at l.48 (`export const env = envSchema.parse(process.env);`), the fail-fast-required keys (l.7-9), any existing key, or the `z.object` shape. Do NOT add the keys to `ImportMetaEnv`/`env.d.ts` (this repo reads env via zod schema + `process.env`, not `import.meta.env` — the Astro-era env.d.ts is retired). Do NOT prefix either key with `PUBLIC_` — server-only is the whole point (T-23-05).
  </action>
  <acceptance_criteria>
    - `src/lib/env.ts` contains `NOUSRESEARCH_API_KEY: z.string().optional(),`
    - `src/lib/env.ts` contains `OPENCODE_API_KEY: z.string().optional(),`
    - Neither key name appears with a `PUBLIC_` or `NEXT_PUBLIC_` prefix anywhere in `src/` (grep check)
    - Neither key is referenced by any file under `src/components/` or any `'use client'` file (declaration-only)
    - The schema still parses with both keys absent: `npx tsx -e "import { env } from './src/lib/env'; if (!('NOUSRESEARCH_API_KEY' in env) || !('OPENCODE_API_KEY' in env)) throw new Error('optional keys missing from parsed env'); console.log('parse-absent OK')"` prints parse-absent OK
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "NEXT_PUBLIC_NOUSRESEARCH\|NEXT_PUBLIC_OPENCODE\|PUBLIC_NOUSRESEARCH\|PUBLIC_OPENCODE" src/ | wc -l | grep -qx 0 && npx tsx -e "import { env } from './src/lib/env'; if (!('NOUSRESEARCH_API_KEY' in env) || !('OPENCODE_API_KEY' in env)) throw new Error('missing'); if (env.NOUSRESEARCH_API_KEY !== undefined) throw new Error('key set unexpectedly'); console.log('parse-absent OK');"</automated>
  </verify>
  <done>env.ts declares both keys optional server-only; the schema parses with them absent (degrade-gracefully proven) and zero PUBLIC_-prefixed references exist in src/.</done>
</task>

<task type="auto">
  <name>Task 2: .env.example — placeholder declarations for both new keys (no value)</name>
  <files>.env.example</files>
  <read_first>
    .env.example (current — 41 lines; the Phase 9 Analyze block at :29-41 is the template)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §.env.example (l.425-443)
  </read_first>
  <action>
    Add two lines to .env.example inside the "Phase 9 Analyze agent" block, after `OPENROUTER_API_KEY=sk-or-xxxxxxxx` (l.37), following the existing "optional, server-only — never PUBLIC_-prefixed" comment convention:

    - `NOUSRESEARCH_API_KEY=` (no value — CONTEXT: "add the two new keys (no value)")
    - `OPENCODE_API_KEY=` (no value; same shared-key note applies)

    Update the "Get from:" comment block above them to add the two sources: `#   NousResearch: portal.nousresearch.com → API Keys` and `#   OpenCode: opencode.ai → Zen settings (one key shared Zen + Go)`. Do NOT put a value on either line (the keys are provisioned in Phase 25), and do NOT add any `NEXT_PUBLIC_` variant.
  </action>
  <acceptance_criteria>
    - `.env.example` contains a line `NOUSRESEARCH_API_KEY=` (with nothing after the `=`)
    - `.env.example` contains a line `OPENCODE_API_KEY=` (with nothing after the `=`)
    - `.env.example` does NOT contain `NEXT_PUBLIC_NOUSRESEARCH` or `NEXT_PUBLIC_OPENCODE`
    - `npx vitest run src/lib/verification/security-grep.test.ts` exits 0 (the permanent VER-04 gate scans .env.example and stays green)
  </acceptance_criteria>
  <verify>
    <automated>grep -E "^NOUSRESEARCH_API_KEY=$|^OPENCODE_API_KEY=$" .env.example | wc -l | grep -qx 2 && grep -c "NEXT_PUBLIC_NOUSRESEARCH\|NEXT_PUBLIC_OPENCODE" .env.example | grep -qx 0 && npx vitest run src/lib/verification/security-grep.test.ts 2>&1 | tail -3</automated>
  </verify>
  <done>.env.example declares both keys with no value; the security-grep gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env.example / env.ts → deployment | Key NAMES cross into repo + Vercel config; key VALUES never do (declaration-only) |
| env.ts → app-wide imports | env.ts is imported app-wide via db/index.ts — a parse failure would crash the whole app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | Information Disclosure | NOUSRESEARCH_API_KEY / OPENCODE_API_KEY leakage | mitigate | Non-`PUBLIC_`/`NEXT_PUBLIC_` names (server-only by convention), `z.string().optional()` degrade-gracefully (D-15), declaration-only — no consumer, no client import this phase; Phase 25 passes both EXPLICITLY at construction (v1.5 SUMMARY finding 3 — no SDK auto-load); Phase 27 VER-04 extends the security-matrix grep to the new keys |
| T-23-06 | Information Disclosure | Key name drift between env.ts and .env.example | mitigate | Both tasks declare the same two names; the acceptance grep asserts both names appear in .env.example and neither leaks a PUBLIC_ prefix in src/ |
| T-23-07 | Denial of Service | Import-time crash on missing keys | mitigate | `.optional()` keys never fail-fast — schema parse proven with keys absent (Task 1 verify); mirrors the OPENROUTER_API_KEY precedent (l.36-41) |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this plan — no package manager invoked (slopcheck N/A; `@ai-sdk/openai-compatible` remains a Phase 25 install) |
</threat_model>

<verification>
1. `npx tsx -e "import { env } from './src/lib/env';"` exits 0 with keys absent (no import-time crash)
2. `grep -rn "NEXT_PUBLIC_" src/lib/env.ts .env.example` → only the pre-existing `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` (env.ts) — zero new PUBLIC_-prefixed references
3. `npx vitest run src/lib/verification/security-grep.test.ts` → green (the permanent VER-04 gate over src/ + .env.example)
4. No package.json / lockfile change (declaration-only phase boundary)
</verification>

<success_criteria>
- `env.ts` declares `NOUSRESEARCH_API_KEY` + `OPENCODE_API_KEY` as `z.string().optional()`, server-only, byte-for-byte mirroring the OPENROUTER_API_KEY precedent
- `.env.example` lists both keys with no value and their Get-from sources
- The app parses env with both keys absent (degrade-gracefully, D-15) — REG-02 declaration half complete
- Vercel env declarations documented as a deferred Phase-25 ops step (user_setup), NOT performed this phase (research Open Question 2)
</success_criteria>

<output>
Create `.planning/phases/23-provider-registry-servable-sources/23-02-SUMMARY.md` when done
</output>
