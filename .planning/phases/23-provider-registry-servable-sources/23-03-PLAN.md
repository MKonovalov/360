---
phase: 23-provider-registry-servable-sources
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/actions/settings.ts
  - src/app/actions/settings.test.ts
autonomous: true
requirements: [REG-07]
must_haves:
  truths:
    - "saveSettingsAction validates every submitted id against the union servable set — structurally unchanged (membership-based), now covering all 4 providers automatically via the widened getUnionServableIds"
    - "A cross-provider chain spanning the new providers (opencode + nousresearch ids) saves with { ok: true } and the ids reach upsertModelSettings verbatim (D-04)"
    - "A non-servable id is still rejected with invalid_model before any write (T-17-06)"
    - "The immutable gate order holds: requireStaffAccess FIRST → zod → union membership → D-08/D-09 dedupe backstop → atomic upsert → revalidatePath"
  artifacts:
    - path: "src/app/actions/settings.ts"
      provides: "REG-07 union-wide validation (verify-only — no code change expected; getUnionServableIds widens mechanically with SERVABLE_PROVIDERS)"
      contains: "getUnionServableIds(catalogJson)"
    - path: "src/app/actions/settings.test.ts"
      provides: "REG-07 cross-provider save case mocking the 4-provider union + verification that the immutable matrix stays green"
      contains: "nousresearch/hermes-4-70b"
  key_links:
    - from: "src/app/actions/settings.ts"
      to: "src/lib/models/catalog.ts"
      via: "getUnionServableIds(catalogJson) union membership check — the 4-provider union is the ONLY source of truth (T-17-03)"
      pattern: "getUnionServableIds\\(catalogJson\\)"
    - from: "src/app/actions/settings.test.ts"
      to: "src/app/actions/settings.ts"
      via: "vi.mock('@/lib/models/catalog', () => ({ getUnionServableIds: mocks.getUnionServableIds })) — mocked seam stays; only the union fixture widens"
      pattern: "getUnionServableIds"
---

<objective>
Prove REG-07 holds across all four providers: the server action's union-membership validation is structurally UNCHANGED (membership-based over `getUnionServableIds` — the provider count is invisible to it), so the widened union from plan 23-01 automatically covers opencode + nousresearch chains. `settings.ts` is verify-only (no code change — its import at l.7 and call at l.41 already use `getUnionServableIds`). The deliverable is a new REG-07 cross-provider save case in `settings.test.ts` that mocks the union with opencode + nousresearch ids and proves save-through with verbatim ids, PLUS a verification sweep proving the existing 8-case security matrix stays green against the widened union.

Purpose: This is the save-side guarantee behind "every servable model id resolves to exactly one provider" — the DB can only ever hold ids the 4-provider union serves, so a badged provider can never silently run a different provider's id.
Output: A green REG-07 cross-provider test case + verified-unchanged settings.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md (REG-07)
@.planning/STATE.md (D-04 raw-ids-verbatim, T-17-02..06 server-action security matrix)
@.planning/phases/23-provider-registry-servable-sources/23-CONTEXT.md (save-validation discretion: reuse invalid_model)
@.planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md (Validation Architecture REG-07 row; Architectural Responsibility Map "Save validation" row)
@.planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md (§settings.ts l.337-359, §settings.test.ts l.362-406)
src/app/actions/settings.ts (current — 68 lines, immutable gate order)
src/app/actions/settings.test.ts (current — 139 lines, 8-case matrix, vi.hoisted seam at :3-18, beforeEach union mock at :24-28)
</context>

<tasks>

<task type="auto">
  <name>Task 1: settings.ts — verify-only (no code change): confirm the union validation already covers 4 providers structurally</name>
  <files>src/app/actions/settings.ts</files>
  <read_first>
    src/app/actions/settings.ts (current — the verify target; import at :7, gate order comment :10-23, union check at :41-45)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §settings.ts (l.337-359 — "NO code change (verify only)")
  </read_first>
  <action>
    VERIFY-ONLY task — do NOT edit src/app/actions/settings.ts unless a compile or test failure in Task 2 proves otherwise (and if one does, stop and re-read the PATTERNS note before touching anything). Verify each of these holds against the CURRENT file and the post-23-01 catalog:

    1. The servable check is `const servableIds = getUnionServableIds(catalogJson);` (l.41) — union membership, provider-count-agnostic (REG-07's "structurally unchanged").
    2. The immutable gate order is intact: `requireStaffAccess()` FIRST (l.35) → zod `safeParse` (l.37-38, `{ primaryModel, fallbacks: z.array(z.string()).max(2) }` — no provider field, provider is DERIVED server-side) → union membership (l.42-45, `invalid_model`) → D-08/D-09 dedupe backstop (l.50-55, `duplicate_model`) → atomic upsert keyed by session userId (l.57-61) → `revalidatePath('/settings')` (l.63) → catch maps to `action_failed` (l.65-66).
    3. Nothing in the file names `opencode`, `nousresearch`, or either new env key — the widened union is invisible to the action by design.
    4. It compiles against the 4-provider union: the only catalog import is `getUnionServableIds` (l.7), whose signature is unchanged by 23-01.

    Record the verification result in the SUMMARY: one sentence stating settings.ts required zero edits and the union validation covers all 4 providers structurally.
  </action>
  <acceptance_criteria>
    - `git diff --stat` for src/app/actions/settings.ts is empty after this task (no edit made)
    - `src/app/actions/settings.ts` contains `getUnionServableIds(catalogJson)` at the servable check
    - `src/app/actions/settings.ts` contains `requireStaffAccess()` as the first statement of `saveSettingsAction`
    - `grep -c "opencode\|nousresearch" src/app/actions/settings.ts` returns 0
    - `npx tsc --noEmit` reports 0 errors for the file (compiles against the 4-provider union)
  </acceptance_criteria>
  <verify>
    <automated>git diff --stat -- src/app/actions/settings.ts | wc -l | grep -qx 0 && grep -v '^#' src/app/actions/settings.ts | grep -c "getUnionServableIds(catalogJson)" | grep -qx 1 && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>settings.ts verified byte-identical with the 4-provider union compiling clean — REG-07 membership validation widened with zero code change.</done>
</task>

<task type="auto">
  <name>Task 2: settings.test.ts — REG-07 cross-provider save case over the mocked 4-provider union</name>
  <files>src/app/actions/settings.test.ts</files>
  <read_first>
    src/app/actions/settings.test.ts (current — 139 lines, 8-case matrix; the file being modified)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §settings.test.ts (l.362-406 — the REG-07 cross-provider case template + beforeEach union override pattern)
    src/app/actions/settings.ts (the action under test — unchanged from Task 1)
  </read_first>
  <action>
    Add ONE new `it(...)` case to the existing `describe('saveSettingsAction security matrix (T-17-02..06)')` block in src/app/actions/settings.test.ts, after the existing cross-provider case (l.51-65). Keep the existing 8 cases byte-identical — the widened union must NOT require touching them (their union members `claude-sonnet-4-6` + `anthropic/claude-sonnet-4.6` are still members of a 4-provider mock).

    New case — "REG-07 (4-provider): a cross-provider chain spanning the new providers saves against the widened union, ids pass through verbatim (D-04)":
    - Override the beforeEach union mock (l.26) inside this case: `mocks.getUnionServableIds.mockReturnValue(['claude-sonnet-4-6', 'anthropic/claude-sonnet-4.6', 'nousresearch/hermes-4-70b', 'deepseek-v4-flash', 'hy3']);` (an anthropic id + an openrouter id + a nousresearch pin + an opencode Zen id + an opencode Go-exclusive id — proving the logical opencode provider spans both snapshot providerIDs at the save seam).
    - Submit `{ primaryModel: 'deepseek-v4-flash', fallbacks: ['nousresearch/hermes-4-70b'] }` (an opencode primary + nousresearch fallback — a chain that was impossible before v1.5).
    - Assert `result` toEqual `{ ok: true }`, `mocks.upsertModelSettings` toHaveBeenCalledWith `{ userId: 'user_123', primaryModel: 'deepseek-v4-flash', fallbackModels: ['nousresearch/hermes-4-70b'] }` (raw ids verbatim — no prefix-strip, no translation, D-04), and `revalidatePath` toHaveBeenCalledWith('/settings').
    - Also assert the reject side in the SAME case or a second small case: an id outside the mocked union (e.g. `'claude-opus-4-9'` — already covered by the existing non-servable case at l.88-99, so only add it if it reads naturally; the existing case already proves the reject path, do not duplicate it).

    Do NOT change: the vi.hoisted seam (l.3-18), the mock factories (l.9-18 — `getUnionServableIds` is mocked, so this test file is independent of catalog.ts's actual union and can run in Wave 1), the beforeEach (l.24-28), or any existing case.
  </action>
  <acceptance_criteria>
    - `src/app/actions/settings.test.ts` contains an `it(...)` whose title includes `REG-07` and `4-provider` (or equivalent)
    - That case mocks the union with an opencode id AND a nousresearch id (mockReturnValue array contains `deepseek-v4-flash` or `hy3` AND `nousresearch/hermes-4-70b`)
    - That case submits a chain with an opencode primary and a nousresearch fallback and asserts `{ ok: true }`
    - That case asserts `upsertModelSettings` received the ids verbatim (`'deepseek-v4-flash'`, `'nousresearch/hermes-4-70b'`)
    - The existing 8 cases are unchanged (git diff shows only additive lines)
    - `npx vitest run src/app/actions/settings.test.ts` exits 0 (9 cases green)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/actions/settings.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>settings.test.ts proves a 4-provider cross-provider chain saves with verbatim ids (REG-07) and the existing 8-case security matrix stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → saveSettingsAction (Server Action) | Untrusted id strings cross here; provider must never arrive from the client (derived server-side from the union) |
| action → DB (upsertModelSettings) | Only union-validated ids may reach the row (T-17-02..06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Tampering | saveSettingsAction id injection | mitigate | Immutable gate order preserved (requireStaffAccess FIRST → zod → union membership over the 4-provider `getUnionServableIds` → dedupe backstop → atomic upsert); arbitrary ids rejected `invalid_model` before any write; provider never client-sent (zod shape unchanged — T-17-02) |
| T-23-09 | Spoofing | Badged-vs-served provider mismatch on saved chains | mitigate | The union is the single save gate — a saved id is provably in exactly the provider's servable set the picker badged it under (23-04 renders from the same union); resolution at run time is the servable-membership registry (23-01), so a chain saved with `deepseek-v4-flash` can only ever instantiate as opencode |
| T-23-10 | Information Disclosure | Catalog/union data leaking to the client via the action return | mitigate | `SettingsActionResult` is `{ ok } \| { ok, reason }` — no id lists, no union contents, no costs; unchanged |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this plan — no package manager invoked |
</threat_model>

<verification>
1. `npx vitest run src/app/actions/settings.test.ts` → 9/9 green (8 existing + 1 REG-07 4-provider)
2. `git diff -- src/app/actions/settings.ts` → empty (verify-only honored)
3. `npx tsc --noEmit` → 0 errors (action compiles against the 4-provider union from 23-01)
4. `npx vitest run src/lib/models/catalog.test.ts` → green (the union the action validates is itself canary-locked — 23-01)
</verification>

<success_criteria>
- saveSettingsAction is provably unchanged and covers all 4 providers via the widened union — REG-07 membership-based validation complete
- A cross-provider chain spanning opencode (Zen + Go-exclusive ids) and nousresearch saves with `{ ok: true }` and verbatim ids (D-04)
- The full 8-case security matrix (gate order, invalid_model, duplicate_model, action_failed, >2 fallbacks) stays green against the widened union
</success_criteria>

<output>
Create `.planning/phases/23-provider-registry-servable-sources/23-03-SUMMARY.md` when done
</output>
