---
phase: 23-provider-registry-servable-sources
plan: 04
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/components/settings/model-picker-logic.ts
  - src/components/settings/model-picker-logic.test.ts
  - src/app/(dashboard)/settings/page.tsx
autonomous: true
requirements: [REG-01, REG-03]
must_haves:
  truths:
    - "providerName() is a registry-driven 4-entry map lookup (PROVIDER_NAMES: Record<ModelProviderId, string>), NOT a 2-way ternary — the client-side branch dies (REG-01)"
    - "The settings page's provider-selector options use the SAME shared map — the second hardcoded branch at page.tsx:93 dies, so NousResearch/OpenCode render correctly instead of 'OpenRouter' (research Pitfall 4)"
    - "The shared map stays client-bundle-safe: model-picker-logic.ts imports ONLY type { ModelProviderId } — never a catalog value (T-17-09)"
    - "trimRow resolves the 5 go-exclusive opencode-go rows via dedupeProviderRows — their name/cost no longer fall back to raw id / 0 (research Open Question 4)"
  artifacts:
    - path: "src/components/settings/model-picker-logic.ts"
      provides: "PROVIDER_NAMES 4-entry map + providerName() map lookup (type-only import preserved)"
      contains: "PROVIDER_NAMES"
    - path: "src/components/settings/model-picker-logic.test.ts"
      provides: "providerName 4-entry assertions (anthropic/openrouter/nousresearch/opencode)"
      contains: "providerName('opencode')"
    - path: "src/app/(dashboard)/settings/page.tsx"
      provides: "Shared-map provider selector (line 93 branch removed) + dedupeProviderRows-backed trimRow"
      contains: "providerName(id)"
  key_links:
    - from: "src/components/settings/model-picker-logic.ts"
      to: "src/lib/models/catalog.ts"
      via: "import type { ModelProviderId } — erased at compile, zero bundle weight (T-17-09)"
      pattern: "import type \\{ ModelProviderId \\}"
    - from: "src/app/(dashboard)/settings/page.tsx"
      to: "src/components/settings/model-picker-logic.ts"
      via: "providerName(id) in the SERVABLE_PROVIDERS.map selector-options — the same map the client uses (single source, REG-01)"
      pattern: "providerName\\(id\\)"
    - from: "src/app/(dashboard)/settings/page.tsx"
      to: "src/lib/models/catalog.ts"
      via: "dedupeProviderRows(catalogJson, provider).find(...) in trimRow — the go-exclusive rows resolve"
      pattern: "dedupeProviderRows"
---

<objective>
Kill BOTH hardcoded 2-way provider-name branches (REG-01, research Pitfall 4): `providerName()` in `model-picker-logic.ts` becomes a registry-driven lookup over a 4-entry `PROVIDER_NAMES: Record<ModelProviderId, string>` map (`anthropic: 'Anthropic', openrouter: 'OpenRouter', nousresearch: 'NousResearch', opencode: 'OpenCode'`), and the settings page's provider-selector options (`settings/page.tsx:90-94`) switch from `id === 'anthropic' ? 'Anthropic' : 'OpenRouter'` to the same shared map — because growing `SERVABLE_PROVIDERS` to 4 makes the ALREADY-RENDERING v1.4 selector surface 4 entries immediately, and without this fix NousResearch/OpenCode would ship labeled "OpenRouter". Additionally, `trimRow` (page.tsx:53-63) switches its row lookup to `dedupeProviderRows(catalogJson, provider)` (research Open Question 4) so the 5 go-exclusive opencode-go ids resolve name/cost instead of the raw-id/0 fallback. The client-bundle contract (T-17-09) is preserved: the map lives in model-picker-logic.ts with its type-only `ModelProviderId` import intact.

Purpose: This is the visible half of "the app recognizes all four providers" — correct labels everywhere a provider name renders, from a single TS-enforced source that makes every future provider a one-line addition.
Output: A shared client-safe PROVIDER_NAMES map, its 4-entry tests, and a settings page whose selector + trimRow consume the registry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 23 success criterion 1 — "providerName() becomes a registry-driven map with no hardcoded 2-way branch")
@.planning/STATE.md (T-17-09 client-bundle contract; Phase 21 decision log on providerName)
@.planning/phases/23-provider-registry-servable-sources/23-CONTEXT.md (D-23-XX, Claude's discretion — map location, labels "NousResearch"/"OpenCode")
@.planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md (Pattern 4 l.221-245, Pitfall 4 l.297-301, Open Question 4 l.389-392)
@.planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md (§model-picker-logic.ts l.237-262, §model-picker-logic.test.ts l.264-278, §settings/page.tsx l.282-333)
src/components/settings/model-picker-logic.ts (current — 145 lines, providerName at :26-28, type-only import at :13)
src/components/settings/model-picker-logic.test.ts (current — 327 lines, providerName describe at :246-252)
src/app/(dashboard)/settings/page.tsx (current — 125 lines, trimRow at :53-63, providers map at :90-94)
src/lib/models/catalog.ts (post-23-01 — dedupeProviderRows, SERVABLE_PROVIDERS, ModelProviderId)
</context>

<tasks>

<task type="auto">
  <name>Task 1: model-picker-logic.ts — PROVIDER_NAMES 4-entry map + registry-driven providerName()</name>
  <files>src/components/settings/model-picker-logic.ts</files>
  <read_first>
    src/components/settings/model-picker-logic.ts (current — 145 lines; the file being modified; type-only import at :13, providerName at :26-28)
    .planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md Pattern 4 (l.221-245 — the exact map shape)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §model-picker-logic.ts (l.237-262 — client-safety contract to preserve)
    src/components/settings/model-picker.tsx + src/components/settings/model-settings-form.tsx (providerName consumers — read to confirm the return-type widening is text-only; no edits here)
  </read_first>
  <action>
    Replace the `providerName` function in src/components/settings/model-picker-logic.ts (l.26-28) with:

    ```
    export const PROVIDER_NAMES: Record<ModelProviderId, string> = {
      anthropic: 'Anthropic',
      openrouter: 'OpenRouter',
      nousresearch: 'NousResearch',
      opencode: 'OpenCode',
    };

    export function providerName(provider: ModelProviderId): string {
      return PROVIDER_NAMES[provider];
    }
    ```

    Keep the D-21-09 comment above it, updated: single source of the display-name map used by badges, section headers, and the provider-switch reset hint — REG-01 makes it the 4-entry registry map (record completeness is TS-enforced at 4 entries; every future provider is a one-line addition). Preserve the module's client-safety contract comment (l.6-11, T-17-09) and the `import type { ModelProviderId } from '@/lib/models/catalog';` (l.13) — the map is a 4-string constant carrying NO snapshot weight, so it is client-safe. Do NOT touch any other export (ServableModel, searchValue, suffixLabel, isHighCost, primaryAfterProviderSwitch, staleIds, groupByProvider, optionsForSlot, triggerLabel, pinnedSelection stay byte-identical).

    The return type widens from `'Anthropic' | 'OpenRouter'` to `string` — the consumers (model-picker.tsx at :95/:132/:139/:168/:208, model-settings-form.tsx at :377) render it as React text, so the widening is compile-safe; verify with `npx tsc --noEmit` (Task 3's gate covers the full build).
  </action>
  <acceptance_criteria>
    - `src/components/settings/model-picker-logic.ts` contains `export const PROVIDER_NAMES: Record<ModelProviderId, string>` with exactly 4 entries and the display strings `'Anthropic'`, `'OpenRouter'`, `'NousResearch'`, `'OpenCode'`
    - `providerName` body is a `PROVIDER_NAMES[provider]` lookup — no `=== 'anthropic'` ternary remains in the file
    - The file's ONLY catalog import is still `import type { ModelProviderId }` (type-only — grep proves no value import)
    - `grep -c "catalog.json" src/components/settings/model-picker-logic.ts` returns 0 (no snapshot reference)
    - All other exports in the file are unchanged (git diff shows only the providerName block replaced + comment)
  </acceptance_criteria>
  <verify>
    <automated>grep -n "PROVIDER_NAMES: Record<ModelProviderId, string>" src/components/settings/model-picker-logic.ts && grep -n "return PROVIDER_NAMES\[provider\]" src/components/settings/model-picker-logic.ts && grep -c "=== 'anthropic' ?" src/components/settings/model-picker-logic.ts | grep -qx 0 && grep -c "import type { ModelProviderId } from '@/lib/models/catalog'" src/components/settings/model-picker-logic.ts | grep -qx 1</automated>
  </verify>
  <done>model-picker-logic.ts exposes the 4-entry PROVIDER_NAMES map and a registry-driven providerName with the type-only import intact.</done>
</task>

<task type="auto">
  <name>Task 2: model-picker-logic.test.ts — providerName 4-entry assertions + PROVIDER_NAMES completeness</name>
  <files>src/components/settings/model-picker-logic.test.ts</files>
  <read_first>
    src/components/settings/model-picker-logic.test.ts (current — 327 lines; the providerName describe at :246-252 is the block to extend)
    src/components/settings/model-picker-logic.ts (the module under test — Task 1 output)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §model-picker-logic.test.ts (l.264-278)
  </read_first>
  <action>
    Extend the existing `describe('providerName (D-21-09)')` block (l.246-252) in src/components/settings/model-picker-logic.test.ts. Add to the import list (l.2-13) `PROVIDER_NAMES`. Add the two new expectations to the existing map test:
    `expect(providerName('nousresearch')).toBe('NousResearch');` and `expect(providerName('opencode')).toBe('OpenCode');` (keep the existing anthropic/openrouter expectations).

    ADD one new test in the same describe: "PROVIDER_NAMES is the complete 4-entry record (TS-enforced shape)" — assert `Object.keys(PROVIDER_NAMES).sort()` toEqual `['anthropic', 'nousresearch', 'opencode', 'openrouter'].sort()` and that each value is a non-empty string. This locks the map against accidental partial growth.

    Do NOT change: the fixture (l.22-63), `servableByProvider`/`defaults` record types (l.65-73 — deliberately left narrowed to `'anthropic' | 'openrouter'`; the fixtures only exercise the existing two providers, per PATTERNS "or stay narrowed; planner's call"), or any other describe block. Do NOT add `Record<ModelProviderId, ...>`-typed fixtures requiring 4 entries (that would churn unrelated tests).
  </action>
  <acceptance_criteria>
    - The providerName test asserts all 4 display names: `'Anthropic'`, `'OpenRouter'`, `'NousResearch'`, `'OpenCode'`
    - A test asserts `PROVIDER_NAMES` has exactly the 4 provider keys
    - The fixture record types at :65-73 are untouched (still the 2-provider narrowed type)
    - `npx vitest run src/components/settings/model-picker-logic.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/settings/model-picker-logic.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>model-picker-logic.test.ts pins all 4 display names and the map's complete key set (REG-01).</done>
</task>

<task type="auto">
  <name>Task 3: settings/page.tsx — shared-map provider selector (line 93 branch dies) + dedupeProviderRows-backed trimRow</name>
  <files>src/app/(dashboard)/settings/page.tsx</files>
  <read_first>
    src/app/(dashboard)/settings/page.tsx (current — 125 lines; the file being modified; trimRow at :53-63, providers map at :90-94, imports at :3-10)
    .planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md Pattern 4 page snippet (l.242-245) + Pitfall 4 (l.297-301) + Open Question 4 (l.389-392)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §settings/page.tsx (l.282-333)
    src/components/settings/model-picker-logic.ts (Task 1 output — providerName export)
    src/lib/models/catalog.ts (post-23-01 — dedupeProviderRows signature)
  </read_first>
  <action>
    Two edits to src/app/(dashboard)/settings/page.tsx:

    A) KILL the second hardcoded branch (l.90-94). Replace the `providers` map:
    - Add `providerName` to the existing import from `@/components/settings/model-picker-logic` (l.10 currently imports `type { ServableModel }` — add a value import: `import { providerName } from '@/components/settings/model-picker-logic';` alongside or merging with the type import. This is safe: the module imports ONLY `type { ModelProviderId }` from catalog, so no snapshot weight reaches this server component's bundle path, and the page is a server component anyway (T-17-09 allows catalog values HERE — but the shared map is the point, one source).
    - Replace the body at l.91-94 with: `const providers = SERVABLE_PROVIDERS.map((id) => ({ id, name: providerName(id) }));` and update the SET-01 comment: 4-choice provider selector options from the shared registry map (REG-01) — Anthropic, OpenRouter, NousResearch, OpenCode.
    - Do NOT change the `unionServableModels` COMPUTATION (l.77-79) — but DO refresh its stale SET-04 row-count comment (l.73-76): replace the hardcoded "337 rows — 336 openrouter + 1 anthropic" with the overlap-aware count at the 2026-08-02 snapshot — "375 rows — 336 openrouter + 1 anthropic + 39 opencode servable − 1 overlap (claude-sonnet-4-6 is servable under both anthropic and opencode)" — and keep the rest of that comment intact (union ids are always servable-scoped so the provider lookup is non-null in practice; `?? 'anthropic'` is the documented defensive fallback, RESEARCH A3). Do NOT change `servableByProvider` (l.66-71 — the `as Record<ModelProviderId, ServableModel[]>` cast stays; with `nousresearch` in SERVABLE_PROVIDERS it now produces an empty array for nousresearch until Phase 24 rows land — correct, no crash), `defaults` (l.83-88 — `PROVIDER_DEFAULT_MODELS[p]` now has all 4 entries from 23-01), or `savedChain` (l.99-105).

    B) REWIRE trimRow (l.53-63) to the deduped-row helper (research Open Question 4). Replace the row lookup
    `const m = catalogJson.models.find((mm) => mm.id === id && mm.providerID === provider);`
    with
    `const m = dedupeProviderRows(catalogJson, provider).find((mm) => mm.id === id);`
    Add `dedupeProviderRows` to the catalog import at l.3. Update the surrounding comment: the lookup now walks the Zen-wins deduped pool (D-23-08) so the 5 go-exclusive opencode-go ids (hy3, mimo-v2.5, mimo-v2.5-pro, qwen3.7-max, qwen3.7-plus) resolve their name/cost instead of the raw-id/0 fallback — identical results for anthropic/openrouter (single-providerID maps) and for opencode Zen rows. Keep the Anti-Pattern 1 provider-scoped intent (the dedup helper IS the provider scope — it filters by SNAPSHOT_PROVIDER_IDS[provider]).
  </action>
  <acceptance_criteria>
    - `src/app/(dashboard)/settings/page.tsx` does NOT contain `id === 'anthropic' ? 'Anthropic' : 'OpenRouter'` (the line-93 branch is gone)
    - `src/app/(dashboard)/settings/page.tsx` contains `name: providerName(id)` in the providers map
    - `src/app/(dashboard)/settings/page.tsx` imports `providerName` from `@/components/settings/model-picker-logic`
    - `src/app/(dashboard)/settings/page.tsx` contains `dedupeProviderRows(catalogJson, provider).find((mm) => mm.id === id)` in trimRow
    - `src/app/(dashboard)/settings/page.tsx` no longer hardcodes "337 rows" in the SET-04 comment (l.73-76) — updated to the overlap-aware "375 rows — 336 openrouter + 1 anthropic + 39 opencode servable − 1 overlap"
    - `grep -rn "=== 'anthropic' ?" src/` returns 0 matches repo-wide (Pitfall 4 warning sign: no provider ternary remains)
    - `npx vitest run src/components/settings/model-picker-logic.test.ts src/lib/models/catalog.test.ts` exits 0 and `npx tsc --noEmit` reports 0 errors (the return-type widening compiles across model-picker.tsx/model-settings-form.tsx)
  </acceptance_criteria>
  <verify>
    <automated>grep -rn "id === 'anthropic' ?" src/ | wc -l | grep -qx 0 && grep -n "providerName(id)" src/app/\(dashboard\)/settings/page.tsx && grep -n "dedupeProviderRows(catalogJson, provider)" src/app/\(dashboard\)/settings/page.tsx && npx tsc --noEmit 2>&1 | head -5</automated>
  </verify>
  <done>The settings page renders all 4 providers via the shared registry map, trimRow resolves the go-exclusive rows, and the build is green with zero provider ternaries repo-wide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server page (settings/page.tsx) → client components (model-settings-form, model-picker) | Only already-trimmed, server-validated props + the 4-string name map cross; catalog.json must never cross (T-17-09) |
| client map (PROVIDER_NAMES) → display | The map is the single source of provider identity LABELS; a wrong label = a mis-badged provider entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | Information Disclosure | catalog.json leakage via the shared name map | mitigate | `PROVIDER_NAMES` lives in model-picker-logic.ts whose ONLY catalog import is `import type { ModelProviderId }` — erased at compile, zero snapshot weight (T-17-09); page.tsx (server component) is the only file importing catalog VALUES and it stays server-side; the canary grep (`catalog.json -> 0` in model-picker-logic.ts) stays clean |
| T-23-12 | Spoofing | Provider mislabeling (NousResearch/OpenCode rendered as "OpenRouter") | mitigate | BOTH hardcoded branches die this plan (model-picker-logic.ts + page.tsx:93) and route through the single TS-enforced `PROVIDER_NAMES` record; the 4-entry key-set test locks completeness; the repo-wide `=== 'anthropic' ?` grep = 0 is an acceptance gate (Pitfall 4 warning sign) |
| T-23-13 | Spoofing | Go-exclusive rows resolving to the wrong provider's name/cost | mitigate | trimRow consults `dedupeProviderRows(catalogJson, provider)` (Zen-wins, D-23-08) — the go-exclusive rows stay under the opencode logical provider with their own row's name/cost; providerID prop is always the caller's provider (never the row's raw providerID) |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | Zero installs this plan — no package manager invoked (shadcn vendoring already shipped in Phase 21; nothing new) |
</threat_model>

<verification>
1. `grep -rn "=== 'anthropic' ?" src/` → 0 matches (both hardcoded provider-name branches gone repo-wide)
2. `npx vitest run src/components/settings/model-picker-logic.test.ts src/lib/models/catalog.test.ts src/app/actions/settings.test.ts` → all green (name map + registry + save matrix)
3. `npx tsc --noEmit` → 0 errors (the providerName return-type widening compiles across model-picker.tsx / model-settings-form.tsx consumers)
4. `npx vitest run src/lib/verification/security-grep.test.ts` → green (T-17-09 / VER-04 gate — the client-safe map introduces no OPENROUTER token and no snapshot import)
5. Client-bundle canary: `grep -c "catalog.json" src/components/settings/model-picker-logic.ts` → 0
</verification>

<success_criteria>
- `providerName()` is registry-driven via the 4-entry `PROVIDER_NAMES` map — REG-01 client half complete; the settings page renders 4 correctly-labeled provider entries (Anthropic, OpenRouter, NousResearch, OpenCode) in SERVABLE_PROVIDERS order
- Both hardcoded 2-way branches are gone (model-picker-logic.ts + page.tsx:93) — research Pitfall 4 closed; no provider ternary remains repo-wide
- The 5 go-exclusive opencode-go ids resolve name/cost through `dedupeProviderRows` — research Open Question 4 closed (REG-03 partial evidence: the deduped row pool is what the page consumes)
- The client bundle stays clean: type-only catalog import in the client-safe module, catalog.json untouched in client reach (T-17-09)
- Full-suite green (excluding the documented pre-existing live-key `openrouter-only-chain` case) at wave merge
</success_criteria>

<output>
Create `.planning/phases/23-provider-registry-servable-sources/23-04-SUMMARY.md` when done
</output>
