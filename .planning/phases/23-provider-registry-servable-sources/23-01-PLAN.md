---
phase: 23-provider-registry-servable-sources
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/models/catalog.ts
  - src/lib/models/catalog.test.ts
  - src/lib/agents/modelFactory.ts
  - src/lib/agents/modelFactory.test.ts
autonomous: true
requirements: [REG-01, REG-03, REG-04, REG-05, REG-06]
must_haves:
  truths:
    - "Every servable model id resolves to exactly one provider with no silent swaps — claude-sonnet-4-6 → anthropic (regression lock), hermes pair → nousresearch over their openrouter mirrors (D-23-07), big-pickle + claude-sonnet-5 → opencode (servable-membership reworks), anthropic/claude-sonnet-4.6 → openrouter"
    - "The opencode servable set is the npm-gated, Zen-wins-deduped 39 ids (23 chat + 16 Claude, 0 GPT/Gemini) — the count-stability (D-23-02) and no-flip (D-23-09) canaries lock the committed snapshot shape"
    - "The NousResearch servable set is the curated hermes-4 pair allowlist pins (fixture-proven; the live snapshot holds 0 nousresearch rows until Phase 24 — Pitfall 5)"
    - "Every provider has a reset-to-default target in PROVIDER_DEFAULT_MODELS (4 entries, TS-enforced via Record<ModelProviderId, string>)"
  artifacts:
    - path: "src/lib/models/catalog.ts"
      provides: "4-provider registry core: ModelProviderId(4), ProviderGate, PROVIDER_GATES(4), SNAPSHOT_PROVIDER_IDS, PROVIDER_PRECEDENCE, dedupeProviderRows, servable-membership getProviderForModelId"
      contains: "PROVIDER_PRECEDENCE"
    - path: "src/lib/models/catalog.test.ts"
      provides: "Extended canary suite: count-stability (39), no-flip (12 dual / 5 go-exclusive), hermes fixture canary, reworked claude-sonnet-5/big-pickle, SERVABLE_PROVIDERS=4"
      contains: "dedupeProviderRows"
    - path: "src/lib/agents/modelFactory.ts"
      provides: "4-entry PROVIDER_DEFAULT_MODELS + NOUSRESEARCH_DEFAULT_MODEL_ID + OPENCODE_DEFAULT_MODEL_ID (D-23-06/D-23-03)"
      contains: "OPENCODE_DEFAULT_MODEL_ID"
  key_links:
    - from: "src/lib/models/catalog.ts"
      to: "getServableIdsForProvider(catalog, provider)"
      via: "getProviderForModelId iterates PROVIDER_PRECEDENCE and returns the first provider whose servable set contains the id"
      pattern: "PROVIDER_PRECEDENCE"
    - from: "src/lib/models/catalog.ts"
      to: "SNAPSHOT_PROVIDER_IDS.opencode"
      via: "dedupeProviderRows first-providerID-wins — the array order IS the Zen-wins rule (D-23-08)"
      pattern: "SNAPSHOT_PROVIDER_IDS"
    - from: "src/lib/agents/modelFactory.ts"
      to: "src/lib/models/catalog.ts"
      via: "Record<ModelProviderId, string> completeness — growing the union forces the 4 default entries at compile time"
      pattern: "PROVIDER_DEFAULT_MODELS"
---

<objective>
Deliver the 4-provider registry keystone: `ModelProviderId` grows to four, `PROVIDER_GATES` gains the `ProviderGate = { allowlist?, npm? }` shape (NousResearch allowlist pins D-23-05, OpenCode npm-value gate D-23-01), the Zen-wins dual-listed-id dedup lands as one data-driven `dedupeProviderRows` helper (D-23-08/D-23-10), `getProviderForModelId` becomes priority-ordered **servable-membership** resolution (research Pattern 3 — `PROVIDER_PRECEDENCE = ['anthropic','nousresearch','openrouter','opencode']`, nousresearch MUST outrank openrouter or the D-23-07 hermes canary fails), and the canary suite is extended with the count-stability (D-23-02, locks post-dedup **39** not pre-dedup 49), no-flip (D-23-09, 12 dual → Zen, 5 go-exclusive → Go), and hermes fixture (D-23-07) canaries plus the two deliberate reworks (`claude-sonnet-5` `'anthropic'→'opencode'`, `big-pickle` `null→'opencode'` — never deletes). `modelFactory.ts`'s `PROVIDER_DEFAULT_MODELS` grows to 4 entries in the SAME plan because growing the union breaks its `Record<ModelProviderId, string>` compile (the v1.4 19-01 "all callers migrate in the same change so next build stays green" doctrine).

Purpose: This plan is the prerequisite for every other Phase 23 consumer — the page props (23-04), save validation (23-03), and Phase 24/25/26 all compose on this registry. The `instantiateModel` dispatch stays 2-provider (research A4: new-provider ids fail-loud until Phase 25 — do NOT add dispatch branches).
Output: A 4-provider registry module with a servable-membership resolver, its extended Vitest canary suite, and the 4-entry default-model map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (Phase 23 + v1.5 locked-decision footnote)
@.planning/STATE.md
@.planning/REQUIREMENTS.md (REG-01, REG-03, REG-04, REG-05, REG-06)
@.planning/research/SUMMARY.md
@.planning/phases/23-provider-registry-servable-sources/23-CONTEXT.md (D-23-01..D-23-10)
@.planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md (Patterns 1-3, Pitfalls 1-3, 5)
@.planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md (§catalog.ts, §catalog.test.ts, §modelFactory.ts)
src/lib/models/catalog.ts (current state — 89 lines)
src/lib/models/catalog.test.ts (current — 228 lines, fixture + snapshot dual-canary convention)
src/lib/agents/modelFactory.ts (current — 77 lines, PROVIDER_DEFAULT_MODELS at :28-31)
src/lib/agents/modelFactory.test.ts (current — 105 lines, defaults assertion at :98-104)
</context>

<tasks>

<task type="auto">
  <name>Task 1: catalog.ts — 4-provider registry core (union, ProviderGate, gates, SNAPSHOT_PROVIDER_IDS, PROVIDER_PRECEDENCE, dedupeProviderRows, servable-membership resolver)</name>
  <files>src/lib/models/catalog.ts</files>
  <read_first>
    src/lib/models/catalog.ts (current state — the file being modified)
    .planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md Patterns 1-3 (l.136-219) and Pitfalls 1-2 (l.279-289)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §src/lib/models/catalog.ts (l.30-115)
    src/lib/models/catalog.json (snapshot shape — types derive from it; read a few opencode/opencode-go rows)
  </read_first>
  <action>
    Modify src/lib/models/catalog.ts. Keep byte-identical: the imports (l.1), `CatalogModel`/`ModelCatalog` types (l.3-4), `ANTHROPIC_ALLOWLIST` (l.13), `FAST_MODEL_ID` (l.24), `getModelDisplayName` (l.30-32), `opencodeSlugToModelId` (l.36-39), and `getUnionServableIds` (l.74-76 — Set-dedup over SERVABLE_PROVIDERS widens mechanically).

    1. GROW the union at l.43: `export type ModelProviderId = 'anthropic' | 'openrouter' | 'nousresearch' | 'opencode';` — `nousresearch` = the direct inference API, `opencode` = ONE logical provider spanning the `opencode` + `opencode-go` snapshot providerIDs (REG-03).

    2. ADD after the `ModelProviderId` union:
    - `export type ProviderGate = { allowlist?: readonly string[]; npm?: readonly string[] };` — research Pattern 1 shape. A present `allowlist` gates by id, a present `npm` gates by `api.npm` value, neither means the full active set (openrouter, D-02).
    - `export const NOUSRESEARCH_ALLOWLIST: readonly string[] = ['nousresearch/hermes-4-70b', 'nousresearch/hermes-4-405b'];` with a why-comment citing D-23-05: concrete pins, never `~latest` aliases (D-07 "never `~`/`:free`/auto in pins" doctrine); the rows land in the snapshot in Phase 24 and must be roster-verified there (D-02).
    - `export const OPENCODE_NPM_GATE: readonly string[] = ['@ai-sdk/openai-compatible', '@ai-sdk/anthropic'];` with a why-comment citing D-23-01: data-driven by `api.npm` — the 49-row count (30 chat + 19 Claude) falls out of the data; GPT-5 (`@ai-sdk/openai`) and Gemini (`@ai-sdk/google`) rows self-exclude forever; new chat/Claude models OpenCode adds become servable on refresh.

    3. REWORK `PROVIDER_GATES` (l.49-52) to `Record<ModelProviderId, ProviderGate>` with FOUR entries, keeping the D-02 why-comment:
    `anthropic: { allowlist: ANTHROPIC_ALLOWLIST }`, `openrouter: {}`, `nousresearch: { allowlist: NOUSRESEARCH_ALLOWLIST }` (REG-04: curated, NOT the 292-row portal roster), `opencode: { npm: OPENCODE_NPM_GATE }`.

    4. ADD `export const SERVABLE_PROVIDERS: readonly ModelProviderId[] = ['anthropic', 'openrouter', 'nousresearch', 'opencode'];` replacing the 2-entry const at l.54 — selector/union iteration order (matches the REG-01 roadmap listing order: Anthropic, OpenRouter, NousResearch, OpenCode). NOTE: this order deliberately DIFFERS from `PROVIDER_PRECEDENCE` below — SERVABLE_PROVIDERS is display/union order, PROVIDER_PRECEDENCE is resolution order; do not merge them.

    5. ADD `export const SNAPSHOT_PROVIDER_IDS: Record<ModelProviderId, readonly string[]> = { anthropic: ['anthropic'], openrouter: ['openrouter'], nousresearch: ['nousresearch'], opencode: ['opencode', 'opencode-go'] };` — research Pattern 2. The `opencode` entry's array order IS the deterministic Zen-wins rule: the Zen row wins by first-providerID-wins; the mapping is data, survives regeneration by construction (D-23-08/CAT-04).

    6. ADD `export const PROVIDER_PRECEDENCE: readonly ModelProviderId[] = ['anthropic', 'nousresearch', 'openrouter', 'opencode'];` — research Pattern 3, with a why-comment that MUST state: (a) the roadmap's "nousresearch-over-openrouter" phrase is a RANKING modifier — nousresearch must outrank openrouter because openrouter's full-catalog gate serves the hermes mirror rows, so a literal `['anthropic','openrouter','nousresearch','opencode']` array would fail the D-23-07 hermes canary; (b) anthropic first = the claude-sonnet-4-6 regression lock (also servable under opencode's npm gate, so order is load-bearing); (c) opencode last — only wins ids no earlier provider serves servably (big-pickle, the dual-listed class).

    7. ADD `export function dedupeProviderRows(catalog: ModelCatalog, provider: ModelProviderId): CatalogModel[]` — research Pattern 2 exact shape: filter `catalog.models` to `SNAPSHOT_PROVIDER_IDS[provider].includes(m.providerID)`, then dedupe by `m.id` with a `seen` Set, first-wins (the first snapshot providerID in the map array wins). MUST return ROWS (not ids) — Phase 26's `trimRow` reuses it for the Zen/Go endpoint caption and go-exclusive rows' `api.url`. Add a why-comment citing D-23-08/D-23-09: dedup lives in the registry layer, expressed once, survives regeneration.

    8. REWORK `getServableIdsForProvider` (l.58-69) to the dedup-then-gate pipeline (D-23-10 — dedup FIRST, then the gate): `const pool = dedupeProviderRows(catalog, provider).filter((m) => m.status !== 'deprecated');` then `const gate = PROVIDER_GATES[provider];` — if `gate.npm` is present return `pool.filter((m) => gate.npm!.includes(m.api.npm)).map((m) => m.id)`; else if `gate.allowlist` is present return `pool.filter((m) => gate.allowlist!.includes(m.id)).map((m) => m.id)`; else return `pool.map((m) => m.id)` (openrouter full active set, D-02). Keep the CAT-03 comment updated to name the dedup+gate pipeline.

    9. REWORK `getProviderForModelId` (l.84-89) to precedence iteration over `PROVIDER_PRECEDENCE` with **servable-membership** checks: `for (const provider of PROVIDER_PRECEDENCE) { if (getServableIdsForProvider(catalog, provider).includes(id)) return provider; } return null;` (fail-closed). Replace the old scoped-`find()` body. Update the why-comment: keep the Anti-Pattern 1 dual-row rationale, ADD the servable-membership note — resolution checks membership in the SERVABLE set, never raw row existence, so (a) the resolver is order-independent of snapshot row order, (b) Phase-24's ~265 non-allowlisted nousresearch snapshot rows resolve to openrouter (not nousresearch) — the exact silent-swap class this phase exists to prevent, (c) `claude-sonnet-5` → opencode and `big-pickle` → opencode are DELIBERATE consequences (both are npm-gated servable under opencode; neither is in the anthropic allowlist), (d) the D-23-07 ranking (nousresearch BEFORE openrouter) MUST be in this comment too.

    Do NOT touch: `instantiateModel`/`instantiateChain`/`defaultChain` (they live in modelFactory.ts and are UNCHANGED this plan — Phase 25), and do NOT edit `scripts/refresh-model-catalog.ts` (stays format-only per D-23-08, Phase 24 concern).
  </action>
  <acceptance_criteria>
    - `src/lib/models/catalog.ts` contains `export type ModelProviderId = 'anthropic' | 'openrouter' | 'nousresearch' | 'opencode';`
    - `src/lib/models/catalog.ts` contains `export type ProviderGate = { allowlist?: readonly string[]; npm?: readonly string[] };`
    - `src/lib/models/catalog.ts` contains `export const NOUSRESEARCH_ALLOWLIST: readonly string[] = ['nousresearch/hermes-4-70b', 'nousresearch/hermes-4-405b'];`
    - `src/lib/models/catalog.ts` contains `export const OPENCODE_NPM_GATE: readonly string[] = ['@ai-sdk/openai-compatible', '@ai-sdk/anthropic'];`
    - `src/lib/models/catalog.ts` contains `SNAPSHOT_PROVIDER_IDS` with `opencode: ['opencode', 'opencode-go']`
    - `src/lib/models/catalog.ts` contains `PROVIDER_PRECEDENCE` whose array is exactly `['anthropic', 'nousresearch', 'openrouter', 'opencode']` (index 1 is `'nousresearch'`, NOT `'openrouter'` — Pitfall 1 warning sign)
    - `src/lib/models/catalog.ts` contains `export function dedupeProviderRows(` returning rows and using `SNAPSHOT_PROVIDER_IDS`
    - `src/lib/models/catalog.ts` contains `export const SERVABLE_PROVIDERS: readonly ModelProviderId[] = ['anthropic', 'openrouter', 'nousresearch', 'opencode'];`
    - `getProviderForModelId` body contains a `for` loop over `PROVIDER_PRECEDENCE` and a `getServableIdsForProvider(catalog, provider).includes(id)` membership check — NO `catalog.models.find(` in its body
    - `getServableIdsForProvider` body calls `dedupeProviderRows` and references both `gate.npm` and `gate.allowlist`
  </acceptance_criteria>
  <verify>
    <automated>grep -n "PROVIDER_PRECEDENCE: readonly ModelProviderId\[\] = \['anthropic', 'nousresearch', 'openrouter', 'opencode'\]" src/lib/models/catalog.ts && npx tsx -e "import { getProviderForModelId, getServableIdsForProvider, dedupeProviderRows, PROVIDER_PRECEDENCE, SERVABLE_PROVIDERS } from './src/lib/models/catalog'; import catalogJson from './src/lib/models/catalog.json'; const oc = getServableIdsForProvider(catalogJson, 'opencode'); if (oc.length !== 39) throw new Error('opencode servable count ' + oc.length + ' !== 39'); if (getProviderForModelId(catalogJson, 'claude-sonnet-4-6') !== 'anthropic') throw new Error('regression lock broken'); if (getProviderForModelId(catalogJson, 'claude-sonnet-5') !== 'opencode') throw new Error('claude-sonnet-5 rework'); if (getProviderForModelId(catalogJson, 'big-pickle') !== 'opencode') throw new Error('big-pickle rework'); if (dedupeProviderRows(catalogJson, 'opencode').length !== 65) throw new Error('pool ' + dedupeProviderRows(catalogJson, 'opencode').length + ' !== 65'); if (getServableIdsForProvider(catalogJson, 'nousresearch').length !== 0) throw new Error('nousresearch snapshot must be empty (Phase 24)'); console.log('OK providers=' + SERVABLE_PROVIDERS.length + ' precedence=' + PROVIDER_PRECEDENCE.join(',') + ' opencode=' + oc.length);"</automated>
  </verify>
  <done>catalog.ts exports the full 4-provider registry surface; the smoke prints OK with providers=4, precedence=anthropic,nousresearch,openrouter,opencode, opencode=39; the regression lock + both reworks resolve as specified.</done>
</task>

<task type="auto">
  <name>Task 2: catalog.test.ts — extended canary suite (hermes fixture, count-stability 39, no-flip 12/5, reworks, SERVABLE_PROVIDERS=4)</name>
  <files>src/lib/models/catalog.test.ts</files>
  <read_first>
    src/lib/models/catalog.test.ts (current — 228 lines; the file being modified)
    src/lib/models/catalog.ts (registry just landed in Task 1)
    .planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md Pitfalls 2-3, 5 (l.285-307) and the Validation Architecture table rows for REG-01/03/04/05
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §catalog.test.ts (l.117-168)
  </read_first>
  <action>
    Rework src/lib/models/catalog.test.ts to the 4-provider canary suite. Extend the import list (l.3-13) with `dedupeProviderRows`, `PROVIDER_PRECEDENCE`, `SNAPSHOT_PROVIDER_IDS`, `NOUSRESEARCH_ALLOWLIST`, `OPENCODE_NPM_GATE`. The inline fixture (l.22-103) and snapshot dual-canary convention (l.16-21 header) stay — every behavior is pinned TWICE (fixture for semantics, catalogJson for roster truth).

    A) EXTEND the fixture — APPEND these rows after the existing ones (preserve all existing rows; the opencode dual row for claude-sonnet-4-6 must stay FIRST in the array):
    - `{ id: 'nousresearch/hermes-4-70b', providerID: 'nousresearch', name: 'Hermes 4 70B', family: 'hermes', status: 'active', api: { npm: '@ai-sdk/openai-compatible', url: 'https://inference-api.nousresearch.com/v1' }, cost: { input: 0.0000016, output: 0.000008 }, limit: { context: 200000, output: 32000 } }` — allowlisted pin.
    - `{ id: 'nousresearch/hermes-4-70b', providerID: 'openrouter', name: 'Hermes 4 70B', family: 'hermes', status: 'active', api: { npm: '@openrouter/ai-sdk-provider', url: '' }, cost: { input: 0.2, output: 0.6 }, limit: { context: 200000, output: 32000 } }` — the OPENROUTER MIRROR (D-23-07: mirrors present so the precedence canary is non-vacuous).
    - The same pair for `nousresearch/hermes-4-405b` (nousresearch row + openrouter mirror).
    - `{ id: 'deepseek-v4-flash', providerID: 'opencode', name: 'DeepSeek V4 Flash', family: 'deepseek', status: 'active', api: { npm: '@ai-sdk/openai-compatible', url: 'https://opencode.ai/zen/v1' }, cost: { input: 0, output: 0 }, limit: { context: 200000, output: 32000 } }` — Zen row.
    - `{ id: 'deepseek-v4-flash', providerID: 'opencode-go', name: 'DeepSeek V4 Flash', family: 'deepseek', status: 'active', api: { npm: '@ai-sdk/openai-compatible', url: 'https://opencode.ai/zen/go/v1' }, cost: { input: 0, output: 0 }, limit: { context: 200000, output: 32000 } }` — Go row (the dual-listed pair — Zen must win in `dedupeProviderRows`).
    - `{ id: 'hy3', providerID: 'opencode-go', name: 'Hy3', family: 'hy3', status: 'active', api: { npm: '@ai-sdk/openai-compatible', url: 'https://opencode.ai/zen/go/v1' }, cost: { input: 0, output: 0 }, limit: { context: 200000, output: 32000 } }` — Go-EXCLUSIVE row (survives dedup, keeps the Go url).

    B) UPDATE the two fixture tests whose expected arrays grow with the new rows:
    - `getServableIdsForProvider(fixture, 'openrouter')` (l.128-132) → expected `['anthropic/claude-sonnet-4.6', 'nousresearch/hermes-4-70b', 'nousresearch/hermes-4-405b']` (mirrors are active openrouter rows; the deprecated `:free` row stays excluded). Keep the D-02 no-allowlist rationale in the test name.
    - `getUnionServableIds(fixture)` (l.150-155) → expected `['claude-sonnet-4-6', 'anthropic/claude-sonnet-4.6', 'nousresearch/hermes-4-70b', 'nousresearch/hermes-4-405b', 'big-pickle', 'deepseek-v4-flash', 'hy3']` (Set-deduped across the 4 providers in SERVABLE_PROVIDERS order).

    C) REWORK (never delete — repo convention, with a why-comment citing the servable-membership semantic change, research Pitfall 2):
    - `big-pickle` fixture canary (l.173-176): `expect(getProviderForModelId(fixture, 'big-pickle')).toBeNull()` → `.toBe('opencode')` — big-pickle's opencode row is npm-gated servable; under servable-membership resolution it now resolves. Keep the `'unknown-id' → null` assertion (fail-closed unchanged).
    - `claude-sonnet-5` SNAPSHOT canary (l.186-188): `'anthropic'` → `'opencode'` with a why-comment: claude-sonnet-5 is NOT in the anthropic sonnet-only allowlist, but its opencode Claude row is npm-gated (`@ai-sdk/anthropic`) servable — the old `'anthropic'` assertion locked raw-row existence, which was only correct while opencode wasn't servable.

    D) ADD the new fixture canaries (D-23-07):
    - `getProviderForModelId(fixture, 'nousresearch/hermes-4-70b')` → `'nousresearch'` and the same for `'nousresearch/hermes-4-405b'` — the openrouter mirror rows are present, so the precedence ranking (nousresearch outranks openrouter) is what makes this pass; name the test "D-23-07 hermes precedence over the openrouter mirror".
    - `getProviderForModelId(fixture, 'deepseek-v4-flash')` → `'opencode'` (the dual-listed pair; the Zen row wins dedup and is npm-gated servable).
    - `getProviderForModelId(fixture, 'hy3')` → `'opencode'` (Go-exclusive id still resolves to the logical opencode provider).
    - `getServableIdsForProvider(fixture, 'nousresearch')` → `['nousresearch/hermes-4-70b', 'nousresearch/hermes-4-405b']` (allowlist ∩ fixture — REG-04).
    - `getServableIdsForProvider(fixture, 'opencode')` → `['claude-sonnet-4-6', 'big-pickle', 'deepseek-v4-flash', 'hy3']` (npm gate over the deduped pool; deepseek-v4-flash appears ONCE via the Zen row).
    - `dedupeProviderRows(fixture, 'opencode')` no-flip shape: the deepseek-v4-flash row in the result has `providerID: 'opencode'` and `api.url: 'https://opencode.ai/zen/v1'`; the hy3 row has `api.url: 'https://opencode.ai/zen/go/v1'`.

    E) UPDATE the `PROVIDER_GATES / SERVABLE_PROVIDERS` describe (l.195-204):
    - `SERVABLE_PROVIDERS` → `['anthropic', 'openrouter', 'nousresearch', 'opencode']` (rename the test title: "lists exactly the four servable providers").
    - Add: `PROVIDER_GATES.nousresearch.allowlist` toEqual the two hermes pins (and `.every((id) => !id.includes('~'))` — never `~latest`, D-23-05); `PROVIDER_GATES.opencode.npm` toEqual `['@ai-sdk/openai-compatible', '@ai-sdk/anthropic']`; `PROVIDER_GATES.openrouter.allowlist` undefined (D-02).
    - Add: `PROVIDER_PRECEDENCE` toEqual `['anthropic', 'nousresearch', 'openrouter', 'opencode']` (with the why-comment note: index 1 is nousresearch — the D-23-07 ranking, NOT the roadmap prose order).

    F) ADD the SNAPSHOT canaries (real catalogJson — drift detection, D-02 roster-verify doctrine):
    - COUNT-STABILITY (D-23-02): `getServableIdsForProvider(catalogJson, 'opencode')` length === 39; the npm split via `dedupeProviderRows(catalogJson, 'opencode')` filtered by the gate: `openai-compatible` === 23 and `anthropic` === 16; zero servable rows with `api.npm === '@ai-sdk/openai'` or `'@ai-sdk/google'`. Why-comment reconciles: "49 = 30+19 is the PRE-DEDUP npm-gated raw count (D-23-02); Zen-wins dedup collapses 10 dual servable pairs → the registry returns 39 — locking 49 would assert a shape the registry never returns".
    - NO-FLIP (D-23-09): `dedupeProviderRows(catalogJson, 'opencode').length` === 65; the 12 dual-listed ids (deepseek-v4-flash, deepseek-v4-pro, glm-5.1, glm-5.2, gpt-5.6-luna, grok-4.5, kimi-k2.6, kimi-k2.7-code, kimi-k3, minimax-m2.7, minimax-m3, qwen3.6-plus — verified 2026-08-03) resolve to the `'opencode'` (Zen) row with `api.url === 'https://opencode.ai/zen/v1'`; the 5 go-exclusive ids (hy3, mimo-v2.5, mimo-v2.5-pro, qwen3.7-max, qwen3.7-plus) keep `providerID: 'opencode-go'` with `api.url === 'https://opencode.ai/zen/go/v1'`; assert "no id's endpoint flipped" by checking both lists' winning rows. Why-comment cites D-23-09 determinism + snapshot shape.
    - ADD: `getProviderForModelId(catalogJson, 'big-pickle')` → `'opencode'`.
    - ADD (Pitfall 5 boundary): `getServableIdsForProvider(catalogJson, 'nousresearch')` → `[]` — the snapshot has 0 nousresearch rows; the live hermes canary is a Phase 24 task (D-23-07) once the rows land. Name the test to say exactly that.
    - REWORK the union slash contract (l.157-161): the assertion `union.filter((id) => !id.includes('/')).length` toEqual 1 becomes STRUCTURAL — `toBe(getServableIdsForProvider(catalogJson, 'anthropic').length + getServableIdsForProvider(catalogJson, 'opencode').length)` (both providers' servable ids are bare; opencode entering the union adds 39 bare ids — a hardcoded 1 would fail now and silently rot later). Keep the `new Set(union).size === union.length` dedup assertion unchanged. Why-comment: provider-aware slash contract — anthropic + opencode bare, openrouter vendor/model.
    - ADD: every opencode servable id is slash-free (`ids.every((id) => !id.includes('/'))`).

    Do NOT delete any existing test block; the existing anthropic/openrouter snapshot canaries (l.134-146, 178-184, 190-192) stay byte-identical except where listed above.
  </action>
  <acceptance_criteria>
    - `src/lib/models/catalog.test.ts` imports `dedupeProviderRows`, `PROVIDER_PRECEDENCE`, `SNAPSHOT_PROVIDER_IDS`, `NOUSRESEARCH_ALLOWLIST`, `OPENCODE_NPM_GATE`
    - The fixture contains: hermes-4-70b + hermes-4-405b as BOTH nousresearch and openrouter rows; deepseek-v4-flash as BOTH opencode and opencode-go rows; hy3 as opencode-go-only
    - A test asserts `getProviderForModelId(fixture, 'nousresearch/hermes-4-70b')` equals `'nousresearch'` with the openrouter mirror present (non-vacuous D-23-07)
    - A test named with count-stability asserts `getServableIdsForProvider(catalogJson, 'opencode')` length 39 and the npm split {openai-compatible: 23, anthropic: 16}
    - A test named with no-flip asserts `dedupeProviderRows(catalogJson, 'opencode')` length 65, the 12 dual-listed ids keep the Zen URL, and the 5 go-exclusive ids (hy3, mimo-v2.5, mimo-v2.5-pro, qwen3.7-max, qwen3.7-plus) keep the Go URL
    - A test asserts `getProviderForModelId(catalogJson, 'claude-sonnet-5')` equals `'opencode'` with a why-comment (rework, not delete)
    - A test asserts `getProviderForModelId(catalogJson, 'big-pickle')` equals `'opencode'`
    - A test asserts `getServableIdsForProvider(catalogJson, 'nousresearch')` equals `[]` (Phase-24 boundary)
    - A test asserts `SERVABLE_PROVIDERS` toEqual `['anthropic', 'openrouter', 'nousresearch', 'opencode']`
    - The union slash-contract test's bare-id count is structural (anthropic + opencode servable lengths), not a hardcoded 1
    - `npx vitest run src/lib/models/catalog.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/models/catalog.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>All catalog.test.ts tests pass: 4-provider SERVABLE_PROVIDERS/PROVIDER_PRECEDENCE, hermes fixture precedence, count-stability (39/23/16), no-flip (65/12/5), REG-04 allowlist pins, the two reworked canaries with why-comments, and the structural union slash contract.</done>
</task>

<task type="auto">
  <name>Task 3: modelFactory.ts + modelFactory.test.ts — 4-entry PROVIDER_DEFAULT_MODELS (D-23-03, D-23-06)</name>
  <files>src/lib/agents/modelFactory.ts, src/lib/agents/modelFactory.test.ts</files>
  <read_first>
    src/lib/agents/modelFactory.ts (current — 77 lines; PROVIDER_DEFAULT_MODELS at :28-31, OPENROUTER_DEFAULT_MODEL_ID at :24)
    src/lib/agents/modelFactory.test.ts (current — 105 lines; defaults assertion at :98-104, vi.hoisted mock seam at :7-19)
    .planning/phases/23-provider-registry-servable-sources/23-RESEARCH.md Pitfall 5 (l.303-307) and Code Examples "default-map extension" (l.332-341)
    .planning/phases/23-provider-registry-servable-sources/23-PATTERNS.md §modelFactory.ts (l.172-200) and §modelFactory.test.ts (l.204-233)
    src/lib/models/catalog.ts (registry from Task 1 — FAST_MODEL_ID, ModelProviderId)
  </read_first>
  <action>
    A) src/lib/agents/modelFactory.ts — extend the D-07 default map. Keep byte-identical: the imports (l.1-5), the module-singleton comment + `createOpenRouter` instance (l.7-17), `OPENROUTER_DEFAULT_MODEL_ID` (l.24), `instantiateModel` (l.38-61 — dispatch stays 2-provider, Phase 25 concern; new-provider ids hit the fail-loud throw, research A4), `instantiateChain` (l.65-67), `defaultChain` (l.75-77 — stays the Anthropic fast path; the D-11 comment stands).

    ADD after `OPENROUTER_DEFAULT_MODEL_ID`:
    - `export const NOUSRESEARCH_DEFAULT_MODEL_ID = 'nousresearch/hermes-4-70b';` with a why-comment citing D-23-06: sonnet-class cost philosophy (cheaper/faster workhorse) — the 405b stays servable but is not the reset target; pinned concrete id, never `~`/`:free`/auto (D-07 doctrine). Rows land in Phase 24; the live-snapshot servability assertion is a Phase 24 task (D-23-07 / research Pitfall 5).
    - `export const OPENCODE_DEFAULT_MODEL_ID = 'claude-sonnet-4-6';` with a why-comment citing D-23-03: mirrors the D-07 sonnet-class philosophy — SAME id as the anthropic default (deliberate: keep-if-valid re-badges, never resets — D-23-04); roster-verified 2026-08-03 against the committed snapshot's opencode dual row (sorts first, npm-gated servable); stable cost captions.

    REWORK `PROVIDER_DEFAULT_MODELS` (l.28-31) to the 4-entry map:
    ```
    export const PROVIDER_DEFAULT_MODELS: Record<ModelProviderId, string> = {
      anthropic: FAST_MODEL_ID,
      openrouter: OPENROUTER_DEFAULT_MODEL_ID,
      nousresearch: NOUSRESEARCH_DEFAULT_MODEL_ID,
      opencode: OPENCODE_DEFAULT_MODEL_ID,
    };
    ```
    Update the D-07 comment above it: the map is consumed by the Phase 21/26 provider-switch reset-to-provider-default — NOT by defaultChain(). The `Record<ModelProviderId, string>` type is what TS-enforces the 4 entries at compile time (Pitfall 9).

    B) src/lib/agents/modelFactory.test.ts — update the defaults assertions:
    - The D-07 defaults test (l.98-104) grows: `expect(PROVIDER_DEFAULT_MODELS).toEqual({ anthropic: 'claude-sonnet-4-6', openrouter: 'anthropic/claude-sonnet-4.6', nousresearch: 'nousresearch/hermes-4-70b', opencode: 'claude-sonnet-4-6' })`; add `expect(NOUSRESEARCH_DEFAULT_MODEL_ID).toBe('nousresearch/hermes-4-70b')` and `expect(OPENCODE_DEFAULT_MODEL_ID).toBe('claude-sonnet-4-6')` (import both new consts at l.21-27).
    - ADD a servability test against the LIVE snapshot for the opencode default only: `import catalogJson from '@/lib/models/catalog.json'` + `import { getServableIdsForProvider } from '@/lib/models/catalog'`, assert `getServableIdsForProvider(catalogJson, 'opencode').includes(OPENCODE_DEFAULT_MODEL_ID)` — the opencode row exists in the committed snapshot (roster-verified, D-23-03). Do NOT assert nousresearch default servability against the live snapshot (0 nousresearch rows — research Pitfall 5; that assertion is Phase 24).
    - Do NOT touch the instantiateModel dispatch tests (l.37-96) — with the 4-way union, `instantiateModel('claude-sonnet-4-6')` still dispatches anthropic (precedence regression lock), `anthropic/claude-sonnet-4.6` → openrouter, the unknown-id fail-loud throw is unchanged, and the vi.hoisted mocks (l.7-19) stay as-is (createOpenAICompatible mocks arrive in Phase 25, NOT here).
  </action>
  <acceptance_criteria>
    - `src/lib/agents/modelFactory.ts` contains `export const NOUSRESEARCH_DEFAULT_MODEL_ID = 'nousresearch/hermes-4-70b';`
    - `src/lib/agents/modelFactory.ts` contains `export const OPENCODE_DEFAULT_MODEL_ID = 'claude-sonnet-4-6';`
    - `src/lib/agents/modelFactory.ts` contains `PROVIDER_DEFAULT_MODELS` with exactly 4 entries keyed `anthropic`, `openrouter`, `nousresearch`, `opencode`
    - `src/lib/agents/modelFactory.ts` `instantiateModel` body still has only `'anthropic'` and `'openrouter'` branches (no nousresearch/opencode dispatch — Phase 25)
    - `src/lib/agents/modelFactory.test.ts` asserts `PROVIDER_DEFAULT_MODELS` toEqual the 4-entry map
    - `src/lib/agents/modelFactory.test.ts` asserts `getServableIdsForProvider(catalogJson, 'opencode')` contains `OPENCODE_DEFAULT_MODEL_ID`
    - `src/lib/agents/modelFactory.test.ts` has NO assertion of nousresearch default servability against the live snapshot
    - `npx vitest run src/lib/agents/modelFactory.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/agents/modelFactory.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>modelFactory exposes the 4-entry defaults map with both new consts; the opencode default is proven servable against the live snapshot; the nousresearch live-servability assertion is correctly absent (Phase 24 per D-23-07); dispatch unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry → resolver consumers (modelFactory, page props, save validation) | Provider identity is DERIVED here (D-01); a wrong resolution = a silent provider swap at instantiation |
| snapshot → servable sets | The committed catalog.json is the only servable authority (T-17-03); gates/dedup are pure reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Spoofing | `getProviderForModelId` resolution | mitigate | Deterministic servable-membership precedence iteration over `PROVIDER_PRECEDENCE = ['anthropic','nousresearch','openrouter','opencode']` (nousresearch outranks openrouter — D-23-07); returns null for unknown ids (fail-closed). Locked by the hermes fixture canary (mirrors present, non-vacuous), the claude-sonnet-4-6 regression canary, and the reworked claude-sonnet-5/big-pickle canaries |
| T-23-02 | Spoofing | Zen-wins dedup endpoint flips | mitigate | `SNAPSHOT_PROVIDER_IDS.opencode = ['opencode','opencode-go']` first-wins ordering is DATA (D-23-08); the no-flip canary (12 dual → Zen URL, 5 go-exclusive → Go URL, 65-row pool) locks determinism + snapshot shape (D-23-09); the count-stability canary (39/23/16, 0 GPT/Gemini) trips loudly on Phase-24 regeneration drift (D-23-02) |
| T-23-03 | Tampering | Id injection via the widened union | mitigate | No new write paths — `saveSettingsAction` membership validation is untouched (23-03 verifies); the resolver's servable-membership check (not raw row existence) prevents non-allowlisted ids from resolving to a provider they can't serve — the Phase-25 silent-swap landmine is closed here, not deferred |
| T-23-04 | Information Disclosure | catalog.json / new env keys in client bundles | mitigate | This plan touches NO client-reachable code — catalog.ts/modelFactory.ts are server-only modules; `PROVIDER_NAMES` (23-04) is the only client-adjacent addition and it imports type-only (T-17-09). The VER-04 security-grep gate scans only `OPENROUTER` tokens — unchanged, still green (`npm test`) |
| T-23-SC | Tampering | npm/pip/cargo installs | accept | Phase 23 installs ZERO packages (CONTEXT.md line 11 — `@ai-sdk/openai-compatible` is a Phase 25 install, already npm-verified in v1.5 research). Do not run `npm install`/`npm audit fix --force` in this plan |
</threat_model>

<verification>
1. `npx vitest run src/lib/models/catalog.test.ts src/lib/agents/modelFactory.test.ts` → all pass (keystone canaries + defaults)
2. `npx vitest run src/lib/agents/modelConfig.test.ts` → still green (resolveModelChain widens mechanically via `getUnionServableIds` — verify-only, no code change)
3. `npx tsc --noEmit` → 0 errors — the union growth + same-plan modelFactory migration keeps the build green (v1.4 19-01 doctrine); this gate is what proves no consumer was left with a 2-entry `Record<ModelProviderId, ...>`
4. Snapshot geometry unchanged: catalog.json is NOT regenerated this plan (Phase 24 owns the data)
</verification>

<success_criteria>
- catalog.ts exports the 4-provider surface: `ModelProviderId`(4), `ProviderGate`, `PROVIDER_GATES`(4), `SERVABLE_PROVIDERS`(4), `SNAPSHOT_PROVIDER_IDS`, `PROVIDER_PRECEDENCE` (nousresearch at index 1), `dedupeProviderRows`, servable-membership `getProviderForModelId`
- The opencode servable set is exactly the 39 npm-gated, Zen-wins-deduped ids (23 chat + 16 Claude, 0 GPT/Gemini); the deduped pool is 65 rows; 12 dual-listed ids keep the Zen row; 5 go-exclusive ids keep Go rows (count-stability + no-flip canaries green)
- NousResearch servable set is the hermes-4 pair allowlist pins (fixture-proven); the live snapshot returns `[]` until Phase 24 (Pitfall 5 boundary documented)
- Resolver: `claude-sonnet-4-6` → anthropic (regression lock), hermes pair → nousresearch over their openrouter mirrors (D-23-07), `big-pickle` + `claude-sonnet-5` → opencode (deliberate reworks with why-comments), `anthropic/claude-sonnet-4.6` → openrouter
- `PROVIDER_DEFAULT_MODELS` is the 4-entry `Record<ModelProviderId, string>` with `nousresearch/hermes-4-70b` (D-23-06) and `claude-sonnet-4-6` (D-23-03, roster-verified servable) — REG-06
- `instantiateModel` dispatch is provably unchanged (2-provider, fail-loud for the new providers until Phase 25 — research A4)
</success_criteria>

<output>
Create `.planning/phases/23-provider-registry-servable-sources/23-01-SUMMARY.md` when done
</output>
