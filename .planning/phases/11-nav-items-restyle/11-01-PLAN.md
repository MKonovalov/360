---
phase: 11-nav-items-restyle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/layout/app-sidebar.tsx]
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04]

must_haves:
  truths:
    - "Nav items are grouped into intent-labeled sections via SidebarGroupLabel — Explore (Start, Companies, Key Personas) and Manage (Reviews) — same 4 routes, no new routes (NAV-01)"
    - "Nav rows follow the Exa anatomy — 30px row height, 16px monochrome lucide icon + 15px/400 label, 10px icon-label gap, 8px horizontal padding (NAV-02)"
    - "The active nav row is a subtle full-row gray fill via the tokenized data-active:bg-sidebar-accent (#909090, 4px radius) with darker text — the v1.1 indigo-50/indigo-600 active treatment is gone (NAV-03)"
    - "The pending-reviews badge keeps its count semantics in Exa mono accent-chip style (mono 10px/600, sidebar-accent fill + accent-foreground text, right-aligned) with a collapsed-rail dot variant on Reviews (NAV-04)"
  artifacts:
    - path: "src/components/layout/app-sidebar.tsx"
      provides: "Grouped nav (SidebarGroupLabel), Exa row anatomy, getActiveNavKey-driven active state, mono accent-chip badge + collapsed dot"
      contains: "SidebarGroupLabel"
      min_lines: 90
  key_links:
    - from: "src/components/layout/app-sidebar.tsx"
      to: "src/lib/nav.ts"
      via: "import { getActiveNavKey } — active-key computation moves from 4 inline pathname expressions to the tested pure function"
      pattern: "getActiveNavKey"
    - from: "src/components/layout/app-sidebar.tsx (isActive props)"
      to: "src/components/ui/sidebar.tsx sidebarMenuButtonVariants (line 469)"
      via: "primitive data-active:bg-sidebar-accent data-active:font-medium data-active:text-sidebar-accent-foreground — the gray active fill resolves --sidebar-accent #909090 from the Phase 10 scoped token block"
      pattern: "data-active:bg-sidebar-accent"
---

<objective>
Restyle the four nav items in `src/components/layout/app-sidebar.tsx` to the Exa item anatomy per the APPROVED UI-SPEC (Phase 11's locked design contract): regroup the same 4 routes into intent-labeled Explore/Manage sections via `SidebarGroupLabel` (NAV-01), apply the 30px/16px/15px/10px/8px row anatomy (NAV-02), swap the active state to the tokenized gray fill by consuming the Phase 10 `getActiveNavKey` function and deleting the 4 inline indigo `data-active` class overrides (NAV-03), and restyle the pending-reviews badge into the mono accent chip with a collapsed-rail dot variant (NAV-04).

Purpose: This is the first consumer phase of the Phase 10 token foundation. The scoped `--sidebar-*` block on `[data-sidebar="sidebar"]` already themes the panel and the vendored primitive already carries the tokenized active/hover/focus classes (`sidebar.tsx:469`) — so NAV-02/NAV-03/NAV-04 are achieved by (a) adding anatomy-override className fragments to each `SidebarMenuButton`, (b) deleting the hardcoded indigo overrides, and (c) consuming `getActiveNavKey` (QLTY-01 contract, UI-SPEC line 178). Zero new packages, zero edits to the vendored `sidebar.tsx`, zero new tokens — the phase consumes Phase 10's 8 tokens only.

Hard constraints honored: routes unchanged (same 4 `href`s); `getActiveNavKey` consumed from `src/lib/nav.ts`; `pendingCount` server-driven badge gating preserved; zero new npm packages; zero edits to vendored `src/components/ui/sidebar.tsx`; `@theme inline` untouched; no `.dark`/`dark:` variants; no new tokens added to globals.css.

Output: `src/components/layout/app-sidebar.tsx` fully restyled — the single source-file change of Phase 11's nav restyle. Visual live-browser confirmation is Phase 14's UAT contract; this plan verifies via type gates, the Phase 10 nav regression suite, and source-assertion grep gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (§Phase 11 — success criteria 1-4)
@.planning/STATE.md (§Accumulated Context — Phase 10 decisions: nav key is 'personas'; function-first order)
@.planning/REQUIREMENTS.md (NAV-01..04)
@.planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md (§Typography Body 15px/400 + Label 13px/600 + Display 10px/600 Geist Mono; §Spacing 30px/10px/8px/12px; §Interaction & State Contract — active = data-active:bg-sidebar-accent + font-medium + accent-foreground, 4px radius, hover==active fill; §Copywriting Badge row; §Color — accent reserved elements)
@.planning/phases/10-sidebar-token-foundation/10-RESEARCH.md (§Architectural Responsibility Map; §Standard Stack — no installs; §Security Domain — negative security behavior)
@.planning/phases/10-sidebar-token-foundation/10-02-SUMMARY.md (§Next Phase Readiness — the exact consumer-swap contract: app-sidebar.tsx:39 → `=== 'start'`, :48 → `=== 'companies'`, :57 → `=== 'personas'`, :66 → `=== 'reviews'`)
@.planning/phases/10-sidebar-token-foundation/10-VALIDATION.md (validation architecture — per-task map rows 11-01-01..03)
@.planning/phases/11-nav-items-restyle/11-VALIDATION.md (Phase 11 validation contract — sampling rate, targeted/full gates)
@src/components/layout/app-sidebar.tsx (the file being modified — 85 lines, current state)
@src/components/ui/sidebar.tsx (vendored primitive — READ ONLY, do not edit; lines 381-410 SidebarGroup/SidebarGroupLabel, 457-488 SidebarMenuItem/sidebarMenuButtonVariants, 566-581 SidebarMenuBadge)
@src/lib/nav.ts (getActiveNavKey + NavKey — Phase 10 shipped, consumed here)
@src/lib/nav.test.ts (the 11-case regression lock)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regroup nav into Explore/Manage sections with SidebarGroupLabel + add lucide icons (NAV-01)</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx — the file being modified. Current state: one `SidebarGroup` (line 31) wrapping a single `SidebarMenu` of 4 `SidebarMenuItem`s; only the Reviews row (line 70) renders an icon (`<Inbox />`); Start/Companies/Key Personas rows have text-only labels.
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Typography (Label = 13px/600 for group labels "Explore"/"Manage") and §Spacing exceptions table (12px vertical separation between section groups — Exa `margin:12px 0`).
    - src/components/ui/sidebar.tsx lines 381-410 — `SidebarGroup` (p-2) and `SidebarGroupLabel` (h-8, px-2, text-xs font-medium text-sidebar-foreground/70) primitive contract. READ ONLY.
    - src/components/layout/app-sidebar.tsx:5 — existing lucide import style is bare names (`Inbox`), not the `XIcon`-suffixed style used elsewhere in src/components.
  </read_first>
  <action>
    Restructure the nav JSX in `src/components/layout/app-sidebar.tsx` into two intent-labeled groups. Import `SidebarGroupLabel` from `@/components/ui/sidebar` and add `LayoutDashboard`, `Building2`, `Users` to the existing lucide-react import (keep `Inbox`; match the file's bare-name import style — do NOT use the `XIcon` suffix convention found in other files).

    Structure: keep the outer `SidebarContent`, then two `SidebarGroup` blocks. Group 1 renders `<SidebarGroupLabel className="text-[13px] font-semibold">Explore</SidebarGroupLabel>` followed by a `SidebarMenu` containing the Start, Companies, and Key Personas `SidebarMenuItem`s in their current order. Group 2 renders `<SidebarGroupLabel className="text-[13px] font-semibold">Manage</SidebarGroupLabel>` with a `SidebarMenu` containing the Reviews item, and carries `className="-mt-1"` on the `SidebarGroup` itself so the inter-group separation lands at the Exa 12px reference (16px default from the two p-2 groups minus 4px). The `text-[13px] font-semibold` override on each label is the UI-SPEC Label tier (13px/600); tailwind-merge resolves it over the primitive's `text-xs font-medium`.

    Add the lucide icon to each previously icon-less row, mirroring the Reviews row's existing shape: the `Link` child gains the icon element before the `<span>Label</span>` — Start → `LayoutDashboard`, Companies → `Building2`, Key Personas → `Users`, Reviews keeps `Inbox`. No color classes on the icons — lucide inherits `currentColor`, so they render monochrome in `text-sidebar-foreground` (the primitive's `[&_svg]:size-4` sizes them to 16px automatically).

    Keep all four `href`s byte-identical (`/`, `/companies`, `/personas`, `/reviews`) and keep each `isActive`/`className` prop exactly as it currently is in this task (Task 2 rewrites them). Do NOT touch the `pendingCount` badge block (Task 3 owns it), the why-comments at the top of the component (Task 2 rewrites the active-detection comment), or any file other than `app-sidebar.tsx`.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      grep -c 'SidebarGroupLabel' src/components/layout/app-sidebar.tsx
      # expect 3 (1 import + 2 usages)
      grep -c '>Explore<' src/components/layout/app-sidebar.tsx
      grep -c '>Manage<' src/components/layout/app-sidebar.tsx
      # each expect 1
      grep -c 'LayoutDashboard' src/components/layout/app-sidebar.tsx
      grep -c 'Building2' src/components/layout/app-sidebar.tsx
      grep -c 'Users' src/components/layout/app-sidebar.tsx
      # each expect 1 (import line + JSX usage count as lines; import line counts once, JSX usage once — expect exactly 2 lines minimum; gate: each >= 1)
      grep -c 'href="/"' src/components/layout/app-sidebar.tsx
      grep -c 'href="/companies"' src/components/layout/app-sidebar.tsx
      grep -c 'href="/personas"' src/components/layout/app-sidebar.tsx
      grep -c 'href="/reviews"' src/components/layout/app-sidebar.tsx
      # each expect exactly 1 — same 4 routes, no new routes
      grep -c 'className="-mt-1"' src/components/layout/app-sidebar.tsx
      # expect 1
      grep -c 'text-\[13px\] font-semibold' src/components/layout/app-sidebar.tsx
      # expect 2 (both group labels)
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `SidebarGroupLabel` appears exactly 3 times (1 import + 2 usages); `>Explore<` and `>Manage<` each appear exactly once.
    - Each of `LayoutDashboard`, `Building2`, `Users` appears at least once (icon added to Start/Companies/Key Personas rows); `Inbox` retained on Reviews.
    - The 4 route hrefs each appear exactly once — same 4 routes, no new routes (NAV-01).
    - The Manage `SidebarGroup` carries `className="-mt-1"`; both group labels carry `text-[13px] font-semibold` (UI-SPEC Label tier 13px/600).
  </acceptance_criteria>
  <done>Nav is grouped into Explore (Start, Companies, Key Personas) and Manage (Reviews) via SidebarGroupLabel with 13px/600 labels; all four rows have 16px monochrome lucide icons; the 4 routes are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Exa row anatomy + consume getActiveNavKey, delete indigo active overrides (NAV-02, NAV-03)</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx — post-Task-1 state: 4 SidebarMenuButtons with the v1.1 indigo className overrides still on lines ~40/49/58/67 and inline `pathname === '/'` / `pathname.startsWith('/companies'|'/personas'|'/reviews')` isActive expressions at ~39/48/57/66; the why-comment block at lines 16-20 still documents the startsWith approach.
    - src/lib/nav.ts — the consumed contract: `getActiveNavKey(pathname): NavKey | null` (boundary-guarded prefix matching; `'/'` → `'start'`, others → prefix-with-boundary, null for /sign-in/''/siblings). Phase 10 shipped; do NOT modify.
    - src/lib/nav.test.ts — the 11-case regression lock this task must keep green (esp. `/companies/123` → 'companies' and `/companies-archive` → null).
    - src/components/ui/sidebar.tsx lines 468-488 — `sidebarMenuButtonVariants`: base `gap-2 rounded-md p-2 text-sm`, size default `h-8`, and the already-tokenized `data-active:bg-sidebar-accent data-active:font-medium data-active:text-sidebar-accent-foreground` plus `hover:bg-sidebar-accent hover:text-sidebar-accent-foreground` (line 469). READ ONLY.
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Typography (Body = 15px/400) and §Interaction & State Contract (active = data-active:bg-sidebar-accent #909090 + font-medium + accent-foreground #111111, 4px radius; hover==active fill; "Active on /companies/[id] must persist — never simplify to exact equality").
    - .planning/phases/10-sidebar-token-foundation/10-02-SUMMARY.md §Next Phase Readiness — the exact swap mapping (line 39 → 'start', 48 → 'companies', 57 → 'personas', 66 → 'reviews').
    - .planning/phases/11-nav-items-restyle/11-VALIDATION.md rows 11-01-02 (NAV-02) and 11-01-03 (NAV-03) — grep-gate expectations.
  </read_first>
  <action>
    Rewrite all four `SidebarMenuButton` blocks and the active-detection comment:

    1. Import `getActiveNavKey` from `@/lib/nav` (matches this file's existing `@/` import style). After the `usePathname()` call, add `const activeKey = getActiveNavKey(pathname);` and replace the four inline isActive expressions: `isActive={activeKey === 'start'}` for Start, `isActive={activeKey === 'companies'}` for Companies, `isActive={activeKey === 'personas'}` for Key Personas, `isActive={activeKey === 'reviews'}` for Reviews. Do NOT inline the function four times; compute once into `activeKey`.

    2. Replace the why-comment block at lines 16-20 (the old `.startsWith()` rationale) with a 3-4 line comment stating: active-key detection now comes from the tested `getActiveNavKey` pure function (`src/lib/nav.ts`, 11-case Vitest suite) so the `/companies/[id]` detail highlight and the `/companies-archive` sibling-prefix guard are permanently locked. CRITICAL GREP HYGIENE: the new comment must not contain the substrings `indigo` or `amber` (the QLTY-04 sweep gate greps the whole `src/components/layout/` directory) — refer to the "v1.1 colored active treatment" without those color words. Keep the existing `pendingCount` comment (lines 22-24) unchanged.

    3. Delete the four identical indigo className overrides (`data-active:bg-indigo-50 data-active:text-indigo-600 data-active:hover:bg-indigo-50 data-active:hover:text-indigo-600`) entirely and replace each button's className with the Exa anatomy override: `h-[30px] p-0 px-2 gap-2.5 rounded-[4px] text-[15px] font-normal`. Do NOT add any `data-active:*` classes of your own — the vendored primitive (sidebar.tsx:469) already applies `data-active:bg-sidebar-accent data-active:font-medium data-active:text-sidebar-accent-foreground`, and the Phase 10 scoped block resolves `--sidebar-accent: #909090` / `--sidebar-accent-foreground: #111111`, so the gray active fill, darker text, and hover==active fill are automatic. The four class fragments are the full anatomy: `h-[30px]` (30px row height, overriding the size-default `h-8`; the primitive's `group-data-[collapsible=icon]:size-8!` carries `!important` so the collapsed rail's 32px sizing is untouched — Phase 13 contract preserved), `p-0 px-2` (8px horizontal padding with zero vertical padding so the 16px icon fits the 30px border-box height), `gap-2.5` (10px icon-label gap, overriding `gap-2`), `rounded-[4px]` (4px pill radius, overriding `rounded-md`), `text-[15px] font-normal` (15px/400 label, overriding `text-sm`; `data-active:font-medium` from the primitive supplies the active weight).

    4. Keep every `href`, the icon elements added in Task 1, the group structure, and the badge block byte-identical.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      grep -c 'getActiveNavKey' src/components/layout/app-sidebar.tsx
      # expect >= 2 (import + activeKey call)
      grep -c 'pathname\.startsWith' src/components/layout/app-sidebar.tsx
      # expect 0 — all inline prefix checks gone
      grep -c 'pathname === ' src/components/layout/app-sidebar.tsx
      # expect 0 — no residual inline exact match; only activeKey comparisons
      grep -c 'activeKey === ' src/components/layout/app-sidebar.tsx
      # expect 4
      grep -c 'indigo' src/components/layout/app-sidebar.tsx
      # expect 0 — sweep begins here; keep the new comment free of the word
      grep -c 'h-\[30px\]' src/components/layout/app-sidebar.tsx
      grep -c 'gap-2\.5' src/components/layout/app-sidebar.tsx
      grep -c 'rounded-\[4px\]' src/components/layout/app-sidebar.tsx
      grep -c 'text-\[15px\]' src/components/layout/app-sidebar.tsx
      # each expect exactly 4 (one per nav row)
      grep -c 'p-0 px-2' src/components/layout/app-sidebar.tsx
      # expect 4
      npx vitest run src/lib/nav.test.ts --bail=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `pathname.startsWith` count in app-sidebar.tsx is 0 and `pathname === ` count is 0 — active detection is fully delegated to `getActiveNavKey` via the single `activeKey` computation with 4 `activeKey ===` comparisons (NAV-03 + QLTY-01 consumption contract).
    - `indigo` count in app-sidebar.tsx is 0 — the v1.1 indigo-50/indigo-600 active treatment and its className overrides are fully deleted.
    - `h-[30px]`, `p-0 px-2`, `gap-2.5`, `rounded-[4px]`, `text-[15px]` each appear exactly 4 times — one Exa-anatomy override per nav row (NAV-02: 30px rows, 8px horizontal padding, 10px gap, 4px radius, 15px labels).
    - `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 with all 11 cases passing — the `/companies/[id]` highlight and `/companies-archive` guard survive the swap.
    - No `data-active:*` classes were added to app-sidebar.tsx (the primitive's tokenized `data-active:bg-sidebar-accent` supplies the gray fill).
  </acceptance_criteria>
  <done>All four nav rows use the Exa anatomy overrides; isActive is driven by the tested getActiveNavKey; the 4 indigo className overrides and the old startsWith comment are gone; nav.test.ts 11/11 green.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle pending-reviews badge into the mono accent chip + add collapsed-rail dot (NAV-04)</name>
  <files>src/components/layout/app-sidebar.tsx</files>
  <read_first>
    - src/components/layout/app-sidebar.tsx — post-Task-2 state: the Reviews `SidebarMenuItem` still renders `<SidebarMenuBadge className="bg-amber-100 text-amber-800">{pendingCount} pending</SidebarMenuBadge>` inside a `{pendingCount > 0 && (...)}` guard (lines ~74-78).
    - src/components/ui/sidebar.tsx lines 566-581 — `SidebarMenuBadge` primitive: `pointer-events-none absolute right-1 flex h-5 min-w-5 items-center justify-center rounded-md px-1 text-xs font-medium text-sidebar-foreground tabular-nums select-none group-data-[collapsible=icon]:hidden ...` — already right-aligned and auto-hidden in the collapsed rail. READ ONLY.
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Typography (Display = 10px/600 Geist Mono — `font-mono` maps `--font-geist-mono` via `@theme inline`), §Copywriting Badge row (visible: count in mono 10px/600 accent chip, keeps `"{n} pending"` semantics; aria-label: `"{n} pending reviews"`), §Interaction & State Contract Collapsed row (icons stay, labels fade; badge hidden when collapsed).
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Color — the UI-SPEC reserves indigo-600 `#4f46e5` for exactly the pending-reviews badge chip (first listed), the collapsed-rail Reviews dot, and the letter-mark glyph. This plan deliberately diverges: the QLTY-04 sweep forbids hardcoded indigo classes in `src/components/layout/` (SC #5, grep gate = 0), so the badge chip consumes the `--sidebar-accent` token (`#909090` fill / `#111111` text — 3.11:1 and 5.91:1, both AA) per SC #4's "accent fill + accent text" reading as the sidebar-accent token. The UI-SPEC indigo reservation remains available to Phase 13's collapsed-rail items (dot restyle, letter-mark), where Phase 13 will own the mechanism.
    - .planning/phases/11-nav-items-restyle/11-VALIDATION.md row 11-01-04 (NAV-04) — grep-gate expectations.
  </read_first>
  <action>
    Restyle the badge and add the collapsed-rail dot inside the Reviews `SidebarMenuItem`, keeping the count-gating logic untouched:

    1. Replace the badge's className `bg-amber-100 text-amber-800` with the Exa mono accent chip: `bg-sidebar-accent text-sidebar-accent-foreground font-mono text-[10px] font-semibold`. tailwind-merge resolves these over the primitive base (`text-xs` → `text-[10px]`, `font-medium` → `font-semibold`, `text-sidebar-foreground` → `text-sidebar-accent-foreground`); the primitive's `right-1 h-5 min-w-5 rounded-md px-1 tabular-nums` and its auto-hide in the collapsed rail (`group-data-[collapsible=icon]:hidden`) remain. The chip is thus mono 10px/600, sidebar-accent fill (#909090) with accent-foreground text (#111111, 5.91:1 — AA), right-aligned. Keep the visible copy `{pendingCount} pending` verbatim (count semantics preserved) and add `role="status"` with `aria-label={`${pendingCount} pending reviews`}` per the UI-SPEC Copywriting contract.

    2. Add the collapsed-rail dot variant inside the same `{pendingCount > 0 && (...)}` guard (wrap badge + dot in a React fragment so the server-driven count gate is a single unchanged condition). The dot is a decorative presence indicator for the 48px collapsed rail: a `<span>` with `aria-hidden="true"` and className `absolute right-1.5 top-1/2 hidden h-2 w-2 -translate-y-1/2 rounded-full bg-sidebar-accent group-data-[collapsible=icon]:block`. Base `hidden` (invisible when expanded — the badge is the expanded-state signal), `group-data-[collapsible=icon]:block` (visible only when collapsed, mirroring the primitive's own `group-data-[collapsible=icon]:hidden` mechanism which proves the ancestor `group` + `data-collapsible` chain resolves from inside a menu item), `bg-sidebar-accent` (#909090, 3.11:1 on the #fbfcfd panel — passes AA 1.4.11). No count text in the dot — the count is carried by the badge (expanded) and by Phase 13's tooltips (collapsed rail legibility is Phase 13 scope).

    3. Do NOT add any hardcoded color utilities (no indigo/amber/slate/hex classes anywhere in this file). Do NOT touch the other three nav rows, the group structure, or the `activeKey` logic.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      grep -c 'bg-sidebar-accent' src/components/layout/app-sidebar.tsx
      # expect 2 (badge fill + collapsed dot)
      grep -c 'text-sidebar-accent-foreground' src/components/layout/app-sidebar.tsx
      # expect 1 (badge text; the primitive supplies the active-row peer variant)
      grep -c 'font-mono' src/components/layout/app-sidebar.tsx
      grep -c 'text-\[10px\]' src/components/layout/app-sidebar.tsx
      grep -c 'font-semibold' src/components/layout/app-sidebar.tsx
      # each expect 1 (badge chip only)
      grep -c 'pending reviews' src/components/layout/app-sidebar.tsx
      # expect 1 (aria-label)
      grep -c 'group-data-\[collapsible=icon\]:block' src/components/layout/app-sidebar.tsx
      # expect 1 (collapsed dot)
      grep -c 'amber' src/components/layout/app-sidebar.tsx
      # expect 0 — v1.1 amber badge treatment gone
      npx vitest run src/lib/nav.test.ts --bail=1
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `bg-sidebar-accent` appears exactly 2 times (badge fill + collapsed dot); `text-sidebar-accent-foreground`, `font-mono`, `text-[10px]`, `font-semibold` each appear exactly 1 time — the badge is the mono 10px/600 accent chip (NAV-04).
    - `amber` count is 0 — the v1.1 amber badge classes are fully deleted.
    - The visible copy `{pendingCount} pending` is retained and the badge carries `role="status"` + `aria-label` `"{n} pending reviews"` (UI-SPEC Copywriting contract).
    - The collapsed-rail dot (`group-data-[collapsible=icon]:block`, `bg-sidebar-accent`, `aria-hidden`) exists inside the same `pendingCount > 0` guard as the badge — count gating is unchanged and server-driven.
    - `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 (11/11).
  </acceptance_criteria>
  <done>Badge is the tokenized mono 10px/600 accent chip with "{n} pending" semantics, right-aligned; collapsed-rail dot variant present on Reviews; amber treatment gone; count gating intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client nav component → route links | `app-sidebar.tsx` renders 4 `<Link>`s to the auth-gated routes; a restyle bug (wrong href, wrong active key) degrades nav integrity without touching auth itself |
| Component className strings → Tailwind build | All className values are static literals in source; they resolve to the Phase 10 scoped tokens at runtime — no user input ever reaches a class string |
| Badge content → DOM | `{pendingCount} pending` renders as React text; `pendingCount` is a server-computed number threaded from `app-shell-layout.tsx` (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Integrity | `src/components/layout/app-sidebar.tsx` active-state + routes | mitigate | Routes stay byte-identical (`/`, `/companies`, `/personas`, `/reviews` — Task 1 grep gate, each href = 1). Active detection delegates to the Phase 10-tested `getActiveNavKey` (boundary-guarded; `/companies/[id]` highlight and `/companies-archive` null case locked by nav.test.ts — Task 2 runs the 11-case suite). `pendingCount` server-driven gating unchanged (Task 3 keeps the single `pendingCount > 0` guard). Auth itself untouched — `app-shell-layout.tsx` and route layouts are out of this plan's file fence. |
| T-11-02 | Tampering | hardcoded color reintroduction / token-scope leak | mitigate | The restyle consumes Phase 10's scoped `--sidebar-*` tokens exclusively: the gray active fill comes from the primitive's `data-active:bg-sidebar-accent` resolving `--sidebar-accent: #909090` (Task 2 deletes the 4 indigo overrides, adds zero `data-active:*` classes), the badge/dot use `bg-sidebar-accent`/`text-sidebar-accent-foreground` (Task 3). Grep gates: `indigo` and `amber` counts = 0 in app-sidebar.tsx. No globals.css edits, no new tokens, no `.dark`/`dark:` variants (hard constraints). |
| T-11-03 | Spoofing | badge content (count) | accept | `pendingCount` is a number from a server DB query, rendered as React text — escaped by React, no `dangerouslySetInnerHTML`, no user-controlled strings reach the chip. The aria-label duplicates the same numeric count. Low-value target; no mitigation beyond default React escaping. |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed this phase (hard constraint, PANE-02 exemption — RESEARCH §Package Legitimacy Audit). Any executor-initiated install is a plan violation and requires a blocking human checkpoint before proceeding. |
</threat_model>

<verification>
Phase-level checks (11-01, Wave 1 — runs alongside no other plan, so build contention is nil):
- `npx tsc --noEmit` exits 0 (run per task)
- `npx vitest run src/lib/nav.test.ts --bail=1` exits 0, 11/11 (run in Tasks 2 and 3 — the QLTY-01 regression lock)
- `npm test` full suite green (run at plan close — 23 files, 224 passed baseline from Phase 10)
- `npm run build` exits 0 (run at plan close)
- Grep gates: `indigo` = 0 and `amber` = 0 in `src/components/layout/app-sidebar.tsx`; the 4 hrefs each appear exactly once
- `git diff --stat` for this plan shows exactly `src/components/layout/app-sidebar.tsx` (plus nothing else); vendored `sidebar.tsx`, `globals.css`, `src/lib/nav.ts`, `src/lib/nav.test.ts` byte-identical
</verification>

<success_criteria>
Measurable completion (roadmap SC #1-#4):
- Nav grouped into Explore (Start, Companies, Key Personas) and Manage (Reviews) via `SidebarGroupLabel` with 13px/600 labels; same 4 routes, no new routes (SC #1, NAV-01)
- Nav rows use the Exa anatomy: `h-[30px]` rows, 16px monochrome lucide icons (`[&_svg]:size-4` from the primitive), 15px/400 labels, `gap-2.5` (10px) icon-label gap, `p-0 px-2` (8px) horizontal padding (SC #2, NAV-02)
- Active row = tokenized gray fill `data-active:bg-sidebar-accent` (#909090, 3.11:1 — the UI-SPEC divergence, NOT `rgba(0,0,0,0.04)`), 4px radius, darker `text-sidebar-accent-foreground`; indigo-50/indigo-600 treatment deleted (SC #3, NAV-03)
- Badge = mono 10px/600 accent chip (`bg-sidebar-accent text-sidebar-accent-foreground font-mono text-[10px] font-semibold`), `"{n} pending"` copy + `"{n} pending reviews"` aria-label, right-aligned, with a `bg-sidebar-accent` collapsed-rail dot on Reviews (SC #4, NAV-04)
- Zero new npm packages; zero edits to vendored `src/components/ui/sidebar.tsx`; no new tokens in globals.css; no `.dark`/`dark:` variants (hard constraints)
</success_criteria>

<output>
Create `.planning/phases/11-nav-items-restyle/11-01-SUMMARY.md` when done
</output>
