---
phase: 11-nav-items-restyle
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [src/components/layout/sidebar-resize-handle.tsx]
autonomous: true
requirements: [QLTY-04]

must_haves:
  truths:
    - "No hardcoded indigo/amber utilities remain in the sidebar subtree — the sweep is complete across src/components/layout/ (QLTY-04)"
    - "The panel's border-r is governed by border-sidebar-border via the Phase 10 companion rule — verified in place, not redone (QLTY-04)"
    - "The nav regression lock still passes — getActiveNavKey consumption and the /companies/[id] highlight survive the phase (QLTY-01 exercised; npm test green)"
  artifacts:
    - path: "src/components/layout/sidebar-resize-handle.tsx"
      provides: "Resize-handle hover affordance with no hardcoded palette color (token-derived hover)"
      contains: "bg-foreground/10"
      min_lines: 85
  key_links:
    - from: "src/components/layout/sidebar-resize-handle.tsx"
      to: ":root global tokens"
      via: "hover:bg-foreground/10 — the handle is a flex sibling of the sidebar, OUTSIDE the [data-sidebar=\"sidebar\"] subtree, so scoped sidebar tokens do not resolve there; the global foreground token at 10% gives a neutral, theme-safe hover with zero hardcoded palette colors"
      pattern: "bg-foreground/10"
    - from: "src/components/ui/sidebar.tsx line 236 (border-r, no color class)"
      to: "src/app/globals.css [data-slot=\"sidebar-container\"] companion rule"
      via: "unlayered border-color: var(--sidebar-border) supplies the hairline color — verified present, not re-implemented"
      pattern: "border-color: var(--sidebar-border)"
---

<objective>
Complete the QLTY-04 sweep for Phase 11: remove the last hardcoded indigo utility in the sidebar subtree (the resize handle's `hover:bg-indigo-200` in `src/components/layout/sidebar-resize-handle.tsx:84`), verify (do NOT redo) that the panel's `border-r` is governed by `border-sidebar-border` via the Phase 10 companion rule, and run the full phase regression battery (sweep grep gates, `npm test`, `npm run build`, diff scope fence) that proves NAV-01..04 landed without collateral damage.

Purpose: This is the closing plan of Phase 11 (Wave 2, depends on 11-01). It depends on 11-01 because its sweep gate scans the whole `src/components/layout/` directory, which only reaches zero indigo/amber matches after 11-01 has committed the app-sidebar.tsx restyle. The `border-r` half of SC #5 was already shipped by Phase 10's `[data-slot="sidebar-container"]` companion rule (globals.css:100-103) — this plan verifies it in place and deliberately does not touch it.

Hard constraints honored: zero new npm packages; zero edits to vendored `src/components/ui/sidebar.tsx`; zero edits to `src/app/globals.css`; `@theme inline` untouched; no `.dark`/`dark:` variants. The only file modified is `src/components/layout/sidebar-resize-handle.tsx`.

Output: a fully swept sidebar subtree (grep gates = 0 across `src/components/layout/`), the hairline mechanism verified, and the phase's full regression battery green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md (§Phase 11 — success criterion 5: indigo/amber sweep complete, border-r uses border-sidebar-border "already done in Phase 10 companion rule — verify, don't redo")
@.planning/REQUIREMENTS.md (QLTY-04)
@.planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md (§Token Contract companion rules — the border-r mechanism; §D4 portal policy)
@.planning/phases/10-sidebar-token-foundation/10-01-SUMMARY.md (Phase 10 companion rule shipped: globals.css:100-103)
@.planning/phases/10-sidebar-token-foundation/10-RESEARCH.md (§Common Pitfalls Pitfall 2 — unlayered companion rules beat @layer base; §Security Domain)
@.planning/phases/11-nav-items-restyle/11-VALIDATION.md (per-task map rows 11-02-01..02; sampling rate — npm test + npm run build per wave)
@.planning/phases/11-nav-items-restyle/11-01-PLAN.md (Wave-1 prerequisite — its Task 3 acceptance criteria: indigo/amber = 0 in app-sidebar.tsx)
@src/components/layout/sidebar-resize-handle.tsx (the file being modified — line 84 has the last layout/ indigo utility)
@src/components/ui/sidebar.tsx (vendored — READ ONLY; line 236 `group-data-[side=left]:border-r` with no color class — verified, not edited)
@src/app/globals.css (READ ONLY; lines 100-103 the `[data-slot="sidebar-container"]` companion rule — verified in place)
@src/components/layout/app-sidebar.tsx (READ ONLY — 11-01's committed restyle; the sweep gate scans it)
@package.json (scripts: `test` = `vitest run`, `build` = `next build`)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the resize handle's indigo hover with a token-derived hover + verify the border-r companion rule in place (QLTY-04)</name>
  <files>src/components/layout/sidebar-resize-handle.tsx</files>
  <read_first>
    - src/components/layout/sidebar-resize-handle.tsx — the file being modified. Line 84: the drag-handle div's className ends with `hover:bg-indigo-200`; the surrounding comments (lines 10-15, 73-78) explain the flex-sibling layout contract (the handle is a plain flex-item sibling of `<Sidebar>`, NOT a descendant of the sidebar subtree).
    - .planning/phases/10-sidebar-token-foundation/10-UI-SPEC.md §Token Contract — the scoped block is on `[data-sidebar="sidebar"]`; custom-property inheritance means tokens resolve only for descendants of that hook. The resize handle is a sibling, so `var(--sidebar-accent)` on it would resolve the `:root` oklch fallback (near-white — an invisible hover). This is why the replacement uses the GLOBAL foreground token, not a sidebar token.
    - src/app/globals.css lines 98-106 — the Phase 10 companion rules. Line 100-103 `[data-slot="sidebar-container"] { border-color: var(--sidebar-border); border-right-width: 0.5px; }` — this is the `border-r` → `border-sidebar-border` mechanism. VERIFY ONLY — do not edit.
    - src/components/ui/sidebar.tsx line 236 — `...group-data-[side=left]:border-r group-data-[side=right]:border-l` — the `border-r` with NO color class; the companion rule supplies the color. VERIFY ONLY — do not edit.
  </read_first>
  <action>
    Edit `src/components/layout/sidebar-resize-handle.tsx` line 84: replace `hover:bg-indigo-200` in the drag-handle div's className with `hover:bg-foreground/10`. The full resulting className is `hidden w-1 shrink-0 cursor-col-resize touch-none bg-transparent hover:bg-foreground/10 md:block` — every other utility unchanged.

    Add a 2-4 line why-comment directly above (or inline at the end of) the className explaining: the handle is a flex sibling of the sidebar, outside the `[data-sidebar="sidebar"]` subtree, so the scoped sidebar tokens do not resolve here — `hover:bg-foreground/10` derives a neutral light-gray hover from the global foreground token at 10% opacity instead of a hardcoded palette color (QLTY-04 sweep). CRITICAL GREP HYGIENE: the new comment must not contain the substrings `indigo` or `amber` (the sweep gate greps the whole `src/components/layout/` directory) — describe the old state as "the v1.1 colored hover" without those words.

    Then run the VERIFY-ONLY gates for the `border-r` half of SC #5 (no file changes): (a) `src/app/globals.css` still contains the `border-color: var(--sidebar-border)` companion rule, and (b) the `border-r` on `sidebar.tsx:236` carries no border color utility of its own (so the companion rule governs it). Do NOT edit `globals.css` or `sidebar.tsx` under any circumstance.
  </action>
  <verify>
    <automated>
      npx tsc --noEmit
      grep -c 'hover:bg-foreground/10' src/components/layout/sidebar-resize-handle.tsx
      # expect 1
      grep -c 'indigo' src/components/layout/sidebar-resize-handle.tsx
      # expect 0
      grep -c 'border-color: var(--sidebar-border)' src/app/globals.css
      # expect 1 — companion rule verified in place
      grep 'border-r' src/components/ui/sidebar.tsx | grep -c 'border-sidebar-border'
      # expect 0 — the border-r on line 236 has no color class; the companion rule supplies it
    </automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `hover:bg-foreground/10` appears exactly 1 time in sidebar-resize-handle.tsx; `indigo` appears 0 times — the last hardcoded indigo utility in the sidebar subtree is replaced with a token-derived hover.
    - `border-color: var(--sidebar-border)` appears exactly 1 time in globals.css — the Phase 10 companion rule is in place (verified, not redone).
    - The `border-r` on sidebar.tsx:236 carries no `border-sidebar-border` color class (grep count 0) — the companion rule is what governs the hairline color, exactly per SC #5's "verify, don't redo".
    - `git diff --stat` shows only `src/components/layout/sidebar-resize-handle.tsx` from this task; `globals.css` and `sidebar.tsx` have empty diffs.
  </acceptance_criteria>
  <done>Resize-handle hover is `hover:bg-foreground/10` (no hardcoded color); the border-r → border-sidebar-border companion rule is verified in place; globals.css and sidebar.tsx untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Full QLTY-04 sweep gates + phase regression battery (npm test, npm run build, diff fence)</name>
  <files>src/components/layout/sidebar-resize-handle.tsx</files>
  <read_first>
    - .planning/phases/11-nav-items-restyle/11-VALIDATION.md — sampling rate (full suite + build per wave), per-task map rows 11-02-01 (sweep gate) and 11-02-02 (NAV-01..04 + QLTY-01 regression).
    - .planning/phases/11-nav-items-restyle/11-01-PLAN.md — Task 3's acceptance criteria confirm app-sidebar.tsx is indigo/amber-free; this task's sweep gate re-scans the whole directory to prove the phase-level SC #5.
    - package.json — scripts (`test`: `vitest run`; `build`: `next build`).
    - src/components/layout/ directory listing — the files in scope of the sweep gate: app-sidebar.tsx (11-01), app-shell-layout.tsx, sidebar-resize-handle.tsx.
  </read_first>
  <action>
    Run the phase-closing regression battery. No production code changes in this task — it is a verification-only task:

    1. Sweep gate: `grep -rn "indigo\|amber" src/components/layout/` must print NOTHING (all three files clean: app-sidebar.tsx from 11-01, sidebar-resize-handle.tsx from Task 1, app-shell-layout.tsx which never had any). If any line matches, fix the offending line in the file it came from (keeping the comment-hygiene rule: do not introduce the words into comments) and re-run until zero matches.
    2. Full suite: `npm test` must exit 0 (baseline: 23 files, 224 passed / 2 skipped from Phase 10 — the restyle may not drop a single existing test).
    3. Targeted regression: `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 with 11/11 — the QLTY-01 lock on the nav restyle.
    4. Build: `npm run build` must exit 0 (SSR build type-checks the restyled app-sidebar.tsx under Next).
    5. Diff scope fence: `git diff --stat` for this plan shows exactly `src/components/layout/sidebar-resize-handle.tsx`; `git diff src/components/ui/sidebar.tsx src/app/globals.css package.json package-lock.json src/lib/nav.ts src/lib/nav.test.ts` must be empty — vendored primitive, token block, nav contract, and dependency manifest all byte-identical.
  </action>
  <verify>
    <automated>
      test -z "$(grep -rn "indigo\|amber" src/components/layout/)" && echo "sweep-clean"
      npm test
      npx vitest run src/lib/nav.test.ts --bail=1
      npm run build
      test -z "$(git diff src/components/ui/sidebar.tsx src/app/globals.css package.json package-lock.json src/lib/nav.ts src/lib/nav.test.ts)" && echo "fence-clean"
      git diff --stat
    </automated>
  </verify>
  <acceptance_criteria>
    - The shell test prints `sweep-clean` — zero indigo/amber matches across `src/components/layout/` (QLTY-04: the indigo/amber class sweep is complete; roadmap SC #5).
    - `npm test` exits 0 — full suite green (224 passed baseline not regressed).
    - `npx vitest run src/lib/nav.test.ts --bail=1` exits 0 with 11/11 — the `/companies/[id]` highlight and `/companies-archive` guard hold (QLTY-01 exercised through the phase).
    - `npm run build` exits 0.
    - The shell test prints `fence-clean` — vendored `sidebar.tsx`, `globals.css` (token block + companion rules), `package.json`/`package-lock.json`, `src/lib/nav.ts`, `src/lib/nav.test.ts` all byte-identical; `git diff --stat` shows only `src/components/layout/sidebar-resize-handle.tsx` for this plan.
  </acceptance_criteria>
  <done>Sweep gate clean (indigo/amber = 0 across src/components/layout/); full suite + targeted nav suite + build all green; scope fence verified — the phase changed exactly app-sidebar.tsx (11-01) and sidebar-resize-handle.tsx (11-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resize-handle className → Tailwind build | Static literal class strings; the sweep enforces no hardcoded palette utilities survive in the sidebar subtree |
| `sidebar.tsx` `border-r` → globals.css companion rule | The hairline color depends on the Phase 10 `[data-slot="sidebar-container"]` rule winning the cascade; a regression (rule removal or `@layer` wrap) silently reverts the hairline to the global light `--border` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Integrity | hardcoded indigo/amber utilities in `src/components/layout/` | mitigate | Task 1 replaces the resize handle's `hover:bg-indigo-200` with the token-derived `hover:bg-foreground/10` (global foreground token at 10% — the handle sits OUTSIDE the `[data-sidebar="sidebar"]` subtree so scoped sidebar tokens do not resolve there). Task 2's `sweep-clean` gate asserts zero `indigo\|amber` matches across the whole directory, including 11-01's app-sidebar.tsx. Comment-hygiene rule prevents the words from re-entering via prose. |
| T-11-05 | Integrity | `border-r` hairline color governance | mitigate | Verified, not re-implemented: the Phase 10 companion rule `[data-slot="sidebar-container"] { border-color: var(--sidebar-border) }` (globals.css:100-103) is confirmed present (grep = 1) and the `border-r` at `sidebar.tsx:236` carries no color class (grep = 0). The fence-clean gate asserts globals.css and sidebar.tsx are byte-identical — zero drift on the SC #5 mechanism. |
| T-11-SC | Tampering | npm/pip/cargo installs | N/A | Zero packages installed this phase (hard constraint, PANE-02 exemption). `package.json`/`package-lock.json` diff asserted empty by the fence-clean gate. Any executor-initiated install requires a blocking human checkpoint. |
</threat_model>

<verification>
Phase-level checks after Wave 2 (11-02) completes:
- `test -z "$(grep -rn "indigo\|amber" src/components/layout/)"` → `sweep-clean` (roadmap SC #5, QLTY-04)
- `border-color: var(--sidebar-border)` present in globals.css (1) and `border-r` on sidebar.tsx:236 color-class-free (0) — hairline governed by `border-sidebar-border`, verified not redone
- `npm test` green; `npx vitest run src/lib/nav.test.ts --bail=1` green (11/11); `npm run build` green; `npx tsc --noEmit` green (run per task)
- `git diff --stat` across the whole phase shows exactly two files: `src/components/layout/app-sidebar.tsx` (11-01) and `src/components/layout/sidebar-resize-handle.tsx` (11-02)
- Vendored `sidebar.tsx`, `globals.css`, `src/lib/nav.ts`, `src/lib/nav.test.ts`, `package.json`, `package-lock.json` byte-identical across the phase
</verification>

<success_criteria>
Measurable completion (roadmap SC #5, QLTY-04):
- Zero hardcoded indigo/amber utilities remain in the sidebar subtree — `grep -rn "indigo\|amber" src/components/layout/` prints nothing (the v1.1 indigo-50/indigo-600 active treatment and amber badge from 11-01, plus the resize-handle indigo hover, are all gone)
- The panel's `border-r` is governed by `border-sidebar-border` via the Phase 10 companion rule — verified in place, zero rework (SC #5 "verify, don't redo")
- Full regression battery green: `npm test`, `npx vitest run src/lib/nav.test.ts --bail=1` (11/11), `npm run build`, `npx tsc --noEmit`
- Scope fence: the phase's total diff is exactly `src/components/layout/app-sidebar.tsx` + `src/components/layout/sidebar-resize-handle.tsx`; zero new packages, zero vendored edits, zero globals.css edits, `@theme inline` untouched
</success_criteria>

<output>
Create `.planning/phases/11-nav-items-restyle/11-02-SUMMARY.md` when done
</output>
