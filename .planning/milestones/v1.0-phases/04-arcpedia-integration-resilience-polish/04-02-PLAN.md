---
phase: 04-arcpedia-integration-resilience-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .env.local
autonomous: false
requirements: [ARCP-01, EXPL-06]

must_haves:
  truths:
    - "A working Cloudflare Access Service Token for arcpedia.arclumen.de exists in both the local (.env.local) and deployed (Vercel) environments"
    - "At least one Company and one Persona 360 view demonstrably show real Arcpedia articles end-to-end in a live/dev environment"
    - "Forcing a DB failure on /companies/[id] or /personas/[id] shows the 'Couldn't load company/persona' card, not a framework 500"
    - "No write/mutating call has ever been issued to Arcpedia (re-confirmed via grep as part of this closure pass)"
  artifacts:
    - path: ".env.local"
      provides: "Working ARCPEDIA_ACCESS_CLIENT_ID / ARCPEDIA_ACCESS_CLIENT_SECRET values"
      contains: "ARCPEDIA_ACCESS_CLIENT_ID"
  key_links:
    - from: ".env.local / Vercel env vars"
      to: "src/lib/arcpedia.ts fetchArcpediaArticles"
      via: "CF-Access-Client-Id / CF-Access-Client-Secret request headers"
      pattern: "CF-Access-Client-Id"
---

<objective>
Provision the Cloudflare Access Service Token that Plan 01's code depends on to actually reach Arcpedia in a live environment (per 04-RESEARCH.md's Critical Finding: `arcpedia.arclumen.de` sits behind a Cloudflare Zero Trust Access application at the edge, invisible to arcpedia's own "public GET routes" application-level docs), then run the phase-closing human verification pass confirming ARCP-01, ARCP-02, and EXPL-06 all hold end-to-end, not just in code review.

Purpose: Plan 01 is "correctly implemented" but cannot be demonstrated "working end-to-end" without this token — this plan closes that specific, infra-only gap and performs the one-time verification pass that turns Milestone 1's last two open requirements from "implemented" into "verified."
Output: A working Cloudflare Access Service Token wired into `.env.local` and Vercel's project environment variables; a completed manual verification pass across both 360 views and both list panes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-arcpedia-integration-resilience-polish/04-CONTEXT.md
@.planning/phases/04-arcpedia-integration-resilience-polish/04-RESEARCH.md
@src/lib/env.ts
@.env.local
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision Cloudflare Access Service Token for arcpedia.arclumen.de</name>
  <files>.env.local</files>
  <read_first>
    .env.local (current state — post-Plan-01, still missing the 3 Arcpedia vars)
    .planning/phases/04-arcpedia-integration-resilience-polish/04-RESEARCH.md "Critical Finding" (Summary section) and "Pitfall 1" and "Open Question 1" and Assumption A1
  </read_first>
  <what-built>
    04-RESEARCH.md confirmed this session (via direct `curl` against 4 separate `arcpedia.arclumen.de` paths, all returning `302`/HTML redirects to a `mkonovalov.cloudflareaccess.com` Zero Trust login page) that the production Arcpedia deployment is gated by a Cloudflare Zero Trust Access application at the edge — a layer entirely outside arcpedia's own repo/application code. There is no Cloudflare API token or `wrangler`/`cloudflared` CLI credential available in this session to provision a Service Token non-interactively; Cloudflare Zero Trust Service Token creation requires the account owner's dashboard access.
  </what-built>
  <action>
    Human action required (no CLI/API path available in this session):
    1. Open the Cloudflare Zero Trust dashboard (https://one.dash.cloudflare.com/) for the account that owns `arcpedia.arclumen.de` -> Access -> Service Auth -> Service Tokens -> Create Service Token. Name it identifiably, e.g. `arclumen-360-server`.
    2. Confirm the existing Access policy protecting the `arcpedia.arclumen.de` application allows this new Service Token to authenticate (per RESEARCH.md Assumption A1 — if the policy is currently scoped only to the existing `task-consumer` worker's token, add the new token to that policy's allowed service-auth list; do not widen the policy beyond what's needed for this one new token).
    3. Copy the generated Client ID and Client Secret — treat the secret as sensitive, do not paste it into any chat, issue, or commit message.
    4. Add 3 lines to `.env.local`: `ARCPEDIA_BASE_URL=https://arcpedia.arclumen.de`, `ARCPEDIA_ACCESS_CLIENT_ID=<client id>`, `ARCPEDIA_ACCESS_CLIENT_SECRET=<client secret>`.
    5. Add the same 3 vars to the Vercel project's environment variables (Vercel Dashboard -> project `360-arclumen` -> Settings -> Environment Variables) for at least Production, so the deployed app can also reach Arcpedia.
    6. Sanity-check the token works before moving to Task 2: `curl -s -o /dev/null -w "%{http_code}\n" -H "CF-Access-Client-Id: <client id>" -H "CF-Access-Client-Secret: <client secret>" "https://arcpedia.arclumen.de/api/wiki/search?q=test"` should print `200`, not a redirect/HTML status.
    Resume signal: reply "provisioned" once `.env.local` and Vercel's env vars both have working Arcpedia values and the curl sanity check above returns `200`.
  </action>
  <acceptance_criteria>
    - `.env.local` contains a non-empty `ARCPEDIA_ACCESS_CLIENT_ID=` line and a non-empty `ARCPEDIA_ACCESS_CLIENT_SECRET=` line.
    - `npx vercel env ls` (or the Vercel dashboard) lists `ARCPEDIA_ACCESS_CLIENT_ID` and `ARCPEDIA_ACCESS_CLIENT_SECRET` for at least the Production environment.
    - A direct `curl` against `https://arcpedia.arclumen.de/api/wiki/search?q=test` with the two `CF-Access-Client-*` headers set returns HTTP `200` with a JSON body (`{"results": [...]}`), not an HTML login page — proves the token actually authenticates, not just that it exists in `.env.local`.
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this is a human-action checkpoint (Cloudflare Zero Trust Service Token provisioning has no non-interactive CLI/API path available in this session); Task 2's live UAT pass is the first end-to-end confirmation that the token actually works inside the app</automated>
  </verify>
  <done>.env.local and Vercel's project env vars both have working ARCPEDIA_BASE_URL/ARCPEDIA_ACCESS_CLIENT_ID/ARCPEDIA_ACCESS_CLIENT_SECRET values; a direct curl with those headers returns real JSON from Arcpedia, not a Cloudflare Access login redirect.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end verification — Arcpedia integration + EXPL-06 resilience across both explorers</name>
  <files>none (verification only, no source files modified)</files>
  <read_first>
    .planning/phases/04-arcpedia-integration-resilience-polish/04-VALIDATION.md (Manual-Only Verifications table — the exact 5 behaviors this task must confirm)
  </read_first>
  <what-built>
    Plan 01 shipped the Related Knowledge section (Company + Persona detail) and the DB-fetch error-card resilience pattern; Task 1 of this plan provisioned the Cloudflare Access Service Token those sections need to reach Arcpedia in a live environment. This task confirms the two together actually work, and confirms EXPL-06's resilience behavior holds across every list/detail surface — not just in code review.
  </what-built>
  <action>
    Walk through this live verification protocol and confirm each step:
    1. Run `npm run dev` (or use the current Vercel deployment) with the Task 1 Arcpedia env vars loaded.
    2. Sign in, open a Company 360 view for a company likely to have related Arcpedia content (search Arcpedia's own UI directly first, signed in, to confirm a real match exists for a name in this seed dataset) — confirm the "Related Knowledge" section renders up to 3 articles with title + snippet, and each title link opens `arcpedia.arclumen.de/wiki/<slug>` in a new tab.
    3. Open a Persona 360 view for a persona with no matching Arcpedia content — confirm the "Related Knowledge" section is fully absent (no heading, no "no articles" box).
    4. Temporarily point `DATABASE_URL` at an invalid host (or otherwise force a DB failure), then reload `/companies/[id]` and `/personas/[id]` — confirm each shows "Couldn't load company"/"Couldn't load persona" respectively, not a framework 500 page. Restore the real `DATABASE_URL` in `.env.local` immediately afterward.
    5. Reload `/companies` and `/personas` (both list and detail) — confirm the existing route-level loading skeleton (`loading.tsx`) still displays correctly during the fetch (no regression from Plan 01's changes).
    6. Run `grep -rn "method:" src/lib/arcpedia.ts` — confirm it returns nothing, re-confirming ARCP-02 (no mutating HTTP method ever specified) as a closing check.
  </action>
  <acceptance_criteria>
    - Step 2 confirms real Arcpedia articles render for at least one Company or Persona (title + snippet + working new-tab link).
    - Step 3 confirms the section is fully absent, not an empty placeholder, when there is no match.
    - Step 4 confirms both detail-pane error cards render correctly, and `DATABASE_URL` is restored afterward.
    - Step 5 confirms no loading-state regression.
    - Step 6's grep returns zero matches.
  </acceptance_criteria>
  <resume-signal>Type "approved" once all six checks pass, or describe which one failed and its exact symptom.</resume-signal>
  <verify>
    <automated>MISSING — this is an end-to-end human-verify checkpoint combining a live external service (Arcpedia) and a forced-failure test; 04-VALIDATION.md confirms no test framework is installed this phase (consistent with Phase 1-3 precedent)</automated>
  </verify>
  <done>All six manual checks pass; ARCP-01, ARCP-02, and EXPL-06 are verified end-to-end in a live/deployed environment, closing Milestone 1's last two open requirements.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Cloudflare Zero Trust dashboard (external, human-operated) → `.env.local` / Vercel encrypted env var store | The Service Token secret crosses from an external admin UI into this app's two environment-variable stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | Cloudflare Access Service Token (Client ID/Secret) handling during manual provisioning | mitigate | Token is pasted only into `.env.local` (gitignored, never committed) and the Vercel dashboard's encrypted env var store — never `.env.example` (placeholder-only), never a chat message, never a commit message |
| T-04-08 | Elevation of Privilege | Overly broad Cloudflare Access policy scope for the new Service Token | mitigate | Task 1's action step explicitly scopes the new token to only the `arcpedia.arclumen.de` Access application's existing policy, not an account-wide grant |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this plan — pure configuration + manual verification |

</threat_model>

<verification>
- Task 1's curl sanity check returns `200` with a JSON body
- Task 2's 6-point manual checklist passes in full
- `grep -c "method:" src/lib/arcpedia.ts` is 0 (re-confirmed)
</verification>

<success_criteria>
ARCP-01 is verified working end-to-end (not just implemented) in at least one live Company and one live Persona 360 view; EXPL-06's detail-pane error/loading behavior is confirmed with no regressions; ARCP-02's read-only contract is re-confirmed. Milestone 1's Phase 4 requirements (ARCP-01, ARCP-02, EXPL-06) are fully closed.
</success_criteria>

<output>
Create `.planning/phases/04-arcpedia-integration-resilience-polish/04-02-SUMMARY.md` when done
</output>
