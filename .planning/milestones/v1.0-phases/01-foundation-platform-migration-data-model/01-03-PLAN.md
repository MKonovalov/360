---
phase: 01-foundation-platform-migration-data-model
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/lib/validation/seed.ts
  - src/lib/db/queries/companies.ts
  - src/lib/db/queries/personas.ts
  - src/lib/db/queries/signals.ts
  - src/lib/db/queries/companyPersonaRoles.ts
  - src/scripts/seed.ts
  - data/seed/companies.csv
  - data/seed/personas.csv
  - data/seed/signals.csv
  - data/seed/company_persona_roles.csv
  - package.json
autonomous: true
requirements: [DATA-02, DATA-03, FOUND-02]

must_haves:
  truths:
    - "A CSV seed file can be loaded end-to-end into Neon via a typed insert script"
    - "Invalid CSV rows are rejected with a clear error before reaching the database"
  artifacts:
    - path: "src/scripts/seed.ts"
      provides: "CSV -> typed insert loader (D-04)"
    - path: "src/lib/validation/seed.ts"
      provides: "Zod row schemas mirroring the Drizzle schema, with formula-injection guards"
    - path: "data/seed/companies.csv"
      provides: "Seed template + 2 placeholder rows, header matches schema.ts column names"
  key_links:
    - from: "src/scripts/seed.ts"
      to: "src/lib/db/queries/*.ts"
      via: "typed insert calls (insertSignal, insertCompanyPersonaRole)"
      pattern: "insertSignal|insertCompanyPersonaRole"
---

<objective>
Build the CSV-to-Postgres seed pipeline (D-04): typed query functions per domain, zod row validation with formula-injection guards, and a `tsx`-run script that loads `data/seed/*.csv` into the tables 01-02 created. Ship placeholder sample CSVs so the pipeline is provably working end-to-end before the user's real dataset (D-01/D-03) is handed over during execution.

Purpose: Completes the "real DB write" half of the Phase 1 walking skeleton and is 01-VALIDATION.md's primary acceptance signal for DATA-02/DATA-03.
Output: A working `npm run seed` command, typed query functions other plans (01-04, Phase 2+) import instead of touching Drizzle tables directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-platform-migration-data-model/01-CONTEXT.md
@.planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md
@.planning/phases/01-foundation-platform-migration-data-model/01-VALIDATION.md
@src/lib/db/schema.ts
@src/lib/db/index.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSV seed templates + zod row-validation schemas with formula-injection guards</name>
  <files>src/lib/validation/seed.ts, data/seed/companies.csv, data/seed/personas.csv, data/seed/signals.csv, data/seed/company_persona_roles.csv</files>
  <read_first>
    src/lib/db/schema.ts, .planning/phases/01-foundation-platform-migration-data-model/01-CONTEXT.md (D-01 through D-04, Claude's Discretion — CSV column layout), .planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md (Don't Hand-Roll: CSV parsing / env validation; Security Domain: CSV injection mitigation)
  </read_first>
  <action>
    Create four CSV templates under `data/seed/` with header rows matching `src/lib/db/schema.ts` column names (Claude's Discretion per 01-CONTEXT.md, proposed for the user to fill in once D-03's real handoff happens): `data/seed/companies.csv` header `name,industry`; `data/seed/personas.csv` header `name,title`; `data/seed/signals.csv` header `company_name,signal_type,strength,source,detected_at,note` (signals reference companies by `company_name` text match, not a raw DB id, since the CSV author won't know generated serial ids — `seed.ts` resolves `company_name` -> `company.id` at insert time); `data/seed/company_persona_roles.csv` header `company_name,persona_name,title,is_current,start_date,end_date`. Populate each with exactly 2 rows of clearly-fake placeholder data (e.g. company name `Acme Test Co`, `Beta Sample Inc`) so the pipeline has something to run against before D-01's real data arrives — do not fabricate real target-company data. Per D-02 (target volume is small, roughly 5-15 companies with linked personas), `seed.ts` (Task 2) reads each CSV synchronously with `csv-parse/sync` and holds all rows in memory — no streaming/batching/chunked-insert support is needed at this scale, so do not over-engineer the parser for a dataset size this phase will never see. Create `src/lib/validation/seed.ts` exporting `companyRowSchema`, `personaRowSchema`, `signalRowSchema`, `companyPersonaRoleRowSchema` (zod objects) whose shapes mirror the CSV headers above; `signal_type` and `strength` fields must use `z.enum(signalTypeEnum.enumValues)` / `z.enum(signalStrengthEnum.enumValues)` imported from `src/lib/db/schema.ts` (not retyped as literal string arrays, so the two never drift). Per 01-RESEARCH.md's Security Domain CSV-injection mitigation, every string field must be wrapped with a shared `safeCsvString` zod refinement that rejects values starting with `=`, `+`, `-`, `@`, a tab, or a carriage return (formula-injection guard) before any row reaches the database.
  </action>
  <acceptance_criteria>
    - All four CSV files exist under `data/seed/` with the header rows specified above plus exactly 2 data rows each.
    - `src/lib/validation/seed.ts` exports `companyRowSchema`, `personaRowSchema`, `signalRowSchema`, `companyPersonaRoleRowSchema`.
    - `grep -n "signalTypeEnum\|signalStrengthEnum" src/lib/validation/seed.ts` returns at least 2 matches (imported from `schema.ts`, not retyped).
    - `signalRowSchema.safeParse({...validRow, note: '=cmd|calc!A1'}).success` evaluates to `false` (formula-injection guard active).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CSV templates with placeholder data exist matching the schema's column names; zod row schemas validate types, enums, and reject formula-injection strings.</done>
</task>

<task type="auto">
  <name>Task 2: Typed query functions + seed.ts CSV-to-Postgres loader, run against the sample CSVs</name>
  <files>src/lib/db/queries/companies.ts, src/lib/db/queries/personas.ts, src/lib/db/queries/signals.ts, src/lib/db/queries/companyPersonaRoles.ts, src/scripts/seed.ts, package.json</files>
  <read_first>
    src/lib/db/index.ts, src/lib/db/schema.ts, src/lib/validation/seed.ts (Task 1), data/seed/*.csv (Task 1), .planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md (Don't Hand-Roll: csv-parse; Structure Rationale: queries/*.ts never re-export db/tables), CLAUDE.md (Error Handling section — establish a deliberate convention, not the retiring silent-swallow pattern)
  </read_first>
  <action>
    Create `src/lib/db/queries/companies.ts` exporting `listCompanies()` (returns all rows) and `getCompanyByName(name: string)` (used to resolve FK ids during seeding); both typed functions only — never re-export `db` or the `company` table object (01-RESEARCH.md Structure Rationale). Create `src/lib/db/queries/personas.ts` mirroring with `listPersonas()`/`getPersonaByName(name)`. Create `src/lib/db/queries/signals.ts` with `insertSignal(row)` and `listSignalsForCompany(companyId: number)`. Create `src/lib/db/queries/companyPersonaRoles.ts` with `insertCompanyPersonaRole(row)`. Create `src/scripts/seed.ts` (run via `npx tsx src/scripts/seed.ts`; add `"seed": "tsx src/scripts/seed.ts"` to `package.json`'s `scripts`): reads each of the four `data/seed/*.csv` files using `csv-parse/sync`'s `parse(content, { columns: true })`, validates every row against the matching zod schema from `src/lib/validation/seed.ts`, and on any validation failure throws a single aggregated `Error` listing every invalid row index and reason (fail loud — per CLAUDE.md's Error Handling guidance, this establishes the deliberate new convention for the data-access layer rather than carrying forward the retiring Astro app's silent-swallow-and-fallback pattern). On success, insert in dependency order: companies first via `db.insert(company).values(...)` returning ids, building a `name -> id` `Map`; then personas the same way; then signals, resolving `company_name` through the companies map and calling `insertSignal`; then company_persona_roles, resolving both `company_name` and `persona_name` through their maps and calling `insertCompanyPersonaRole`. Log a final summary line to stdout: `Inserted: {n} companies, {n} personas, {n} signals, {n} company_persona_roles`. Run `npm run seed` once against the Task 1 placeholder CSVs to prove the full pipeline works end-to-end.
  </action>
  <acceptance_criteria>
    - `grep -n "^export const db\|^export const company\b\|^export const persona\b\|^export const signal\b" src/lib/db/queries/*.ts` returns no matches (query files never re-export raw `db`/table objects).
    - `npm run seed` exits `0` against the sample CSVs and prints an `Inserted:` line with all four counts >= 1.
    - After running, a direct `db.select().from(company)` returns at least the 2 sample rows inserted.
    - Temporarily editing `data/seed/signals.csv`'s `strength` column to an invalid value (`super-high`) and re-running `npm run seed` causes a non-zero exit with a validation error naming the offending row/column, not an unhandled Postgres exception — then revert the CSV back to valid sample data before finishing.
  </acceptance_criteria>
  <verify>
    <automated>npm run seed</automated>
  </verify>
  <done>Typed query layer exists across `src/lib/db/queries/*.ts`; `npm run seed` successfully loads the sample CSV data into Neon end-to-end; malformed rows are rejected with a descriptive error before any insert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `data/seed/*.csv` (file on disk, eventually user-provided per D-01) -> Postgres | Untrusted external input crosses this boundary at every `npm run seed` run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-1-05 | Tampering | `src/scripts/seed.ts`, `src/lib/validation/seed.ts` | mitigate | `csv-parse` handles quoting/escaping correctly, but every string field is additionally validated against zod's `safeCsvString` refinement (rejects `=`/`+`/`-`/`@`/tab/CR-leading values) before any row reaches the database, per 01-RESEARCH.md's Security Domain CSV-injection mitigation |
| T-1-03 | Tampering | `src/lib/db/queries/*.ts` | mitigate | Continuity from 01-02 — all inserts go through Drizzle's parameterized query builder (`db.insert(...).values(...)`), never raw SQL string concatenation |
| T-1-SC | Tampering | No new package installs this plan | accept | Continuity from 01-01/01-02 — `csv-parse` was already audited `[OK]` in 01-RESEARCH.md's Package Legitimacy Audit |

</threat_model>

<verification>
`npm run seed` exits 0 and inserts the 4 sample rows-per-table into Neon; the malformed-row rejection check in Task 2 passes.
</verification>

<success_criteria>
- CSV -> typed insert pipeline works end-to-end against real Neon Postgres.
- Zod validation rejects malformed/malicious rows before they reach the database.
- Query functions are the only DB access surface — no page or future plan needs to import `src/lib/db/schema.ts` tables directly.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-platform-migration-data-model/01-03-SUMMARY.md` when done
</output>
