---
phase: 01-foundation-platform-migration-data-model
plan: 04
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - src/app/page.tsx
  - src/app/actions.ts
  - src/components/RefreshCompanyCount.tsx
autonomous: true
requirements: [FOUND-01, FOUND-04]

must_haves:
  truths:
    - "The dashboard shows a live company count read from Neon"
    - "A UI button triggers a Server Action that re-reads the live count (Client -> Server Action -> Postgres round trip)"
    - "The app is deployed on the existing Vercel project on Node 22.x with no Astro routes remaining live"
  artifacts:
    - path: "src/app/actions.ts"
      provides: "Gated Server Action (refreshCompanyCount) — proves Server Actions are checked, not just pages"
      contains: "requireStaffAccess"
    - path: "src/components/RefreshCompanyCount.tsx"
      provides: "Client Component wired to the Server Action"
  key_links:
    - from: "src/components/RefreshCompanyCount.tsx"
      to: "src/app/actions.ts refreshCompanyCount"
      via: "onClick handler calling the Server Action"
      pattern: "refreshCompanyCount\\("
    - from: "src/app/actions.ts"
      to: "src/lib/auth/requireStaffAccess.ts"
      via: "gate called before any DB access"
      pattern: "requireStaffAccess\\(\\)"
---

<objective>
Complete the Phase 1 walking skeleton: wire the dashboard's signed-in branch to a real Neon read (`listCompanies()`), add a Client Component -> Server Action -> Postgres round trip (proving Server Actions are gated, not just pages), then deploy to the existing Vercel project and confirm Node 22.x runtime with no dead Astro routes.

Purpose: This is the final wave — it proves the entire stack (auth from 01-01, schema from 01-02, data access from 01-03) works together end-to-end on a deployed URL, which is what "everything after this phase can be built on solid ground" (Phase 1's ROADMAP goal) actually means in practice.
Output: A deployed Next.js app where signed-in staff see a live, refreshable company count.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-platform-migration-data-model/01-CONTEXT.md
@.planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md
@src/app/page.tsx
@src/lib/auth/requireStaffAccess.ts
@src/lib/db/queries/companies.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire dashboard to a live company count + Client Component -> Server Action refresh</name>
  <files>src/app/page.tsx, src/app/actions.ts, src/components/RefreshCompanyCount.tsx</files>
  <read_first>
    src/app/page.tsx (01-01 state), src/lib/auth/requireStaffAccess.ts, src/lib/db/queries/companies.ts, .planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md (Architectural Responsibility Map row "Walking-skeleton UI interaction")
  </read_first>
  <action>
    Extend `src/app/page.tsx`: inside the existing `if (userId)` branch (do NOT move the query outside this conditional — anonymous visitors must never trigger a database query), replace the placeholder text `'Signed in (staff). Dashboard data loads here in 01-04.'` with `await listCompanies()` from `src/lib/db/queries/companies.ts`, rendering `<p>{companies.length} companies loaded.</p>` followed by `<RefreshCompanyCount initialCount={companies.length} />`. Create `src/app/actions.ts` starting with `'use server'` on its own first line, exporting `export async function refreshCompanyCount()`: calls `await requireStaffAccess()` FIRST, before any DB access — this is the walking skeleton's concrete proof that Server Actions are gated independently of page-level checks (T-1-01, per 01-RESEARCH.md's Pitfall 1) — then `return (await listCompanies()).length;`. Create `src/components/RefreshCompanyCount.tsx` starting with `'use client'` on its own first line: accepts `{ initialCount }: { initialCount: number }`, holds `count` via `useState(initialCount)`, imports `refreshCompanyCount` from `../app/actions`, and renders the current count plus a `<button>` whose `onClick` handler is an async function that calls `const next = await refreshCompanyCount(); setCount(next);` (a plain async `onClick` handler is sufficient here — no `useFormState`/form markup needed for a single button). This is the exact Client Component -> Server Action -> Postgres round trip 01-RESEARCH.md's Architectural Responsibility Map specifies for the walking skeleton.
  </action>
  <acceptance_criteria>
    - `src/app/page.tsx`: the `listCompanies()` call appears nested inside the `if (userId)` block (`grep -B3 "listCompanies" src/app/page.tsx` shows the surrounding `if (userId)` line).
    - `src/app/actions.ts`'s first non-empty line is `'use server';` and `requireStaffAccess()` appears in the file before any call to `listCompanies` (byte-order check).
    - `src/components/RefreshCompanyCount.tsx`'s first non-empty line is `'use client';` and it imports `refreshCompanyCount` from a path resolving to `src/app/actions.ts`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <human-check>Sign in at the dev/deployed URL, confirm the dashboard shows a company count matching the seeded data (>=2 from 01-03's sample CSV run) and clicking "Refresh" updates the displayed count via a Server Action round trip without a full page reload.</human-check>
  </verify>
  <done>The dashboard's signed-in branch reads a live company count from Neon; the Refresh button round-trips through a `requireStaffAccess()`-gated Server Action back to the same table.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy to Vercel, confirm Node 22.x runtime and no dead Astro routes</name>
  <files>none (deployment operation, no repo files change)</files>
  <read_first>
    .vercel/project.json, package.json (build script), README.md (existing `vercel deploy --prebuilt --prod` convention), .planning/phases/01-foundation-platform-migration-data-model/01-RESEARCH.md Pitfall 4 (Node version precedence)
  </read_first>
  <action>
    Run `npm run build` locally as a pre-flight check — must exit `0` before deploying. Deploy via `npx vercel --prod` (uses the already-linked `360-arclumen` project per `.vercel/project.json`; do not create a new Vercel project). After deploy completes, note the printed production URL, then run `npx vercel inspect <deployment-url>` and confirm the reported Function runtime is `nodejs22.x`, not `nodejs20.x` — per Pitfall 4, `package.json`'s `"engines": { "node": "22.x" }` should override any stale Vercel dashboard Project Settings, but this must be verified against the actual deployment output, not assumed. Run `curl -s -o /dev/null -w '%{http_code}' <deployment-url>` and confirm it returns `200` (the landing page renders for anonymous visitors too, per `page.tsx`'s intentionally non-gating design). Run `curl -s -o /dev/null -w '%{http_code}' <deployment-url>/l/anything` and confirm it returns `404` (the old Astro dynamic route `src/pages/l/[code].astro` is gone from production, not just deleted locally — confirms D-09 actually shipped).
  </action>
  <acceptance_criteria>
    - `npm run build` exits `0` immediately before deploy.
    - `npx vercel --prod` completes and prints an `https://` deployment URL.
    - `npx vercel inspect <deployment-url>` output (or the Vercel dashboard Functions tab) reports the runtime as `nodejs22.x`.
    - `curl` to the deployed root URL returns HTTP `200`.
    - `curl` to the deployed `/l/anything` returns HTTP `404`.
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npx vercel --prod && npx vercel inspect &lt;deployment-url&gt;</automated>
    <human-check>In the Vercel dashboard (Deployments -> latest -> Functions), confirm the runtime shows Node 22.x, and if the custom domain `360.arclumenpartners.com` is aliased, confirm it also serves the new deployment. Also confirm via browser devtools Network/Sources that no server-only env var value (DATABASE_URL, CLERK_SECRET_KEY) appears in any client-shipped JS chunk.</human-check>
  </verify>
  <done>The migrated Next.js app is deployed to the existing Vercel project on Node 22.x; the old Astro `/l/[code]` route returns 404 in production, confirming the migration and D-09's deletion both shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (Client Component) -> `refreshCompanyCount` Server Action -> Postgres | The first Server Action in this codebase; must independently enforce the staff-access gate, not rely on the page having already checked |
| Deployed Vercel Function -> client-shipped JS bundle | Server-only secrets (`DATABASE_URL`, `CLERK_SECRET_KEY`) must never cross into what the browser downloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-1-01 | Elevation of Privilege | `src/app/actions.ts` `refreshCompanyCount` | mitigate | `requireStaffAccess()` is called first, before any DB access — Task 1's byte-order acceptance criterion enforces this structurally; this is the walking skeleton's proof that the FOUND-04 gate applies to Server Actions, not only pages |
| T-1-04 | Information Disclosure | Deployed Vercel Function env vars vs. client bundle | mitigate | `DATABASE_URL`/`CLERK_SECRET_KEY` are never `NEXT_PUBLIC_`-prefixed (continuity from 01-01/01-02); Task 2's human-check step explicitly verifies via browser devtools that neither value ships in client JS |
| T-1-SC | Tampering | No new package installs this plan | accept | Continuity — no new dependencies introduced in this plan |

</threat_model>

<verification>
`npm run build` passes; the deployed URL serves 200 at `/` and 404 at `/l/anything`; the Vercel dashboard confirms Node 22.x. Manual smoke check (deferred to end-of-phase per `human_verify_mode: end-of-phase`): sign in on the deployed URL, confirm the dashboard shows the seeded company count and the Refresh button works.
</verification>

<success_criteria>
- Signed-in staff see a live company count read from Neon on the dashboard.
- The Refresh button demonstrates a full Client -> Server Action -> Postgres round trip, gated by `requireStaffAccess()`.
- The app is deployed on the existing Vercel project, Node 22.x confirmed, old Astro routes gone from production.
- All six Phase 1 requirements (FOUND-01..04, DATA-02, DATA-03) are satisfied end-to-end.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-platform-migration-data-model/01-04-SUMMARY.md` when done
</output>
