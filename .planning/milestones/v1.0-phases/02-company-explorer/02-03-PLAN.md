---
phase: 02-company-explorer
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/companies/[id]/page.tsx
  - src/components/companies/company-detail.tsx
  - src/components/companies/company-list.tsx
  - src/app/companies/page.tsx
autonomous: true
requirements: [COMP-02, COMP-03, COMP-04, EXPL-04, EXPL-07]

must_haves:
  truths:
    - "Clicking a Company row navigates to /companies/[id] and opens that company's full 360 detail while the list pane stays visible"
    - "The detail pane shows firmographics, tech stack (as badges), buying signals (each with source and last-updated/detected date), and linked Personas inline"
    - "Pasting a copied /companies/[id] URL into a fresh browser tab (no prior client state) restores the exact same selected company"
    - "On narrow viewports, selecting a company replaces the list with the detail pane; a back action returns to the list"
    - "An unauthenticated visitor hitting /companies/[id] directly is redirected to /sign-in, never sees company data"
  artifacts:
    - path: "src/app/companies/[id]/page.tsx"
      provides: "/companies/[id] route: awaits params+searchParams, renders CompanyList (selectedId set) + CompanyDetail"
      contains: "notFound"
    - path: "src/components/companies/company-detail.tsx"
      provides: "Firmographics, tech stack badges, signals (source+date), linked personas — COMP-02/03/04"
      exports: ["CompanyDetail"]
  key_links:
    - from: "src/components/companies/company-list.tsx"
      to: "src/app/companies/[id]/page.tsx"
      via: "Link href=/companies/{id} per row, preserving selectedId highlight state"
      pattern: "href=.*companies/"
    - from: "src/components/companies/company-detail.tsx"
      to: "src/lib/db/queries/companies.ts getCompanyById"
      via: "async Server Component data fetch"
      pattern: "getCompanyById\\("
    - from: "src/components/companies/company-detail.tsx"
      to: "src/lib/db/queries/companyPersonaRoles.ts listPersonasForCompany"
      via: "async Server Component data fetch"
      pattern: "listPersonasForCompany\\("
---

<objective>
Add the `/companies/[id]` route and `CompanyDetail` component so clicking any Company row opens its full 360 detail (firmographics, tech stack, buying signals with source/date, linked Personas) in the master-detail pane while the list stays visible and the URL becomes the shareable source of truth for the selection (D-05, EXPL-04, EXPL-07).

Purpose: Plan 02 proved staff can browse the list; this plan delivers the actual "full 360 detail" promise from the phase goal — the second half of COMP-01..04 and the master-detail pattern Phase 3 will clone.
Output: `/companies/[id]/page.tsx`, `CompanyDetail`, an updated `CompanyList` with per-row links + selected-row highlight + mobile responsive swap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-company-explorer/02-CONTEXT.md
@.planning/phases/02-company-explorer/02-RESEARCH.md
@.planning/phases/02-company-explorer/02-UI-SPEC.md
@.planning/phases/02-company-explorer/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build the Company detail pane and /companies/[id] route</name>
  <files>src/app/companies/[id]/page.tsx, src/components/companies/company-detail.tsx</files>
  <read_first>
    src/app/companies/page.tsx (the two-column grid shape from Plan 02 — this route mirrors it, swapping the empty-detail placeholder for a real CompanyDetail)
    src/lib/db/queries/companies.ts (getCompanyById(id) — returns undefined if not found, never throws, per Plan 02-01)
    src/lib/db/queries/signals.ts (listSignalsForCompany(companyId) — already exists, reuse as-is for COMP-03)
    src/lib/db/queries/companyPersonaRoles.ts (listPersonasForCompany(companyId) — added in Plan 02-01, for COMP-04)
    src/components/companies/signal-badge.tsx (reuse for the tech-stack section's badge styling and the signals section)
    .planning/phases/02-company-explorer/02-RESEARCH.md "Architecture Patterns > Pattern 1" (params/searchParams are Promises — Pitfall 3) and "Open Questions #1" (use notFound() for a nonexistent id)
    .planning/phases/02-company-explorer/02-UI-SPEC.md "Typography" (Display 24/600 for the company name header, Heading 18/600 for section headers: Firmographics/Buying Signals/Tech Stack/Linked Personas) and "Spacing Scale" (2xl/48px between detail-pane sections)
  </read_first>
  <action>
    Create `src/components/companies/company-detail.tsx`: an `async` named-export `CompanyDetail({ id }: { id: number })` Server Component. Call `getCompanyById(id)`; if `undefined`, call Next's `notFound()` (renders the App Router's 404 boundary — RESEARCH.md's Open Question #1 recommendation, distinct from the UI-SPEC's "fetch failure" error copy). Otherwise render, in this order with 48px (`gap-12`/`space-y-12`) section spacing: (1) company name as the Display-role header (24px/600), industry as a subtitle; (2) "Firmographics" (Heading 18/600) section listing employeeCountBand, hqLocation, revenueBand, ownershipType as labeled fields (Label role 12px/400 for field labels); (3) "Tech Stack" section rendering each `techStack` array entry as a `Badge` with `variant="outline"` (a visually distinct style from `SignalBadge`'s amber "attention" badges — tech stack items are neutral inventory, not buying signals); (4) "Buying Signals" section calling `listSignalsForCompany(id)`, rendering each signal as a `SignalBadge` plus its `source` and `detectedAt` displayed via `Intl.DateTimeFormat` (no `date-fns` dependency needed at this data volume, per RESEARCH.md's Standard Stack note) — each signal's source/date must be visible next to its badge, not hidden; (5) "Linked Personas" section calling `listPersonasForCompany(id)`, rendering each persona's name + role title inline (no Persona detail route exists yet — Phase 3 — so render as plain text/row, not a link).
    Create `src/app/companies/[id]/page.tsx`: an `async` default-export `CompanyDetailPage({ params, searchParams })` typed with `params: Promise<{ id: string }>` and `searchParams: Promise<{ [key: string]: string | string[] | undefined }>` (both Promises — Pitfall 3). Call `await requireStaffAccess()` first. `await params` and `await searchParams`; parse `id` with `Number(id)` and call `notFound()` if `Number.isNaN`. Render the same two-column grid shape as `companies/page.tsx`, passing `selectedId={companyId}` to `CompanyList` (list stays visible per EXPL-04) and `<CompanyDetail id={companyId} />` in the right column.
  </action>
  <acceptance_criteria>
    - `grep -c "notFound" src/components/companies/company-detail.tsx` is at least 1
    - `grep -c "requireStaffAccess" src/app/companies/[id]/page.tsx` is 1
    - `grep -c "await params\|await searchParams" src/app/companies/[id]/page.tsx` is 2
    - `grep -c "listSignalsForCompany\|listPersonasForCompany\|getCompanyById" src/components/companies/company-detail.tsx` is 3
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>CompanyDetail renders firmographics, tech stack badges, buying signals with source+date, and linked personas for a valid id, and calls notFound() for an invalid/nonexistent one; the [id] route gates access and awaits both Promise-based props correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire list-to-detail navigation, selected-row highlight, and mobile responsive swap</name>
  <files>src/components/companies/company-list.tsx, src/app/companies/page.tsx</files>
  <read_first>
    src/components/companies/company-list.tsx (current Plan 02 version — Table with no row links yet)
    src/app/companies/page.tsx (current Plan 02 version — passes filters={undefined} to CompanyList with no selectedId concept yet)
    .planning/phases/02-company-explorer/02-RESEARCH.md "Mobile/Narrow-Viewport Behavior (D-07)" and its Anti-Pattern note against `router.back()`
    .planning/phases/02-company-explorer/02-UI-SPEC.md "Color" (indigo-600 selected-row indicator)
  </read_first>
  <action>
    Modify `src/components/companies/company-list.tsx`: change the exported `CompanyList` signature to accept an optional `selectedId?: number` alongside `filters`. Wrap each table row's primary cell in a `<Link href={\`/companies/${company.id}\`}>` (plain Next.js `Link`, not a client-side selection handler — D-05 locks route-param navigation). Apply an indigo-600 left-border/background highlight class to the row whose `company.id === selectedId`. Add the mobile responsive wrapper classes from RESEARCH.md's D-07 pattern: the list's outer wrapper gets `className={selectedId ? 'hidden md:block' : 'block'}` so a selected company hides the list on narrow viewports.
    Modify `src/app/companies/page.tsx`: pass `selectedId={undefined}` explicitly to `CompanyList` (list-only view, no detail selected) and wrap the right-hand "Select a company to view details" placeholder pane in the complementary responsive class `className={'hidden md:block'}` so on mobile, with no selection, only the list shows (not an empty detail pane). In `src/app/companies/[id]/page.tsx` (already created in Task 1 — read it, do not recreate), the equivalent detail-pane wrapper needs `className={'block'}` (always visible when a company is selected) — apply that adjustment there too as part of this task's mobile-responsive pass. Add a "back to list" affordance inside `CompanyDetail`'s or the `[id]/page.tsx`'s mobile-only view: a plain `<Link href="/companies">Back to list</Link>` (never `router.back()` — RESEARCH.md's Anti-Pattern note: `router.back()` breaks the deep-link/shared-URL case since a visitor may have arrived via a direct link, not in-app navigation).
  </action>
  <acceptance_criteria>
    - `grep -c "Link href" src/components/companies/company-list.tsx` is at least 1
    - `grep -c "selectedId" src/components/companies/company-list.tsx` is at least 2 (prop + highlight usage)
    - `grep -c "hidden md:block" src/components/companies/company-list.tsx` is at least 1
    - `grep -c "router.back" src/app/companies/[id]/page.tsx src/components/companies/company-detail.tsx` is 0 (anti-pattern must not appear)
    - `npx tsc --noEmit` and `npm run build` both exit 0
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <human-check>Sign in, visit /companies, click a company row: confirm the URL becomes /companies/{id}, the list stays visible with the clicked row highlighted, and the detail pane on the right shows firmographics, tech stack badges, buying signals with source+date, and linked personas. Copy that URL, open it in a fresh incognito-then-signed-in tab: confirm the same company opens directly. Narrow the browser window below the md breakpoint with a company selected: confirm the list hides and only the detail pane (with a working "Back to list" link) shows. Visit /companies/999999 (nonexistent id): confirm a 404, not a crash.</human-check>
  </verify>
  <done>Clicking any row opens /companies/{id} with the list still visible and the row highlighted; deep-linked URLs restore the exact same view; mobile viewports swap list/detail correctly with a URL-preserving back link; invalid ids 404 cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous browser → `/companies/[id]` route | Second new authenticated surface this phase — must be gated independently of `/companies`, per Pitfall 4's "easy to miss on the second route" warning |
| URL-supplied `id` route param → `getCompanyById` | User-controlled input reaching a Drizzle query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-2-05 | Elevation of Privilege | `/companies/[id]` route | mitigate | `await requireStaffAccess()` as the first line of `CompanyDetailPage`, in addition to the already-gating `companies/layout.tsx` from Plan 02 — verified via the human-check redirect test in Task 2. |
| T-2-06 | Tampering | `id` route param passed to `getCompanyById` | mitigate | `Number(id)` + `Number.isNaN` guard before any DB call; `getCompanyById` uses Drizzle's parameterized `eq()`, never string-interpolated SQL — a malformed/non-numeric id 404s via `notFound()` rather than reaching the query layer. |
| T-2-04 | Information Disclosure | Sequential integer company IDs now directly navigable via `/companies/{id}` | accept | Carried forward from Plan 02's threat register — accepted risk per `.planning/STATE.md`'s existing "any authenticated user = full access" model, not a Phase 2 fix. |
| T-2-07 | Tampering / Information Disclosure | Rendering `note`/`source` (signal), persona name/title, and `techStack` array entries as page content | accept | React auto-escapes all JSX text content by default; no `dangerouslySetInnerHTML` is introduced anywhere in `company-detail.tsx` — confirmed via the acceptance grep in Task 1/2 code review (no such API used). |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` pass after Task 2
- Human-check: row click → detail pane opens with list visible; URL round-trips on fresh tab; mobile swap + back-link work; nonexistent id 404s; signed-out visitor redirected on both /companies and /companies/[id]
</verification>

<success_criteria>
Staff can click any Company row to open its complete 360 detail (firmographics, tech stack, sourced/dated buying signals, linked personas) in a master-detail pane that stays synced to a shareable, deep-linkable URL, on both desktop and mobile viewports.
</success_criteria>

<output>
Create `.planning/phases/02-company-explorer/02-03-SUMMARY.md` when done
</output>
