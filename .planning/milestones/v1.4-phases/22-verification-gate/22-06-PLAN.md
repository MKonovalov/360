---
phase: 22-verification-gate
plan: 06
type: execute
wave: 2
depends_on: [22-03]
files_modified:
  - e2e/ver-05-settings.spec.ts
autonomous: true
requirements: [VER-05]
user_setup:
  - service: clerk
    why: "VER-05's live-browser spec authenticates through the real Clerk login (storageState from plan 22-03 auth-setup) — the dedicated test staff account"
    env_vars:
      - name: E2E_CLERK_USER_EMAIL
        source: "Provisioned in plan 22-03 Task 3 (Backend API createUser or operator dashboard) — .env.local, gitignored"
    dashboard_config: []
must_haves:
  truths:
    - "The browser spec proves provider-switch draft preservation: switching the AI provider keeps a valid primary, resets to the provider default when invalid, preserves fallbacks, and shows the non-blocking hint (SET-03)"
    - "The browser spec proves picker search + grouping: the OpenRouter picker (336 rows) filters by type-to-filter and groups by provider/family (SET-06)"
    - "The browser spec proves badge disambiguation: same-name models across providers (e.g. claude-sonnet-5 vs anthropic/claude-sonnet-5) render distinct provider badges (SET-05)"
    - "The browser spec proves no ~/:free id is savable-or-served outside its labels: picker rows show the suffix labels and the saved chain never shows a raw ~/:free id without its label (SET-07)"
    - "The spec runs against the dev server with real Clerk auth (webServer auto-start, storageState from plan 22-03) — no cookie stubs (D-22-05)"
    - "The IN-02 review observation (stale-primary badge guess) is observed and recorded in the spec output/summary (21-REVIEW carry)"
  artifacts:
    - path: "e2e/ver-05-settings.spec.ts"
      provides: "Live-browser UAT spec for provider-switch draft preservation, picker search/grouping, badge disambiguation, labels"
      contains: "Saved chain"
  key_links:
    - from: "e2e/ver-05-settings.spec.ts"
      to: "/settings"
      via: "page.goto('/settings') with real Clerk auth"
      pattern: "/settings"
    - from: "e2e/ver-05-settings.spec.ts"
      to: "src/components/settings/model-picker.tsx"
      via: "CommandInput search + provider badge rows"
      pattern: "Search models"
---

<objective>
Deliver VER-05: a live-browser Playwright spec (D-22-04/05) proving the three locked Settings-UI behaviors — provider-switch draft preservation (SET-03), picker search + grouping (SET-06), and provider-badge disambiguation (SET-05) — plus the `~latest`/`:free` label discipline (SET-07: no such id ever savable-or-served outside its labels). Runs against the dev server with the REAL Clerk login (plan 22-03 harness).

Purpose: Phase 21 shipped the Settings UI (22/22 must-haves unit-verified); VER-05 is the designed human-check layer — automated as a reusable Playwright spec per D-22-04, replacing one-off manual UAT. The IN-02 review observation (stale-primary badge guess, 21-REVIEW) is observed here.
Output: `e2e/ver-05-settings.spec.ts`, green under `npx playwright test e2e/ver-05-settings.spec.ts` (targeted spec — the combined full-suite `npm run e2e` run lands at the phase gate in verify-work, once plan 22-05's ver-02 spec exists).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-verification-gate/22-CONTEXT.md (D-22-04 Playwright e2e spec for the three VER-05 behaviors; D-22-05 real Clerk login; Claude's discretion on single-file vs split + dev-server gating)
@.planning/phases/22-verification-gate/22-RESEARCH.md (§VER-05 row l.49 — 4 assertions + IN-02 observation; §Standard Stack — Playwright harness; §Pitfall 5 l.408-412 — workers: 1 serialization)
@.planning/phases/22-verification-gate/22-PATTERNS.md (§ver-05 pattern rules l.242; §model-picker-logic.ts exports l.15-140 — the unit-locked logic the spec observes)
src/components/settings/model-settings-form.tsx (provider SelectTrigger aria-label="AI provider" l.215; Primary model label l.234; Save changes l.356; 'Saved.' l.360; Saved chain recap l.372; reset hint l.169)
src/components/settings/model-picker.tsx (CommandInput placeholder "Search models…" l.111; provider Badge l.87-91; suffixLabel spans; provider groups)
src/components/settings/model-picker-logic.ts (providerName, searchValue, suffixLabel, groupByProvider — the unit-locked seams the spec observes rendered)
src/app/(dashboard)/settings/page.tsx (settings page props: providers, servableByProvider, unionServableModels, defaults, savedChain)
.planning/phases/21-settings-ui/21-REVIEW.md (IN-02 stale-primary badge guess — the observation carry)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the VER-05 live-browser spec (draft preservation, search/grouping, badges, labels)</name>
  <files>e2e/ver-05-settings.spec.ts</files>
  <read_first>
    .planning/phases/22-verification-gate/22-RESEARCH.md §VER-05 row l.49 (the 4 assertions + IN-02 observation) + §Pitfall 5 l.408-412 (workers: 1 — the config already serializes)
    src/components/settings/model-settings-form.tsx (l.215 AI provider SelectTrigger aria-label; l.234 Primary model; l.356 Save changes; l.360 'Saved.'; l.372 Saved chain recap; l.169 reset hint string "Primary model reset to ...")
    src/components/settings/model-picker.tsx (l.111 CommandInput "Search models…"; l.87-91 provider Badge; pinned selection row; suffixLabel labels)
    src/components/settings/model-picker-logic.ts (providerName l.26 — 'Anthropic' | 'OpenRouter'; searchValue l.34; suffixLabel l.41 — '~latest'/'always the latest', ':free'/'free tier'; groupByProvider l.79)
    src/app/(dashboard)/settings/page.tsx (the five props the form consumes)
    .planning/phases/21-settings-ui/21-REVIEW.md (IN-02 — stale-primary badge guess observation to record)
  </read_first>
  <action>
    Create `e2e/ver-05-settings.spec.ts` — a single spec file (Claude's discretion: one file for the three behaviors keeps the harness simple; the config's `workers: 1` + `fullyParallel: false` serializes it against plan 22-05's live run). Per RESEARCH Pattern 1 harness + VER-05 behaviors:

    - Header why-comment: VER-05 live-browser UAT (D-22-04/05); real Clerk auth via the auth-setup storageState; the unit side of these behaviors is already locked in model-picker-logic.test.ts (31 tests) — this spec observes the RENDERED behavior in a real browser.
    - `import { test, expect } from '@playwright/test';` — no DB imports needed (pure browser surface).
    - One `test('VER-05: provider-switch draft preservation (SET-03)', ...)`: `await page.goto('/settings')` (real auth — the config's storageState covers it). Drive: open the AI provider Select (aria-label "AI provider"), switch to 'OpenRouter' → assert the non-blocking reset hint renders ("Primary model reset to ..." — form l.169) and the primary picker's trigger reflects the OpenRouter default (anthropic/claude-sonnet-4.6). Then switch back to 'Anthropic' → assert fallbacks from the draft are preserved (add a fallback via "Add fallback" before switching, assert it survives the switch) and the primary resets to the anthropic default (keep-if-valid → reset-to-provider-default, D-07 draft-only).
    - One `test('VER-05: picker search + provider/family grouping (SET-06)', ...)`: open the primary or fallback ModelPicker, type in the CommandInput ("Search models…" placeholder) e.g. 'sonnet' → assert the list filters (fewer items, matching rows); assert provider/family grouping renders (CommandGroup headings from groupByProvider — 'Anthropic' group and 'OpenRouter' group sections present when the union picker is open).
    - One `test('VER-05: provider badges disambiguate same-name models (SET-05)', ...)`: in the union fallback picker, search 'claude-sonnet-5' → assert BOTH `claude-sonnet-5` (Anthropic badge) and `anthropic/claude-sonnet-5` (OpenRouter badge) rows appear with DISTINCT provider badges — the disambiguation claim (research: 336-row picker, dup-name models). Assert the saved-chain recap (form l.372) renders per-entry provider badges after saving a cross-provider chain.
    - One `test('VER-05: ~latest/:free ids never savable-or-served outside their labels (SET-07)', ...)`: search 'latest' in the OpenRouter picker → assert a `~...-latest` row renders its suffix label ("always the latest"); search ':free' → assert the free-tier label ("free tier" — rate-limited shared quota copy) renders; assert the saved-chain recap never shows a raw `~`/`:free` id without its label — the label comes from suffixLabel (unit-locked; the spec observes the rendered text).
    - IN-02 observation (21-REVIEW carry): after saving a chain, observe the saved-chain recap's primary trigger behavior for a stale id — record the observation as a comment in the spec + a note in the SUMMARY (does the badge guess correctly on a stale primary? record what renders). This is an OBSERVATION, not a hard assertion — 21-REVIEW flagged it for Phase 22 UAT observation.
    - Web-first assertions only (`page.getByText`, `page.getByRole`, `page.getByLabel`) — no waits on arbitrary selectors; no `test.setTimeout` needed (no live-key call in this spec — VER-05 is pure browser).
    - Run the file: `npx playwright test e2e/ver-05-settings.spec.ts` — the auth-setup project runs first via the config dependency; webServer auto-starts the dev server (Claude's discretion answer: the spec starts it itself via the harness — it does not gate on an already-running server except through reuseExistingServer).
  </action>
  <acceptance_criteria>
    - `e2e/ver-05-settings.spec.ts` exists with ≥ 4 test() blocks covering: draft preservation (SET-03), search + grouping (SET-06), badge disambiguation (SET-05), labels (SET-07)
    - The spec uses `page.getByLabel('AI provider')` / aria-label targeting (the form's SelectTrigger aria-label) and the CommandInput "Search models…" placeholder — real selectors from the shipped components
    - The IN-02 observation is recorded (comment in the spec + SUMMARY note)
    - `npx playwright test e2e/ver-05-settings.spec.ts` exits 0 (green against the dev server with real Clerk auth) — this is the TARGETED spec for this wave; plan 22-05's ver-02 spec does not exist yet at wave-2 execution, so the combined `npm run e2e` full-suite run is deferred to the phase gate at verify-work (serialized via workers: 1 — Pitfall 5)
  </acceptance_criteria>
  <verify>
    <automated>test -f e2e/ver-05-settings.spec.ts && grep -c "test(" e2e/ver-05-settings.spec.ts | awk '$1 >= 4' && npx playwright test e2e/ver-05-settings.spec.ts</automated>
  </verify>
  <done>The VER-05 spec is authored and green: it observes provider-switch draft preservation, picker search/grouping, badge disambiguation, ~latest/:free label discipline, and the IN-02 stale-primary observation — all through the real Clerk-authenticated browser against the dev server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /settings (dev server) | The spec drives the real Settings UI with the real Clerk session (storageState) — full user-path surface |
| spec → Clerk session | Real login (D-22-05), never a cookie-injection stub — the auth gate is genuinely exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-13 | Spoofing | e2e auth bypass via cookie stub | mitigate | D-22-05 mandates the real Clerk login — the auth-setup project (plan 22-03) signs in through Clerk's real infrastructure; this spec consumes the resulting storageState |
| T-22-14 | Tampering | spec asserting on implementation details instead of user-visible behavior | mitigate | The spec uses web-first user-visible assertions (labels, badges, group headings, rendered label text) — the unit logic is already locked in model-picker-logic.test.ts; the spec observes the rendered outcome, not internals |
| T-22-15 | Tampering | the ~/:free label claim drifting into "savable without label" | mitigate | SET-07 assertion: saved-chain recap must render the suffix label for any ~/:free id (suffixLabel unit-locked; the spec verifies the rendered text — no raw id without its label) |
| T-22-SC | Tampering | npm installs | accept | No installs in this plan (harness landed in plan 22-03) |

## Package Legitimacy

No packages installed by this plan.
</threat_model>

<verification>
1. `npx playwright test e2e/ver-05-settings.spec.ts` → green (4+ behavior tests with real Clerk auth)
2. Combined full-suite `npm run e2e` (ver-02 + ver-05 serially, workers: 1 — Pitfall 5) runs at the phase gate in verify-work once plan 22-05's spec exists — NOT during this wave-2 plan (ver-02 doesn't exist yet)
3. IN-02 observation recorded in the SUMMARY (stale-primary badge guess — what rendered)
4. No key values in the spec or SUMMARY (no live-key call in this spec by design)
</verification>

<success_criteria>
- VER-05 proven in a real browser: provider-switch draft preservation (SET-03), picker search + grouping (SET-06), provider-badge disambiguation (SET-05), and ~/:free label discipline (SET-07)
- The IN-02 review observation is recorded (21-REVIEW carry)
- The e2e spec is a reusable permanent gate (D-22-04), running with the real Clerk login (D-22-05) — no manual UAT session needed for these behaviors
</success_criteria>

<output>
Create `.planning/phases/22-verification-gate/22-06-SUMMARY.md` when done
</output>
