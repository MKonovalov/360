---
phase: 22-verification-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/verification/security-grep.test.ts
autonomous: true
requirements: [VER-04]
must_haves:
  truths:
    - "The security-matrix grep runs with every `npm test` (D-22-07) — a permanent gate, no manual step"
    - "The gate fails if OPENROUTER appears in any client component ('use client' files or src/components/)"
    - "The gate fails if OPENROUTER appears in any Server Action (src/app/actions/)"
    - "The gate fails on any NEXT_PUBLIC_OPENROUTER in src/ (the gate's own file excepted — it legitimately holds the literal as the leak token under test) or .env.example; .env.example must still declare OPENROUTER_API_KEY (server-only)"
    - "The gate is non-vacuous — an allowlist canary proves the scan actually matches the token (Pitfall 6: a rename can't silently disable the gate)"
    - "The current tree passes the gate (verified 2026-08-03: OPENROUTER in exactly 3 non-test server files — env.ts, modelFactory.ts, analyzeCompany.ts)"
  artifacts:
    - path: "src/lib/verification/security-grep.test.ts"
      provides: "Codified VER-04 security-matrix gate (allowlist + canary assertions)"
      contains: "NEXT_PUBLIC_OPENROUTER"
  key_links:
    - from: "src/lib/verification/security-grep.test.ts"
      to: "src/lib/env.ts"
      via: "allowlist Set membership + canary assertion"
      pattern: "OPENROUTER_API_KEY"
    - from: "src/lib/verification/security-grep.test.ts"
      to: ".env.example"
      via: "readFileSync at process.cwd()"
      pattern: "\\.env\\.example"
---

<objective>
Codify the VER-04 security-matrix grep as a Vitest test (D-22-07): a node-env test that scans `src/**` source files + `.env.example`, asserting `OPENROUTER` never appears in client-reachable code ('use client' files, `src/components/`, Server Actions, `NEXT_PUBLIC_*`) while the allowlisted server files DO contain `OPENROUTER_API_KEY` (canary — proves the gate isn't vacuous). It runs with every `npm test` forever.

Purpose: VER-04's claim is "no OpenRouter key-name leakage to the client bundle / Server Action returns / NEXT_PUBLIC_*". Today that proof is a manual grep; D-22-07 makes it a permanent automated gate. RESEARCH verified the current tree is green (exactly 3 non-test server files contain OPENROUTER) — this test locks that baseline.
Output: `src/lib/verification/security-grep.test.ts` (auto-discovered by the existing vitest include glob `src/**/*.test.ts`), green under `npm test`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-verification-gate/22-CONTEXT.md (D-22-07 — codified grep; VER-04 scope "client components + Server Action returns + .env.example + NEXT_PUBLIC_* usage")
@.planning/phases/22-verification-gate/22-RESEARCH.md (§Pattern 3 l.272-330 — the verified test skeleton incl. the required self-file exclusion in Test 3; §VER-04 row l.48 — currently green baseline; §Pitfall 6 l.417-421 — the vacuous-gate trap)
@.planning/phases/22-verification-gate/22-PATTERNS.md (§security-grep.test.ts l.92-106 — imports, walk shape, allowlist, canary; §No Analog Found l.304 — no fs-reading test exists in repo)
vitest.config.ts (include `['src/**/*.test.ts']` l.12, environment node l.11 — auto-discovers the new file, zero config change)
.env.example (l.33 OPENROUTER_API_KEY; no NEXT_PUBLIC_OPENROUTER — canary input verified)
src/lib/env.ts (allowlisted server file containing OPENROUTER_API_KEY)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the security-grep Vitest gate with allowlist + canary assertions</name>
  <files>src/lib/verification/security-grep.test.ts</files>
  <read_first>
    vitest.config.ts (l.11 environment node, l.12 include ['src/**/*.test.ts'] — auto-discovers the new file, zero config change)
    .env.example (l.33 OPENROUTER_API_KEY; no NEXT_PUBLIC_OPENROUTER — the canary inputs, verified 2026-08-03)
    src/lib/env.ts (allowlisted server file containing OPENROUTER_API_KEY — canary anchor)
    .planning/phases/22-verification-gate/22-RESEARCH.md §Pattern 3 l.272-330 (the verified skeleton — copy, with the ONE documented self-file deviation in Test 3) + §VER-04 row l.48 (green baseline) + §Pitfall 6 l.417-421 (vacuous-gate trap)
    .planning/phases/22-verification-gate/22-PATTERNS.md §security-grep.test.ts l.92-106 (imports, walk shape, allowlist, canary)
  </read_first>
  <behavior>
    - Test 1: for every file under src/ that is a client component ('use client' marker or under src/components/), the file must NOT contain 'OPENROUTER'
    - Test 2: for every file under src/app/actions/, the file must NOT contain 'OPENROUTER' (Server Action returns carry no key names)
    - Test 3: no file under src/ (EXCEPT the gate's own file — it legitimately holds the literal as the leak token under test) and no .env.example may contain 'NEXT_PUBLIC_OPENROUTER'; .env.example MUST contain 'OPENROUTER_API_KEY' (server-only declaration preserved)
    - Test 4 (canary): each allowlisted file (lib/env.ts, lib/agents/modelFactory.ts, lib/agents/analyzeCompany.ts) MUST contain 'OPENROUTER_API_KEY' — proving the scan matches the token and the gate is not vacuous
  </behavior>
  <action>
    Create `src/lib/verification/security-grep.test.ts` following RESEARCH Pattern 3 (l.280-330) and PATTERNS (l.92-106) — copy the verified skeleton with the ONE documented deviation (the self-file exclusion in Test 3, see item 3), then finalize:

    - Imports: `import { describe, expect, it } from 'vitest';` + `import { readdirSync, readFileSync, statSync } from 'node:fs';` + `import { join } from 'node:path';` (node builtins; no `@/` alias needed — paths are cwd-relative like seed.ts:26).
    - `const SRC = join(process.cwd(), 'src');` and `const ALLOWED = new Set(['lib/env.ts', 'lib/agents/modelFactory.ts', 'lib/agents/analyzeCompany.ts']);` — the verified 2026-08-03 baseline (RESEARCH l.286: exactly 3 non-test server files).
    - `function walk(dir)` — recursive readdirSync/statSync over `src/`, returning `.ts`/`.tsx` file paths relative to SRC (skip .test.ts? NO — the RESEARCH skeleton walks all files; keep it verbatim so test files containing OPENROUTER in assertions are scanned for the client checks — they are not 'use client' and not in components/, so they pass correctly; the canary's ALLOWED set is what anchors the token match). The ONE deviation from the skeleton is inside Test 3's assertion loop (see item 3) — the walk itself is unchanged.
    - Four `it()` blocks as RESEARCH l.300-328 (with the single documented deviation in Test 3):
      1. `no OPENROUTER in client components ("use client") or src/components` — isClient = `src.includes("'use client'") || rel.startsWith('components/')`; `expect(src, rel).not.toContain('OPENROUTER')`.
      2. `no OPENROUTER in Server Actions` — files filter `f.startsWith('app/actions/')`.
      3. `no NEXT_PUBLIC_OPENROUTER anywhere in src/ or .env.example; OPENROUTER_API_KEY present in .env.example` — loop all files EXCEPT the gate's own file + readFileSync('.env.example', 'utf8') (process.cwd()-relative, PATTERNS l.106); assert no NEXT_PUBLIC_OPENROUTER and assert `expect(example).toContain('OPENROUTER_API_KEY')`. **Self-file exclusion (MANDATORY — prevents the self-defeating gate):** inside the loop, `if (rel === 'lib/verification/security-grep.test.ts') continue;` BEFORE the assertion. This file's own assertion strings and test title legitimately contain the literal 'NEXT_PUBLIC_OPENROUTER' — it IS the leak token under test — so without the skip the gate can never pass on its own source. Every OTHER src file + .env.example remains scanned: the leak-detection assertion is NOT weakened (a leak in any real client/action/env file still fails the suite). Add a why-comment citing this exact reasoning. The canary-that-the-canary-works structure is preserved — Test 4 still asserts the ALLOWED server files contain OPENROUTER_API_KEY (Pitfall 6: the gate is still non-vacuous).
      4. `canary: the allowlisted server files DO contain OPENROUTER_API_KEY (the gate is not vacuous)` — loop ALLOWED, `expect(readFileSync(join(SRC, rel), 'utf8'), rel).toContain('OPENROUTER_API_KEY')`. Why-comment: Pitfall 6 — if a refactor renames the token to a casing variant, the canary fails loudly instead of the gate silently passing.
    - Add a header why-comment block (house style): D-22-07 mandates this as a permanent gate; the walk is source-level only (a post-build bundle scan is deferred per CONTEXT §Deferred — do NOT add build-time coupling).
    - Run `npx vitest run src/lib/verification/security-grep.test.ts` — must pass on the current tree (baseline verified green 2026-08-03). Do not weaken any assertion to make it pass.
  </action>
  <acceptance_criteria>
    - `src/lib/verification/security-grep.test.ts` exists with the four it() blocks from RESEARCH Pattern 3 (client scan, actions scan, NEXT_PUBLIC scan + .env.example, allowlist canary)
    - The ALLOWED set equals exactly `['lib/env.ts', 'lib/agents/modelFactory.ts', 'lib/agents/analyzeCompany.ts']`
    - Test 3's NEXT_PUBLIC loop excludes the gate's own file: `grep -c "security-grep.test.ts" src/lib/verification/security-grep.test.ts` → ≥ 1 (the `if (rel === 'lib/verification/security-grep.test.ts') continue;` skip is present — the only src file that legitimately holds the literal; every other src file + .env.example remains scanned)
    - `npx vitest run src/lib/verification/security-grep.test.ts` exits 0 on the current tree (the gate now passes its own source)
    - `npm test` exits 0 (the gate is now part of every full run — D-22-07)
    - `grep -c "NEXT_PUBLIC_OPENROUTER" src/lib/verification/security-grep.test.ts` ≥ 1 (the leakage token is asserted — kept verbatim, the exclusion does not remove the literal)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/verification/security-grep.test.ts && npm test && grep -q "security-grep.test.ts" src/lib/verification/security-grep.test.ts</automated>
  </verify>
  <done>The VER-04 security-matrix gate is codified as a Vitest test: it scans client components + Server Actions + .env.example + NEXT_PUBLIC_* usage, fails on any leak, includes the allowlist canary (non-vacuous), and runs with every npm test — green on the current tree, with Test 3's NEXT_PUBLIC walk excluding the gate's own file (which legitimately holds the literal) so the gate passes its own source without weakening a single leak-detection assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/** source files + .env.example → test | The gate reads every source file's TEXT looking for the OPENROUTER key-name — a static scan, no execution; the test itself is the security control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure | client components / Server Actions / NEXT_PUBLIC_* env | mitigate | This plan IS the mitigation: the gate fails on any OPENROUTER occurrence in client-reachable code or NEXT_PUBLIC_OPENROUTER anywhere except the gate's own test file (which legitimately holds the literal — Test 3's self-file skip); runs with every npm test (D-22-07). Baseline verified green 2026-08-03 |
| T-22-02 | Tampering | the gate itself (vacuous pass after a token rename) | mitigate | Allowlist canary (Test 4): the three server files MUST contain OPENROUTER_API_KEY — if the token string changes, the gate fails loudly instead of silently passing (Pitfall 6) |
| T-22-03 | Information Disclosure | .env.example server-only declarations | mitigate | Test 3 asserts OPENROUTER_API_KEY remains declared (server-only, non-PUBLIC_) and NEXT_PUBLIC_OPENROUTER never appears — a future PUBLIC_-prefix regression fails the suite |
| T-22-SC | Tampering | npm installs | accept | No installs in this plan; phase installs live in plan 22-03 (both packages slopcheck-[OK]) |

## Package Legitimacy

No packages installed by this plan.
</threat_model>

<verification>
1. `npx vitest run src/lib/verification/security-grep.test.ts` → green (the gate itself, passing its own source via the Test-3 self-file exclusion)
2. `npm test` → green (gate runs with the full suite — D-22-07 "runs with every npm test")
3. Canary sanity (negative control, one-off manual check): temporarily rename 'OPENROUTER' to 'OPENROUTERX' in a scratch copy is NOT required — the canary assertion structure (Test 4 asserting the token IS present in allowlisted files) is the structural guarantee; instead verify `grep -c "OPENROUTER_API_KEY" src/lib/env.ts` → ≥ 1 (the canary input is real)
</verification>

<success_criteria>
- VER-04 is codified as a permanent Vitest gate that runs with every `npm test` (D-22-07) — no manual grep step ever again
- The gate covers exactly the VER-04 wording: client components ('use client') + Server Action returns + .env.example + NEXT_PUBLIC_* usage
- The gate is non-vacuous (allowlist canary) and green on the current tree (baseline: 3 server files only)
</success_criteria>

<output>
Create `.planning/phases/22-verification-gate/22-02-SUMMARY.md` when done
</output>
