---
phase: 21-settings-ui
plan: 07
type: execute
wave: 2
depends_on: [21-06]
files_modified:
  - src/components/settings/model-settings-form.tsx
autonomous: true
gap_closure: true
requirements: [SET-05, SET-06]

must_haves:
  truths:
    - "The primary picker trigger shows the resolved display name next to the provider badge — a valid primary renders its display name (e.g. 'Claude Sonnet 4.6'), not the raw id (GAP-1 / SET-05, UI-SPEC §Row Anatomy l.137 '{provider badge} {selected model name}')"
    - "Save feedback no longer goes stale on draft edits — a failed save's errorMsg clears when the user edits, and 'Saved.' hides once the draft diverges from the saved state (WR-01)"
  artifacts:
    - path: "src/components/settings/model-settings-form.tsx"
      provides: "valueName prop on the primary ModelPicker + markDirty draft-edit feedback reset"
      contains: "valueName={unionServableModels.find((m) => m.id === primary)?.name}"
  key_links:
    - from: "src/components/settings/model-settings-form.tsx"
      to: "src/components/settings/model-picker.tsx"
      via: "JSX prop (valueName — the 21-06 seam consumer)"
      pattern: "valueName="
    - from: "src/components/settings/model-settings-form.tsx"
      to: "model-settings-form.tsx state"
      via: "markDirty() calls in the six draft mutators"
      pattern: "markDirty"
---

<objective>
Wire the CR-01 name-resolution seam and the WR-01 feedback-reset polish into the form, consuming the pure helpers and `valueName` prop shipped by plan 21-06.

Purpose: GAP-1 (SET-05) — the primary `ModelPicker` receives `optionsForSlot(primary, fallbacks, -1, servableByProvider[provider])`, which excludes the primary id by design, so the trigger could never resolve its display name. The form now passes a resolved `valueName` (union-servable lookup) that `triggerLabel` prefers — closing GAP-1 at the call site. GAP-2's check state and the WR-02 empty-list explanation were already closed in the wrapper (21-06); this plan completes the fix by feeding the wrapper the name-resolvable source. WR-01 — the `status`/`errorMsg` staleness after draft edits (failed save leaves a red message while the user edits; a successful save leaves 'Saved.' on a dirty draft) — is folded into this plan because it touches the same single file (form is 'extended, not rewritten' per CONTEXT.md).

Output: `model-settings-form.tsx` passes `valueName` on the primary slot (stale/unknown values yield `undefined` → unchanged raw-id + staleLabel path) and a `markDirty()` helper clears `status`/`errorMsg` from every draft mutator without clobbering an in-flight save.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-settings-ui/21-VERIFICATION.md
@.planning/phases/21-settings-ui/21-REVIEW.md
@src/components/settings/model-settings-form.tsx
@src/components/settings/model-picker.tsx
@src/components/settings/model-picker-logic.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pass the resolved valueName to the primary ModelPicker (CR-01 call-site fix)</name>
  <files>src/components/settings/model-settings-form.tsx</files>
  <read_first>
    - src/components/settings/model-settings-form.tsx (the file being modified — read the primary ModelPicker block lines 224-243 in full: `options={optionsForSlot(primary, fallbacks, -1, servableByProvider[provider])}`, `badge={provider}`, `staleLabel` from savedChain, and the `isStale(primary)` hint below)
    - src/components/settings/model-picker.tsx (the 21-06 `valueName?: string | null` prop — how the wrapper prefers it in `triggerLabel(value, options, valueName)` and feeds `pinnedSelection`)
    - src/components/settings/model-picker-logic.test.ts lines 63-71 (the fixture's servableByProvider — confirms the anthropic primary `claude-sonnet-4-6` IS in unionServableModels, so the union lookup resolves valid primaries)
  </read_first>
  <action>
    In the primary `<ModelPicker>` JSX block (lines 224-243 of model-settings-form.tsx), add one prop after `value={primary}`:

    `valueName={unionServableModels.find((m) => m.id === primary)?.name}`

    - Resolution source = `unionServableModels` (the server-trimmed union prop already passed to this form, T-17-09) — the review's recommended fix (21-REVIEW.md CR-01). Every valid, non-stale primary is in the union (the union is the superset of both per-provider servable lists), so the lookup resolves any current primary's display name.
    - A stale primary (id absent from the union, e.g. dropped from the roster) yields `undefined` → the wrapper's `triggerLabel` falls through to the raw id and the existing `staleLabel` disabled-row + red-hint path (lines 238-248) is preserved verbatim. This is REQUIRED: the 21-06 `pinnedSelection` guard (`!valueName` → no pin) depends on stale values never receiving a valueName, so the pinned current-selection row can never collide with the stale-row rendering.
    - Add a one-line why-comment above the prop: the primary id is excluded from its own options by optionsForSlot(slotIndex=-1) dup-chain prevention, so the trigger name must resolve from the union — the CR-01 name seam.
    - Do NOT pass valueName on the fallback slots (their own id IS retained in their options — the wrapper's options-lookup path already resolves them, and `pinnedSelection` would return null for them anyway); do NOT touch `badge`, `grouped`, `staleLabel`, `placeholder`, `onChange`, or the submit contract.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "valueName={unionServableModels.find((m) => m.id === primary)?.name}" src/components/settings/model-settings-form.tsx` → 1
    - Source assertion: `grep -c "valueName=" src/components/settings/model-settings-form.tsx` → 1 (primary slot only — fallback ModelPicker sites must NOT pass it)
    - Behavior assertion (source trace): for `primary='claude-sonnet-4-6'` (valid anthropic default) the union lookup returns `'Claude Sonnet 4.6'`; `triggerLabel` prefers it → trigger renders `{Anthropic badge} Claude Sonnet 4.6` (UI-SPEC l.137). For a stale primary the union lookup returns `undefined` → no valueName → trigger shows the raw id and the staleLabel path renders (unchanged).
    - Client-safety (T-17-09): the valueName source is the already-trimmed `unionServableModels` prop — no new import; `grep -c "catalog.json" src/components/settings/model-settings-form.tsx` → 0; the only `lib/models/catalog` reference remains the pre-existing type-only `ModelProviderId` import
    - Test command: `npx tsc --noEmit` — exit 0
    - Test command: `npx vitest run src/components/settings/model-picker-logic.test.ts` — all pass (21-06 suite, no regression)
  </acceptance_criteria>
  <verify>
    npx tsc --noEmit && npx vitest run src/components/settings/model-picker-logic.test.ts && grep -c "valueName={unionServableModels.find((m) => m.id === primary)?.name}" src/components/settings/model-settings-form.tsx && grep -c "valueName=" src/components/settings/model-settings-form.tsx
  </verify>
  <done>The primary slot passes valueName resolved from the union; stale/unknown primaries yield undefined (raw-id + staleLabel paths unchanged); fallback slots untouched; tsc + logic suite green; canaries hold.</done>
</task>

<task type="auto">
  <name>Task 2: Reset status/errorMsg on draft edits via markDirty (WR-01)</name>
  <files>src/components/settings/model-settings-form.tsx</files>
  <read_first>
    - src/components/settings/model-settings-form.tsx (the file being modified — read the state block lines 67-97, `handleSave` lines 99-124, and ALL six draft mutators: `handleProviderChange` (145-165), the primary `onChange` (229-234), the fallback `onChange` (276-282), `moveFallback` (126-134), `removeFallback` (136-138), `addFallback` (140-142))
    - src/components/settings/model-picker.tsx (unchanged by this task — the `onChange` callbacks the form wires)
    - 21-REVIEW.md WR-01 (the review's recommended fix shape: preserve the 'saving' status mid-flight)
  </read_first>
  <action>
    Add a `markDirty()` helper to model-settings-form.tsx and call it from every draft mutator:

    `function markDirty() { setStatus((s) => (s === 'saving' ? s : 'idle')); setErrorMsg(null); }`

    - `setStatus((s) => (s === 'saving' ? s : 'idle'))` — the updater form PRESERVES an in-flight `'saving'` (a save started moments ago must not be relabeled by a concurrent edit; the review's exact fix). Any other status (`'saved'`, `'error'`, `'idle'`) → `'idle'`.
    - `setErrorMsg(null)` — clears the red error copy.
    - Call `markDirty()` FIRST in all six draft mutators:
      1. `handleProviderChange` — the provider switch stages a new primary (draft edit), even when keep-if-valid preserves the value
      2. primary `onChange` (the `setPrimary(v)` callback, next to the existing `setResetHint(null)`)
      3. fallback `onChange` (the `setFallbacks` callback)
      4. `moveFallback`
      5. `removeFallback`
      6. `addFallback`
    - Do NOT call markDirty from `handleSave` (it manages its own status transitions: `'saving'` → `'saved'` | `'error'`), and do NOT touch `setResetHint` (its lifecycle — cleared on manual primary edit + on save success — is separate and already correct).
    - Add a one-line why-comment: WR-01 — after a failed save the red errorMsg must not persist while the user edits, and 'Saved.' must not survive a dirty draft; the 'saving' status is exempt so a just-started save is never relabeled.
    - Placement: define `markDirty` adjacent to the other handlers (e.g. right after `handleSave`, before `moveFallback`), matching the existing function-declaration house style.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c "function markDirty" src/components/settings/model-settings-form.tsx` → 1
    - Source assertion: `grep -c "markDirty();" src/components/settings/model-settings-form.tsx` → 6 (one call per draft mutator: handleProviderChange, primary onChange, fallback onChange, moveFallback, removeFallback, addFallback — the `;` excludes the definition line `function markDirty() {`, so the count is 6 call sites, not 7)
    - Source assertion: `grep -c "s === 'saving' ? s : 'idle'" src/components/settings/model-settings-form.tsx` → 1 (in-flight save preserved)
    - Source assertion: markDirty contains `setErrorMsg(null)`; handleSave does NOT call markDirty (verify by reading the authored code)
    - Behavior assertion (source trace): a failed save leaves `status='error'` + `errorMsg`; the next primary/fallback edit (or provider switch, or fallback add/move/remove) fires markDirty → `status='idle'`, `errorMsg=null` → the error paragraph (line 364-366) un-renders and the button re-enables per the stale gate. After a successful save, any edit hides 'Saved.' (status → idle) while the recap already self-hides via the lastSaved equality gate.
    - Test command: `npx tsc --noEmit` — exit 0
    - Test command: `npm test` — full suite passes (356 + the 21-06 additions, no regressions)
    - Test command: `npm run build` — exit 0 (the phase's production-build gate)
  </acceptance_criteria>
  <verify>
    npx tsc --noEmit && npm test && npm run build && grep -c "function markDirty" src/components/settings/model-settings-form.tsx && grep -c "markDirty();" src/components/settings/model-settings-form.tsx && grep -c "s === 'saving' ? s : 'idle'" src/components/settings/model-settings-form.tsx
  </verify>
  <done>markDirty exists (updater preserves 'saving'), is called from all six draft mutators, and is NOT called from handleSave; 'Saved.' and errorMsg clear on draft edits; tsc, full npm test, and the production build all pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form (model-settings-form.tsx) ← server props | `valueName` derives from the already-trimmed `unionServableModels` prop (server-validated `ServableModel` rows, T-17-09). Same boundary assessed in 21-05 — names were already rendered from these props in the recap and staleLabel. No new data crosses. |
| auth boundary | Settings page route + `saveSettingsAction` (`requireStaffAccess` FIRST) are server code this plan does not modify. The submit contract `{primaryModel, fallbacks}` is untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07-01 | Tampering | valueName rendered in the primary trigger | mitigate | React text interpolation (no `dangerouslySetInnerHTML` anywhere in the form — verify `grep -c "dangerouslySetInnerHTML"` → 0); the valueName string is the same server-trimmed `name` already rendered in 21-05's recap and stale rows. |
| T-21-07-02 | Information Disclosure | catalog data reaching the client via the valueName source | mitigate | The lookup runs over the already-trimmed `unionServableModels` prop — `catalog.json` stays server-only (T-17-09); canary `grep -c "catalog.json" src/components/settings/model-settings-form.tsx` → 0; the form's only `lib/models/catalog` reference is the pre-existing type-only import (erased at compile). |
| T-21-07-03 | Tampering | markDirty feedback-state mutation | accept | Pure client state; no data crosses a boundary. Worst case is stale feedback text, which this task removes; no security impact. The 'saving' exemption is a UX guard, not a security control. |
| T-21-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this gap plan — nothing new to audit. |

Security posture unchanged: auth boundary, server action order, ERROR_COPY map, D-13 draft preservation, and the submit contract are all untouched by this plan (extended form only).
</threat_model>

<verification>
- `npx tsc --noEmit` — exit 0 (after both tasks)
- `npm test` — full suite passes (no regressions; the 21-06 logic additions included)
- `npm run build` — exit 0 (production build, the phase gate)
- Client-safety canary: `grep -c "catalog.json" model-settings-form.tsx` → 0; only the pre-existing type-only `ModelProviderId` import references lib/models/catalog
- `grep -c "dangerouslySetInnerHTML" model-settings-form.tsx` → 0
- Anti-Pattern 1 lock: no new model-id→provider lookup added; valueName resolves over the already-provider-scoped union prop
- optionsForSlot dedupe untouched: the primary id exclusion from its own options remains (form still passes slotIndex=-1)
</verification>

<success_criteria>
- GAP-1 closed end-to-end: the primary trigger renders `{provider badge} {display name}` — valueName flows form → wrapper → triggerLabel, and the union lookup resolves every valid primary (unit-pinned seam from 21-06 consumed at the call site)
- GAP-2 closed end-to-end: the wrapper's pinned current-selection row (21-06) now receives its valueName from the form, so the primary picker always shows the checked current model and explains the legitimately-empty anthropic list
- WR-01 closed: markDirty from all six draft mutators clears status/errorMsg without clobbering an in-flight save; 'Saved.' and error copy never outlive the draft state they describe
- Preserved invariants: T-17-09 client-safety canaries hold, Anti-Pattern 1 provider-scoped lookups untouched, optionsForSlot dedupe semantics unchanged, submit contract + ERROR_COPY + D-13 verbatim
- All gates green: tsc, full npm test, npm run build
</success_criteria>

<output>
Create `.planning/phases/21-settings-ui/21-07-SUMMARY.md` when done
</output>
