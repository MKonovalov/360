---
phase: 21-settings-ui
plan: 05
type: execute
wave: 3
depends_on: [21-03, 21-04]
files_modified:
  - src/components/settings/model-settings-form.tsx
autonomous: true
requirements: [SET-01, SET-02, SET-03, SET-04, SET-05, SET-06, SET-07, SET-08]
must_haves:
  truths:
    - "Staff see an always-valued AI Provider selector (Anthropic + OpenRouter) directly above the 'Primary model' label; switching provider refreshes the Primary picker from that provider's servable source (SET-01, SET-02, D-21-03)"
    - "Switching provider follows keep-if-valid → reset-to-provider-default with a non-blocking hint under the selector; fallbacks are preserved verbatim; Save stays gated on union-wide staleness (SET-03, SET-08, D-21-01, D-21-02, D-21-04)"
    - "Primary + fallback slots render as Command Comboboxes with provider-scoped/union options, provider badges, ~latest/:free labels, and amber high-cost captions; the saved-chain recap shows provider badges per model (SET-04/05/06/07, D-21-06/10/12/13)"
    - "The submit contract is unchanged ({primaryModel, fallbacks} → saveSettingsAction) and the ERROR_COPY map + D-13 draft preservation are untouched (SET-08 backstop)"
  artifacts:
    - path: "src/components/settings/model-settings-form.tsx"
      provides: "Extended form: provider state + Select above Primary label + reset hint + union staleIds gate + ModelPicker on all model slots + badges + saved-chain recap"
      contains: "primaryAfterProviderSwitch"
    - path: "src/components/settings/model-settings-form.tsx"
      provides: "Union-wide client staleness gate (D-21-14)"
      contains: "unionIds"
  key_links:
    - from: "src/components/settings/model-settings-form.tsx"
      to: "src/app/actions/settings.ts"
      via: "unchanged saveSettingsAction submit contract"
      pattern: "saveSettingsAction"
    - from: "src/components/settings/model-settings-form.tsx"
      to: "src/components/settings/model-picker.tsx (plan 21-04)"
      via: "ModelPicker on primary + fallback slots"
      pattern: "ModelPicker"
    - from: "src/components/settings/model-settings-form.tsx"
      to: "src/components/settings/model-picker-logic.ts (plan 21-02)"
      via: "primaryAfterProviderSwitch/staleIds/optionsForSlot/providerName"
      pattern: "optionsForSlot"
---

<objective>
Extend `src/components/settings/model-settings-form.tsx` — the Phase 17 form extended in place, never rewritten (CONTEXT.md: "all extended, not rewritten") — into the provider-aware Settings UI. Task 1 adds the provider dimension: the always-valued AI Provider Select above the Primary label (D-21-03), provider state + the keep-if-valid → reset-to-provider-default reducer wiring with the non-blocking hint (D-21-01, SET-03), fallback preservation (D-21-02), and the union-wide staleness gate (D-21-14, SET-08). Task 2 swaps every model slot from Select to the Command Combobox wrapper (D-21-06), wires the row anatomy (provider badges, suffix labels, high-cost captions — SET-05/07/08), and adds the saved-chain recap with per-model provider badges (D-21-10).

Purpose: This is the phase's visible deliverable — the milestone goal's UI half. The server half (props) shipped in plan 21-03, the picker primitive in 21-04, and the decision logic in 21-02; this plan consumes all three and reuses the existing draft-staging, ERROR_COPY, save-flow, and D-13 failure-preservation machinery verbatim. The submit contract stays `{ primaryModel, fallbacks }` — saveSettingsAction is unchanged (already union-validating since Phase 19, REG-07).
Output: The complete provider-aware Settings AI Model Configuration card, gated by `npm test` + `npm run build` (the VALIDATION.md phase gate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-settings-ui/21-CONTEXT.md (D-21-01..14 — the locked behaviors; Claude's discretion items)
@.planning/phases/21-settings-ui/21-RESEARCH.md (§Open Questions (RESOLVED) — Q2 hint lifecycle + Q3 primary-direction dedupe; Pattern 2 l.257-318)
@.planning/phases/21-settings-ui/21-UI-SPEC.md (§Copywriting l.105-123 — exact strings incl. the reset hint; §Interaction Contract l.148-171 — the provider-switch sequence + save flow; §Row Anatomy l.127-143)
@.planning/phases/21-settings-ui/21-PATTERNS.md (§model-settings-form.tsx l.275-300 — patterns to preserve verbatim + new additions)
@.planning/phases/21-settings-ui/21-VALIDATION.md (§Sampling Rate l.30-36 — phase gate = npm test + npm run build)
src/components/settings/model-settings-form.tsx (current — 322 lines; the file being extended)
src/components/settings/model-picker.tsx (plan 21-04 output)
src/components/settings/model-picker-logic.ts (plan 21-02 output)
src/app/(dashboard)/settings/page.tsx (plan 21-03 output — the new props)
src/app/actions/settings.ts (unchanged — the submit contract)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Form provider dimension — Select above Primary, provider state, reset reducer + hint, union staleness gate</name>
  <files>src/components/settings/model-settings-form.tsx</files>
  <read_first>
    src/components/settings/model-settings-form.tsx (current — 322 lines: props l.31-41, draft state l.45-49, servableIds + staleIds gate l.51-61, handleSave l.63-82, add/move/remove fallbacks l.84-100, optionLabel l.106-110, isStale l.112, JSX shell l.114-166)
    src/app/(dashboard)/settings/page.tsx (plan 21-03 output — the new props being passed: providers, servableByProvider, unionServableModels, defaults, savedChain)
    .planning/phases/21-settings-ui/21-UI-SPEC.md §Interaction Contract l.148-171 (provider switch sequence 1-5) + §Copywriting l.113 (the exact hint string)
    .planning/phases/21-settings-ui/21-PATTERNS.md §model-settings-form.tsx (l.275-300 — preserve-verbatim list)
    .planning/phases/21-settings-ui/21-CONTEXT.md D-21-01/02/03/04
  </read_first>
  <action>
    Extend `src/components/settings/model-settings-form.tsx` in place. Task 1 scope — the provider dimension PLUS re-pointing the existing Select-based pickers at the new props so the file compiles (the page, plan 21-03, no longer passes `servableModels`/`defaultPrimary`); the visual swap to ModelPicker lands in Task 2. Declare the provider-switch handler as a `function handleProviderChange(next: ModelProviderId) { ... }` declaration (house style — matches the existing `handleSave`/`moveFallback`/`removeFallback` handlers; the Task 1 automated gate scopes its body).

    A) Prop signature (l.31-41): replace `servableModels: ServableModel[]` and `defaultPrimary: { id: string; name: string }` with the plan-21-03 set:
    ```
    providers: { id: ModelProviderId; name: string }[];
    servableByProvider: Record<ModelProviderId, ServableModel[]>;
    unionServableModels: ServableModel[];
    defaults: Record<ModelProviderId, { id: string; name: string }>;
    savedChain: { id: string; name: string; providerID: ModelProviderId | null }[] | null;
    ```
    Keep `saved: SavedSettings | null` and `catalogGeneratedAt: string`. Import the logic helpers: `import { primaryAfterProviderSwitch, staleIds as computeStaleIds, optionsForSlot, providerName } from './model-picker-logic';` plus `import type { ServableModel, ModelProviderId } from './model-picker-logic';` (type-only — T-17-09). Remove the now-unused local `type ServableModel` (l.29) if duplicated.

    B) State (l.45-49 area):
    - `const [provider, setProvider] = useState<ModelProviderId>(savedChain?.[0]?.providerID ?? 'anthropic');` with a why-comment: initial provider = the saved primary's provider (A4), else the REG-05 Anthropic fast path.
    - Initial primary: `saved?.primaryModel ?? defaults[provider].id` (the empty-state prefill now reads from `defaults` — the D-07 provider default).
    - `const [resetHint, setResetHint] = useState<string | null>(null);` (the D-21-01 non-blocking hint text).
    - `const [lastSaved, setLastSaved] = useState<{ primary: string; fallbacks: string[] } | null>(null);` (the saved-chain recap gate — Task 2 consumes it).
    - `const unionIds = new Set(unionServableModels.map((m) => m.id));` (the union-wide staleness set — D-21-14).

    C) Union staleness gate (l.51-61, D-21-14): replace `servableIds`/`staleIds` with `const staleIds = computeStaleIds([primary, ...fallbacks], unionIds);` — keep the existing why-comment verbatim (staleness derives from the CURRENT DRAFT, never the immutable saved props; '' is an in-progress row) and extend it: the gate widened from the anthropic-only list to the union servable set (D-21-14). `saveDisabled = isPending || staleIds.length > 0` unchanged. `isStale(id)` becomes `id !== '' && !unionIds.has(id)` (union-wide only — the provider-scoped option source for the primary picker is `servableByProvider[provider]` directly, no separate Set needed).

    D) Provider selector (D-21-03, SET-01): directly above the "Primary model" label (l.140), render:
    - a label `<p className="text-[12px] font-normal leading-[1.4] text-slate-500">AI provider</p>` and a shadcn Select (existing select.tsx import stays — D-21-06 keeps the 2-choice provider selector as Select) with `value={provider}` and `onValueChange={handleProviderChange}`, `aria-label="AI provider"`, mapping `providers` prop to `SelectItem`s.
    - `function handleProviderChange(next: ModelProviderId)` — calls `primaryAfterProviderSwitch(primary, next, servableByProvider, defaults)`; `setPrimary(result.primary)`; when `result.resetToDefault` → `setResetHint(\`Primary model reset to ${defaults[next].name} for ${providers.find((p) => p.id === next)?.name ?? next}.\`)` (the UI-SPEC §Copywriting string); else `setResetHint(null)`; `setProvider(next)`. Why-comments: (1) fallbacks are NEVER touched here (D-21-02 — the chain may become cross-provider by design; the union pickers still render them); (2) the reset is draft-only (D-07) — nothing persists until Save; (3) Pitfall 6: if the reset lands on an id a preserved fallback already holds, the server duplicate_model backstop surfaces the existing ERROR_COPY (research recommendation — do NOT clear the fallback).
    - The hint renders under the selector when `resetHint !== null`: `<p className="text-[14px] font-normal leading-[1.5] text-slate-600">{resetHint}</p>` (informational — never red, D-21-01).
    - Hint lifecycle (RESEARCH Open Question 2 — RESOLVED): clear the hint (`setResetHint(null)`) whenever the user changes the primary AND in `handleSave` success — the reset is then moot. Wire the primary onChange (Task 2's ModelPicker onChange) to also `setResetHint(null)`.

    E) Empty-state copy (l.122-125): replace `{defaultPrimary.name}` with `{defaults[provider].name}` (REG-05 default chain head is the anthropic default at mount).

    F) Save flow (l.63-82): on success, in addition to `setStatus('saved')`, set `setLastSaved({ primary, fallbacks: fallbacks.filter((id) => id !== '') })` and `setResetHint(null)`. D-13 unchanged (draft preserved verbatim on failure). Submit payload unchanged: `{ primaryModel: primary, fallbacks: fallbacks.filter((id) => id !== '') }`.

    G) Re-point the existing Select-based pickers at the new props (the file must compile at the end of Task 1 — the page no longer passes `servableModels`):
    - Delete `const servableIds = servableModels.map((m) => m.id);` (l.51) — `unionIds` (section B) replaces it. `isStale(id)` (l.112) becomes `id !== '' && !unionIds.has(id)`.
    - Primary Select options (l.146): `servableModels.map((m) => ...)` → `servableByProvider[provider].map((m) => ...)` — the provider-scoped servable source (SET-02); `value={primary}`/`onValueChange={setPrimary}` unchanged.
    - `optionLabel` (l.106-110): change the lookup to `const m = unionServableModels.find((mm) => mm.id === id);` — the union contains every servable id, so the label/cost resolution is provider-correct (Anti-Pattern 1 is not at risk here because `unionServableModels` rows are already provider-scoped from the page); a stale id falls through to the existing raw-id return.
    - Fallback Select options (l.234-248): replace `servableIds.filter((id) => id !== primary && !fallbacks.some((f, j) => f === id && j !== i))` with `unionServableModels.filter((m) => m.id !== primary && !fallbacks.some((f, j) => f === m.id && j !== i))` and render `m.name`/`m.costInput`/`m.costOutput` directly from the row objects — the D-08/D-09 client dedupe preserved, widened to the union (D-21-14).
    - REMOVE the sonnet-only conditional branch (l.169-208, the `servableModels.length === 1 ? ... : ...` ternary): the fallback section always renders its full form. The union is 337 rows — never 1 — so "No additional models available — only one model is runnable right now." is factually impossible and the branch is dead weight from the v1.3 anthropic-only world (checker resolution: keying it on the provider list would wrongly suppress the 337-option union pickers under Anthropic and hide saved cross-provider fallbacks). Stale saved fallbacks are handled by the general path — disabled stale item + red "no longer runnable" hint + remove button — so the D-10/D-11 "stale fallback stays renderable and removable" contract is preserved. Add a why-comment at the site noting the branch was removed because the union spans both providers.

    This task's JSX must compile with the re-pointed Select-based pickers (Task 2 replaces them with ModelPicker) — only the selector block, state/props, gate, data-source re-pointing, and branch removal land here.
  </action>
  <acceptance_criteria>
    - `model-settings-form.tsx` renders an "AI provider" labeled Select (from select.tsx) directly above the "Primary model" label, with options from the `providers` prop and `value={provider}` / `onValueChange={handleProviderChange}`; the string "AI provider" appears exactly twice (the visible `<p>` label AND the Select's `aria-label` — checker resolution)
    - `handleProviderChange` is a `function` declaration that calls `primaryAfterProviderSwitch` and whose BODY contains no `setFallbacks` call (fallbacks preserved — D-21-02)
    - the reset hint string template `Primary model reset to ${defaults[next].name}` is present; the hint `<p>` uses `text-slate-600`
    - `staleIds` derives from `unionIds` (union servable set — D-21-14): `computeStaleIds([primary, ...fallbacks], unionIds)`
    - `defaults[provider].id` used for the initial primary prefill and `defaults[provider].name` in the empty-state copy (no `defaultPrimary` reference remains)
    - `setLastSaved({` present in the save-success branch
    - `servableModels` is gone from the prop signature; the primary Select reads `servableByProvider[provider]` and `optionLabel` resolves via `unionServableModels`; the sonnet-only conditional branch is removed (the fallback section always renders)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "function handleProviderChange" src/components/settings/model-settings-form.tsx | grep -qx 1 && awk '/function handleProviderChange\(/,/^  }$/' src/components/settings/model-settings-form.tsx | grep -c "setFallbacks" | grep -qx 0 && grep -c "primaryAfterProviderSwitch(primary, next" src/components/settings/model-settings-form.tsx | grep -qx 1 && grep -c "computeStaleIds(\[primary, ...fallbacks\], unionIds)" src/components/settings/model-settings-form.tsx | grep -qx 1 && grep -c "AI provider" src/components/settings/model-settings-form.tsx | grep -qx 2 && grep -c "defaultPrimary" src/components/settings/model-settings-form.tsx | grep -qx 0 && grep -c "servableByProvider\[provider\]" src/components/settings/model-settings-form.tsx | grep -qx 1 && grep -c "servableModels" src/components/settings/model-settings-form.tsx | grep -qx 0 && npx tsc --noEmit</automated>
  </verify>
  <done>The form carries provider state, the Select above the Primary label, the keep-if-valid→reset reducer wiring with the non-blocking hint, fallback preservation, the union-wide staleness gate, re-pointed Select data sources, and the removed sonnet-only branch; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: ModelPicker swaps + row anatomy + saved-chain recap + phase gate</name>
  <files>src/components/settings/model-settings-form.tsx</files>
  <read_first>
    src/components/settings/model-settings-form.tsx (Task 1 output — the extended form)
    src/components/settings/model-picker.tsx (plan 21-04 output — the wrapper props contract: id, ariaLabel, value, options, onChange, placeholder, badge, grouped, staleLabel)
    src/components/settings/model-picker-logic.ts (plan 21-02 output — optionsForSlot slotIndex semantics: -1 = primary direction)
    .planning/phases/21-settings-ui/21-UI-SPEC.md §Row Anatomy (l.127-143) + §Copywriting (l.119 — the saved-chain recap string "Saved chain: …")
    .planning/phases/21-settings-ui/21-PATTERNS.md §model-settings-form.tsx (l.275-300 — preserve-verbatim list: D-08/D-09 dedupe l.234-237 → optionsForSlot; stale-row rendering l.154-165/252-256; cost caption l.106-110)
  </read_first>
  <action>
    Continue extending `model-settings-form.tsx` (Task 1 output). Task 2 scope — the picker swaps + row anatomy + recap:

    A) Replace the PRIMARY model Select (l.141-160) with `ModelPicker`:
    - `options={optionsForSlot(primary, fallbacks, -1, servableByProvider[provider])}` (slotIndex -1 = the primary direction — excludes the primary id AND all fallback-chosen ids, Open Question 3: the primary never duplicates a fallback so Save can't hit duplicate_model; why-comment).
    - `badge={provider}` (trigger badge — the closed state disambiguates at a glance, D-21-10), `grouped={false}` (provider-scoped single group with the provider header), `value={primary}`, `onChange={(v) => { setPrimary(v); setResetHint(null); }}`, `id="primary-model"`, `ariaLabel="Primary model"`, `placeholder="Select a model…"`.
    - `staleLabel={isStale(primary) ? (savedChain?.find((sc) => sc.id === primary)?.name ?? primary) : null}` (D-10/D-11 stale-row rendering: name from savedChain, raw-id fallback).
    - Keep the existing red hint below the picker (`isStale(primary)` → the "no longer runnable" paragraph, unchanged copy l.161-165).

    B) Replace the FALLBACK model Selects (l.217-258) with `ModelPicker`:
    - `options={optionsForSlot(primary, fallbacks, i, unionServableModels)}` (union-wide — SET-04; excludes primary + other fallback slots, D-08/D-09 widened; why-comment).
    - `grouped={true}` (provider CommandGroup sections — D-21-08), `badge={unionServableModels.find((m) => m.id === fb)?.providerID ?? undefined}` — the trigger badge is REQUIRED on every picker (UI-SPEC §Row Anatomy l.137: closed-state trigger badges disambiguate same-name models at a glance, e.g. `claude-sonnet-5` vs `anthropic/claude-sonnet-5`; checker resolution). A stale fallback id absent from the union yields no badge (raw label shown). `value={fb}`, `onChange={(v) => setFallbacks((prev) => { const next = [...prev]; next[i] = v; return next; })}`, `id={`fallback-${i + 1}`}`, `ariaLabel={`Fallback model ${i + 1}`}`, `placeholder="Select a fallback…"`.
    - `staleLabel={isStale(fb) ? (savedChain?.find((sc) => sc.id === fb)?.name ?? fb) : null}`.
    - Keep the move-up/move-down/remove ghost buttons + aria-labels exactly as-is (l.259-284) and the per-row red stale hint (l.286-290).

    C) The sonnet-only conditional branch was REMOVED in Task 1 G (the fallback section always renders its full form — the union is 337 rows, never 1; stale saved fallbacks ride the general staleLabel + red-hint + remove path, D-10/D-11 preserved). No action in this task.

    D) Remove the now-unused `optionLabel` helper (l.106-110 — its union lookup from Task 1 G is superseded by the ModelPicker's own label/cost rendering) and the Select model-picker imports if no longer referenced (keep `Select` — the provider selector still uses it, D-21-06). The `isStale` helper stays (used for the red hints + staleLabel computation).

    E) Saved-chain recap (D-21-10, UI-SPEC §Copywriting): next to the "Saved." confirmation (l.306-310), when `status === 'saved' && lastSaved && primary === lastSaved.primary && fallbacks.filter((f) => f !== '').join('|') === lastSaved.fallbacks.join('|')` render:
    `<p className="text-[14px] font-normal leading-[1.5] text-slate-600">Saved chain: {badge + name} → {badge + name} …</p>` — one entry per model in [primary, ...fallbacks.filter(f => f !== '')], each a `Badge variant="secondary"` (providerName of `unionServableModels.find((m) => m.id === id)?.providerID ?? 'anthropic'`) + the model name, joined by " → ". The recap hides once the user edits any slot (the equality check fails — the draft no longer equals lastSaved). Why-comment: the chain-entry badges are what disambiguate a saved cross-provider chain at a glance (D-21-10); saved ids are union-validated so the union lookup resolves their provider.

    F) ERROR_COPY (l.22-26), the submit payload, `handleSave`'s useTransition flow, and D-13 draft preservation: UNCHANGED. The submit contract stays `{ primaryModel: primary, fallbacks: fallbacks.filter((id) => id !== '') }` — saveSettingsAction is already union-validating (REG-07, Phase 19).

    G) Phase gate (VALIDATION.md §Sampling Rate): after both tasks, run `npm test` (full suite — the untouched catalog/settings action tests must still pass) and `npm run build` (catches the vendored-file dangling-import pitfall, the RSC boundary, and the props wiring). Both must be green before declaring the plan done. Fix anything they surface within this plan.
  </action>
  <acceptance_criteria>
    - `model-settings-form.tsx` renders `ModelPicker` for the primary slot with `options={optionsForSlot(primary, fallbacks, -1, servableByProvider[provider])}` and `badge={provider}`
    - renders `ModelPicker` for the fallback rows (one JSX site inside the `.map`) with `optionsForSlot(primary, fallbacks, i, unionServableModels)`, `grouped` true, and `badge={unionServableModels.find((m) => m.id === fb)?.providerID ?? undefined}` (UI-SPEC trigger badges everywhere)
    - exactly 2 `<ModelPicker` JSX usage sites (primary + the fallback map — the import line excluded) and exactly 1 `<Select ` root usage — the "AI provider" selector (D-21-06: no model-slot Select remains)
    - the saved-chain recap string `Saved chain:` is present with per-entry `Badge variant="secondary"` and providerName lookup over `unionServableModels`
    - `ERROR_COPY` still has exactly the three keys (action_failed/invalid_model/duplicate_model); the submit payload still reads `{ primaryModel: primary, fallbacks: fallbacks.filter((id) => id !== '') }`
    - `npx vitest run src/components/settings/model-picker-logic.test.ts` exits 0 (logic untouched)
    - `npm test` exits 0 (full suite — exit code asserted, output suppressed; rerun `npm test` alone for diagnostics)
    - `npm run build` exits 0 (the VALIDATION.md phase gate — exit code asserted)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "<ModelPicker" src/components/settings/model-settings-form.tsx | grep -qx 2 && grep -c "<Select " src/components/settings/model-settings-form.tsx | grep -qx 1 && grep -c "optionsForSlot(primary, fallbacks, -1" src/components/settings/model-settings-form.tsx | grep -qx 1 && grep -c "unionServableModels.find((m) => m.id === fb)?.providerID" src/components/settings/model-settings-form.tsx | grep -qx 1 && grep -c "Saved chain:" src/components/settings/model-settings-form.tsx | grep -qx 1 && npm test >/dev/null 2>&1 && npm run build >/dev/null 2>&1</automated>
  </verify>
  <done>All model slots are Command Comboboxes with the full row anatomy; the saved-chain recap shows provider badges; submit contract + ERROR_COPY + D-13 untouched; npm test and npm run build both green (phase gate).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client draft state → saveSettingsAction | The form's staged values cross the Server Action boundary; the client-side gates are UX, never security |
| savedChain prop → recap/badge rendering | Server-resolved provider identity rendered as text; no untrusted content constructed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-12 | Tampering | submit bypass of the client staleness gate | mitigate | Server-side backstop is unchanged and authoritative: requireStaffAccess FIRST → zod → union servable check → dedupe backstop → atomic upsert (saveSettingsAction, immutable order); the client gate is UX only (existing architecture, RESEARCH §Security Domain) |
| T-21-13 | Tampering | provider-switch reset onto a preserved fallback id (Pitfall 6) | mitigate | Accept the server `duplicate_model` backstop (ERROR_COPY already explains "Each model can only be used once."); fallbacks are NEVER cleared (D-21-02); the primary-direction dedupe (optionsForSlot slotIndex -1) prevents the common case client-side, locked by the 21-02 test suite |
| T-21-14 | Spoofing | provider identity rendered from untrusted input | mitigate | Provider names come only from `providerName()` over the `ModelProviderId` union (pure, locked); recap provider identity resolves via `unionServableModels` (server-validated); ids render as React text children (escaped) — no HTML/URL construction |
| T-21-15 | Information Disclosure | stale saved-chain ids leaking raw catalog data | mitigate | Stale labels resolve via `savedChain` prop names (server-computed) with raw-id fallback; the union props are the trimmed six-field shape only (T-17-09) |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — cmdk shipped in plan 21-01 (audited `[OK]`) |

## Package Legitimacy

No installs in this plan. All imports are existing project modules + the plan 21-01/02/04 outputs.
</threat_model>

<verification>
1. `grep -c "<ModelPicker" src/components/settings/model-settings-form.tsx` → 2 (primary + the fallback map JSX sites; the import line excluded — the provider selector stays a Select per D-21-06)
2. `grep -c "computeStaleIds(\[primary, ...fallbacks\], unionIds)" src/components/settings/model-settings-form.tsx` → 1 (union-wide gate, D-21-14)
3. `npx vitest run src/components/settings/model-picker-logic.test.ts` → all green
4. `npm test` → full suite green (catalog/settings action tests untouched and passing); exit code asserted
5. `npm run build` → 0 errors (the phase gate — vendored imports, RSC boundary, props wiring); exit code asserted
</verification>

<success_criteria>
- SET-01/02: always-valued AI Provider selector above the Primary label; provider switch refreshes the primary picker's options from `servableByProvider[provider]`
- SET-03: keep-if-valid → reset-to-provider-default with the non-blocking hint; fallbacks preserved verbatim; draft-only (D-07); Save gated (D-21-04)
- SET-04/05: union fallback pickers grouped by provider with badges; saved-chain recap with per-model provider badges
- SET-06/07: Command type-to-filter search on every model slot; `~latest`/`:free` suffix labels
- SET-08: union-wide staleness gate; catalog freshness caption retained; amber high-cost captions (o1-pro $150 trips ≥50)
- Server action unchanged; ERROR_COPY + D-13 preservation intact; `npm test` + `npm run build` green
</success_criteria>

<output>
Create `.planning/phases/21-settings-ui/21-05-SUMMARY.md` when done
</output>
