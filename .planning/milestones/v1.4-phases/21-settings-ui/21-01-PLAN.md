---
phase: 21-settings-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/ui/command.tsx
  - src/components/ui/popover.tsx
  - src/components/ui/input-group.tsx
  - src/components/ui/textarea.tsx
autonomous: true
requirements: [SET-06]
must_haves:
  truths:
    - "The cmdk-based Command primitive and its Popover shell exist, compile, and are importable — the model-picker wrapper (plan 21-04) can build on them with zero dangling registry paths"
    - "cmdk is recorded in the npm lockfile (the de-facto package manager) with no yarn.lock introduced"
    - "The existing select.tsx and its 4 non-settings consumers are untouched by the vendoring"
  artifacts:
    - path: "src/components/ui/command.tsx"
      provides: "Vendored shadcn Command (cmdk wrapper): Command/CommandInput/CommandList/CommandGroup/CommandItem/CommandEmpty"
      contains: "cmdk"
    - path: "src/components/ui/popover.tsx"
      provides: "Vendored shadcn Popover (radix-ui) for the Combobox trigger/overlay"
      contains: "Popover"
    - path: "package.json"
      provides: "New npm dependency cmdk@^1.1.1"
      contains: "\"cmdk\""
  key_links:
    - from: "src/components/ui/command.tsx"
      to: "cmdk (node_modules)"
      via: "import from 'cmdk'"
      pattern: "from \"cmdk\""
    - from: "package.json"
      to: "package-lock.json"
      via: "npm-managed lockfile entry"
      pattern: "cmdk"
---

<objective>
Vendor the shadcn Command component set (cmdk-based) — `command.tsx`, `popover.tsx`, and the auto-registry-dependencies `input-group.tsx`/`textarea.tsx` — via the official shadcn CLI, installing the phase's single new npm dependency `cmdk@^1.1.1` (D-21-05, SET-06 infra). The vendored primitives are the foundation the model-picker Combobox wrapper (plan 21-04) imports.

Purpose: This phase replaces the shadcn Select on every model slot with a Command-based Combobox (D-21-06). The Command primitive is NEW — cmdk is not installed today (verified 2026-08-03: no `node_modules/cmdk`, no `package.json` entry). The CLI-generated files must be verified immediately for the two known registry-contract traps: dangling monorepo-internal imports (`@/app/(create)…`, `@/registry/…`) and unresolved icon placeholders. Do NOT install `@shadcn/combobox` — the v4 registry item is Base UI-based and contradicts the locked cmdk decision (D-21-05 / RESEARCH Anti-Patterns / UI-SPEC §Registry Safety).
Output: The vendored `src/components/ui/{command,popover,input-group,textarea}.tsx` files, `cmdk` in package.json + package-lock.json, and a green `tsc` gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md (v1.4 locked decisions; D-21-05 vendoring path)
@.planning/phases/21-settings-ui/21-CONTEXT.md (D-21-05/06; Claude's discretion on vendoring structure)
@.planning/phases/21-settings-ui/21-RESEARCH.md (§Standard Stack — exact CLI command l.120-126, vendored-file facts l.433, Pitfall 2 dangling-import l.391-395, Package Legitimacy Audit l.130-139 — cmdk [OK])
@.planning/phases/21-settings-ui/21-UI-SPEC.md (§Component Inventory l.35-48 vendoring decision + post-add verification, §Registry Safety l.190-197)
@.planning/phases/21-settings-ui/21-PATTERNS.md (§command.tsx l.27-49 vendoring contract — NOT hand-written)
components.json (style radix-nova, baseColor neutral, iconLibrary lucide, `registries: {}` — verified 2026-08-03)
package.json (scripts: test=vitest run, build=next build; shadcn CLI installed as a dependency)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor command/popover via shadcn CLI + cmdk install + post-add verification</name>
  <files>package.json, package-lock.json, src/components/ui/command.tsx, src/components/ui/popover.tsx, src/components/ui/input-group.tsx, src/components/ui/textarea.tsx</files>
  <read_first>
    components.json (registry contract — radix-nova/neutral/lucide, `registries: {}`, aliases `@/components/ui`)
    package.json (scripts; no cmdk today; shadcn CLI already a dependency)
    src/components/ui/select.tsx (the vendored-primitive house shape to verify against: "use client", data-slot attributes, `@/lib/utils` cn import, named exports, double quotes in vendored files)
    .planning/phases/21-settings-ui/21-RESEARCH.md l.120-126 (exact CLI invocation) + l.391-395 (Pitfall 2) + l.130-139 (Package Legitimacy Audit — cmdk [OK])
    .planning/phases/21-settings-ui/21-PATTERNS.md §command.tsx (l.27-49)
  </read_first>
  <action>
    Run the vendoring command from the project root: `npx shadcn@latest add command popover -y` (`-y` skips interactive prompts; the installed CLI is a project dependency). This vendors `src/components/ui/command.tsx` (cmdk wrapper), `src/components/ui/popover.tsx` (radix-ui), and the auto-registry-dependencies `input-group.tsx` + `textarea.tsx`, and installs `cmdk@^1.1.1` into package.json. Do NOT run `npx shadcn add combobox` — the v4 `combobox` registry item is built on `@base-ui/react`, contradicting D-21-05 (UI-SPEC §Registry Safety); ignore any CLI suggestion to that effect.

    Lockfile discipline (RESEARCH §Project Constraints): package.json declares `packageManager: yarn` but package-lock.json is the de-facto lockfile. If the CLI creates a `yarn.lock`, remove it immediately and run `npm install cmdk@^1.1.1` so package-lock.json records the dependency. Never commit a yarn.lock.

    Post-add verification (MANDATORY — UI-SPEC §Vendoring / RESEARCH Pitfall 2), before any further work:
    - grep `src/components/ui/command.tsx` for `@/app/(create` and `@/registry` — BOTH must return zero occurrences (the CLI must have rewritten the shadcn monorepo-internal paths; a survivor breaks `next build` with module-not-found).
    - The registry's `IconPlaceholder` must have resolved to lucide `Search`/`Check` imports (grep `lucide-react` in command.tsx — at least one import line must exist).
    - The vendored `CommandList` must carry `max-h-72` and the `CommandGroup` heading must be `text-xs font-medium text-muted-foreground` (the registry contract — UI-SPEC §Spacing/§Color inherit these).
    - `src/components/ui/select.tsx` must be untouched: `git status --porcelain src/components/ui/select.tsx` empty, and none of its 4 non-settings consumers modified (src/components/companies/company-filters.tsx, src/components/import/column-mapping-step.tsx, src/components/personas/persona-filters.tsx, src/components/reviews/reject-dialog.tsx).
    - If the CLI behavior diverges from the above (dangling imports, wrong primitive, prompts for a different registry), consult the shadcn skill (`/shadcn`) for the repair path rather than hand-fixing the registry contract — the vendored files must stay byte-close to the official registry item.
    - Do NOT re-quote, reformat, or hand-edit the vendored files (house rule: vendored files keep their double-quote style; CLAUDE.md §Conventions).
  </action>
  <acceptance_criteria>
    - `src/components/ui/command.tsx` exists and contains `from "cmdk"` (the cmdk primitive import)
    - `grep -c '@/app/(create\|@/registry' src/components/ui/command.tsx` returns 0
    - `src/components/ui/popover.tsx`, `src/components/ui/input-group.tsx`, `src/components/ui/textarea.tsx` all exist
    - package.json contains `"cmdk": "^1.1.1"` in dependencies; package-lock.json contains a cmdk entry; NO yarn.lock at the repo root
    - `git status --porcelain src/components/ui/select.tsx` is empty
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>ls src/components/ui/command.tsx src/components/ui/popover.tsx src/components/ui/input-group.tsx src/components/ui/textarea.tsx >/dev/null && grep -c '@/app/(create\|@/registry' src/components/ui/command.tsx | grep -qx 0 && grep -v '^#' package.json | grep -c '"cmdk"' | grep -qx 1 && test ! -f yarn.lock && test -z "$(git status --porcelain src/components/ui/select.tsx)" && grep -c "lucide-react" src/components/ui/command.tsx | grep -qx 1 && npx tsc --noEmit</automated>
  </verify>
  <done>command/popover/input-group/textarea vendored from the official shadcn registry with zero dangling imports; cmdk@^1.1.1 recorded in package.json + package-lock.json via npm; select.tsx and its 4 other consumers untouched; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadcn registry + npm registry → node_modules | Untrusted third-party source code (vendored UI primitives + one new package) crosses into the build; `registries: {}` limits component sources to the official shadcn registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-SC | Tampering | npm install of cmdk | mitigate | Package Legitimacy Audit passed (RESEARCH l.130-139): cmdk 1.1.1 `[OK]` via slopcheck, MIT, official repo `pacocoursey/cmdk`, no postinstall script, ~6yr history, de-facto standard primitive (shadcn/Vercel/Supabase). Registry `view` passed 2026-08-03 (no fetch/process.env/eval/dynamic imports/obfuscation — UI-SPEC §Registry Safety). components.json `registries: {}` — no third-party registries. `[OK]` disposition requires NO blocking human checkpoint. Post-install verification in Task 1 (grep + tsc) is the enforcement. |
| T-21-01 | Tampering | vendored command.tsx dangling imports | mitigate | The post-add grep (`@/app/(create` / `@/registry` → 0) plus the `npx tsc --noEmit` gate fail the task before the wrapper (21-04) can consume a broken primitive; `next build` in plan 21-05 re-asserts |
| T-21-02 | Tampering | accidental overwrite of select.tsx or its consumers | mitigate | Explicit `git status --porcelain src/components/ui/select.tsx` emptiness check + acceptance criterion listing the 4 untouched consumers |

## Package Legitimacy

Only new npm package: `cmdk@^1.1.1` — `[OK]` per the RESEARCH Package Legitimacy Audit table (2026-08-03, slopcheck + npm registry + Context7 /dip/cmdk + official shadcn registry item dependency list). All vendored components come from the official shadcn registry (`registries: {}`). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — no legitimacy checkpoint required.
</threat_model>

<verification>
1. `grep -c '@/app/(create\|@/registry' src/components/ui/command.tsx` → 0 (no dangling monorepo paths — Pitfall 2)
2. `grep -c "lucide-react" src/components/ui/command.tsx` → ≥1 (IconPlaceholder resolved to lucide icons)
3. `npx tsc --noEmit` → 0 errors (vendored files compile against the project)
4. package-lock.json updated via npm; no yarn.lock; select.tsx + 4 consumers untouched
</verification>

<success_criteria>
- `src/components/ui/{command,popover,input-group,textarea}.tsx` exist, sourced from the official shadcn registry, verified free of dangling imports (SET-06 infrastructure ready for the wrapper in plan 21-04)
- `cmdk@^1.1.1` is the only package.json change; recorded in package-lock.json via npm with no yarn.lock
- `select.tsx` and its 4 non-settings consumers are untouched (D-21-06: Select stays for the provider selector + other consumers)
</success_criteria>

<output>
Create `.planning/phases/21-settings-ui/21-01-SUMMARY.md` when done
</output>
