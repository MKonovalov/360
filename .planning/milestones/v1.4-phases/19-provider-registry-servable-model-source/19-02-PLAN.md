---
phase: 19-provider-registry-servable-model-source
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/env.ts
  - .env.example
  - scripts/refresh-model-catalog.ts
  - src/lib/models/catalog.json
autonomous: true
requirements: [REG-02, REG-03]
user_setup:
  - service: vercel
    why: "Declare OPENROUTER_API_KEY in the Vercel project env so Phase 20's chain-aware gate can read it in production"
    env_vars:
      - name: OPENROUTER_API_KEY
        source: "OpenRouter dashboard (openrouter.ai → Keys) → Vercel project Settings → Environment Variables"
    notes: "NON-BLOCKING for Phase 19 — the key is declaration-only (D-11); nothing consumes it until Phase 20's run-entry gate. Set it as a server-only (non-Preview/Development-exposed) variable; never prefix PUBLIC_."
must_haves:
  truths:
    - "OPENROUTER_API_KEY is declared optional, server-only in env.ts, mirroring the D-15 ANTHROPIC_API_KEY degrade-gracefully pattern"
    - "OPENROUTER_API_KEY appears in .env.example with a placeholder value and Get-from source"
    - "The committed catalog snapshot carries a per-model structuredOutputs capability flag derived from the live OpenRouter API"
    - "The D-07 OpenRouter default slug anthropic/claude-sonnet-4.6 is roster-verified present in the regenerated snapshot"
  artifacts:
    - path: "src/lib/env.ts"
      provides: "Optional server-only OPENROUTER_API_KEY declaration"
      contains: "OPENROUTER_API_KEY: z.string().optional()"
    - path: ".env.example"
      provides: "Placeholder + Get-from line for the OpenRouter key"
      contains: "OPENROUTER_API_KEY="
    - path: "scripts/refresh-model-catalog.ts"
      provides: "D-08 live-API fetch + exact-id join populating structuredOutputs on openrouter rows"
      contains: "structuredOutputs"
    - path: "src/lib/models/catalog.json"
      provides: "Regenerated snapshot where every model row has a structuredOutputs boolean"
      contains: "structuredOutputs"
  key_links:
    - from: "scripts/refresh-model-catalog.ts"
      to: "https://openrouter.ai/api/v1/models"
      via: "Node 22 global fetch (public GET, no key) + exact-id join against openrouter rows"
      pattern: "openrouter.ai/api/v1/models"
---

<objective>
Deliver REG-02 (the `OPENROUTER_API_KEY` declaration, install half already satisfied — `@openrouter/ai-sdk-provider@^3.0.0` is installed + committed) and the D-08 structured-output capability field: extend `scripts/refresh-model-catalog.ts` to fetch the live OpenRouter `/api/v1/models` roster (public, no key), join by exact id, and write a per-model `structuredOutputs: boolean` onto every catalog row — then regenerate the committed snapshot ONCE (the regen lands in Phase 19 per CONTEXT's file list; D-08 "regenerate the snapshot once").

Purpose: The snapshot capability flag is the data that makes `modelFactory`'s per-model `strict:false` pass (D-08) purely data-driven — never a code-side map, never a global `strict:false`.
Output: The env declaration + `.env.example` entry, the extended refresh script, and the regenerated snapshot carrying `structuredOutputs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/19-provider-registry-servable-model-source/19-CONTEXT.md
@.planning/phases/19-provider-registry-servable-model-source/19-RESEARCH.md
@.planning/phases/19-provider-registry-servable-model-source/19-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: env.ts + .env.example — OPENROUTER_API_KEY optional server-only declaration (REG-02, D-11)</name>
  <files>src/lib/env.ts, .env.example</files>
  <read_first>
    src/lib/env.ts (current — 42 lines; the ANTHROPIC_API_KEY pattern at l.30-35)
    .env.example (Phase 9 key block l.30-35)
    .planning/phases/19-provider-registry-servable-model-source/19-PATTERNS.md §env.ts + .env.example (l.239-263)
    .planning/research/PITFALLS.md Pitfall 11 (key discipline — server-only, never NEXT_PUBLIC_)
  </read_first>
  <action>
    A) src/lib/env.ts — insert immediately AFTER the `ANTHROPIC_API_KEY: z.string().optional(),` line (l.35), inside the same Phase 9 comment block context:
    - `OPENROUTER_API_KEY: z.string().optional(),`
    - Add a why-comment (2-4 lines, CONVENTIONS.md style) directly above it: Phase 19 (REG-02) — OpenRouter key, optional/degrade-gracefully like the Anthropic key; an unset key must not crash at import time; the chain-aware env gate lands in Phase 20 (D-11); non-PUBLIC_ prefix = server-only; never logged, never sent to client; auto-loaded by createOpenRouter (no explicit apiKey pass). Mirror the ANTHROPIC_API_KEY comment tone byte-for-byte in structure.
    - Do NOT add any other env key, do NOT touch the fail-fast block (l.7-9), do NOT add a `NEXT_PUBLIC_OPENROUTER_*` variant.

    B) .env.example — extend the Phase 9 comment block (l.30-31 area):
    - In the `#   Anthropic: console.anthropic.com → API Keys` get-from list, add a line `#   OpenRouter: openrouter.ai → Keys` (matching the existing two-space-indented comment style).
    - Immediately after `ANTHROPIC_API_KEY=sk-ant-xxxxxxxx` add `OPENROUTER_API_KEY=sk-or-xxxxxxxx` (placeholder value, no real key — matches the repo convention of fake-shaped placeholders).
  </action>
  <acceptance_criteria>
    - `src/lib/env.ts` contains `OPENROUTER_API_KEY: z.string().optional(),` immediately after the ANTHROPIC_API_KEY line
    - `src/lib/env.ts` has NO `NEXT_PUBLIC_OPENROUTER` and NO `z.string().min(1)` for the OpenRouter key (optional is the whole point — D-15 degrade-gracefully)
    - `.env.example` contains `OPENROUTER_API_KEY=sk-or-xxxxxxxx` and the get-from line `#   OpenRouter: openrouter.ai → Keys`
    - `grep -rn "OPENROUTER" src/app src/components` → 0 matches (no client-component surface — Phase 22 VER-04 pre-clean)
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/lib/env.ts | grep -c "OPENROUTER_API_KEY: z.string().optional()" | grep -qx 1 && grep -c "OPENROUTER_API_KEY=sk-or-xxxxxxxx" .env.example | grep -qx 1 && grep -rn "NEXT_PUBLIC_OPENROUTER" src/ | wc -l | grep -qx 0</automated>
  </verify>
  <done>env.ts declares the optional server-only key mirroring ANTHROPIC_API_KEY; .env.example has the placeholder + source line; zero client-side OPENROUTER surface.</done>
</task>

<task type="auto">
  <name>Task 2: refresh-model-catalog.ts — D-08 live-API fetch + exact-id join + structuredOutputs field</name>
  <files>scripts/refresh-model-catalog.ts</files>
  <read_first>
    scripts/refresh-model-catalog.ts (current — 142 lines; trimRecord l.85-108, main() l.110-135)
    .planning/phases/19-provider-registry-servable-model-source/19-RESEARCH.md §D-08 capability-field sourcing (l.44-53) and Execution-Time Caveats 3-4 (l.79-80)
    .planning/phases/19-provider-registry-servable-model-source/19-PATTERNS.md §scripts/refresh-model-catalog.ts (l.265-289)
    .planning/research/PITFALLS.md Pitfall D (per-model strict flag) and the v1.4 addendum Pitfall A (~ ids verbatim)
  </read_first>
  <action>
    Extend scripts/refresh-model-catalog.ts with the D-08 capability join. The script must STILL shell the opencode CLI for the base catalog (its existing behavior) — the live OpenRouter fetch is an ADDITION that annotates openrouter rows. Do not change `resolveOpencodeBin`, `parseModels`, `braceDelta`, the snapshot `{ generatedAt, models }` shape, the `writeFileSync` target, or the `models:fetch` script name in package.json.

    Add:
    - `async function fetchOpenRouterStructuredOutputs(): Promise<Map<string, boolean>>` — `await fetch('https://openrouter.ai/api/v1/models')` (Node 22 global fetch, no import needed, public endpoint verified HTTP 200 with 337 models); on non-ok response or network failure, THROW a clear Error (`Failed to fetch OpenRouter model roster from https://openrouter.ai/api/v1/models — snapshot NOT regenerated`) so main() aborts WITHOUT writing (the committed snapshot stays usable — matches the script's fail-with-clear-message posture at l.17-25). On success, build `Map<string, boolean>` keyed by exact `id` → `(row.supported_parameters ?? []).includes('structured_outputs')` (research: the live API exposes `supported_parameters` on every row; 262/337 include `structured_outputs`).
    - `function familyFallbackStructuredOutputs(family: string): boolean` — the D-09 fallback for openrouter rows the live join misses (research verified 100% join coverage, so this is effectively unreachable): return `false` when the lowercased family contains any of `qwen|llama|deepseek|mistral|gemma|glm`, else `true`. Add a why-comment: derive from `supported_parameters`, NEVER from family name alone (research: llama-3.3-70b and deepseek-v4-flash DO support structured_outputs; qwen3-235b does NOT — family misclassifies).
    - Change `trimRecord` signature to `function trimRecord(m: Record<string, unknown>, structuredOutputs: boolean)` and add `structuredOutputs,` to the returned object (keep the total/deterministic shape — every row gets the field).
    - In `main()`: after parsing `models` (l.126), `const live = await fetchOpenRouterStructuredOutputs();` then map: `const models = parsed.map((m) => trimRecord(m, m.providerID === 'openrouter' ? (live.get(m.id as string) ?? familyFallbackStructuredOutputs(m.family as string)) : true));` — non-openrouter rows get `true` (inert: modelFactory only consults the flag for openrouter dispatch; Anthropic/opencode rows are strict-capable by default). Keep the console.log line reporting the count.
    - Add a comment near the fetch noting the scripts/ placement rationale already documented at l.6-9 (child_process + now fetch stays OUT of src/ — the Phase 18 gate greps zero exec|spawn|child_process in src/).
  </action>
  <acceptance_criteria>
    - `scripts/refresh-model-catalog.ts` contains `https://openrouter.ai/api/v1/models` (the fetch URL)
    - `scripts/refresh-model-catalog.ts` contains `structuredOutputs` in the trimRecord return AND a `familyFallbackStructuredOutputs` function containing `qwen|llama|deepseek|mistral|gemma|glm`
    - The fetch failure path THROWS (aborts) rather than writing a flag-less snapshot
    - Non-openrouter rows are written with `structuredOutputs: true` (total deterministic shape — every row has the boolean)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "openrouter.ai/api/v1/models" scripts/refresh-model-catalog.ts | grep -qx 1 && grep -c "structuredOutputs" scripts/refresh-model-catalog.ts | grep -qx 1 && grep -c "familyFallbackStructuredOutputs" scripts/refresh-model-catalog.ts | grep -qx 1 && npx tsx --version >/dev/null 2>&1 && echo "tsx available"</automated>
  </verify>
  <done>Script extensions in place: live fetch + exact-id join + family fallback + total snapshot shape with structuredOutputs on every row.</done>
</task>

<task type="auto">
  <name>Task 3: regenerate the committed snapshot once + roster-verify D-07 + capability-field checks</name>
  <files>src/lib/models/catalog.json</files>
  <read_first>
    src/lib/models/catalog.json (first ~30 rows — current shape; it will be overwritten by the regen)
    scripts/refresh-model-catalog.ts (Task 2 output)
    .planning/phases/19-provider-registry-servable-model-source/19-RESEARCH.md §D-07 roster verification (l.81) and §Snapshot geometry (l.14, 52)
  </read_first>
  <action>
    Run `npm run models:fetch` (package.json script → `tsx scripts/refresh-model-catalog.ts`). This shells the local opencode CLI (`opencode models --verbose`) AND hits the live OpenRouter API; both must succeed (the script aborts without writing if the fetch fails — committed snapshot stays usable if it does; report the failure and pause rather than hand-patching the JSON).

    AFTER the regen, verify with a node one-liner (exact assertions):
    - every model row has a boolean `structuredOutputs` field (no row missing it — total shape)
    - the openrouter row count is unchanged at 336 (the regen must not drop rows; if the opencode CLI drifted and the count differs, note the delta in the SUMMARY but the 336-openrouter invariant must hold — if it does not, the regen is wrong, revert and investigate)
    - `anthropic/claude-sonnet-4.6` (D-07 OpenRouter default primary) is present with `structuredOutputs === true` (roster-verified + strict-capable — research l.81; this is the standing D-02 doctrine check)
    - `qwen/qwen3-235b-a22b` is present with `structuredOutputs === false` (D-08 live-derivation proof — research verified qwen does NOT advertise structured_outputs)
    - the 11 `~`-prefixed rows and the 14 `:free` rows are still present (D-02: INCLUDED, labels land in Phase 21)
    - `claude-sonnet-4-6` (anthropic) present with `structuredOutputs === true` (inert for anthropic dispatch but shape-complete)
    - `git diff --stat src/lib/models/catalog.json` shows ONLY the added field (plus possibly a changed `generatedAt`) — no id/providerID/status mutations
  </action>
  <acceptance_criteria>
    - `src/lib/models/catalog.json` has `structuredOutputs` on 100% of model rows (assert `models.every(m => typeof m.structuredOutputs === 'boolean')` via node)
    - `src/lib/models/catalog.json` still has 336 `providerID === 'openrouter'` rows
    - The D-07 slug `anthropic/claude-sonnet-4.6` exists with `structuredOutputs: true` in the regenerated snapshot
    - `qwen/qwen3-235b-a22b` exists with `structuredOutputs: false`
    - 11 `~` rows and 14 `:free` rows remain present (included per D-02)
    - `git diff` of catalog.json shows no id/providerID/status drift — only `structuredOutputs` additions and the `generatedAt` stamp
  </acceptance_criteria>
  <verify>
    <automated>npm run models:fetch && node -e "const j=require('./src/lib/models/catalog.json'); const or=j.models.filter(m=>m.providerID==='openrouter'); if(!j.models.every(m=>typeof m.structuredOutputs==='boolean')) throw new Error('field missing'); if(or.length!==336) throw new Error('openrouter count '+or.length); const d7=or.find(m=>m.id==='anthropic/claude-sonnet-4.6'); if(!d7||d7.structuredOutputs!==true) throw new Error('D-07 slug not verified'); const qw=or.find(m=>m.id==='qwen/qwen3-235b-a22b'); if(!qw||qw.structuredOutputs!==false) throw new Error('qwen flag wrong'); const tilde=or.filter(m=>m.id.startsWith('~')).length; const free=or.filter(m=>m.id.includes(':free')).length; if(tilde!==11||free!==14) throw new Error('labels rows '+tilde+'/'+free); console.log('OK 336 OR rows, D-07 verified, qwen flagged, '+tilde+' tilde, '+free+' free');"</automated>
  </verify>
  <done>Snapshot regenerated once with the capability field; D-07 slug roster-verified; 336 OR rows + 11 ~ + 14 :free intact; diff is field-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → server runtime | OPENROUTER_API_KEY secret lifecycle — declaration-only in Phase 19 (D-11); consumption starts Phase 20 |
| dev machine → OpenRouter API | The refresh script's public GET — dev-time only, never on Vercel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | Information Disclosure | `OPENROUTER_API_KEY` leakage | mitigate | `env.ts` declares it `z.string().optional()` with NO `PUBLIC_` prefix (server-only, byte-mirroring `ANTHROPIC_API_KEY`); `.env.example` holds a fake placeholder (`sk-or-xxxxxxxx`), never a real key; grep asserts zero `OPENROUTER` surface in `src/app` + `src/components` (client components) — Phase 22 VER-04 pre-clean |
| T-19-06 | Tampering | snapshot integrity during regen | mitigate | The refresh script THROWS (aborts, no write) if the live fetch fails; the post-regen verification asserts 336 openrouter rows, no id/providerID/status drift (`git diff`), and 100% `structuredOutputs` coverage — a partial or flag-less snapshot cannot be committed silently |
| T-19-07 | Spoofing | wrong rows flagged via family heuristics | mitigate | `structuredOutputs` derives from live `supported_parameters.includes('structured_outputs')` by EXACT id join; the family fallback (D-09) exists only for unknown rows and is effectively unreachable (100% join coverage verified) — family alone misclassifies (llama-3.3-70b/deepseek support it, qwen3-235b doesn't) |
| T-19-08 | Tampering | scripts/ network surface | accept | New `fetch` in the dev-time refresh script — public endpoint, no credentials, runs only on the dev machine (`scripts/` placement keeps the Phase 18 zero-`exec\|spawn\|child_process`-in-`src/` gate green); never deployed to Vercel |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan; pin stays `^3.0.0` (research-verified). Do not run `npm audit fix --force` (12 pre-existing findings, research caveat 8). |

## Package Legitimacy

`@openrouter/ai-sdk-provider@3.0.0` installed + dist-tag-verified in research (`latest: 3.0.0`, not `ai-sdk-v4: 0.7.5` / `6.0.0-alpha`). No install task exists in this phase — T-19-SC is verify-only (grep package.json keeps `^3.0.0`).
</threat_model>

<verification>
1. `npx vitest run src/lib/models/catalog.test.ts` still passes against the REGENERATED snapshot (the snapshot-level canaries in plan 19-01 must hold — anthropic `['claude-sonnet-4-6']`, openrouter >= 300 rows, all-slash openrouter ids, zero overlap)
2. `npx tsc --noEmit` → 0 errors (env.ts addition is type-safe; CatalogModel now carries `structuredOutputs`)
3. `grep -rn "NEXT_PUBLIC_OPENROUTER" .` → 0 matches; `grep -rn "OPENROUTER" src/app src/components` → 0 matches (client-surface clean)
4. catalog.json diff is field-only (`structuredOutputs` + `generatedAt`)
</verification>

<success_criteria>
- `OPENROUTER_API_KEY: z.string().optional()` sits in env.ts next to `ANTHROPIC_API_KEY`; `.env.example` has the placeholder + get-from line (REG-02)
- `refresh-model-catalog.ts` derives `structuredOutputs` from the live API by exact id, with the D-09 family fallback for unknown rows
- The snapshot is regenerated ONCE: every row carries `structuredOutputs: boolean`; 336 openrouter rows incl. the 11 `~` and 14 `:free` (REG-03); `anthropic/claude-sonnet-4.6` roster-verified with `structuredOutputs: true` (D-07)
- Vercel env declaration for `OPENROUTER_API_KEY` flagged to the operator via `user_setup` (non-blocking — nothing consumes it in Phase 19)
</success_criteria>

<output>
Create `.planning/phases/19-provider-registry-servable-model-source/19-02-SUMMARY.md` when done
</output>
