---
phase: 19-provider-registry-servable-model-source
plan: 04
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/lib/agents/modelConfig.ts
  - src/lib/agents/modelConfig.test.ts
autonomous: true
requirements: [REG-05]
must_haves:
  truths:
    - "resolveModelChain's default servable set is the union of both providers' servable ids, not the anthropic-only allowlist"
    - "Cross-provider chains (primary + fallback from different providers) resolve and pass through intact"
    - "Ids outside the union drop to the REG-05 default chain (FAST_MODEL_ID) — provider-agnostic dedupe and cap-2 untouched"
  artifacts:
    - path: "src/lib/agents/modelConfig.ts"
      provides: "Union-filtered chain resolution (D-06) — default servableIds = getUnionServableIds(catalogJson)"
      contains: "getUnionServableIds(catalogJson)"
    - path: "src/lib/agents/modelConfig.test.ts"
      provides: "Cross-provider chain cases in the explicit-fixture convention"
      contains: "anthropic/claude-sonnet-latest"
  key_links:
    - from: "src/lib/agents/modelConfig.ts"
      to: "src/lib/models/catalog.ts"
      via: "getUnionServableIds(catalogJson) default argument"
      pattern: "getUnionServableIds\\(catalogJson\\)"
---

<objective>
Make chain resolution provider-aware with the smallest possible change (D-06): `resolveModelChain`'s allowlist default widens from `ANTHROPIC_ALLOWLIST` to the union servable set (`getUnionServableIds(catalogJson)`). Everything else in the function — the D-08 stable-unique dedupe, the D-10 cap-after-dedupe at primary + 1 fallback, and the REG-05 `[FAST_MODEL_ID]` default — is provider-agnostic and stays byte-identical.

Purpose: This is what lets a saved OpenRouter primary (or a mixed chain) survive chain resolution instead of being silently dropped by the anthropic-only allowlist filter (PITFALLS 7). The run path consumes it in plan 19-05.
Output: Union-filtered chain resolution + cross-provider Vitest cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md Pattern 2 (l.147-166)
@.planning/research/PITFALLS.md Pitfall 7 (l.166-188 — the anthropic-only filter silently drops every OpenRouter id)
@.planning/phases/19-provider-registry-servable-model-source/19-CONTEXT.md (D-06)
@.planning/phases/19-provider-registry-servable-model-source/19-PATTERNS.md §modelConfig.ts (l.152-168) and §modelConfig.test.ts (l.320-338)
src/lib/models/catalog.ts (getUnionServableIds from plan 19-01)
</context>

<tasks>

<task type="auto">
  <name>Task 1: modelConfig.ts — resolveModelChain default widens to the union servable set (D-06)</name>
  <files>src/lib/agents/modelConfig.ts</files>
  <read_first>
    src/lib/agents/modelConfig.ts (current — 82 lines; resolveModelChain l.71-82, imports l.1-9)
    src/lib/models/catalog.ts (getUnionServableIds — plan 19-01)
    .planning/phases/19-provider-registry-servable-model-source/19-PATTERNS.md §modelConfig.ts (l.152-168)
  </read_first>
  <action>
    Two-line change to src/lib/agents/modelConfig.ts — the ONLY behavioral change of this plan:

    A) Imports (l.9): change `import { ANTHROPIC_ALLOWLIST, FAST_MODEL_ID } from '@/lib/models/catalog';` to `import { ANTHROPIC_ALLOWLIST, FAST_MODEL_ID, getUnionServableIds } from '@/lib/models/catalog';` and ADD `import catalogJson from '@/lib/models/catalog.json';` after it. Constraint 11 stays intact: modelConfig imports only 'ai' + '@/lib/models/catalog' + the JSON data file — no db/env/runAgent. Add a one-line why-comment noting the JSON import mirrors catalog.ts itself (ARCHITECTURE.md Pattern 2 trade-off) and keeps the pure-module contract.

    B) Signature (l.73): change the default argument `allowlist: readonly string[] = ANTHROPIC_ALLOWLIST` to `servableIds: readonly string[] = getUnionServableIds(catalogJson)`. Rename the parameter to `servableIds` (the word "allowlist" no longer describes the union — the pattern map specifies this rename). Update the body references (l.77 `.filter((id) => allowlist.includes(id))` → `servableIds.includes(id)`) and the l.77 comment `// Pitfall 1/7: allowlist gate` → `// Pitfall 1/7: union servable gate`.

    C) Header comment (l.12-15): the phrase "D-08 dedupe → D-10 cap → allowlist filter → REG-05 default all live here" describes the OLD anthropic-only filter — change "allowlist filter" → "union servable gate" so the header matches the new default (and so the verify grep cannot spuriously match "allowlist" in the doc comment, which `grep -v '^#'` does not strip since `//` comments do not start with `#`).

    D) Do NOT touch: `classifyModelError`, `isFailoverEligible`, the `ModelErrorClass` type, `ModelSettingsRow`, the dedupe line (l.76), the cap line (l.79), or the REG-05 default return (l.81) — all provider-agnostic by design.
  </action>
  <acceptance_criteria>
    - `src/lib/agents/modelConfig.ts` contains `servableIds: readonly string[] = getUnionServableIds(catalogJson)` as the resolveModelChain default
    - `src/lib/agents/modelConfig.ts` imports `getUnionServableIds` from `'@/lib/models/catalog'` and `catalogJson` from `'@/lib/models/catalog.json'`
    - The parameter is named `servableIds` (no `allowlist` identifier remains in resolveModelChain)
    - `classifyModelError`/`isFailoverEligible` untouched (no diff beyond imports + signature + one comment)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "servableIds: readonly string\[\] = getUnionServableIds(catalogJson)" src/lib/agents/modelConfig.ts | grep -qx 1 && grep -vE '^\s*//' src/lib/agents/modelConfig.ts | grep -c "allowlist" | grep -qx 0 && npx tsc --noEmit 2>&1 | head -3; echo "tsc done"</automated>
  </verify>
  <done>resolveModelChain's default is the union servable set; no allowlist identifier remains in the function; project typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: modelConfig.test.ts — cross-provider chain cases (explicit-fixture convention)</name>
  <files>src/lib/agents/modelConfig.test.ts</files>
  <read_first>
    src/lib/agents/modelConfig.test.ts (current — 146 lines; resolveModelChain block l.112-146, explicit-fixture convention l.15-17)
    src/lib/agents/modelConfig.ts (Task 1 output)
    .planning/phases/19-provider-registry-servable-model-source/19-PATTERNS.md §modelConfig.test.ts (l.320-338 — the two target cases)
  </read_first>
  <action>
    ADD two cases to the existing `describe('resolveModelChain')` block (l.112-146) in src/lib/agents/modelConfig.test.ts, in the same explicit-fixture style as the existing 6 cases (they pass an explicit `servableIds` array, so they are unaffected by the default-argument change and must NOT be edited):

    1. Cross-provider acceptance (D-06): `expect(resolveModelChain({ primaryModel: 'claude-sonnet-4-6', fallbackModels: ['anthropic/claude-sonnet-latest'] }, ['claude-sonnet-4-6', 'anthropic/claude-sonnet-latest'])).toEqual(['claude-sonnet-4-6', 'anthropic/claude-sonnet-latest']);` — a chain spanning anthropic + openrouter passes through intact (the exact failure mode PITFALLS 7 describes: the old allowlist filter silently dropped every OpenRouter id).
    2. Union drop (REG-05): `expect(resolveModelChain({ primaryModel: 'not-in-union', fallbackModels: [] }, ['claude-sonnet-4-6'])).toEqual([FAST_MODEL_ID]);` — ids outside the union drop to the default chain (same semantics as the existing 'drops non-allowlisted ids' case, now framed as union membership).

    Also confirm (no edit needed — just verify) that `resolveModelChain(undefined)` (l.114) still returns `[FAST_MODEL_ID]` — no-settings input bypasses the union entirely.
  </action>
  <acceptance_criteria>
    - `src/lib/agents/modelConfig.test.ts` contains a case asserting a cross-provider chain `['claude-sonnet-4-6', 'anthropic/claude-sonnet-latest']` passes through intact
    - `src/lib/agents/modelConfig.test.ts` contains a case asserting `resolveModelChain({ primaryModel: 'not-in-union', fallbackModels: [] }, ['claude-sonnet-4-6'])` → `[FAST_MODEL_ID]`
    - The existing 6 resolveModelChain cases + the classifyModelError/isFailoverEligible cases are untouched and still pass
    - `npx vitest run src/lib/agents/modelConfig.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/agents/modelConfig.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Cross-provider chain passes, non-union id drops to the default; all existing cases still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved settings → chain resolution | Persisted id strings cross into the resolvable chain; membership decides what may reach the provider factory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | Spoofing | non-servable ids reaching the run path | mitigate | `resolveModelChain`'s default is now the union servable set — an id must be a member of a per-provider servable set to survive resolution; non-members drop to `[FAST_MODEL_ID]` (REG-05), never crash and never reach the factory as an arbitrary string |
| T-19-13 | Tampering | pure-module contract (constraint 11) | mitigate | modelConfig imports only `'ai'` + `'@/lib/models/catalog'` + the JSON data file — no db/env/runAgent and no provider SDK; the union default argument is a pure data read, keeping classifyModelError/isFailoverEligible testable with zero mocks |
| T-19-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan. |

## Package Legitimacy

No installs — pure TypeScript change over existing committed data (`catalog.json`) and the plan 19-01 registry functions.
</threat_model>

<verification>
1. `npx vitest run src/lib/agents/modelConfig.test.ts` → all cases pass (existing 6 + 2 new)
2. `npx tsc --noEmit` → 0 errors
3. `grep -rn "@ai-sdk/anthropic\|@openrouter/ai-sdk-provider" src/lib/agents/modelConfig.ts` → 0 matches (constraint 11 intact)
</verification>

<success_criteria>
- `resolveModelChain(settings)` now filters against the union servable set by default — an OpenRouter primary survives resolution (D-06, PITFALLS 7 closed)
- Cross-provider chains pass through intact; non-union ids drop to `[FAST_MODEL_ID]`; dedupe/cap-2/REG-05 default unchanged (provider-agnostic)
- modelConfig stays pure: no SDK, db, or env imports
</success_criteria>

<output>
Create `.planning/phases/19-provider-registry-servable-model-source/19-04-SUMMARY.md` when done
</output>
