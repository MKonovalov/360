---
phase: 20-cross-provider-run-path
plan: 04
type: execute
wave: 4
depends_on: [20-03]
files_modified:
  - src/app/api/companies/[id]/analyze/route.ts
autonomous: true
requirements: [FAL-02, FAL-04, FAL-05]
must_haves:
  truths:
    - "not_configured returns HTTP 400 naming the missing key (was 503) — staff know which provider to configure (FAL-04/D-20-01/09)"
    - "billing returns HTTP 402 with the 'provider credits exhausted' reason (FAL-02/D-20-09/10)"
    - "rate_limited returns HTTP 429 with the platform/upstream reason (D-20-09/10)"
    - "agent_run.model_used/model_chain record the actual provider id served — raw ids verbatim from the loop through createRun (FAL-05)"
  artifacts:
    - path: "src/app/api/companies/[id]/analyze/route.ts"
      provides: "Distinct HTTP statuses for the three NEW classes; existing 502 analysis_failed propagation untouched (D-20-11)"
      contains: "status: 402"
    - path: "src/app/api/companies/[id]/analyze/route.ts"
      provides: "FAL-05 audit persistence (modelUsed/modelChain into createRun)"
      contains: "modelUsed: result.modelUsed"
  key_links:
    - from: "src/app/api/companies/[id]/analyze/route.ts"
      to: "src/lib/agents/analyzeCompany.ts"
      via: "the if (!result.ok) switch maps AnalyzeResult.reason → HTTP status (billing 402, not_configured 400, rate_limited 429)"
      pattern: "case 'billing':"
    - from: "src/app/api/companies/[id]/analyze/route.ts"
      to: "src/lib/db/queries/runs.ts"
      via: "createRun({ modelUsed: result.modelUsed, modelChain: result.modelChain }) — the durable audit columns (D-14)"
      pattern: "modelChain: result.modelChain"
---

<objective>
Finish the route surface of Phase 20 (D-20-09/10/11): the analyze route maps the three NEWLY-distinct AnalyzeResult reasons to their own HTTP statuses — `not_configured` → 400 naming the missing key (was 503/D-15), `billing` → 402 with "provider credits exhausted", `rate_limited` → 429 with the platform/upstream reason — while `gate_failed` → 422, `company_not_found` → 404, `db_error` → 502, the default → 502, and the outer `analysis_failed` 502 catch-all stay untouched (minimal blast radius). The plan closes with the FAL-05 audit VERIFICATION gate: the model_used/model_chain wiring (loop → analyzeCompany → createRun) is proven provider-accurate by grep + the already-added verbatim test cases — Phase 19's plumbing already exists, so this is verification, not new code (research caveat 6).

Purpose: The route is the contract surface staff interact with — the UI branches on status + reason string, and the distinct statuses (400/402/429) are what make billing vs rate-limit vs misconfiguration distinguishable. FAL-05's audit columns are the durable record (D-14) that Phase 22's UAT asserts against; proving the wiring here closes the requirement without re-plumbing it.
Output: The extended status map + the proven audit wiring + a green full-suite/typecheck/build gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-cross-provider-run-path/20-CONTEXT.md (D-20-01/09/10/11)
@.planning/phases/20-cross-provider-run-path/20-PATTERNS.md §route.ts (l.244-277) and the No-Analog note (l.494-498 — no route-level test file exists; route behavior is verified via analyzeCompany.test.ts reason mapping + Phase 22 UAT)
@.planning/phases/20-cross-provider-run-path/20-RESEARCH.md (caveat 6 — audit columns need no new plumbing; caveat 7 — route status mapping)
src/app/api/companies/[id]/analyze/route.ts (current — 143 lines)
src/lib/agents/analyzeCompany.ts (plan 20-03 output — AnalyzeResult union with billing/missingKey/message)
</context>

<tasks>

<task type="auto">
  <name>Task 1: route.ts — D-20-09/10/11 status map (not_configured→400, billing→402, rate_limited→429)</name>
  <files>src/app/api/companies/[id]/analyze/route.ts</files>
  <read_first>
    src/app/api/companies/[id]/analyze/route.ts (current — 143 lines; the if (!result.ok) switch l.64-85, persist l.128-140)
    src/lib/agents/analyzeCompany.ts (plan 20-03 output — the union shape the switch narrows on)
    .planning/phases/20-cross-provider-run-path/20-PATTERNS.md §route.ts (l.244-277 — the exact case rewrites)
  </read_first>
  <action>
    Modify the `if (!result.ok) switch` in src/app/api/companies/[id]/analyze/route.ts (l.64-85) — EXACTLY three cases change, everything else in the file untouched (l.17 maxDuration, l.23-35 auth+id parse, l.52-62 startActiveObservation/502 catch, l.107-115 success shape, l.128-140 persist):

    A) `case 'not_configured':` (l.69-71) — replace the current 503 bare response with:
    - `return Response.json({ error: 'not_configured', message: result.missingKey ? \`${result.missingKey} not configured\` : undefined }, { status: 400 });`
    - Update the comment: D-20-01/09 — 400 (was 503, D-15) + names the missing key so staff know which provider to configure (Phase 21 surfaces the pickers; D-20-04 keeps the gate-only surface in Phase 20). The `undefined` message on the bare fast-gate path (FIRECRAWL, no missingKey) is intentional — JSON drops it.

    B) `case 'billing':` (NEW — the reason union from plan 20-03 now includes it) — add before `company_not_found`:
    - `return Response.json({ error: 'billing', message: result.message ?? 'provider credits exhausted' }, { status: 402 });`
    - Why-comment: FAL-02/D-20-10 — account-level credits exhausted; distinct 402 so the UI can branch on status + reason (never a generic 502).

    C) `case 'rate_limited':` (l.77-81) — replace the current 502 response with:
    - `return Response.json({ error: 'rate_limited', message: result.message }, { status: 429 });`
    - Update the comment: D-20-09 — distinct 429 (was the D-04 502 carve-out) + the platform-vs-upstream reason from the runAgent diagnostics helper (D-20-07).

    D) UNTOUCHED (D-20-11 minimal blast radius — grep-verify afterwards): `gate_failed` → 422 (l.66-68), `company_not_found` → 404 (l.72-73), `db_error` → 502 (l.74-76), the `default` → 502 (l.82-83), and the outer catch at l.61 (`analysis_failed` 502 with String(err)) — the existing 502 propagation for auth/input/output/config/connection stays byte-identical. Do NOT add statuses for any other class.
  </action>
  <acceptance_criteria>
    - `case 'not_configured':` returns `{ error: 'not_configured', message: ... , status: 400 }` referencing `result.missingKey`
    - A `case 'billing':` returns `{ error: 'billing', message: ..., status: 402 }`
    - `case 'rate_limited':` returns `{ error: 'rate_limited', message: result.message, status: 429 }`
    - `gate_failed` still 422; `db_error` and `default` still 502; the l.61 catch-all still `analysis_failed` 502 (D-20-11)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/app/api/companies/[id]/analyze/route.ts | grep -c "status: 400" | grep -qx 1 && grep -v '^#' src/app/api/companies/[id]/analyze/route.ts | grep -c "status: 402" | grep -qx 1 && grep -v '^#' src/app/api/companies/[id]/analyze/route.ts | grep -c "status: 429" | grep -qx 1 && grep -v '^#' src/app/api/companies/[id]/analyze/route.ts | grep -c "status: 503" | grep -qx 0 && npx tsc --noEmit</automated>
  </verify>
  <done>route.ts maps the three NEW classes to 400/402/429; no 503 remains; gate_failed 422 + the 502 family untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: FAL-05 audit wiring verification + full-suite/typecheck/build gate</name>
  <files>src/app/api/companies/[id]/analyze/route.ts, src/lib/agents/runAgent.ts, src/lib/agents/analyzeCompany.ts</files>
  <read_first>
    src/lib/agents/runAgent.ts (plan 20-02 output — `modelUsed: modelIdOf(models[i])` in the loop return)
    src/lib/agents/analyzeCompany.ts (plan 20-03 output — `modelUsed: run.modelUsed, modelChain` in the ok:true return)
    src/app/api/companies/[id]/analyze/route.ts (Task 1 output — createRun args l.128-140)
  </read_first>
  <action>
    Integration gate — NO production code changes (FAL-05 is verification-only; research caveat 6 confirms Phase 19 already wired the plumbing). Run in order and record each result in the SUMMARY:

    1. FAL-05 wiring greps (the provider-accurate audit chain, raw ids verbatim end-to-end):
       - `grep -n "modelUsed: modelIdOf(models\[i\])" src/lib/agents/runAgent.ts` → the loop return attaches the ACTUAL served model's raw id (verbatim — modelIdOf returns the string as-is, no transformation).
       - `grep -n "modelUsed: run.modelUsed" src/lib/agents/analyzeCompany.ts` → analyzeCompany passes the loop's modelUsed through, plus `modelChain` (the resolved chain snapshot).
       - `grep -n "modelUsed: result.modelUsed, modelChain: result.modelChain" src/app/api/companies/[id]/analyze/route.ts` → createRun receives both audit fields (the durable agent_run columns, D-14).
       - `npx tsx -e "import { getProviderForModelId } from './src/lib/models/catalog'; import catalogJson from './src/lib/models/catalog.json'; if (getProviderForModelId(catalogJson, 'anthropic/claude-sonnet-latest') !== 'openrouter') throw new Error('alias resolution'); if (getProviderForModelId(catalogJson, 'claude-sonnet-4-6') !== 'anthropic') throw new Error('anthropic resolution'); console.log('OK audit identity resolves');"` → the provider identity the audit keys off resolves for both a concrete slug and a ~latest alias (alias ids pass through verbatim, D-04).
    2. Full test suite: `npx vitest run` — all files green, including the FAL-05 verbatim cases added in plan 20-02 (`modelUsed === 'anthropic/claude-sonnet-latest'`) and the plan 20-03 reason cases. Any failure in a file this phase touched is a bug in this phase; any failure elsewhere is pre-existing — record it in the SUMMARY and report, do not silently fix unrelated code.
    3. Typecheck: `npx tsc --noEmit` → 0 errors.
    4. Build: `npm run build` — next build completes (requires the local `.env.local` which exists). If build fails on env or unrelated pre-existing grounds, record the exact error and verify the compile-only path via `npx tsc --noEmit` + the vitest suite as the acceptable substitute, noting the reason.
    5. Deferred-boundary check: `grep -rn "mid-stream\|stream_aborted" src/ --include="*.ts" | grep -v "\.test\.ts"` → only the comment-only documentation sites (modelConfig.ts 'output' branch + runAgent.ts loop note) — no detection/reclassification path was added (D-20-05/06), and no `stream_aborted` reason code exists anywhere (deferred idea excluded).
  </action>
  <acceptance_criteria>
    - Greps prove the modelUsed/modelChain chain: runAgent loop return → analyzeCompany ok:true → createRun args (all three match)
    - The tsx smoke prints 'OK audit identity resolves' (concrete + alias ids resolve to their providers; ids flow verbatim)
    - `npx vitest run` exits 0 (full suite; FAL-05 verbatim + reason cases included)
    - `npx tsc --noEmit` exits 0
    - `npm run build` completes (or the exact failure is recorded in the SUMMARY with the tsc+vitest substitute proven)
    - No `stream_aborted` reason and no mid-stream detection path in non-test source (D-20-05/06 boundaries hold)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "modelUsed: modelIdOf(models\[i\])" src/lib/agents/runAgent.ts | grep -qx 1 && grep -c "modelUsed: run.modelUsed" src/lib/agents/analyzeCompany.ts | grep -qx 1 && grep -c "modelChain: result.modelChain" src/app/api/companies/[id]/analyze/route.ts | grep -qx 1 && npx vitest run 2>&1 | tail -4 && echo "---tsc---" && npx tsc --noEmit && echo "TSC OK"</automated>
  </verify>
  <done>FAL-05 wiring proven end-to-end (loop → analyzeCompany → createRun, verbatim ids); full suite + tsc green; build green (or failure recorded); deferred boundaries intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AnalyzeResult → HTTP response | Server-side reason codes become client-visible statuses + messages |
| run return → agent_run columns | The audit record (model_used/model_chain) — durable truth (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-10 | Information Disclosure | status/reason surfacing | mitigate | Statuses are constants mapped from server-side reason codes (D-20-09); the not_configured message names only the missing KEY NAME (constant, never the value); billing/rate_limited messages are fixed server-side strings — no raw provider error text crosses in these cases (the pre-existing `String(err)` 502 catch-all at l.61 is unchanged per D-20-11) |
| T-20-11 | Spoofing | client influencing the status mapping | mitigate | No client input reaches the switch — the only client-supplied data (company id) is zod-validated at l.31-35 and the userId is the authed Clerk identity; the switch keys exclusively on the server-produced AnalyzeResult.reason |
| T-20-12 | Tampering | audit columns (model_used/model_chain) | mitigate | model_used/model_chain flow only from the run's actual return (`modelIdOf(models[i])` → `run.modelUsed` → `createRun`), never client-supplied; FAL-05 wiring greps + the tsx identity smoke lock the chain; raw ids verbatim (D-04) — no slug transformation anywhere |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — no package.json changes; `npm run build` is a compile gate, not an install |

## Package Legitimacy

No installs. The build gate consumes the existing dependency set; nothing new is fetched.
</threat_model>

<verification>
1. `grep -v '^#' src/app/api/companies/[id]/analyze/route.ts | grep -c "status: 503"` → 0 (the D-15 503 is gone)
2. Statuses present: 400 (not_configured), 402 (billing), 429 (rate_limited); 422 (gate_failed), 404 (company_not_found), 502 (db_error/default/catch-all) unchanged
3. FAL-05 wiring greps all match (runAgent → analyzeCompany → createRun); the tsx identity smoke prints OK
4. `npx vitest run` → full suite green; `npx tsc --noEmit` → 0 errors; `npm run build` → green (or recorded)
5. Deferred boundaries: no `stream_aborted`, no mid-stream detection path in non-test source (D-20-05/06)
</verification>

<success_criteria>
- `not_configured` → 400 naming the missing key; `billing` → 402 "provider credits exhausted"; `rate_limited` → 429 with the platform/upstream reason (D-20-09/10)
- Existing 502 propagation for all other classes untouched (D-20-11 minimal blast radius)
- FAL-05 proven: model_used/model_chain record the actual provider id served, raw ids verbatim incl. ~latest aliases, from the loop through createRun
- Full Vitest suite + typecheck + next build all green
</success_criteria>

<output>
Create `.planning/phases/20-cross-provider-run-path/20-04-SUMMARY.md` when done
</output>
