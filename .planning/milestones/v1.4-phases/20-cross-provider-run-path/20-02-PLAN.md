---
phase: 20-cross-provider-run-path
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - src/lib/agents/runAgent.ts
  - src/lib/agents/runAgent.test.ts
autonomous: true
requirements: [FAL-01, FAL-02, FAL-03, FAL-05]
must_haves:
  truths:
    - "A 429 on a cross-provider hop advances to the fallback — the fallback from the other provider serves (FAL-03/FAL-01)"
    - "A 402 billing error never advances, even cross-provider — single attempt, throws (FAL-02)"
    - "Same-provider 429 keeps v1.3 never-advance behavior (D-01/D-03 preserved verbatim)"
    - "modelUsed records the served id verbatim — OpenRouter slugs incl. ~latest aliases pass through unchanged (FAL-05)"
  artifacts:
    - path: "src/lib/agents/runAgent.ts"
      provides: "Hop-aware failover loop composing shouldAdvance with isFailoverEligible + the loop-side isOpenRouterPlatformRateLimit diagnostics helper (D-20-07/08)"
      contains: "shouldAdvance(cls"
    - path: "src/lib/agents/runAgent.test.ts"
      provides: "Hoisted catalog mock seam (getProviderForModelId) + cross-provider loop cases + verbatim-modelUsed case"
      contains: "vi.mock('@/lib/models/catalog'"
  key_links:
    - from: "src/lib/agents/runAgent.ts"
      to: "src/lib/agents/modelConfig.ts"
      via: "loop catch composes isFailoverEligible(cls) || cls === 'rate_limited' with shouldAdvance(cls, from, to)"
      pattern: "shouldAdvance\\(cls, from, to\\)"
    - from: "src/lib/agents/runAgent.ts"
      to: "src/lib/models/catalog.ts"
      via: "getProviderForModelId(catalogJson, modelIdOf(models[i])) — from/to provider identity for the hop"
      pattern: "getProviderForModelId\\(catalogJson"
---

<objective>
Wire the FAL-03 hop-aware advance into the failover loop of `src/lib/agents/runAgent.ts`: the catch block now computes from/to provider identity via `getProviderForModelId` (catalog-derived, never body-derived per D-20-07) and advances only when `isFailoverEligible(cls)` holds OR the class is the FAL-03 `rate_limited` carve-out AND `shouldAdvance(cls, from, to)` consents. The loop also gains the loop-side diagnostics helper `isOpenRouterPlatformRateLimit(err)` (D-20-08: separate narrow helper in the runAgent module, NOT inside pure `classifyModelError`; diagnostics-only — informs the reason string + telemetry, never the advance decision per D-20-07) and the D-20-06 accepted-mid-stream-429 loop comment. The test file gains the CRITICAL catalog mock seam — `runAgent.ts` newly imports the catalog, and the string-form stubs `'m1'` are NOT in the real snapshot — plus the cross-provider/billing/verbatim loop cases.

Purpose: This is the execution half of FAL-01/FAL-03 — the decision layer landed test-locked in plan 20-01; here it becomes the loop's actual behavior. The composition `(isFailoverEligible(cls) || cls === 'rate_limited') && shouldAdvance(cls, from, to)` is the minimal correct reading of FAL-03: `isFailoverEligible('rate_limited')` is false by locked D-03, so the rate_limited carve-out must be OR-ed in for the cross-provider case to ever advance; `shouldAdvance` then fail-closes same-provider 429 and passes every other eligible class through.
Output: A provider-aware failover loop + the diagnostics helper + a green runAgent test suite covering the 4-cell behavior at loop level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md Pitfall 2/3 (never burn fallbacks), Pitfall 3 (dual-source 429 body semantics), Pitfall 5 (4-cell matrix)
@.planning/research/ARCHITECTURE.md Pattern 3 (loop seam) — provider identity from the catalog
@.planning/phases/20-cross-provider-run-path/20-CONTEXT.md (D-20-05/06/07/08)
@.planning/phases/20-cross-provider-run-path/20-PATTERNS.md §runAgent.ts (l.92-149) and §runAgent.test.ts (l.352-397)
@.planning/phases/20-cross-provider-run-path/20-RESEARCH.md (l.21-30 — APICallError.data/responseBody/responseHeaders population; caveats 1-4)
src/lib/agents/runAgent.ts (current — 92 lines)
src/lib/agents/runAgent.test.ts (current — 301 lines)
src/lib/agents/modelConfig.ts (plan 20-01 output — shouldAdvance)
</context>

<tasks>

<task type="auto">
  <name>Task 1: runAgent.ts — hop-aware loop composition + isOpenRouterPlatformRateLimit diagnostics helper + D-20-06 loop comment</name>
  <files>src/lib/agents/runAgent.ts</files>
  <read_first>
    src/lib/agents/runAgent.ts (current — 92 lines; import l.1 + l.5-6, loop catch l.86-89, modelIdOf l.30-32, safety-net comment l.40-42)
    src/lib/agents/modelConfig.ts (plan 20-01 output — shouldAdvance signature)
    .planning/phases/20-cross-provider-run-path/20-PATTERNS.md §runAgent.ts (l.92-149 — imports, loop composition, helper body)
    .planning/phases/20-cross-provider-run-path/20-RESEARCH.md (l.21-30 — data-first / responseBody-fallback / optional-chained; l.43-48 caveats 1-4)
  </read_first>
  <action>
    Modify src/lib/agents/runAgent.ts — three change points, everything else untouched (loop, budgets, timeouts, audit return, `models?: LanguageModel[]` signature):

    A) Import block:
    - l.1: add `APICallError` to the existing `import { generateText, isStepCount, Output, type LanguageModel } from 'ai';`.
    - l.5: extend the './modelConfig' import to `import { classifyModelError, isFailoverEligible, shouldAdvance } from './modelConfig';` (shouldAdvance from plan 20-01).
    - Add `import { getProviderForModelId } from '@/lib/models/catalog';` and `import catalogJson from '@/lib/models/catalog.json';` (mirror modelConfig.ts l.9-12 Pattern 2: the catalog + its JSON are static, env-free imports — constraint 11 untouched, the catalog is NOT a provider SDK).

    B) Loop catch (l.86-89) — the ONLY decision change:
    - Replace the single-line throw guard with the hop-aware composition:
      - `const cls = classifyModelError(err);`
      - `const from = getProviderForModelId(catalogJson, modelIdOf(models[i]));`
      - `const to = i + 1 < models.length ? getProviderForModelId(catalogJson, modelIdOf(models[i + 1])) : null;`
      - `const eligible = isFailoverEligible(cls) || cls === 'rate_limited';`
      - `if (!(eligible && shouldAdvance(cls, from, to))) throw err;`
    - Why-comment above the block (FAL-03/D-20-07, house style, 4-8 lines): decision uses provider identity ONLY (getProviderForModelId on from/to model ids), never the response body. `isFailoverEligible` gates the D-03 set (404/5xx/connection) and short-circuits so billing/output/config/auth/input never reach shouldAdvance; `cls === 'rate_limited'` is the FAL-03 carve-out that reaches shouldAdvance — same-provider 429 keeps v1.3 never-advance (D-01/D-03), cross-provider advances. Note the `to === null` fail-closed case (last model or catalog drift → shouldAdvance('rate_limited', ...) returns false). D-20-05: mid-stream 429s classify 'output' and never reach this branch (accepted + documented, no detection path).
    - Keep `lastError = err;` and the post-loop `throw lastError;` (l.91) untouched.

    C) Diagnostics helper (NEW — module-scope named export in runAgent.ts, per D-20-08: loop-side, NOT inside pure classifyModelError; reads only the error object's fields, no provider SDK import needed — APICallError is now imported at l.1):
    - `export function isOpenRouterPlatformRateLimit(err: unknown): boolean`.
    - Guard: `if (!APICallError.isInstance(err)) return false;`.
    - Read `err.data` FIRST (the parsed envelope; OpenRouterErrorResponseSchema has .passthrough() on both levels so `error.metadata.error_type` / `provider_code` survive — verified @openrouter/ai-sdk-provider@3.0.0 dist/index.js:2385-2441 non-2xx handler), via optional-chained structural access: `(err.data as { error?: { metadata?: { error_type?: string; provider_code?: string } } } | undefined)?.error?.metadata`.
    - Upstream pass-through detection: `metadata?.error_type === 'rate_limit_exceeded' && metadata.provider_code` → return false (upstream provider rate-limit, e.g. Anthropic behind OpenRouter).
    - Platform-level: `metadata?.error_type === 'rate_limit_exceeded'` (no provider_code) → return true.
    - Header fallback: `const headers = err.responseHeaders ?? {};` then `Object.keys(headers).some((k) => k.toLowerCase().startsWith('x-ratelimit'))` → true (platform = X-RateLimit-* headers, verified extractResponseHeaders dist:685). Do NOT parse `err.responseBody` as JSON — `data` is the structured primary; responseBody is raw text and may be `""` on empty-body 429s or undefined on the mid-stream 200-with-error path (research caveat 1: both optional-chained).
    - Why-comment (D-20-07/08): DIAGNOSTICS-ONLY — informs the structured reason string + telemetry (platform-level vs upstream pass-through) and NEVER changes the advance decision (that is shouldAdvance's pure provider matrix). Exact structural typing is Claude's discretion but must stay defensive (optional-chained, no JSON.parse).

    D) D-20-06 loop comment: append one note to the existing safety-net comment (l.40-42) documenting the accepted mid-stream 429 — mid-stream OpenRouter rate limits (finish_reason: "error" after HTTP 200) classify as 'output' via the flat generateText contract and are never failover-eligible; accepted + documented here and at the classifier's 'output' branch, no detection path in Phase 20. Do not change any behavior.
  </action>
  <acceptance_criteria>
    - `src/lib/agents/runAgent.ts` imports `shouldAdvance`, `getProviderForModelId`, `catalogJson`, and `APICallError`
    - The loop catch contains `const eligible = isFailoverEligible(cls) || cls === 'rate_limited';` and `shouldAdvance(cls, from, to)`
    - `src/lib/agents/runAgent.ts` exports `isOpenRouterPlatformRateLimit` reading `err.data` with an `x-ratelimit` header fallback
    - The audit return (`modelUsed: modelIdOf(models[i])`) is untouched
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "cls === 'rate_limited'" src/lib/agents/runAgent.ts | grep -qx 1 && grep -c "shouldAdvance(cls, from, to)" src/lib/agents/runAgent.ts | grep -qx 1 && grep -c "export function isOpenRouterPlatformRateLimit" src/lib/agents/runAgent.ts | grep -qx 1 && grep -c "x-ratelimit" src/lib/agents/runAgent.ts | grep -qx 1 && npx tsc --noEmit</automated>
  </verify>
  <done>runAgent.ts composes shouldAdvance with isFailoverEligible (rate_limited carve-out), exports the diagnostics-only 429 helper, and typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: runAgent.test.ts — catalog mock seam + cross-provider/billing/verbatim loop cases</name>
  <files>src/lib/agents/runAgent.test.ts</files>
  <read_first>
    src/lib/agents/runAgent.test.ts (current — 301 lines; hoisted seam l.9-30, loop describe l.126-287, apiErr factory l.138-139, string stubs l.149)
    src/lib/agents/runAgent.ts (Task 1 output — new catalog imports + shouldAdvance composition)
    .planning/phases/20-cross-provider-run-path/20-PATTERNS.md §runAgent.test.ts (l.352-397 — the hoisted mock + new cases)
  </read_first>
  <action>
    Modify src/lib/agents/runAgent.test.ts — the CRITICAL seam addition first, then the new loop cases. Existing tests MUST survive unchanged (the default mock resolves every stub id to 'anthropic', so `'m1'`→`'m1'` stays same-provider: 429-never-advances l.157-164, 404/5xx/RetryError advance l.141-155/246-286 — v1.3 verbatim).

    A) Hoisted seam — add to the vi.hoisted mocks (l.9-14):
    - `getProviderForModelId: vi.fn((_catalog: unknown, id: string) => (id.includes('/') || id === 'm2' ? 'openrouter' : 'anthropic'))` with a Phase 20 why-comment: provider identity for the hop decision; default 'anthropic' for every stub id preserves all existing same-provider tests; slashed ids (real OpenRouter slugs) and 'm2' resolve 'openrouter' for the cross-provider cases.
    - Add the module mock after the existing seams (l.16-30): `vi.mock('@/lib/models/catalog', () => ({ getProviderForModelId: mocks.getProviderForModelId }));` — NOTE: runAgent.ts's separate `@/lib/models/catalog.json` JSON import is a DIFFERENT module specifier and is untouched by this mock (the real static JSON loads — harmless).
    - Do NOT remove or alter: the 'ai' mock (importOriginal spread keeps APICallError/RetryError real — the D-16 apiErr factory depends on it), the './modelFactory', '@/lib/env', 'firecrawl', or langfuse seams.

    B) New loop cases inside the 'runAgent failover loop' describe (after l.286, before the closing bracket) — follow the existing `apiErr` factory + string-stub style:
    - `it('429 advances ONLY on a cross-provider hop — mixed chain serves the fallback (FAL-03)')`: `mocks.generateText.mockRejectedValueOnce(apiErr(429)).mockResolvedValueOnce(resolvedRun);` then `runAgent({ company, liveSignals: [], models: ['m1', 'm2'] })` → assert 2 calls and `result` equals `{ ...resolvedRun, modelUsed: 'm2', usedFallback: true }`.
    - `it('reverse hop — openrouter primary to anthropic fallback also advances on 429 (FAL-03)')`: same shape with `models: ['m2', 'm1']` → 2 calls, `modelUsed: 'm1'`, `usedFallback: true` (locks the from/to computation both directions).
    - `it('402 billing never advances even cross-provider — throws on the primary (FAL-02)')`: `mocks.generateText.mockRejectedValueOnce(apiErr(402));` with `models: ['m1', 'm2']` → `rejects.toThrow()` and exactly 1 call.
    - `it('records the served OpenRouter slug verbatim incl. aliases (FAL-05)')`: `models: ['m1', 'anthropic/claude-sonnet-latest']` with `mocks.generateText.mockRejectedValueOnce(apiErr(429)).mockResolvedValueOnce(resolvedRun)` → 2 calls and `result.modelUsed === 'anthropic/claude-sonnet-latest'` (verbatim — modelIdOf returns the raw string, no transformation; the hoisted mock maps the slashed id to 'openrouter' so the hop advances).
  </action>
  <acceptance_criteria>
    - `src/lib/agents/runAgent.test.ts` contains `vi.mock('@/lib/models/catalog', () => ({ getProviderForModelId: mocks.getProviderForModelId }))`
    - The hoisted block contains `getProviderForModelId: vi.fn(` with the slashed-id/'m2' → 'openrouter' default
    - The four new loop cases exist (cross-provider 429 advance ×2 directions, 402 never advances, verbatim slug)
    - All pre-existing tests pass unchanged (the default 'anthropic' resolution preserves them)
    - `npx vitest run src/lib/agents/runAgent.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "vi.mock('@/lib/models/catalog'" src/lib/agents/runAgent.test.ts | grep -qx 1 && grep -c "anthropic/claude-sonnet-latest" src/lib/agents/runAgent.test.ts | grep -qx 1 && npx vitest run src/lib/agents/runAgent.test.ts 2>&1 | tail -3</automated>
  </verify>
  <done>runAgent.test.ts mocks the catalog seam; the 4-cell behavior is locked at loop level (both hop directions, billing never-advance, verbatim slug); all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider error objects → runAgent catch | Untrusted external data crosses the failover decision gate |
| from/to provider identity → shouldAdvance | The hop decision — derived from the catalog, never from the error body (D-20-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | Spoofing | hop decision from untrusted body content | mitigate | from/to derived via `getProviderForModelId(catalogJson, modelIdOf(model))` on model ids only; `isOpenRouterPlatformRateLimit` is diagnostics-only (D-20-07) — returns a boolean used for reason strings/telemetry, structurally unable to flip the advance decision (it is never read inside `shouldAdvance` or the eligibility composition) |
| T-20-05 | Information Disclosure | diagnostics helper reading err.data/responseHeaders | accept | The helper reads the error envelope fields (passthrough preserves `error.metadata.error_type`/`provider_code`) and returns only a boolean; no secrets are logged, no raw body is echoed anywhere in the decision path |
| T-20-06 | Denial of Service | hostile 429 storm burning fallbacks on cross-provider chains | mitigate | `LOOP_BUDGET_MS` 54s clamp + the D-10 chain cap (primary + 1) bound total attempts; same-provider 429 keeps v1.3 never-advance; a null `to` (last model / catalog drift) fail-closes a 429 advance; billing is account-level and never advance-eligible |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — no package.json changes; `@openrouter/ai-sdk-provider@3.0.0` already installed (Phase 19) |

## Package Legitimacy

No installs. The loop imports only existing modules ('ai', './modelConfig', './modelFactory', the catalog + its committed JSON); no provider SDK import is added (constraint 11 intact — the factory stays the sole SDK surface).
</threat_model>

<verification>
1. `grep -c "cls === 'rate_limited'" src/lib/agents/runAgent.ts` → 1 (the FAL-03 carve-out is in the eligibility composition)
2. `grep -c "vi.mock('@/lib/models/catalog'" src/lib/agents/runAgent.test.ts` → 1 (the seam exists)
3. `npx vitest run src/lib/agents/runAgent.test.ts` → all pass, existing + 4 new cases
4. `npx tsc --noEmit` → 0 errors
</verification>

<success_criteria>
- The loop advances on 429 ONLY cross-provider (both hop directions locked by tests); same-provider never-advance preserved verbatim (FAL-03, D-01/D-03)
- A 402 throws on the primary — billing never burns a fallback (FAL-02)
- `isOpenRouterPlatformRateLimit` lives loop-side (D-20-08), is diagnostics-only (D-20-07), and reads `err.data` first with an X-RateLimit header fallback (research-verified population paths)
- `modelUsed` records the served id verbatim incl. slashed OpenRouter slugs (FAL-05)
- The catalog mock seam keeps every pre-existing loop test green (D-16 zero-live-call discipline intact)
</success_criteria>

<output>
Create `.planning/phases/20-cross-provider-run-path/20-02-SUMMARY.md` when done
</output>
