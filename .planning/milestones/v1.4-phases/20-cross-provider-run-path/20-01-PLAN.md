---
phase: 20-cross-provider-run-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agents/modelConfig.ts
  - src/lib/agents/modelConfig.test.ts
autonomous: true
requirements: [FAL-02, FAL-03]
must_haves:
  truths:
    - "A 402 error classifies as 'billing' — never failover-eligible, with a documented 'provider credits exhausted' reason (FAL-02)"
    - "502/503 stay 'server_error' and failover-eligible, documented as OpenRouter model-availability signals (FAL-02)"
    - "shouldAdvance implements the locked 4-cell matrix — rate_limited advances ONLY cross-provider; all other eligible classes advance regardless (FAL-03)"
    - "billing/output/config/auth/input never reach shouldAdvance (isFailoverEligible gates them out first)"
  artifacts:
    - path: "src/lib/agents/modelConfig.ts"
      provides: "Extended pure classifier: 'billing' class for 402, 502/503 model-availability comment, D-20-06 output-branch comment, hop-aware shouldAdvance predicate"
      contains: "shouldAdvance"
    - path: "src/lib/agents/modelConfig.test.ts"
      provides: "FAL-02 billing/502-503 matrix + FAL-03 4-cell shouldAdvance matrix (D-16 zero-live-call, real constructed SDK error instances)"
      contains: "describe('shouldAdvance"
  key_links:
    - from: "src/lib/agents/modelConfig.ts"
      to: "classifyModelError"
      via: "402 case returns 'billing'; >=500 branch comment documents 502/503 model-availability"
      pattern: "code === 402"
    - from: "src/lib/agents/modelConfig.ts"
      to: "runAgent.ts loop (plan 20-02)"
      via: "shouldAdvance(cls, from, to) export — the hop-aware predicate the loop composes with isFailoverEligible"
      pattern: "export function shouldAdvance"
---

<objective>
Extend the pure classifier (`src/lib/agents/modelConfig.ts`) with the FAL-02 `billing` class (402 → `'billing'`, never failover-eligible, reason "provider credits exhausted"), the comment-only 502/503 model-availability documentation and the D-20-06 mid-stream-429 note, and add the FAL-03 hop-aware `shouldAdvance(cls, from, to)` predicate that implements the locked 4-cell provider matrix. The matrix is locked here in the pure module with zero mocks (D-16), BEFORE the loop consumes it in plan 20-02 — this plan is the decision layer, plan 20-02 is the wiring.

Purpose: `isFailoverEligible` stays the locked D-03 set (404/5xx/connection); `rate_limited` is NOT in it, so FAL-03's cross-provider 429 advance is a deliberate, tested extension layered on top via `shouldAdvance` — never a relaxation of D-01/D-03. The classifier stays dependency-free (D-16): no new imports beyond the `ModelProviderId` type from the catalog, no provider SDKs, no body parsing (D-20-07).
Output: The extended `ModelErrorClass` union, the billing classifier case, the two comment-only documentation sites, and the test-locked 4-cell matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md Pitfall 3 (402/502/503 semantics), Pitfall 5 (429 blanket-fix regression; the 4-cell matrix)
@.planning/research/SUMMARY.md (Conflicts section — FAL-02/FAL-03 decisions trace here)
@.planning/phases/20-cross-provider-run-path/20-CONTEXT.md (D-20-05/06/07/10)
@.planning/phases/20-cross-provider-run-path/20-PATTERNS.md §modelConfig.ts (l.28-88) and §modelConfig.test.ts (l.299-347)
@.planning/phases/20-cross-provider-run-path/20-RESEARCH.md (l.32-34 — classifier taxonomy confirmed, no shape change, no new imports)
src/lib/agents/modelConfig.ts (current — 85 lines)
src/lib/agents/modelConfig.test.ts (current — 161 lines)
</context>

<tasks>

<task type="auto">
  <name>Task 1: modelConfig.ts — 'billing' class + 502/503 + D-20-06 comments + shouldAdvance predicate</name>
  <files>src/lib/agents/modelConfig.ts</files>
  <read_first>
    src/lib/agents/modelConfig.ts (current — 85 lines; union l.23-31, classifier switch l.40-51, isFailoverEligible l.68-70)
    .planning/phases/20-cross-provider-run-path/20-PATTERNS.md §modelConfig.ts (l.28-88 — the exact change map)
    .planning/phases/20-cross-provider-run-path/20-RESEARCH.md l.32-34 (taxonomy confirmed: 402/502/503 arrive as APICallError with real statusCode; "no shape change, no new imports")
  </read_first>
  <action>
    Modify src/lib/agents/modelConfig.ts — four additive changes, all in the pure module (D-16: imports stay 'ai' + '@/lib/models/catalog'; no provider SDKs, no body parsing, no db/env/runAgent):

    A) Import (l.9): add `type ModelProviderId` to the existing `import { ANTHROPIC_ALLOWLIST, FAST_MODEL_ID, getUnionServableIds } from '@/lib/models/catalog';` line. Nothing else moves.

    B) `ModelErrorClass` union (l.23-31): add `| 'billing';` as the last member, with the why-comment: FAL-02 — 402 is account-level credits exhausted on OpenRouter, NEVER failover-eligible (advancing to any model would fail identically — nobody later "fixes" it into the advance set, PITFALLS 3). Keep the existing D-04/D-03 union comment (l.20-22) untouched.

    C) `classifyModelError` switch (l.40-51):
    - Add after the 404 line (l.46): `if (code === 402) return 'billing';` with the FAL-02 why-comment (mirror the D-01 comment style on the 429 line: "account-level credits exhausted — advancing would fail identically; never failover-eligible").
    - Extend the `>= 500` branch comment (l.48, currently "D-02: advances") with the FAL-02 model-availability note: 502/503 on OpenRouter are model-availability signals — the purest failover case — stay `server_error`/eligible, comment-only, never reclassified (D-20-06 sibling). Exact wording is Claude's discretion; 1-4 lines directly above the line, house why-comment style.
    - Add the D-20-06 comment block directly above the `InvalidResponseDataError || NoObjectGeneratedError` output branch (l.53): OpenRouter mid-stream 429s (`finish_reason: "error"` after HTTP 200) surface here as `'output'` via the flat generateText contract — safe (fail loud, never burn a fallback wrongly), accepted + documented, NOT reclassified in Phase 20 (would require digging the v7 step/stream result shape beyond budget); Phase 22's error matrix records the expected behavior. No code change on that line.
    - Do NOT change: RetryError-unwrap-first (l.37-39), the connection/undefined-statusCode case (l.42-45), 429 (l.47), 401/403 (l.49), the input default (l.50), the NoSuchModelError/TimeoutError branches, or the unknown → 'input' fallback (l.61).

    D) New `shouldAdvance` export, placed directly after `isFailoverEligible` (l.68-70) in this same pure module (recommended placement per PATTERNS — keeps the 4-cell matrix D-16-testable with zero mocks):
    - Signature: `export function shouldAdvance(cls: ModelErrorClass, from: ModelProviderId | null, to: ModelProviderId | null): boolean`.
    - Body: `if (cls !== 'rate_limited') return true;` then `return from !== null && to !== null && from !== to;`.
    - Why-comment above it (FAL-03, D-20-07): the 4-cell matrix is provider-keyed — rate_limited advances ONLY on a cross-provider hop; all other eligible classes (404/5xx/connection) advance regardless. Decision uses ONLY provider identity (from/to), never the response body. v1.3 same-provider never-advance (D-01/D-03) is preserved verbatim; hop-aware advance is a DELIBERATE TESTED EXTENSION, not a relaxation. from/to are nullable (getProviderForModelId returns null on catalog drift / last-model sentinel) — fail-closed: a null provider identity never advances a 429.
  </action>
  <acceptance_criteria>
    - `src/lib/agents/modelConfig.ts` contains `| 'billing';` in the ModelErrorClass union
    - `src/lib/agents/modelConfig.ts` contains `if (code === 402) return 'billing';`
    - The `>= 500` branch comment mentions 502/503 model-availability (grep 'model-availability' or '502/503')
    - The output branch has the D-20-06 comment mentioning mid-stream 429 / 'output'
    - `src/lib/agents/modelConfig.ts` contains `export function shouldAdvance(` with the `from !== null && to !== null && from !== to` fail-closed 429 line
    - Import line includes `type ModelProviderId`; no new module imports added (grep the import block — still only 'ai' + '@/lib/models/catalog' + the JSON)
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' src/lib/agents/modelConfig.ts | grep -c "return 'billing'" | grep -qx 1 && grep -c "export function shouldAdvance" src/lib/agents/modelConfig.ts | grep -qx 1 && grep -c "model-availability" src/lib/agents/modelConfig.ts | grep -qx 1 && grep -v '^#' src/lib/agents/modelConfig.ts | grep -c "from 'ai'\|from '@/lib/models/catalog'" | grep -qx 2 && npx tsc --noEmit</automated>
  </verify>
  <done>modelConfig.ts exports the billing class + shouldAdvance; the two comment-only sites are in place; imports unchanged (pure module intact); tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: modelConfig.test.ts — FAL-02 billing/502-503 matrix + FAL-03 4-cell shouldAdvance matrix</name>
  <files>src/lib/agents/modelConfig.test.ts</files>
  <read_first>
    src/lib/agents/modelConfig.test.ts (current — 161 lines; D-16 header l.13-17, apiErr factory l.19-25, RetryError-unwrap tests l.34-54)
    src/lib/agents/modelConfig.ts (Task 1 output — billing + shouldAdvance)
    .planning/phases/20-cross-provider-run-path/20-PATTERNS.md §modelConfig.test.ts (l.299-347 — the exact matrix shapes)
    .planning/STATE.md Blockers/Concerns (same-provider 429 invariants D-01/D-03 must survive — this matrix is the lock)
  </read_first>
  <action>
    Extend src/lib/agents/modelConfig.test.ts with two describe blocks. Follow the D-16 zero-live-call conventions already in the file: real constructed SDK error instances via the existing `apiErr` factory (l.19-25), zero mocks, no provider SDK imports. Add `shouldAdvance` to the import from './modelConfig' (l.10).

    BLOCK 1 — `classifyModelError` billing/502-503 matrix (FAL-02), mirroring the RetryError-unwrap style at l.34-54:
    - `it('classifies 402 as billing — NEVER failover-eligible (FAL-02)')`: `classifyModelError(apiErr(402))` → `'billing'` and `isFailoverEligible('billing')` → false.
    - `it('keeps 502/503 as server_error — failover-eligible model-availability signals (FAL-02)')`: loop over [502, 503], assert `classifyModelError(apiErr(code))` → `'server_error'` and `isFailoverEligible(...)` → true.
    - `it('unwraps a RetryError-wrapped 402 to billing (Pitfall 3 unwrap-first)')`: `new RetryError({ message: 'retries exhausted', reason: 'maxRetriesExceeded', errors: [apiErr(402)] })` → `'billing'`, `isFailoverEligible('billing')` → false.

    BLOCK 2 — `describe('shouldAdvance — FAL-03 4-cell matrix (provider-keyed, D-20-07)')`:
    - Cell test (the 4-cell lock): `shouldAdvance('rate_limited', 'anthropic', 'anthropic')` → false (v1.3 verbatim), `shouldAdvance('rate_limited', 'openrouter', 'openrouter')` → false (v1.3 verbatim), `shouldAdvance('rate_limited', 'anthropic', 'openrouter')` → true (FAL-03), `shouldAdvance('rate_limited', 'openrouter', 'anthropic')` → true (FAL-03).
    - Non-429 eligible classes advance regardless of provider (v1.3 preserved, not a relaxation): loop over `['model_not_found', 'server_error', 'connection'] as const`, assert true for both `('anthropic','anthropic')` and `('openrouter','anthropic')`.
    - Never-reach set: loop over `['billing', 'input', 'output', 'config', 'auth'] as const` asserting `isFailoverEligible(cls)` → false (they are gated out before shouldAdvance ever sees them — the loop composition in plan 20-02 short-circuits).
    - Fail-closed null identity (documents the planner decision): `shouldAdvance('rate_limited', 'anthropic', null)` → false and `shouldAdvance('rate_limited', null, 'openrouter')` → false (catalog drift / last-model sentinel never advances a 429); `shouldAdvance('server_error', null, null)` → true (non-429 eligible unaffected by unknown identity).
  </action>
  <acceptance_criteria>
    - `src/lib/agents/modelConfig.test.ts` imports `shouldAdvance` and contains a `describe('shouldAdvance` block
    - A test asserts `classifyModelError(apiErr(402))` === `'billing'` and `isFailoverEligible('billing')` === false
    - A test asserts both 502 and 503 stay `'server_error'` with `isFailoverEligible` true
    - A RetryError-wrapped 402 unwraps to billing
    - All four matrix cells asserted explicitly (same-provider false ×2, cross-provider true ×2)
    - The fail-closed null cases are asserted
    - `npx vitest run src/lib/agents/modelConfig.test.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c "describe('shouldAdvance" src/lib/agents/modelConfig.test.ts | grep -qx 1 && grep -v '^#' src/lib/agents/modelConfig.test.ts | grep -c "shouldAdvance('rate_limited'" | grep -qx 6 && npx vitest run src/lib/agents/modelConfig.test.ts 2>&1 | tail -3</automated>
  </verify>
  <done>All modelConfig.test.ts tests pass: billing/502-503 matrix, RetryError unwrap, the 4-cell matrix, the never-reach set, and the fail-closed null cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider error objects → classifyModelError | Untrusted external data (statusCode + error classes) crosses into the pure classifier; no I/O, no side effects |
| ModelErrorClass → shouldAdvance | The hop decision consumes only catalog-derived provider identity, never error-body content (D-20-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Tampering | error-class spoofing (provider emits misleading statusCodes) | mitigate | Classifier keys on `APICallError.isInstance` + numeric `statusCode` only, RetryError-unwrap-first invariant intact; no response-body parsing anywhere in the decision path (D-20-07); 402 `billing` is account-level and structurally excluded from the advance set (`isFailoverEligible('billing')` false, locked by tests) |
| T-20-02 | Spoofing | provider-identity manipulation of the hop decision | mitigate | `shouldAdvance` consumes only `ModelProviderId` values (null-tolerant) — provider identity is derived from the catalog via `getProviderForModelId` in the loop (plan 20-02), never from the error body; the 4-cell matrix + fail-closed null cases lock the mapping in tests |
| T-20-03 | Information Disclosure | reason text at the classifier layer | accept | The classifier emits only the `ModelErrorClass` enum — no message text crosses this layer; reason strings land in plan 20-03 (server-side constants only) |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No installs in this plan — `@openrouter/ai-sdk-provider@3.0.0` + `OPENROUTER_API_KEY` already shipped in Phase 19; no package.json changes (research caveat 5) |

## Package Legitimacy

No installs in this plan. The classifier consumes only existing imports ('ai' + '@/lib/models/catalog' + the committed catalog.json snapshot).
</threat_model>

<verification>
1. `grep -v '^#' src/lib/agents/modelConfig.ts | grep -c "return 'billing'"` → 1 (the classifier case exists)
2. `grep -c "export function shouldAdvance" src/lib/agents/modelConfig.ts` → 1
3. `npx vitest run src/lib/agents/modelConfig.test.ts` → all pass (billing/502-503 matrix + 4-cell matrix + never-reach set + fail-closed nulls)
4. `npx tsc --noEmit` → 0 errors (imports unchanged — pure module intact)
</verification>

<success_criteria>
- `classifyModelError` maps 402 → `'billing'` (never failover-eligible) and documents 502/503 as failover-eligible model-availability signals (FAL-02)
- The D-20-06 comment-only note sits on the `'output'` branch documenting the accepted mid-stream 429 (no detection path added)
- `shouldAdvance` implements the locked 4-cell matrix with fail-closed null handling (FAL-03); same-provider never-advance (D-01/D-03) preserved verbatim
- The classifier remains dependency-free (D-16): imports still only 'ai' + the catalog modules; no provider SDKs, no body parsing
</success_criteria>

<output>
Create `.planning/phases/20-cross-provider-run-path/20-01-SUMMARY.md` when done
</output>
